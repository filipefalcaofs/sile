---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: executing
stopped_at: Completed Phase 12 (Auditoria e Compliance — EP12) — a trilha RN-002 (registrada desde a Fase 1) virou consultável/exportável (HU-098/100/101, CSV streaming + meta-auditoria), com explicabilidade passo a passo das decisões projetada do decision_trace SEM recomputo (HU-099, spy do motor = 0 / RN-005, legado "não registrado"), painel LGPD minimizado (HU-102, personal_data nos call sites reais, decisões fora do pruning) e detecção de abuso com 5 detectores determinísticos + encaminhamento à malha fina que NUNCA pune (HU-149, toggle features.deteccao_abuso OFF default, idempotente, painel humano com efetividade + confirmar/descartar auditado). 12 planos em 6 waves, guardião-entrega APROVADO; composer test 1315/1315 SQLite + 29/29 @group postgis; 85 parâmetros / 27 permissões. Correção transversal anti-fachada: a caixa do setor (Fase 10, HU-080/081) renderizava página inexistente → tela criada (lista + assumir + distribuir) + backend exposto (analistas/podeDistribuir, TDD). Smoke humano dispensado pelo usuário (sessão autônoma). Próxima acionável: Fase 13 (Integrações), bloqueada externamente (Regin/SIGIS/SEFAZ/homologação — SEDUR); Fase 15 (Relatórios) autonomamente executável. [Antes — Fase 11] Completed Phase 11 (Pendências e Comunicação) — comunicação multicanal PLENA: pendência (in-app via canal database nativo + e-mail reais) que reabre a análise e notifica o analista (listeners AUTO-DESCOBERTOS NotificarPendencia/NotificarRespostaPendencia → NotificationDispatcher → ledger communications HU-096), central de notificações (sininho/badge) + histórico unificado por processo, scheduler de vencimento (HU-093)/escalonamento SLA (HU-147)/expiração de pendência idempotentes (só notificam, sem dupla contagem), e WhatsApp (HU-095) atrás de toggle off + contrato UnavailableWhatsAppGateway: OFF→desativado, ON+indisponível→bloqueado auditado, NUNCA "enviado" (provado pela PIPELINE REAL em WhatsAppPipelineIntegrationTest — fecha o gap do caminho ON pré-Fase 13). Fechamento (11-10): ComunicacaoDevSeeder produz comunicações/notificações REAIS pelo fluxo de verdade (processo dedicado → em_analise pelo caminho legítimo: protocolo + máquina de estados + DistribuicaoService::assumir → PendenciaService::abrir/responder), territorial-agnóstico (roda em SQLite, ≠ AnaliseDevSeeder) e idempotente; DatabaseSeederTest ajustado (cidadão 6→7). guardião de entrega: APROVADO (evidência fresca independente). Verificação integral FRESCA: pint --test/typecheck/build verdes; `composer test` = 1205/1205 SQLite + 29/29 @group postgis em 2 processos (POSTGIS_TESTS_REQUIRED=true). Parâmetros 78 / permissões 24. ZERO dependência nova. BLOQUEADO/pendente (sem fachada, registrado): WhatsApp provedor real (HU-095) → Fase 13 (troca só o binding); convite/resposta Regin (HU-091 RN-004) → Fase 13; "gestor do setor" do escalonamento (HU-147) e roteamento ao setor → SEDUR (default role gestor); rito de não-resposta da pendência (indeferir por prazo) → SEDUR (hoje expira+notifica+mantém estado); templates oficiais → SEDUR (pt-BR default). ÚNICO gate restante: smoke navegável humano (roteiro em 11-10-SUMMARY). [Antes — Fase 10] Completed Phase 10 (Análise Técnica SEDUR) — decisão técnica HUMANA real e auditável de ponta a ponta: encaminhar→fila/SLA (HU-144)→caixa/distribuir/assumir (HU-080/081/138)→ficha pré-analisada pelo motor (HU-140, reusa SolicitacaoViabilityResolver)→divergências analista×motor (HU-140/145)→AnaliseTecnicaDecisionService::decide (HU-085-089: ViabilityDecision flow 'analise_tecnica', decided_by analista ≠ null + número TVL só no deferimento)→TVL PDF interno (HU-132, dompdf, URL assinada, NÃO ao cidadão)→pendência (HU-083/084 ciclo interno, em_analise↔em_pendencia)→malha fina (HU-136, flag+tabela ortogonal ao status, atinge deferido)→precedentes (HU-142, @group postgis). 18/18 planos; fechamento (10-18): SectorSeeder/StandardTextSeeder (catálogos sempre + analista@sile.dev/gestor@sile.dev dev/teste) + AnaliseDevSeeder (driver-aware: estágios reais só em pgsql, no-op honesto em SQLite, igual ExpressoDevSeeder) + comando analise:decidir (evidência: analise:decidir 7 --finalizar → DEFERIDA + TVL-2026-000003) + golden(#[DataProvider])/smoke(fluxo humano real) + AnalisePrecedentesPostgisTest (ST_Intersects via PrecedentService). guardião de entrega: APROVADO (após corrigir a redação de verificação — ver abaixo). Verificação integral FRESCA: pint limpo, `composer test` = 1132/1132 SQLite + 29/29 @group postgis em DOIS processos isolados (POSTGIS_TESTS_REQUIRED=true), tsc/build verdes. Parâmetros 67 / permissões 24. ZERO dependência nova além do dompdf (10-13). NOTA DE VERIFICAÇÃO (correção do guardião): o comando ÚNICO `php artisan test` mistura SQLite+postgis no mesmo processo e a flag global RefreshDatabaseState::$migrated faz o migrate do banco pgsql_testing ser pulado — então a verificação correta é em 2 passos (agora encapsulados no `composer test`, fix 0cb1c8b; o CI já roda os 2 steps). BLOQUEADO/pendente (sem fachada): ROTEAMENTO automático ao setor (SEDUR; no dev caixa de triagem, em produção o gestor atribui manualmente), transmissão Regin/SEFAZ (HU-104/110) → Fase 13 (auditam 'bloqueado'), HU-083/084 pleno + multicanal → EP11, export HU-131 → Fase 15, zona oficial Quadro 10/SEDUR, HU-137 feriados (seam pronto), assinatura gov.br/ICP do TVL. ÚNICO gate restante: smoke navegável humano. [Antes — Fase 9] Fluxo Expresso CORE VALUE entregue: deferimento/indeferimento automático REAL e auditável. Gatilho protocolar→evento SolicitacaoProtocolada→listener AvaliarFluxoExpresso→DecidirFluxoExpressoJob→FluxoExpressoService::decide (autoritativo: reexecuta os motores FRESCO, Cache::lock+re-check idempotente). RN-009: nao_permitido→INDEFERE; permitido(_com_condicoes)→DEFERE (ViabilityDecision imutável + número TVL só no deferimento via TvlNumberGenerator + transição StateMachine + auditoria SÍNCRONA HU-078); pendente (sem zona)/semi-expresso/toggle off→em_analise SEM decisão e SEM evento (anti-fachada). 2º evento de domínio ResultadoEmitido (after-commit) com 3 listeners auto-descobertos: NotificarResultadoExpresso (HU-077, e-mail sem anexo TVL), ComunicarResultadoRegin (HU-104) e EnviarViabilidadeSefaz (HU-110, só deferida) — Regin/SEFAZ BLOQUEADOS honestos (contratos Unavailable* auditam pendência 'bloqueado'/'ignorado', nunca "enviado") → Fase 13. HU-134 (prazo BAP) DORMENTE: IndeferirSemBapService + expresso:indeferir-sem-bap + seam BusinessDeadlineCalculator, no-op até o Regin → Fase 13. Retaguarda (09-11): ResultadoExpressoController + ViabilityDecisionResource (lista+detalhe somente leitura sob consultar-solicitacoes; transmissão Regin/SEFAZ lida da trilha, pendente honesto) + link no menu. Fechamento (09-12): zona fictícia ZCN-1 SÓ dev/teste (gate de ambiente + driver-aware) torna um DEFERIMENTO NAVEGÁVEL sobre a LÓGICA REAL (ExpressoDevSeeder protocola+decide um deferimento com TVL e um em_analise honesto); comando expresso:decidir (evidência real); golden (#[DataProvider]) + smoke (cadeia automática real, $fakeExpressoDecisionJob=false) travam os três caminhos + anti-fachada. Verificação integral FRESCA: pint limpo, suíte 931/931 SQLite + 22/22 @group postgis, tsc/build verdes; evidência dev: expresso:decidir 8→DEFERIDA+TVL-2026-000001, 9→EM ANÁLISE. ZERO dependência nova; parâmetros 56 / permissões 19. BLOQUEADO→Fase 13: Regin/SEFAZ (HU-104/110) + HU-134 ativa; TVL PDF (HU-132)→Fase 10; zona oficial Quadro 10/SEDUR pendente (degradação honesta em_analise). ÚNICO gate restante: smoke navegável humano. [Antes — Fase 8] PROCESSO FORMAL + PRIMEIRO evento de domínio. Aggregate ViabilityRequest (imóvel embutido 1:1, polígono jsonb fonte + geometry derivada driver-aware só pgsql) + satélites (cnaes/documents/transitions) + cadastros parametrizáveis (viability_service_types, document_requirements + pivot cnae_document_requirement) + protocol_sequences. Fluxo real ponta a ponta: criar rascunho (ViabilityRequestPolicy reusa representação Fase 1; duplicidade RN-007 por CNPJ, alerta não bloqueia) → imóvel/área (TerritoryService+LocationValidationService Fase 4, alerta área×polígono) → atividades/CNAEs (até 99 parametrizável) → documentos (Storage disk parametrizado + sha256 + DocumentRequirementResolver) → simular (SimulacaoSolicitacaoService REUSA ConsultaViabilidadeService Fase 7, propaga veredito, não bloqueia) → protocolar (ProtocolNumberGenerator lockForUpdate número único; ViabilityRequestStateMachine rascunho→protocolada auditada; evento SolicitacaoProtocolada após commit + listener RegistrarTrilhaProtocolo — auditoria independe do evento) → consultar (autenticado + público por temporarySignedRoute + throttle, payload mínimo LGPD) → cancelar (estados canceláveis parametrizáveis). Canais operador: contingência HU-148 (mesmo motor/estado, origem auditada — caminho REAL hoje) e atendimento presencial HU-150 (AssistedAttendance reusa "em nome de"). UI wizard multi-etapas + Minhas solicitações. guardião de entrega: APROVADO (após corrigir bug RN-002 de auditoria duplicada — listeners auto-descobertos NÃO devem ser re-registrados via Event::listen). Suíte 848/848 SQLite+postgis (19 @group postgis), pint/typecheck/build verdes; evidência real: VIA-2026-000006, bloqueio documental honesto, pendente sem zona. BLOQUEADO → Fase 13: DAM HU-071/072 (SEFAZ; sem contrato especulativo) e origem Regin (contingência é o caminho real). HU-139 = address_complement texto livre. ÚNICO gate restante: smoke navegável humano. [Antes: Fases 7 (consulta prévia), 5 (LOUOS), 6 (risco), 4 (georref) concluídas.]
last_updated: "2026-06-16T18:30:00.000Z"
last_activity: 2026-06-16 -- 15-13 CONCLUÍDO (dashboard executivo HU-122 + tela Indicadores de Viabilidade HU-123–128 + grupo Relatórios na nav). Bloco KPI `relatorios` no DashboardController gated por `consultar-relatorios` (padrão Fase 2.4): volume (array_sum de porPeriodo)/taxa deferimento/indeferimento/tempo médio de análise/taxa de resposta expressa REAIS da janela corrente (config técnica `relatorios.dashboard.janela_dias`=30) + séries `serie_volume`/`por_risco` p/ gráficos — SEM chave `delta` (anti-fachada CA-03, sem série histórica), null sem permissão (gated), degradação honesta sem dados (taxa null/volume 0). dashboard.tsx ganhou a seção Relatórios (KpiCards + gráfico de volume linha + risco rosca via wrapper <Chart> client-only 15-12). Nova `relatorios/indicadores.tsx`: PageHeader + filtros (período/bairro/CNAE/categoria) + KPIs de taxa + gráficos porPeriodo/porRisco/porCnae + DataTable por bairro com rótulo honesto de degradação zona→bairro (HU-124) + <ExportMenu> (endpoint agregado: filtros server-driven + currentParams montado dos filtros, sem useServerTable). Nav: grupo "Relatórios" (Indicadores/Tempo/Produtividade/Quedas por consultar-relatorios + Feriados por manter-parametros) adicionado ao `command-search.tsx` (Cmd+K, COMMITADO) e ao `gestao-layout.tsx` (sidebar — refactor do usuário: grupos vêm por props, app-sidebar NÃO tocado), porém gestao-layout.tsx foi deixado SEM COMMIT por ser arquivo do dono da Fase 14 (PENDÊNCIA DE INTEGRAÇÃO). TDD estrito (RED b480325→GREEN a18692c); --filter=DashboardKpis 5/5 (89 asserções); tsc/build exit 0. Commits 0c807c8 (telas), 76aba17 (Cmd+K). Staging seletivo por caminho; NADA da Fase 14 do usuário commitado. [Antes] 15-11 CONCLUÍDO (retrofit de export HU-131/RN-009 nas 5 listagens das Fases 1–2: cnaes/parâmetros/usuários/perfis/acessos ganharam ?formato= csv/xlsx/pdf delegando ao ReportExporter via 5 ReportSources novos — SEM rota nova, cumprindo o critério de pronto transversal 8). RN-005: cada source reproduz a query filtrada do index lendo o BAG do ReportFilters (reconstrutível via fromArray — síncrono E assíncrono). RN-007: usuários cpf_masked por default; gate de PII viaja no bag e está provado NA FONTE, mas o controller NUNCA o seta (não há permissão de PII no projeto — PENDÊNCIA DPO), CPF completo nunca sai; parâmetro sensível sai [sensível] (RN-009); RN-008 auditado. Acessos usam as colunas REAIS da tela (evento/ip/canal/data-hora, não user_agent). --filter=RetrofitListagens 10/10 (37 asserções); --filter=Relatorios 114/114 (465). TDD estrito RED→GREEN por task; staging seletivo, nada da Fase 14 tocado. Commits 5604f9e/06c4982/139299c. [Antes] 15-10 CONCLUÍDO (retrofit dos 2 CSVs históricos no contrato único HU-131/RN-009): AuditoriaController::export (trilha HU-101) e ProcessoController CSV (consulta HU-082) agora delegam ao ReportExporter via AtividadesReportSource/AcessosReportSource/ProcessosReportSource; fputcsv eliminado dos dois controllers; colunas/base de arquivo/personalData/guarda de volume (agora ReportDefinition::maxRows honrado por CSV/XLSX/PDF) preservados; telas herdam XLSX/PDF pelo mesmo ?formato=; anti-regressão 12-04/10-14 verde (338/338 em Relatorios+Auditoria+Analise). Fonte dedicada ProcessosReportSource (≠ SolicitacoesReportSource) preserva as 9 colunas históricas. Coordenação concorrente (padrão da fase): outro agente executou parte do plano, design convergiu (guarda via maxRows), staging seletivo, nada da Fase 14 tocado. Commits 1530719/844570a/8343f1b. [Antes] 15-12 CONCLUÍDO (UI base dos relatórios, wave 5): wrapper de gráfico ECharts client-only SSR-safe (ui/chart/* — tree-shake echarts/core+echarts.use; tema pela classe dark do <html> via MutationObserver+setTheme sem recriar a instância) + <ExportMenu> (ui/data-table/export-menu.tsx — dropdown CSV/XLSX/PDF montando {url}?formato=+filtros atuais por âncora nativa) + useServerTable.currentParams (snapshot de query reusável). npx tsc --noEmit e npm run build VERDES; commits c432e94/9a69228 (staging seletivo, nada da Fase 14). [Antes] Phase 12 COMPLETE (Auditoria e Compliance; 12/12 planos em 6 waves; guardião APROVADO). `composer test` = 1315/1315 SQLite + 29/29 @group postgis; 85 parâmetros / 27 permissões; pint/typecheck/build verdes. Achado do guardião corrigido (Fase 10): tela ausente da caixa do setor criada (fachada de runtime removida; backend exposto com analistas/podeDistribuir, TDD). Smoke humano dispensado pelo usuário. Próxima: Fase 13 bloqueada externamente (SEDUR) / Fase 15 autonomamente executável. [Antes] Phase 11 COMPLETE (Pendências e Comunicação; 10/10 planos; guardião de entrega APROVADO). Fechamento 11-10: ComunicacaoDevSeeder (comunicações/notificações REAIS pelo fluxo de verdade, roda em SQLite, idempotente) + WhatsAppPipelineIntegrationTest (pipeline real: ON+indisponível → bloqueado, nunca enviado). Verificação: `composer test` = 1205/1205 SQLite + 29/29 @group postgis em 2 processos; pint --test/typecheck/build verdes. Parâmetros 78 / permissões 24. WhatsApp provedor real + convite Regin → Fase 13; gestor do setor/rito de não-resposta/templates → SEDUR. Smoke navegável humano pendente. [Antes] Phase 10 COMPLETE (guardião de entrega APROVADO; 18/18; decisão técnica humana real flow analise_tecnica + TVL). Verificação: `composer test` = 1132/1132 SQLite + 29/29 @group postgis em 2 processos isolados (fix 0cb1c8b — o comando único misturava os grupos e pulava o migrate do postgis); evidência analise:decidir 7 --finalizar → DEFERIDA + TVL. Roteamento ao setor pendente SEDUR (dev: caixa de triagem); Regin/SEFAZ → Fase 13; HU-083/084 pleno → EP11; export → Fase 15. Smoke navegável humano pendente. [Antes] Phase 9 COMPLETE (guardião de entrega APROVADO após corrigir bug de SSR na retaguarda: a ViabilityDecisionFactory gravava fundamentacao/per_cnae/rules_versions em shape divergente do FluxoExpressoService, mascarando um crash em resultados-expresso/show.tsx; fix 8eb82a4 alinha a factory ao contrato real + blinda Resource/frontend (array_values/Array.isArray) + 2 regressões. Suíte 955/955 + 22 postgis). [Antes] 09-12 fechamento: zona fictícia dev/teste + expresso:decidir + golden/smoke + verificação integral; evidência dev expresso:decidir 8→DEFERIDA+TVL-2026-000001, 9→EM ANÁLISE; smoke navegável humano pendente.
_prev_stopped_at_p8: Completed Phase 8 (Solicitação de Viabilidade) — processo formal de solicitação ponta a ponta (aggregate ViabilityRequest, máquina de estados, ProtocolNumberGenerator, primeiro evento SolicitacaoProtocolada, anexos Storage, consulta pública assinada, contingência HU-148, atendimento presencial HU-150). guardião APROVADO (após fix RN-002 de auditoria duplicada). Suíte 848/848. DAM HU-071/072 e Regin → Fase 13; HU-139 texto livre. [Antes] 09-11 concluído (retaguarda do resultado expresso): tela do console SEDUR para a decisão automática (HU-076/HU-078) sob reuso de consultar-solicitacoes (ZERO permissão nova, permanecem 19). ResultadoExpressoController (index lista só decididas via join + filtros resultado/busca/período + paginação; show 404 sem decisão) + 1º ViabilityDecisionResource (->resolve, payload honesto outcome/consolidado/TVL/per_cnae/rules_versions/fundamentação/motivo + resumo da solicitação) + páginas Inertia index/show TailAdmin somente leitura. Status de transmissão Regin/SEFAZ lido da trilha 'integracoes' (bloqueado⇒PENDENTE, nunca "enviado") + nota de canal/PDF (HU-132 Fase 10). ResultadoExpressoConsoleTest 6/6; suíte SQLite 918/918 (4674 asserções); pint/tsc/build verdes. Commit 0e22e8a. [Antes: 09-10 HU-134 DORMENTE]: seam BusinessDeadlineCalculator (horas-calendário; HU-137 troca por dias úteis sem tocar call sites) + IndeferirSemBapService (indefere aguardando_bap vencida: decisão consolidated_result 'sem_atuacao_bap'/reason 'indeferido sem atuação' + transição aguardando_bap→indeferida + auditoria SÍNCRONA + ResultadoEmitido → Regin comunica/SEFAZ ignora RN-003) + comando expresso:indeferir-sem-bap (hourly/withoutOverlapping/onOneServer, prazo expresso.bap.prazo_horas). NO-OP em produção (nada entra em aguardando_bap até o Regin/Fase 13); motor real testado com seed awaitingBap. IndeferirSemBapTest 3/3 + ExpressoIndeferirSemBapCommandTest 3/3; suíte SQLite 912/912 (4618 asserções). Commits ab58f6c/6b6f09b. [Antes: 09-07/08/09 efeitos do ResultadoEmitido (notificação/Regin/SEFAZ); 09-06 gatilho/job; 09-05 motor de decisão.]
_prev_stopped_at_p7: Completed Phase 7 (Consulta Prévia de Viabilidade) — ConsultaViabilidadeService orquestra Geocoder→TerritoryService→LouosEnquadramentoService→RiscoClassificationService e PROPAGA o veredito (não recomputa). 3 entradas (endereço/CNAE/inscrição bloqueada). Pública com throttle+toggle+auditoria; histórico autenticado. guardião APROVADO; suíte 657/657 + 15/15 postgis.
_prev_stopped_at_p5: Completed Phase 5 (Motor de Regras da LOUOS) — Quadros 7/10/11/11A como dados versionados (REUSANDO rule_versions da Fase 6, RuleDomain estendido com louos_*), LouosEnquadramentoService (enquadrar/consolidar/fundamentação/vagas HU-042/restrições ZEIS HU-043 + auditoria RN-002 com rules_version), mantenedores HU-015..018 (publicação 4-olhos) + UI console, sandbox HU-143 (simula rascunho sobre cenários reais sem afetar a vigente — RN-001 por transação revertida; 4-olhos), comando louos:enquadrar, golden cases (#[DataProvider]). guardião de entrega: APROVADO. Suíte 600/600 SQLite + 15/15 @group postgis; evidência real: sem zona→pendente (fundamentação não cita Q10), ZPAM→nao_permitido, ZPR-1→permitido. ESCOPO HONESTO: Quadro 7 real (40 faixas/24 CNAEs, Lei 9.148/2016); Quadro 10 DEGRADA para pendente sem zona (bloqueado SEDUR); Quadros 11/11A parciais (atributo viário pendente). [Antes: Fase 6 (risco) e Fase 4 (georref) concluídas.]
last_updated: "2026-06-14T07:15:00.000Z"
last_activity: 2026-06-14 -- Phase 5 COMPLETE (guardião APROVADO; motor LOUOS real; Quadro 10/zona pendente SEDUR)
_prev_stopped_at_p6: Completed Phase 6 (Classificação de Risco) — regras como dados versionados (rule_versions genérico + RuleVersionService publish 4-olhos; infra herdada pela Fase 5), risco municipal (Decreto 32.636/2020: 767 baixo_a/328 baixo_b/236 alto = 1.331) e sanitário (VISA: 261) como dimensões SEPARADAS, condicionante-pergunta que reclassifica (67; golden 1031-7/00), encaminhamento parametrizável (risco.mapa_encaminhamento — decreto NÃO tem "médio"; mapeia para baixo_b), gatilhos CNAE (risk_triggers, semi-expresso) + exceção ZEIS, mantenedores versionados + UI console, comando risco:classificar, golden cases. guardião de entrega: APROVADO. Suíte 519/519 SQLite + 15/15 @group postgis; evidência real do motor (0111-3/01→expresso, frigorífico→análise, sem-regra→análise, ZEIS→análise). [PHASE 4 também concluída antes: PostGIS, geocodificação Nominatim, mapa Leaflet; zona/lote bloqueados pendente SEDUR.]
last_updated: "2026-06-14T04:05:00.000Z"
last_activity: 2026-06-14 -- Phase 6 COMPLETE (guardião de entrega APROVADO; classificação de risco real)
_prev_stopped_at: Completed Phase 4 (Georreferenciamento e Território) — PostGIS (geo_layers/geo_features versionados com vigência, migrations driver-aware), geocodificação Nominatim atrás de contrato (throttle/retry parametrizados), TerritoryService/SpatialRepository (ST_Contains/ST_DWithin/ST_Intersects reais), LocationValidationService (sobreposição ST_Area/ST_Intersection com limiar parametrizado), mapa Leaflet (react-leaflet v5, client-side/SSR-safe) e página de consulta territorial no console SEDUR, comando geo:importar auditado + carga REAL do GeoSalvador (bairro 171, via 800, restrição ZEIS 234), CI real com PostGIS (.github/workflows/tests.yml + POSTGIS_TESTS_REQUIRED). gsd-verifier: passed 5/5. Suíte 448/448 SQLite + 15/15 @group postgis; evidência real: Nominatim (Elevador Lacerda) + consulta espacial (Farol da Barra → bairro "Barra").
last_updated: "2026-06-13T23:55:00.000Z"
last_activity: 2026-06-13 -- Phase 4 COMPLETE (verificação passed; bairro/via/restrição reais; zona/lote bloqueados pendente SEDUR)
progress:
  total_phases: 15
  completed_phases: 12
  total_plans: 138
  completed_plans: 130
  percent: 94
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-06-09)

**Core value:** Responder a viabilidade locacional de atividade econômica de forma automática, correta e auditável — fluxo expresso quando a lei permite, fundamentação legal em toda decisão.
**Current focus:** Phases 3, 3.1, 4, 5, 6, 7, 8, 9, 10, 11 e 12 concluídas — próxima acionável: Phase 13 (Integrações, EP13), porém BLOQUEADA externamente (contrato Regin/JUCEB, base SIGIS/CA 2000, endpoint/credenciais SEFAZ, ambiente de homologação — pendências SEDUR; regra anti-fachada: integração sem acesso fica explicitamente bloqueada, nunca adaptador falso). A Fase 15 (Relatórios e Indicadores) é a próxima autonomamente executável (sobre dados reais, sem dependência externa); a Fase 14 (IA) depende de credenciais de provider. [Histórico — Fase 12, concluída 2026-06-15] Phase 12 (Auditoria e Compliance, EP12), que torna a trilha de auditoria RN-002 (registrada por HasAuditoria/AuditService/access_logs desde a Fase 1, e acumulada por TODAS as fases) consultável, exportável e monitorada para LGPD. ATENÇÃO Fase 12: HU-099 (consultar regras aplicadas) tem visualização EXPLICÁVEL passo a passo das decisões automáticas (a ViabilityDecision já guarda per_cnae/rules_versions/fundamentacao das Fases 9/10 — é a fonte); HU-149 (detectar abuso/fraude por regras parametrizáveis) gera alertas + envio à malha fina (Fase 10), NUNCA punição automática; export pleno cruza com HU-131 (Fase 15). Bloqueios herdados: WhatsApp provedor + Regin → Fase 13; roteamento ao setor/zona Quadro 10/templates → SEDUR. Phase 3.2 (GOV.BR) implementada e desligada, aguardando credenciamento. Fases 8/9/10/11 com smoke navegável humano como único gate de UI pendente.

### Fase 11 (Pendências e Comunicação) — concluída 2026-06-15

- Comunicação multicanal PLENA sobre o que a Fase 10 deixou pronto (não reconstruiu o ciclo). In-app via canal `database` nativo (tabela notifications) + e-mail real; ledger DEDICADO `communications` (HU-096, fonte única por processo — EmailLog não tem viability_request_id). `NotificationDispatcher` resolve canais (notificacoes.mapa_canais ∩ toggles features.notificacao_*), cria communications (na_fila/desativado), congela canais + mapa {canal→communicationId}; `RegistrarEnvioComunicacao` (auto-descoberto) fecha mail/database (NUNCA whatsapp). Listeners auto-descobertos `NotificarPendencia`/`NotificarRespostaPendencia` no PendenciaSolicitada/PendenciaRespondida.
- WhatsApp (HU-095): `WhatsAppChannel` + `WhatsAppGateway`/`UnavailableWhatsAppGateway` + toggle `features.notificacao_whatsapp` OFF. OFF→desativado, ON+indisponível→bloqueado auditado, NUNCA "enviado" — dupla defesa (RegistrarEnvioComunicacao ignora whatsapp + guarda do Communication::markAsSent) provada pela PIPELINE REAL em WhatsAppPipelineIntegrationTest. Provedor real troca só o binding → Fase 13.
- Scheduler (reusa analysis_due_at/AnalysisSlaService::statusFor, fonte única; idempotente via consulta ao ledger; withoutOverlapping/onOneServer): notificacoes:alertar-vencimentos (HU-093), notificacoes:escalonar-sla (HU-147, amarelo→analista/vermelho→gestor role param), pendencias:expirar (HU-091 RN-005 → Expirada, SEM indeferir). Só notificam (sem dupla contagem HU-129).
- REFACTORS anti-regressão: PendenciaService::abrir NÃO chama mais notificarRequerente (listener notifica); NotificarResultadoExpresso passa pelo dispatcher (ganha in-app+histórico). Testes das Fases 9/10 migrados de email_logs→communications, verdes (Analise+Expresso 297/297).
- guardião APROVADO (evidência fresca independente). `composer test` = 1205/1205 SQLite + 29/29 @group postgis; pint/typecheck/build verdes. Parâmetros 78 / permissões 24 (sem nova; central por dono, histórico reusa consultar-solicitacoes). ZERO dependência nova.
- BLOQUEADO/pendente (registrado, sem fachada): WhatsApp provedor (HU-095) + convite/resposta Regin (HU-091 RN-004) → Fase 13; "gestor do setor"/roteamento ao setor + rito de não-resposta (indeferir por prazo) + templates oficiais → SEDUR; export pleno HU-131 → Fase 15.

## Current Position

Phase: 15 (Relatórios e Indicadores — EP15) — EM ANDAMENTO. **15-13 (dashboard executivo HU-122 + tela Indicadores de Viabilidade HU-123–128 + grupo Relatórios na nav, wave 6) CONCLUÍDO** — detalhes na seção dedicada "### Fase 15 — Plano 15-13" abaixo (bloco KPI `relatorios` gated/sem delta; telas com gráficos ECharts + ExportMenu; grupo Relatórios no Cmd+K commitado e no gestao-layout SEM COMMIT — pendência do dono da Fase 14). **15-11 (retrofit de export HU-131 nas listagens das Fases 1–2, wave 5) CONCLUÍDO:** as 5 listagens administrativas (cnaes/parâmetros/usuários/perfis/acessos das Fases 1–2) ganharam `?formato=` (csv/xlsx/pdf) delegando ao `ReportExporter` via 5 `ReportSource` novos (`CnaesReportSource`/`ParametrosReportSource`/`UsuariosReportSource`/`PerfisReportSource`/`AcessosUsuarioReportSource`), SEM rota nova (RN-009) — cumprindo o critério de pronto transversal 8 do ROADMAP. Cada source reproduz a query filtrada do `index` lendo os filtros do BAG do `ReportFilters` (reconstrutível via `fromArray` — RN-005 síncrono E assíncrono). RN-007: usuários exportam `cpf_masked` por default; o gate de PII viaja no bag e está provado em teste NA FONTE, mas o controller NUNCA o seta (não há permissão de PII no projeto — **pendência DPO**), então o CPF completo nunca sai. Parâmetro sensível sai como `[sensível]` (RN-009). Histórico de acessos usa as colunas REAIS da tela (evento/ip/canal/data-hora, não user_agent). `--filter=RetrofitListagens` 10/10 (37 asserções); `--filter=Relatorios` 114/114 (465 asserções); `vendor/bin/pint` (arquivos do plano) passed. TDD estrito RED→GREEN por task; staging seletivo por caminho, NADA da Fase 14 do usuário tocado; commits `07b3686`/`5604f9e` (T1), `35f643c`/`06c4982` (T2), `82de476`/`139299c` (T3). **15-10 (retrofit dos CSVs históricos no contrato único, wave 5) CONCLUÍDO:** os DOIS `fputcsv` que viviam em `AuditoriaController::export` (trilha de atividades/alterações/acessos — HU-101) e `ProcessoController::exportarCsv` (consulta de processos — HU-082) foram consolidados no contrato único (RN-009): hoje delegam ao `ReportExporter` via `AtividadesReportSource`/`AcessosReportSource`/`ProcessosReportSource`. Colunas (12 trilha / 6 acessos / 9 processos), base do nome de arquivo, marca `personalData`/HU-101 e a guarda de volume preservados — esta última GENERALIZADA como `ReportDefinition::maxRows` (default null), honrada por CSV (para o chunk), XLSX (para o cursor) e PDF (para o get), SEM afetar o `count()` que decide o limiar síncrono/assíncrono. `ProcessosReportSource` é fonte DEDICADA (≠ `SolicitacoesReportSource` de 15-03, que tem 10 colunas/evento/arquivo próprios) para preservar EXATAMENTE o CSV de processos sem quebrar o `SolicitacoesReportSourceTest`. Meta-auditoria idêntica (`exporta-trilha-csv`/`exporta-processos-csv`). As telas de auditoria e processos herdam XLSX/PDF pelo mesmo `?formato=` (consumível pelo `<ExportMenu>` de 15-12). Anti-regressão (Pitfall 7) provada: `tests/Feature/Relatorios/` + `tests/Feature/Auditoria/` + `tests/Feature/Analise/` = **338/338 (1572 asserções)**; guarda de volume `max_linhas` continua verde via `maxRows`; XLSX/PDF e RN-005 assíncrono provados em `RetrofitCsvAuditoriaTest`/`RetrofitCsvProcessosTest`. COORDENAÇÃO CONCORRENTE (padrão da fase): outro agente da Fase 15 executou parte do 15-10 em paralelo (commits `1530719` Task 1 + `844570a` Task 2/convergência); o design convergiu de forma independente (fonte dedicada + guarda via `maxRows`); o `RetrofitCsvProcessosTest` (cobertura dos formatos novos de processos, exigida pela Task 2 e ausente) foi acrescentado em `8343f1b`. Staging seletivo por caminho; NADA da Fase 14 do usuário tocado. **15-12 (UI base, wave 5) CONCLUÍDO:** wrapper de gráfico ECharts client-only SSR-safe (`resources/js/components/ui/chart/*`: `echarts-core` tree-shake `echarts/core`+`echarts.use` de Bar/Line/Pie+Grid/Tooltip/Legend/Title/Dataset+CanvasRenderer; `chart-impl` client-only com init/setOption/ResizeObserver/dispose e tema pela classe `dark` do `<html>` via MutationObserver+`setTheme` SEM recriar a instância — Pitfall 5; `chart.tsx` guarda SSR lazy+mounted+skeleton espelhando MapaSection, sem `from 'echarts'`) + `<ExportMenu>` (`ui/data-table/export-menu.tsx`: dropdown CSV/XLSX/PDF montando `{url}?formato=`+`currentParams` por âncora nativa = download real, não visita Inertia) + `useServerTable` agora expõe `currentParams` (extraído `buildParams` como fonte única do snapshot de `visit`). `npx tsc --noEmit` e `npm run build` VERDES (Dimensão 6 — chart client-only não quebra o SSR); commits c432e94/9a69228 (staging seletivo dos 5 arquivos; NADA da Fase 14 tocado). Base pronta p/ dashboard (15-13) e telas (15-14). Planos 15-01 (fundação), 15-02 (contrato único de exportação HU-131) e 15-06 (produtividade por analista HU-130 — agregado/anonimizado por default, nominal/escopo parametrizados, `ProdutividadeReportSource` SyncOnly; commits 0168c1e/8973c5d) de 15 concluídos (15-03/15-04+ em wave paralela; 15-04 entregou feriados HU-137 + duração em tempo útil HU-129 sem mexer no dueAt). **15-08 (driver XLSX, wave 2) CONCLUÍDO:** `XlsxExporter` (openspout v4.32.0) por streaming `builder()->cursor()` (RN-005, baixa memória) ligado ao `ReportExporter` (match + `DRIVERS_DISPONIVEIS` → `['csv','xlsx','pdf']`) e ao `GerarExportacaoJob` (este também ganhou o ramo `'xlsx'` — anti-fachada: sem isso o async estouraria no `handle()`); os três formatos CSV/XLSX/PDF habilitados. `stream()` síncrono = `streamDownload` de arquivo temporário (`openToFile` + `fpassthru`), NÃO `openToBrowser` (que chama header()/ob_end_clean() e conflita com a StreamedResponse, impedindo asserir o Content-Type). XLSX provado legível pelo `Reader` do openspout no síncrono E no assíncrono (Job real). `--filter=Relatorios` 42/42 (186 asserções); pint limpo nos meus arquivos; commits 817bd0b/6eeaa09/07d1c8a. 15-01 instalou `openspout/openspout ^4.0` (v4.32.0; NÃO v5, que exigiria php ~8.4/8.5 e quebraria o piso `^8.3`) e `echarts ^6.1`, criou a migration ADITIVA de índices de desempenho dos relatórios (RESEARCH Pitfall 1 — `protocoled_at`, `viability_decisions.decided_at/(flow,outcome)/decided_by_user_id`, `viability_request_transitions(viability_request_id, created_at)`; up/down validados em pgsql via `--path`), e registrou 5 parâmetros HU-014 do grupo `relatorios` (catálogo 85 → 90) + 2 permissões (`consultar-relatorios`, `relatorios.produtividade.nominal` para gestor/admin; 27 → 29). Testes de seeder verdes (`ParameterSeederTest` 19/19, `RolesAndPermissionsSeederTest` 12/12); pint limpo. Commits: e1802c5 (deps+migration), 74a8d47 (params+config+perms), 1265c1a (testes). DECISÕES: meta_taxa NÃO espelhada no config (default null no config devolve null; ausente cai no fallback honesto "meta não definida"); SEM permissão `relatorios.exportar` (export herda a leitura da tela — RN-007); `ReportFilters` movido para 15-02. **15-02 (export base, wave 1) ENTREGUE o CONTRATO ÚNICO de exportação (HU-131):** `ReportFilters` (bag serializável, round-trip toArray/fromArray garantindo RN-005 no SÍNCRONO E no ASSÍNCRONO; `toProcessoFiltros` recorta as 17 chaves do ProcessoQueryService; acessores tipados from/to/setorId/analistaId/bairro/cnae/categoria), `ReportDefinition`/`ReportSource`/`SyncOnlyReportSource`/`ReportFormatExporter`, drivers `CsvExporter`+`PdfExporter` (Blade `relatorios.relatorio` com rodapé 'Total de registros: N' + data/hora + filtros), `ReportExporter::export(source,filtros,formato,user)` (streaming síncrono abaixo do limiar OU `GerarExportacaoJob` acima — guarda SyncOnly força síncrono; auditoria RN-008; xlsx recusado até 15-08), `GerarExportacaoJob` (tries/timeout/backoff de config; reconstrói pelo bag — RN-005 async; disco não-público; cria `ExportFile`; notifica; `failed()` audita sem fabricar link) e Notification STANDALONE `ExportacaoPronta` (database+mail). Model/migration `export_files` (`2026_06_15_000002`; up/down validados ISOLADOS em pgsql via `--path` — migrate global NÃO rodado por causa das migrations pendentes da Fase 14) + factory. Commits: 2bfdc6e (contrato+ReportFilters+ExportFile), d79b8dd (drivers+Blade), 544fec0 (exporter+job+notification). `php artisan test --compact --filter=Relatorios` 23/23 (112 asserções; 20 dos 4 arquivos novos + 3 pré-existentes que referenciam "relatorios"); pint limpo nos meus arquivos. DECISÕES 15-02: contrato (a) bag serializável primário (round-trip garante RN-005 no async e evita dump de PII da trilha — RN-007) + (b) guarda SyncOnly para sources com estado de construtor; Notification standalone (NÃO o NotificationDispatcher, process-bound); rota de download `gestao.relatorios.exportacoes.download` PENDENTE (nasce em 15-09); driver XLSX (openspout) entra em 15-08. ATENÇÃO trabalho paralelo (Fase 14, NÃO commitado pelo usuário): durante a sessão o usuário adicionou a permissão `manter-config-email` ao `RolesAndPermissionsSeeder` e mexeu em package.json (vitest)/sidebar/rotas/e-mail — TUDO preservado e fora dos meus commits (staging seletivo via plumbing; seeder isolado e restaurado). Enquanto o `manter-config-email` não for commitado com a contagem ajustada, o working tree terá 30 permissões vs as 29 commitadas (reconciliação a cargo do dono da Fase 14). Próximas: 15-02 (export base) e demais waves. Fase 13 (Integrações) segue BLOQUEADA externamente (SEDUR). [Fase 12 COMPLETE — guardião APROVADO; `composer test` = 1315/1315 SQLite + 29/29 @group postgis. Fase 11 COMPLETE.]

### Fase 15 — Plano 15-13 (dashboard executivo HU-122 + Indicadores de Viabilidade HU-123–128 + nav Relatórios) — concluído 2026-06-16

- **Dashboard executivo (HU-122):** bloco `kpis.relatorios` no `DashboardController::__invoke` gated por `consultar-relatorios` (estende o padrão Fase 2.4 "KPI gated por permissão, sem delta inventado"). Método privado `relatoriosKpis` (injeção dos 3 serviços no `__invoke`) monta a janela corrente (`ReportFilters` de `now()->subDays($janela)`→`now()`, `$janela` = constante técnica `relatorios.dashboard.janela_dias`=30 em `config/sile.php`, fora do catálogo HU-014). KPIs REAIS: `volume` (`array_sum` de `IndicadoresViabilidadeService::porPeriodo` — sem método novo no serviço), `taxa_deferimento`/`taxa_indeferimento` (taxa null sem decisões), `tempo_analise_minutos` (etapa `analise` de `TempoAnaliseService::tempoPorEtapa`, null sem amostras), `taxa_expressa` (`ExpressoQuedaService::taxaRespostaExpressa`, null sem elegíveis), `meta_expressa` (null = "não definida", RN-004), `janela_dias`, e séries `serie_volume`/`por_risco` p/ os gráficos. **SEM chave `delta`** em nenhum nível — anti-fachada CA-03 (sem série histórica persistida, nenhum "+X%" fabricado).
- **TDD estrito:** `tests/Feature/Relatorios/DashboardKpisTest.php` prova (a) números EXATOS de dataset factory (volume 3, deferimento 75%, indeferimento 25%, tempo análise 120 min úteis, taxa expressa 50%), (b) AUSÊNCIA de `delta` (`->missing('kpis.relatorios.delta')`), (c) gating (null sem `consultar-relatorios`, via `analista`), (d) degradação honesta sem dados (volume 0, taxas null). RED `b480325` → GREEN `a18692c`. `--filter=DashboardKpis` 5/5 (89 asserções; inclui o `GestaoDashboardKpisTest` da Fase 2.4, intacto). Asserção de taxa por comparação numérica `(float) $taxa === X` (a taxa float trafega como inteiro no JSON).
- **Telas (commit `0c807c8`):** `dashboard.tsx` ganhou a seção "Operação dos últimos N dias" (KpiCards do EP15 sem delta + gráfico de volume linha + distribuição por risco rosca via wrapper `<Chart>` client-only de 15-12) com link para os indicadores. Nova `resources/js/pages/gestao/relatorios/indicadores.tsx` (HU-123–128): PageHeader + filtros (período/bairro/CNAE/categoria — `setor`/`analista` precisam de listas de opções, ficam p/ 15-14) + KPIs de taxa + gráficos `porPeriodo` (linha)/`porRisco` (rosca, com legenda da `fonte` real/derivada/indefinida — HU-126)/`porCnae` (barras, código formatado 9999-9/99) + DataTable por bairro com Badge do rótulo honesto de degradação zona→bairro (HU-124) + `<ExportMenu url="/gestao/relatorios/indicadores" params={currentParams}>`. Endpoint é AGREGADO (não paginado): filtros server-driven por `router.get` + `currentParams` montado dos filtros (NÃO usa `useServerTable`, que é p/ listas paginadas) — export idêntico (RN-004).
- **Shape das props de `indicadores.tsx`** (= `RelatorioController::indicadores` de 15-09): `porPeriodo {dia,total}[]`; `porZona {degradacao,rotulo,itens:{bairro,total}[]}`; `porCnae {cnae,total}[]`; `porRisco {nivel,fonte,total}[]`; `taxaDeferimento {deferidas,total,taxa|null}`; `taxaIndeferimento {indeferidas,total,taxa|null}`; `filtros` (bag aplicado).
- **Navegação — grupo "Relatórios":** Indicadores de viabilidade (`/gestao/relatorios/indicadores`), Tempo de análise (`/gestao/relatorios/tempo`), Produtividade (`/gestao/relatorios/produtividade`), Quedas do expresso (`/gestao/relatorios/quedas`) — todos por `consultar-relatorios` — e Feriados (`/gestao/feriados`) por `manter-parametros`. **Cmd+K** (`command-search.tsx`, lista própria DESTINOS) COMMITADO (`76aba17`). **Sidebar** (`gestao-layout.tsx`) — após o refactor do usuário a sidebar só renderiza `groups: SidebarGroup[]` por props e os grupos da gestão são declarados no `gestao-layout.tsx`; o grupo "Relatórios" foi adicionado lá (mesmo padrão de "Auditoria e compliance", ícones já importados GridIcon/ListIcon/GroupIcon/AlertIcon/TagIcon), MAS o arquivo foi deixado **SEM COMMIT** por ser trabalho NÃO commitado do dono da Fase 14. O `app-sidebar.tsx` NÃO foi tocado; o critério de aceite original do plano (`grep consultar-relatorios app-sidebar.tsx`) estava DESATUALIZADO (pré-refactor) e foi ignorado.
- **PENDÊNCIA DE INTEGRAÇÃO (dono da Fase 14):** commitar o grupo "Relatórios" do `gestao-layout.tsx` (working tree) junto com o trabalho da Fase 14 — sem isso a sidebar não exibe o grupo no repositório (Cmd+K já exibe).
- **Verificação FRESCA:** `--filter=DashboardKpis` 5/5 (89 asserções); `npx tsc --noEmit` exit 0; `npm run build` exit 0 (chart-impl lazy/client-only, SSR intacto); greps de aceite das 3 tasks ok. `vendor/bin/pint` (DashboardController/teste/config) passed. Staging seletivo por caminho; NADA da Fase 14 do usuário (IA/e-mail/sidebar/seeder/routes/package*) tocado/commitado. Decisão: `config/sile.php` (limpo no repo, não é da Fase 14) commitado junto da implementação.

### Fase 15 — Plano 15-11 (retrofit de export HU-131 nas listagens das Fases 1–2) — concluído 2026-06-16

- **Critério de pronto transversal 8 cumprido (RN-009):** as 5 listagens das Fases 1–2 herdam o contrato único de exportação SEM reimplementar export e SEM rota nova. Cada `index` ganhou o branch `?formato=` (idiom do `ProcessoController`/15-09: `$request->string('formato')->lower()->toString()` + `in_array(['csv','xlsx','pdf'])`) delegando ao `ReportExporter` com `app(<Source>::class)` + `ReportFilters::fromArray($request->only([...]))`.
- **5 ReportSources novos (reconstrutíveis só pelo bag — INVARIANTE):** `CnaesReportSource` (busca por prefixo de dígitos no código OU denominação case-insensitive + situação + ordenação `code`/`description`), `ParametrosReportSource` (catálogo ordem grupo/chave; valor **sensível → `[sensível]`**, nunca lido em claro — RN-009), `UsuariosReportSource` (aba gestao/portal + busca nome/e-mail; `cpf_masked` por default), `PerfisReportSource` (`withCount(['permissions','users'])`), `AcessosUsuarioReportSource` (`user`-alvo no bag, e-mail derivado do id para não vazar PII na auditoria; query `user_id` OU e-mail do alvo). Todos lêem os filtros do **BAG** (não do construtor) — RN-005 no síncrono E no assíncrono.
- **Colunas (espelham a tela — RN-005):** cnaes [Código, Denominação, Situação, Classe]; parâmetros [Grupo, Chave, Tipo, Descrição, Valor, Padrão, Atualizado em]; usuários [Nome, E-mail, Papel, CPF, Situação, Criado em]; perfis [Perfil, Permissões, Usuários]; acessos [Evento, IP, Canal, Data/hora].
- **RN-007 (minimização de PII) + pendência DPO:** o projeto NÃO tem permissão de PII (as 30 do `RolesAndPermissionsSeeder` não incluem nenhuma de liberação de dado pessoal). Decisão anti-fachada: o branch de usuários **nunca seta `pii`** (CPF sempre mascarado `***.***.***-NN`); a capacidade de emitir o CPF completo existe e está provada em teste NA FONTE (`UsuariosReportSource` lê `pii` do bag), pronta para ligar quando a permissão for definida. **PENDÊNCIA DPO registrada:** definir a permissão de liberação de PII e quais colunas sensíveis ela libera.
- **DESVIO vs plano (RN-005 prevalece):** o plano sugeria a coluna `user_agent` no histórico de acessos; a tela `gestao/acessos` exibe `channel` (canal), não `user_agent` — colunas do `AcessosUsuarioReportSource` espelham a listagem real (a fonte irmã `AcessosReportSource` da trilha também usa `channel`).
- **Ajustes de assinatura:** `ParameterController::index` e `RoleController::index` passaram a receber `Request` (não recebiam) e `AccessHistoryController::__invoke`/os 5 índices tiveram o retorno alargado para `Inertia\Response|Symfony\...\Response` (padrão do `ProcessoController`) — necessário para o branch devolver o streaming do driver.
- **Verificação FRESCA:** `--filter=RetrofitListagens` **10/10 (37 asserções)**; `--filter=Relatorios` **114/114 (465 asserções)** (anti-regressão); `vendor/bin/pint` (11 arquivos do plano) passed; greps de aceite (implements ReportSource × 5, ReportExporter × 5 controllers, cpf_masked) ok. `routes/gestao.php` NÃO tocado (só a modificação pré-existente da Fase 14 do usuário permanece no working tree).
- **TDD estrito + staging seletivo:** cada task commitou o teste falhando (RED verificado) antes da implementação (GREEN verificado) — `07b3686`/`5604f9e` (T1 cnaes+parâmetros), `35f643c`/`06c4982` (T2 usuários+perfis), `82de476`/`139299c` (T3 acessos). NADA da Fase 14 do usuário (IA/e-mail/sidebar/seeder) tocado/commitado.

### Fase 15 — Plano 15-10 (retrofit dos CSVs históricos no contrato único HU-131) — concluído 2026-06-16

- **Consolidação RN-009:** os dois streamings `fputcsv` históricos foram migrados para o contrato único de 15-02. `AuditoriaController::export` monta `ReportFilters::fromArray($filtros + ['fonte' => $fonte])` e delega ao `ReportExporter` via `AtividadesReportSource` (trilha completa OU só alterações, conforme a chave `fonte` do bag — reconstrutível no assíncrono) e `AcessosReportSource`; `ProcessoController::index` delega o `?formato=` via `ProcessosReportSource`. `exportarAtividades`/`exportarAcessos`/`exportarCsv`/`fputcsv` removidos dos dois controllers; `index`/`export` aceitam csv/xlsx/pdf (ausente/desconhecido → csv, preserva o histórico).
- **Colunas preservadas (anti-regressão, Pitfall 7):** trilha `auditoria` (12 colunas via `ActivityResource`: Data/hora, Fonte, Ação, Descrição, Usuário, Em nome de, Entidade, Entidade ID, Resultado, Versão de regras, IP, Canal); `auditoria-acessos` (6: Data/hora, Evento, Usuário, E-mail, IP, Canal); `processos` (9 via `ProcessoResource`: Processo, BAP, Produto TVL, Empresa, CNPJ, Status, Categoria, Analista, Prazo). Base do nome preservada; o carimbo de data/hora (`fileName`) é herança do contrato 15-02 (nenhum teste verifica o nome).
- **Guarda de volume generalizada:** `auditoria.export.max_linhas` virou `ReportDefinition::maxRows` (default null, último parâmetro retrocompatível), honrado pelos três drivers — CSV para o `chunk`, XLSX para o `cursor`, PDF para o `get` — SEM tocar o `count()` que decide o limiar síncrono/assíncrono. Substitui a alternativa de subquery `whereIn+limit` no source (menos geral; não cobriria XLSX/PDF). `Relatorios` 96/96 confirma retrocompatibilidade dos sources existentes.
- **Fonte dedicada de processos:** `ProcessosReportSource` (≠ `SolicitacoesReportSource` de 15-03) preserva EXATAMENTE o CSV histórico (9 colunas, evento `exporta-processos` → `exporta-processos-csv`, arquivo `processos`, `personalData=false`). Reusar/ajustar o `SolicitacoesReportSource` (10 colunas, bairro/decisão) quebraria o `SolicitacoesReportSourceTest` e misturaria propósitos. Meta-auditoria preservada: trilha `auditoria`/`exporta-trilha-csv`/`personalData=true`; processos `analise`/`exporta-processos-csv`.
- **GANHO HU-131:** auditoria e processos herdam XLSX/PDF pelo mesmo `?formato=`; acima do limiar o `ReportExporter` despacha o `GerarExportacaoJob` com o bag (RN-005/RN-007 no assíncrono — o recorte filtrado é reconstruído, nunca dump integral da trilha com PII).
- **Verificação FRESCA:** `tests/Feature/Relatorios/` + `tests/Feature/Auditoria/` + `tests/Feature/Analise/` = **338/338 (1572 asserções)**; `--filter=Auditoria` 49/49, `--filter=Processo` 79/79; novos `RetrofitCsvAuditoriaTest`/`RetrofitCsvProcessosTest` verdes; `vendor/bin/pint` (arquivos do plano) passed; sem `fputcsv` nos controllers.
- **Coordenação concorrente (padrão da fase):** outro agente executou parte do plano em paralelo — commits `1530719` (Task 1: contrato `maxRows` + `AuditoriaController` + sources de auditoria + `RetrofitCsvAuditoriaTest`) e `844570a` (Task 2: `ProcessoController` + `ProcessosReportSource` + convergência dos sources de auditoria para `maxRows`). Esta sessão acrescentou `RetrofitCsvProcessosTest` em `8343f1b`. Race no índice git gerou dois commits `feat(15-10)` em vez de um por task (conteúdo coerente e verde; registrado para transparência). Staging seletivo; NADA da Fase 14 do usuário tocado.

### Fase 15 — Plano 15-12 (UI base: wrapper ECharts client-only + ExportMenu) — concluído 2026-06-16

- **Wrapper de gráfico ECharts client-only SSR-safe** (`resources/js/components/ui/chart/`), espelhando o padrão do `MapaSection`/`MapImovel` (Fase 4) em 3 camadas: `echarts-core.ts` (registro tree-shakeable `echarts.use([BarChart, LineChart, PieChart, GridComponent, TooltipComponent, LegendComponent, TitleComponent, DatasetComponent, CanvasRenderer])` importando de `echarts/core` — NÃO o pacote cheio; tipo `EChartsOption` = `ComposeOption` dos módulos registrados); `chart-impl.tsx` (toca o DOM: `echarts.init`/`setOption`/`ResizeObserver`/`dispose`; carregado SÓ no cliente via lazy); `chart.tsx` (guarda SSR: `lazy(() => import('./chart-impl'))` + flag `mounted` + skeleton de mesma altura). **`chart.tsx` não tem `from 'echarts'`** (só `import type` da prop) — caminho do servidor sem ECharts.
- **Tema do DS sem recriar a instância (Pitfall 5):** o impl lê `document.documentElement.classList.contains('dark')` (o `ThemeProvider`/`theme-context.tsx` alterna a classe `dark` no `<html>`, NUNCA `prefers-color-scheme`) e observa a classe do `<html>` com `MutationObserver`, aplicando `chart.setTheme('dark'|'default')` + `setOption` na instância viva. O tema `dark` embutido vem registrado pela cadeia do `echarts/core` (`lib/core/echarts.js` → `registerTheme('dark', ...)`) — `init`/`setTheme` aplicam estilo real (sem fachada). `<Chart>` expõe só `option` (+ `className`); o tema é resolvido internamente.
- **`<ExportMenu>`** (`resources/js/components/ui/data-table/export-menu.tsx`, irmão de `table-toolbar`/`per-page-select`): dropdown CSV/XLSX/PDF (usa o `Dropdown` do DS) onde cada item é uma **âncora nativa** `<a href="{url}?{URLSearchParams({...params, formato})}">` — download real, NÃO visita Inertia (mesmo padrão dos exports de `auditoria`/`processos`; o `DropdownItem` do DS renderiza `Link` do Inertia e quebraria o download binário). Props: `url`/`params`/`formatos?=['csv','xlsx','pdf']`/`label?`/`className?`; `formatos=[]` não renderiza nada; acessível (`aria-haspopup`/`aria-expanded`, `role=menu/menuitem`); rótulos pt-BR. A tela passa `relatorios.export.formatos_habilitados` em `formatos`; acima do limiar o backend (`ReportExporter`, 15-02/08) processa async e a notificação leva ao arquivo.
- **`useServerTable` expõe `currentParams`:** extraído `buildParams(state)` (módulo) como **fonte única** do snapshot de query (`sort`/`direction`/`per_page` + `search` + filtros não-vazios); `visit` (navegação) passou a usá-lo (comportamento inalterado) e o hook retorna `currentParams` via `useMemo` — o MESMO objeto montado em `visit`, para o ExportMenu montar a URL sem reimplementar a serialização (sem drift tela×export). Tipo exportado `ServerTableParams = Record<string, string | number>`; a coerção a string ocorre no boundary da `URLSearchParams` (`per_page` é number).
- **Commits (staging seletivo dos 5 arquivos do plano; NADA da Fase 14/outras frentes commitado):** `c432e94` (Task 1: wrapper ECharts — echarts-core/chart-impl/chart), `9a69228` (Task 2: ExportMenu + currentParams). Verificação FRESCA: `npx tsc --noEmit` exit 0 e `npm run build` exit 0 (Dimensão 6); critérios de aceite (grep) das 2 tasks satisfeitos; sem lints. ZERO dependência nova (echarts ^6.1 instalado em 15-01).
- DECISÕES: tema interno (não prop) p/ as telas não terem que propagar tema; `setTheme` em vez de dispose/init (must-have "sem recriar a instância"); `EChartsOption` definido em `echarts-core` (não `from 'echarts'`) p/ satisfazer o acceptance E o SSR-safe; sem testes JS (plano não inclui; `vitest.config.ts`/`package.json` do usuário intactos — verificação por grep+typecheck+build). PENDÊNCIA (não bloqueia): registrar novos `*Chart`/`*SeriesOption` em `echarts-core` quando 15-13/15-14 usarem outros tipos de gráfico.

### Fase 15 — Plano 15-09 (camada HTTP dos relatórios + download assinado + CRUD de feriados) — concluído 2026-06-16

- **CAMADA HTTP DA FASE (DONO ÚNICO de `routes/gestao.php` no EP15).** `RelatorioController` (fino) liga os serviços route-free (15-03/05/06/07) e o contrato de exportação (15-02/08) à retaguarda: `indicadores`/`tempo`/`produtividade`/`quedas` servem a tela Inertia com dado REAL e, quando vem `?formato=`, delegam ao `ReportExporter` (CSV/XLSX/PDF — RN-004/009). `RelatorioFiltersRequest` valida os 7 filtros das telas (período/setor/analista/bairro/cnae/categoria) e gera o `ReportFilters` bag (whitelisting fino é do source — RN-005). Consulta auditada (RN-002, logName `relatorios`, events `consulta-indicadores/tempo/produtividade/quedas`); gate `consultar-relatorios`; 403 auditado no ponto único.
- **PRODUTIVIDADE NOMINAL GATED (RN-007):** no HTTP `nominal = user->can('relatorios.produtividade.nominal')`, `escopo = nominal ? null : user->id` — sem a permissão, o default conservador anonimiza (rótulo ordinal, sem nome) e restringe ao próprio analista; o mesmo par alimenta a tela (`porAnalista`) e o export (`new ProdutividadeReportSource($nominal,$escopo)`, SyncOnly).
- **`?relatorio=` (um endpoint, mais de um source):** `tempo` exporta `TempoAnaliseReportSource` (default, detalhamento por etapa) OU `EscritorioVirtualReportSource` quando `?relatorio=escritorio-virtual`.
- **DOWNLOAD ASSINADO (HU-131/HU-132):** `ExportacaoController::download` serve o `ExportFile` do disco NÃO público por streaming — rota `gestao.relatorios.exportacoes.download` com middleware `signed` (alvo do link da Notification `ExportacaoPronta` de 15-02, que já apontava para ela). Guarda anti-disco-`public` (404), defesa-em-profundidade (dono OU `consultar-relatorios`), 404 honesto para arquivo podado por retenção (15-15) e auditoria do acesso (RN-008, event `baixa-exportacao`, `personalData=true`). Espelha o `TvlDocumentController`.
- **CRUD DE FERIADOS (HU-137):** `HolidayController` (index server-driven/store/update/toggleActivation, SEM destroy — RN-004) espelhando o `SectorController`, gated por `manter-parametros` (REUSO — como setores/textos-padrão; SEM permissão nova). `HolidayRequest` valida data única (normalizada para `Y-m-d H:i:s` em `prepareForValidation` para a regra `unique` casar com o formato do cast `date` — resolve falso negativo data-só × datetime). Auditoria automática via `HasAuditoria` do model (created/updated) — sem duplo log.
- **Rotas (nomes):** `gestao.relatorios.{indicadores,tempo,produtividade,quedas}`, `gestao.relatorios.exportacoes.download` (signed), `gestao.feriados.{index,store,update,ativacao.update}`. Rota estática de download ANTES dos relatórios; sem wildcard `{relatorio}`. Componentes Inertia para 15-13/15-14: `gestao/relatorios/{indicadores,tempo,produtividade,quedas}` e `gestao/feriados/index`.
- **Commits (staging seletivo por caminho; trabalho paralelo do usuário da Fase 14 intacto):** `94c6dc6` (Task 1: RelatorioController+RelatorioFiltersRequest+rotas), `9b1ddd3` (Task 2: ExportacaoController+rota signed), `e2123e1` (Task 3: HolidayController+HolidayRequest+rotas). VERIFICAÇÃO FRESCA: `--filter=RelatorioControllerTest` 9/9, `--filter=ExportacaoDownloadTest` 4/4, `--filter=HolidayControllerTest` 7/7; anti-regressão `--filter=Relatorios` 90/90 (379 asserções, zero regressão); `route:list -v` confirma `signed` no download; pint limpo (caminhos explícitos); `migrate` global NÃO rodado.
- DECISÕES: feriados reusam `manter-parametros` (sem permissão nova — registrado); `RelatorioFiltersRequest` valida só os filtros das telas e o source faz o whitelisting; download dentro do grupo `consultar-relatorios` + `signed` com guardas defensivas; HolidayController sem auditoria explícita (HasAuditoria já cobre).

### Fase 15 — Plano 15-03 (indicadores de viabilidade + SolicitacoesReportSource) — concluído 2026-06-15

- `IndicadoresViabilidadeService` (route-free, espelha `ProcessoQueryService`: Builder + `when()` + `groupBy`/`selectRaw`, NUNCA loop PHP) entrega 6 dos 11 indicadores por AGREGAÇÃO SQL real sobre as Fases 8–10: `porPeriodo` (HU-123, `date(protocoled_at)` portável), `porZona` (HU-124, DEGRADA para bairro com rótulo honesto — zona oficial bloqueada SEDUR/GIS), `porCnae` (HU-125, `count(distinct)` por código, top N), `porRisco` (HU-126, nível REAL do Decreto pelo CNAE principal × `risk_classifications` vigente → fallback `analysis_category` → `'nao_classificado'`, com `fonte` por linha — nunca nível inventado), `taxaDeferimento`/`taxaIndeferimento` (HU-127/128, razão real sobre as decididas no período; `taxa` null sem decisões — CA-03, nunca 0% fabricado).
- `SolicitacoesReportSource implements ReportSource` (RN-005): `definition()` reusa `ProcessoQueryService::filtered($f->toProcessoFiltros())` — o conjunto exportado é EXATAMENTE o filtrado da consulta de processos (bag carrega o vocabulário completo das telas; round-trip garante o mesmo no síncrono e no assíncrono), sem filtro reimplementado. Colunas [protocolo/empresa/cnpj/status/categoria/bairro/analista/protocolado_em/decidido_em/resultado], `personalData=true`, `event='exporta-solicitacoes'`; reconstrutível só pelo bag (sem estado de filtro no construtor).
- Commits: `e9f707b` (Task 1: indicadores HU-123/124/125/126 + teste), `ac354f3` (Task 2: taxas HU-127/128 + SolicitacoesReportSource + testes), `a16dc27` (fix: reaplica os métodos de taxa no serviço, perdidos por edição concorrente do working tree antes do stage). Staging seletivo (só os 4 arquivos do plano por caminho); nenhum arquivo de outras frentes commitado.
- Verificação FRESCA: `--filter=IndicadoresViabilidadeServiceTest` 8/8 + `--filter=SolicitacoesReportSourceTest` 4/4 (12/12, 35 asserções, exit 0); anti-regressão `--filter=Relatorios` 48/48 (206 asserções); `grep` confirma agregação em SQL (sem `->get()->groupBy(`); pint limpo nos meus 4 arquivos. Sem dependência nova; sem migration nova (testes em SQLite/RefreshDatabase).
- ATENÇÃO concorrência (mesma frente, Fase 15): durante a execução outro(s) agente(s) editaram/reverteram o working tree do `IndicadoresViabilidadeService.php` (15-06/15-08); resolvido preservando o trabalho alheio (refatoração de `porRisco` mantida) e reaplicando os métodos de taxa via `a16dc27`. Próximos planos da fase (15-04 tempo/SAPS, 15-06 já concluído, 15-09 dashboard) seguem.

### Fase 15 — Plano 15-04 (cadastro de feriados HU-137 + duração em tempo útil HU-129) — concluído 2026-06-15

- Entrega o habilitador da HU-129 SEM fachada: `Holiday` (model `#[Fillable]` + `HasAuditoria` RN-002 + casts `date`/`recurring_annually`/`active` + scope `active`) com migration ADITIVA `2026_06_15_000003_create_holidays_table` (date unique, recurring_annually, active; up/down validados ISOLADOS via `--path` — `migrate` global NÃO rodado por causa das migrations pendentes da Fase 14) + factory + `HolidaySeeder` idempotente (`updateOrCreate` com chave Carbon para casar o cast `date`).
- ANTI-FACHADA (HU-137): o seeder semeia APENAS os 8 feriados NACIONAIS de data fixa (Lei 662/1949 e 6.802/1980: 01/01, 21/04, 01/05, 07/09, 12/10, 02/11, 15/11, 25/12) marcados `recurring_annually=true`. A lista MUNICIPAL de Salvador (2 de Julho, padroeiras, móveis) é pendência SEDUR — NÃO é semeada, nenhum feriado é inventado. `hasOfficialCalendar()` é verdadeiro só quando há feriado NÃO-recorrente (municipal específico) cadastrado → hoje false (degradação honesta, base da ressalva no relatório).
- `HolidayProvider` (contrato `isHoliday(DateTimeInterface): bool` + `hasOfficialCalendar(): bool`) + `DatabaseHolidayProvider` (lê os ATIVOS com `Cache::remember`, TTL técnico `sile.relatorios.cache_ttl_segundos`=300; recorrentes casam por mês/dia, específicos por data exata) + binding SINGLETON `HolidayProvider→DatabaseHolidayProvider` no `AppServiceProvider` (dono único deste arquivo nesta wave).
- `BusinessDeadlineCalculator::businessDurationBetween(from,to): int` (NOVO — HU-129) = duração em MINUTOS úteis decorridos, dia a dia, descontando fim de semana || feriado ativo (parcial nas pontas); `to <= from ⇒ 0`. Provider injetado por construtor OPCIONAL (`?HolidayProvider $holidays = null`): `new BusinessDeadlineCalculator` (sem container) segue funcionando e degrada honesto (só fins de semana). É a causa-raiz da distorção do legado (19 dias reportados vs 42h reais), resolvida sem reusar o `dueAt` forward (RESEARCH Pitfall 3).
- ANTI-REGRESSÃO (Fases 9/10): `dueAt`/`isOverdue` INALTERADOS (prazo SLA/BAP em horas-calendário `addHours`); `--filter=AnalysisSlaService` 9/9 e `--filter=IndeferirSemBap` 6/6 verdes. Tornar o PRAZO em dias úteis fica PENDENTE (depende do critério legal SEDUR) — registrado, não implementado.
- Commits (staging seletivo dos 10 arquivos do plano; NADA da Fase 14 do usuário commitado): `a80c30c` (Task 1: Holiday+migration+factory+seeder+HolidayCadastroTest), `6ec9e9f` (Task 2: HolidayProvider+DatabaseHolidayProvider+binding+businessDurationBetween), `94b28a2` (Task 3: testes cravados).
- Verificação FRESCA: `--filter=HolidayCadastroTest` 5/5, `--filter=BusinessDeadlineCalculatorTest` 7/7, `--filter=Holiday` 7/7, `--filter=AnalysisSla` 9/9; migration up/down isolada; binding resolve `DatabaseHolidayProvider` no container (tinker: 1440 min com fim de semana descontado). pint limpo nos meus arquivos (caminhos explícitos).
- PENDÊNCIAS registradas (sem fachada): lista municipal oficial de feriados (SEDUR); wiring do `HolidaySeeder` no `DatabaseSeeder` + invalidação de cache no CRUD de feriados (fora do escopo de arquivos deste plano — próximos planos; hoje efeito limitado ao TTL, padrão HU-014); prazo SLA em dias úteis (critério legal SEDUR).

### Fase 15 — Plano 15-07 (captura estruturada da queda HU-145 + relatório de quedas) — concluído 2026-06-15

- **Captura ADITIVA da queda (HU-145, pré-requisito anti-fachada):** `ExpressoQueda` (model `#[Fillable]` + relação `viabilityRequest` + migration `2026_06_15_000004_create_expresso_quedas_table` com `cnae/tipo_gatilho/dimensao` nullable, `motivo` text, índice `(tipo_gatilho, created_at)`; up/down validados ISOLADOS em pgsql via `--path`) + factory. Gravada por `FluxoExpressoService::capturarQueda()` chamado DENTRO da `DB::transaction` de `encaminharAnalise`, APÓS o `audit->log` — aditivo, mesma atomicidade, SEM tocar decisão/transição/SLA/auditoria das Fases 9/10.
- **Chaves lidas** de `$item['consulta_array']['risco']['encaminhamento']` (shape do `RiscoClassificationService`: `fluxo`/`dimensao_decisiva`/`motivo`/`gatilhos_acionados[]{codigo,motivo}`): `tipo_gatilho ← gatilhos_acionados[0]['codigo']`, `dimensao ← dimensao_decisiva`, `motivo ← motivo ?? $reason`, só para `$item['fluxo']===Fluxo::Analise`. ANTI-FACHADA (RN-001): `$resolved===null` (toggle off) grava 1 linha de nível-processo (`cnae/tipo_gatilho/dimensao` NULL); CNAE caído sem gatilho de contexto grava `tipo_gatilho` NULL com `cnae` — gatilho NUNCA inventado.
- **`ExpressoQuedaService`** (route-free, agregação SQL): `taxaRespostaExpressa` (respondidas = `viability_decisions` flow='expresso' & `decided_by_user_id` IS NULL ÷ elegíveis = transições `protocolada→{deferida|indeferida|em_analise}`; `taxa` null sem base; `meta` de `relatorios.expresso.meta_taxa` cast float, null quando não definida — RN-004), `serieTemporal` (taxa por dia na janela parametrizável, `date()` portável), `rankingMotivos` (groupBy `tipo_gatilho` desc, null→'não classificado'), `drillDown` (reusa `ProcessoQueryService::filtered` ∩ processos com queda → base p/ divergências analista×motor). **`ExpressoQuedaReportSource`** (HU-131, `personalData=false`, `event='exporta-quedas-expresso'`).
- **ANTI-REGRESSÃO Fases 9/10 (crítica):** captura 100% aditiva — `--filter=FluxoExpresso` 20/20, `--filter=Expresso` 123/123 (558 asserções) verdes. Testes: `--filter=ExpressoQuedaCapturaTest` 3/3 (gatilho real via stub do resolver — wiring do `gatilhosContexto` só no EP07; motor degradado; queda real CNAE alto + anti-regressão), `--filter=ExpressoQuedaServiceTest` 7/7 (taxa 75.0 = 6÷8, taxa null, meta null/80.0, ranking, série, drill-down, report source). Suíte de relatórios junta: `--filter=Relatorios` 63/63.
- Commits (staging seletivo dos 8 arquivos do plano; NADA da Fase 14/outras waves commitado): `5a35e88` (Task 1: ExpressoQueda + captura aditiva), `5de0208` (Task 2: ExpressoQuedaService + ExpressoQuedaReportSource). pint limpo (caminhos explícitos). Sem dependência nova.
- PENDÊNCIAS registradas (sem fachada): meta da taxa expressa (RN-004, hoje "não definida" — SEDUR); wiring do `gatilhosContexto` (EP07 — a captura já extrai o `tipo_gatilho` real quando ligado, sem mudança de código); telas/controller do relatório de quedas (15-06).

### Fase 12 (Auditoria e Compliance) — concluída 2026-06-15

- Torna a trilha RN-002 (registrada desde a Fase 1) consultável/exportável e monitorada (LGPD), e adiciona detecção de abuso que NUNCA pune. 12 planos em 6 waves paralelas, cada wave verificada por integração; guardião-entrega APROVADO com evidência fresca própria.
- HU-098/100/101: `AuditTrailQueryService` (filtered/apenasAlteracoes/acessos) + `AuditoriaController` (index por fonte atividade/alterações/acessos + export CSV streaming) server-driven sobre `activity_log` (índices da 12-01); meta-auditoria (a própria consulta/export auditada, `personal_data`). Export pleno XLSX/PDF → Fase 15 (bloqueado honesto).
- HU-099: `decision_trace` forward-snapshot gravado no momento da decisão (motor `FluxoExpressoService` e analista `AnaliseTecnicaDecisionService`, via `DecisionTraceBuilder`, ADITIVO — Fases 9/10 verdes); `DecisionExplanationService::explain()` é PROJEÇÃO PURA (spy do motor = 0, RN-005), legado degrada honesto ("não registrado"); prop `explicacao` nos dois `show`, sem rota/permissão nova.
- HU-102: `LgpdMonitorService` (consentimentos × termo vigente, retenção + último pruning com decisões FORA do pruning, acessos a PII via `personal_data`) + painel gated `monitorar-lgpd`, minimizado; `personal_data` marcado nos call sites reais (ProcessoController/AccessHistoryController/UserManagementController).
- HU-149: `abuse_alerts` (idempotência por índice único parcial) + 5 detectores determinísticos (2 volume + 3 estruturais: polígono repetido, inscrição com atividades incompatíveis, evasão por condicionante) na tag `abuse.detectors`; `AbuseDetectionService` toggle-gated (`features.deteccao_abuso` OFF default → no-op honesto), encaminha à malha fina acima do limiar pelo caminho de sistema, NUNCA transiciona o status do processo (RN-001); `abuso:detectar` no scheduler; painel humano (efetividade confirmados÷gerados + confirmar/descartar com justificativa obrigatória auditada) + nav/Cmd+K gated.
- AuditoriaDevSeeder: dados de dev REAIS pelo fluxo de verdade (padrão de abuso dedicado → detecção real → alerta + malha fina; acesso personal_data; decisão com trace + decisão legada), idempotente, toggle restaurado OFF.
- CORREÇÃO TRANSVERSAL (Fase 10): a rota `gestao/caixa-setor` renderizava página inexistente (fachada de runtime — backend real e testado, mas tela ausente do plano 10-16); criada `gestao/caixa-setor/index.tsx` (lista + assumir + distribuir em modal) e o index passou a expor `analistas`/`podeDistribuir` (TDD). Commits `1c7c7df`, `6e1c7de`.
- Parâmetros 85 / permissões 27. Verificação fresca: pint limpo; `composer test` = 1315/1315 SQLite + 29/29 @group postgis; build/typecheck verdes.
- BLOQUEADO/pendente (sem fachada, registrado): export pleno XLSX/PDF (HU-131) → Fase 15; limiares/calibração de fraude, matriz de CNAEs incompatíveis, captura de respostas de condicionante, papel auditor → SEDUR; retenção/eliminação/anonimização LGPD → DPO; `EscritorioVirtualEncadeadoDetector` → 2ª onda.

### Fase 11 (Pendências e Comunicação) — concluída 2026-06-15

- Generaliza e ATIVA o que a Fase 10 deixou pronto/dormente, SEM refazer estado: o ciclo notificar→responder→reabrir (em_analise↔em_pendencia) vira comunicação MULTICANAL real (in-app via canal `database` nativo + e-mail) pelos listeners AUTO-DESCOBERTOS `NotificarPendencia` (PendenciaSolicitada → requerente) e `NotificarRespostaPendencia` (PendenciaRespondida → analista), roteados pelo `NotificationDispatcher` (toggles `features.notificacao_*` ∩ `notificacoes.mapa_canais`, HU-014). `PendenciaService::abrir` parou de notificar direto (anti-duplicação); o `RegistrarEnvioComunicacao` fecha o ledger `communications` (na_fila→enviado/falhou) por evento de notificação. Histórico unificado HU-096 = tabela dedicada `communications` (fonte única, ledger imutável); central in-app sobre o canal database nativo + badge por shared prop (`notificacoes.nao_lidas`).
- WhatsApp (HU-095) atrás de `WhatsAppChannel` + contrato `WhatsAppGateway` (binding default `UnavailableWhatsAppGateway`): OFF→`desativado`, ON+indisponível→`bloqueado` AUDITADO, NUNCA `enviado`. Dono ÚNICO da linha whatsapp; o `RegistrarEnvioComunicacao` EXCLUI whatsapp e a guarda do `Communication::markAsSent` protege o estado terminal. Scheduler idempotente (HU-093 `notificacoes:alertar-vencimentos`, HU-147 `notificacoes:escalonar-sla`, HU-091 `pendencias:expirar`) — fonte única `analysis_due_at`/`analysis_pendencies.due_at`, SÓ notifica (sem transição/timeline → sem dupla contagem), idempotência via checagem em `communications`.
- Fechamento (11-10): `ComunicacaoDevSeeder` produz comunicações/notificações REAIS pelo fluxo de verdade (processo dedicado → em_analise pelo caminho legítimo: protocolo + máquina de estados + `DistribuicaoService::assumir` → `PendenciaService::abrir/responder`), territorial-agnóstico (roda em SQLite, ≠ AnaliseDevSeeder/ExpressoDevSeeder que exigem PostGIS) e idempotente. `WhatsAppPipelineIntegrationTest` prova o invariante anti-fachada pela PIPELINE COMPLETA (sem `Notification::fake`): ON+indisponível → bloqueado, nunca enviado; e-mail/in-app na mesma notificação → enviado.
- Verificação integral FRESCA: pint --test/typecheck/build verdes; `composer test` = **1205/1205 SQLite + 29/29 @group postgis** em 2 processos (POSTGIS_TESTS_REQUIRED=true). **Parâmetros 78 / permissões 24** inalterados. ZERO dependência nova. guardião de entrega: APROVADO (evidência fresca independente).
- BLOQUEADO/pendente (sem fachada, registrado): **WhatsApp provedor real (HU-095)** → Fase 13 (toggle off + contrato Unavailable hoje; liga trocando SÓ o binding, credenciais `integrations.whatsapp.*` criptografadas + teste de conexão); **convite/resposta via Simplifica/Regin (HU-091 RN-004)** → Fase 13 (resposta pelo portal já funciona); **"gestor do setor" do escalonamento (HU-147)** e roteamento ao setor → SEDUR (default `role gestor` parametrizável); **rito de não-resposta da pendência (indeferir por prazo)** → SEDUR (hoje expira+notifica+mantém estado, sem decisão automática); **templates oficiais** (texto/identidade/base legal) → SEDUR (pt-BR default parametrizável); preferência de canal por usuário → gancho no dispatcher (default por toggle/mapa).
- ÚNICO gate restante: smoke navegável humano (roteiro em 11-10-SUMMARY: abrir pendência → in-app+e-mail ao requerente → responder no portal → análise reabre → analista notificado → histórico HU-096 + WhatsApp OFF honesto).

### Fase 10 (Análise Técnica SEDUR) — concluída 2026-06-15

- Fluxo de decisão HUMANA real: o parecer grava na MESMA `viability_decisions` (flow='analise_tecnica', decided_by=analista ≠ null) a partir da FICHA finalizada — `AnaliseTecnicaDecisionService` PODE deferir o caso pendente sem zona (diferente do expresso, que recusa), com fundamentação própria; dispara `ResultadoEmitido` (listeners da Fase 9 reusados → Regin/SEFAZ bloqueados honestos). Ficha versionada `analysis_records` (autosave/finalizar imutável/diff/nova-revisão) + `analysis_divergences` (analista×motor → HU-145). Pré-análise: listener auto-descoberto `PreAnalisarProcesso` no evento `EncaminhadoParaAnalise` reusa o `SolicitacaoViabilityResolver` (degrada honesto sem zona/motor).
- Setores/caixa (HU-138: sectors + sector_user), distribuir/assumir (`DistribuicaoService`, RN-004/007), fila com SLA (HU-144: `AnalysisSlaService` semáforo on-the-fly + analysis_due_at), consulta HU-082 (`ProcessoQueryService` filtros SAPS + categorias + busca global + índices + CSV), precedentes HU-142 (`PrecedentRepository`/`PostgisPrecedentRepository` ST_Intersects sobre property_polygon, fake em tests/Support/Analise), TVL PDF HU-132 (`barryvdh/laravel-dompdf`, só deferida, disco não-público, URL assinada, nunca ao cidadão), malha fina HU-136 (`MalhaFinaService`, flag+tabela ortogonal, atinge deferido), pendência HU-083/084 ciclo interno (`PendenciaService` + evento `PendenciaSolicitada` gancho EP11 + portal + e-mail). Endpoints em `Gestao\*Controller`; UI: fila/consulta/detalhe (mini-mapa+timeline) + ficha SAPS + admin setores/textos + nav/Cmd+K.
- Parâmetros grupo `analise` (catálogo 67), permissões 24 (analisar-processos/distribuir-processos/emitir-tvl/encaminhar-malha-fina/manter-setores). StateMachine ganhou em_analise→{em_pendencia,deferida,indeferida} e em_pendencia→em_analise (deferida/indeferida finais = encerramento HU-089).
- guardião APROVADO; verificação `composer test` = 1132/1132 SQLite + 29/29 @group postgis em 2 processos isolados. Evidência: analise:decidir 7 --finalizar → DEFERIDA + TVL.
- BLOQUEADO/pendente (sem fachada): roteamento automático ao setor (SEDUR; dev usa caixa de triagem, produção atribui manual); transmissão Regin/SEFAZ na conclusão (HU-104/110) → Fase 13 (auditam 'bloqueado'); HU-083/084 pleno (convite Simplifica/Regin + multicanal) → EP11; export pleno XLSX/PDF (HU-131) → Fase 15; zona Quadro 10, HU-137 feriados (seam pronto), assinatura gov.br/ICP do TVL, valor TLL monetário (DAM) → SEDUR/Fase 13.
- LIÇÃO (verificação): rodar SQLite + @group postgis no MESMO processo `php artisan test` pula o migrate do banco pgsql_testing (flag global RefreshDatabaseState::$migrated) → falha num banco limpo. Verificar SEMPRE em 2 processos: `composer test` agora encapsula o split (fix 0cb1c8b); o CI já roda os 2 steps. NÃO afirmar "X/X num comando único com POSTGIS_TESTS_REQUIRED".
Plan: 10-18 de 18 concluído (fechamento: seeds dev + comando analise:decidir + golden/smoke + precedentes @group postgis + verificação integral)
Status: Fase 10 implementada e verde. Decisão técnica HUMANA real (flow analise_tecnica + TVL PDF interno por URL assinada, NÃO ao cidadão), ficha pré-analisada pelo motor, precedentes, pendência e malha fina; produção mantém a degradação honesta sem a zona oficial (Quadro 10/SEDUR) e sem o roteamento automático ao setor (no dev: caixa de triagem; em produção o gestor atribui manualmente). Suíte 1161/1161 (SQLite + @group postgis com POSTGIS_TESTS_REQUIRED=true); pint/tsc/build verdes.
Last activity: 2026-06-15 -- 10-18 concluído (fechamento da Fase 10): SectorSeeder/StandardTextSeeder + AnaliseDevSeeder (driver-aware) + comando analise:decidir + AnaliseGoldenCaseTest (3) + AnaliseSmokeTest (fluxo humano real, 5) + AnalisePrecedentesPostgisTest + AnaliseSeedPostgisTest. Evidência dev: analise:decidir 7 --finalizar → DEFERIDA + TVL-2026-000003; auditoria 'analise'=13; decisões flow analise_tecnica=2; malha fina=1; pendência=1.

Progress: [███████▍░░] 73% (Fases 1/2/2.4/3/3.1/4/5/6/7/8/9/10/11/12 concluídas; 135 planos; próxima acionável: Fase 13 Integrações — bloqueada externamente; Fase 15 Relatórios autonomamente executável)

Next step: Fase 12 concluída (guardião APROVADO; smoke humano dispensado). A próxima no roadmap é a **Fase 13 (Integrações)**, porém BLOQUEADA externamente — contrato Regin/JUCEB, base SIGIS/CA 2000, endpoint/credenciais SEFAZ e ambiente de homologação são pendências SEDUR (regra anti-fachada: integração sem acesso fica explicitamente bloqueada, nunca adaptador falso). A **Fase 15 (Relatórios e Indicadores)** é a próxima fase autonomamente executável (calcula sobre dados reais já no sistema, sem dependência externa); a **Fase 14 (IA)** depende de credenciais de provider (infra/toggles possíveis, OCR/LLM reais bloqueados sem chave). **DECISÃO DO USUÁRIO (2026-06-15): próxima fase = Fase 15 (Relatórios e Indicadores), a ser INICIADA EM SESSÃO NOVA (contexto limpo, esta sessão cobriu as Fases 7–12).** Ao retomar, seguir o mesmo fluxo das Fases 7–12: brainstorm (analista-negocio + arquiteto-tecnico, convergência) → spec/CONTEXT commitados → planejamento (gsd-planner + plan-checker) → execução por waves paralelas com gsd-executor (TDD, integração verde por wave, `composer test` nos 2 processos) → guardião-entrega → atualizar ROADMAP/STATE/REQUIREMENTS. Fase 15 calcula sobre dados REAIS (taxas de deferimento/indeferimento, tempo médio por etapa da timeline, produtividade por analista, HU-145 quedas por gatilho + drill-down nas divergências analista×motor); export pleno HU-131 (assíncrono acima de limiar, padrão RN-004 a RN-009) é o entregável transversal que as Fases 8–12 vinham diferindo para cá. Fase 13 (Integrações) permanece BLOQUEADA externamente (SEDUR); Fase 14 (IA) depende de credenciais.

### Fase 10 (Análise Técnica SEDUR) — concluída 2026-06-15

- DECISÃO TÉCNICA HUMANA real e auditável, fechando o ciclo dos processos que o expresso NÃO decide (em_analise: sem zona, semi-expresso, toggle off). Cadeia real: encaminhar (FluxoExpressoService, Fase 9 dispara EncaminhadoParaAnalise) → listener auto-descoberto PreAnalisarProcesso cria a ficha rev 1 pré-preenchida pelo motor (HU-140, reusa SolicitacaoViabilityResolver; FA-01 degrada para modo manual sem zona) → caixa do setor + distribuir/assumir (HU-080/081/138, SLA recalculado HU-144) → ficha versionada (HU-135: autosave RN-008, finalização imutável RN-003, diff, divergências analista×motor RN-002 em analysis_divergences) + precedentes (HU-142, @group postgis, sem CPF) → AnaliseTecnicaDecisionService::decide (HU-085-089): grava na MESMA viability_decisions com flow='analise_tecnica' e decided_by_user_id=analista (≠ null), número TVL só no deferimento, transição em_analise→deferida|indeferida (encerramento), auditoria SÍNCRONA + ResultadoEmitido após commit (reusa os 3 listeners da Fase 9).
- O humano PODE DEFERIR o caso PENDENTE (sem zona oficial) com fundamentação própria da ficha — é exatamente o que a análise técnica existe para resolver (≠ do expresso, que recusa o pendente). Malha fina (HU-136) ortogonal ao status (flag in_fine_mesh + fine_mesh_referrals), atinge até deferido (corrige bug legado). Pendência (HU-083/084 PARCIAL) ciclo interno: abrir (em_analise→em_pendencia) + e-mail simples real + resposta pelo portal (→em_analise); convite Simplifica/Regin + multicanal → EP11. TVL PDF (HU-132, dompdf — única dep nova): só deferida, disco não público, download por temporarySignedRoute, auditado — NÃO vai ao cidadão.
- Fechamento (10-18): SectorSeeder/StandardTextSeeder (catálogos sempre + usuários de gestão dev analista@sile.dev/gestor@sile.dev só dev/teste, vinculados ao setor de triagem) + AnaliseDevSeeder (driver-aware: monta os estágios reais — em análise/distribuído, deferido humano+TVL, malha fina, pendência — só em pgsql; no-op honesto em SQLite, igual ExpressoDevSeeder, pois decide() reexecuta os motores territoriais). Comando analise:decidir {solicitacao} {--finalizar} (contraparte humana do expresso:decidir). Golden (#[DataProvider], 3 fixtures) + smoke (fluxo humano real, FakeSpatialRepository, 5 cenários) + AnalisePrecedentesPostgisTest (PrecedentService sobre PostGIS) + AnaliseSeedPostgisTest.
- Verificação integral FRESCA: pint limpo; suíte **1161/1161** (5982 asserções) = SQLite + @group postgis com `POSTGIS_TESTS_REQUIRED=true` (container sile-pgsql); tsc/build verdes. Evidência dev (sile_testing, sem tocar o dev): `analise:decidir 7 --finalizar` → DEFERIDA + TVL-2026-000003; auditoria 'analise'=13, decisões flow analise_tecnica=2, malha fina=1, pendência=1, setores=1, textos-padrão=4. **Parâmetros 67 / permissões 24**. ZERO dependência nova além do dompdf.
- BLOQUEADO/pendente (sem fachada, registrado): **roteamento automático ao setor** = pendência SEDUR (FluxoExpressoService deixa sector_id nulo; no dev o seed usa caixa de triagem, em produção o gestor atribui via distribuir/caixa); transmissão Regin (HU-104)/SEFAZ (HU-110) na conclusão → Fase 13 (DECISÃO/TVL/auditoria reais, transmissão 'bloqueado' auditada nunca "enviado", provado no AnaliseSmokeTest); HU-083/084 pleno (convite + multicanal) → EP11; exportação plena XLSX/PDF (HU-131) → Fase 15; zona oficial Quadro 10/SEDUR (sem ela o humano decide o pendente); HU-137 feriados (seam pronto); assinatura gov.br/ICP do TVL → SEDUR; quadros/ficha SAPS oficiais/gatilhos/valor TLL monetário (DAM bloqueado) e recurso administrativo → SEDUR.
- LIÇÃO: seeder/teste que depende de motor espacial é driver-aware — estágios reais só em pgsql, com no-op honesto em SQLite e prova dedicada em @group postgis (espelha ExpressoDevSeeder/ExpressoSeedPostgisTest). A janela de precedentes (HU-142) é relativa a now() — fixtures de data devem cair dentro da janela parametrizada (analise.precedentes.janela_meses).
- ÚNICO gate restante: smoke navegável humano (roteiro registrado em 10-18-SUMMARY: fila→ficha→deferir→TVL + pendência ida-e-volta + malha fina sobre deferido + consulta/Cmd+K + mobile/dark).

### Fase 9 (Fluxo Expresso) — concluída 2026-06-14

- CORE VALUE entregue: decisão automática real e auditável. Gatilho protocolar→evento SolicitacaoProtocolada→listener auto-descoberto AvaliarFluxoExpresso→DecidirFluxoExpressoJob (fila)→FluxoExpressoService::decide (autoritativo, reexecuta os motores FRESCO via SolicitacaoViabilityResolver; Cache::lock+re-check idempotente). RN-009: nao_permitido→INDEFERE; permitido(_com_condicoes)→DEFERE; pendente(sem zona)/semi-expresso/toggle off→em_analise SEM decisão e SEM evento (anti-fachada).
- ViabilityDecision imutável 1:1 (per_cnae/rules_versions/fundamentacao + número TVL só no deferimento via TvlNumberGenerator lockForUpdate); transição StateMachine auditada; auditoria HU-078 SÍNCRONA (independe do evento). 2º evento de domínio ResultadoEmitido (after-commit) → 3 listeners auto-descobertos: NotificarResultadoExpresso (HU-077 e-mail sem anexo TVL), ComunicarResultadoRegin (HU-104), EnviarViabilidadeSefaz (HU-110 só deferida) — Regin/SEFAZ BLOQUEADOS honestos (contratos Unavailable* auditam 'bloqueado'/'ignorado', nunca "enviado") → Fase 13.
- HU-134 (prazo BAP) DORMENTE: IndeferirSemBapService + comando expresso:indeferir-sem-bap + seam BusinessDeadlineCalculator (HU-137 troca por dias úteis sem tocar call sites); no-op em produção até o Regin (Fase 13). Retaguarda: ResultadoExpressoController + 1º ViabilityDecisionResource (lista+detalhe read-only sob consultar-solicitacoes; transmissão Regin/SEFAZ = pendente honesto). Comando expresso:decidir; golden(#[DataProvider])+smoke real (job não fakeado); zona fictícia ZCN-1 SÓ dev/teste (gate de ambiente) demonstra deferimento navegável sobre a lógica real.
- guardião APROVADO (após fix 8eb82a4: factory alinhada ao contrato real do serviço + Resource/frontend blindados contra shape não-lista — corrigiu crash de SSR na tela de detalhe; 2 regressões por contrato). Suíte 955/955 SQLite + 22 @group postgis; pint/typecheck/build verdes. Parâmetros 56 / permissões 19. ZERO dependência nova.
- BLOQUEADO→Fase 13: Regin/SEFAZ (HU-104/110) + HU-134 ativa (contratos prontos, trocar 3 bindings). TVL PDF (HU-132)→Fase 10 (fonte ViabilityDecision). Zona oficial Quadro 10/SEDUR pendente (sem ela, degradação honesta em_analise — a zona fictícia NÃO vaza p/ produção). E-mail Resend free tier = pendência de config (notificação real, EmailLog+failed honesto). HU-137 feriados + canais plenos → EP11.
- LIÇÃO: factory/seed de teste DEVE refletir o contrato real do serviço (shape exato) — senão mascara bugs de consumo (ex.: tela que itera um campo). Blindar a borda (Resource array_values + Array.isArray no front) contra shapes inesperados, e travar com teste de contrato, não só de existência de prop.

### Fase 9 (Fluxo Expresso) — COMPLETE (12/12 planos; smoke navegável humano pendente)

#### 09-01 (fundação de parametrização HU-014) — concluído 2026-06-14

- **Catálogo HU-014 de 49 → 56 parâmetros**, grupo NOVO `expresso` (10 grupos). 7 parâmetros administráveis do fluxo expresso, todos com tipo/descrição pt-BR/default/validação e `value` null (upsert só de metadados — value preservado no re-seed):
  - Grupo `features` (toggles): `features.fluxo_expresso` (default 1; off ⇒ toda protocolada vai para análise técnica, degradação comunicada), `features.notificacao_resultado_expresso` (default 1; e-mail de resultado sem anexo TVL).
  - Grupo `expresso` (negócio): `expresso.bap.prazo_horas` (48), `expresso.notificacao.assunto_deferida`/`_indeferida`, `expresso.tvl.prefixo` (TVL)/`expresso.tvl.padding` (6).
- **config/sile.php**: bloco `expresso` espelha os parâmetros (fallback do `Settings::get` sem banco) + 2 toggles em `features.*`; e guarda as constantes TÉCNICAS FORA do catálogo (precedente [02-02]): `expresso.lock.ttl_segundos`=10 (Cache::lock da emissão), `expresso.fila`='default', `expresso.job` (tries=3/timeout=120/backoff=[30,60,120]) do DecidirFluxoExpressoJob.
- **REUSA `risco.mapa_encaminhamento`** (Fase 6) para a elegibilidade — NÃO criou parâmetro novo. **ZERO permissão nova** (reusa `consultar-solicitacoes`; permanecem 19). ZERO dependência nova.
- TDD estrito (RED→GREEN fresco): `ParameterSeederTest` 15/15, `DatabaseSeederTest`+`ParameterSeederTest` 21/21; `config:show sile.expresso.tvl.prefixo`=TVL. Suíte completa 857/857 (4489 asserções, @group postgis com sile-pgsql healthy). Commits `88145aa` (catálogo+teste) / `c963d71` (config+contagem).
- **Insumo direto**: 09-05 (toggle/TVL/assuntos), 09-07 (notificação HU-077), 09-10 (prazo BAP HU-134 dormente), 09-06 (fila/tries/backoff/lock do job). Wave 1 paralela (09-02/03/04) NÃO toca este escopo — 09-01 é dono exclusivo do catálogo/config/seeder-tests.

#### 09-02 (fundação de dados + domínio da decisão expressa) — concluído 2026-06-14

- **Tabela IMUTÁVEL `viability_decisions` (1:1, append-only)** — fonte do PDF/TVL (Fase 10) e da explicabilidade (Fase 12): `viability_request_id` (FK cascadeOnDelete, **unique** 1:1), `flow` (default 'expresso'), `outcome` (deferida/indeferida), `consolidated_result` (permitido/permitido_com_condicoes/nao_permitido), `tvl_product_number` (**unique** nullable — só deferimento), `per_cnae`/`rules_versions`/`fundamentacao` (jsonb, RN-005/009), `reason` (text), `decided_by_user_id` (FK users nullable, **null=sistema**), `decided_at`. `tvl_sequences` (year unique, last_number) + colunas DORMENTES `bap_due_at`/`bap_linked_at` em `viability_requests` (HU-134, gravadas via forceFill quando o Regin entrar).
- **Domínio**: enum `App\Enums\DecisionOutcome` (Deferida/Indeferida + label() pt-BR — mapeamento ResultadoViabilidade→outcome fica no 09-05, onde mora a consolidação RN-009). Model `App\Models\ViabilityDecision` (casts outcome→enum + 3 jsonb→array + decided_at→datetime; relações `viabilityRequest()`/`decidedBy()`; `isDeferida()`; **SEM HasAuditoria** — auditoria síncrona no service, HU-078). `App\Models\TvlSequence` espelha ProtocolSequence. `ViabilityRequest::decision()` HasOne + casts bap_*→datetime (bap_* FORA do fillable). Factories: `ViabilityDecisionFactory` (deferida default; states `indeferida()`/`semAtuacaoBap()`), `TvlSequenceFactory`.
- **`App\Services\Expresso\TvlNumberGenerator::generate(?int $year): string`** (TVL-AAAA-NNNNNN) concorrência-seguro espelhando ProtocolNumberGenerator: `tvl_sequences->lockForUpdate()` em DB::transaction; NÃO grava em viability_decisions (só sequencia — caller 09-05 usa o número na transação da decisão); parametrização HU-014 com **default inline TVL/6** (não depende do seeder 09-01). `unique(tvl_product_number)` é a defesa final.
- **`ViabilityRequestStateMachine` só ESTENDIDA** (mapa final): `protocolada → {cancelada, em_analise, deferida, indeferida, aguardando_bap}`; `aguardando_bap → {deferida, indeferida, em_analise}`. Decisão é FINAL (sem saída). Anti-regressão travada: `cancelada→deferida`, `protocolada→rascunho`, `rascunho→em_analise` seguem INVÁLIDAS; os 5 testes da Fase 8 intactos.
- TDD estrito (RED→GREEN fresco): `ExpressoSchemaTest` 3/3, `ViabilityDecisionTest` 6/6, `ViabilityRequestStateMachineTest` 10/10 (5 Fase 8 + 5 novos), `TvlNumberGeneratorTest` 4/4 (SQLite), `TvlNumberGeneratorPostgisTest` 1/1 (concorrência real, `POSTGIS_TESTS_REQUIRED=true`). **Suíte completa 887/887 (4569 asserções, @group postgis)** — zero regressão. Commits `6a85838` (schema) / `30d79c8` (domínio+transições, inclui fix do índice unique 1:1) / `c2b25ff` (TvlNumberGenerator).
- **Desvio (Rule 1 — bug)**: na migration o `->unique()` estava após `->constrained()` (recaía na FK, não criava o índice da coluna); movido para antes — 1:1 garantida (provada por QueryException na 2ª decisão).
- **Insumo direto**: 09-05 (cria decisão+TVL+transição na transação), 09-07/08/09 (carregam a ViabilityDecision via decision()), 09-10 (aguardando_bap→indeferida + grava bap_* via forceFill), 09-11 (exibe a decisão). Consumido JÁ pelo 09-03 (contratos/evento usam ViabilityDecision).

#### 09-03 (contratos bloqueados Regin/SEFAZ/BAP + evento ResultadoEmitido) — concluído 2026-06-14

- **Fundação de integração desacoplada (Track C da Wave 1)**: as transmissões de saída do expresso vivem atrás de interface, com provider `Unavailable` que degrada HONESTO (espelha `PropertyRegistryLookup`) — ZERO fachada, ZERO dependência nova.
  - `App\Services\Regin\ReginParecerNotifier::notifyParecer(ViabilityRequest, ViabilityDecision): void` (HU-104) → `UnavailableReginParecerNotifier` LANÇA `ReginUnavailableException`.
  - `App\Services\Sefaz\SefazViabilidadeGateway::sendViabilidade(ViabilityRequest, ViabilityDecision): void` (HU-110, SÓ deferimento) → `UnavailableSefazViabilidadeGateway` LANÇA `SefazUnavailableException`.
  - `App\Services\Regin\BapRegistry::findLinkage(ViabilityRequest): ?BapLinkage` (HU-134) → `UnavailableBapRegistry` retorna `null` (sem vínculo, nada entra em `aguardando_bap`). DTO readonly `BapLinkage(string $bapNumber, DateTimeImmutable $linkedAt)` — `bap_due_at` é derivado em 09-10.
- **3 bindings** no `AppServiceProvider::register()` apontando para os `Unavailable` (Fase 13 troca SÓ o binding, sem tocar call sites/listeners). Exceções carregam `?protocolNumber`/`?motivo` para o audit de pendência dos listeners 09-08/09 (capturam e auditam 'bloqueado', nunca sucesso).
- **SEGUNDO evento de domínio** `App\Events\ResultadoEmitido implements ShouldDispatchAfterCommit` (espelha `SolicitacaoProtocolada`): `__construct(public ViabilityRequest $request, public ViabilityDecision $decision)`. Base desacoplada dos efeitos da Wave 4 (listeners AUTO-DESCOBERTOS — NÃO `Event::listen`); a auditoria autoritativa da decisão (HU-078) é SÍNCRONA e não depende do evento.
- TDD estrito (RED→GREEN fresco): `BlockedIntegrationContractsTest` 6/6, `ResultadoEmitidoEventTest` 3/3 (filtro combinado 9/9, 10 asserções). Suíte completa SQLite 867/867 (4458 asserções). Commits `615f9b7` (contratos+bindings) / `86eecd2` (evento).
- **Dependência cross-plan resolvida**: os contratos e o evento consomem `ViabilityDecision` (09-02). Aguardei o 09-02 publicar o model+factory antes do TDD (RED pelo motivo certo). Boundary respeitado (só meus arquivos + AppServiceProvider, staging individual).
- **Insumo direto**: 09-05 (dispara `ResultadoEmitido`), 09-07 (listener notificação), 09-08 (consome `ReginParecerNotifier`), 09-09 (consome `SefazViabilidadeGateway`), 09-10 (consome `BapRegistry`/`BapLinkage` + emite `ResultadoEmitido`), Fase 13 (liga Regin/SEFAZ/BAP reais trocando 3 bindings).

#### 09-04 (extração do SolicitacaoViabilityResolver + DTO ResolvedViability) — concluído 2026-06-14

- **`SolicitacaoViabilityResolver::resolve(ViabilityRequest): ResolvedViability`** (app/Services/Solicitacao): núcleo reutilizável EXTRAÍDO de `SimulacaoSolicitacaoService::simulate`. Itera os CNAEs (`orderedCnaes`: principal primeiro), reusa o motor da Fase 7 PELO PONTO (centroide do polígono — `consultarPorPontoConhecido`; sem polígono degrada para `consultarPorCnae`), PROPAGA o veredito LOUOS + o encaminhamento de risco por CNAE (RN-001), consolida o PIOR CASO (`SEVERIDADE` nao_permitido>pendente>permitido_com_condicoes>permitido) e coleta `rules_versions`. **NÃO persiste, NÃO audita, NÃO decide** — insumo puro (só injeta `ConsultaViabilidadeService`).
- **`ResolvedViability` (final readonly DTO)**: `por_cnae[]` (cada item com `cnae`, `cnae_formatado`, `is_primary`, `tendencia`, `tendencia_label`, `fluxo` 'expresso'|'analise', `consulta` = ConsultaViabilidadeResult **objeto** para a decisão ler fresco, e `consulta_array` = toArray para o snapshot), `consolidado`, `rules_versions` (territorio/louos/risco), `ponto`, `area_m2`. Métodos: `elegivelExpresso()` (HU-073 — todos em 'expresso'; qualquer 'analise' ou zero CNAEs → false; elegibilidade por ENCAMINHAMENTO, não pelo veredito) e `toSnapshot()` (shape IDÊNTICO ao da Fase 8: `consulta` = toArray).
- **`SimulacaoSolicitacaoService` refatorado**: agora injeta o resolver (dropou `ConsultaViabilidadeService`), monta o snapshot via `$resolved->toSnapshot()` e mantém a camada orientativa (persiste snapshot/versões/resultado/simulated_at + audita RN-002/RN-003). Encolheu 198→81 linhas; `consolidar`/`centroid`/`orderedCnaes`/`SEVERIDADE` MOVIDOS (não duplicados).
- TDD estrito (RED→GREEN fresco): `SolicitacaoViabilityResolverTest` 10/10 (36 asserções; vereditos REAIS via Quadro 10 — permitido vs proibido→nao_permitido; degradação sem polígono; rules_versions; não-persiste). **Anti-regressão Fase 8** (Simulacao/Wizard/Smoke/Golden/ConsultaViabilidade) 43/43 sem alterar expectativas. Suíte completa SQLite 867/867 (4458 asserções). Commits `964616a` (feat resolver+DTO+teste) / `3ba3944` (refactor serviço). ZERO dependência nova.
- **Insumo direto do 09-05** (decisão autoritativa): reexecuta `resolve()` fresco e lê `elegivelExpresso()` + `consolidado` + `por_cnae[].consulta` (objeto) — nunca confia no `simulation_snapshot` pré-protocolo. Boundary respeitado: não tocou ParameterSeeder (09-01), schema/StateMachine/ViabilityRequest (09-02) nem contratos/evento (09-03).

#### 09-05 (FluxoExpressoService — o MOTOR de decisão automática, NÚCLEO da fase) — concluído 2026-06-14

- **`App\Services\Expresso\FluxoExpressoService::decide(ViabilityRequest, ?User): DecisionResult`** — o CORE VALUE do SILE. AUTORITATIVO e idempotente: roda sob `Cache::lock("expresso:decisao:{id}", expresso.lock.ttl_segundos=10)->block(...)`; dentro do lock RECARREGA e re-checa `status === Protocolada` (se já saiu, devolve a decisão existente/estado atual com `emitted=false`, sem redecidir). REEXECUTA `SolicitacaoViabilityResolver::resolve($request)` FRESCO — **nunca** o `simulation_snapshot` pré-protocolo (orientativo/stale).
- **Roteamento (HU-073):** toggle `features.fluxo_expresso` off → `em_analise` ('fluxo expresso desativado'); `!elegivelExpresso()` (algum CNAE em 'analise') → `em_analise` (semi-expresso, RN-008); **CRÍTICO anti-fachada** — `consolidado === pendente` (sem a zona/Quadro 10) → `em_analise` SEM criar `ViabilityDecision` e SEM disparar `ResultadoEmitido` (o motor NÃO defere/indefere sem o dado real; liga sozinho quando a base oficial entrar). `encaminharAnalise` transiciona `protocolada→em_analise` e AUDITA síncrono ('expresso'/'decisao', result 'analise') além da transição.
- **Decisão (HU-074/075/076, RN-009):** `nao_permitido`→INDEFERE; `permitido`/`permitido_com_condicoes`→DEFERE. Numa `DB::transaction`: cria a `ViabilityDecision` imutável (flow 'expresso', outcome, consolidated_result, per_cnae [cnae/tendencia/fluxo/fundamentação], rules_versions, fundamentação consolidada, reason null, decided_by_user_id=actor?->id null=sistema, decided_at), **número TVL só no deferimento** (`TvlNumberGenerator`), transição pela `StateMachine` (timeline+auditoria) e **auditoria SÍNCRONA HU-078** (logName 'expresso'/event 'decisao'/result deferida|indeferida + rules_version representativa + por_cnae). `ResultadoEmitido::dispatch($request,$decision)` SÓ **após o commit**. A auditoria autoritativa NÃO depende do evento (provado com `Event::fake`, lição Fase 8).
- **Robustez idempotente sem fachada:** corrida rara que escape do `Cache::lock` (cache não compartilhado) é barrada por `unique(viability_request_id)`; a `QueryException` vira NO-OP (recarrega a decisão real já gravada, `emitted=false` — não redispara o evento), jamais deixa o job falhar nem inventa resultado. Sem decisão existente, a exceção propaga (job falha honesto).
- **`DecisionResult` (final readonly):** `status` (ViabilityRequestStatus final) + `decision` (?ViabilityDecision) + `reason` (?string) + `emitted` (bool). Helpers `paraAnalise(reason)` / `decidida(decision, emitted=true)`. Insumo de 09-06 (job/comando) e 09-11 (UI).
- TDD estrito (RED→GREEN fresco): `FluxoExpressoElegibilidadeTest` 4/4 (24 — inclui o teste CRÍTICO sem-zona), `FluxoExpressoDecisaoTest` 4/4, `FluxoExpressoAuditoriaTest` 2/2 (44 combinado), `FluxoExpressoIdempotenciaPostgisTest` 1/1 (@group postgis real — 1 decisão/1 TVL/1 evento). Filtro do plano 10/10 (68). **Suíte completa SQLite 877/877 (4526 asserções)** + **grupo postgis 21/21 (121 asserções)** — zero regressão. Commits `f64aae2` (elegibilidade) / `8693668` (defere-indefere) / `b657e1f` (idempotência postgis). ZERO dependência nova.
- **Insumo direto:** 09-06 (DecidirFluxoExpressoJob/`expresso:decidir` chamam `decide()`, idempotente — reprocesso seguro), 09-07/08/09 (listeners no `ResultadoEmitido`), 09-10 (reusa a emissão p/ indeferimento sem atuação BAP), 09-11 (exibe a `ViabilityDecision`).

#### 09-06 (gatilho + orquestração: listener → job + scheduler de segurança) — concluído 2026-06-14

- **Gatilho AUTO-DESCOBERTO** `App\Listeners\AvaliarFluxoExpresso` no `SolicitacaoProtocolada` (Fase 8): apenas DESPACHA o `DecidirFluxoExpressoJob` (NÃO decide inline — protocolo rápido + resiliência da fila). Registro ÚNICO por auto-descoberta (`event:list` mostra `RegistrarTrilhaProtocolo` + `AvaliarFluxoExpresso`); NUNCA `Event::listen` (lição Fase 8 — RN-002). Travado por CONTAGEM: 1 job por protocolo.
- **`App\Jobs\DecidirFluxoExpressoJob` (ShouldQueue)**: `__construct(public readonly int $viabilityRequestId)`; `tries`/`timeout`/`backoff` E a FILA lidos de `config('sile.expresso.job.*'|'.fila')` no construtor (fila = fonte ÚNICA — gatilho e rede de segurança enfileiram igual; 'default' não sobrescreve). `handle(FluxoExpressoService)`: carrega a request, guard idempotente (null OU status != Protocolada → no-op; o service também re-checa sob lock) → `decide()`. `failed(?Throwable)` AUDITA 'expresso'/'decisao-falha' result 'falha' com id+erro (RN-002, FA-03 — nunca silenciosa, jamais decisão falsa).
- **Rede de SEGURANÇA** `expresso:reavaliar {--limit=}` (auto-descoberto; NÃO é o gatilho principal): varre `status===protocolada` + `whereDoesntHave('decision')` (órfãs) e redespacha o MESMO job (reusa o caminho real; não decide inline). No-op honesto sem órfãs ("Nenhuma solicitação pendente de decisão.", exit 0). Agendado em `routes/console.php`: `Schedule::command('expresso:reavaliar')->everyTenMinutes()->withoutOverlapping()->onOneServer()` (cadência técnica). Idempotente (o service é).
- **Harness de teste** (não toca a Fase 8): a fila da suíte é `sync` (phpunit.xml), então o job rodaria INLINE no protocolo e a decisão (ASSÍNCRONA em produção, fora do request) mudaria o status logo após protocolar — 12 testes da Fase 8 quebrariam. O `TestCase` base fakeia SÓ o `DecidirFluxoExpressoJob` por padrão (`$fakeExpressoDecisionJob`); opt-out (false) no `ImportRedesimJobTest` (worker real). **Nenhum arquivo nem lógica da Fase 8 alterado.**
- TDD estrito (RED→GREEN fresco): `AvaliarFluxoExpressoListenerTest` 3/3 (contagem 1 job + id + fila parametrizada), `DecidirFluxoExpressoJobTest` 6/6 (tries/backoff de config; decisão REAL via service; no-op fora de protocolada/inexistente; failed audita), `ExpressoReavaliarCommandTest` 3/3 (só órfãs; no-op; --limit). Filtro do plano 12/12 (30). **Suíte completa SQLite 889/889 (4556 asserções)** + **grupo postgis 21/21 (121)** — zero regressão. Commits `5ba0979` (gatilho+job) / `f8a4db0` (comando+scheduler+refactor da fila) / `e97d2a4` (isolamento de teste). ZERO dependência nova.
- **Insumo direto:** 09-12 (smoke protocolar→decidir — processa a fila ou desliga o fake do harness). Wave 4 (09-07/08/09) segue no `ResultadoEmitido` com o mesmo padrão de auto-descoberta.

#### 09-07 (efeito HU-077: notifica o resultado ao cidadão por e-mail — sem anexo de TVL) — concluído 2026-06-14

- **Listener AUTO-DESCOBERTO (SÍNCRONO)** `App\Listeners\NotificarResultadoExpresso` no `ResultadoEmitido` (Wave 4, track ‖): notifica o requerente do desfecho por e-mail complementar. Registro ÚNICO por auto-descoberta (`event:list` mostra os 3 listeners do `ResultadoEmitido`: `NotificarResultadoExpresso` + `ComunicarResultadoRegin` + `EnviarViabilidadeSefaz`); NUNCA `Event::listen` (lição Fase 8 — RN-002), travado por CONTAGEM (`assertSentToTimes` 1×). Listener síncrono; quem enfileira é a notificação (ShouldQueue) — padrão da casa.
- **`App\Notifications\ResultadoExpressoNotification` (mail, ShouldQueue)** espelha `VerifyEmailQueued`/`EmailLog`: `__construct(string $protocolNumber, DecisionOutcome $outcome)`; assunto parametrizável por desfecho (`expresso.notificacao.assunto_deferida`/`_indeferida` via `Settings::get` — efeito sem deploy HU-014), corpo pt-BR (protocolo + resultado + orientação Regin/Junta), `emailLogId` + `failed()`. **SEM anexo de TVL (RN-004)** — o canal oficial de entrega do documento é Regin/SEFAZ (decisão SEDUR); nenhuma chamada de anexação na notificação.
- **Anti-fachada / degradação COMUNICADA:** `EmailLog` criado no DISPARO (status 'na_fila'; `LogNotificationSent` marca 'enviado' no worker — envio percorre a infra real). Toggle `features.notificacao_resultado_expresso` off → NÃO envia e AUDITA (`notificacoes`/`resultado-expresso`, result `desativado`); `request->requester` nulo/e-mail em branco → audita `sem-destinatario` (honesto, jamais finge envio).
- TDD estrito (RED→GREEN fresco): `ResultadoExpressoNotificationTest` 7/7 (enfileirável; canal mail; assunto por outcome; override administrável; nº no corpo; SEM anexo), `NotificarResultadoExpressoListenerTest` 4/4 (notifica 1×/EmailLog; toggle off audita; indeferida usa assunto; sem destinatário audita). Filtro combinado 11/11 (15 asserções). **Suíte completa SQLite 906/906 (4590 asserções)** — zero regressão, com o listener rodando de verdade no caminho real do `ResultadoEmitido`. Commits `cc7ce5b` (notificação) / `315d984` (listener). ZERO dependência nova.
- **Boundary Wave 4 respeitado:** criados só `ResultadoExpressoNotification` + `NotificarResultadoExpresso` + 2 testes; NÃO toquei Regin (09-08) nem SEFAZ (09-09); staging individual. Commits intercalados com 09-08/09-09 na MESMA working dir, sem conflito.
- **Insumo direto:** 09-12 (smoke: deferimento navegável dispara a notificação + trilha `notificacoes`/`resultado-expresso`), EP11 (canais plenos WhatsApp/in-app plugam no mesmo `via()`), Fase 13 (entrega oficial do TVL via Regin/SEFAZ — este e-mail NUNCA entrega o documento).

#### 09-08 (efeito HU-104: comunica o parecer ao Regin/Junta — bloqueado honesto) — concluído 2026-06-14

- **Listener AUTO-DESCOBERTO** `App\Listeners\ComunicarResultadoRegin` (ShouldQueue) no `ResultadoEmitido` (Wave 4, track ‖): tenta comunicar o parecer — deferido OU indeferido (HU-076 RN-008: vai ao Regin nos dois casos) — via o contrato `ReginParecerNotifier::notifyParecer`. Registro ÚNICO por auto-descoberta (`event:list` mostra `ResultadoEmitido ⇂ ComunicarResultadoRegin@handle (ShouldQueue)`); NUNCA `Event::listen` (lição Fase 8 — RN-002), travado por CONTAGEM.
- **BLOQUEADO honesto (anti-fachada):** o binding atual `UnavailableReginParecerNotifier` LANÇA `ReginUnavailableException`; o listener CAPTURA e AUDITA a pendência (`integracoes`/`regin-parecer`, result `bloqueado`, subject=solicitação, properties viability_request_id/protocolo/outcome/erro) — NUNCA sucesso fictício, NUNCA simula o envio. A exceção **não é relançada**: a decisão (gravada/auditada síncrona, HU-078) jamais falha por causa da integração pendente; a pendência `bloqueado` é a falha visível (outbox/trilha).
- **SUCESSO ligável (Fase 13):** caminho implementado e provado com um fake do notifier SÓ no teste (`app()->instance`) — chama `notifyParecer` e audita result `sucesso` (mesmo logName/event, result distinto). A Fase 13 troca SÓ o binding no `AppServiceProvider`; este listener NÃO muda.
- TDD estrito (RED→GREEN fresco): `ComunicarResultadoReginListenerTest` 3/3 (10 asserções — bloqueado audita 1×/sem sucesso falso; sucesso com fake; indeferimento também comunica). **Suíte completa SQLite 902/902 (4583 asserções)** — zero regressão, com o listener rodando de verdade no caminho real do `ResultadoEmitido` (testes de decisão não-fakeados). Commits `284d5d2` (test) / `55f63a8` (feat). ZERO dependência nova.
- **Boundary Wave 4 respeitado:** criados só `ComunicarResultadoRegin` + seu teste; NÃO toquei notificação (09-07) nem SEFAZ (09-09); staging individual.
- **Insumo direto:** 09-10 (indeferimento sem atuação BAP → `ResultadoEmitido` aciona este listener → Regin, pendência auditada, sem código novo), 09-12 (smoke encontra a trilha `integracoes`/`regin-parecer` result `bloqueado`), Fase 13 (liga o Regin real trocando 1 binding → audita `sucesso`).

#### 09-09 (efeito HU-110: envia viabilidade à SEFAZ só no deferimento — bloqueado honesto) — concluído 2026-06-14

- **Listener AUTO-DESCOBERTO** `App\Listeners\EnviarViabilidadeSefaz` (ShouldQueue) no `ResultadoEmitido` (09-03): envia a viabilidade à SEFAZ municipal via `SefazViabilidadeGateway::sendViabilidade` SÓ no DEFERIMENTO. Guarda HU-134 RN-003 ANTES de tocar o gateway: indeferida → audita 'ignorado' e retorna (não aciona a SEFAZ). `event:list` confirma o registro ÚNICO (`ResultadoEmitido ⇂ EnviarViabilidadeSefaz@handle (ShouldQueue)`); NUNCA `Event::listen` (lição Fase 8 — RN-002), travado por CONTAGEM (1 auditoria por resultado emitido).
- **Bloqueio honesto (anti-fachada):** binding default `UnavailableSefazViabilidadeGateway` LANÇA `SefazUnavailableException`; o listener CAPTURA e AUDITA pendência (`integracoes`/`sefaz-viabilidade`/result `bloqueado`, properties viability_request_id/protocolo/outcome, subject=solicitação) — nunca finge envio. NÃO relança (a decisão já efetivada/auditada síncrona HU-078 não falha pela integração bloqueada). Caminho 'sucesso' provado com gateway disponível (spy). A Fase 13 liga a SEFAZ trocando SÓ o binding, listener intacto.
- TDD estrito (RED→GREEN fresco): `EnviarViabilidadeSefazListenerTest` 3/3 (9 asserções — deferida+bloqueado audita 'bloqueado' sem quebrar o fluxo; deferida+spy chama sendViabilidade e audita 'sucesso'; indeferida zero chamadas + 'ignorado'). **Suíte completa SQLite 902/902 (4583 asserções)** — zero regressão (listener roda em todo dispatch de ResultadoEmitido). Commit `7e84037` (feat). ZERO dependência nova.
- **Coordenação Wave 4:** toquei SÓ `EnviarViabilidadeSefaz` + teste (staging individual); 09-07 (notificação) e 09-08 (Regin) permaneceram intactos. Três listeners coexistem no MESMO `ResultadoEmitido` sem duplicar (isolamento por event 'sefaz-viabilidade').
- **Insumo direto:** 09-10 (indeferimento sem atuação BAP NÃO aciona SEFAZ — este listener ignora), 09-12 (smoke registra a pendência SEFAZ só no deferimento), Fase 13 (liga SEFAZ trocando o binding).

#### 09-10 (HU-134 DORMENTE: indeferir por prazo BAP — rotina + parâmetro + estado, no-op honesto) — concluído 2026-06-14

- **Seam `BusinessDeadlineCalculator`** (`app/Services/Expresso`): `dueAt(\DateTimeInterface $from, int $hours): Carbon` (HOJE horas-CALENDÁRIO via `addHours`; a HU-137 troca por dias úteis/feriados AQUI sem tocar nenhum call site) + `isOverdue(\DateTimeInterface $dueAt, ?$now): bool`. Prazo parametrizável `expresso.bap.prazo_horas` (48, 09-01). NÃO inventa calendário de feriados.
- **`IndeferirSemBapService::indeferir(ViabilityRequest): ViabilityDecision`** — guarda `status===AguardandoBap` (senão `InvalidArgumentException`, sem criar nada). `DB::transaction`: cria `ViabilityDecision` (flow 'expresso', outcome Indeferida, **`consolidated_result` 'sem_atuacao_bap'**, `per_cnae`/`rules_versions`/`fundamentacao` VAZIOS — o indeferimento é PROCEDIMENTAL, não veredito locacional 'nao_permitido'; `reason` 'indeferido sem atuação'; `decided_by_user_id` null) + transição `aguardando_bap→indeferida` (StateMachine: timeline+auditoria) + auditoria SÍNCRONA 'expresso'/'decisao' result 'indeferida'. APÓS o commit: `ResultadoEmitido::dispatch` → Regin comunica (bloqueado Fase 13), SEFAZ IGNORA (RN-003 — listeners da Wave 4).
- **Comando `expresso:indeferir-sem-bap`** (auto-descoberto): lê `expresso.bap.prazo_horas`, varre `aguardando_bap` cujo vencimento (`bap_due_at` ?? derivado de `bap_linked_at`+prazo via `dueAt`) já passou (`isOverdue`) e indefere cada uma. Agenda `hourly()->withoutOverlapping()->onOneServer()` (routes/console.php) — reprocessável (RN-004); status `indeferida` impede reprocesso do já decidido (idempotência natural).
- **DORMENTE / anti-fachada**: NADA coloca solicitações em `aguardando_bap` hoje (BapRegistry indisponível, Fase 13) → em produção a varredura é NO-OP (zero). Provado: dev `php artisan expresso:indeferir-sem-bap` → "Nenhum processo aguardando BAP vencido"; teste no-op com protocolada ativa → ZERO `ViabilityDecision`. O motor é REAL e testado AGORA com seed (`ViabilityRequestFactory::awaitingBap(?Carbon $dueAt)` — `bap_due_at` vencido por padrão, `bap_linked_at` null).
- TDD estrito (RED→GREEN fresco): `IndeferirSemBapTest` 3/3 (indefere vencida + reason + transição + evento; seam de prazo; guarda de estado) + `ExpressoIndeferirSemBapCommandTest` 3/3 (no-op dormente zero decisões; indefere SÓ as vencidas; Regin 'bloqueado' + SEFAZ 'ignorado' com listeners reais). Filtro combinado 6/6 (28 asserções). **Suíte completa SQLite 912/912 (4618 asserções)** — zero regressão. `--help` e `schedule:list` confirmam o comando/agenda. Commits `ab58f6c` (calculadora+serviço+factory) / `6b6f09b` (comando+agenda). ZERO dependência nova.
- **Bloqueio honesto registrado (Fase 13 / HU-137):** a ATIVAÇÃO real (algo alimentar `bap_due_at` e colocar em `aguardando_bap`) depende do conector Regin (Fase 13 — `BapRegistry`/`UnavailableBapRegistry::findLinkage`→null); a contagem em DIAS ÚTEIS depende da HU-137 (seam pronto, horas-calendário até lá). Insumo do fechamento 09-12.

#### 09-11 (retaguarda do resultado expresso: lista + detalhe somente leitura) — concluído 2026-06-14

- **Tela do console SEDUR para a decisão automática (HU-076/HU-078)** — `routes/gestao.php` sob `permission:consultar-solicitacoes` (**REUSO — ZERO permissão nova; permanecem 19**; a decisão é parte da solicitação, decisão a validar com a SEDUR): `gestao.resultados-expresso.index` (`GET resultados-expresso`) + `gestao.resultados-expresso.show` (`GET resultados-expresso/{viabilityRequest}`). SOMENTE LEITURA (sem store/update/destroy).
- **`App\Http\Controllers\Gestao\ResultadoExpressoController`** (espelha RiscoController, server-driven): `index` lista SÓ decididas via **join em `viability_decisions`** (`->through()` projeção leve — padrão da casa), filtros por `outcome` (deferida/indeferida), busca (protocolo/TVL, case-insensitive) e período `data_de`/`data_ate` (`decided_at`), ordenado por `decided_at desc`, paginação server-side (reusa `ui.cnaes.per_page`; PER_PAGE_OPTIONS [10,15,25,50]); audita a consulta (`expresso`/`consulta-resultado-lista`). `show` carrega decision+company, **404 se sem decisão** (em_analise — sem inventar desfecho), audita (`expresso`/`consulta-resultado`).
- **`App\Http\Resources\ViabilityDecisionResource`** (1º Eloquent API Resource do projeto): payload de leitura honesto — outcome(+label), consolidated_result(+label via `ResultadoViabilidade`), tvl_product_number, per_cnae, rules_versions, fundamentacao, reason, decided_at, decided_by (null=sistema) + resumo da solicitação (protocolo/empresa/CNPJ/endereço). **Consumido com `->resolve()`** no controller (Inertia serializa `JsonResource` como `Responsable` → wrapper `data`; `->resolve()` entrega o array puro). O `index` NÃO usa o resource (projeção via join preserva o shape do paginator que o `Pagination`/`useServerTable` esperam) — decisão registrada.
- **Status de transmissão Regin/SEFAZ HONESTO (anti-fachada)**: lido da trilha `Activity` (`log_name 'integracoes'`, event `regin-parecer`/`sefaz-viabilidade`), resultado mais recente por canal → `bloqueado`⇒**pendente** (canal bloqueado, Fase 13), `sucesso`⇒transmitido, `ignorado`⇒não se aplica, sem registro⇒aguardando. NUNCA exibe "enviado" sem sucesso real. `show.tsx` traz nota explícita: canal oficial = Regin/SEFAZ; PDF/TVL é HU-132 (Fase 10).
- **Páginas Inertia/React** (padrão TailAdmin, ZERO dependência nova): `gestao/resultados-expresso/index.tsx` (DataTable + TableToolbar + filtros/chips + Pagination + EmptyState distinguindo busca vazia) e `.../show.tsx` (detalhe read-only: cabeçalho com outcome/TVL/decidido por; resultado consolidado + motivo; decisão por CNAE; versões de regra — achatando shape aninhado/plano; fundamentação; transmissão Regin/SEFAZ; nota de canal/PDF). Criadas no commit da Task 1 porque o `assertInertia(->component())` exige o arquivo em disco (`inertia.testing.ensure_pages_exist=true`).
- TDD estrito (RED→GREEN fresco): `ResultadoExpressoConsoleTest` 6/6 (56 asserções) — 403 sem permissão (auditado), lista só decididas (protocolada sem decisão não aparece), filtro por resultado, show com TVL/per_cnae, show 404 sem decisão, transmissão Regin/SEFAZ pendente da auditoria. **Suíte completa SQLite 918/918 (4674 asserções)** — zero regressão (912→+6). `vendor/bin/pint --dirty`, `npx tsc --noEmit` e `npm run build` verdes. Commit `0e22e8a` (feat). ZERO dependência nova.
- **Insumo direto**: 09-12 (smoke navegável encontra a tela e a trilha Regin/SEFAZ 'bloqueado') e Fase 10/HU-132 (o PDF/TVL sai da MESMA fonte — `ViabilityDecision` exposta pelo resource). **Follow-up (fora do escopo deste plano):** link na navegação do console (sidebar) para a tela — hoje acessível por URL/rota nomeada; sugerido para 09-12 ou ajuste de UX.
- **CHECKPOINT HUMANO sugerido (não bloqueia):** abrir `/gestao/resultados-expresso` autenticado com `consultar-solicitacoes`, conferir lista/filtros, abrir um deferimento (TVL + por CNAE + versões + fundamentação) e ver a transmissão Regin/SEFAZ como **pendente**; mobile (375px) + dark mode. Roteiro no `09-11-SUMMARY.md`.

#### 09-12 (fechamento: seeds dev + zona fictícia + expresso:decidir + golden/smoke + verificação integral) — concluído 2026-06-14

- **Zona fictícia SÓ dev/teste (entrega-funcional — dado fictício, LÓGICA REAL)**: `ZonaFicticiaDevSeeder` carrega uma feição de zona com o código REAL `ZCN-1` (que o Quadro 10 já mapeia para `nR1`→permitido) sobre um polígono do Centro de Salvador, para o motor REAL DEFERIR de verdade. O fictício é a geometria/proveniência (versão `zona-ficticia-dev`, origem/propriedades rotuladas), NUNCA a regra. **Gate de ambiente** (`local`/`testing` — em produção é no-op) + **driver-aware** (geometria só pgsql; usa `GeoJsonLayerImporter` que fecha a camada `pendente_fonte` para a fictícia virar a vigente — senão `vigente(Zona)->first()` devolveria a pendente, pois o scope só filtra `valid_to` nulo, não status).
- **`ExpressoDevSeeder`** (gated + driver-aware): protocola (ProtocolarSolicitacaoService) e DECIDE (FluxoExpressoService, explícito em pgsql porque a fila dev é redis/async) dois exemplos do `cidadao@sile.dev` — um DEFERIMENTO sobre a zona fictícia (Centro → TVL) e um EM ANÁLISE honesto (Pituba, sem zona, sem decisão). Idempotente por marcador. Registrados no `DatabaseSeeder` (Zona antes do Expresso).
- **Comando `expresso:decidir {solicitacao}`** (auto-descoberto): roda o motor real e imprime status/desfecho/TVL/fundamentação; em_analise sai com exit 0 (degradação honesta não é erro), inexistente exit 1. Idempotente (re-check sob lock), ator sistema.
- **Golden + smoke**: `ExpressoGoldenCaseTest` (#[DataProvider] + 5 fixtures JSON) trava os três caminhos + condicionado + semi-expresso (alto risco) sobre o motor real. `ExpressoSmokeTest` exercita a CADEIA AUTOMÁTICA REAL (`$fakeExpressoDecisionJob=false`): protocolar→evento→listener→job→decide→efeitos — deferimento (TVL + Notification::fake + Regin/SEFAZ `bloqueado`), em_analise sem zona (assertNotDispatched(ResultadoEmitido) + sem notificação — anti-fachada), indeferimento (SEFAZ `ignorado` RN-003). `ExpressoSeedPostgisTest` prova o deferimento sobre a zona REAL (PostGIS).
- **Link de navegação** no console (`gestao-layout`) para `resultados-expresso` (gate `consultar-solicitacoes`) — fecha o follow-up de UX do 09-11.
- **Verificação integral FRESCA verde**: `vendor/bin/pint --test` limpo; suíte SQLite **931/931 (4747 asserções)** (baseline 918 + 13: comando 4, seeder +1, golden 5, smoke 3); `@group postgis` **22/22 (131)** (baseline 21 + `ExpressoSeedPostgisTest`); `npx tsc --noEmit` e `npm run build` verdes. Evidência dev (após `migrate`+`db:seed`): `expresso:decidir 8`→**DEFERIDA + TVL-2026-000001** (fundamentação Quadro 7/10 + Decreto 32.636/2020 + risco sanitário), `expresso:decidir 9`→**EM ANÁLISE**; auditoria síncrona `log_name 'expresso'`/`event 'decisao'` = 2 (1 deferida + 1 analise). Parâmetros 56 / permissões 19 (ZERO permissão e ZERO dependência nova). Commits `4e2497e` (comando) / `5e8c0c1` (seeders) / golden+smoke / `ae3f69c` (link).
- **CHECKPOINT HUMANO PENDENTE (único gate da fase)**: smoke navegável (`composer dev`) — roteiro no `09-12-SUMMARY.md` (deferimento navegável VIA-2026-000007 com TVL na retaguarda + notificação sem anexo + Regin/SEFAZ pendente; em_analise honesto VIA-2026-000008 sem decisão; mobile/dark).
- **Bloqueios/pendências que PERMANECEM (registrados, não simulados)**: transmissão Regin (HU-104) e SEFAZ (HU-110) → Fase 13; HU-134 ativa (prazo BAP) → Fase 13; TVL PDF (HU-132) → Fase 10; zona oficial (Quadro 10/SEDUR) — sem ela a maioria cai honestamente em em_analise; HU-137 (feriados) para o prazo BAP em dias úteis; canais plenos de notificação → EP11; política do "médio risco" e numeração oficial do TVL → SEDUR.

### Fase 8 (Solicitação de Viabilidade) — concluída 2026-06-14

- Aggregate `ViabilityRequest` (app/Models) com imóvel embutido 1:1; polígono `property_polygon_geojson` (jsonb, fonte) + `property_polygon` (geometry derivada driver-aware, só pgsql, GiST condicional — espelha GeoJsonLayerImporter da Fase 4). Satélites: viability_request_cnaes (is_primary), _documents (disk/path/sha256), _transitions (fonte da timeline). Cadastros parametrizáveis: viability_service_types, document_requirements + pivot cnae_document_requirement. protocol_sequences (contador por ano).
- Máquina de estados `ViabilityRequestStatus` (rascunho/protocolada/cancelada ATIVOS; aguardando_bap/em_analise/deferida/indeferida/em_pendencia GANCHOS, fora do mapa). `ViabilityRequestStateMachine` audita toda transição (RN-002) + grava transition (timeline). `ProtocolNumberGenerator` (VIA-AAAA-NNNNNN, lockForUpdate; teste de concorrência @group postgis).
- PRIMEIRO evento de domínio `SolicitacaoProtocolada` (app/Events, ShouldDispatchAfterCommit) + listener `RegistrarTrilhaProtocolo` (AUTO-DESCOBERTO em app/Listeners — NÃO registrar via Event::listen, duplicaria a auditoria; bug RN-002 corrigido nesta fase, fix af4750d). Auditoria do protocolo INDEPENDE do evento (transição síncrona garante a trilha).
- Fluxo: criar (ViabilityRequestPolicy reusa CurrentRepresentation Fase 1; DuplicateRequestDetector RN-007 por CNPJ, janela em config 180d) → imóvel/área (PropertyGeometryWriter + TerritoryService + LocationValidationService Fase 4; alerta área×polígono RN-004; markSimulationStale RN-005) → atividades/CNAEs (sync pivot, limite solicitacao.cnaes_complementares.max=99) → documentos (SolicitacaoDocumentoController: Storage disk parametrizado nunca público + sha256 + streaming autenticado anti-IDOR; DocumentRequirementResolver::missing() bloqueia o protocolo) → simular (SimulacaoSolicitacaoService reusa ConsultaViabilidadeService::consultarPorPontoConhecido — propaga veredito, não bloqueia; toggle features.simulacao_solicitacao) → protocolar (ProtocolarSolicitacaoService valida fora da transação; ciência applicant_proceeded_despite) → consultar (autenticado + público temporarySignedRoute + throttle:consulta-protocolo, payload mínimo LGPD; timeline das transitions) → cancelar (estados canceláveis em solicitacao.cancelamento.estados_cancelaveis).
- Canais operador: contingência HU-148 (Gestao\ContingenciaController, origin=contingencia auditada, MESMO ProtocolarSolicitacaoService — caminho REAL hoje; bloqueio reverte tudo) e atendimento presencial HU-150 (AssistedAttendance + ResolveAssistedAttendance populam o MESMO CurrentRepresentation; permissão atendimento-presencial). UI: wizard multi-etapas (rota portal.solicitacoes.create/edit DISTINTA da show) + Minhas solicitações; reusa MapaSection (Fase 4) e ResultadoViabilidade (Fase 7).
- Parâmetros: catálogo 49 (grupo solicitacao + features.* + storage.documentos.disk + seguranca.throttle.consulta_protocolo). Permissões 19 (registrar-contingencia, atendimento-presencial, consultar-solicitacoes, manter-tipos-servico, manter-requisitos-documentais). Seeds dev (ViabilityServiceTypeSeeder/DocumentRequirementSeeder com foto-fachada/termo-concessao/contrato-locacao; pivot por-CNAE VAZIO honesto; SolicitacaoDevSeeder 4 exemplos). Comando solicitacao:protocolar. Golden cases + smoke (#[DataProvider]).
- guardião APROVADO; suíte 848/848 (829 SQLite + 19 @group postgis), pint/typecheck/build verdes. Evidência real: VIA-2026-000006, bloqueio documental honesto, pendente sem zona, 1 auditoria protocolada por protocolo.
- BLOQUEADO → Fase 13 (degrada honesto, registrado): DAM HU-071/072 (SEFAZ; SILE consulta/exibe — NENHUM contrato especulativo criado) e origem Regin (contingência é o caminho real). ADIADO pendente SEDUR: HU-139 (edifício comercial — address_complement texto livre). Pendências SEDUR parametrizadas com default+ressalva: lista oficial de tipos de serviço, requisitos por CNAE (pivot vazio), prazo estimado (HU-129/Fase 15), zona urbanística (Quadro 10), ciência presencial (HU-150 RN-004), regra fina de cancelamento/duplicidade.
- Lição (RN-002): no Laravel 11/12 os listeners em app/Listeners são AUTO-DESCOBERTOS; registrá-los também via Event::listen DUPLICA a execução (auditoria 2×). Manter o registro único (auto-descoberta) e travar com teste por CONTAGEM, não só assertDatabaseHas.

### Fase 8 — 08-16 (fechamento: seeds dev + comando + golden/smoke + verificação integral) — implementado 2026-06-14

- **Seeds dev (dados fictícios, lógica REAL — entrega-funcional)**: `ViabilityServiceTypeSeeder` (4 tipos com códigos estáveis — lista oficial pendente SEDUR, substituível sem deploy), `DocumentRequirementSeeder` (requisitos-base por código usados pelo resolver: `foto-fachada` sempre + `termo-concessao` área pública + `contrato-locacao` opcional; pivot `cnae_document_requirement` VAZIO — planilha oficial pendente SEDUR, degradação honesta), `SolicitacaoDevSeeder` (4 solicitações de exemplo do `cidadao@sile.dev` — rascunho instruído, protocolada, cancelada, contingência — via `ProtocolarSolicitacaoService`/`CancelarSolicitacaoService` REAIS: número/timeline/auditoria genuínos). Registrados no `DatabaseSeeder` (catálogos antes do admin; `SolicitacaoDevSeeder` por último). Idempotentes (firstOrCreate por code; exemplos por marcador estável em `address_reference`). DECISÃO: simulação NÃO roda no seed (território exige PostGIS) para manter o seed SQLite-safe — não afeta o protocolo. DECISÃO: `factory()->draft()` (não `create()` cru) hidrata o `status` em memória (o default é só do banco) — com FKs explícitos para não criar empresa/usuário extra (Company count estável em 3).
- **Catálogo de parâmetros mantido em 49** (sem alteração nesta fase). `DatabaseSeederTest` cobre catálogos, exemplos e idempotência.
- **Comando de evidência** `solicitacao:protocolar {solicitacao} {--proceed-despite}` (auto-descoberto): protocola pelo serviço real e imprime empresa/tipo/atividade + número + transição; bloqueio honesto (documento/dados mínimos/transição inválida) sai com exit 1. Evidência real no dev pgsql: `VIA-2026-000004`, exit 0.
- **Golden + smoke** travam a regressão do fluxo: `SolicitacaoGoldenCaseTest` (#[DataProvider] + 4 fixtures JSON em `tests/Fixtures/golden/solicitacao/`) executando protocolar/cancelar/simular sobre o seed oficial; `SolicitacaoSmokeTest` dirige a jornada inteira pelos ENDPOINTS reais (criar→imóvel→atividades→documento→simular→protocolar→consultar→cancelar). Anti-fachada provado: faltar a foto da fachada BLOQUEIA o protocolo; sem zona a simulação fica PENDENTE (nunca permitido/não permitido). `SolicitacaoProtocolarCommandTest` cobre o comando (5).
- **Verificação integral FRESCA verde**: `vendor/bin/pint --test` limpo; `npx tsc --noEmit` + `npm run build` verdes; suíte 848/848 (829 SQLite + 19 @group postgis com `sile-pgsql` healthy; 4411 asserções). Baseline 835 → +13. Auditoria no dev: 13 entradas `solicitacoes`, 5 transições, 4 exemplos do cidadão. Commits 12cf547 (seeds) / f49800c (comando+golden+smoke).
- **CHECKPOINT HUMANO PENDENTE**: smoke navegável (composer dev → wizard cidadão → protocolo → consulta pública assinada + contingência/atendimento presencial no console). Ambiente de dev preparado (`php artisan migrate` aplicou as tabelas da Fase 8 no banco `sile`; `php artisan db:seed` populou catálogos + exemplos). Roteiro no `08-16-SUMMARY.md`.
- **Pendências SEDUR/bloqueios da fase (registrados, NÃO simulados)**: DAM HU-071/072 BLOQUEADO → Fase 13 (Success criteria 4 do ROADMAP); origem `regin` → Fase 13 (contingência HU-148 é o caminho real); HU-139 edifício comercial (texto livre); lista oficial de tipos de serviço; requisitos documentais por CNAE (pivot vazio); prazo estimado real (HU-129/Fase 15); zona urbanística (Quadro 10); ciência presencial (HU-150) e regra fina de duplicidade/cancelamento (HU-070).

### Fase 8 — 08-13 (UI da jornada do cidadão) — concluída 2026-06-14

- Lista "Minhas solicitações" (`portal/solicitacoes/index.tsx`): DataTable com status amigável (Badge por publicLabel), busca/paginação; por linha continuar (rascunho → wizard editar), consultar (→ protocolo 08-11) e cancelar (ConfirmDialog com motivo obrigatório → DELETE cancelar, só quando `cancelable` pelo parâmetro `solicitacao.cancelamento.estados_cancelaveis`); `duplicateAlert` (RN-007) com link ao processo anterior. `SolicitacaoController@index` ganhou `editable`/`cancelable` por linha + `duplicateAlert` da sessão.
- Wizard multi-etapas (`portal/solicitacoes/wizard.tsx`) com ROTA GET PRÓPRIA: `GET solicitacoes/nova` (`portal.solicitacoes.create`, literal ANTES de `{solicitacao}`) e `GET solicitacoes/{solicitacao}/editar` (`portal.solicitacoes.edit`, policy update — terceiro/protocolada 403), DISTINTAS da `GET solicitacoes/{solicitacao}` (08-11). `SolicitacaoController@create/@edit` reidrata o rascunho REAL do banco (imóvel/área/endereço/indicadores, CNAEs, anexos, snapshot de simulação) + dados de apoio (tipos ativos, empresas do dono) + requisitos (DocumentRequirementResolver) + config de anexos (HU-014). store/protocolar NÃO alterados (→ index; navegação via index-hub Continuar/Consultar).
- Etapas (`resources/js/components/solicitacao/etapa-*.tsx`), cada uma consome o endpoint real: início (tipo+empresa → store), imóvel (reusa MapaSection/MapImovel Fase 4; polígono de 4 pontos derivado do ponto+área; território honesto sem zona; alerta área RN-004 → PUT imovel), atividades (reusa CnaePicker/portal.cnaes.search; principal+complementares no limite parametrizável → PUT atividades), documentos (upload/remove + obrigatórios; faltantes AVISO → documentos.*), simulação (POST simular + ResultadoViabilidade/Fase 7 por CNAE; pendente com motivo; orientativa, não bloqueia), revisão/protocolar (resumo + bloqueio documental do botão + ciência `proceed_despite` na tendência de indeferimento → POST protocolar). Mobile-first/acessível (stepper aria-current). ZERO dependência nova.
- Evidência fresca: route:list (create/edit distintas da show), `npx tsc --noEmit` e `npm run build` verdes, `SolicitacaoWizardPaginaTest` 9/9, suíte completa 835/835 (4310 asserções, @group postgis com container healthy). Commits 0611c9e / 40041a4 / 901ef5f. CHECKPOINT HUMANO PENDENTE: verificação visual `composer dev` percorrendo o wizard (mapa, simulação honesta, bloqueio documental, protocolo) + mobile 375px + dark mode.

### Fase 7 (Consulta Prévia de Viabilidade) — concluída 2026-06-14

- PRIMEIRO fluxo de decisão de ponta a ponta. `ConsultaViabilidadeService` (app/Services/Viabilidade/) orquestra Geocoder→TerritoryService→LouosEnquadramentoService→RiscoClassificationService; `ConsultaViabilidadeResult` readonly (toArray snake_case, versoes() de território+louos+risco). O orquestrador NÃO recomputa o veredito — PROPAGA o consolidado do motor LOUOS (vereditoLocacional lê enquadramento->resultado()).
- 3 entradas: endereço (geocode real Nominatim → território → motores); CNAE (risco real + Quadro 7 por área, sem território); inscrição imobiliária BLOQUEADA atrás do contrato `PropertyRegistryLookup` (provider `UnavailablePropertyRegistryLookup` sempre lança; degrada para via CNAE com aviso, NUNCA inventa ponto; fake nos testes prova a lógica — Fase 13 troca o binding).
- Honesto sem zona: risco + Quadro 7 + restrições/bairro/via reais; veredito locacional = pendente (motivo "zona pendente SEDUR"); UI nunca mostra permitido/não permitido sem zona (golden endereco-sem-zona-pendente trava a regressão).
- Consulta PÚBLICA (/portal/viabilidade/* — index GET + endereco/cnae/inscricao POST) com throttle:consulta-viabilidade (parâmetro seguranca.throttle.consulta_viabilidade.por_minuto) + toggle features.consulta_viabilidade (422 comunicado) + auditoria 'viabilidade'/'consulta' (causer null quando anônimo + IP, versões das regras). Histórico AUTENTICADO (HU-060): tabela imutável viability_queries (input+result jsonb snapshot+rules_versions+user_id), gravada SÓ quando autenticado e SÓ no sucesso; usuário vê só o próprio (scopeForUser); UI mostra o snapshot da época em Modal (não reprocessa).
- UI portal light reusa o mapa Leaflet MapaSection/MapImovel (Fase 4). Comando viabilidade:consultar {cnae} {--area=}{--endereco=}. Golden cases (#[DataProvider]). Parâmetros novos (catálogo 33→35). ZERO dependência nova.
- BLOQUEADO pendente SEDUR (degrada honesto, registrado): veredito locacional permitido/não permitido (Quadro 10/zona — SIGIS/CA 2000); resolução por inscrição imobiliária (lote/Cadastro Multifinalitário — contrato pronto, binding trocado na Fase 13). HU-141 (simulação dentro da solicitação) reusa este service na Fase 8.
- Nota operacional: cache Redis stale do `composer run dev` de longa duração pode causar __PHP_Incomplete_Class em DTOs após mudanças — `php artisan cache:clear` resolve. Avaliar invalidação de cache de DTOs entre deploys (Fase 13).

### Fase 5 (Motor de Regras da LOUOS) — concluída 2026-06-14

- Quadros 7/10/11/11A como DADOS VERSIONADOS reusando `rule_versions`/`RuleVersionService` da Fase 6 (RuleDomain estendido com louos_quadro7|10|11|11a, todos sensíveis/4-olhos). HU-046 é essa reutilização.
- `LouosEnquadramentoService` espelha o TerritoryService/RiscoClassificationService: DTOs readonly EnquadramentoInput {area, cnaes, TerritoryResult} / EnquadramentoResult (cada Quadro = dimensão {status identificado|nao_encontrado|indisponivel} + consolidado {permitido|permitido_com_condicoes|nao_permitido|pendente} + fundamentacao[] + versoes() + toArray()). Resolução de versão 3 modos (vigente/naData/versao p/ sandbox). Auditoria 'louos'/'enquadramento' com rules_version.
- Quadro 7 (HU-015/038): seed REAL da Lei 9.148/2016 (40 faixas, 24 CNAEs; modelo enquadramento TVL do SAPS). Enquadra por área (faixa inclusiva; area_max nula = sem teto); CNAE sem faixa → nao_encontrado.
- DEGRADAÇÃO honesta (sem fachada): Quadro 10 (HU-016/039) lê zona do TerritoryResult; zona indisponivel → quadro10 indisponivel SEM consultar a tabela → consolidado PENDENTE (nunca permitido/nao_permitido sem zona). Quadros 11/11A (HU-017/018/040/041) degradam sem o atributo CLASSE_VIA_LOUOS. Consolidação HU-044 (precedência: Q7 ausente→pendente; Q10 indisponível→pendente; proibido→nao_permitido; condicionante→permitido_com_condicoes; senão permitido), fundamentação HU-045, vagas parametrizado HU-042 (louos.vagas.exigencia_por_grupo), restrições ZEIS HU-043 (camada da Fase 4).
- Sandbox HU-143: simula uma versão rascunho contra cenários reais (derivados dos CNAEs do Quadro 7 vigente) reexecutando o motor real em transação SEMPRE revertida (RN-001 — vigente intacta); publicação 4-olhos (autor≠publicador). Mantenedores HU-015..018 no console; comando louos:enquadrar; golden cases (#[DataProvider], requer_zona=pendente). Parâmetros novos louos.vagas.exigencia_por_grupo / louos.sandbox.amostra_padrao (catálogo 31→33). Permissões consultar-louos/manter-louos (total 14).
- BLOQUEADO pendente SEDUR (degrada honesto, registrado): Quadro 10 sobre zona real (SIGIS/CA 2000), atributo viário LOUOS (CLASSE_VIA_LOUOS — Quadros 11/11A), correspondência Quadro 11↔11B, planilhas oficiais dos Quadros. Quando a base chegar, muda a carga, não a lógica.

### Fase 6 (Classificação de Risco) — concluída 2026-06-14

- Regras como DADOS VERSIONADOS: cabeçalho genérico `rule_versions` (domain/version/status rascunho|vigente|substituida/vigência/4-olhos) espelhando geo_layers + `RuleVersion` (scopes vigente/naData/versao) + `RuleVersionService` (openDraft/publish — fecha a vigente sem apagar, audita). **A Fase 5 (Motor LOUOS) HERDA essa infra** para os Quadros 7/10/11/11A.
- Risco MUNICIPAL (Decreto 32.636/2020) e SANITÁRIO (VISA) como dimensões SEPARADAS (tabelas/enums/versões distintas — RN-009). Seed real: municipal 1.331 (767 baixo_a/328 baixo_b/236 alto), sanitário 261, condicionantes-pergunta 67, gatilhos 3.
- Enum `RiscoMunicipal` = baixo_a|baixo_b|alto — **o decreto NÃO tem "médio"** (fromDecreto rejeita desconhecido). "Médio operacional" mapeia para baixo_b via parâmetro `risco.mapa_encaminhamento` (default {baixo_a:expresso, baixo_b:expresso, alto:analise}); nível ausente/CNAE sem regra → análise (nunca inventa nível).
- `RiscoClassificationService` espelha o `TerritoryService` (DTOs readonly RiscoInput/RiscoResult, dimensões separadas, toArray snake_case, versoes(), auditoria 'risco/classificacao' com rules_version). Encaminhamento é DECISÃO DE ROTEAMENTO auditada (não o processo expresso — Fases 8/9).
- Gatilhos CNAE como tabela administrável `risk_triggers` (semi-expresso, motivo auditável); exceção ZEIS usa a restrição da Fase 4. Condicionante-pergunta reclassifica (golden 1031-7/00). Comando `risco:classificar {cnae}` dá evidência real do motor.
- Mantenedores HU-019/020/052/053 no console (consulta vigente + publicação versionada 4-olhos + CRUD de condicionantes); UI no padrão console. Golden cases (#[DataProvider]) + RiscoSeedDistributionTest travam a regressão de domínio. Parâmetros novos: risco.mapa_encaminhamento, risco.dimensao_tvl (catálogo 29→31). Permissões consultar-risco/manter-risco (total 12).
- PENDÊNCIAS SEDUR (parametrizadas, NÃO bloqueiam — registradas): correspondência "médio"↔baixo_b (parâmetro mapa_encaminhamento), lista completa de gatilhos CNAE (tabela risk_triggers, 3 conhecidos), dimensão decisiva no TVL (parâmetro dimensao_tvl, default municipal).

### Fase 2.1 (INSERTED) — Template TailAdmin (concluída 2026-06-10)

- Visual TailAdmin (React 19 + Tailwind v4) aplicado em todo o app: design system no app.css (@theme com fonte Outfit, cores brand/gray/success/error/warning, shadows), contexts de tema (dark mode com localStorage) e sidebar, 19 ícones SVG inline, componentes base em resources/js/components/{ui,form,app}/ (Button, Badge, Modal, Alert, Dropdown, Table, Input, Select, Switch, AppSidebar, AppHeader, AppShell), 4 layouts e 19 páginas redesenhados.
- Zero dependências novas (sem react-router/ApexCharts/svgr); sem features de fachada (search/notificações do template não portados).
- Padrões para novas telas: AppShell parametrizado (items/homeHref/subtitle); páginas com PageBreadcrumb + cards rounded-2xl; tabelas com componente Table; forms com Input error/hint. Novas páginas DEVEM usar esses componentes.
- Regressão: 197/197 testes, typecheck e build verdes; screenshots validados (home, login, painel, CNAEs, parâmetros, dark mode).

### Fase 2.2 (INSERTED) — Refinamento premium de UI/UX (concluída 2026-06-10)

- Landing institucional completa em home.tsx: header sticky, hero, serviços (4 cards), como funciona (4 passos), base legal (LOUOS 9.148/2016, Decreto 32.636/2020, CNAE 2.3), CTA e footer — conteúdo 100% verdadeiro, sem estatísticas/depoimentos inventados.
- Sidebar subdividida em grupos com headings (API: SidebarGroup[] no AppShell/AppSidebar). Gestão: Visão geral/Cadastros/Sistema. Portal: Início/Serviços/Minha conta.
- Auth premium: painel institucional com gradiente brand, bullets verdadeiros (LOUOS, decreto, auditoria), hierarquia forte nos forms.
- PADRÃO DE CRUD (obrigatório para novas telas): datatable em card único com ação primária no header; criar/editar em Modal (700px forms grandes, 600px simples); ações destrutivas/impacto via ui/confirm-dialog.tsx (danger/warning/info, processing); ui/pagination.tsx compartilhado com meta "Mostrando X–Y de Z" (backend envia from/to/total); ui/empty-state.tsx distinguindo busca vazia de lista vazia; edição inline apenas quando for melhor usabilidade (ex.: parâmetros por grupo).
- Regressão: 197/197 testes, typecheck e build verdes; screenshots validados (landing, login, CNAEs com modal, sidebar agrupada).

### Fase 2.3 (INSERTED) — Segregação de rotas portal × retaguarda (concluída 2026-06-10)

- MAPA DE ROTAS VIGENTE: `/` redireciona para `/portal` (landing pública, name `home`); auth pública do cidadão sob `/portal/*` (Fortify com `prefix: portal` — `/portal/login`, `/portal/register`, `/portal/forgot-password`, `/portal/reset-password`, `/portal/email/verify`, `/portal/logout`, `/portal/user/password`); painel do cidadão em `/portal/painel` (name `portal.dashboard`, `fortify.home`); retaguarda com login interno próprio em `/gestao/login` (GET tela `auth/gestao-login` + POST no `AuthenticatedSessionController` do Fortify, names `gestao.login`/`gestao.login.store`), sem cadastro público.
- `bootstrap/app.php`: `redirectGuestsTo` por contexto (rotas `gestao*` → `gestao.login`; demais → `login`) e `redirectUsersTo` por perfil (`acessar-gestao` → `gestao.dashboard`; senão → `portal.dashboard`).
- LoginResponse por perfil (Fase 1) inalterado — usa nomes de rota. Logins separados em TELAS/rotas; autenticação única (mesmo guard `web` + permissões), padrão Laravel.
- Identidade visual: logo SILE (pin + edifício, `resources/js/components/app/logo.tsx` + `public/favicon.svg`); tela interna com aviso de acesso restrito.
- Regressão: 201/201 testes (4 novos de roteamento), pint/typecheck/build verdes.
- IDENTIDADES DISTINTAS por contexto (decisão do usuário, 2026-06-10): portal público = claro/acolhedor (AuthLayout split com painel institucional, "Bem-vindo de volta"); retaguarda = console interno escuro standalone (`auth/gestao-login.tsx` — gray-950 com grade técnica, badge "Ambiente interno", "Entrar no console", aviso de auditoria; SEM AuthLayout). Novas telas internas de auth seguem o padrão console; novas telas públicas seguem o padrão portal.
- MCP de UI/UX instalado no projeto (`.cursor/mcp.json`): `shadcn-ui` (@jpisnice/shadcn-ui-mcp-server — contexto de componentes/blocks shadcn v4 para referência de padrões). Requer reload do Cursor para carregar; usar como referência de UI/UX nas próximas telas, mantendo o design system TailAdmin próprio.

### Fase 2.4 (INSERTED) — Template SaaS de listagens e dashboard (concluída 2026-06-11)

- Biblioteca de componentes de listagem em `resources/js/components/` (spec: `.planning/phases/02.4-template-listagens/02.4-01-PLAN.md`; referências: SILE Design System kit, Horizon UI, Spruko Dashtic/Rixzo — identidade SILE mantida, zero dependências novas):
  - `ui/data-table/` — DataTable genérica tipada (`ColumnDef<T>`, ordenação controlada com aria-sort, skeleton rows, empty state integrado, densidade default/compact), `use-server-table.ts` (hook Inertia: busca com debounce 350ms, sort/filtros/per_page imediatos, preserveState+replace, volta à página 1), `table-toolbar.tsx` (busca com lupa + filtros + ações), `per-page-select.tsx` (whitelist + valor vigente).
  - `ui/` — Card/CardHeader/CardContent, KpiCard (delta opcional só com dado real), Skeleton/SkeletonText, Avatar (iniciais, cor determinística), ProgressBar (role progressbar), TableAction (tones brand/warning/success/error/neutral, href ou onClick, title); Button ganhou variantes ghost/danger e size xs.
  - `app/page-header.tsx` — título + trilha (ancestrais via prop; item final é o título) + ações; substituiu TODOS os PageBreadcrumb duplicados.
- PADRÃO DE LISTAGEM (obrigatório para novas telas): PageHeader → Card(CardHeader com ação primária) → CardContent(TableToolbar → chips de filtros ativos → DataTable → Pagination). CNAEs é a listagem-modelo (ordenação por código/denominação, filtro de situação com chip "Limpar filtro", page size 10/15/25/50 com whitelist no backend e default parametrizado).
- Backend: CnaeController@index aceita sort/direction/per_page/active com whitelists (constantes técnicas) e ecoa `filters` + `perPageOptions`; DashboardController (gestão) envia `kpis` reais condicionados à permissão (cnaes ativos/total, usuários ativos/total, perfis/permissões, logins na janela `ui.dashboard.acessos_janela_dias` — parâmetro novo no catálogo, 11 parâmetros). Sem delta inventado nos KPIs (sem série histórica ainda).
- FIX importante: busca com `LIKE` era case-sensitive no PostgreSQL (banco real) — testes SQLite não pegavam. CnaeController e UserManagementController agora usam `whereLike(..., caseSensitive: false)`. Padrão para TODAS as buscas futuras.
- Regressão: 209/209 testes (8 novos: CnaeIndexTableTest com 6, GestaoDashboardKpisTest com 2), pint/typecheck/build verdes; screenshots validados (dashboard e CNAEs claro/escuro, usuários com Avatar, perfis, parâmetros, mobile 375px com toolbar empilhada e scroll-x).
- RE-TEMATIZAÇÃO "Console SEDUR" (plano 02.4-02, pedido do usuário em 2026-06-11): a GESTÃO usa sidebar escura permanente gray-950 (`variant="console"` no AppShell/AppSidebar — estende a identidade do login interno da Fase 2.3 para dentro do app; item ativo `bg-brand-500/15 text-white`, logo branca com marca brand-400); KpiCard redesenhado em layout horizontal com well colorido 56px (`tone: brand|success|warning|error|info` — cor é diferenciação visual, não estado); DataTable com thead `bg-gray-50 dark:bg-white/[0.02]`; listagens da gestão em `density="compact"`. O PORTAL permanece `variant` light (claro/acolhedor, decisão da Fase 2.3). Novas telas da gestão DEVEM usar o console; telas do cidadão, o light.

## Performance Metrics

**Velocity:**

- Total plans completed: 22
- Average duration: 11 min
- Total execution time: ~4.21 h

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 01-identidade | 9/9 ✓ | ~96 min | 11 min |
| 02-administracao-base | 8/8 ✓ | ~100 min | 12 min |
| 03-cadastro-empresarial | 6/9 | ~85 min | 14 min |

**Recent Trend:**

- Last 5 plans: 03-02 (18 min), 03-03 (12 min), 03-04 (14 min), 03-05 (9 min), 03-06 (10 min)
- Trend: 03-06 sem deviations (plano executado como escrito); HU-025/HU-026 backend em 2 tasks TDD; 82 testes do grupo Companies verdes (17 novos)

*Atualizado após cada plano concluído*

## Accumulated Context

### Decisions

Registro completo na tabela Key Decisions de PROJECT.md. Mais relevantes para o trabalho atual:

- [2026-06-12, decisão do usuário] AUTENTICAÇÕES SEPARADAS por ambiente: portal (guard web) e gestão (guard gestao) têm sessões independentes — estar logado num ambiente não significa nada no outro. Revoga a interpretação "login único com destino por perfil" da Fase 2.3 (a HU-002 não exigia sessão única). Login do console em `Gestao\LoginController` (POST /gestao/login, guard gestao) com paridade de segurança: throttle parametrizado + Lockout auditado, anti-oráculo, conta inativada, CA-04 (sem permissão = bloqueado+auditado NO LOGIN) e e-mail verificado como pré-condição de login (rotas internas SEM middleware verified — o aviso/reenvio de verificação é do fluxo do portal). Login do portal leva sempre ao portal (admin no portal é cidadão comum; console só via /gestao/login). `User::$guard_name = 'web'` fixo (papéis/permissões são conceito único, independente do guard de sessão). Settings e termo LGPD com `auth:web,gestao`; troca de senha via PUT /settings/password próprio (rota Fortify exige guard web). Logout da gestão derruba só o guard gestao (cookie de sessão é único por domínio; logout do portal via Fortify invalida a sessão inteira — limitação conhecida). Testes de rotas /gestao usam `actingAs($user, 'gestao')`.

- Auditoria transversal (RN-002: usuário, data/hora, origem, ação, resultado, versão de regras) nasce como infraestrutura na Fase 1; a Fase 12 só consulta/exporta/LGPD.
- Requisitos rastreados pelo ID da HU (HU-001 a HU-131); cada CA BDD vira feature test PHPUnit — nenhuma HU concluída sem CAs passando.
- Mantenedores de quadros/condicionantes/risco (HU-015 a HU-020) entram junto com os motores (Fases 5 e 6) — fatia vertical.
- Motor de regras parametrizável: regras como dados versionados, nunca código; carga oficial (planilhas SEDUR) substitui seeds quando entregue.
- Integrações atrás de contratos; adaptador só é concluído contra homologação real — sem acesso = feature explicitamente bloqueada, nunca simulada.
- Risco municipal (Decreto 32.636/2020) e risco sanitário (planilha VISA) são dimensões separadas no modelo.
- Parametrização máxima (HU-014): nenhum valor de negócio hardcoded; funcionalidades acopláveis com feature toggle administrável.
- [01-01] Fortify com exatamente 4 features (registration, resetPasswords, emailVerification, updatePasswords); 2FA/passkeys fora — migrations publicadas mantidas, inofensivas com features off.
- [01-01] Parâmetros de segurança em config/sile.php lidos via App\Support\Settings::get(); Password::defaults() parametrizado — HU-014 troca o backend para banco sem tocar call sites.
- [01-01] Permissions-Policy com geolocation=(self) — mapa da fase 4 pode pedir localização no próprio domínio.
- [01-01] Rotas Fortify confirmadas (login.store, register.store, password.*, verification.*, user-password.update) — registradas no 01-01-SUMMARY.md.
- [01-02] 403 auditado via render callback em AccessDeniedHttpException (exceção PREPARADA pelo Handler — callback em AuthorizationException nunca dispararia) + UnauthorizedException do spatie.
- [01-02] fortify.limiters.login = null: pipeline usa EnsureLoginIsNotThrottled e dispara Lockout (auditado em access_logs); limiter nomeado daria 429 sem evento. 01-06 deve parametrizar respeitando isso (limite atual: 5 fixo do LoginRateLimiter).
- [01-02] AuditService::log(logName, event, description, properties, subject, result, rulesVersion) e logBlocked() — assinaturas travadas para 01-06/01-07/01-08.
- [01-02] access_logs é tabela dedicada imutável (sem updated_at); AccessLogFactory pronta para HU-010 (01-08).
- [01-03] Papéis travados: cidadao/analista/gestor/administrador num único guard web; contador/procurador NÃO são papéis (poder vem de dados). Factory states por papel exigem seed prévio de RolesAndPermissionsSeeder.
- [01-03] Ambientes segregados por middleware permission:acessar-gestao (não por guard); nomes portal.*/gestao.* alimentam o channel da auditoria. Portal usa auth+verified; lgpd.accepted entra no 01-05; MustVerifyEmail no 01-04.
- [01-03] Props Inertia compartilhadas (auth.user/roles/permissions, flash.status) tipadas em SharedProps (resources/js/types/index.d.ts); auth-layout.tsx pronto para as telas de auth dos planos 01-04/01-05/01-06.
- [01-04] Cadastro coleta cpf (obrigatório, único, 11 dígitos sem máscara, Rule ValidCpf própria) e phone (opcional); UserFactory gera CPF válido. Senha hasheada SÓ pelo cast hashed do User (Hash::make removido da action publicada).
- [01-04] User implements MustVerifyEmail — portal bloqueia não verificados; testes que postam /register precisam seedar RolesAndPermissionsSeeder (assignRole no CreateNewUser).
- [01-04] Mensagem required do laravel-lang pt-BR é "É obrigatória a indicação..." — asserções de validação devem usar 'obrigatória', não 'obrigatório'.
- [01-05] Termo LGPD é dado versionado: seed publica v1; gate lgpd.accepted desarma sem termo publicado; rotas do termo ficam fora do subgrupo protegido; na gestão a permissão é avaliada ANTES do termo (403 prevalece).
- [01-05] Testes que acessam rotas protegidas com termo seedado usam User::factory()->...->withAcceptedLgpdTerm(); aceite é firstOrCreate (idempotente sob unique user_id+legal_term_id).
- [01-06] Limite de login parametrizado nos DOIS pontos: RateLimiter::for('login') E rebinding do LoginRateLimiter (com limiters.login=null o limiter nomeado não é consultado; sem o rebinding o 5 do vendor governaria).
- [01-06] Bloqueio temporário em request web responde redirect 302 com erro auth.throttle em 'email' (não 429 — status só é honrado em JSON); testes assertam redirect+erro+access_logs bloqueio.
- [01-06] Form do Inertia v3 aceita errorBag nativamente (errorBag="updatePassword" em settings/password); rotas de recuperação guest-only redirecionam autenticados para route('home').
- [01-07] Representação vive na sessão (acting_procuration_id) e é revalidada em TODA request do portal por ResolveRepresentation — revogação tem efeito imediato; Context acting_for_user_id + scoped CurrentRepresentation alimentam auditoria e share actingFor do Inertia.
- [01-07] ResolveRepresentation RESETA Context e scoped service quando não há representação válida (estado sobrevive entre requests em testes/queue); unicidade "uma procuração ativa por par" na aplicação (FormRequest::after), sem índice único parcial.
- [01-08] CPF imutável por omissão nas regras do ProfileUpdateRequest (valor enviado é ignorado); troca de e-mail zera email_verified_at e reenvia VerifyEmail.
- [01-08] Histórico de acessos filtra user_id OR email — titular vê falhas/bloqueios pré-login gravados sem user_id; page size parametrizado em sile.ui.access_history.per_page via Settings::get.
- [01-08] Consulta administrativa de acessos auditada explicitamente (log 'acessos', event 'consulta-acessos', target_user_id nas properties) — consulta relevante a dados de terceiro (CA-02).
- [02-01] CSV oficial CNAE fiel à fonte: 1.331 subclasses versionadas em database/data/; 9900-8/00 ausente do arquivo NÃO inventada (proveniência em scripts/convert-cnae-xlsx.py; relatório formal da divergência no import 02-04). Códigos no formato oficial; normalização para dígitos é do CnaeImportService.
- [02-01] RolesAndPermissionsSeeder aditivo (givePermissionTo, nunca sync): re-seed não desfaz ajustes de permissão feitos pelo admin via HU-013. 8 permissões; administrador com todas as manter-*; analista/gestor com consultar-cnaes.
- [02-03] Inativação de conta: users.inactivated_at (fora do fillable — só forceFill em fluxo autorizado); Fortify::authenticateUsing bloqueia com mensagem pt-BR + access_log evento 'inativada' (curto — varchar(20)); anti-oráculo: senha errada retorna null (fluxo padrão 'falha').
- [02-03] EnsureUserIsActive no append GLOBAL do grupo web (bootstrap/app.php): sessão aberta de inativado derrubada na request seguinte (invalidate+regenerateToken); cobre portal/gestão/settings sem tocar arquivos de rota. 02-05 só grava/limpa inactivated_at.
- [02-02] Settings::get com backend banco+cache+fallback (cache → parameters.value ?? default do catálogo → config/sile.php → default do call site; QueryException → config) mantendo assinatura — call sites e tests/Unit/Support/SettingsTest.php intactos. Settings::enabled(feature) para toggles.
- [02-02] Parameter: sensitive FORA do fillable e ordenado antes de value na factory (mutator condicional lê a flag — Pitfall 3); SEM HasAuditoria (vazaria sensível) — histórico explícito com mascaramento entra no 02-07; requires_connection_test é contrato do RN-010 (implementação real na Fase 13).
- [02-02] ParameterSeeder upsert SÓ de metadados (value administrado preservado em re-seed); 10 chaves nos grupos seguranca/ui/features; NÃO registrado no DatabaseSeeder (responsabilidade do 02-08). TTL do cache é constante técnica (sile.parameters.cache_ttl), não parâmetro do registry.
- [02-04] Import oficial de CNAEs: upsert por code NÃO toca active (desativação administrada sobrevive a re-import); divergência 9900-8/00 (publicação cita 1.332, arquivo traz 1.331) registrada no relatório auditado (rules_version cnae-subclasses-2.3), nunca inserida silenciosamente — caminho é o CRUD manual (testado com esse caso real).
- [02-04] Cnae: PK surrogate id (code unique em dígitos, não PK), hierarquia desnormalizada, accessor formatted_code — pronto para dimensões de risco da Fase 6 sem retrabalho. UpdateCnaeRequest valida só description/active (código imutável na edição, padrão CPF). CnaeSeeder fora do DatabaseSeeder (02-08).
- [02-04] Form do Inertia v3 SOBRESCREVE onSubmit custom (handler interno definido após spread das props): confirmações de submit vão no onClick do botão type=submit com preventDefault. Busca por código só aplica branch de dígitos quando o termo contém dígitos (termo textual não vira LIKE '%').
- [02-05] Inativação pela UI é toggle único (PUT usuarios/{user}/inativacao alterna pelo estado) com anti-lockout no FormRequest::after(); auditoria de usuários é EXPLÍCITA (log 'usuarios', target_user_id nas properties) — inactivated_at fora do fillable e papéis (relação) não entram no diff do HasAuditoria.
- [02-05] Listagem de usuários nunca expõe CPF em claro (LGPD): transform por item com cpf_masked ***.***.***-DD; teste asserta missing('cpf') no payload Inertia.
- [02-06] Papéis estruturais protegidos por Roles::STRUCTURAL (constante única) validada em FormRequest::after(): rename/exclusão bloqueados, acessar-gestao não removível do administrador — três proteções anti-lockout com teste nomeado cada. Regra real no backend; UI só comunica (checkbox disabled + hidden, nome readOnly).
- [02-06] Update de perfil pela UI usa syncPermissions (conjunto exato marcado — intenção do admin) em contraste com o seeder aditivo givePermissionTo do 02-01; sem forgetCachedPermissions manual (spatie v8 reseta nos métodos built-in). Roles do spatie sem HasAuditoria — auditoria explícita no log 'perfis' com permissoes_antes/depois.
- [02-07] Toggle real features.procuracoes (CA-06): store bloqueado com aviso pt-BR, index acessível, destroy NUNCA bloqueado — segurança do outorgante prevalece sobre o toggle. Toggles futuros (Fases 9/11/13/14) só registram chaves features.* no catálogo e usam Settings::enabled — mecanismo fechado.
- [02-07] Validação de parâmetro é dinâmica via validation_rules do próprio registro (FormRequest::after); sensível com campo vazio = manter valor (sem gravação, sem activity); UI nunca recebe valor sensível (prop null + password vazio). Histórico via AuditService explícito com [criptografado] e latest('id') para ordem estável no mesmo segundo.
- [03-01] ValidCnpj nasce compatível com CNPJ alfanumérico (IN RFB 2.229/2024, produção julho/2026): normaliza `[^A-Z0-9]` + uppercase, valida `/^[A-Z\d]{12}\d{2}$/`, DV por módulo 11 sobre `ord(char)-48`. Pesos `[6,5,4,3,2,9,8,7,6,5,4,3,2]` com `array_slice($weights, 13 - $position)` para posições 12 e 13 (NÃO usar o snippet de 12 pesos do RESEARCH). Coluna cnpj é string(14) SEMPRE, nunca numérico.
- [03-01] Schema empresarial: companies (cnpj unique), company_user (modelo próprio CompanyUser com started_at/ended_at — encerramento nunca apaga, histórico preservado), company_cnae (pivot SEM modelo). Unicidade "um vínculo ativo por par" e "exatamente um principal" ficam NA APLICAÇÃO (precedente procurations/[01-07]); company_cnae.cnae_id com restrictOnDelete como defesa no banco + unique(company_id, cnae_id).
- [03-01] Pivot company_cnae exige nome EXPLÍCITO no belongsToMany em Company::cnaes() e Cnae::companies() — Eloquent inferiria cnae_company (ordem alfabética). Accessor formatted_cnpj é posicional (regex por posição), funciona com alfanumérico.
- [03-01] Parâmetros do lookup de CNPJ: features.cnpj_lookup (default true), integrations.cnpj_lookup.base_url (requires_connection_test, contrato RN-010 — teste real na Fase 13), ui.companies.per_page — catálogo passa a 14 chaves. Constantes técnicas (timeout 8s, retries 2, cache_ttl 86400) ficam SÓ em config/sile.php, nunca no registry (precedente [02-02]).
- [03-01] Pendência da Fase 2 resolvida: CnaeController::destroy bloqueia exclusão de CNAE vinculado (companies()->exists()) com mensagem pt-BR via flash.error. Canal flash.error agora compartilhado no HandleInertiaRequests e exibido via Alert variant=error na gestao-layout — padrão para bloqueios comunicados (nunca silenciosos).
- [03-02] Consulta de CNPJ (HU-021) atrás de contrato: interface App\Services\Cnpj\CnpjLookup (lookup(string): CnpjData) + DTO readonly CnpjData (fromBrasilApi/toArray snake_case) + provider real BrasilApiCnpjLookup. Binding no AppServiceProvider — a Fase 13 (HU-105) troca SÓ o binding pelo provider conveniado RFB, sem tocar call sites. base_url lido via Settings::get (banco→cache→config); trocar BrasilAPI↔minhareceita é mudança de parâmetro, sem deploy (provado em teste).
- [03-02] Cache de consulta CNPJ (Cache::remember key sile.cnpj_lookup.{cnpj}, ttl config 86400) grava SÓ sucesso: exceção dentro do closure impede a gravação (falha nunca cacheada). Constantes técnicas (timeout 8s, connectTimeout 3s, retries 2) em config/sile.php. ConnectionException convertida em CnpjLookupException no provider; 404 → CnpjNotFoundException.
- [03-02] Endpoint POST /portal/empresas/consultar-cnpj (name portal.empresas.consultar-cnpj) auditado em TODAS as saídas via AuditService (event consulta-cnpj, result sucesso/falha/bloqueado, properties com cnpj e provider). Toggle features.cnpj_lookup desligado bloqueia ANTES de qualquer request HTTP (degradação comunicada 422). Shape JSON = CnpjData::toArray (snake_case) — contrato do formulário React no 03-06.
- [03-02] bootstrap/app.php: shouldRenderJsonWhen estendido para portal/empresas/consultar-cnpj quando expectsJson() — o projeto restringia render JSON de exceções a api/*, fazendo o portal redirecionar (302) em vez de 401/422/404 JSON. Padrão para futuros endpoints JSON do portal (useHttp): adicionar a rota na condição.
- [03-03] Importação REDESIM (HU-022) com lógica REAL atrás de contrato: RedesimImportService->import(jsonPath) retorna {lidos, importados, atualizados, rejeitados[], avisos[]} e audita via AuditService (log 'empresas', event 'importacao-redesim', rules_version 'redesim-import-v1', result falha SÓ quando todos rejeitados; causer null = sistema). Validação por item (protocolo/razão social/CNPJ via ValidCnpj/CNAE principal existente); item inválido rejeitado com motivo pt-BR carregando o protocolo, sem inserção parcial (DB::transaction por item). Estrutura do payload é REFERÊNCIA A VALIDAR COM A SEDUR (REGIN/JUCEB) — o transporte real é a Fase 13 (HU-103) e ajusta só o parsing.
- [03-03] Upsert por CNPJ NÃO reescreve source (origem de criação imutável — Pitfall 8): empresa criada manual continua manual após reimport, marcando redesim_synced_at + redesim_protocol. CNAEs sincronizados com sync() exato (principal is_primary=true + secundários resolvidos). CNAE inativo (principal/secundário) IMPORTA com aviso (dado da Junta é fato consumado; "somente ativos" vale só para seleção manual HU-025/026); secundário inexistente vira aviso e é ignorado. Import NUNCA cria company_user (payload não traz usuário do portal — associação é da Fase 13).
- [03-03] Comando redesim:importar {arquivo} (App\Console\Commands\ImportRedesimCommand, auto-descoberto): relatório pt-BR (Lidos/Importados/Atualizados + seções Rejeitados/Avisos), exit 1 para arquivo inexistente / JSON inválido (JsonException) / todos os itens rejeitados; exit 0 caso contrário. NÃO existe rota pública de import (testado — entrada é exclusivamente comando/serviço, CA-04). Payload de referência REAL versionado em database/data/redesim-exemplo.json (dados públicos RFB) para homologação; fixture em tests/Fixtures/redesim/.
- [03-04] CompanyPolicy integrada à representação [01-07]: effectiveUser() = CurrentRepresentation::grantor() ?? $user, replicado na policy E no CompanyController. view() aceita vínculo ativo OU encerrado (histórico visível); update/manageCnaes/endLink exigem vínculo ATIVO (whereNull ended_at). Gate manageCnaes pronto para o 03-05; endLink para o 03-06 (HU-028).
- [03-04] store de empresa (HU-023) cria Company (source=manual) + CompanyUser (responsavel, started_at=now) na MESMA DB::transaction, em nome do usuário efetivo — em representação o vínculo nasce para o REPRESENTADO (user_id=grantor), nunca para o procurador; auditoria created enriquecida com acting_for_user_id=grantor. CNPJ duplicado bloqueado no StoreCompanyRequest com unique + mensagem "Já existe empresa cadastrada com este CNPJ." (CA-03); prepareForValidation normaliza CNPJ (uppercase/sem máscara), telefone e CEP (só dígitos).
- [03-04] Listagem Minhas empresas (HU-027) server-driven (padrão CnaeController@index): escopo whereHas('links', user efetivo), busca por razão social/fantasia (whereLike caseSensitive:false) ou prefixo de CNPJ, ordenação whitelistada (SORTABLE_COLUMNS=['legal_name']), paginação parametrizada ui.companies.per_page; eager loading anti-N+1 (wherePivot is_primary + vínculo do usuário). Company::countForUser exposto em totalCompanies (contagem reutilizável p/ painel do cidadão). Rotas portal.empresas.index/create/store ANTES de empresas/{company} (03-05).
- [03-05] HU-024 atualizar empresa: CompanyController::show (Gate view, aceita vínculo ativo OU encerrado) com props completas (company, cnaes primary/secondaries, links com is_current_user, abilities update/manageCnaes/endLink) — contrato direto da tela de detalhe 03-08. update (Gate update, vínculo ATIVO) com CNPJ IMUTÁVEL POR OMISSÃO: o UpdateCompanyRequest não inclui cnpj nas rules, logo validated() nunca o contém e o valor enviado é ignorado (padrão CPF [01-08]); auditoria 'updated' com attribute_changes automática (HasAuditoria). Rotas empresas/{company} (show/update) DEPOIS das literais.
- [03-05] HU-028 encerrar vínculo: CompanyLinkController::destroy encerra o PRÓPRIO vínculo ativo do usuário efetivo via ended_at/ended_reason — NUNCA delete físico (histórico preservado). Proteção do último responsável ativo em EndCompanyLinkRequest::after() (precedente anti-lockout [02-05]): bloqueia com "A empresa não pode ficar sem responsável ativo." contando APENAS o papel responsavel (procurador único encerra normalmente). Auditoria de negócio explícita event 'encerramento-vinculo' (AuditService, log_name 'empresas', props empresa_id/vinculo_id/motivo) coexiste com o 'updated' técnico do HasAuditoria. Rota DELETE empresas/{company}/vinculo.
- [03-04] Testes Inertia da Fase 3 usam assertInertia has()/where() SEM ->component() — as páginas React (portal/empresas/index, cadastrar) só nascem no 03-07 (wave 6) e a checagem de componente exige o arquivo em disco. PENDÊNCIA p/ 03-07: adicionar a verificação visual/componente quando as telas existirem. Shape das props do index documentado no 03-04-SUMMARY (contrato da DataTable).
- [03-06] Escrita do pivot company_cnae EXCLUSIVAMENTE via CompanyCnaeService em DB::transaction: setPrimary demove→promove (o antigo principal VIRA secundário — linha do pivot preservada, nunca removida); syncSecondaries com sync() calculado incluindo o principal no payload (conjunto exato dos secundários, padrão syncPermissions [02-06]). Auditoria explícita dentro da transação: 'cnae-principal' (cnae_anterior/cnae_novo) e 'cnaes-secundarios' (antes/depois) — insumo das Fases 5/6. Seleção manual aceita SÓ CNAEs ativos (Rule::exists where active nos FormRequests; principal bloqueado entre os secundários via after()); GET /portal/cnaes (portal.cnaes.search) retorna só ativos, máx 20 (constante técnica MAX_RESULTS), shape id/formatted_code/description — contrato do picker da tela 03-08.

- [08-01] Fundação do DOMÍNIO da Fase 8 (irmã paralela do 08-02 — zero sobreposição de arquivos): aggregate `viability_requests` (imóvel embutido 1:1) + satélites `viability_request_cnaes` (is_primary; cnae restrictOnDelete; unique(request,cnae)) / `viability_request_documents` (disk/path/sha256/uploaded_by; requirement_id nullOnDelete) / `viability_request_transitions` (from/to/reason/public_label/actor — FONTE da timeline HU-069) + cadastros `viability_service_types` / `document_requirements` (+ pivot `cnae_document_requirement`) + `protocol_sequences` (year unique). GEOMETRIA DRIVER-AWARE espelhando geo_features/GeoJsonLayerImporter (Fase 4): `property_polygon_geojson` (jsonb) é a FONTE de verdade portável (suíte SQLite); `property_polygon` geometry(Polygon,4326) DERIVADA nullable + índice GiST adicionados por DB::statement SÓ no pgsql (em SQLite a coluna NEM EXISTE — teste prova a ausência); a derivada é gravada via ST_* no 08-06 (gancho espacial reverso p/ HU-142/Fase 10). Enums `ViabilityRequestStatus` (ATIVOS rascunho/protocolada/cancelada + GANCHOS aguardando_bap/em_analise/deferida/indeferida/em_pendencia, com label()/publicLabel()) e `ViabilityRequestOrigin` (portal/contingencia + gancho regin). `ViabilityRequest` usa HasAuditoria; **protocol_number/status FORA do #[Fillable]** (governados por gerador/máquina — sem auditoria dupla, a StateMachine audita); `markSimulationStale()` (escrita única, HU-063 RN-005); `transitions()` é hasMany()->latest(); `cnaes()` belongsToMany withPivot('is_primary') espelha company_cnae. `ProtocolNumberGenerator::generate(?int $year): string` = {prefixo}-{AAAA}-{NNNNNN} sob `protocol_sequences->lockForUpdate()` em DB::transaction (FOR UPDATE real no pgsql, no-op SQLite; unique(protocol_number) defesa final); prefixo/padding via Settings::get com DEFAULT INLINE VIA/6 (independe do banco/seeder do 08-02). `ViabilityRequestStateMachine`: mapa explícito SÓ dos ativos (rascunho⇒[protocolada,cancelada], protocolada⇒[cancelada]); transição inválida → `InvalidStatusTransitionException` (CA-03, estado/timeline intactos); toda transição grava viability_request_transitions + AuditService::log('solicitacoes','transicao') (RN-002); NÃO abre transação própria (o caller 08-10/08-12 controla). Ganchos NÃO transicionáveis (teste trava a regressão). Factory states draft/protocoled/cancelled/contingency + helpers withPrimaryCnae/withCnaes. ZERO dependência nova. TDD estrito (RED→GREEN com evidência fresca): 25 testes SQLite + 1 @group postgis (concorrência real); suíte completa 700/700.
- [08-02] Fundação de parametrização da Fase 8 (HU-014, aditivo, irmã paralela do 08-01 — zero sobreposição de arquivos): 13 parâmetros novos (catálogo 35→48) no NOVO grupo de negócio 'solicitacao' — toggles features.solicitacao_viabilidade/simulacao_solicitacao, protocolo.prefixo(VIA)/padding(6), consulta_publica.assinatura_ttl_dias(30), anexos.max_mb(10)/mime_permitidos(json), storage.documentos.disk(grupo solicitacao, default local, NUNCA público), area_poligono.tolerancia_percentual(10), prazo_estimado_dias(30, ressalva HU-129/Fase 15), atendimento.expiracao_minutos(30), seguranca.throttle.consulta_protocolo.por_minuto(30) — todos com fallback espelhado em config/sile.php (Settings::get banco→cache→config, efeito sem deploy). key≠group mantido (storage.* fica no grupo 'solicitacao', como security.*→'seguranca'); lista de grupos cresce só com 'solicitacao'. 5 permissões aditivas (14→19): registrar-contingencia/atendimento-presencial/consultar-solicitacoes/manter-tipos-servico/manter-requisitos-documentais (admin+gestor as 5; analista só consultar-solicitacoes; cidadão por policy). Colateral corrigido: ManageRolesTest contagem total 14→19. ZERO dependência nova. Insumo direto de 08-01(ProtocolNumberGenerator prefixo/padding)/08-06(tolerância)/08-08(anexos/disk)/08-09(toggle)/08-11(ttl/throttle/prazo)/08-15(expiração) e das permissões em 08-03/04/14/15.
- [08-04] Cadastro administrável dos REQUISITOS DOCUMENTAIS por CNAE (HU-067) — Wave 2, CRUD+UI: a obrigatoriedade documental por CNAE virou DADO administrável (HU-014), não código. `Gestao\DocumentRequirementController` server-driven (busca code/name caseSensitive:false, ordenação whitelist [code,name], filtro de situação, per_page via Settings::get('ui.requisitos_documentais.per_page',15) com DEFAULT INLINE — sem novo parâmetro, escopo do 08-02 restrito); store/update com `code` único e IMUTÁVEL (omitido do UpdateRequest — padrão CPF/CNAE); `toggle` dedicado (PUT {requirement}/toggle) que desativa preservando o registro (NUNCA exclui — referenciado por solicitações); `syncCnaes` (PUT {requirement}/cnaes) sincroniza o conjunto EXATO de CNAEs por `sync()` em DB::transaction (padrão syncSecondaries [03-06], mas NO controller — não há invariante extra tipo "único principal"). Só CNAEs ATIVOS podem ser vinculados (Rule::exists where active). Rotas `gestao.requisitos-documentais.{index,store,update,toggle,cnaes,cnaes-disponiveis}` TODAS sob `permission:manter-requisitos-documentais` (sem ela → 403 auditado seguranca/bloqueado, CA-04). Auditoria MISTA: **HasAuditoria no model** (created/updated/deleted, consistente com 08-03) + AuditService explícito do vínculo N:N (`log 'solicitacoes'`, event `requisito-cnaes`, antes/depois — relações não entram no diff). Picker reusa o `Portal\CnaeSearchController` numa rota da GESTÃO (`cnaes-disponiveis`, só ativos) — o console (guard gestao) não acessa /portal/cnaes (sessões separadas por ambiente). Tela `gestao/requisitos-documentais/index.tsx` (console: DataTable + modais criar/editar + toggle confirm-dialog + gerenciador de CNAEs com chips+picker incremental via useHttp) + item de navegação "Requisitos documentais" (FileIcon) condicionado à permissão. ESCOPO HONESTO (anti-fachada): a tabela `cnae_document_requirement` nasce VAZIA (planilha SEDUR vazia hoje) — CRUD real para a SEDUR popular; o resolver (08-08) degrada honesto até a carga. CONTRATO p/ 08-08: `DocumentRequirement::cnaes()` belongsToMany `cnae_document_requirement`; o resolver une `DocumentRequirement::where('active',true)` filtrados pelos CNAEs da solicitação (whereHas('cnaes')) ∪ condicionais base (fachada sempre; concessão se is_public_area). `validation_instructions` é gancho IA EP14, inerte. ZERO dependência nova. TDD estrito (RED→GREEN com evidência fresca): DocumentRequirementCrudTest 8/8 (52 asserções); typecheck/build/pint verdes; suíte 698/698 SQLite + 16/16 @group postgis.
- [08-05] CRIAR RASCUNHO da solicitação (HU-061) — Wave 3, nascimento do processo formal no portal: `ViabilityRequestPolicy` (auto-discovery, espelha `CompanyPolicy`) com `effectiveUser = CurrentRepresentation::grantor() ?? user` — `view` (dono/representado, qualquer status), `update`/`protocol` (dono E status rascunho), `cancel` (dono E status em {rascunho, protocolada}; regra fina dos canceláveis fica no 08-12). `SolicitacaoController@store` cria o rascunho REAL em DB::transaction: `requester_user_id` = usuário EFETIVO (representado quando "em nome de"), `created_by_user_id` = ator real (`$request->user()`), `origin` = portal; `status` NÃO é setado (default 'rascunho' do banco — consistente com [08-01], status fora do fillable); auditoria created (HasAuditoria) enriquecida com `acting_for_user_id` pelo RecordActivityAction quando em representação. `StoreSolicitacaoRequest`: `service_type_id` (Rule::exists where active — só tipo ATIVO do 08-03) + `company_id` (exists + `after()` exige vínculo ATIVO do efetivo — não cria p/ empresa de terceiro), mensagens pt-BR. `DuplicateRequestDetector::detect(Company, ?int $excludeRequestId = null): ?array` (RN-007): aponta o processo anterior mais recente da MESMA empresa por CNPJ (company_id) — não cancelada E (created_at na janela OU status protocolada=ativo) — retorna {request_id, protocol_number, status, created_at} para ALERTA com link real, NUNCA bloqueia (direito de petição); por inscrição imobiliária degrada (lote bloqueado pendente SEDUR — só por CNPJ aqui). Janela em `config/sile.php` `sile.solicitacao.duplicidade.janela_dias` (default 180, default inline no serviço) — CONSTANTE de config, NÃO Parameter de catálogo (não toca ParameterSeeder; contagens travadas em 48); definição de duplicidade é pendência SEDUR. `index` "Minhas solicitações" server-driven escopado ao requerente efetivo (busca protocolo/empresa, ordenação whitelist [protocol_number, created_at] default created_at desc, per_page CONSTANTE 15+whitelist — precedente [08-03], sem parâmetro). Toggle `features.solicitacao_viabilidade`: store degrada comunicado (back+status, sem criar) quando off; index acessível expondo `solicitacaoEnabled`. Rotas `portal.solicitacoes.{index,store}` no grupo auth:web+verified+lgpd.accepted+ResolveRepresentation (literais; {solicitacao} vem em 08-06/08-11). Shape da listagem p/ 08-13: data[]={id, protocol_number, status{value,label,public_label}, service_type, company{legal_name,formatted_cnpj}, created_at}; flash `duplicateAlert`. ZERO dependência nova; ZERO tela React nova (08-13). TDD estrito: 21 testes novos (5 policy + 8 detector + 8 fluxo); suíte completa 735/735 (3737 asserções, incl. 16 @group postgis).
- [08-03] Cadastro administrável dos TIPOS DE SERVIÇO (HU-061 RN-005) — Wave 2, CRUD+UI: `ViabilityServiceTypeController` server-driven espelhando o `CnaeController` (busca code/name caseSensitive:false, ordenação whitelistada [code,name], filtro de situação, paginação com DEFAULT_PER_PAGE=15 + whitelist [10,15,25,50] — constante, sem parâmetro dedicado para não tocar o ParameterSeeder do 08-04 paralelo). Rotas `gestao.tipos-servico.{index,store,update,ativacao.update}` TODAS sob `permission:manter-tipos-servico` (listagem E CRUD na MESMA permissão — sem "consultar" separado; sem ela → 403 auditado seguranca/acesso-negado/bloqueado, CA-04). `code` único no store e IMUTÁVEL na edição (omitido do UpdateRequest — padrão CPF/CNAE); name/flow_hint/active editáveis; formato do code livre (max:50) porque a lista oficial é pendência SEDUR (seed mínimo no 08-16, upsert por code, substituível sem deploy). Desativação por TOGGLE dedicado (`toggleActivation`, PUT {serviceType}/ativacao) que NUNCA exclui o registro (preserva histórico — tipo referenciado por solicitações); NÃO há rota destroy. Auditoria por **HasAuditoria no model** (created/updated dos fillable, como Company — decisão do plano), não AuditService explícito. Tela `gestao/tipos-servico/index.tsx` no padrão console (PageHeader→Card→TableToolbar→DataTable→Pagination, modal criar/editar, toggle via confirm-dialog) + item de navegação "Tipos de serviço" no grupo Cadastros condicionado à permissão + ícone TagIcon novo. CONTRATO p/ 08-05: `ViabilityServiceType::active()->orderBy('name')->get(['id','code','name','flow_hint'])` alimenta o select; service_type_id nullOnDelete mas toggle preserva o vínculo histórico. ZERO dependência nova. TDD estrito: ViabilityServiceTypeCrudTest 6/6 (28 asserções); typecheck/build/pint verdes; suíte 697/698 (a única falha é o DocumentRequirementCrudTest do 08-04 em andamento — página requisitos-documentais ainda não criada — fora do escopo).
- [08-07] INSTRUIR ATIVIDADES da solicitação (HU-064 principal + HU-065 complementares) — Wave 3, irmão paralelo do 08-06 (arquivos distintos; o 08-06 não tocou routes/portal.php — confirmado por git show). `Portal\SolicitacaoAtividadeController@update(UpdateSolicitacaoAtividadesRequest, ViabilityRequest $solicitacao)` grava o pivot `viability_request_cnaes` em `DB::transaction` com um ÚNICO `cnaes()->sync($payload)` do conjunto EXATO — o principal primeiro (`is_primary=true`) + complementares (`is_primary=false`) — garantindo "exatamente um principal" e a unicidade `(request, cnae)` na aplicação. DECISÃO: escrita no CONTROLLER (não em service): como toda a lista (principal+complementares) chega numa única request, o sync exato basta — diferente do `CompanyCnaeService` (Fase 3) que precisa de setPrimary/syncSecondaries por ter 2 endpoints; `files_modified` do plano não previa service. `markSimulationStale()` é chamado DENTRO da mesma transação (mudou CNAEs → invalida a simulação orientativa anterior, HU-063 RN-005, atômico com o sync). Auditoria explícita `AuditService::log('solicitacoes','solicitacao-cnaes', antes/depois com códigos+is_primary)` coexiste com o `updated` do HasAuditoria (relações não entram no diff). `UpdateSolicitacaoAtividadesRequest`: `principal_cnae_id` required + `Rule::exists('cnaes','id')->where('active',true)`; `complementares` nullable array com `max:{Settings::get('solicitacao.cnaes_complementares.max', config(...,99))}` LIDO DINAMICAMENTE no rules() (efeito sem deploy, espelha validação dinâmica [02-07]) — exceder = erro comunicado em 'complementares' (não silencioso); `complementares.*` integer/`distinct`/CNAE ativo; `after()` impede o principal entre os complementares; mensagens pt-BR. Só CNAEs ATIVOS vinculáveis (mesma regra da seleção manual [03-06]); a busca reusa `portal.cnaes.search` (CnaeSearchController, já só ativos). Autorização dono+rascunho via `Gate::authorize('update', $solicitacao)` (ViabilityRequestPolicy do 08-05). Rota `PUT portal/solicitacoes/{solicitacao}/atividades` (name portal.solicitacoes.atividades) ANEXADA após as literais (reli routes/portal.php antes; staging individual; git diff = só import+rota). CONTRATO p/ 08-08 (resolver une document_requirements dos CNAEs via whereHas('cnaes')), 08-09 (simulação itera os CNAEs — lista garantida + invalidação RN-005) e 08-13 (UI picker consome a rota + busca). ZERO dependência nova. TDD estrito (RED→GREEN com evidência fresca): InformarAtividadesTest 7/7 (29 asserções); pint --dirty limpo; suíte completa 744/744 (3774 asserções; 735 baseline + 2 do 08-06 InformarImovelPostgisTest + 7 novos; inclui 16 @group postgis com container healthy). Commits 8d6302d (test) / 985c582 (feat).
- [08-06] INFORMAR IMÓVEL (HU-062) + ÁREA UTILIZADA (HU-063) — Wave 3, irmão paralelo do 08-07 (arquivos distintos; ambos só ANEXARAM em routes/portal.php, git diff HEAD = só o próprio import+rota, zero clobber). `App\Services\Solicitacao\PropertyGeometryWriter` concentra a geometria do imóvel DRIVER-AWARE espelhando o `GeoJsonLayerImporter` (Fase 4): `write(ViabilityRequest)` deriva a coluna `property_polygon` (geometry) via `DB::update('... ST_MakeValid(ST_SetSRID(ST_GeomFromGeoJSON(?),4326)) ...')` SÓ quando `DB::getDriverName()==='pgsql'` (no-op em SQLite, onde a coluna nem existe; geometry NUNCA mass-assign); `polygonAreaSquareMeters(array): ?float` calcula a área em m² (ST_Area::geography REAL no pgsql; aproximação planar shoelace graus→metros pelo cosseno da latitude em SQLite, só p/ o alerta orientativo). A FONTE de verdade é sempre o `property_polygon_geojson` (jsonb). `Portal\SolicitacaoImovelController@update(UpdateSolicitacaoImovelRequest, ViabilityRequest $solicitacao)`: `Gate::authorize('update')` (dono+rascunho, policy 08-05); em `DB::transaction` grava polígono(fonte)/used_area_m2/address_*(incl. address_complement TEXTO LIVRE, HU-139 adiado)/address_reference(obrigatório)/indicadores(is_virtual_office/is_public_area/has_independent_access via `$request->boolean()`), chama `PropertyGeometryWriter::write` (deriva geometry pgsql), `markSimulationStale()` (RN-005, mudou imóvel/área → zera snapshot) e, se houver alerta, grava nota auditada. ANTES da transação identifica o território pelo CENTROIDE do polígono (média dos vértices em PHP — portável SQLite) via `TerritoryService::identify($lat,$lng)` (REUSA a Fase 4, NÃO recria geo; degrada honesto — zona pendente SEDUR → status 'indisponivel' com motivo e nome null, NUNCA inventa; NÃO recomputa veredito). VALIDAÇÃO ÁREA×POLÍGONO (HU-063 RN-004): contra a área do PRÓPRIO polígono (lote oficial degrada via LocationValidationService — pendente SEDUR); ALERTA só quando a declarada EXCEDE o polígono além de `solicitacao.area_poligono.tolerancia_percentual` (Settings::get, default 10 — usar menos área é legítimo); NUNCA bloqueia (sem 422), retorna flash `areaAlert` {area_declarada_m2, area_poligono_m2, tolerancia_percentual, divergencia_percentual} + nota auditada `AuditService::log('solicitacoes','imovel-area-inconsistente', result 'alerta', subject=solicitação)` (a inconsistência mantida fica registrada para o analista — SEM campo/flag novo no schema, fechado no 08-01). `back()` com flash `status` + `territorio` (TerritoryResult::toArray, resumo por dimensão p/ a UI 08-13) + condicional `areaAlert`. `UpdateSolicitacaoImovelRequest`: polígono GeoJSON (type Polygon, coordinates.0 ≥4 pontos, pares [lng,lat] numéricos), address_reference required, used_area_m2 required numeric gt:0, indicadores sometimes boolean, demais address_* nullable; mensagens pt-BR. Rota `PUT portal/solicitacoes/{solicitacao}/imovel` (name portal.solicitacoes.imovel) ANEXADA após as literais e a rota de atividades do 08-07. DECISÃO: território NÃO persistido (schema fechado no 08-01, sem migração neste plano) — devolvido no flash + auditado pelo próprio TerritoryService; a persistência associada chega via simulação (08-09 simulation_snapshot). CONTRATO p/ 08-08 (concessão se is_public_area), 08-09 (ponto/centroide + área prontos, markSimulationStale já zera a cada mudança), 08-10 (geometry derivada = gancho espacial; imóvel/área pré-condição) e 08-13 (UI mapa consome a rota + GeoJSON + flash territorio/areaAlert). ZERO dependência nova. TDD estrito (RED→GREEN com evidência fresca, @group postgis rodando de verdade): InformarImovelPostgisTest 2/2 (geometria derivada real + área ST_Area) + InformarImovelTest 6/6 SQLite; suíte completa 750/750 (3806 asserções; inclui 16 @group postgis com container sile-pgsql healthy + os 7 testes do 08-07). Commits 29fa6c9 (writer+postgis) / 4cc6301 (controller/request/rota+SQLite).
- [08-08] ANEXAR DOCUMENTOS (HU-066) + VALIDAR OBRIGATORIEDADE DOCUMENTAL (HU-067) — Wave 3, em paralelo com 08-09 (simulação; mesmo routes/portal.php — reli imediatamente antes e ANEXEI só import+3 rotas após a do imóvel, git diff = só meu trecho, staging individual; o pint --dirty tocou o SimulacaoSolicitacaoTest.php do 08-09 e foi REVERTIDO com git checkout — não commitei arquivo de simulação). `App\Services\Solicitacao\DocumentRequirementResolver`: `required(ViabilityRequest): Collection` = UNIÃO (por id, sem duplicar) dos `DocumentRequirement` (required=true, active=true) vinculados aos CNAEs da solicitação via `whereHas('cnaes', whereIn cnaes.id)` (08-04) COM os condicionais BASE por CÓDIGO ESTÁVEL — `CODE_FACHADA='foto-fachada'` SEMPRE; `CODE_CONCESSAO='termo-concessao'` SE `is_public_area` (08-06) —, ambos só se active; `missing(ViabilityRequest): Collection` = required() menos os já cobertos por anexo (`documents()->whereNotNull('requirement_id')->pluck`) = base do BLOQUEIO do protocolo (08-10) e do aviso. ANTI-FACHADA provado: com `cnae_document_requirement` vazia (carga oficial pendente SEDUR) e/ou requisito-base ausente, degrada honesto e ainda valida o conhecido (test_tabela_por_cnae_vazia_ainda_valida_base) — nunca inventa. `Portal\SolicitacaoDocumentoController` (store/download/destroy): `store` `Gate::authorize('update')` (dono+rascunho) → disk = `Settings::get('storage.documentos.disk', config(...,'local'))` NUNCA público → captura sha256 REAL (`hash_file('sha256', getRealPath())`) + original_name/mime/size ANTES do `store()` (a movimentação invalida getRealPath) → em DB::transaction remove o anexo anterior do MESMO requirement_id (disk+linha, substituição) + `$file->store('solicitacoes/{id}', $disk)` + cria a linha + audita `documento-anexado` (RN-002); `download` `Gate::authorize('view')` + anti-IDOR (`abort_unless($documento->viability_request_id===$solicitacao->id,404)`) → `response()->streamDownload(... Storage::disk($disk)->readStream($path) ...)` (STREAMING autenticado, NUNCA URL pública — LGPD) + audita `documento-download`; `destroy` `Gate::authorize('update')` → apaga do disk + linha, audita `documento-removido`. `StoreSolicitacaoDocumentoRequest`: `file` required|file|`mimetypes:{solicitacao.anexos.mime_permitidos}` (tipos MIME completos — não `mimes`/extensão) `max:{solicitacao.anexos.max_mb*1024}` lidos DINAMICAMENTE no rules() via Settings (efeito sem deploy, HU-014); `requirement_id` nullable + `Rule::exists`. Rotas `portal.solicitacoes.documentos.{store,download,destroy}` (POST/GET download/DELETE) após as literais. 403 auditado globalmente (CA-04, [01-02]). CONTRATO p/ 08-10 (missing() bloqueia o protocolo; anexos imutáveis após protocolo pela policy update), 08-13 (UI consome as 3 rotas + required/missing) e 08-16 (seedar foto-fachada/termo-concessao). ZERO dependência nova. TDD estrito (RED→GREEN com evidência fresca, Storage::fake): DocumentRequirementResolverTest 5/5 + AnexarDocumentoTest 6/6 (44 asserções juntos); pint limpo; suíte 761/761 (= 750 baseline + 11 do 08-08; 16 @group postgis com container healthy) EXCLUINDO só o SimulacaoSolicitacaoTest do 08-09 em andamento (raw: 770, 762 passaram, 8 erros — todos da rota portal.solicitacoes.simular ainda não criada pelo 08-09, não regressão minha). Commits 19c9b87 (resolver) / df61479 (controller/request/rotas).
- [08-09] SIMULAÇÃO PRÉ-PROTOCOLO (HU-141) — Wave 3, em paralelo com 08-08 (mesmo routes/portal.php — reli imediatamente antes e ANEXEI só import+rota `simular` após as de documentos; git diff vs HEAD = só meu trecho, staging individual; não toquei arquivos de documentos do 08-08). REUSO TOTAL do motor da Fase 7, sem lógica paralela (RN-001). REFACTOR MÍNIMO: `ConsultaViabilidadeService::consultarPorPontoConhecido(float $lat, float $lng, string $cnae, ?float $area=null): ConsultaViabilidadeResult` — entrada PÚBLICA por ponto+CNAE que delega à pipeline privada `consultarPorPonto` (geocode null, NÃO geocodifica de novo); `ConsultaViabilidadeInput::paraPonto` com `TIPO_PONTO='ponto'` (honesto na auditoria). Os 3 públicos (endereço/CNAE/inscrição) ficaram INTACTOS — `ConsultaViabilidadeServiceTest` 9/9 sem regressão. `App\Services\Solicitacao\SimulacaoSolicitacaoService::simulate(ViabilityRequest): array` itera os CNAEs (principal primeiro via orderByPivot is_primary desc + code), chama o motor PELO PONTO da solicitação (centroide do polígono recomputado em PHP portável, reusando o cálculo do 08-06; SEM polígono → degrada para `consultarPorCnae`, território null, NUNCA inventa ponto), PROPAGA o veredito por CNAE e consolida o PIOR CASO (severidade nao_permitido>pendente>permitido_com_condicoes>permitido; sem CNAEs → pendente). Persiste em DB::transaction (snapshot+auditoria atômicos): `simulation_snapshot` {ponto:{lat,lng}|null, area_m2, por_cnae[{cnae, cnae_formatado, is_primary, tendencia, tendencia_label, consulta=ConsultaViabilidadeResult::toArray() completo}]} + `simulation_rules_versions` {territorio,louos,risco} (representativas do 1º CNAE) + `simulation_resultado` (consolidado) + `simulated_at`=now() (RN-003 — o protocolo 08-10 LÊ o snapshot, NÃO reprocessa). `Portal\SolicitacaoSimulacaoController@store`: `Gate::authorize('update')` (dono+rascunho, policy 08-05) → toggle `features.simulacao_solicitacao` off degrada comunicado (back+status, sem simular/persistir, sem erro — RN-005) → senão simula e devolve flash `simulacao` {resultado, resultado_label, por_cnae, rules_versions, simulated_at}. ORIENTATIVA: NÃO bloqueia o protocolo, NÃO muda status, NÃO mexe em applicant_proceeded_despite (a ciência "prosseguir mesmo assim" é do 08-10) — RN-002. Auditoria `solicitacoes/simulacao` result sucesso com versões+resultado (RN-002). ANTI-FACHADA: sem zona → pendente PROPAGADO (nunca permitido/não permitido inventado); mesma versão de regras do fluxo oficial. Rota `POST portal/solicitacoes/{solicitacao}/simular` (name portal.solicitacoes.simular). CONTRATO p/ 08-10 (lê o snapshot + registra applicant_proceeded_despite) e 08-13 (etapa do wizard consome flash `simulacao` e reusa ResultadoViabilidade por CNAE). ZERO dependência nova. TDD estrito (RED→GREEN com evidência fresca): SimulacaoSolicitacaoTest 9/9 (46 asserções) + ConsultaViabilidadeServiceTest 9/9 (anti-regressão); filtros do plano 18/18; suíte completa 770/770 (3896 asserções, incl. 16 @group postgis com container healthy + os 11 testes do 08-08). Commits 9dab9dc (entrada por ponto) / e949390 (service+controller+rota+toggle).
- [08-10] PROTOCOLAR (HU-068) — Wave 6, o CORAÇÃO da fase e o PRIMEIRO evento de domínio do sistema (não havia `app/Events/`). `ProtocolarSolicitacaoService::protocol(ViabilityRequest, User $actor, bool $proceedDespite = false): ViabilityRequest` valida as pré-condições FORA da transação (status rascunho → `InvalidStatusTransitionException` [08-01]; dados mínimos empresa/imóvel-polígono/área/CNAE principal → `SolicitacaoIncompletaException`; documentos obrigatórios via `DocumentRequirementResolver::missing()` [08-08] → `DocumentacaoIncompletaException`) — bloqueio NÃO consome número (anti-fachada). DENTRO de `DB::transaction`: `ProtocolNumberGenerator::generate()` [08-01] (`protocol_sequences->lockForUpdate()` + `unique` defesa final), `forceFill` protocol_number/protocoled_at/applicant_proceeded_despite e `ViabilityRequestStateMachine::transition(rascunho→protocolada, publicLabel 'Recebida — em processamento')` (timeline + auditoria `solicitacoes/transicao` RN-002). Ciência (`applicant_proceeded_despite`) só quando `proceed_despite=true` E `simulation_resultado==='nao_permitido'` (indeferimento; 'pendente' é indeterminado, NÃO dispara); snapshot do 08-09 CONGELADO (RN-003 — não chama markSimulationStale, não reprocessa). APÓS o commit `SolicitacaoProtocolada::dispatch($request)` — `app/Events/SolicitacaoProtocolada` (implements `ShouldDispatchAfterCommit`, Dispatchable, `public ViabilityRequest $request`); DECISÃO: dispatch FORA da transação (após retornar) torna "não dispara no rollback" testável com Event::fake, e a interface é contrato/defesa adicional. Listener síncrono `RegistrarTrilhaProtocolo` (registrado por `Event::listen` no `AppServiceProvider::boot` — convenção do projeto, NÃO auto-discovery): marco amigável da timeline IDEMPOTENTE (a transição já grava o public_label; só cria "se ainda não houver") + auditoria de alto nível `solicitacoes/protocolada`. CRÍTICO (anti-fachada): a auditoria do protocolo INDEPENDE do evento — a transição síncrona garante RN-002 mesmo se um listener falhar; ganchos EP09 (elegibilidade)/EP11 (notificação)/EP13 (Regin) só DOCUMENTADOS no PHPDoc do evento (penduram listeners no MESMO evento, sem tocar o protocolo). `ProtocoloController@store`: `Gate::authorize('protocol')` (ViabilityRequestPolicy [08-05], dono+rascunho — terceiro/não-rascunho → 403 auditado CA-04) → toggle `features.solicitacao_viabilidade` off degrada comunicado (flash status) → traduz as exceções de domínio em `flash.error` com a lista (aviso, nunca silencioso) → sucesso redirect `portal.solicitacoes.index` + flash `status` com o número (rota de detalhe/consulta é o 08-11). `ProtocolarSolicitacaoRequest` (proceed_despite boolean). Rota `POST portal/solicitacoes/{solicitacao}/protocolar` (name `portal.solicitacoes.protocolar`) após as literais. ZERO dependência nova. TDD estrito (RED→GREEN com evidência fresca): ProtocolarSolicitacaoTest 13/13 (62 asserções, SQLite) + ProtocolarConcorrenciaPostgisTest 1/1 (@group postgis, lockForUpdate REAL, sem skip com POSTGIS_TESTS_REQUIRED=true); suíte completa 784/784 (3966 asserções, incl. 19 @group postgis com container sile-pgsql healthy). Commits ef8b6f7 (service+evento+exceções) / 92acb4d (listener+controller+request+rota+registro) / d7e704a (postgis concorrência).
- [08-12] CANCELAR (HU-070) — Wave 7, em paralelo com 08-11/08-13/08-14/08-15 no MESMO diretório. `CancelarSolicitacaoService::cancel(ViabilityRequest,User $actor,string $reason): void` lê os estados canceláveis do parâmetro `solicitacao.cancelamento.estados_cancelaveis` (json, default `["rascunho","protocolada"]`, fallback espelhado em config/sile.php — Settings::get banco→cache→config, efeito sem deploy HU-014; definição fina pendente SEDUR). Se o status atual está FORA da lista → AUDITA o bloqueio (`AuditService::log('solicitacoes','cancelamento-bloqueado', subject=solicitação, result='bloqueado')`) e lança `CancelamentoNaoPermitidoException` (->status, ->estadosCancelaveis) — CA-03, anti-fachada (nada é cancelado, timeline intacta). Dentro dos estados, em DB::transaction grava cancelled_at/cancelled_reason/cancelled_by_user_id (forceFill — fora do #[Fillable], como protocol_number) e transiciona para Cancelada pela `ViabilityRequestStateMachine` (reason=$reason, publicLabel 'Cancelada a pedido do requerente') — timeline (viability_request_transitions) + auditoria 'solicitacoes/transicao' RN-002. CAMADAS: a `ViabilityRequestPolicy::cancel` (08-05) garante dono + não-decidido (rascunho/protocolada) e barra terceiro com 403 auditado (CA-04); a regra FINA de estados é do parâmetro no service — com o parâmetro restrito (ex.: ['rascunho']) uma protocolada passa na policy mas é bloqueada+auditada no service. `CancelamentoSolicitacaoController@destroy`: Gate cancel → `CancelarSolicitacaoRequest` (reason required|string|max:1000, pt-BR) → traduz CancelamentoNaoPermitidoException em flash.error; sucesso → redirect index + flash status. Rota `DELETE solicitacoes/{solicitacao}` (name portal.solicitacoes.cancelar) ANEXADA após as literais e demais {solicitacao} (reli routes/portal.php antes; staging individual; git diff = só import+rota). SEM toggle de feature: cancelar o próprio processo é direito do requerente, não deve depender do toggle de novas solicitações (degradação correta). DECISÃO sobre o plano: auditoria do bloqueio via `log(subject,result='bloqueado')` em vez de `logBlocked()` (que o plano sugeria) — logBlocked fixa event 'acesso-negado' e NÃO grava subject; o bloqueio aqui é de ESTADO (CA-03), não negação de acesso (CA-04) — `log` com event 'cancelamento-bloqueado' + subject + result 'bloqueado' deixa o RN-002 mais completo (registra QUAL solicitação). ÚNICO DONO da mudança de catálogo na wave: adicionou solicitacao.cancelamento.estados_cancelaveis (catálogo 48→49) ao ParameterSeeder + config/sile.php e atualizou as contagens em ParameterSeederTest e DatabaseSeederTest (48→49 em todos os pontos). ZERO dependência nova. TDD estrito (RED→GREEN com evidência fresca): CancelarSolicitacaoTest 6/6 (45 asserções) + ParameterSeederTest/DatabaseSeederTest 19/19 (contagem 49); filtros do plano 25/25 (280 asserções); regressão focada criar/protocolar/cancelar+seeders 46/46; suíte completa 794/798 (as 4 falhas são EXCLUSIVAMENTE do 08-11 em andamento — ConsultarProtocoloTest, rota portal.protocolo.publico ainda não registrada — NÃO regressão do 08-12); pint --dirty limpo. Commits bebca1d (parâmetro 49) / 20faf21 (service+exceção+controller+request+rota+testes). PARALELO seguro: ca32767 (08-11) NÃO tocou routes/portal.php; não toquei arquivos de 08-11/13/14/15.
- [08-15] ATENDIMENTO PRESENCIAL ASSISTIDO (HU-150) — Wave 7, em paralelo com 08-11/08-13/08-14 no MESMO diretório. Canal de balcão (inclusão digital) REUSANDO integralmente o "em nome de" da Fase 1, sem recriar. `AssistedAttendance` (attendant_user_id/citizen_user_id/started_at/expires_at/ended_at; scope active()=ended_at null & expires_at>now; isActive()) — NÃO é procuração jurídica (sem outorga), é atendimento de balcão. `CurrentRepresentation` estendido de forma COMPATÍVEL: setAttendance()/attendance()/clearAttendance() + `grantor()` agora retorna `$this->attendance?->citizen ?? $this->procuration?->grantor` (atendimento tem PRECEDÊNCIA; procuração intacta — guard LinkAttorney/RevokeAttorney/MyCompanies 27/27 verde). `ResolveAssistedAttendance` ESPELHA o ResolveRepresentation: lê attending_attendance_id da sessão, revalida (pertence ao atendente, ativo, não expirado) e popula o MESMO Context('acting_for_user_id')/CurrentRepresentation::setAttendance; expirado/ausente/de-outro-atendente limpa o estado + descarta a sessão (CA-03 exige reabertura). Aplicado por CLASSE na rota (como o portal aplica ResolveRepresentation) — bootstrap/app.php NÃO alterado (desvio do files_modified, fiel ao padrão citado no plano). ATENDENTE OPERA POR ENDPOINTS DA GESTÃO (guard gestao): sessões separadas por ambiente ([2026-06-12]) impedem usar as rotas do portal; os endpoints reusam o domínio (effectiveUser/ViabilityRequestPolicy/auditoria RN-002), mudando só ambiente/ator. `Gestao\AssistedAttendanceController`: store (inicia pelo CPF — ValidCpf + lookup; cidadão SEM conta bloqueia, NÃO fabrica — anti-fachada; expires_at=now()+solicitacao.atendimento.expiracao_minutos [08-02, já seedado]; auditoria 'atendimento/atendimento-iniciado'), storeSolicitacao (ABRE a solicitação direta em nome do cidadão — requester=cidadão, created_by=atendente, origin portal, assisted_attendance_id; guard de atendimento ATIVO ANTES da validação → expirado bloqueia com reabertura; reusa o escopo por vínculo ativo do StoreSolicitacaoRequest replicado inline; auditoria 'created' com causer=atendente E acting_for=cidadão = os DOIS CPFs RN-001), destroy (encerra ended_at; auditoria 'atendimento-encerrado'), index (estação do console). Rotas gestao.atendimento.{index,store,destroy,solicitacoes.store} sob `permission:atendimento-presencial` [08-02, já seedada gestor+admin] + ResolveAssistedAttendance — ANEXADAS após o grupo de contingência do 08-14 (que commitou primeiro — 15cc505; diff = só meu trecho, staging individual). RN-005 (dimensão balcão): coluna nullable `assisted_attendance_id` em viability_requests (migração própria nullOnDelete; origin permanece 'portal'; reports EP15 filtram whereNotNull). CA-01 provada pela ABERTURA (ação primária do atendente na HU); o PROTOCOLO (HU-068) reusa o MESMO ProtocolarSolicitacaoService/transição auditada (provado no 08-10) — muda só ambiente/ator. CA-02 (escopo limitado): rotas de análise/decisão são Fases 9/10; hoje o middleware de permissão é o enforcement — provado contra /gestao/parametros (manter-parametros que o gestor-atendente NÃO tem → 403 auditado). Banner via shared prop `attendingFor` (espelha actingFor; preenchido pelo CurrentRepresentation::attendance() — middleware roda SÓ nas rotas do atendimento, Context não vaza para ações não relacionadas) + tela gestao/atendimento/index.tsx + item de navegação por permissão. PENDÊNCIA SEDUR (RN-004, degrada honesto): instrumento de ciência presencial (assinatura/impresso/gov.br) e cadastro assistido do cidadão no balcão — não bloqueiam o canal. ZERO dependência nova. NÃO toquei ParameterSeeder/config/sile.php/seeder-tests (catálogo é do 08-12 na wave) nem arquivos de 08-11/13/14. TDD estrito (RED→GREEN com evidência fresca): AtendimentoPresencialTest 15/15 (52 asserções: modelo+CurrentRepresentation+middleware+5 fluxos CA-01..04/RN-005); filtros do plano + guard 42/42; suíte completa 825/825 (4212 asserções, incl. @group postgis com container sile-pgsql healthy); pint/typecheck/build verdes. Commits d1d7082 (modelo+CurrentRepresentation+coluna) / b4c7a91 (middleware) / 7d9f0b0 (controller+rotas+tela+fluxos).

- [08-14] CONTINGÊNCIA (HU-148) — Wave 7, em paralelo com 08-11/08-12/08-15 no MESMO diretório. Canal de operador na retaguarda e o caminho REAL de operação enquanto o contrato Regin não chega (Fase 13) — zero adaptador simulado. `Gestao\ContingenciaController@store` cria a solicitação `origin=Contingencia` (auditada, RN-003) com `requester`=beneficiário (informado por CPF, resolvido para um User existente) e `created_by`=operador, e PROTOCOLA pelo MESMO `ProtocolarSolicitacaoService` do canal normal (08-10) numa transação externa — sem atalho decisório (RN-002): os guards do motor (status rascunho via `refresh()` do default do banco; dados mínimos; documentos obrigatórios via `DocumentRequirementResolver::missing()`) valem igual e qualquer bloqueio REVERTE a criação inteira (anti-fachada — nada meio-criado, número de protocolo não consumido). Motivo obrigatório (RN-001, `RegistrarContingenciaRequest`: CPF via ValidCpf→User existente; CNPJ→Company existente). RN-004 (não duplica): `DuplicateRequestDetector::detectByExternalReference` (ADITIVO; `detect()` intacto, portal inalterado) BLOQUEIA a mesma referência externa (BAP/Regin) não cancelada; reincidência por CNPJ permanece ALERTA não-bloqueante (reuso do `detect`, RN-007/direito de petição). Anexos opcionais por requisito (`documents[{requirement_id}]`) gravados como no 08-08 (disk Settings nunca público + sha256 real + auditoria) DENTRO da transação com limpeza no rollback (entrega-funcional: o gate documental protocola de verdade quando satisfeito — test_anexo_satisfaz_requisito; test_mesmo_motor prova o bloqueio). Geometria derivada via `PropertyGeometryWriter` (08-06); CNAE picker reusa o `CnaeSearchController` na rota `gestao.contingencia.cnaes-disponiveis` (só ativos, como 08-04). Sem permissão `registrar-contingencia` → 403 auditado (CA-04). CONSEQUÊNCIA do schema (requester_user_id NOT NULL + protocolo exige company_id): beneficiário e empresa precisam PRÉ-EXISTIR → bloqueio comunicado honesto (nunca dado fantasma); auto-provisionamento/requester nullable é evolução futura (mudança de schema, fora do escopo). Rotas `gestao.contingencia.{create,store,cnaes-disponiveis}` ANEXADAS em routes/gestao.php (COMPARTILHADO com 08-15: removi cirurgicamente os trechos do 08-15 para um commit self-contained, commitei só o meu grupo, e restaurei os deles; o 08-15 anexou após — ambos os grupos coexistem no HEAD, zero clobber). Tela `gestao/contingencia/create.tsx` (console: aviso de origem, mapa do imóvel, picker de CNAE, anexos por requisito) + item de navegação por permissão (gestao-layout.tsx compartilhado — o 08-15 já commitou o item de atendimento; meu diff = só contingência). ZERO dependência nova. TDD estrito (RED→GREEN com evidência fresca): ContingenciaTest 8/8 (52 asserções); suíte completa 826/826 (4223 asserções, incl. 19 @group postgis com container sile-pgsql healthy); pint/typecheck/build verdes. Commits 15cc505 (backend) / 2fe98f5 (tela). [Posição/progresso/continuidade da wave: integração final do orquestrador.]

- [15-02] CONTRATO ÚNICO DE EXPORTAÇÃO (HU-131) — Wave 1, dono único do contrato (executado isolado das frentes paralelas do usuário — Fase 14 IA/e-mail + sidebar/vitest — via staging seletivo; nenhum arquivo deles tocado/commitado). DECISÃO DE CONTRATO (a)+(b): (a) `App\Services\Relatorios\ReportFilters` é um BAG SERIALIZÁVEL (`final readonly class`, `__construct(public array $bag)`): `fromArray` normaliza (trim, descarta vazios/nulos) e PRESERVA o vocabulário completo das telas; `toArray` faz round-trip idêntico — é o que o Job serializa e reconstrói, garantindo RN-005 no SÍNCRONO E no ASSÍNCRONO e evitando dump de PII da trilha (RN-007); `toProcessoFiltros()` recorta as 17 chaves do `ProcessoQueryService::filtered` (grupo/status/protocolo/bap/produto_tvl/servico/setor/analista/inscricao/cep/logradouro/bairro/nome/cnpj/data_de/data_ate/categoria); acessores tipados `from()`(startOfDay)/`to()`(endOfDay)/`setorId()`/`analistaId()`/`bairro()`/`cnae()`/`categoria()` (validada contra ProcessoQueryService::CATEGORIAS, fora do whitelist → null); (b) marcador `SyncOnlyReportSource` (interface que estende ReportSource) para sources com ESTADO de construtor (ex.: produtividade nominal 15-06) — o `ReportExporter` FORÇA o caminho síncrono para elas (nunca despacha o Job, que perderia o estado). `ReportDefinition` (`final readonly`: titulo/colunas/builder Closure/mapRow Closure/filtrosAplicados/logName/event/personalData/arquivoBase; métodos columnLabels/builder/mapRow/fileName). `ReportSource::definition(ReportFilters): ReportDefinition` — INVARIANTE: reconstrutível SÓ pelo bag; as Closures builder/mapRow NUNCA são serializadas (só sourceClass + bag trafegam). `ReportFormatExporter` (stream(): Response síncrono; write(path): void assíncrono). Drivers: `CsvExporter` (fputcsv + chunk, sem total no corpo — RN-010, integridade tabular) e `PdfExporter` (dompdf sobre Blade `relatorios.relatorio` com rodapé 'Total de registros: N' + data/hora + filtros — CA-07/RN-010; `dados()` PÚBLICO para verificar conteúdo via render do Blade, já que o dompdf comprime os streams do PDF). `ReportExporter::export(ReportSource, ReportFilters, string $formato, ?User): Response` — valida o formato contra `relatorios.export.formatos_habilitados` ∩ drivers disponíveis (['csv','pdf'] hoje; xlsx RECUSADO até 15-08 — anti-fachada); AUDITA SEMPRE (RN-008: logName+event do definition + tela/filtros/formato/volume + personalData); abaixo de `relatorios.export.assincrono_limiar_linhas` (Settings→config 5000) OU SyncOnly → `driver($formato)->stream($definition)`; acima → `GerarExportacaoJob::dispatch($source::class, $filtros->toArray(), $formato, $user?->id)` + `response()->noContent(202)` (CA-06, tela não trava). `App\Jobs\GerarExportacaoJob` (ShouldQueue; tries/timeout/backoff/fila de `config('sile.relatorios.job.*')` — espelha DecidirFluxoExpressoJob): `handle` reconstrói via `app($sourceClass)->definition(ReportFilters::fromArray($filtros))` (RN-005 async); disco `relatorios.export.disk` NÃO público (guarda anti-'public' como TvlPdfService); local → `Storage::path()`, remoto → tempnam + `Storage::put`; cria `ExportFile` (row_count REAL) e notifica o dono; `failed()` AUDITA 'relatorios'/'exporta-falha'/result='falha' e NÃO cria ExportFile/link (anti-fachada, Pitfall 6). Notification STANDALONE `App\Notifications\ExportacaoPronta` (canais database+mail — NÃO o NotificationDispatcher, que é process-bound; link por `URL::temporarySignedRoute('gestao.relatorios.exportacoes.download', ...)`). Model/migration `export_files` (`2026_06_15_000002`: user_id nullOnDelete / disk / path / filename / format / row_count / filtros jsonb; up/down validados ISOLADOS em pgsql via `--path`) + factory. PENDÊNCIA p/ 15-09: rota+endpoint de download `gestao.relatorios.exportacoes.download` (URL assinada servindo o disco não-público); 15-08 adiciona o driver XLSX (openspout). ZERO dependência nova (openspout/echarts já vieram no 15-01). TDD estrito (RED→GREEN com evidência fresca): `php artisan test --compact --filter=Relatorios` 23/23 (112 asserções; 20 dos 4 arquivos do 15-02 — ReportFiltersTest 7, ExportDriversTest 4, ReportExporterTest 6, GerarExportacaoJobTest 3 — + 3 pré-existentes que referenciam "relatorios"); RN-005 provado no sync (linha fora do filtro não aparece no CSV) e no async (handle do Job); guarda SyncOnly (acima do limiar ainda streama, nada despachado); PDF '%PDF' real; failed() audita sem ExportFile; pint limpo nos meus arquivos. Commits 2bfdc6e (contrato+ReportFilters+ExportFile) / d79b8dd (drivers+Blade) / 544fec0 (exporter+job+notification).

- [15-06] PRODUTIVIDADE POR ANALISTA (HU-130) — Wave 2, sobre o contrato de 15-02, em modo CONSERVADOR RH/LGPD (executado isolado das frentes paralelas do usuário — Fase 14 IA/e-mail + sidebar/vitest — e de um agente paralelo do 15-08 XLSX que commitou entre minhas tasks; staging seletivo, nenhum arquivo deles tocado/commitado). `App\Services\Relatorios\ProdutividadeAnalistaService::porAnalista(ReportFilters, bool $nominal=false, ?int $scopeUserId=null): array` agrega em SQL as decisões da análise técnica por analista (`viability_decisions` `where flow='analise_tecnica'` + `whereNotNull(decided_by_user_id)` + `when()` período em `decided_at` + `groupBy(decided_by_user_id)` + `selectRaw count(*)/sum(case when outcome=? ...)` com BIND dos outcomes → `decididas`/`deferidas`/`indeferidas`) e complementa com os processos atribuídos por `viability_requests.assigned_user_id` (`atribuidas`, agregação SQL separada por período em `protocoled_at`). DEFAULT AGREGADO/ANONIMIZADO (minimização RN-007): cada linha recebe o rótulo ordinal estável `Analista #N` (ordem de volume), SEM nome nem id. `nominal=true` (parâmetro; o gate `relatorios.produtividade.nominal` e a auditoria moram no HTTP de 15-09) faz join em `users` e inclui `analista_id`+`nome`. `scopeUserId` (analista vendo o próprio) filtra `decided_by_user_id=scopeUserId` e IGNORA o nominal (é o próprio) → só a linha dele, anonimizada. Sem decisões no período ⇒ `[]` (CA-03 anti-fachada — nunca produtividade inventada). Método público `decisoesAgregadas(): Builder` extraído como FONTE compartilhada com o source. `App\Services\Relatorios\Export\Sources\ProdutividadeReportSource implements ReportSource, SyncOnlyReportSource` (construtor `(bool $nominal=false, ?int $scopeUserId=null)`): exporta com a MESMA minimização — colunas `analista_rotulo` por default / `analista_nome` só sob `nominal`; `personalData=nominal`; `event='exporta-produtividade'`, `arquivoBase='produtividade-analistas'`. É a CORREÇÃO do checker para o estado de construtor: implementar `SyncOnlyReportSource` faz o `ReportExporter` FORÇAR o síncrono e NUNCA despachar o `GerarExportacaoJob` (que reconstruiria via `app($sourceClass)` e perderia nominal/escopo). O builder ENVELOPA `decisoesAgregadas` em `fromSub` (para o `count()` do exporter contar ANALISTAS, não a contagem do 1º grupo) + `row_number() over (order by decididas desc, analista_id asc)` para o ordinal anônimo estável no streaming por chunks. DESVIO DO PLANO (coluna): o plano citava `assigned_user_id (analysis_records)`, mas `analysis_records` usa `analyst_user_id`; `assigned_user_id` mora em `viability_requests` — usei a coluna real/indexada, honrando o must_have "por decided_by_user_id e assigned_user_id". TDD estrito (RED→GREEN): `--filter=ProdutividadeAnalistaServiceTest` 4/4 (26 asserções) + `--filter=ProdutividadeReportSourceTest` 5/5 (17) = `--filter=Produtividade` 9/9 (43); ruído (decisão expressa do sistema + decisão da Ana em outro fluxo) NÃO conta; default sem nome/id; nominal sob flag; escopo só o próprio; período vazio → `[]`. pint limpo nos 4 arquivos. NÃO rodei `migrate` global (migrations pendentes da Fase 14 — evidência via RefreshDatabase/SQLite). Commits 0168c1e (serviço) / 8973c5d (source). PENDÊNCIA p/ 15-09: instanciar o source resolvendo `nominal` pela permissão e `scopeUserId=$request->user()->id` para o analista (gestor vê todos) + auditar a consulta nominal; rota de download `gestao.relatorios.exportacoes.download` (herdada do 15-02). Política definitiva de quem vê nominal = confirmação SEDUR (default conservador não bloqueia).
- [15-08] DRIVER XLSX (HU-131) — Wave 2, terceiro formato do contrato único de 15-02 (executado isolado das frentes paralelas do usuário — Fase 14 IA/e-mail + sidebar/vitest — e de agentes paralelos da wave 2 que commitaram entre/depois das minhas tasks: 8973c5d 15-06, e9f707b 15-03; staging seletivo por caminho, nenhum arquivo deles tocado/commitado). `App\Services\Relatorios\Export\XlsxExporter implements ReportFormatExporter` (`final class`) — openspout v4.32.0 (NÃO v5): itera `definition->builder()->cursor()` (RN-005, baixa memória, nunca all()) escrevendo linha a linha; cabeçalho em negrito via `Row::fromValues($labels, (new Style)->setFontBold())`. `write(definition, $absolutePath)` = `openToFile` direto (assíncrono, caminho real do Job — Pitfall 4, XLSX é ZIP e exige arquivo gravável). `stream(definition): Response` = gera o XLSX num arquivo TEMPORÁRIO via `openToFile` e o devolve por `response()->streamDownload(fpassthru, fileName('xlsx'), ['Content-Type' => 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'])` — DECISÃO (resolve a questão aberta do plano): NÃO usar `openToBrowser` do openspout, que chama `header()`/`ob_end_clean()` por fora e conflita com o ciclo da `StreamedResponse` do Symfony (headers enviados primeiro) + destrói o buffer do PHPUnit, impossibilitando asserir status 200 + Content-Type na resposta; o XLSX já é bufferizado em temp pelo próprio openspout, então `fpassthru` mantém a baixa memória. `ReportExporter`: ramo `'xlsx' => app(XlsxExporter::class)` no `match` + `DRIVERS_DISPONIVEIS` `['csv','pdf']`→`['csv','xlsx','pdf']` (o config `relatorios.export.formatos_habilitados` já trazia 'xlsx' do 15-01). DESVIO ANTI-FACHADA (RULE 2): `GerarExportacaoJob::driver()` tem o PRÓPRIO `match` (csv/pdf) — adicionei `'xlsx'` lá também; sem isso, exportação xlsx acima do limiar despacharia o Job que estouraria `InvalidArgumentException` no `handle()` (`failed()` auditaria falha, arquivo nunca gerado) = fachada quebrada no async, violando `entrega-funcional` e o must_have "encaixe no Job assíncrono"; commit `fix` atômico separado. DESVIO ANTI-REGRESSÃO: `ReportExporterTest::formato_nao_disponivel_e_recusado` (15-02) asseria xlsx recusado "até 15-08" — agora recusa `'json'` (formato sem driver), preservando a intenção anti-fachada para formatos desconhecidos (sem remover teste). TDD estrito (RED→GREEN pelo motivo certo, evidência fresca): `--filter=XlsxExporterTest` 4/4 (16 asserções — write/Reader; xlsx síncrono 200+Content-Type+auditoria 'exporta-processos-xlsx'; assíncrono despacha Job com formato xlsx; Job REAL grava XLSX legível e filtrado no disco — RN-005 async ponta a ponta) + anti-regressão `--filter=ReportExporterTest` 6/6 e `--filter=GerarExportacaoJobTest` 3/3; `--filter=Relatorios` 42/42 (186 asserções). pint com caminhos explícitos (NÃO `--dirty`, que reformataria os PHP não commitados do usuário); `migrate` global NÃO rodado (Fase 14 pendente — RefreshDatabase/SQLite). Commits 817bd0b (XlsxExporter+teste) / 6eeaa09 (liga ReportExporter+anti-regressão) / 07d1c8a (fix xlsx no Job). ZERO dependência nova. PENDÊNCIA herdada p/ 15-09: rota de download `gestao.relatorios.exportacoes.download` (o async xlsx já grava o `ExportFile` corretamente).

- [15-05] TEMPO POR ETAPA (HU-129) + RELATÓRIOS SAPS DE TEMPO (RN-006) — Wave 3, sobre 15-02/15-04 (executado isolado das frentes paralelas do usuário — Fase 14 IA/e-mail + sidebar/vitest; staging seletivo por caminho, nenhum arquivo deles tocado/commitado). `App\Services\Relatorios\TempoAnaliseService` (route-free, injeta `BusinessDeadlineCalculator`): `tempoPorEtapa(ReportFilters): array` mede o tempo médio POR ETAPA em MINUTOS ÚTEIS — varre `viability_request_transitions` dos processos protocolados no período (SQL escopado por `protocoled_at` + join em viability_requests + `cursor()` baixa memória), pareia as transições consecutivas por processo e soma a duração ÚTIL de cada etapa via `businessDurationBetween` (15-04, desconta fim de semana/feriado). 4 etapas pelo status DEIXADO (from_status): `rascunho→preenchimento`, `protocolada→espera`, `aguardando_bap→espera` (Junta/BAP — torna VISÍVEL a espera que não é trabalho da SEDUR, objetivo HU-129), `em_analise→analise`, `em_pendencia→pendencia`; reentradas (em_analise após pendência) SOMAM na mesma etapa, isolando a pendência da análise (corrige a distorção do legado que conta calendário e mistura etapas). Média/amostras POR PROCESSO que passou pela etapa; sem amostras ⇒ `media_minutos=null` (CA-03, jamais 0 disfarçado). `tempoEmissaoTvl(ReportFilters): array` (SAPS RN-006) = `protocoled_at→decided_at` em minutos úteis das decisões com `tvl_product_number IS NOT NULL` e `decided_at` no período (cursor); sem registros ⇒ `media_minutos=null`/`amostras=0`. `sedesEscritorioVirtual(ReportFilters): Builder` (SAPS RN-006) recorta `is_virtual_office=true` + filtros comuns (when), reusado pelo source. Helper PÚBLICO PURO `minutosPorEtapa(criadoEm, transicoes): array` (só etapas ocorridas, base honesta das amostras) compartilhado entre a agregação e `minutosPorEtapaDoProcesso(ViabilityRequest)` (sortBy created_at asc, a relação vem `latest()`). DECISÃO PORTABILIDADE: o desconto de tempo útil é PHP por par de transições (não `LAG` SQL) — não há expressão portável SQLite+pgsql (15-RESEARCH Open Q #3); a varredura/recorte segue em SQL (não é loop-para-contar). EXPORT (RN-005, contrato 15-02): `Export\Sources\TempoAnaliseReportSource implements ReportSource` (detalhamento por processo — protocolo + minutos úteis por etapa + total; builder eager-carrega `transitions` p/ o mapRow; `personalData=false` só protocolo+tempos; `event='exporta-tempo-analise'`, `arquivoBase='tempo-por-etapa'`) e `Export\Sources\EscritorioVirtualReportSource implements ReportSource` (reusa `sedesEscritorioVirtual` como builder; colunas protocolo/empresa/cnpj/bairro/protocolado_em/decidido_em/resultado via `ProcessoResource` — CNPJ formatado, PII minimizada RN-007; `personalData=true`, `event='exporta-escritorio-virtual'`, `arquivoBase='sedes-escritorio-virtual'`); ambos reconstrutíveis só pelo bag (injetam só o serviço, sem estado de construtor → seguem sync OU async). DESVIOS (refinamentos, escopo intacto): (1) ESPERA agrega `aguardando_bap` além de `protocolada` (honra o objetivo "espera/BAP visível" sem criar 5ª etapa); (2) PREENCHIMENTO derivado da 1ª transição (from=rascunho) com marco em `created_at` (unificado no MESMO pareamento, sem caminho especial). TDD estrito (RED confirmado pelo motivo certo → GREEN): `--filter=TempoAnalise` 7/7 (39 asserções — pendência cruzando sáb/dom = 3840 min úteis, NÃO 6720 de calendário = prova anti-distorção; TVL ignora decisão sem número e fora do período; período vazio → null/0; RN-005 nos dois sources; recorte is_virtual_office); `--filter=Relatorios` 70/70 (302 asserções — zero regressão); pint limpo (caminhos explícitos); `migrate` global NÃO rodado (Fase 14 pendente — RefreshDatabase/SQLite). Commits cd3e3ed (serviço tempoPorEtapa+tempoEmissaoTvl+teste) / 2d0bb03 (sedesEscritorioVirtual+2 sources+teste). ZERO dependência nova. PENDÊNCIA p/ 15-09: instanciar os sources via app() + delegar ao ReportExporter + expor os indicadores na tela + auditar consulta; rota de download herdada do 15-02. Pendência SEDUR (degrada honesto): calendário municipal de feriados (herdado 15-04) e layout/colunas oficiais dos relatórios SAPS.

### Roadmap Evolution

- Phase 3.1 inserida após a Phase 3: Fundação assíncrona — scheduler, jobs e retenção (URGENT). Origem: levantamento "Laravel 13 — recursos prontos não usados" (2026-06-12). Escopo: scheduler ativo com primeira rotina real (padrão para HU-134/HU-147), importações REDESIM/CNAE como jobs em fila com retry e relatório, `throttle` parametrizado nas rotas públicas, pruning de access_logs com retenção parametrizada (LGPD; trilha de auditoria RN-002 fica fora) e retry/backoff no HTTP client. Demais recursos do levantamento anotados nas fases consumidoras como "Nota (recursos do framework)": Storage/URLs assinadas (Fases 8 e 10), atomic locks e eventos de domínio (Fases 8 e 9), canal database de Notifications (Fase 11), `Concurrency`/`Http::pool`/`Queue::route()`/Horizon (Fase 13).

### Pending Todos

Nenhum.

### Blockers/Concerns

- [Fase 3.2 / HU-151] Credenciamento no Login Único GOV.BR: a SEDUR ainda não possui client_id/client_secret (Termo de Adesão junto à Secretaria de Governo Digital). A autenticação GOV.BR do portal está IMPLEMENTADA e DESLIGADA (`features.govbr_login` default false); a validação contra `sso.staging.acesso.gov.br` real é critério de conclusão da integração — bloqueada até a credencial. Spec: `docs/superpowers/specs/2026-06-12-autenticacao-govbr-design.md`.
  - Implementação concluída em 2026-06-12 (commits 48194e6/a55369c): GovBrProvider Socialite (PKCE S256, nonce, token via Basic auth, id_token validado contra JWK com firebase/php-jwt), GovBrAuthService (vínculo por CPF, criação de conta cidadao, nível mínimo parametrizável, inativada/conflito de e-mail bloqueados), govbr_accounts 1:1, 6 parâmetros novos (credenciais sensíveis criptografadas), botão oficial nas telas de login/cadastro. 24 testes novos (7 unit JWK + 2 schema + 15 feature); suíte 317/317.
  - Evidência E2E real (browser, 2026-06-12): com credencial de exemplo, o clique no botão redirecionou ao staging real e o gov.br respondeu `invalid_client "cliente-exemplo-dev was not found"` — fluxo OAuth correto de ponta a ponta; falta apenas a credencial verdadeira. Toggle do dev restaurado para desligado.
  - DECISÕES da fase: níveis de confiabilidade lidos do `reliability_info` do id_token (API /confiabilidades é obsoleta no roteiro — parâmetro api_base_url removido); nível ausente = bronze; primeiro acesso sem e-mail verificado no gov.br é bloqueado com orientação (users.email é NOT NULL); e-mail do gov.br NUNCA sobrescreve o local nem vincula sozinho (CPF é a única chave); logout local apenas (sem Single Logout); seeder de parâmetros ganhou suporte a `sensitive` via forceFill (fora do fillable, decisão 02-02).
- [Produto] Painel consolidado do cidadão (padrão SIGVISA: situação atual ao logar — empresas, solicitações, DAMs, TVLs): NÃO há HU no catálogo (HU-122 é dashboard do GESTOR). Lacuna identificada em 2026-06-10; decisão pendente (criar HU-132 proposta ou evoluir o painel dentro das fases). Pauta para a SEDUR. As peças surgem nas Fases 3 (empresas), 7 (consultas), 8 (solicitações) e 9 (TVL) — o painel atual de atalhos evolui junto.
- [Identidade] Marca institucional oficial a confirmar: o usuário indicou a página de marcas da SEDUR **estadual** (https://www.ba.gov.br/sedur/institucional/marcas — manual Secom, marca Sedur, marca do Governo, brasão do Estado; arquivos em SharePoint com login, download direto bloqueado). A documentação do projeto referencia a SEDUR **municipal de Salvador** (LOUOS municipal, Decreto 32.636/2020, Prefeitura de Salvador). Confirmar qual ente é o dono do SILE antes de aplicar marca institucional/co-branding; até lá, o sistema usa a logo própria do SILE (components/app/logo.tsx).

Pendências com a SEDUR (pauta: docs/ANALISE-HUs-REUNIAO-SEDUR.md seção 5). Nenhuma bloqueia as Fases 1 a 3.

- [Fase 8] HU-071/HU-072 (DAM e pagamento): escopo a confirmar — não implementar antes da confirmação.
- [Fase 13] HU-110 (SEFAZ): API confirmada; endpoint de envio do deferimento e credenciais SenhaWeb pendentes.
- [Fase 13] HU-111 (migração do legado .NET): estratégia a definir.
- [Fases 3/13] Contrato REDESIM/integrador (entrada e devolução de parecer): aguardando documentação.
- [Fases 4/13] Base GIS municipal (camadas, formato, acesso): aguardando — Fase 4 opera com camadas carregadas de dados oficiais da LOUOS até a entrega.
- [Fase 4/13 — FONTE GIS OFICIAL IDENTIFICADA, 2026-06-15] O usuário confirmou que a base GIS oficial da SEDUR é **Oracle Spatial exposto pelo GeoServer** (`https://geoserver.sedur.salvador.ba.gov.br`) — não a referência genérica anterior "SIGIS/CA 2000/S69". DECISÃO DE ARQUITETURA: consumir via **OGC WFS** (vetorial→GeoJSON, alimenta a camada `zona` em `geo_layers` para o `ST_Contains` do `TerritoryService` — destrava HU-031 → Quadro 10/Fase 5) e **WMS** (tiles, overlay no mapa Leaflet); o SILE NÃO conecta ao Oracle direto (sem driver/acoplamento) e reusa o versionamento/auditoria do `GeoLayerService` via um `WfsGeoLayerImporter` a criar (Fase 13/HU-107). A arquitetura da Fase 4 (`geo_layers`/`geo_features` versionadas, importador GeoJSON) já é genérica — muda a carga, não a lógica. **Acesso externo BLOQUEADO pela política de segurança da PMS** (firewall "Portal de Acesso Remoto Salvador Digital" barra o IP externo — verificado por WebFetch 2026-06-15): bloqueio RECLASSIFICADO de "fonte vetorial inexistente" para "fonte existe, acesso de rede restrito". PENDENTE SEDUR/NTI: liberação de rede para o IP do servidor do SILE; confirmação das camadas que o GeoServer expõe (zoneamento LOUOS? lote? classificação viária Mapa 04?) e de credenciais; WFS é essencial (WMS sozinho, só imagem, não serve ao enquadramento por zona). PRÓXIMO PASSO ÚTIL p/ destravar: GetCapabilities do WFS (`.../geoserver/ows?service=WFS&request=GetCapabilities`) de dentro da rede. Nenhuma implementação feita (sem acesso = sem validação real; anti-fachada).
- [Fase 4 — RESOLVIDO PARCIAL/BLOQUEADO, 2026-06-13] EP04 concluído com escopo honesto. ENTREGUE com dado público real do GeoSalvador (ArcGIS REST, f=geojson&outSR=4326): bairro (171, Dec. 38.776/2024), eixo viário (800, extrato central) e restrições ambientais ZEIS (234, PDDU 2016); geocodificação Nominatim real atrás de contrato. BLOQUEADO pendente SEDUR (sem fonte vetorial pública — comunicado na UI como "pendente SEDUR", nunca inventado): **zona urbanística LOUOS** (HU-031 — Quadro 10 só em PDF) e **lote cadastral** (HU-033 — Cadastro Multifinalitário/SEFAZ restrito); por consequência HU-037 RN-005 (divergência por inscrição imobiliária). A classificação viária LOUOS (Quadros 11/11A) entra como geometria, mas o ATRIBUTO operacional pende de confirmação SEDUR (PDDU 2016 ≟ Mapa 04 da LOUOS). IMPACTO NA FASE 5: o motor de enquadramento depende da zona urbanística — manter o bloqueio explícito; o motor nasce parametrizável e roda com seeds derivados da Lei 9.148/2016 até a base oficial chegar. A infraestrutura (camadas versionadas, TerritoryService, sobreposição polígono×lote) já está pronta e testada — quando a SEDUR entregar SIGIS/CA 2000, muda a carga, não a lógica.
- [Fase 5] Quadros parametrizados vigentes e correspondência "Quadro 11" ↔ 11B: a confirmar — motor nasce parametrizável.
- [Fase 13] Acesso a ambiente de homologação (integrador/SEFAZ/GIS): solicitado.

## Session Continuity

Last session: 2026-06-16 (Fase 15: 15-11 — retrofit de export HU-131 nas 5 listagens das Fases 1–2: cnaes/parâmetros/usuários/perfis/acessos ganham ?formato= delegando ao ReportExporter via 5 ReportSources novos, sem rota nova)
Stopped at: Completed 15-11-PLAN.md (Fase 15 — retrofit de export HU-131/RN-009 nas listagens das Fases 1–2: 5 ReportSources novos (CnaesReportSource/ParametrosReportSource/UsuariosReportSource/PerfisReportSource/AcessosUsuarioReportSource) + branch ?formato= em CnaeController/ParameterController/UserManagementController/RoleController/AccessHistoryController, SEM rota nova; RN-005 via bag reconstrutível (fromArray); RN-007 usuários cpf_masked por default — gate de PII no bag desligado no controller por não haver permissão de PII, PENDÊNCIA DPO; RN-008 auditado; parâmetro sensível → [sensível]; acessos com colunas reais da tela evento/ip/canal/data-hora. --filter=RetrofitListagens 10/10, --filter=Relatorios 114/114; pint limpo; TDD RED→GREEN; commits 07b3686/5604f9e/35f643c/06c4982/82de476/139299c; staging seletivo, nada da Fase 14 tocado). [Antes] Completed 15-10-PLAN.md (Fase 15 — retrofit dos 2 CSVs históricos no contrato único HU-131/RN-009: AuditoriaController::export e ProcessoController CSV delegam ao ReportExporter via AtividadesReportSource/AcessosReportSource/ProcessosReportSource; fputcsv eliminado dos dois controllers; colunas (12/6/9)/base de arquivo/personalData/guarda de volume — generalizada como ReportDefinition::maxRows honrado por CSV/XLSX/PDF — preservados; XLSX/PDF herdados pelo mesmo ?formato=; anti-regressão 12-04/10-14 verde, 338/338 Relatorios+Auditoria+Analise. ProcessosReportSource é fonte dedicada ≠ SolicitacoesReportSource. Coordenação concorrente: outro agente executou parte do plano, design convergiu; commits 1530719/844570a/8343f1b; staging seletivo, nada da Fase 14 tocado). [Antes] Completed 15-09-PLAN.md (Fase 15, wave 4 — CAMADA HTTP da fase, dono único de routes/gestao.php). Entregue: `RelatorioController` (fino) com `indicadores`/`tempo`/`produtividade`/`quedas` servindo Inertia com dado real dos serviços route-free (15-03/05/06/07) E export transversal por `?formato=` delegando ao `ReportExporter` (RN-009; sources: SolicitacoesReportSource, TempoAnaliseReportSource/EscritorioVirtualReportSource via `?relatorio=`, ProdutividadeReportSource, ExpressoQuedaReportSource); `RelatorioFiltersRequest` (valida os 7 filtros das telas → ReportFilters bag); produtividade nominal gated por `user->can('relatorios.produtividade.nominal')` (escopo = nominal ? null : user->id — default conservador anonimiza/escopa, RN-007), mesmo par no export. `ExportacaoController::download` serve o `ExportFile` do disco NÃO público por streaming — rota `gestao.relatorios.exportacoes.download` com middleware `signed` (alvo da Notification `ExportacaoPronta` de 15-02), guarda anti-disco-'public' (404), defesa-em-profundidade (dono OU consultar-relatorios), 404 honesto p/ arquivo podado (retenção 15-15), auditado (RN-008, `baixa-exportacao`, personalData). `HolidayController` (CRUD HU-137, sem destroy — RN-004) gated por `manter-parametros` (REUSO, sem permissão nova) + `HolidayRequest` (data única normalizada p/ casar com o cast); auditoria via HasAuditoria do model. Rotas: `gestao.relatorios.{indicadores,tempo,produtividade,quedas}`, `gestao.relatorios.exportacoes.download` (signed), `gestao.feriados.{index,store,update,ativacao.update}`. Componentes Inertia p/ 15-13/15-14: `gestao/relatorios/{indicadores,tempo,produtividade,quedas}` + `gestao/feriados/index`. STAGING SELETIVO: só os 8 arquivos do 15-09 + routes/gestao.php; trabalho paralelo do usuário (Fase 14 IA/e-mail + sidebar/vitest) intacto. VERIFICAÇÃO FRESCA: RelatorioControllerTest 9/9, ExportacaoDownloadTest 4/4, HolidayControllerTest 7/7; `--filter=Relatorios` 90/90 (379 asserções, zero regressão); `route:list -v` confirma `signed` no download; pint limpo (caminhos explícitos); `migrate` global NÃO rodado. Commits 94c6dc6 / 9b1ddd3 / e2123e1. Próximo: 15-13/15-14 (telas React dos relatórios e feriados — consomem os componentes/props acima e exportam por `?formato=`); 15-15 (retenção dos ExportFile — torna o 404 do download o caminho real ao expirar). [Fase 13 Integrações segue BLOQUEADA externamente — SEDUR.]
_prev_stopped_at_1505: Completed 15-05-PLAN.md (Fase 15, wave 3 — TEMPO POR ETAPA HU-129 + SAPS de tempo RN-006). Entregue: `TempoAnaliseService` (route-free, injeta BusinessDeadlineCalculator) com `tempoPorEtapa(ReportFilters): array` (tempo médio por etapa em MINUTOS ÚTEIS a partir de viability_request_transitions — varredura escopada por período + cursor, pareamento das transições consecutivas por processo, `businessDurationBetween` de 15-04 descontando fim de semana/feriado; 4 etapas pelo status DEIXADO: rascunho→preenchimento, protocolada/aguardando_bap→espera, em_analise→analise, em_pendencia→pendencia; reentradas somam isolando a pendência da análise; sem amostras ⇒ media_minutos null, CA-03), `tempoEmissaoTvl(ReportFilters): array` (SAPS RN-006: protocoled_at→decided_at em minutos úteis das decisões com tvl_product_number e decided_at no período; sem registros ⇒ null/0), `sedesEscritorioVirtual(ReportFilters): Builder` (SAPS RN-006: recorte is_virtual_office=true) e o helper PÚBLICO PURO `minutosPorEtapa`/`minutosPorEtapaDoProcesso` compartilhado com o export. EXPORT (RN-005, contrato 15-02): `TempoAnaliseReportSource` (detalhamento por processo — minutos úteis por etapa + total; personalData=false; event 'exporta-tempo-analise'; arquivoBase 'tempo-por-etapa') e `EscritorioVirtualReportSource` (reusa sedesEscritorioVirtual; ProcessoResource minimiza CNPJ; personalData=true; event 'exporta-escritorio-virtual'; arquivoBase 'sedes-escritorio-virtual'). DECISÃO portabilidade: desconto de tempo útil em PHP por par de transições (não LAG SQL — não portável SQLite+pgsql, Open Q #3); varredura/recorte em SQL. STAGING SELETIVO: só os 5 arquivos do 15-05; trabalho paralelo do usuário (Fase 14 IA/e-mail + sidebar/vitest) intacto. VERIFICAÇÃO FRESCA: `--filter=TempoAnalise` 7/7 (39 asserções — pendência cruzando sáb/dom = 3840 min úteis, NÃO 6720 de calendário; TVL ignora sem número/fora do período; período vazio null/0; RN-005 + recorte is_virtual_office); `--filter=Relatorios` 70/70 (302 asserções, zero regressão); pint limpo (caminhos explícitos); `migrate` global NÃO rodado. Commits cd3e3ed (serviço) / 2d0bb03 (sources). Próximo: 15-09 (HTTP/dashboard — instanciar os sources via app() + ReportExporter + expor indicadores + auditar; rota de download `gestao.relatorios.exportacoes.download` herdada do 15-02); demais waves do EP15. [Fase 13 Integrações segue BLOQUEADA externamente — SEDUR.]
_prev_stopped_at_1508: Completed 15-08-PLAN.md (Fase 15, wave 2 — DRIVER XLSX, terceiro formato do contrato único HU-131). Entregue: `XlsxExporter` (openspout v4.32.0) por streaming `builder()->cursor()` (RN-005, baixa memória): `write()` = `openToFile` direto (assíncrono, Pitfall 4); `stream()` = arquivo temporário (`openToFile`) + `response()->streamDownload(fpassthru)` com Content-Type `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` — NÃO `openToBrowser` (header()/ob_end_clean() conflitam com a StreamedResponse). Ligado ao `ReportExporter` (match + `DRIVERS_DISPONIVEIS` → `['csv','xlsx','pdf']`). DESVIO ANTI-FACHADA: `GerarExportacaoJob::driver()` (match próprio csv/pdf) também ganhou `'xlsx'` — sem isso o async estouraria no `handle()` (fachada quebrada). DESVIO ANTI-REGRESSÃO: `ReportExporterTest` passou a recusar `'json'` (xlsx agora disponível por design). STAGING SELETIVO: só os 5 arquivos do 15-08; trabalho paralelo do usuário (Fase 14) e dos agentes 15-03/15-06 intactos. VERIFICAÇÃO FRESCA: `--filter=XlsxExporterTest` 4/4 (16 asserções; XLSX reaberto pelo Reader no sync E no async — Job real), `--filter=ReportExporterTest` 6/6 e `--filter=GerarExportacaoJobTest` 3/3 (anti-regressão), `--filter=Relatorios` 42/42 (186 asserções); pint limpo (caminhos explícitos); `migrate` global NÃO rodado. Commits 817bd0b / 6eeaa09 / 07d1c8a. Próximo: 15-09 (HTTP/dashboard + endpoint de download `gestao.relatorios.exportacoes.download` — o async xlsx já grava o ExportFile); demais waves do EP15. [Fase 13 Integrações segue BLOQUEADA externamente — SEDUR.]
_prev_stopped_at_1506: Completed 15-06-PLAN.md (Fase 15, wave 2 — PRODUTIVIDADE POR ANALISTA HU-130, modo conservador RH/LGPD). Entregue: `ProdutividadeAnalistaService::porAnalista(ReportFilters, bool $nominal=false, ?int $scopeUserId=null)` (agrega em SQL as decisões de análise técnica por `decided_by_user_id` — flow='analise_tecnica', período em `decided_at` — em `decididas`/`deferidas`/`indeferidas`, + processos atribuídos por `assigned_user_id`; DEFAULT anonimizado com rótulo ordinal `Analista #N`; nominal junta `users`+nome como PARÂMETRO; scopeUserId devolve só o recorte do próprio e ignora nominal; período vazio → `[]`, CA-03) + `decisoesAgregadas(): Builder` (fonte compartilhada); `ProdutividadeReportSource implements ReportSource, SyncOnlyReportSource` (mesma minimização, colunas `analista_rotulo`/`analista_nome`, `personalData=nominal`, `event='exporta-produtividade'`; builder `fromSub`+`row_number()` → count() conta analistas e ordinal anônimo no streaming; SyncOnly preserva o estado de construtor pelo síncrono forçado). STAGING SELETIVO: só os 4 arquivos do 15-06 commitados; trabalho paralelo do usuário (Fase 14) e do agente 15-08 (XLSX) intactos. VERIFICAÇÃO FRESCA: `--filter=Produtividade` 9/9 (43 asserções); pint limpo nos 4 arquivos; `migrate` global NÃO rodado (Fase 14 pendente — RefreshDatabase/SQLite). Commits 0168c1e (serviço) / 8973c5d (source). Próximo: 15-09 (HTTP/dashboard/download — resolve nominal pela permissão `relatorios.produtividade.nominal` + `scopeUserId` do analista + auditoria; endpoint de download `gestao.relatorios.exportacoes.download` herdado do 15-02); demais waves do EP15 (15-03+ indicadores, 15-08 XLSX) em paralelo. [Fase 13 Integrações segue BLOQUEADA externamente — SEDUR.]
_prev_stopped_at_1502: Completed 15-02-PLAN.md (Fase 15, wave 1 — CONTRATO ÚNICO de exportação, coração da HU-131). Entregue: `ReportFilters` (bag serializável com round-trip toArray/fromArray — RN-005 sync E async; toProcessoFiltros recorta as 17 chaves do ProcessoQueryService; acessores tipados); `ReportDefinition`/`ReportSource`/`SyncOnlyReportSource`/`ReportFormatExporter`; drivers `CsvExporter`+`PdfExporter` (Blade `relatorios.relatorio`, rodapé 'Total de registros: N'); `ReportExporter::export` (streaming síncrono ↓limiar OU GerarExportacaoJob ↑limiar; guarda SyncOnly força síncrono; auditoria RN-008; xlsx recusado até 15-08); `GerarExportacaoJob` (reconstrói pelo bag; disco não-público; cria ExportFile; notifica; failed() audita sem link — Pitfall 6); Notification STANDALONE `ExportacaoPronta` (database+mail, NÃO o NotificationDispatcher). Model/migration `export_files` (000002; up/down validados isolados em pgsql via `--path` — migrate global NÃO rodado, há migrations pendentes da Fase 14) + factory. STAGING SELETIVO: nenhum arquivo do trabalho paralelo do usuário (Fase 14 IA/e-mail + sidebar/vitest) tocado/commitado. VERIFICAÇÃO FRESCA: `--filter=Relatorios` 23/23 (112 asserções; 20 dos 4 arquivos novos + 3 pré-existentes); pint limpo nos meus arquivos. Commits 2bfdc6e / d79b8dd / 544fec0. PENDÊNCIA p/ 15-09: rota+endpoint de download `gestao.relatorios.exportacoes.download` (URL assinada). Próximo: demais waves do EP15 (15-03+ serviços de indicador route-free, 15-06 HTTP/telas, 15-08 driver XLSX, 15-09 dashboard + endpoint de download). [Fase 13 Integrações segue BLOQUEADA externamente — SEDUR.]
_prev_stopped_at_0909: Completed 09-09-PLAN.md (Fase 9, wave 4 — efeito HU-110, track ‖ com 09-07/09-08). Entregue: listener AUTO-DESCOBERTO `App\Listeners\EnviarViabilidadeSefaz` (ShouldQueue) no `ResultadoEmitido` que envia a viabilidade à SEFAZ municipal via `SefazViabilidadeGateway::sendViabilidade` SÓ no DEFERIMENTO (guarda HU-134 RN-003: indeferida não toca o gateway — audita 'ignorado' e retorna). BLOQUEADO honesto (anti-fachada): binding default `UnavailableSefazViabilidadeGateway` LANÇA `SefazUnavailableException`; o listener CAPTURA e AUDITA pendência (`integracoes`/`sefaz-viabilidade`, result `bloqueado`, subject=solicitação, properties viability_request_id/protocolo/outcome) — NUNCA finge envio; a exceção NÃO é relançada (a decisão, gravada/auditada síncrona HU-078, jamais falha pela integração pendente; a pendência `bloqueado` é a falha visível). SUCESSO ligável provado com gateway disponível (spy) — Fase 13 troca SÓ o binding, listener intacto. Registro único por auto-descoberta (`event:list`: ResultadoEmitido ⇂ EnviarViabilidadeSefaz@handle (ShouldQueue)); NUNCA Event::listen (lição Fase 8), travado por CONTAGEM. BOUNDARY Wave 4: criados só `EnviarViabilidadeSefaz` + `EnviarViabilidadeSefazListenerTest`; NÃO toquei 09-07 (notificação) nem 09-08 (Regin); staging individual. VERIFICAÇÃO FINAL (evidência fresca): RED 3 falhas pelo motivo certo → GREEN `EnviarViabilidadeSefazListenerTest` 3/3 (9 asserções); pint --dirty limpo; greps de aceite (SefazUnavailableException + isDeferida + result 'bloqueado'/'ignorado' no listener; EnviarViabilidadeSefaz ausente do AppServiceProvider); suíte completa SQLite 902/902 (4583 asserções) — zero regressão, listener rodando de verdade no caminho real do ResultadoEmitido. Commit 7e84037 (feat). ZERO dependência nova. Próximo: 09-07 (notificação HU-077 — docs/STATE pendente), 09-10 (indeferimento sem atuação BAP) e 09-12 (smoke).
_prev_stopped_at_0908: Completed 09-08-PLAN.md (Fase 9, wave 4 — efeito HU-104, track ‖ com 09-07/09-09). Entregue: listener AUTO-DESCOBERTO `App\Listeners\ComunicarResultadoRegin` (ShouldQueue) no `ResultadoEmitido` que comunica o parecer (deferido E indeferido — HU-076 RN-008) ao Regin/Junta via `ReginParecerNotifier::notifyParecer` dentro de try/catch. BLOQUEADO honesto (anti-fachada): o binding atual `UnavailableReginParecerNotifier` LANÇA `ReginUnavailableException`; o listener CAPTURA e AUDITA a pendência (`integracoes`/`regin-parecer`, result `bloqueado`, subject=solicitação, properties viability_request_id/protocolo/outcome/erro) — NUNCA sucesso fictício, NUNCA simula o envio; a exceção NÃO é relançada (a decisão, gravada/auditada síncrona HU-078, jamais falha pela integração pendente; a pendência `bloqueado` é a falha visível). SUCESSO ligável provado com fake do notifier SÓ no teste (audita `sucesso`) — Fase 13 troca SÓ o binding, listener intacto. Registro único por auto-descoberta (`event:list`: ResultadoEmitido ⇂ ComunicarResultadoRegin@handle (ShouldQueue)); NUNCA Event::listen (lição Fase 8), travado por CONTAGEM. BOUNDARY Wave 4: criados só `ComunicarResultadoRegin` + `ComunicarResultadoReginListenerTest`; NÃO toquei 09-07 (notificação) nem 09-09 (SEFAZ); staging individual. VERIFICAÇÃO FINAL (evidência fresca): RED 3 falhas pelo motivo certo → GREEN `ComunicarResultadoReginListenerTest` 3/3 (10 asserções); pint --dirty limpo; greps de aceite (ReginUnavailableException + logName 'integracoes' + result 'bloqueado' no listener; ComunicarResultadoRegin ausente do AppServiceProvider); suíte completa SQLite 902/902 (4583 asserções) — zero regressão, listener rodando de verdade no caminho real do ResultadoEmitido. Commits 284d5d2 (test) / 55f63a8 (feat). ZERO dependência nova. Próximo: 09-07 (notificação HU-077) e 09-09 (SEFAZ HU-110) — restantes da Wave 4 — e 09-10 (indeferimento sem atuação BAP) / 09-12 (smoke).
_prev_stopped_at_0906: Completed 09-06-PLAN.md (Fase 9, wave 3 — gatilho). Entregue: listener AUTO-DESCOBERTO `AvaliarFluxoExpresso` no SolicitacaoProtocolada (despacha o `DecidirFluxoExpressoJob`, sem Event::listen, 1 job por protocolo por CONTAGEM), `DecidirFluxoExpressoJob` (ShouldQueue; tries/timeout/backoff/fila de config/sile.php; guard idempotente null|status!=Protocolada→no-op; failed() audita 'expresso'/'decisao-falha'/'falha'), comando+scheduler `expresso:reavaliar {--limit=}` (rede de SEGURANÇA das protocoladas órfãs whereDoesntHave('decision'); everyTenMinutes withoutOverlapping/onOneServer; no-op honesto). Fila = fonte única no construtor do job. HARNESS: `TestCase` base fakeia SÓ o `DecidirFluxoExpressoJob` por padrão (opt-out `$fakeExpressoDecisionJob`, desligado no ImportRedesimJobTest do worker real) — a fila sync rodaria a decisão inline mudando o status pós-protocolo; FASE 8 INTACTA (zero arquivo/lógica tocado). VERIFICAÇÃO FINAL (evidência fresca): filtro do plano 12/12 (30); suíte completa SQLite 889/889 (4556 asserções) + grupo postgis 21/21 (121); pint --dirty limpo; event:list mostra AvaliarFluxoExpresso ligado ao evento (sem Event::listen); schedule:list mostra */10 expresso:reavaliar. Commits 5ba0979 (gatilho+job) / f8a4db0 (comando+scheduler+refactor da fila) / e97d2a4 (isolamento de teste). Próximo: Wave 4 (09-07/08/09 — efeitos do ResultadoEmitido: notificação HU-077, Regin HU-104 bloqueado, SEFAZ HU-110 bloqueado) e 09-12 (smoke protocolar→decidir).
_prev_stopped_at_0812: Completed 08-12-PLAN.md (Fase 8, wave 7 — cancelar HU-070). Entregue: `CancelarSolicitacaoService::cancel(ViabilityRequest,User $actor,string $reason): void` (estados canceláveis do parâmetro solicitacao.cancelamento.estados_cancelaveis, json default rascunho+protocolada, fallback config/sile.php — HU-014; fora deles AUDITA o bloqueio 'solicitacoes/cancelamento-bloqueado' result bloqueado com subject e lança CancelamentoNaoPermitidoException — CA-03, anti-fachada; dentro, em DB::transaction grava cancelled_at/cancelled_reason/cancelled_by_user_id via forceFill e transiciona para Cancelada pela ViabilityRequestStateMachine reason+publicLabel 'Cancelada a pedido do requerente' — timeline + auditoria RN-002), `CancelamentoNaoPermitidoException` (->status, ->estadosCancelaveis), `CancelamentoSolicitacaoController@destroy` (Gate cancel dono+não-decidido CA-04 → motivo obrigatório → bloqueio em flash.error → sucesso redirect index+status), `CancelarSolicitacaoRequest` (reason required|string|max:1000 pt-BR), rota DELETE portal/solicitacoes/{solicitacao} (name portal.solicitacoes.cancelar) ANEXADA após as literais. ÚNICO DONO do catálogo na wave: solicitacao.cancelamento.estados_cancelaveis (48→49) no ParameterSeeder + config/sile.php; contagens 48→49 atualizadas em ParameterSeederTest e DatabaseSeederTest. SEM toggle (cancelar é direito do requerente). DECISÃO: bloqueio auditado via log(subject,result='bloqueado') em vez de logBlocked() — RN-002 mais completo (registra a solicitação) e evento próprio (CA-03 ≠ acesso-negado). Commits bebca1d (parâmetro 49) / 20faf21 (service+exceção+controller+request+rota+testes). PARALELO (wave 7, mesmo diretório): reli routes/portal.php antes de editar e ANEXEI só import+rota (git diff = só meu trecho); staging individual; NÃO toquei arquivos de 08-11/13/14/15 (ConsultaProtocolo*, Contingencia, AtendimentoPresencial, resources/js/pages/portal/solicitacoes). ca32767 (08-11) commitou entre minhas tasks mas NÃO tocou routes/portal.php (sem clobber; história linear 93e86e5→bebca1d→ca32767→20faf21). VERIFICAÇÃO FINAL (evidência fresca): CancelarSolicitacaoTest 6/6 (45 asserções); ParameterSeederTest+DatabaseSeederTest 19/19 (catálogo 49); filtros do plano CancelarSolicitacaoTest|ParameterSeederTest|DatabaseSeederTest 25/25 (280 asserções); regressão focada criar/protocolar/cancelar+seeders 46/46 (383 asserções); suíte completa `php artisan test --compact` 798 testes, 794 passaram, 4 falhas — as 4 são EXCLUSIVAMENTE do 08-11 em andamento (ConsultarProtocoloTest, RouteNotFoundException portal.protocolo.publico ainda não registrada), NÃO regressão do 08-12; pint --dirty limpo. Próximo: 08-16 (fechamento/seeds) após a wave 7 fechar.
_prev_stopped_at_10: Completed 08-10-PLAN.md (Fase 8, wave 6 — protocolar HU-068). Entregue: ProtocolarSolicitacaoService::protocol(ViabilityRequest,User $actor,bool $proceedDespite=false) — valida FORA da transação (status rascunho → InvalidStatusTransitionException; dados mínimos empresa/imóvel-polígono/área/CNAE principal → SolicitacaoIncompletaException; documentos obrigatórios via DocumentRequirementResolver::missing() → DocumentacaoIncompletaException; bloqueio NÃO consome número) e DENTRO de DB::transaction gera número único (ProtocolNumberGenerator lockForUpdate+unique), grava protocol_number/protocoled_at/applicant_proceeded_despite e transiciona rascunho→protocolada (StateMachine: timeline + auditoria RN-002). Ciência (applicant_proceeded_despite) só quando proceed_despite=true E simulation_resultado==='nao_permitido' (indeferimento); snapshot CONGELADO (RN-003, não reprocessa). APÓS o commit dispara SolicitacaoProtocolada (PRIMEIRO app/Events/ do sistema, implements ShouldDispatchAfterCommit; dispatch após a transação retornar → não dispara no rollback) com listener RegistrarTrilhaProtocolo (Event::listen no AppServiceProvider::boot, NÃO auto-discovery): marco amigável da timeline idempotente + auditoria de alto nível solicitacoes/protocolada. CRÍTICO: a auditoria do protocolo INDEPENDE do evento (a transição síncrona garante RN-002 mesmo se um listener falhar); ganchos EP09/EP11/EP13 só documentados no PHPDoc do evento. ProtocoloController@store: Gate protocol (dono+rascunho CA-04 — terceiro/não-rascunho 403 auditado) → toggle features.solicitacao_viabilidade off degrada comunicado → traduz DocumentacaoIncompleta/SolicitacaoIncompleta em flash.error (aviso, nunca silencioso) → sucesso redirect index + flash status com o número. ProtocolarSolicitacaoRequest (proceed_despite boolean). Rota POST portal/solicitacoes/{solicitacao}/protocolar (name portal.solicitacoes.protocolar) após as literais. Exceções DocumentacaoIncompletaException(->requisitos)/SolicitacaoIncompletaException(->campos). Commits ef8b6f7 (service+evento+exceções) / 92acb4d (listener+controller+request+rota+registro) / d7e704a (postgis concorrência). VERIFICAÇÃO FINAL (evidência fresca): ProtocolarSolicitacaoTest 13/13 (62 asserções, SQLite) + ProtocolarConcorrenciaPostgisTest 1/1 (@group postgis, lockForUpdate real, sem skip com POSTGIS_TESTS_REQUIRED=true); suíte completa 784/784 (3966 asserções, incl. 19 @group postgis com container sile-pgsql healthy); pint --dirty limpo. ZERO dependência nova. Próximo: 08-11 (consulta de protocolo — timeline pública assinada; substitui o redirect provisório ao index), 08-12 (cancelar — protocolada→cancelada já no mapa do 08-01), 08-13 (UI revisar/protocolar consome proceed_despite + flash error/status), 08-14/15 (operador protocola pelo mesmo service, muda só origem/ator), 08-16 (seeds dev incl. foto-fachada/termo-concessao).
_prev_stopped_at_09: Completed 08-09-PLAN.md (Fase 8, wave 3 — simulação pré-protocolo HU-141). Entregue: REUSO total do motor da Fase 7 sem lógica paralela (RN-001). `ConsultaViabilidadeService::consultarPorPontoConhecido(float $lat, float $lng, string $cnae, ?float $area=null): ConsultaViabilidadeResult` (entrada PÚBLICA por ponto+CNAE que delega à pipeline privada consultarPorPonto, geocode null — não geocodifica de novo; 3 públicos intactos) + `ConsultaViabilidadeInput::paraPonto` (TIPO_PONTO='ponto'). `App\Services\Solicitacao\SimulacaoSolicitacaoService::simulate(ViabilityRequest): array` itera os CNAEs (principal primeiro via orderByPivot is_primary desc + code), chama o motor PELO PONTO da solicitação (centroide do polígono recomputado em PHP portável; SEM polígono degrada para consultarPorCnae, território null, nunca inventa ponto), PROPAGA o veredito por CNAE, consolida o PIOR CASO (nao_permitido>pendente>permitido_com_condicoes>permitido; sem CNAEs→pendente) e persiste em DB::transaction simulation_snapshot {ponto,area_m2,por_cnae[{cnae,cnae_formatado,is_primary,tendencia,tendencia_label,consulta=ConsultaViabilidadeResult::toArray()}]} + simulation_rules_versions {territorio,louos,risco} (1º CNAE) + simulation_resultado (consolidado) + simulated_at=now() (RN-003 — 08-10 lê, não reprocessa) + auditoria solicitacoes/simulacao (RN-002). `Portal\SolicitacaoSimulacaoController@store` Gate::authorize('update') (dono+rascunho) → toggle features.simulacao_solicitacao off degrada comunicado (back+status, sem simular/persistir, sem erro — RN-005) → senão simula e devolve flash `simulacao`. ORIENTATIVA: NÃO bloqueia o protocolo, NÃO muda status (RN-002). Sem zona → pendente PROPAGADO (anti-fachada). Rota POST portal/solicitacoes/{solicitacao}/simular (name portal.solicitacoes.simular). Commits 9dab9dc (entrada por ponto) / e949390 (service+controller+rota+toggle). PARALELO com 08-08 (anexos; mesmo routes/portal.php): reli imediatamente antes e ANEXEI só import+rota simular após as de documentos (git diff vs HEAD = só meu trecho, zero clobber); staging individual; não toquei arquivos de documentos do 08-08. VERIFICAÇÃO FINAL (evidência fresca): SimulacaoSolicitacaoTest 9/9 (46 asserções) + ConsultaViabilidadeServiceTest 9/9 (anti-regressão da Fase 7); filtros do plano 18/18 (92 asserções); suíte completa 770/770 (3896 asserções, incl. 16 @group postgis com container healthy + os 11 testes do 08-08); pint --dirty limpo. Próximo: 08-10 (protocolo — lê o snapshot e registra applicant_proceeded_despite).
_prev_stopped_at_08: Completed 08-08-PLAN.md (Fase 8, wave 3 — anexar documentos HU-066 + validar obrigatoriedade documental HU-067). Entregue: `App\Services\Solicitacao\DocumentRequirementResolver` (required() une os document_requirements required+ativos dos CNAEs via whereHas('cnaes') com os condicionais base por código estável CODE_FACHADA='foto-fachada' sempre / CODE_CONCESSAO='termo-concessao' se is_public_area, sem duplicar por id; missing() desconta os já cobertos por anexo = base do bloqueio do protocolo 08-10; degrada honesto com a tabela por-CNAE vazia, nunca inventa), `Portal\SolicitacaoDocumentoController` (store: Gate update dono+rascunho, disk = Settings::get('storage.documentos.disk', config(...,'local')) NUNCA público, sha256 REAL via hash_file ANTES do store(), substituição do mesmo requirement_id, auditoria documento-anexado; download: Gate view + anti-IDOR abort_unless + response()->streamDownload lendo Storage::disk($disk)->readStream — streaming autenticado, nunca URL pública LGPD, auditoria documento-download; destroy: Gate update, apaga disk+linha, auditoria documento-removido), `StoreSolicitacaoDocumentoRequest` (file required|file|mimetypes:{solicitacao.anexos.mime_permitidos}|max:{solicitacao.anexos.max_mb*1024} dinâmicos no rules() HU-014; requirement_id nullable Rule::exists), rotas portal.solicitacoes.documentos.{store,download,destroy} (após as literais). 403 auditado globalmente (CA-04). Commits 19c9b87 (resolver+DocumentRequirementResolverTest) / df61479 (controller/request/rotas+AnexarDocumentoTest). PARALELO com 08-09 (simulação HU-141, em andamento): mesmo routes/portal.php — reli imediatamente antes e ANEXEI só import+3 rotas após a do imóvel (git diff = só meu trecho, zero clobber); staging individual; o pint --dirty tocou o SimulacaoSolicitacaoTest.php do 08-09 e foi REVERTIDO com git checkout (não commitei arquivo de simulação). VERIFICAÇÃO FINAL (evidência fresca, Storage::fake): DocumentRequirementResolverTest 5/5 + AnexarDocumentoTest 6/6 (filtros do plano juntos 11/11, 44 asserções); suíte completa 761/761 (= 750 baseline + 11 do 08-08; inclui 16 @group postgis com container sile-pgsql healthy) EXCLUINDO só o SimulacaoSolicitacaoTest do 08-09 em andamento (raw 770: 762 passaram, 8 erros — todos da rota portal.solicitacoes.simular ainda não criada pelo 08-09, NÃO regressão minha); pint --dirty limpo nos arquivos do 08-08. Próximo: 08-09 (simulação, em andamento em paralelo) e depois 08-10 (protocolo — consome missing() para bloquear).
_prev_stopped_at_06: Completed 08-06-PLAN.md (Fase 8, wave 3 — informar imóvel HU-062 + área utilizada HU-063). Entregue: `App\Services\Solicitacao\PropertyGeometryWriter` (write() deriva property_polygon via ST_MakeValid(ST_SetSRID(ST_GeomFromGeoJSON(?),4326)) SÓ no pgsql espelhando GeoJsonLayerImporter, no-op em SQLite; polygonAreaSquareMeters() ST_Area::geography no pgsql / shoelace planar em SQLite; fonte = property_polygon_geojson jsonb), `Portal\SolicitacaoImovelController@update` (Gate update dono+rascunho; transação grava polígono/complemento texto livre/referência obrigatória/indicadores via boolean()/used_area_m2 + write geometry + markSimulationStale RN-005 + nota auditada condicional; ANTES identifica território por centroide via TerritoryService::identify degradando honesto sem zona — nunca inventa, NÃO recomputa veredito; valida área×polígono contra o próprio polígono com tolerância solicitacao.area_poligono.tolerancia_percentual default 10 → flash areaAlert + auditoria 'imovel-area-inconsistente', NUNCA bloqueia RN-004; back() com flash status/territorio/areaAlert), `UpdateSolicitacaoImovelRequest` (polígono GeoJSON type Polygon + anel ≥4 pontos + pares numéricos, address_reference required, used_area_m2 gt:0, indicadores boolean; pt-BR), rota PUT portal/solicitacoes/{solicitacao}/imovel (name portal.solicitacoes.imovel). DECISÃO: território NÃO persistido (schema fechado no 08-01, sem migração) — flash + auditoria do TerritoryService; inconsistência de área registrada por nota auditada (sem campo novo). Commits 29fa6c9 (writer+InformarImovelPostgisTest) / 4cc6301 (controller/request/rota+InformarImovelTest). PARALELO com 08-07 (atividades): arquivos distintos; 08-07 já commitado ao editar (f0e07b9); reli routes/portal.php imediatamente antes e ANEXEI só import+rota do imóvel após a de atividades (git diff HEAD = só meu trecho, zero clobber); staging individual. VERIFICAÇÃO FINAL: InformarImovelPostgisTest 2/2 (@group postgis REAL, sem skip) + InformarImovelTest 6/6 SQLite; filtros do plano rodados SEPARADAMENTE (artefato conhecido do RefreshDatabase em filtro misto SQLite+postgis — reproduzido também no par já existente ProtocolNumberGenerator, não é do código); suíte completa 750/750 (3806 asserções, incl. 16 @group postgis com container sile-pgsql healthy); pint --dirty limpo. Próximo: Wave 3 (08-08 anexos/resolver de requisitos, 08-09 simulação HU-141).
_prev_stopped_at_07: Completed 08-07-PLAN.md (Fase 8, wave 3 — atividade principal + CNAEs complementares HU-064/065). Entregue: Portal\SolicitacaoAtividadeController@update (sync transacional ÚNICO do pivot viability_request_cnaes — principal is_primary=true + complementares is_primary=false, um principal único, unicidade request/cnae), UpdateSolicitacaoAtividadesRequest (só CNAEs ATIVOS no principal e complementares + limite parametrizável DINÂMICO solicitacao.cnaes_complementares.max default 99 lido no rules() + principal fora dos complementares + sem duplicados, mensagens pt-BR), markSimulationStale() na MESMA transação (RN-005), auditoria explícita 'solicitacao-cnaes' antes/depois, rota PUT portal/solicitacoes/{solicitacao}/atividades (name portal.solicitacoes.atividades), autorização dono+rascunho via Gate::authorize('update') (ViabilityRequestPolicy 08-05). DECISÃO: pivot escrito no CONTROLLER (não em service) — toda a lista chega numa request, o sync exato basta; não recriei CompanyCnaeService. Commits 8d6302d (test) / 985c582 (feat). PARALELO com 08-06 (imóvel/geometry — PropertyGeometryWriter): arquivos distintos; o 08-06 NÃO tocou routes/portal.php (confirmado por git show); reli portal.php imediatamente antes e ANEXEI só import+rota (git diff = 2 adições, zero clobber); staging individual — NÃO toquei os arquivos untracked do 08-06 (SolicitacaoImovelController/UpdateSolicitacaoImovelRequest/InformarImovelTest). VERIFICAÇÃO FINAL: InformarAtividadesTest 7/7 (29 asserções); suíte completa 744/744 (3774 asserções; 735 baseline + 2 do 08-06 InformarImovelPostgisTest + 7 novos; inclui 16 @group postgis com container healthy); pint --dirty limpo. Próximo: Wave 3 (08-08 anexos/resolver de requisitos, 08-09 simulação HU-141).
_prev_stopped_at_05: Completed 08-05-PLAN.md (Fase 8, wave 3 — criar rascunho HU-061). Entregue: `ViabilityRequestPolicy` (auto-discovery, espelha CompanyPolicy; view/update/protocol/cancel pelo efetivo via CurrentRepresentation), `SolicitacaoController` store (requester=efetivo, created_by=ator, origin portal, status default banco) + index "Minhas solicitações" escopado ao dono, `StoreSolicitacaoRequest` (tipo ativo + empresa com vínculo ativo do efetivo), `DuplicateRequestDetector` (RN-007 alerta por CNPJ, nunca bloqueia; janela em config/sile.php default 180d, ressalva SEDUR), rotas portal.solicitacoes.{index,store}, toggle features.solicitacao_viabilidade degradando comunicado, auditoria RN-002 com acting_for. Commits 01efdf5 (policy) / 5da70ff (detector+config) / 0c3172f (controller/request/rotas). NÃO tocou ParameterSeeder/seeder-tests (contagens travadas em 48 intactas); ZERO tela React nova (08-13). VERIFICAÇÃO FINAL: ViabilityRequestPolicyTest 5/5 + DuplicateRequestDetectorTest 8/8 + CriarSolicitacaoTest 8/8 (21 novos); suíte completa 735/735 (3737 asserções, incl. 16 @group postgis com container healthy); pint --dirty limpo. Próximo: Wave 3 (08-06 imóvel/geometry, 08-07 área, atividades/CNAEs). [Wave 2 antes: 08-03 tipos de serviço + 08-04 requisitos documentais por CNAE.]
_prev_stopped_at_w2: Completed 08-04-PLAN.md (Fase 8, wave 2 — cadastros admin). 08-04 (requisitos documentais HU-067): Gestao\DocumentRequirementController (index server-driven, store/update, toggle que preserva histórico, syncCnaes vínculo N:N exato), StoreDocumentRequirementRequest/UpdateDocumentRequirementRequest (code único/imutável; só CNAEs ativos vinculáveis), rotas gestao.requisitos-documentais.* sob permission:manter-requisitos-documentais (+ cnaes-disponiveis reusando Portal\CnaeSearchController), HasAuditoria no DocumentRequirement + auditoria explícita 'requisito-cnaes' antes/depois, tela gestao/requisitos-documentais/index.tsx (DataTable + modais + gerenciador de CNAEs com chips+picker) + nav por permissão; commits 55a3684/08ee796. Tabela por-CNAE nasce vazia (carga oficial pendente SEDUR), sem fachada; contrato do resolver para 08-08 documentado no SUMMARY. Execução paralela com 08-03 na MESMA working dir (arquivos compartilhados routes/gestao.php + gestao-layout.tsx): 08-03 já havia commitado (cab7a33/b4edaf3); reli ambos antes de editar, anexei só meu trecho (âncoras estáveis), git diff confirmou zero clobber. VERIFICAÇÃO FINAL: DocumentRequirementCrudTest 8/8; suíte completa 698/698 SQLite + 16/16 @group postgis (container sile-pgsql healthy); typecheck/build/pint verdes. Próximo: Wave 3 (criar rascunho + imóvel/geometry + atividades). [Wave 1 antes: 08-01 domínio + 08-02 parametrização.]
_prev_stopped_at_w1: Completed 08-01-PLAN.md E 08-02-PLAN.md (Fase 8, wave 1 — fundação). 08-01 (domínio): 8 migrations driver-aware (geometry property_polygon + GiST só pgsql; fonte jsonb), enums Status(3 ativos+5 ganchos)/Origin, 6 models (ViabilityRequest com HasAuditoria + markSimulationStale) + 6 factories (draft/protocoled/cancelled/contingency), ProtocolNumberGenerator (lockForUpdate, default inline VIA/6), ViabilityRequestStateMachine (mapa explícito + InvalidStatusTransitionException CA-03 + timeline/auditoria RN-002); commits ad88011/c8face4/c241369/b620abd. 08-02 (parametrização): catálogo 35→48 (grupo 'solicitacao'), 5 permissões 14→19, fallbacks em config/sile.php; commits até 9d353ec. Execução concorrente na MESMA working dir, commits intercalados — boundary respeitado (staging sempre individual; 08-01 não tocou ParameterSeeder/config/RolesAndPermissionsSeeder; 08-02 não tocou domínio). VERIFICAÇÃO FINAL CONJUNTA: suíte completa 700/700 (3581 asserções, inclui @group postgis inline com container de pé) — os "8 erros remanescentes" anotados pelo 08-02 eram os testes RED do 08-01 em andamento, agora GREEN. Pint limpo. Próximo: Wave 2 (08-03 ‖ 08-04 cadastros admin).
Resume file: None

Nota operacional: durante o 01-09 houve uma sessão de agente concorrente no mesmo working directory (commits 79b3b81/e4010ce da Task 1 e composer run dev). Conteúdo validado e aproveitado sem duplicação. RECORRÊNCIA no 02-05: TRÊS sessões executoras despachadas para o mesmo plano; a segunda e a terceira detectaram a colisão no início (SUMMARY/commits já no HEAD), não editaram código e validaram o trabalho da primeira com evidência fresca (Users 15/15, suíte 168/168, typecheck/build/pint verdes, 3 rotas usuarios.*). Corrigir o despacho: um único executor por wave/plano — nunca sessões GSD simultâneas ou repetidas no mesmo plano sem checar SUMMARY antes.

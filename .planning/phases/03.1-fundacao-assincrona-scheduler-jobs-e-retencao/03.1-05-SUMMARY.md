---
phase: 03.1-fundacao-assincrona-scheduler-jobs-e-retencao
plan: "03.1-05"
subsystem: docs-verificacao
tags: [deploy, scheduler, queue, retention, verification]

requires:
  - phase: 03.1
    plan: "03.1-01"
    provides: parâmetros do catálogo (retenção, throttle, retry HTTP)
  - phase: 03.1
    plan: "03.1-02"
    provides: jobs ImportRedesimJob/ImportCnaeJob com failed_jobs
  - phase: 03.1
    plan: "03.1-03"
    provides: scheduler + pruning auditado de access_logs
  - phase: 03.1
    plan: "03.1-04"
    provides: throttle e retry HTTP parametrizados
provides:
  - Guia operacional de produção (docs/deploy/producao-assincrona.md)
  - Verificação integral fresca da fase (suíte/pint/typecheck/build/migrate:fresh)
affects: [deploy futuro (stack Docker/Horizon), fase-13 (integrações herdam o padrão de filas)]

tech-stack:
  added: []
  patterns:
    - "Produção documentada (cron schedule:run + queue:work supervisionado) sem construir o stack Docker — decisão travada"

key-files:
  created:
    - docs/deploy/producao-assincrona.md
  modified: []

key-decisions:
  - "Produção apenas documentada nesta fase; stack Docker (Dockerfile/supervisord/Portainer) e Horizon são deploy futuro (Fase 13/deploy)"
  - "retry_after deve exceder o maior timeout de job (jobs=60s, worker=120s, retry_after>=150) — documentado para evitar execução dupla"
  - "onOneServer/withoutOverlapping exigem CACHE_STORE compartilhado em multi-instância"

requirements: []
---

# 03.1-05 — Documentação de produção + verificação integral da Fase 3.1

## One-liner
Guia operacional de produção (scheduler via cron, worker de fila, reprocesso de `failed_jobs`, retenção e limites) e verificação integral fresca fechando a Fase 3.1 — sem construir o stack Docker (deploy futuro), conforme decisão travada.

## Entregue
- `docs/deploy/producao-assincrona.md` (pt-BR): visão dev×prod, cron do `schedule:run`, `queue:work` supervisionado, aviso `retry_after > timeout`, cache compartilhado para `onOneServer`, reprocesso de `failed_jobs`, variáveis de ambiente, retenção parametrizada (`activity_log` fora) e nota de escopo (stack Docker/Horizon futuros).

## Evidência de verificação integral (2026-06-13)
- `php artisan test --compact` → **385 testes, 1949 asserções, 0 falhas**.
- `vendor/bin/pint --dirty --format agent` → passed.
- `npm run typecheck` e `npm run build` → verdes.
- `php artisan migrate:fresh --seed --no-interaction` → todos os seeders DONE; `Parameter::count()` = **25** (sem migration duplicada de jobs/failed_jobs).
- `php artisan schedule:list` → `0 0 * * * php artisan model:prune --model='App\Models\AccessLog'`.
- `php artisan list` → `redesim:importar` e `cnae:importar` presentes.

## Cobertura dos 5 success criteria (ROADMAP)
1. Scheduler ativo em dev + rotina real idempotente E auditada + produção documentada → 03.1-03 + este doc. ✓
2. REDESIM/CNAE como jobs com retry/timeout/relatório; falhos em `failed_jobs` → 03.1-02. ✓
3. Throttle parametrizado em rota pública (CNPJ) → 03.1-04. ✓
4. Retenção de `access_logs` por pruning; `activity_log` fora → 03.1-03. ✓
5. HTTP client com retry/backoff parametrizado → 03.1-04. ✓

## Deviations
Nenhuma — plano executado como escrito.

---
phase: 03.1-fundacao-assincrona-scheduler-jobs-e-retencao
verified: 2026-06-13T19:17:47Z
status: passed
score: 5/5 success criteria verificados
re_verification: false
test_suite:
  command: "php artisan test --compact"
  tests: 385
  assertions: 1949
  failures: 0
gaps: []
---

# Fase 3.1: Fundação assíncrona — scheduler, jobs e retenção — Relatório de Verificação

**Phase Goal:** "O sistema opera com a infraestrutura assíncrona que o Laravel 13 entrega pronta e que ainda não usamos — scheduler, jobs em fila com retry e retenção de dados — fechando lacunas que travariam prazos automáticos, SLA e integrações nas fases seguintes."

**Verificado:** 2026-06-13T19:17:47Z
**Status:** passed
**Re-verificação:** Não — verificação inicial

## Resumo

Os 5 success criteria do ROADMAP foram verificados nos três níveis (existe, é substantivo, está conectado) contra o código real — não apenas contra os SUMMARYs. Toda peça executa lógica real de ponta a ponta: o scheduler tem rotina real agendada e auditada, os jobs envelopam serviços de domínio reais e provam visibilidade de falha em `failed_jobs`, o throttle e o HTTP client leem parâmetros administráveis (HU-014) com efeito sem deploy. Sem fachada detectada.

**Evidência dinâmica fresca (2026-06-13):**
- `php artisan test --compact` → **385 testes, 1949 asserções, 0 falhas**.
- `php artisan schedule:list` → `0 0 * * * php artisan model:prune --model='App\Models\AccessLog'` (Next Due: em 4 horas).
- `php artisan list` → `redesim:importar`, `cnae:importar` e `model:prune` presentes.

## Cobertura dos 5 Success Criteria

| # | Success Criterion | Status | Evidência (código + teste) |
|---|---|---|---|
| 1 | Scheduler ativo em dev + produção documentada; >=1 rotina real idempotente E auditada | ✓ VERIFICADO | `composer.json` (schedule:work), `routes/console.php`, `AuditModelsPruned`, `docs/deploy/producao-assincrona.md`; `PruneAccessLogsTest` |
| 2 | REDESIM/CNAE como jobs em fila com retry/timeout/relatório auditado; falhos em `failed_jobs`, nunca silenciosos | ✓ VERIFICADO | `ImportRedesimJob`, `ImportCnaeJob`, comandos `--queue`, migration `failed_jobs`; `ImportRedesimJobTest`, `ImportCnaeJobTest` |
| 3 | Rotas públicas com throttle parametrizado via HU-014 (consulta de CNPJ) | ✓ VERIFICADO | `FortifyServiceProvider` (limiter `cnpj-lookup`), `routes/portal.php` (`throttle:cnpj-lookup`); `CnpjThrottleTest` |
| 4 | `access_logs` com retenção parametrizada por pruning (MassPrunable); `activity_log` FORA | ✓ VERIFICADO | `AccessLog` (MassPrunable), `Activity` (sem Prunable), `routes/console.php`; `PruneAccessLogsTest` |
| 5 | HTTP client de integrações com retry/backoff parametrizado | ✓ VERIFICADO | `BrasilApiCnpjLookup` (Settings::get retries/timeout/backoff_ms); `CnpjLookupParametersTest` |

**Score:** 5/5 success criteria verificados.

---

## SC#1 — Scheduler ativo, rotina real idempotente E auditada

**Verdade observável:** O scheduler roda em dev e tem ao menos uma rotina real, idempotente e auditada; produção está documentada.

| Nível | Verificação | Resultado |
|---|---|---|
| Existe | `composer.json` script `dev` contém `php artisan schedule:work` (5 processos: server, queue, schedule, logs, vite) | ✓ |
| Existe | `routes/console.php` agenda `model:prune --model=AccessLog::class` | ✓ |
| Substantivo | Agendamento real com `->daily()->withoutOverlapping()->onOneServer()` (não rotina artificial) | ✓ |
| Conectado | `schedule:list` lista `0 0 * * * php artisan model:prune --model='App\Models\AccessLog'` (evidência dinâmica) | ✓ |
| Conectado | `AppServiceProvider::boot()` registra `Event::listen(ModelsPruned::class, AuditModelsPruned::class)` | ✓ |
| Conectado | `AuditModelsPruned` grava auditoria real (`log_name: retencao`, `event: pruning-access-logs`, modelo + contagem removida, `rules_version`) | ✓ |
| Substantivo | `docs/deploy/producao-assincrona.md` existe e documenta cron `schedule:run`, worker `queue:work` supervisionado, `retry_after > timeout`, cache compartilhado para `onOneServer` | ✓ |

**Teste comprovador:** `tests/Feature/Scheduling/PruneAccessLogsTest.php` assere `withoutOverlapping == true` e `onOneServer == true` no evento agendado (idempotência) e a presença da auditoria `pruning-access-logs` no `activity_log` após a poda.

**Status: ✓ VERIFICADO** — a rotina é real (poda de retenção), idempotente e deixa rastro de auditoria a cada execução.

---

## SC#2 — Jobs REDESIM/CNAE com retry/timeout/relatório; falhos visíveis e reprocessáveis

**Verdade observável:** As importações executam como jobs de fila com retry/timeout/backoff envolvendo os serviços reais; um job esgotado fica visível em `failed_jobs` e gera auditoria de falha.

| Artefato | Nível | Resultado |
|---|---|---|
| `app/Jobs/ImportRedesimJob.php` | `ShouldQueue` + `tries=3`, `timeout=60`, `backoff=[30,60,120]`; `handle()` chama `RedesimImportService::import`; `failed()` audita | ✓ VERIFICADO |
| `app/Jobs/ImportCnaeJob.php` | `ShouldQueue` + `tries/timeout/backoff`; `handle()` chama `CnaeImportService::import` e audita o relatório (`importacao-oficial`); `failed()` audita | ✓ VERIFICADO |
| `app/Console/Commands/ImportRedesimCommand.php` | flag `--queue` despacha `ImportRedesimJob`; caminho síncrono preservado (homologação) | ✓ VERIFICADO |
| `app/Console/Commands/ImportCnaeCommand.php` | novo comando `cnae:importar {arquivo} {--queue}` com auditoria do relatório e exit codes | ✓ VERIFICADO |
| `database/migrations/0001_01_01_000002_create_jobs_table.php` | tabela `failed_jobs` existe (sem migration duplicada) | ✓ VERIFICADO |

**Wiring crítico — visibilidade de falha (onde stubs se escondem):** `tests/Feature/Jobs/ImportRedesimJobTest.php::test_job_que_falha_fica_visivel_em_failed_jobs_e_e_auditado` exercita falha REAL: `queue:work --once` + `travel()` esgotando as 3 tentativas, assere `DB::table('failed_jobs')->count() === 1` e `assertDatabaseHas('activity_log', ['log_name' => 'empresas'])`. Não é falha silenciosa.

**Status: ✓ VERIFICADO** — jobs são wrappers finos dos serviços de domínio reais; falha esgotada é persistida e auditada.

---

## SC#3 — Throttle parametrizado na rota pública de CNPJ (HU-014)

**Verdade observável:** A rota pública de consulta de CNPJ tem rate limiting parametrizado: N/min passam, a N+1 recebe 429; alterar o parâmetro muda o teto sem deploy.

| Nível | Verificação | Resultado |
|---|---|---|
| Existe | `FortifyServiceProvider::boot()` registra `RateLimiter::for('cnpj-lookup', ...)` | ✓ |
| Substantivo/Conectado | Limite vem de `Settings::get('seguranca.throttle.cnpj_lookup.por_minuto', 30)`, `->by(user?->id ?: ip)` | ✓ |
| Conectado | `routes/portal.php`: `empresas/consultar-cnpj` tem `->middleware('throttle:cnpj-lookup')` (somente nesta rota, não no grupo) | ✓ |

**Teste comprovador:** `tests/Feature/Companies/CnpjThrottleTest.php` ajusta `seguranca.throttle.cnpj_lookup.por_minuto` para `2`, assere `assertStatus(429)` ao exceder, e depois eleva para `5` provando que o parâmetro muda o teto efetivo (efeito sem deploy).

**Status: ✓ VERIFICADO** — throttle real, limite administrável via HU-014.

---

## SC#4 — Retenção de `access_logs` por pruning; `activity_log` FORA

**Verdade observável:** `access_logs` além da janela parametrizada são removidos por pruning agendado; a trilha de auditoria (`activity_log`) não é podada.

| Nível | Verificação | Resultado |
|---|---|---|
| Existe/Substantivo | `app/Models/AccessLog.php` usa `MassPrunable` e implementa `prunable()` filtrando `created_at < now()->subDays((int) Settings::get('retencao.access_logs.dias', 365))` | ✓ |
| Conectado | Janela parametrizada lê `retencao.access_logs.dias` (HU-014) com fallback em `config/sile.php` | ✓ |
| Exclusão correta | `app/Models/Activity.php` (estende Spatie Activity) NÃO usa `Prunable`/`MassPrunable` — `rg "Prunable" app/Models/Activity.php` = vazio | ✓ |

**Teste comprovador:** `tests/Feature/Scheduling/PruneAccessLogsTest.php` altera `retencao.access_logs.dias` para `30` (prova efeito do parâmetro), e roda `model:prune` GLOBAL (sem `--model`) assertando que a Activity antiga sobrevive — `activity_log` provado fora do pruning por prune global (prova real, não tautológica).

**Status: ✓ VERIFICADO** — retenção parametrizada de alto volume; `activity_log` preservado por compliance.

---

## SC#5 — HTTP client com retry/backoff parametrizado

**Verdade observável:** O HTTP client de consulta de CNPJ lê `retries`, `timeout` e `backoff_ms` de parâmetros (fallback em config); o número de tentativas segue o parâmetro.

| Nível | Verificação | Resultado |
|---|---|---|
| Existe/Substantivo | `app/Services/Cnpj/BrasilApiCnpjLookup.php` resolve `Http::timeout(Settings::get('integrations.cnpj_lookup.timeout', config(...)))` e `->retry(Settings::get('...retries'...), Settings::get('...backoff_ms'...), throw: false)` | ✓ |
| Conectado | Os 3 valores vêm de `Settings::get` com fallback técnico em `config/sile.php` (`integrations.cnpj_lookup.*`) | ✓ |

**Teste comprovador:** `tests/Feature/Companies/CnpjLookupParametersTest.php::test_numero_de_tentativas_segue_o_parametro_retries` fixa `retries=1`/`backoff_ms=0`, empilha `[falha de conexão, sucesso]` e prova por exaustão de sequência que o número de tentativas segue o parâmetro (discriminante contra o default de config).

**Status: ✓ VERIFICADO** — resiliência HTTP parametrizada e administrável.

---

## Parametrização (HU-014) — fundação dos 5 critérios

5 parâmetros novos registrados no catálogo (`database/seeders/ParameterSeeder.php`), cada um com tipo, default, regras de validação e grupo, com fallback técnico espelhado em `config/sile.php`:

| Chave | Grupo | Tipo | Default | Validação |
|---|---|---|---|---|
| `retencao.access_logs.dias` | retencao | integer | 365 | `min:30 max:3650` |
| `seguranca.throttle.cnpj_lookup.por_minuto` | seguranca | integer | 30 | `min:1 max:300` |
| `integrations.cnpj_lookup.retries` | integracoes | integer | 2 | `min:0 max:5` |
| `integrations.cnpj_lookup.timeout` | integracoes | integer | 8 | `min:1 max:30` |
| `integrations.cnpj_lookup.backoff_ms` | integracoes | integer | 200 | `min:0 max:5000` |

Catálogo total = 25 parâmetros (confirmado nos testes de seeder, parte das 385/1949).

## Anti-padrões / fachada

Nenhum detectado. Verificações realizadas:
- Jobs não duplicam lógica — envelopam serviços reais (`RedesimImportService`/`CnaeImportService`).
- Nenhuma migration duplicada de `jobs`/`failed_jobs` (reuso da migration padrão do Laravel).
- Nenhum valor de negócio hardcoded nos pontos verificados — todos via `Settings::get` com fallback em config.
- `failed()` audita falha real (RN-002) — sem falha silenciosa.
- Testes comprovadores são discriminantes (exaustão de sequência, `travel()` real, prune global), não tautológicos.

## Verificação humana

Nenhuma verificação humana bloqueante. Itens de produção (cron `schedule:run`, worker `queue:work` supervisionado, Redis compartilhado para `onOneServer`) são DOCUMENTADOS nesta fase por decisão de escopo travada — o stack Docker/Horizon é deploy futuro e não bloqueia o GOAL desta fase.

---

_Verificado: 2026-06-13T19:17:47Z_
_Verificador: Claude (gsd-verifier)_

---
phase: 03.1-fundacao-assincrona-scheduler-jobs-e-retencao
plan: "03.1-01"
type: execute
wave: 1
depends_on: []
requirements: []
files_modified:
  - database/seeders/ParameterSeeder.php
  - config/sile.php
  - tests/Feature/Seeders/ParameterSeederTest.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
autonomous: true

must_haves:
  truths:
    - "Os 5 parâmetros novos da fundação assíncrona existem no catálogo HU-014 (tipo, default, validação, grupo) e são administráveis sem deploy."
    - "Cada chave nova tem fallback técnico correspondente em config/sile.php (Settings::get resolve banco→cache→config)."
    - "O catálogo tem 25 parâmetros e o grupo novo `retencao` aparece na lista de grupos."
    - "Re-seed preserva valor administrado e é idempotente (precedente 02-02)."
  artifacts:
    - path: "database/seeders/ParameterSeeder.php"
      provides: "5 entradas novas no catálogo com chaves/defaults/validações exatas do CONTEXT"
      contains: "retencao.access_logs.dias"
    - path: "config/sile.php"
      provides: "Fallbacks técnicos das 5 chaves novas (incl. integrations.cnpj_lookup.backoff_ms)"
      contains: "backoff_ms"
    - path: "tests/Feature/Seeders/ParameterSeederTest.php"
      provides: "Asserções de contagem (25), grupos (com retencao) e metadados dos 5 parâmetros"
      contains: "assertSame(25"
  key_links:
    - from: "App\\Support\\Settings::get(chave)"
      to: "config('sile.{chave}')"
      via: "fallback quando não há Parameter no banco"
      pattern: "seguranca\\.throttle\\.cnpj_lookup\\.por_minuto"
---

<objective>
Registrar no catálogo de parâmetros administráveis (HU-014) os 5 valores de negócio que a Fase 3.1 introduz, e seus fallbacks técnicos em `config/sile.php`. Esta é a fundação consumida pelas demais ondas: pruning de retenção (03.1-03), throttle e retry/backoff do HTTP client (03.1-04).

Purpose: garantir parametrização máxima desde o primeiro plano — nenhum valor de negócio hardcoded (retenção, limite de throttle, retries/timeout/backoff do lookup). Tudo administrável com tipo, default, validação, histórico e efeito sem deploy (precedente Fase 2).
Output: 5 parâmetros no `ParameterSeeder`, 5 fallbacks em `config/sile.php`, e os testes de seeder atualizados (20 → 25, novo grupo `retencao`).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-fundacao-assincrona-scheduler-jobs-e-retencao/03.1-CONTEXT.md
@docs/superpowers/specs/2026-06-13-fundacao-assincrona-design.md
@app/Support/Settings.php
@app/Models/Parameter.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrar os 5 parâmetros no catálogo + fallbacks em config/sile.php (TDD)</name>
  <files>
    database/seeders/ParameterSeeder.php
    config/sile.php
    tests/Feature/Seeders/ParameterSeederTest.php
  </files>
  <read_first>
    - database/seeders/ParameterSeeder.php (catálogo atual: 20 chaves; upsert SÓ de metadados, `sensitive` via forceFill)
    - config/sile.php (fallbacks técnicos; chaves `security`/`integrations`/`ui`/`features`)
    - tests/Feature/Seeders/ParameterSeederTest.php (asserções de contagem 20 e lista de grupos)
    - app/Models/Parameter.php (typedValue, casts validation_rules=array, value cast condicional sensitive)
    - app/Support/Settings.php (resolução banco→cache→config; chave do Parameter == chave do config sile.*)
    - .planning/phases/03.1-fundacao-assincrona-scheduler-jobs-e-retencao/03.1-CONTEXT.md (seção "Parâmetros novos" — chaves/defaults/validações EXATAS)
  </read_first>
  <action>
    RED — Atualizar tests/Feature/Seeders/ParameterSeederTest.php:
    1. `test_seeder_cria_catalogo_completo`: trocar `assertSame(20, ...)` por `assertSame(25, ...)` e a lista de grupos de
       `['features', 'integracoes', 'seguranca', 'ui']` para `['features', 'integracoes', 'retencao', 'seguranca', 'ui']`.
    2. `test_seeder_e_idempotente`: trocar `assertSame(20, ...)` por `assertSame(25, ...)`.
    3. Adicionar o teste `test_seeder_registra_parametros_da_fundacao_assincrona` que, após `$this->seed(ParameterSeeder::class)`, asserta os 5 registros com EXATIDÃO:
       - `retencao.access_logs.dias`: group `retencao`, type `integer`, default_value `'365'`, validation_rules `['required','integer','min:30','max:3650']`, `value` null.
       - `seguranca.throttle.cnpj_lookup.por_minuto`: group `seguranca`, type `integer`, default_value `'30'`, validation_rules `['required','integer','min:1','max:300']`.
       - `integrations.cnpj_lookup.retries`: group `integracoes`, type `integer`, default_value `'2'`, validation_rules `['required','integer','min:0','max:5']`.
       - `integrations.cnpj_lookup.timeout`: group `integracoes`, type `integer`, default_value `'8'`, validation_rules `['required','integer','min:1','max:30']`.
       - `integrations.cnpj_lookup.backoff_ms`: group `integracoes`, type `integer`, default_value `'200'`, validation_rules `['required','integer','min:0','max:5000']`.
       Use `Parameter::query()->where('key', ...)->first()` + `assertNotNull` + `assertSame` (espelhe o estilo de `test_seeder_registra_parametros_do_cadastro_empresarial`).
    Rodar: `php artisan test --compact --filter=ParameterSeederTest` → DEVE FALHAR (catálogo ainda tem 20).

    GREEN — Adicionar ao array `catalog()` de database/seeders/ParameterSeeder.php as 5 entradas (mesmo formato das existentes; NÃO incluir chave `value`; `sensitive` não se aplica a nenhuma destas):
    ```php
    'retencao.access_logs.dias' => [
        'group' => 'retencao',
        'type' => 'integer',
        'default_value' => '365',
        'validation_rules' => ['required', 'integer', 'min:30', 'max:3650'],
        'description' => 'Dias de retenção do histórico de acessos antes da limpeza automática (LGPD)',
    ],
    'seguranca.throttle.cnpj_lookup.por_minuto' => [
        'group' => 'seguranca',
        'type' => 'integer',
        'default_value' => '30',
        'validation_rules' => ['required', 'integer', 'min:1', 'max:300'],
        'description' => 'Limite de consultas de CNPJ por minuto por usuário no portal',
    ],
    'integrations.cnpj_lookup.retries' => [
        'group' => 'integracoes',
        'type' => 'integer',
        'default_value' => '2',
        'validation_rules' => ['required', 'integer', 'min:0', 'max:5'],
        'description' => 'Número de novas tentativas na consulta de CNPJ quando a integração falha',
    ],
    'integrations.cnpj_lookup.timeout' => [
        'group' => 'integracoes',
        'type' => 'integer',
        'default_value' => '8',
        'validation_rules' => ['required', 'integer', 'min:1', 'max:30'],
        'description' => 'Tempo limite em segundos para a consulta de CNPJ',
    ],
    'integrations.cnpj_lookup.backoff_ms' => [
        'group' => 'integracoes',
        'type' => 'integer',
        'default_value' => '200',
        'validation_rules' => ['required', 'integer', 'min:0', 'max:5000'],
        'description' => 'Intervalo em milissegundos entre as tentativas de consulta de CNPJ',
    ],
    ```
    Em config/sile.php, adicionar os fallbacks técnicos com as MESMAS chaves (Settings::get lê `config("sile.{$key}")`):
    - Novo top-level `'retencao' => ['access_logs' => ['dias' => 365]],`
    - Novo top-level `'seguranca' => ['throttle' => ['cnpj_lookup' => ['por_minuto' => 30]]],`
      ATENÇÃO: a chave do parâmetro é pt-BR (`seguranca.throttle...`, `retencao...`) por decisão TRAVADA no CONTEXT — NÃO renomear para `security`. O bloco `security` (inglês) existente permanece intacto; `seguranca` é um top-level NOVO e distinto.
    - Em `integrations.cnpj_lookup`, acrescentar `'backoff_ms' => 200,` (mantendo `base_url`/`timeout`/`retries`/`cache_ttl`).
    Rodar: `php artisan test --compact --filter=ParameterSeederTest` → DEVE PASSAR.

    REFACTOR — `vendor/bin/pint --dirty --format agent`. Conferir que nenhum valor de negócio ficou hardcoded fora do par catálogo+config.
  </action>
  <verify>
    php artisan test --compact --filter=ParameterSeederTest
    php artisan config:show sile.retencao.access_logs.dias
    php artisan config:show sile.integrations.cnpj_lookup.backoff_ms
  </verify>
  <acceptance_criteria>
    - `rg -n "retencao.access_logs.dias|seguranca.throttle.cnpj_lookup.por_minuto|integrations.cnpj_lookup.retries|integrations.cnpj_lookup.timeout|integrations.cnpj_lookup.backoff_ms" database/seeders/ParameterSeeder.php` lista as 5 chaves.
    - `rg -n "backoff_ms|'retencao'|'seguranca'" config/sile.php` mostra os 3 fallbacks novos.
    - `php artisan test --compact --filter=ParameterSeederTest` verde, incluindo `test_seeder_registra_parametros_da_fundacao_assincrona`.
    - `rg -n "assertSame\(25" tests/Feature/Seeders/ParameterSeederTest.php` retorna 2 ocorrências.
  </acceptance_criteria>
  <done>Catálogo com 25 parâmetros (grupo `retencao` novo), fallbacks em config/sile.php espelhando as chaves, ParameterSeederTest verde.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar a contagem de parâmetros no DatabaseSeederTest (TDD)</name>
  <files>
    tests/Feature/Seeders/DatabaseSeederTest.php
  </files>
  <read_first>
    - tests/Feature/Seeders/DatabaseSeederTest.php (asserções `assertSame(20, Parameter::query()->count())` em duas linhas: seed completo e idempotência)
    - database/seeders/DatabaseSeeder.php (ordem dos seeders; ParameterSeeder incluído)
  </read_first>
  <action>
    RED — Em tests/Feature/Seeders/DatabaseSeederTest.php, trocar AS DUAS ocorrências de
    `$this->assertSame(20, Parameter::query()->count());` por `$this->assertSame(25, Parameter::query()->count());`
    (uma em `test_seed_completo_prepara_ambiente_de_desenvolvimento`, outra em `test_seed_e_idempotente`).
    Rodar: `php artisan test --compact --filter=DatabaseSeederTest` → DEVE FALHAR antes da Task 1 estar aplicada; com a Task 1 aplicada, RED é só a diferença 20→25.
    GREEN — A correção é a própria troca (o catálogo já tem 25 após a Task 1).
    Rodar: `php artisan test --compact --filter=DatabaseSeederTest` → DEVE PASSAR.
    REFACTOR — `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    php artisan test --compact --filter=DatabaseSeederTest
  </verify>
  <acceptance_criteria>
    - `rg -n "assertSame\(25, Parameter::query\(\)->count\(\)\)" tests/Feature/Seeders/DatabaseSeederTest.php` retorna 2 ocorrências.
    - `php artisan test --compact --filter=DatabaseSeederTest` verde.
  </acceptance_criteria>
  <done>DatabaseSeederTest verde com a contagem 25.</done>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=Seeder` (ParameterSeederTest + DatabaseSeederTest) verde.
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan config:show sile.seguranca.throttle.cnpj_lookup.por_minuto` retorna 30.
</verification>

<success_criteria>
- 5 parâmetros novos no catálogo com chave/tipo/default/validação/grupo exatos do CONTEXT.
- 5 fallbacks correspondentes em config/sile.php.
- Testes de seeder atualizados (25 parâmetros, grupo `retencao`) e verdes.
- Nenhum valor de negócio da Fase 3.1 hardcoded fora do par catálogo+config.
</success_criteria>

<output>
Após completar, criar `.planning/phases/03.1-fundacao-assincrona-scheduler-jobs-e-retencao/03.1-01-SUMMARY.md`.
</output>

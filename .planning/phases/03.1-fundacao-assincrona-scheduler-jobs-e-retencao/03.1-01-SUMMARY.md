---
phase: 03.1-fundacao-assincrona-scheduler-jobs-e-retencao
plan: "03.1-01"
subsystem: config
tags: [parametrizacao, hu-014, seeders, config, retencao, throttle, http-client]

# Dependency graph
requires:
  - phase: 02-parametrizacao
    provides: "padrão Settings::get (banco→cache→config) e ParameterSeeder com upsert de metadados que preserva valor administrado"
provides:
  - "5 parâmetros administráveis novos no catálogo HU-014 (retencao.access_logs.dias; seguranca.throttle.cnpj_lookup.por_minuto; integrations.cnpj_lookup.retries/timeout/backoff_ms)"
  - "fallbacks técnicos correspondentes em config/sile.php (top-levels retencao e seguranca + backoff_ms em integrations.cnpj_lookup)"
  - "grupo novo `retencao` no catálogo"
affects:
  - "03.1-03 (pruning de retenção consome retencao.access_logs.dias)"
  - "03.1-04 (throttle consome seguranca.throttle.cnpj_lookup.por_minuto; HTTP client consome integrations.cnpj_lookup.retries/timeout/backoff_ms)"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Chave de parâmetro HU-014 espelha 1:1 a chave de config/sile.php (fallback do Settings::get)"
    - "Constante técnica/fallback em config; valor de negócio administrável no catálogo — nada hardcoded fora do par catálogo+config"

key-files:
  created: []
  modified:
    - database/seeders/ParameterSeeder.php
    - config/sile.php
    - tests/Feature/Seeders/ParameterSeederTest.php
    - tests/Feature/Seeders/DatabaseSeederTest.php

key-decisions:
  - "Chaves pt-BR (retencao.*, seguranca.*) são top-levels NOVOS e distintos do bloco `security` (inglês) existente — decisão travada no CONTEXT; não renomear"
  - "backoff_ms é parâmetro novo (intervalo entre tentativas), somado a retries/timeout já existentes no HTTP client"
  - "Grupo `retencao` criado no catálogo; throttle reusa o grupo `seguranca`; retries/timeout/backoff_ms reusam `integracoes`"

patterns-established:
  - "Parâmetro de fundação assíncrona segue o precedente da Fase 2: catálogo (metadados, sem `value`) + fallback técnico em config de mesma chave"

# Metrics
duration: 3 min
completed: 2026-06-13
---

# Phase 03.1 Plan 01: Parâmetros da fundação assíncrona Summary

**5 parâmetros administráveis (HU-014) para retenção LGPD, throttle de CNPJ e retry/timeout/backoff do HTTP client, com fallbacks técnicos espelhados em config/sile.php — catálogo de 20 para 25.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-06-13T18:45:43Z
- **Completed:** 2026-06-13T18:49:31Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Catálogo HU-014 agora registra os 5 valores de negócio que a Fase 3.1 introduz, todos administráveis sem deploy (tipo, default, validação, grupo, histórico via padrão da Fase 2).
- `config/sile.php` ganhou os fallbacks técnicos de mesma chave (top-levels `retencao` e `seguranca`, mais `integrations.cnpj_lookup.backoff_ms`), garantindo `Settings::get` resolvendo banco→cache→config mesmo sem registro no banco.
- Grupo novo `retencao` no catálogo; testes de seeder atualizados (20 → 25) e verdes, incluindo asserção exata dos 5 parâmetros.

## Task Commits

Cada task foi commitada atomicamente (ciclo TDD RED → GREEN):

1. **Task 1 (RED): asserções dos 5 parâmetros + contagem 25 + grupo retencao** - `14937f8` (test)
2. **Task 1 (GREEN): 5 entradas no catálogo + fallbacks em config/sile.php** - `5cef95c` (feat)
3. **Task 2: contagem de parâmetros no DatabaseSeederTest para 25** - `b608ced` (test)

**Plan metadata:** `docs(03.1-01)` (este SUMMARY)

## Files Created/Modified
- `database/seeders/ParameterSeeder.php` - +5 entradas no `catalog()` (retencao.access_logs.dias; seguranca.throttle.cnpj_lookup.por_minuto; integrations.cnpj_lookup.retries/timeout/backoff_ms)
- `config/sile.php` - novos top-levels `retencao` e `seguranca` + `backoff_ms` em `integrations.cnpj_lookup` (fallbacks técnicos)
- `tests/Feature/Seeders/ParameterSeederTest.php` - contagem 25, grupo `retencao` na lista ordenada e teste `test_seeder_registra_parametros_da_fundacao_assincrona`
- `tests/Feature/Seeders/DatabaseSeederTest.php` - duas asserções de contagem de parâmetros de 20 para 25

## Decisions Made
- **Chaves pt-BR distintas de `security`:** `retencao.*` e `seguranca.*` são top-levels novos em `config/sile.php`, coexistindo com o bloco `security` (inglês) existente. Decisão travada no CONTEXT — espelha a chave do parâmetro no catálogo, que é pt-BR. Adicionado comentário curto no config explicando o porquê (evita renomeação futura indevida).
- **`backoff_ms` como parâmetro novo:** complementa `retries`/`timeout` (já existentes em config) para o retry/backoff parametrizado do HTTP client consumido em 03.1-04.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - nenhuma configuração de serviço externo necessária.

## Next Phase Readiness
- Fundação de parametrização pronta para as ondas seguintes: 03.1-03 (pruning de retenção lê `retencao.access_logs.dias`) e 03.1-04 (throttle lê `seguranca.throttle.cnpj_lookup.por_minuto`; `BrasilApiCnpjLookup` lê `integrations.cnpj_lookup.retries/timeout/backoff_ms`).
- Sem bloqueios. Verificação escopada (`tests/Feature/Seeders`) verde: 13 testes, 110 asserções. STATE.md/ROADMAP.md não editados (execução em wave paralela — consolidação pelo orquestrador).

---
*Phase: 03.1-fundacao-assincrona-scheduler-jobs-e-retencao*
*Completed: 2026-06-13*

---
phase: 03.1-fundacao-assincrona-scheduler-jobs-e-retencao
plan: "03.1-05"
type: execute
wave: 3
depends_on: ["03.1-01", "03.1-02", "03.1-03", "03.1-04"]
requirements: []
files_modified:
  - docs/deploy/producao-assincrona.md

autonomous: true

must_haves:
  truths:
    - "Produção tem documentação para rodar o scheduler (cron schedule:run) e o worker (queue:work) com filas Redis e failed_jobs reprocessáveis."
    - "A documentação registra que retry_after deve exceder o maior timeout de job e que onOneServer exige cache compartilhado."
    - "A documentação registra explicitamente que o stack Docker de produção (Dockerfile/supervisord/compose Portainer) e o Horizon são trabalho de deploy futuro — não construídos nesta fase."
    - "A suíte completa, pint, typecheck, build e migrate:fresh --seed passam frescos no fechamento da fase."
  artifacts:
    - path: "docs/deploy/producao-assincrona.md"
      provides: "Guia de produção: cron, worker, failed_jobs, variáveis de fila, retenção e limites; nota de escopo (stack Docker/Horizon futuros)"
      contains: "schedule:run"
  key_links:
    - from: "docs/deploy/producao-assincrona.md"
      to: "scheduler + queue do app"
      via: "instruções operacionais reais (cron + worker)"
      pattern: "queue:work"
---

<objective>
Documentar a operação assíncrona em produção (scheduler + fila + falhas) e fazer a verificação integral fresca da Fase 3.1. Produção é apenas DOCUMENTADA nesta fase (decisão travada): o stack Docker/Portainer e o Horizon são trabalho de deploy futuro. Não há UI nova — o fechamento é autônomo (sem checkpoint humano de smoke), com evidência por comandos.

Purpose: fecha o Success Criterion #1 (produção documentada) e o critério de pronto transversal (suíte verde, pint/typecheck/build, migrate:fresh --seed).
Output: `docs/deploy/producao-assincrona.md` e evidência fresca de verificação integral.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-fundacao-assincrona-scheduler-jobs-e-retencao/03.1-CONTEXT.md
@docs/superpowers/specs/2026-06-13-fundacao-assincrona-design.md
@config/queue.php
@composer.json
@.planning/phases/03.1-fundacao-assincrona-scheduler-jobs-e-retencao/03.1-01-SUMMARY.md
@.planning/phases/03.1-fundacao-assincrona-scheduler-jobs-e-retencao/03.1-02-SUMMARY.md
@.planning/phases/03.1-fundacao-assincrona-scheduler-jobs-e-retencao/03.1-03-SUMMARY.md
@.planning/phases/03.1-fundacao-assincrona-scheduler-jobs-e-retencao/03.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Documentação de produção assíncrona</name>
  <files>
    docs/deploy/producao-assincrona.md
  </files>
  <read_first>
    - config/queue.php (default redis via env; failed.driver database-uuids → tabela failed_jobs; batching; retry_after por conexão)
    - .env (QUEUE_CONNECTION=redis, CACHE_STORE=redis, SESSION_DRIVER=redis, REDIS_* — base real de dev)
    - composer.json (script dev com schedule:work e queue:listen — referência do que produção substitui por cron + queue:work)
    - .planning/phases/.../03.1-CONTEXT.md (seção "Produção (documentada)" e "Deferred Ideas": Horizon/Portainer fora desta fase)
    - .planning/phases/.../03.1-02-SUMMARY.md e 03.1-03-SUMMARY.md (jobs/timeout/backoff e rotina agendada já implementados — refletir os valores reais na doc)
  </read_first>
  <action>
    Criar docs/deploy/producao-assincrona.md (pt-BR) cobrindo, com base nos valores REAIS do código já implementado nesta fase:
    1. Visão geral: dev usa `composer dev` (schedule:work + queue:listen); produção usa cron + worker persistente.
    2. Scheduler em produção: entrada de cron
       `* * * * * cd /caminho/do/projeto && php artisan schedule:run >> /dev/null 2>&1`
       e o que está agendado hoje (pruning diário de access_logs, idempotente: withoutOverlapping + onOneServer).
    3. Worker de fila em produção: `php artisan queue:work redis --tries=3 --timeout=120` (supervisord/systemd para reinício automático). Explicar a diferença para o `queue:listen` do dev.
    4. ATENÇÃO retry_after > timeout: documentar que `DB_QUEUE_RETRY_AFTER`/`REDIS_QUEUE_RETRY_AFTER` (config/queue.php) DEVEM exceder o maior `timeout` de job (jobs desta fase usam timeout=60s; default retry_after=90s já é seguro). Sem isso, um job é reprocessado enquanto ainda roda (execução dupla).
    5. onOneServer exige cache compartilhado: em multi-instância, `CACHE_STORE=redis` (ou database/memcached) é obrigatório para o lock de `onOneServer`/`withoutOverlapping`.
    6. Jobs falhos: `php artisan queue:failed` (lista), `php artisan queue:retry all` / `php artisan queue:retry {uuid}` (reprocessa), tabela `failed_jobs` (driver `database-uuids`). Nunca falha silenciosa — falhas ficam visíveis e reprocessáveis.
    7. Variáveis de ambiente relevantes: `QUEUE_CONNECTION=redis`, `QUEUE_FAILED_DRIVER=database-uuids`, `REDIS_*`, `CACHE_STORE`, `DB_QUEUE_RETRY_AFTER`/`REDIS_QUEUE_RETRY_AFTER`.
    8. Retenção: a rotina de pruning aplica `retencao.access_logs.dias` (parâmetro HU-014, default 365), administrável sem deploy; a trilha de auditoria (`activity_log`) fica FORA do pruning (compliance; política completa na Fase 12).
    9. Nota de escopo (decisão travada): o stack Docker de produção (Dockerfile/supervisord/compose Portainer) e o Horizon (visibilidade de filas Redis) são trabalho de DEPLOY futuro (Fase 13/deploy) — NÃO construídos nesta fase. Esta fase entrega scheduler/jobs/retenção funcionando e a documentação operacional.

    NÃO inventar caminhos/credenciais reais; usar placeholders claros (`/caminho/do/projeto`). Markdown em pt-BR.
  </action>
  <verify>
    rg -n "schedule:run|queue:work|failed_jobs|retry_after|onOneServer|Horizon|Portainer" docs/deploy/producao-assincrona.md
  </verify>
  <acceptance_criteria>
    - O arquivo docs/deploy/producao-assincrona.md existe.
    - `rg -n "schedule:run" docs/deploy/producao-assincrona.md` (cron do scheduler) e `rg -n "queue:work" docs/deploy/producao-assincrona.md` (worker) presentes.
    - `rg -n "queue:failed|queue:retry|failed_jobs" docs/deploy/producao-assincrona.md` (reprocesso de falhas) presente.
    - `rg -n "retry_after" docs/deploy/producao-assincrona.md` (aviso retry_after > timeout) presente.
    - `rg -ni "Horizon|Portainer|deploy futuro|trabalho de deploy" docs/deploy/producao-assincrona.md` (nota de escopo) presente.
  </acceptance_criteria>
  <done>Guia de produção assíncrona escrito, refletindo o código real da fase e a fronteira de escopo (stack/Horizon futuros).</done>
</task>

<task type="auto">
  <name>Task 2: Verificação integral fresca da fase</name>
  <files>
    docs/deploy/producao-assincrona.md
  </files>
  <read_first>
    - composer.json (scripts test/dev)
    - database/migrations/0001_01_01_000002_create_jobs_table.php (já cria `jobs`, `failed_jobs` e `job_batches` — `migrate:fresh` NÃO deve ter migration duplicada dessas tabelas)
    - .planning/phases/.../03.1-01-SUMMARY.md a 03.1-04-SUMMARY.md (o que foi entregue em cada onda — para confirmar cobertura dos 5 success criteria)
  </read_first>
  <action>
    Rodar a verificação integral FRESCA (ler o output inteiro de cada comando antes de afirmar sucesso — nenhuma afirmação sem evidência):
    1. Suíte completa: `php artisan test --compact` → todos verdes (incluindo os testes novos das ondas 1–2: ParameterSeederTest, DatabaseSeederTest, ImportRedesimJobTest, ImportCnaeJobTest, PruneAccessLogsTest, CnpjThrottleTest, CnpjLookupParametersTest).
    2. Estilo: `vendor/bin/pint --dirty --format agent` → sem pendências.
    3. Front-end: `npm run typecheck` e `npm run build` → verdes (esta fase não altera TS/React, mas o critério transversal exige a verificação fresca).
    4. Banco real (pgsql dev): `php artisan migrate:fresh --seed` → sem erros; confirma que NÃO há migration duplicada de jobs/failed_jobs e que o catálogo seeda 25 parâmetros. (Requer o serviço de banco do dev de pé.)
    5. Scheduler listável: `php artisan schedule:list` → mostra a rotina `model:prune` mirando AccessLog.
    6. Comandos disponíveis: `php artisan list | rg "redesim:importar|cnae:importar"` → ambos presentes.

    Registrar no fim de docs/deploy/producao-assincrona.md uma seção "Evidência de verificação (Fase 3.1)" com os resultados reais (contagem de testes, status de pint/typecheck/build, resultado do migrate:fresh --seed). Sem números inventados — colar os resultados observados.

    Se QUALQUER comando falhar: parar, diagnosticar (debugging sistemático), corrigir no plano de origem do problema e re-rodar. Não marcar a fase como concluída sem evidência fresca de tudo verde.
  </action>
  <verify>
    php artisan test --compact
    vendor/bin/pint --dirty --format agent
    npm run typecheck
    npm run build
    php artisan migrate:fresh --seed
    php artisan schedule:list
  </verify>
  <acceptance_criteria>
    - `php artisan test --compact` termina com 0 falhas (suíte inteira verde).
    - `vendor/bin/pint --dirty --format agent` sem pendências.
    - `npm run typecheck` e `npm run build` sem erros.
    - `php artisan migrate:fresh --seed` conclui sem erro e o catálogo fica com 25 parâmetros (`php artisan tinker --execute 'echo \App\Models\Parameter::count();'` → 25).
    - `php artisan schedule:list` lista `model:prune`; `php artisan list | rg "redesim:importar|cnae:importar"` lista os dois comandos.
    - Seção "Evidência de verificação (Fase 3.1)" presente em docs/deploy/producao-assincrona.md com resultados reais.
  </acceptance_criteria>
  <done>Fase 3.1 verificada de ponta a ponta com evidência fresca; nenhum critério de fachada; os 5 success criteria cobertos.</done>
</task>

</tasks>

<verification>
- Suíte completa, pint, typecheck, build e migrate:fresh --seed verdes (evidência fresca colada no doc).
- schedule:list e list dos comandos confirmam scheduler e jobs ativos.
</verification>

<success_criteria>
- Produção documentada (cron + worker + failed_jobs + variáveis + retenção; stack/Horizon explicitamente futuros).
- Verificação integral fresca da fase passando — os 5 success criteria do ROADMAP cobertos sem fachada.
</success_criteria>

<output>
Após completar, criar `.planning/phases/03.1-fundacao-assincrona-scheduler-jobs-e-retencao/03.1-05-SUMMARY.md`.
</output>

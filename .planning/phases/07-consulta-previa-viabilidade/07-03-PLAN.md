---
phase: 07-consulta-previa-viabilidade
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/XXXX_XX_XX_XXXXXX_create_viability_queries_table.php
  - app/Models/ViabilityQuery.php
  - database/factories/ViabilityQueryFactory.php
  - tests/Feature/Viabilidade/ViabilityQuerySchemaTest.php
autonomous: true
requirements: [HU-060]

must_haves:
  truths:
    - "Existe a tabela imutável viability_queries (input + result snapshot + rules_versions como JSON + user_id nullable) e roda em SQLite (tabular)"
    - "O model ViabilityQuery casta input/result/rules_versions para array e tem escopo forUser (só o dono lê o próprio histórico)"
    - "A tabela é imutável: só created_at (sem updated_at) — o registro é um snapshot da consulta, nunca editado"
    - "user_id é nullable e nullOnDelete: consulta anônima não grava histórico pessoal (gravação só quando autenticado é regra do controller, 07-07)"
  artifacts:
    - path: "app/Models/ViabilityQuery.php"
      provides: "Model imutável do histórico de consultas com casts json e scope forUser"
      contains: "class ViabilityQuery"
    - path: "database/migrations/XXXX_XX_XX_XXXXXX_create_viability_queries_table.php"
      provides: "Tabela imutável com user_id, entry_type, input, result, rules_versions (json) e created_at"
      contains: "create('viability_queries'"
  key_links:
    - from: "database/migrations/XXXX_XX_XX_XXXXXX_create_viability_queries_table.php"
      to: "users"
      via: "foreignId user_id nullable constrained nullOnDelete (consulta anônima não vincula)"
      pattern: "nullOnDelete"
    - from: "app/Models/ViabilityQuery.php"
      to: "App\\Models\\User"
      via: "scope forUser (histórico só do dono — precedente Company::countForUser)"
      pattern: "function scopeForUser"
---

<objective>
Construir a **persistência imutável do histórico** de consultas (HU-060): a tabela `viability_queries` (snapshot da entrada + resultado + versões de regra + dono), o model com casts e escopo por usuário, e a factory. Tabular (SQLite), sem PostGIS.

Purpose: HU-060 — usuário autenticado consulta o histórico das PRÓPRIAS consultas (precedente "Minhas empresas": escopo pelo dono). O registro é um SNAPSHOT imutável (input, resultado e versões da época) — auditável e reproduzível. A gravação só-quando-autenticado e a listagem são do controller (07-07); aqui entra a fundação de dados.
Output: migration imutável + `ViabilityQuery` model (casts json, scope forUser) + factory + schema test.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-consulta-previa-viabilidade/07-CONTEXT.md
@docs/superpowers/specs/2026-06-14-consulta-previa-viabilidade-design.md

# PADRÃO A ESPELHAR (tabela imutável; model com casts/escopo por dono) — ler antes de codar:
@database/migrations/2026_06_10_014429_create_access_logs_table.php
@database/migrations/2026_06_12_035745_create_companies_table.php
@app/Models/Company.php
@database/factories/CompanyFactory.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: migration viability_queries (imutável) + model ViabilityQuery + factory</name>
  <read_first>database/migrations/2026_06_10_014429_create_access_logs_table.php, app/Models/Company.php, database/factories/CompanyFactory.php</read_first>
  <files>database/migrations/XXXX_XX_XX_XXXXXX_create_viability_queries_table.php, app/Models/ViabilityQuery.php, database/factories/ViabilityQueryFactory.php</files>
  <action>
    Criar a migration com `php artisan make:migration create_viability_queries_table --no-interaction` (substituir o placeholder XXXX no files_modified pelo timestamp gerado). TABULAR (sem PostGIS) — roda em SQLite e NÃO pode quebrar a baseline.

    Migration `viability_queries` (espelhar a imutabilidade de access_logs — sem updated_at):
    - `$table->id();`
    - `$table->foreignId('user_id')->nullable()->constrained('users')->nullOnDelete();` // anônima = null; dono preservado se a conta sumir
    - `$table->string('entry_type', 20);` // 'endereco' | 'cnae' | 'inscricao'
    - `$table->json('input');` // entrada normalizada (endereço/cnae/área/inscrição) — use json() (PostgreSQL: jsonb-compatível; SQLite: text)
    - `$table->json('result');` // snapshot ConsultaViabilidadeResult::toArray()
    - `$table->json('rules_versions');` // versoes() de TODAS as regras aplicadas (territorio/louos/risco)
    - `$table->string('resultado', 30)->nullable();` // veredito locacional propagado (permitido|permitido_com_condicoes|nao_permitido|pendente) — coluna desnormalizada p/ filtro/listagem
    - `$table->ipAddress('ip_address')->nullable();` // origem (consulta anônima/autenticada)
    - `$table->timestamp('created_at');` // SEM updated_at (imutável)
    - `$table->index(['user_id', 'created_at']);`
    - down(): `Schema::dropIfExists('viability_queries');`

    Model `App\Models\ViabilityQuery` (espelhar Company; NÃO usar HasAuditoria — o snapshot já é o registro):
    - `#[Fillable(['user_id', 'entry_type', 'input', 'result', 'rules_versions', 'resultado', 'ip_address', 'created_at'])]`
    - `use HasFactory;`
    - `public $timestamps = false;` // imutável: gravamos created_at à mão (sem updated_at)
    - `protected function casts(): array { return ['input' => 'array', 'result' => 'array', 'rules_versions' => 'array', 'created_at' => 'datetime']; }`
    - `belongsTo(User::class)` user().
    - scope `scopeForUser(Builder $query, User $user): Builder` → `$query->where('user_id', $user->id)` (histórico SÓ do dono — precedente Company::countForUser).

    Factory `ViabilityQueryFactory`: definition com entry_type 'endereco', input ['endereco' => fake address, 'cnae' => '4712100', 'area' => 120.0], result ['veredito_locacional' => ['resultado' => 'pendente']], rules_versions ['louos' => [], 'risco' => [], 'territorio' => []], resultado 'pendente', created_at now(). user_id via `User::factory()` por padrão; estado `anonima()` com user_id null + ip_address fake.
  </action>
  <verify>php artisan migrate:fresh --env=testing 2>&1 | tail -5 && vendor/bin/pint --dirty --format agent</verify>
  <acceptance_criteria>
    - grep "create('viability_queries'" database/migrations/*_create_viability_queries_table.php
    - grep "nullOnDelete" database/migrations/*_create_viability_queries_table.php
    - migration tem `timestamp('created_at')` e NÃO tem `$table->timestamps()` (sem updated_at)
    - grep "public \$timestamps = false" app/Models/ViabilityQuery.php
    - grep "function scopeForUser" app/Models/ViabilityQuery.php
    - migrate:fresh roda sem erro em SQLite.
  </acceptance_criteria>
  <done>Tabela imutável + model com casts json e scopeForUser + factory (incl. estado anônima); migra em SQLite.</done>
</task>

<task type="auto">
  <name>Task 2: schema test (migra em SQLite; casts array; forUser; imutabilidade)</name>
  <read_first>tests/Feature/Louos/LouosQuadrosSchemaTest.php, app/Models/ViabilityQuery.php</read_first>
  <files>tests/Feature/Viabilidade/ViabilityQuerySchemaTest.php</files>
  <action>
    TDD: escrever PRIMEIRO (RED se o model/migration ainda não estiverem prontos), depois GREEN. RefreshDatabase, SQLite.

    `tests/Feature/Viabilidade/ViabilityQuerySchemaTest.php`:
    - `test_grava_e_recupera_snapshot_com_casts_array`: criar via factory com input/result/rules_versions arrays; recarregar do banco e asserir `is_array($q->input)`, `is_array($q->result)`, `is_array($q->rules_versions)` e que o conteúdo bate (ex.: `$q->result['veredito_locacional']['resultado']`).
    - `test_consulta_anonima_grava_sem_dono`: `ViabilityQuery::factory()->anonima()->create()`; asserir `user_id` null e `ip_address` preenchido (consulta anônima é registrável sem dono — a regra de "gravar só quando autenticado" é do controller 07-07, mas o schema aceita null).
    - `test_escopo_for_user_retorna_so_do_dono`: criar 2 usuários, gravar 2 consultas do A e 1 do B; asserir `ViabilityQuery::forUser($a)->count() === 2` e que nenhuma do B aparece.
    - `test_tabela_e_imutavel_sem_updated_at`: asserir que a tabela NÃO tem coluna updated_at — `$this->assertFalse(Schema::hasColumn('viability_queries', 'updated_at'))` e `Schema::hasColumn('viability_queries', 'created_at')` true. (`use Illuminate\Support\Facades\Schema;`)
  </action>
  <verify>php artisan test --compact --filter=ViabilityQuerySchemaTest</verify>
  <acceptance_criteria>
    - grep "assertFalse(Schema::hasColumn('viability_queries', 'updated_at'))" tests/Feature/Viabilidade/ViabilityQuerySchemaTest.php
    - grep "forUser(" tests/Feature/Viabilidade/ViabilityQuerySchemaTest.php
    - ViabilityQuerySchemaTest verde (4 testes).
  </acceptance_criteria>
  <done>Schema test prova casts array, escopo do dono e imutabilidade (sem updated_at).</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=ViabilityQuerySchemaTest` verde.
- `php artisan test --compact` — suíte completa CONTINUA verde (tabela nova tabular não toca a baseline).
</verification>

<success_criteria>
- viability_queries imutável (sem updated_at), tabular (SQLite), com user_id nullable nullOnDelete.
- ViabilityQuery casta input/result/rules_versions para array e expõe forUser (histórico só do dono).
- Factory com estado anônima; schema test verde.
</success_criteria>

<output>
Criar `.planning/phases/07-consulta-previa-viabilidade/07-03-SUMMARY.md`: nome final da tabela/colunas, casts, o scope forUser e a decisão de imutabilidade. Insumo direto de 07-07 (persistência quando autenticado + listagem) e 07-09 (UI do histórico).
</output>

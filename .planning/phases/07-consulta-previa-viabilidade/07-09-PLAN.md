---
phase: 07-consulta-previa-viabilidade
plan: 09
type: execute
wave: 5
depends_on: ["07-07", "07-08"]
files_modified:
  - resources/js/pages/portal/viabilidade/historico.tsx
  - resources/js/layouts/portal-layout.tsx
  - tests/Feature/Viabilidade/HistoricoConsultaPaginaTest.php
autonomous: true
requirements: [HU-060]

must_haves:
  truths:
    - "O usuário autenticado vê a página de histórico das próprias consultas em /portal/viabilidade/historico, dentro do PortalLayout"
    - "A listagem mostra data, tipo de entrada, resumo da entrada e o veredito de cada consulta, paginada"
    - "Cada item permite ver o snapshot do resultado reusando o componente ResultadoViabilidade (07-08) — sem reprocessar, é o resultado da época"
    - "Há um item de navegação no portal (sidebar) para o histórico de consultas"
  artifacts:
    - path: "resources/js/pages/portal/viabilidade/historico.tsx"
      provides: "Página autenticada do histórico de consultas do próprio usuário"
      contains: "historico"
  key_links:
    - from: "resources/js/pages/portal/viabilidade/historico.tsx"
      to: "@/components/viabilidade/resultado-viabilidade"
      via: "reusa ResultadoViabilidade para exibir o snapshot salvo"
      pattern: "ResultadoViabilidade"
    - from: "resources/js/layouts/portal-layout.tsx"
      to: "/portal/viabilidade/historico"
      via: "item de navegação do portal"
      pattern: "viabilidade/historico"
---

<objective>
Construir a **página do histórico autenticado** (HU-060): lista as consultas do próprio usuário (data, tipo, entrada, veredito), paginada, com visualização do snapshot salvo reusando o componente `ResultadoViabilidade` do 07-08. Adicionar o item de navegação no portal.

Purpose: critério 3 do ROADMAP. O snapshot é o resultado DA ÉPOCA (não reprocessa) — coerente com a imutabilidade da tabela e o versionamento de regras. Mobile-first e acessível (portal do cidadão).
Output: página `portal/viabilidade/historico.tsx` (PortalLayout) + entrada na sidebar + feature test do componente.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-consulta-previa-viabilidade/07-CONTEXT.md
@.planning/phases/07-consulta-previa-viabilidade/07-07-SUMMARY.md
@.planning/phases/07-consulta-previa-viabilidade/07-08-SUMMARY.md

# PADRÕES A REUSAR (listagem do portal; layout; componente de resultado) — ler antes de codar:
@resources/js/pages/portal/empresas/index.tsx
@resources/js/layouts/portal-layout.tsx
@resources/js/components/viabilidade/resultado-viabilidade.tsx
@resources/js/components/ui/data-table/index.tsx
@resources/js/components/ui/badge.tsx
@resources/js/components/ui/empty-state.tsx
@resources/js/components/ui/pagination.tsx
</context>

<skill>
ATIVAR a skill `inertia-react-development` (Inertia v3 + React 19). Página em resources/js/pages com PortalLayout (autenticado). Reusar o componente ResultadoViabilidade (07-08) para o snapshot. O contrato dos itens vem do 07-07-SUMMARY (id, entry_type, resultado, resultado_label, input, created_at, result).
</skill>

<tasks>

<task type="auto">
  <name>Task 1: página historico.tsx (listagem do dono + ver snapshot) + item de navegação</name>
  <read_first>resources/js/pages/portal/empresas/index.tsx, resources/js/layouts/portal-layout.tsx, resources/js/components/viabilidade/resultado-viabilidade.tsx, resources/js/components/ui/empty-state.tsx</read_first>
  <files>resources/js/pages/portal/viabilidade/historico.tsx, resources/js/layouts/portal-layout.tsx</files>
  <action>
    Página `portal/viabilidade/historico.tsx` com `PortalLayout` (autenticado). Props: `{ consultas: { data: ItemHistorico[], from, to, total, ... } }` (contrato do 07-07).

    - Cabeçalho (PageHeader) "Histórico de consultas" com trilha [{label:'Meu painel', href:'/portal/painel'}].
    - Listagem (reusar DataTable OU uma lista de cards — seguir o padrão de portal/empresas/index.tsx): colunas Data (created_at formatado pt-BR), Tipo (entry_type → "Endereço"/"CNAE"/"Inscrição"), Entrada (resumo: endereço ou cnae+área ou inscrição), Veredito (Badge por resultado_label/cor — pendente=warning).
    - Ação "Ver resultado": expandir/abrir um modal (ui/modal) exibindo `<ResultadoViabilidade result={item.result} />` (snapshot salvo — NÃO refaz a consulta). Deixar claro na UI que é "Resultado registrado em {data}" (resultado da época).
    - EmptyState quando não há consultas: "Você ainda não realizou consultas" com CTA para `/portal/viabilidade`.
    - Paginação (ui/pagination com from/to/total do backend).
    - CTA "Nova consulta" → /portal/viabilidade.

    `portal-layout.tsx`: acrescentar no grupo de navegação "Serviços" (ou "Início") um item `{ name: 'Consultas de viabilidade', href: '/portal/viabilidade/historico', icon: <EyeIcon /> }` (usar um ícone já existente em @/components/icons). Edição mínima do array `groups`.
  </action>
  <verify>npx tsc --noEmit 2>&1 | tail -20 && npm run build 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - grep "ResultadoViabilidade" resources/js/pages/portal/viabilidade/historico.tsx
    - grep "PortalLayout" resources/js/pages/portal/viabilidade/historico.tsx
    - grep "/portal/viabilidade/historico" resources/js/layouts/portal-layout.tsx
    - typecheck e build verdes.
  </acceptance_criteria>
  <done>Página do histórico (PortalLayout) lista consultas do dono, exibe o snapshot via ResultadoViabilidade, com EmptyState/paginação; item de navegação no portal.</done>
</task>

<task type="auto">
  <name>Task 2: feature test da página do histórico (Inertia render autenticado, escopo do dono)</name>
  <read_first>tests/Feature/Viabilidade/HistoricoConsultaTest.php, app/Http/Controllers/Portal/HistoricoConsultaController.php</read_first>
  <files>tests/Feature/Viabilidade/HistoricoConsultaPaginaTest.php</files>
  <action>
    TDD. Agora que a página existe, asserir o componente Inertia.

    `tests/Feature/Viabilidade/HistoricoConsultaPaginaTest.php` (RefreshDatabase):
    - `test_pagina_do_historico_renderiza_consultas_do_dono` — `actingAs($user)`; criar 2 ViabilityQuery do usuário (factory) + 1 de outro; GET /portal/viabilidade/historico → `assertInertia(fn ($page) => $page->component('portal/viabilidade/historico')->has('consultas.data', 2))` (só as do dono).
    - `test_pagina_do_historico_exige_autenticacao` — GET sem login → redirect login.
    (usar `use Inertia\Testing\AssertableInertia;`)
  </action>
  <verify>php artisan test --compact --filter=HistoricoConsultaPaginaTest</verify>
  <acceptance_criteria>
    - grep "component('portal/viabilidade/historico')" tests/Feature/Viabilidade/HistoricoConsultaPaginaTest.php
    - HistoricoConsultaPaginaTest verde.
  </acceptance_criteria>
  <done>Teste assegura render autenticado e escopo do dono na página.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` e `npm run build` verdes.
- `php artisan test --compact --filter="HistoricoConsultaPaginaTest"` verde.
- `php artisan test --compact` — suíte completa CONTINUA verde.
- Verificação visual (composer dev): logar, fazer 1 consulta, abrir /portal/viabilidade/historico, ver a consulta listada e o snapshot no modal; mobile + dark mode.
</verification>

<success_criteria>
- Página autenticada do histórico lista só as consultas do dono, com snapshot reutilizando ResultadoViabilidade (resultado da época, sem reprocessar).
- Navegação do portal aponta para o histórico; EmptyState/paginação; acessível e responsivo.
</success_criteria>

<output>
Criar `.planning/phases/07-consulta-previa-viabilidade/07-09-SUMMARY.md`: caminho da página, decisão de exibir o snapshot (não reprocessar), e o item de navegação adicionado. Insumo do smoke E2E do 07-10 (histórico autenticado).
</output>

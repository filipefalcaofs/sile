---
phase: 07-consulta-previa-viabilidade
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Viabilidade/ConsultaViabilidadeInput.php
  - app/Services/Viabilidade/ConsultaViabilidadeResult.php
  - tests/Unit/Viabilidade/ConsultaViabilidadeResultTest.php
autonomous: true
requirements: [HU-054, HU-056, HU-057, HU-058, HU-059]

must_haves:
  truths:
    - "ConsultaViabilidadeInput é readonly com construtores nomeados paraEndereco/paraCnae/paraInscricao (tipo de entrada explícito)"
    - "ConsultaViabilidadeResult agrega geocode + território + enquadramento + risco como sub-resultados reais (toArray de cada), sem recomputar"
    - "O veredito locacional é PROPAGADO do consolidado do EnquadramentoResult (resultado/label/motivo) — o Result NÃO decide nada"
    - "versoes() agrega as versões de TODAS as regras: territorio + louos + risco (RN-002/RN-004)"
    - "toArray() é o contrato snake_case do endpoint JSON e do snapshot do histórico, com seções enquadramento (HU-057), risco (HU-058) e restrições (HU-059)"
  artifacts:
    - path: "app/Services/Viabilidade/ConsultaViabilidadeResult.php"
      provides: "DTO readonly de saída da consulta (agrega motores + veredito propagado + versoes de todas as regras + avisos)"
      contains: "final readonly class ConsultaViabilidadeResult"
    - path: "app/Services/Viabilidade/ConsultaViabilidadeInput.php"
      provides: "DTO readonly de entrada (tipo, endereco/cnae/area/inscricao) com construtores nomeados"
      contains: "final readonly class ConsultaViabilidadeInput"
  key_links:
    - from: "app/Services/Viabilidade/ConsultaViabilidadeResult.php"
      to: "App\\Enums\\ResultadoViabilidade"
      via: "veredito locacional propaga o consolidado do EnquadramentoResult (sem recomputar)"
      pattern: "vereditoLocacional"
    - from: "app/Services/Viabilidade/ConsultaViabilidadeResult.php"
      to: "App\\Services\\Louos\\EnquadramentoResult"
      via: "agrega o resultado do motor (toArray/versoes/resultado)"
      pattern: "EnquadramentoResult"
---

<objective>
Definir os **contratos de entrada e saída** da consulta de viabilidade: `ConsultaViabilidadeInput` (3 entradas honestas) e `ConsultaViabilidadeResult` (agrega os resultados reais dos motores, propaga o veredito locacional e expõe as versões de TODAS as regras). O Result é o snapshot que o serviço (07-05) preenche, o endpoint (07-06) serializa e o histórico (07-03/07) persiste.

Purpose: HU-054/056 (entradas), HU-057 (enquadramento), HU-058 (risco), HU-059 (restrições) — as "simulações" são VIEWS do mesmo resultado: o Result carrega `enquadramento`, `risco` e as restrições/bairro/via do território, e o veredito locacional é o consolidado do motor LOUOS PROPAGADO (a degradação "sem zona → pendente" vem do motor — o Result não recomputa). Sem fachada: o Result espelha exatamente o que os motores produziram.
Output: 2 DTOs readonly (espelham TerritoryResult/EnquadramentoResult/RiscoResult) com toArray snake_case e versoes() agregada; unit test do shape e da propagação.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-consulta-previa-viabilidade/07-CONTEXT.md
@docs/superpowers/specs/2026-06-14-consulta-previa-viabilidade-design.md

# CONTRATOS A ESPELHAR/AGREGAR (DTOs readonly dos motores + enum do veredito) — ler antes de codar:
@app/Services/Geo/GeocodeResult.php
@app/Services/Geo/TerritoryResult.php
@app/Services/Louos/EnquadramentoResult.php
@app/Services/Louos/EnquadramentoInput.php
@app/Services/Risco/RiscoResult.php
@app/Enums/ResultadoViabilidade.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConsultaViabilidadeInput (readonly + construtores nomeados das 3 entradas)</name>
  <read_first>app/Services/Louos/EnquadramentoInput.php, app/Services/Risco/RiscoInput.php</read_first>
  <files>app/Services/Viabilidade/ConsultaViabilidadeInput.php</files>
  <action>
    Namespace `App\Services\Viabilidade`. Espelhar o estilo de EnquadramentoInput/RiscoInput (readonly + named constructors). Definir uma constante de tipo de entrada.

    `final readonly class ConsultaViabilidadeInput`:
    - constantes: `const TIPO_ENDERECO = 'endereco'; const TIPO_CNAE = 'cnae'; const TIPO_INSCRICAO = 'inscricao';`
    - construtor:
      ```php
      public function __construct(
          public string $tipo,            // um dos TIPO_*
          public string $cnae,            // subclasse CNAE (dígitos ou formatada — o serviço normaliza)
          public ?float $area = null,     // m² — usada no Quadro 7 (HU-057)
          public ?string $endereco = null,
          public ?string $inscricao = null,
      ) {}
      ```
    - construtores nomeados:
      - `public static function paraEndereco(string $endereco, string $cnae, ?float $area = null): self` → tipo TIPO_ENDERECO.
      - `public static function paraCnae(string $cnae, ?float $area = null): self` → tipo TIPO_CNAE.
      - `public static function paraInscricao(string $inscricao, string $cnae, ?float $area = null): self` → tipo TIPO_INSCRICAO.
    - phpdoc explicando cada entrada e que `area` é opcional (sem área o Quadro 7 não enquadra → consolidado pendente honesto).
  </action>
  <verify>vendor/bin/pint --dirty --format agent</verify>
  <acceptance_criteria>
    - grep "final readonly class ConsultaViabilidadeInput" app/Services/Viabilidade/ConsultaViabilidadeInput.php
    - grep "public static function paraEndereco" app/Services/Viabilidade/ConsultaViabilidadeInput.php
    - grep "public static function paraInscricao" app/Services/Viabilidade/ConsultaViabilidadeInput.php
    - grep "TIPO_INSCRICAO = 'inscricao'" app/Services/Viabilidade/ConsultaViabilidadeInput.php
  </acceptance_criteria>
  <done>Input readonly com os 3 construtores nomeados e o tipo explícito.</done>
</task>

<task type="auto">
  <name>Task 2: ConsultaViabilidadeResult (agrega motores + veredito propagado + versoes de todas as regras + toArray) + unit test</name>
  <read_first>app/Services/Geo/TerritoryResult.php, app/Services/Louos/EnquadramentoResult.php, app/Services/Risco/RiscoResult.php, app/Enums/ResultadoViabilidade.php</read_first>
  <files>app/Services/Viabilidade/ConsultaViabilidadeResult.php, tests/Unit/Viabilidade/ConsultaViabilidadeResultTest.php</files>
  <action>
    TDD estrito (RED → verificar RED → GREEN → verificar GREEN → REFACTOR). Espelhar RiscoResult/EnquadramentoResult: readonly, agrega os sub-resultados TIPADOS (não recria nem recomputa) e expõe toArray snake_case + versoes() agregada.

    `final readonly class ConsultaViabilidadeResult`:
    - construtor (recebe os objetos dos motores — agregação, sem recomputar):
      ```php
      /**
       * @param array<string, mixed> $entrada metadados da entrada (tipo, cnae, cnae_formatado, area, endereco?, inscricao?)
       * @param list<string> $avisos degradações honestas comunicadas (zona pendente, inscrição indisponível, consulta por CNAE sem local)
       */
      public function __construct(
          public array $entrada,
          public ?GeocodeResult $geocode,
          public ?TerritoryResult $territory,
          public EnquadramentoResult $enquadramento,
          public RiscoResult $risco,
          public array $avisos = [],
      ) {}
      ```
      (imports: App\Services\Geo\GeocodeResult, App\Services\Geo\TerritoryResult, App\Services\Louos\EnquadramentoResult, App\Services\Risco\RiscoResult, App\Enums\ResultadoViabilidade.)

    - `public function vereditoLocacional(): array` — PROPAGA o consolidado do motor LOUOS (NÃO decide):
      ```php
      $resultado = $this->enquadramento->resultado(); // value de ResultadoViabilidade — vindo do motor
      return [
          'resultado' => $resultado,
          'label' => ResultadoViabilidade::from($resultado)->label(),
          'motivo' => $this->enquadramento->consolidado['motivo'] ?? null,
      ];
      ```
      Comentar: a degradação "sem zona → pendente" já vem do motor LOUOS (HU-044) — aqui só propagamos; o orquestrador NUNCA recomputa o veredito.

    - `public function fundamentacao(): array` — união honesta das referências legais já produzidas pelos motores: `array_values(array_unique([...($this->enquadramento->consolidado['fundamentacao'] ?? []), ...$this->risco->fundamentacao]))`.

    - `public function versoes(): array` — agrega as versões de TODAS as regras (RN-002/RN-004):
      ```php
      return [
          'territorio' => $this->territory?->versoes() ?? [],
          'louos' => $this->enquadramento->versoes(),
          'risco' => $this->risco->versoes(),
      ];
      ```

    - `public function toArray(): array` — contrato snake_case do endpoint JSON e do snapshot do histórico:
      ```php
      return [
          'entrada' => $this->entrada,
          'geocode' => $this->geocode?->toArray(),
          'territorio' => $this->territory?->toArray(),
          'enquadramento' => $this->enquadramento->toArray(), // HU-057 (Quadro 7) + restrições/condicionantes
          'risco' => $this->risco->toArray(),                  // HU-058 (municipal/sanitário/encaminhamento)
          'restricoes' => $this->territory?->restricoes,        // HU-059 (atalho às restrições do território)
          'veredito_locacional' => $this->vereditoLocacional(),
          'fundamentacao' => $this->fundamentacao(),
          'avisos' => $this->avisos,
          'versoes' => $this->versoes(),
      ];
      ```

    Unit test `tests/Unit/Viabilidade/ConsultaViabilidadeResultTest.php` (sem banco — montar os sub-resultados à mão):
    - helper para montar um EnquadramentoResult pendente (consolidado ['resultado' => 'pendente', 'motivo' => 'Permissão por zona pendente da base oficial (SEDUR)', 'fundamentacao' => ['Lei nº 9.148/2016 (LOUOS) — Quadro 7'], 'condicionantes' => []], versoes ['quadro7' => 'louos-q7-v1', ...]) e um RiscoResult classificado (fundamentacao ['Decreto Municipal nº 32.636/2020'], versoes ['municipal' => 'decreto-32636-2020', 'sanitario' => null]).
    - `test_veredito_locacional_propaga_o_consolidado_do_motor` — asserir veredito ['resultado' => 'pendente', 'label' => 'Pendente de análise técnica', 'motivo' => '...zona pendente...']; o Result NÃO recomputa.
    - `test_versoes_agrega_territorio_louos_e_risco` — asserir que versoes() tem as 3 chaves e que 'louos' carrega quadro7 'louos-q7-v1' e 'risco' carrega municipal 'decreto-32636-2020'.
    - `test_to_array_expoe_secoes_de_enquadramento_risco_e_restricoes` — asserir chaves snake_case (entrada, enquadramento, risco, restricoes, veredito_locacional, fundamentacao, avisos, versoes) e que `toArray()['veredito_locacional']['resultado'] === 'pendente'`.
    - `test_fundamentacao_une_motor_louos_e_risco_sem_duplicar` — asserir que a fundamentação contém as referências dos dois motores, sem duplicatas.
  </action>
  <verify>php artisan test --compact --filter=ConsultaViabilidadeResultTest</verify>
  <acceptance_criteria>
    - grep "final readonly class ConsultaViabilidadeResult" app/Services/Viabilidade/ConsultaViabilidadeResult.php
    - grep "public function vereditoLocacional" app/Services/Viabilidade/ConsultaViabilidadeResult.php
    - grep "'louos' => \$this->enquadramento->versoes()" app/Services/Viabilidade/ConsultaViabilidadeResult.php
    - grep "->enquadramento->resultado()" app/Services/Viabilidade/ConsultaViabilidadeResult.php
    - ConsultaViabilidadeResultTest verde (4 testes); prova que o veredito é PROPAGADO, não recomputado.
  </acceptance_criteria>
  <done>Result agrega os motores, propaga o veredito do consolidado LOUOS, agrega versoes() de todas as regras e expõe toArray snake_case; unit test verde.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="ConsultaViabilidadeResultTest"` verde.
- Evidência anti-fachada: o teste do veredito prova PROPAGAÇÃO (lê `enquadramento->resultado()`), não recomputo.
</verification>

<success_criteria>
- Input com as 3 entradas honestas (construtores nomeados).
- Result agrega geocode/território/enquadramento/risco como sub-resultados reais; veredito locacional propagado do motor LOUOS (sem recomputar); versoes() de todas as regras; toArray snake_case com seções HU-057/058/059.
- Unit test cobre propagação, agregação de versões, fundamentação unida e shape do toArray.
</success_criteria>

<output>
Criar `.planning/phases/07-consulta-previa-viabilidade/07-04-SUMMARY.md`: shape EXATO do ConsultaViabilidadeResult::toArray() (todas as chaves), assinatura dos construtores do Input, e a confirmação de que o veredito é propagado. Insumo direto de 07-05 (serviço preenche o Result), 07-06 (endpoint serializa), 07-07 (snapshot do histórico) e 07-08 (UI consome o JSON).
</output>

---
phase: 07-consulta-previa-viabilidade
plan: 10
type: execute
wave: 6
depends_on: ["07-05", "07-06", "07-07", "07-08", "07-09"]
files_modified:
  - app/Console/Commands/ConsultaViabilidadeCommand.php
  - tests/Feature/Viabilidade/ConsultaViabilidadeGoldenCaseTest.php
  - tests/Fixtures/golden/viabilidade/endereco-sem-zona-pendente.json
  - tests/Fixtures/golden/viabilidade/cnae-risco-real.json
  - tests/Fixtures/golden/viabilidade/inscricao-indisponivel.json
  - tests/Feature/Viabilidade/ConsultaViabilidadeCommandTest.php
autonomous: false
requirements: [HU-054, HU-055, HU-056, HU-057, HU-058, HU-059, HU-060]

must_haves:
  truths:
    - "Golden cases entrada→esperado rodam o orquestrador REAL sobre os seeds oficiais (#[DataProvider]) — regressão de domínio da consulta"
    - "Os golden cases provam a degradação honesta: sem zona → veredito pendente; inscrição indisponível → degrada com aviso, sem ponto inventado"
    - "O comando viabilidade:consultar dá evidência real de ponta a ponta (CNAE/endereço/inscrição) imprimindo o parecer e as versões"
    - "A suíte completa passa (SQLite, baseline + novos) e o grupo postgis (território real) passa onde tocar geometria"
    - "Smoke navegável aprovado: consulta pública por endereço (com mapa + resultado honesto) e histórico autenticado"
  artifacts:
    - path: "app/Console/Commands/ConsultaViabilidadeCommand.php"
      provides: "Comando de evidência real do orquestrador (espelha louos:enquadrar / risco:classificar)"
      contains: "viabilidade:consultar"
    - path: "tests/Feature/Viabilidade/ConsultaViabilidadeGoldenCaseTest.php"
      provides: "Golden cases da consulta (entrada→esperado) sobre o seed oficial via DataProvider"
      contains: "DataProvider"
  key_links:
    - from: "tests/Feature/Viabilidade/ConsultaViabilidadeGoldenCaseTest.php"
      to: "App\\Services\\Viabilidade\\ConsultaViabilidadeService"
      via: "executa o orquestrador real sobre fixtures JSON (anti-fachada)"
      pattern: "ConsultaViabilidadeService"
    - from: "app/Console/Commands/ConsultaViabilidadeCommand.php"
      to: "App\\Services\\Viabilidade\\ConsultaViabilidadeService"
      via: "consultarPorCnae/Endereco/Inscricao — evidência de ponta a ponta"
      pattern: "consultarPor"
---

<objective>
Fechar a fase com **regressão de domínio + evidência real + verificação integral**: golden cases da consulta (entrada→esperado sobre os motores reais), o comando `viabilidade:consultar` (parecer fundamentado de ponta a ponta) e a verificação completa (suíte SQLite + grupo postgis + pint + typecheck/build), encerrando com o smoke navegável (checkpoint humano).

Purpose: critérios 1, 2 e 3 do ROADMAP validados de ponta a ponta com EVIDÊNCIA FRESCA (regra de verificação antes de completar). Golden cases travam a regressão (precedente Fases 5/6) e provam a degradação honesta (sem zona → pendente; inscrição indisponível → degrada). O comando é a evidência de que a orquestração roda de verdade.
Output: comando + golden cases (3 fixtures) + ConsultaViabilidadeCommandTest + checkpoint de verificação integral e smoke.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-consulta-previa-viabilidade/07-CONTEXT.md
@.planning/phases/07-consulta-previa-viabilidade/07-05-SUMMARY.md
@.planning/phases/07-consulta-previa-viabilidade/07-06-SUMMARY.md
@.planning/phases/07-consulta-previa-viabilidade/07-08-SUMMARY.md

# PADRÕES A ESPELHAR (golden cases #[DataProvider]; comando de evidência) — ler antes de codar:
@tests/Feature/Louos/LouosGoldenCaseTest.php
@tests/Feature/Risco/RiscoGoldenCaseTest.php
@app/Console/Commands/LouosEnquadrarCommand.php
@app/Console/Commands/RiscoClassificarCommand.php
@app/Services/Viabilidade/ConsultaViabilidadeService.php
@app/Services/Viabilidade/ConsultaViabilidadeResult.php
@app/Services/Realty/PropertyRegistryLookup.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: comando viabilidade:consultar (evidência real de ponta a ponta)</name>
  <read_first>app/Console/Commands/LouosEnquadrarCommand.php, app/Console/Commands/RiscoClassificarCommand.php, app/Services/Viabilidade/ConsultaViabilidadeService.php</read_first>
  <files>app/Console/Commands/ConsultaViabilidadeCommand.php, tests/Feature/Viabilidade/ConsultaViabilidadeCommandTest.php</files>
  <action>
    TDD. Comando auto-descoberto `viabilidade:consultar`, espelhando louos:enquadrar/risco:classificar.

    Signature:
    ```
    viabilidade:consultar
      {cnae : Subclasse CNAE (dígitos ou formatada)}
      {--endereco= : Consulta por endereço (geocodifica de verdade)}
      {--inscricao= : Consulta por inscrição imobiliária (resolução pendente SEDUR — degrada)}
      {--area= : Área ocupada em m² (Quadro 7)}
    ```
    handle(ConsultaViabilidadeService $service): valida o CNAE (7 dígitos após normalização; erro+exit 1 se inválido). Escolhe o método pelo input:
    - `--endereco` presente → `consultarPorEndereco(endereco, cnae, area)` (capturar AddressNotFoundException/GeocoderException e imprimir erro honesto + exit 1).
    - senão `--inscricao` presente → `consultarPorInscricao(inscricao, cnae, area)`.
    - senão → `consultarPorCnae(cnae, area)`.
    Renderizar o parecer (reusar o estilo de LouosEnquadrarCommand::renderParecer): tipo de entrada, CNAE/denominação, área; RISCO (municipal/sanitário/encaminhamento); ENQUADRAMENTO (Quadro 7); VEREDITO LOCACIONAL (label + motivo — pendente quando sem zona); FUNDAMENTAÇÃO (lista); AVISOS; VERSÕES (todas as regras). exit 0 mesmo quando pendente (pendente NÃO é erro — é degradação honesta).

    Teste `ConsultaViabilidadeCommandTest` (RefreshDatabase; seeds reais; Geocoder fake bind p/ --endereco):
    - `test_comando_por_cnae_imprime_parecer_real` — `artisan('viabilidade:consultar', ['cnae'=>'4712-1/00','--area'=>120])` → exit 0, output contém o nível de risco e "Pendente de análise técnica" (sem zona) e a versão das regras.
    - `test_comando_cnae_invalido_falha` — cnae '123' → exit 1 com mensagem.
    - `test_comando_por_inscricao_degrada_com_aviso` — `--inscricao=123` (binding real Unavailable) → exit 0, output contém o aviso de "Resolução por inscrição imobiliária indisponível"; sem coordenada inventada.
  </action>
  <verify>php artisan test --compact --filter=ConsultaViabilidadeCommandTest && php artisan viabilidade:consultar 4712-1/00 --area=120 2>&1 | tail -25</verify>
  <acceptance_criteria>
    - grep "viabilidade:consultar" app/Console/Commands/ConsultaViabilidadeCommand.php
    - grep "consultarPorCnae\|consultarPorEndereco\|consultarPorInscricao" app/Console/Commands/ConsultaViabilidadeCommand.php
    - ConsultaViabilidadeCommandTest verde; o comando roda e imprime parecer real do seed.
  </acceptance_criteria>
  <done>Comando de evidência roda de ponta a ponta (CNAE/endereço/inscrição), imprime parecer + versões; pendente não é erro.</done>
</task>

<task type="auto">
  <name>Task 2: golden cases da consulta (#[DataProvider] sobre o orquestrador real) + 3 fixtures</name>
  <read_first>tests/Feature/Louos/LouosGoldenCaseTest.php, tests/Feature/Risco/RiscoGoldenCaseTest.php, app/Services/Viabilidade/ConsultaViabilidadeService.php</read_first>
  <files>tests/Feature/Viabilidade/ConsultaViabilidadeGoldenCaseTest.php, tests/Fixtures/golden/viabilidade/endereco-sem-zona-pendente.json, tests/Fixtures/golden/viabilidade/cnae-risco-real.json, tests/Fixtures/golden/viabilidade/inscricao-indisponivel.json</files>
  <action>
    TDD. Espelhar LouosGoldenCaseTest/RiscoGoldenCaseTest: carregar fixtures JSON entrada→esperado via `#[DataProvider]` (glob por __DIR__) e executar o `ConsultaViabilidadeService` REAL sobre o SEED OFICIAL (LouosQuadro7/10/11 + RiscoMunicipal/Sanitario). Para entradas por endereço/inscrição-resolvida, dar bind de Geocoder fake / SpatialRepository fake (sem zona — território real fica no smoke/postgis); a fixture declara o tipo de entrada e o esperado.

    Estrutura de cada fixture `{ "nome": "...", "input": { "tipo": "...", "cnae": "...", "area": ..., "endereco": ..., "inscricao": ... }, "esperado": { ... } }`. Chaves de `esperado` suportadas (assertGolden por match): `veredito` (resultado), `veredito_motivo_contem` (substring do motivo), `risco_municipal_status`, `quadro7_status`, `tem_geocode` (bool), `tem_territorio` (bool), `avisos_contem` (substring em algum aviso).

    Fixtures (casos-âncora, anti-fachada):
    - `endereco-sem-zona-pendente.json`: tipo 'endereco', cnae '4712-1/00', area 120 → esperado veredito 'pendente', veredito_motivo_contem 'zona', risco_municipal_status 'classificado', quadro7_status 'identificado', tem_geocode true, avisos_contem 'zona'. (PROVA: endereço real roda, mas sem zona oficial o veredito é pendente — nunca permitido/não permitido.)
    - `cnae-risco-real.json`: tipo 'cnae', cnae '4712-1/00', area 120 → veredito 'pendente', risco_municipal_status 'classificado', quadro7_status 'identificado', tem_territorio false, avisos_contem 'não avalia o local'.
    - `inscricao-indisponivel.json`: tipo 'inscricao', inscricao '123456', cnae '4712-1/00', area 120 → veredito 'pendente', tem_geocode false, tem_territorio false, avisos_contem 'Resolução por inscrição imobiliária indisponível'. (PROVA: degrada honesto, sem ponto inventado.)
    (Confirmar o CNAE-âncora real no seed do Quadro 7; se 4712-1/00 não estiver enquadrado, escolher um CNAE presente no seed oficial e ajustar a fixture — o golden deve bater contra o DADO REAL.)

    Teste `ConsultaViabilidadeGoldenCaseTest` (RefreshDatabase; setUp seeda os Quadros + risco; data provider lê os JSON; cada caso monta o input, escolhe o método do service conforme `tipo`, e asserta cada chave de `esperado` com mensagem que identifica o golden case).
  </action>
  <verify>php artisan test --compact --filter=ConsultaViabilidadeGoldenCaseTest</verify>
  <acceptance_criteria>
    - grep "DataProvider" tests/Feature/Viabilidade/ConsultaViabilidadeGoldenCaseTest.php
    - ls tests/Fixtures/golden/viabilidade/*.json mostra os 3 fixtures
    - grep "pendente" tests/Fixtures/golden/viabilidade/endereco-sem-zona-pendente.json
    - grep "Resolução por inscrição imobiliária indisponível" tests/Fixtures/golden/viabilidade/inscricao-indisponivel.json
    - ConsultaViabilidadeGoldenCaseTest verde (3 casos), batendo contra o seed oficial.
  </acceptance_criteria>
  <done>Golden cases entrada→esperado sobre o orquestrador real provam a degradação honesta (sem zona/inscrição); regressão de domínio travada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: verificação integral + smoke navegável (consulta pública + histórico autenticado)</name>
  <what-built>
    A Fase 7 completa: orquestrador real (Geocoder→Território→LOUOS→Risco) sem recomputar veredito; 3 entradas públicas (endereço/CNAE/inscrição) com throttle/toggle/auditoria; inscrição bloqueada via contrato; histórico autenticado escopado ao dono; UI pública (mapa reusado + resultado honesto) e UI do histórico; golden cases + comando de evidência.
  </what-built>
  <how-to-verify>
    EVIDÊNCIA FRESCA — rodar e LER o output inteiro antes de afirmar (regra de verificação antes de completar):
    1. `vendor/bin/pint --dirty --format agent` — sem pendências.
    2. `php artisan test --compact` — suíte completa SQLite verde (baseline ~600 + novos da fase, incluindo Seeders 35, Viabilidade*, Historico*, golden). Registrar o total no SUMMARY.
    3. Território real (onde tocar geometria): `php artisan test --compact --group=postgis` com o container PostGIS de pé — 15/15 (ou o baseline vigente) verde. Se o orquestrador tiver teste @group postgis (consulta por endereço real → identificação espacial), incluir.
    4. `npx tsc --noEmit` e `npm run build` — verdes.
    5. Evidência do comando: `php artisan viabilidade:consultar 4712-1/00 --area=120` (parecer pendente com motivo de zona) e `php artisan viabilidade:consultar 4712-1/00 --endereco="Praça da Sé, Salvador" --area=120` (geocodifica real; veredito pendente sem zona). Colar o output no SUMMARY.
    6. SMOKE NAVEGÁVEL (`composer dev`):
       a. Anônimo: abrir /portal/viabilidade, consultar por endereço de Salvador → mapa mostra o ponto; risco + enquadramento reais; veredito "Pendente de análise técnica" com motivo de zona (NUNCA Permitido/Não permitido). Testar CNAE e inscrição (aviso de indisponível). Conferир mobile (375px) e dark mode.
       b. Autenticado: logar no portal, refazer uma consulta, abrir /portal/viabilidade/historico → a consulta aparece; abrir o snapshot. Confirmar que outro usuário não vê.
    7. Confirmar a auditoria: `php artisan tinker --execute 'echo App\Models\Activity::where("log_name","viabilidade")->count();'` > 0 (consultas auditadas, anônimas incluídas).
  </how-to-verify>
  <resume-signal>Responder "aprovado" (anexando os outputs/prints) ou descrever divergências para correção antes de fechar a fase.</resume-signal>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact` (SQLite) verde; `--group=postgis` verde onde aplicável.
- `npx tsc --noEmit` e `npm run build` verdes.
- Golden cases + comando dão evidência real; smoke navegável aprovado (pública + histórico).
</verification>

<success_criteria>
- Critério 1 (3 entradas, sem processo formal), 2 (enquadramento+risco+restrições com fundamentação, motores reais) e 3 (histórico do próprio usuário) do ROADMAP validados com evidência fresca.
- Golden cases travam a regressão e provam a degradação honesta (sem zona → pendente; inscrição indisponível → aviso).
- Orquestrador não recomputa veredito; nada hardcoded; ZERO dependência nova; sem fachada.
</success_criteria>

<output>
Criar `.planning/phases/07-consulta-previa-viabilidade/07-10-SUMMARY.md`: total final da suíte (SQLite + postgis), output do comando viabilidade:consultar (CNAE e endereço), os golden cases criados, e o registro do smoke navegável aprovado. Registrar explicitamente as PENDÊNCIAS que permanecem bloqueadas pendente SEDUR: veredito locacional permitido/não permitido (zona — Quadro 10) e resolução por inscrição imobiliária (lote/Cadastro — contrato pronto, binding trocado na Fase 13). Atualizar STATE.md/ROADMAP no fechamento da fase.
</output>

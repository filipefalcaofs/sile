---
phase: 07-consulta-previa-viabilidade
plan: 07
type: execute
wave: 4
depends_on: ["07-03", "07-05", "07-06"]
files_modified:
  - app/Http/Controllers/Portal/ConsultaViabilidadeController.php
  - app/Http/Controllers/Portal/HistoricoConsultaController.php
  - routes/portal.php
  - tests/Feature/Viabilidade/HistoricoConsultaTest.php
autonomous: true
requirements: [HU-060]

must_haves:
  truths:
    - "Quando o usuário está autenticado, a consulta grava um snapshot imutável em viability_queries (input + result + rules_versions + user_id); anônima NÃO grava histórico pessoal (segue auditada)"
    - "O usuário autenticado lista o histórico das PRÓPRIAS consultas (escopo forUser) — nunca vê as de outro usuário"
    - "A listagem é paginada (parâmetro ui), ordenada da mais recente, e a consulta da listagem é auditada (RN-002)"
    - "A rota de histórico é AUTENTICADA (auth:web + verified + lgpd), enquanto a consulta em si segue pública (07-06)"
  artifacts:
    - path: "app/Http/Controllers/Portal/HistoricoConsultaController.php"
      provides: "Listagem do histórico do próprio usuário (server-driven, paginada, auditada)"
      contains: "class HistoricoConsultaController"
    - path: "app/Http/Controllers/Portal/ConsultaViabilidadeController.php"
      provides: "Persistência do snapshot quando autenticado (nos 3 endpoints)"
      contains: "ViabilityQuery"
  key_links:
    - from: "app/Http/Controllers/Portal/ConsultaViabilidadeController.php"
      to: "App\\Models\\ViabilityQuery"
      via: "grava snapshot só quando auth()->check() (input + result + rules_versions + user_id)"
      pattern: "ViabilityQuery::create"
    - from: "app/Http/Controllers/Portal/HistoricoConsultaController.php"
      to: "App\\Models\\ViabilityQuery"
      via: "forUser(user) — escopo do dono"
      pattern: "forUser"
---

<objective>
Entregar o **histórico autenticado** (HU-060): gravar o snapshot imutável da consulta QUANDO o usuário está autenticado (nos mesmos endpoints públicos do 07-06) e listar o histórico das PRÓPRIAS consultas (escopo pelo dono, precedente "Minhas empresas"). Consulta anônima permanece auditada, mas sem histórico pessoal.

Purpose: critério 3 do ROADMAP (usuário autenticado consulta o histórico das próprias consultas). O service permanece reutilizável (HU-141, Fase 8) — a persistência é decisão do controller, não do service. Sem fachada: o snapshot guarda o resultado e as versões da época (reproduzível); o escopo do dono é regra real no backend (testado contra acesso cruzado).
Output: persistência só-quando-autenticado nos 3 endpoints + `HistoricoConsultaController@index` (paginado, escopado, auditado) + rota autenticada + feature test.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-consulta-previa-viabilidade/07-CONTEXT.md
@.planning/phases/07-consulta-previa-viabilidade/07-03-SUMMARY.md
@.planning/phases/07-consulta-previa-viabilidade/07-05-SUMMARY.md
@.planning/phases/07-consulta-previa-viabilidade/07-06-SUMMARY.md

# PADRÃO A ESPELHAR (escopo por dono + listagem server-driven paginada + rota autenticada) — ler antes de codar:
@app/Models/ViabilityQuery.php
@app/Http/Controllers/Portal/CompanyController.php
@app/Http/Controllers/Portal/ConsultaViabilidadeController.php
@app/Models/Company.php
@routes/portal.php
@app/Support/Settings.php
@app/Support/Audit/AuditService.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: persistência do snapshot quando autenticado (nos 3 endpoints) — anônima não grava</name>
  <read_first>app/Http/Controllers/Portal/ConsultaViabilidadeController.php, app/Models/ViabilityQuery.php, app/Services/Viabilidade/ConsultaViabilidadeResult.php</read_first>
  <files>app/Http/Controllers/Portal/ConsultaViabilidadeController.php, tests/Feature/Viabilidade/HistoricoConsultaTest.php</files>
  <action>
    TDD estrito (RED→GREEN). Editar o controller do 07-06 sem quebrar os testes do ConsultaViabilidadePublicaTest.

    Acrescentar um método privado no `ConsultaViabilidadeController`:
    ```php
    private function registrarHistorico(\Illuminate\Http\Request $request, ConsultaViabilidadeResult $result): void
    {
        // HU-060: histórico pessoal SÓ quando autenticado (guard web). Anônima
        // segue auditada (RN-002, no service), mas não cria histórico pessoal.
        $user = $request->user();
        if ($user === null) {
            return;
        }
        $payload = $result->toArray();
        ViabilityQuery::create([
            'user_id' => $user->id,
            'entry_type' => $payload['entrada']['tipo'],
            'input' => $payload['entrada'],
            'result' => $payload,
            'rules_versions' => $payload['versoes'],
            'resultado' => $payload['veredito_locacional']['resultado'],
            'ip_address' => $request->ip(),
            'created_at' => now(),
        ]);
    }
    ```
    Chamar `$this->registrarHistorico($request, $result);` nos 3 métodos (endereco/cnae/inscricao) ANTES do `return response()->json(...)`, SOMENTE no caminho de sucesso (após obter o `$result`). NÃO gravar nos caminhos de erro (endereço não localizado / serviço indisponível / toggle off). Passar o `$request` (os FormRequests já são instâncias de Request) ao método.

    Casos de teste em `tests/Feature/Viabilidade/HistoricoConsultaTest.php` (RefreshDatabase; seeds reais; Geocoder/Spatial fakes):
    - `test_consulta_autenticada_grava_snapshot_no_historico` (HU-060/CA-01) — `actingAs($user)` (guard web) + POST /portal/viabilidade/cnae → 200; asserir `ViabilityQuery::count() === 1`, user_id do $user, entry_type 'cnae', `result['veredito_locacional']['resultado']` presente, rules_versions não vazio.
    - `test_consulta_anonima_nao_grava_historico_mas_e_auditada` — POST sem autenticar → 200; `ViabilityQuery::count() === 0`; Activity 'viabilidade'/'consulta' existe (auditada).
    - `test_consulta_com_erro_nao_grava_historico` — autenticado + endereço que faz o Geocoder fake lançar AddressNotFoundException → 404; `ViabilityQuery::count() === 0` (erro não vira histórico).
  </action>
  <verify>php artisan test --compact --filter="HistoricoConsultaTest|ConsultaViabilidadePublicaTest"</verify>
  <acceptance_criteria>
    - grep "ViabilityQuery::create" app/Http/Controllers/Portal/ConsultaViabilidadeController.php
    - grep "registrarHistorico" app/Http/Controllers/Portal/ConsultaViabilidadeController.php
    - Testes de gravação autenticada / não-gravação anônima / não-gravação em erro verdes.
    - ConsultaViabilidadePublicaTest (07-06) continua verde.
  </acceptance_criteria>
  <done>Snapshot gravado só quando autenticado (3 endpoints, caminho de sucesso); anônima e erros não gravam; auditoria intacta.</done>
</task>

<task type="auto">
  <name>Task 2: HistoricoConsultaController@index (escopo do dono, paginado, auditado) + rota autenticada</name>
  <read_first>app/Http/Controllers/Portal/CompanyController.php, app/Models/ViabilityQuery.php, routes/portal.php</read_first>
  <files>app/Http/Controllers/Portal/HistoricoConsultaController.php, routes/portal.php, tests/Feature/Viabilidade/HistoricoConsultaTest.php</files>
  <action>
    TDD. Criar o controller e a rota AUTENTICADA.

    1. `HistoricoConsultaController` (single-action `__invoke` ou `index`): construtor injeta `AuditService $audit`.
       - escopo do dono: `ViabilityQuery::forUser($request->user())->latest('created_at')->latest('id')`.
       - paginação parametrizada: `->paginate((int) Settings::get('ui.consultas.per_page', 15))` — usar uma chave de page-size; se preferir NÃO criar parâmetro novo (para não mexer no catálogo do 07-01), reutilizar uma constante técnica `15` direta NÃO; melhor: usar `Settings::get('ui.companies.per_page', 15)` como page-size compartilhado do portal OU registrar a chave no 07-01. DECISÃO: reutilizar `ui.companies.per_page` (precedente de listagens do portal) para NÃO crescer o catálogo de novo nesta fase. Documentar essa reutilização no SUMMARY.
       - transformar cada item para o contrato da UI: `['id'=>, 'entry_type'=>, 'resultado'=>, 'resultado_label'=> ResultadoViabilidade::from($q->resultado)->label() (quando não nulo), 'input'=>$q->input, 'created_at'=>$q->created_at->toIso8601String(), 'result'=>$q->result]` (o `result` completo alimenta a visualização do snapshot no 07-09).
       - `Inertia::render('portal/viabilidade/historico', ['consultas' => <paginator transformado com from/to/total>])` (página construída no 07-09).
       - auditar a consulta da listagem: `audit->log('viabilidade','consulta-historico','Consulta do histórico de viabilidade', ['itens'=>$paginator->total()], result:'sucesso')` (consulta relevante a dado do próprio usuário — precedente histórico de acessos).

    2. `routes/portal.php`: acrescentar a rota DENTRO do grupo autenticado `['auth:web','verified']` + `lgpd.accepted` (o mesmo grupo de painel/empresas), como rota LITERAL:
       ```php
       Route::get('viabilidade/historico', HistoricoConsultaController::class)->name('viabilidade.historico');
       ```
       ATENÇÃO: declarar no grupo autenticado correto; a rota pública `viabilidade.index` (07-06) é literal distinta, sem conflito. Confirmar que `viabilidade/historico` não é capturada por nenhuma rota pública.

    Casos de teste:
    - `test_historico_lista_so_as_consultas_do_dono` (HU-060/CA-04 — escopo) — usuário A com 2 consultas gravadas, usuário B com 1; `actingAs($a)` GET /portal/viabilidade/historico → 200, asserir via Inertia que `consultas.total === 2` (ou que os ids retornados são só os do A); nenhuma do B presente.
    - `test_historico_exige_autenticacao` — GET sem autenticar → redirect para login (rota é autenticada).
    - `test_consulta_do_historico_e_auditada` — Activity 'viabilidade'/'consulta-historico' gravada com causer = usuário.
    - `test_historico_ordena_da_mais_recente` — gravar 2 consultas em tempos distintos; asserir ordem (mais recente primeiro).
  </action>
  <verify>php artisan test --compact --filter=HistoricoConsultaTest</verify>
  <acceptance_criteria>
    - grep "class HistoricoConsultaController" app/Http/Controllers/Portal/HistoricoConsultaController.php
    - grep "forUser" app/Http/Controllers/Portal/HistoricoConsultaController.php
    - grep "viabilidade.historico" routes/portal.php
    - grep "consulta-historico" app/Http/Controllers/Portal/HistoricoConsultaController.php
    - Testes de escopo/autenticação/auditoria/ordenação verdes.
  </acceptance_criteria>
  <done>Listagem do histórico escopada ao dono, paginada e auditada, sob rota autenticada; acesso cruzado bloqueado por escopo (testado).</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="HistoricoConsultaTest|ConsultaViabilidadePublicaTest"` verde.
- `php artisan test --compact` — suíte completa CONTINUA verde.
- Evidência: snapshot só-quando-autenticado e escopo do dono provados por teste (acesso cruzado bloqueado).
</verification>

<success_criteria>
- Consulta autenticada grava snapshot imutável (input+result+rules_versions+user_id); anônima e erros não gravam (mas anônima é auditada).
- Histórico lista só as consultas do próprio usuário (escopo real, testado); paginado, ordenado, auditado, autenticado.
</success_criteria>

<output>
Criar `.planning/phases/07-consulta-previa-viabilidade/07-07-SUMMARY.md`: regra de gravação (só quando auth, caminho de sucesso), shape do item da listagem (contrato da UI 07-09), nome da rota (portal.viabilidade.historico), page-size reutilizado (ui.companies.per_page) e evento de auditoria ('consulta-historico'). Insumo direto de 07-09 (página do histórico).
</output>

---
phase: 07-consulta-previa-viabilidade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/seeders/ParameterSeeder.php
  - config/sile.php
  - app/Providers/FortifyServiceProvider.php
  - tests/Feature/Seeders/ParameterSeederTest.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
autonomous: true
requirements: [HU-054, HU-056]

must_haves:
  truths:
    - "O administrador pode ligar/desligar a consulta de viabilidade por interface (toggle features.consulta_viabilidade), sem deploy"
    - "O administrador pode ajustar o limite de consultas públicas por minuto (seguranca.throttle.consulta_viabilidade.por_minuto), sem deploy"
    - "Existe um RateLimiter nomeado 'consulta-viabilidade' que lê o limite do parâmetro (por usuário autenticado, ou por IP quando anônimo)"
    - "O catálogo de parâmetros cresce de 33 para 35 e os DOIS testes de seeder travam a nova contagem"
    - "Nenhuma dependência nova; os fallbacks em config/sile.php espelham os parâmetros (Settings funciona com banco indisponível)"
  artifacts:
    - path: "database/seeders/ParameterSeeder.php"
      provides: "2 parâmetros novos: features.consulta_viabilidade (toggle) e seguranca.throttle.consulta_viabilidade.por_minuto"
      contains: "features.consulta_viabilidade"
    - path: "app/Providers/FortifyServiceProvider.php"
      provides: "RateLimiter::for('consulta-viabilidade') parametrizado, espelhando 'cnpj-lookup'/'geocoding'"
      contains: "consulta-viabilidade"
  key_links:
    - from: "app/Providers/FortifyServiceProvider.php"
      to: "App\\Support\\Settings"
      via: "lê seguranca.throttle.consulta_viabilidade.por_minuto (sem hardcode)"
      pattern: "consulta_viabilidade.por_minuto"
    - from: "database/seeders/ParameterSeeder.php"
      to: "tests/Feature/Seeders/ParameterSeederTest.php"
      via: "catálogo 33→35; os dois testes de seeder travam a contagem"
      pattern: "consulta_viabilidade"
---

<objective>
Fundação de **parametrização** da Fase 7: registrar os 2 parâmetros administráveis que governam a consulta pública (toggle + throttle), espelhar os fallbacks em `config/sile.php` e expor o RateLimiter nomeado que as rotas públicas (07-06) vão usar — sem nenhuma dependência nova.

Purpose: a consulta prévia é uma rota PÚBLICA; pela disciplina da Fase 3.1 toda rota pública nasce com throttle parametrizado (HU-014) e toda funcionalidade acoplável nasce com feature toggle administrável. Sem fachada: desligar o toggle degrada de forma comunicada (implementado no 07-06); o throttle protege o serviço sem hardcode.
Output: catálogo de parâmetros 33→35, fallbacks em config, RateLimiter `consulta-viabilidade`, e os dois testes de seeder atualizados (contagem + caso dedicado).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-consulta-previa-viabilidade/07-CONTEXT.md
@docs/superpowers/specs/2026-06-14-consulta-previa-viabilidade-design.md

# PADRÃO A ESPELHAR (parâmetro + fallback + RateLimiter parametrizado) — ler antes de codar:
@database/seeders/ParameterSeeder.php
@config/sile.php
@app/Providers/FortifyServiceProvider.php
@app/Support/Settings.php
@tests/Feature/Seeders/ParameterSeederTest.php
@tests/Feature/Seeders/DatabaseSeederTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: 2 parâmetros novos + fallbacks em config/sile.php + RateLimiter consulta-viabilidade</name>
  <read_first>database/seeders/ParameterSeeder.php, config/sile.php, app/Providers/FortifyServiceProvider.php, app/Support/Settings.php</read_first>
  <files>database/seeders/ParameterSeeder.php, config/sile.php, app/Providers/FortifyServiceProvider.php</files>
  <action>
    TDD: escrever os asserts dos parâmetros no ParameterSeederTest (Task 2) PRIMEIRO se preferir, ou implementar o catálogo e travar com o teste em seguida — mas em ambos os casos confirme RED→GREEN. O catálogo é dado: a verificação é o teste de seeder.

    1. `database/seeders/ParameterSeeder.php` — acrescentar ao array `catalog()` (NÃO remover nada; manter a ordem/estilo das entradas existentes):
       - `features.consulta_viabilidade`: group 'features', type 'boolean', default_value '1', validation_rules ['required','boolean'], description 'Habilita a consulta prévia de viabilidade no portal do cidadão (endereço, CNAE e inscrição imobiliária)'.
       - `seguranca.throttle.consulta_viabilidade.por_minuto`: group 'seguranca', type 'integer', default_value '20', validation_rules ['required','integer','min:1','max:300'], description 'Limite de consultas de viabilidade por minuto por usuário/IP no portal'.
       (Nenhum grupo novo: 'features' e 'seguranca' já existem — a lista de grupos dos testes não muda.)

    2. `config/sile.php` — acrescentar os fallbacks espelhando os parâmetros (Settings::get lê config("sile.{$key}") quando o banco está indisponível):
       - no bloco `features` existente: `'consulta_viabilidade' => true`.
       - no bloco `seguranca.throttle` existente: `'consulta_viabilidade' => ['por_minuto' => 20]` (ao lado de cnpj_lookup e geocoding).

    3. `app/Providers/FortifyServiceProvider.php` — no método boot(), DEPOIS do RateLimiter 'geocoding', acrescentar (espelhar EXATAMENTE o padrão de 'cnpj-lookup'/'geocoding'):
       ```php
       // Throttle da consulta pública de viabilidade (EP07) — limite
       // administrável sem deploy; reusa o padrão de throttle parametrizado
       // estabelecido na Fase 3.1 (cnpj-lookup/geocoding).
       RateLimiter::for('consulta-viabilidade', function (Request $request) {
           return Limit::perMinute(
               (int) Settings::get('seguranca.throttle.consulta_viabilidade.por_minuto', 20),
           )->by($request->user()?->id ?: $request->ip());
       });
       ```
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan config:show sile.seguranca.throttle.consulta_viabilidade.por_minuto</verify>
  <acceptance_criteria>
    - grep "features.consulta_viabilidade" database/seeders/ParameterSeeder.php
    - grep "seguranca.throttle.consulta_viabilidade.por_minuto" database/seeders/ParameterSeeder.php
    - grep "'consulta_viabilidade' => true" config/sile.php
    - grep "consulta-viabilidade" app/Providers/FortifyServiceProvider.php
    - `php artisan route:list` continua sem erro de boot (provider carrega).
  </acceptance_criteria>
  <done>2 parâmetros no catálogo (grupos existentes), fallbacks em config, RateLimiter 'consulta-viabilidade' parametrizado por usuário/IP.</done>
</task>

<task type="auto">
  <name>Task 2: travar a contagem 33→35 e o caso dedicado nos dois testes de seeder</name>
  <read_first>tests/Feature/Seeders/ParameterSeederTest.php, tests/Feature/Seeders/DatabaseSeederTest.php</read_first>
  <files>tests/Feature/Seeders/ParameterSeederTest.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <action>
    TDD: estas edições devem FALHAR antes da Task 1 estar aplicada (RED por contagem) e passar depois (GREEN). Se você fez a Task 1 primeiro, confirme que os asserts antigos (33) ficariam vermelhos sem a edição — não pule a verificação.

    1. `tests/Feature/Seeders/ParameterSeederTest.php`:
       - em `test_seeder_cria_catalogo_completo`: trocar `$this->assertSame(33, Parameter::query()->count());` por `35`.
       - em `test_seeder_e_idempotente`: trocar `$this->assertSame(33, ...)` por `35`.
       - a asserção da lista de grupos NÃO muda (features/seguranca já existem) — manter `['features', 'geo', 'integracoes', 'louos', 'retencao', 'risco', 'seguranca', 'ui']`.
       - adicionar método `test_seeder_registra_parametros_da_consulta_de_viabilidade`:
         ```php
         public function test_seeder_registra_parametros_da_consulta_de_viabilidade(): void
         {
             $this->seed(ParameterSeeder::class);

             $toggle = Parameter::query()->where('key', 'features.consulta_viabilidade')->first();
             $this->assertNotNull($toggle);
             $this->assertSame('features', $toggle->group);
             $this->assertSame('boolean', $toggle->type);
             $this->assertSame('1', $toggle->default_value);
             $this->assertSame(['required', 'boolean'], $toggle->validation_rules);
             $this->assertNull($toggle->value);

             $throttle = Parameter::query()->where('key', 'seguranca.throttle.consulta_viabilidade.por_minuto')->first();
             $this->assertNotNull($throttle);
             $this->assertSame('seguranca', $throttle->group);
             $this->assertSame('integer', $throttle->type);
             $this->assertSame('20', $throttle->default_value);
             $this->assertSame(['required', 'integer', 'min:1', 'max:300'], $throttle->validation_rules);
             $this->assertNull($throttle->value);
         }
         ```

    2. `tests/Feature/Seeders/DatabaseSeederTest.php`: trocar `$this->assertSame(33, Parameter::query()->count());` por `35` nos DOIS pontos (`test_seed_completo_prepara_ambiente_de_desenvolvimento` e `test_seed_e_idempotente`).
  </action>
  <verify>php artisan test --compact --filter="ParameterSeederTest|DatabaseSeederTest"</verify>
  <acceptance_criteria>
    - grep "assertSame(35" tests/Feature/Seeders/ParameterSeederTest.php
    - grep "test_seeder_registra_parametros_da_consulta_de_viabilidade" tests/Feature/Seeders/ParameterSeederTest.php
    - grep "assertSame(35" tests/Feature/Seeders/DatabaseSeederTest.php
    - ParameterSeederTest e DatabaseSeederTest verdes (todos os métodos).
  </acceptance_criteria>
  <done>Contagem 35 travada nos dois seeders; caso dedicado dos 2 parâmetros verde.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="ParameterSeederTest|DatabaseSeederTest"` verde.
- `php artisan test --compact` — suíte completa (baseline ~600, default --exclude-group postgis do phpunit.xml) CONTINUA verde: só foram acrescidos 2 parâmetros e 1 RateLimiter.
</verification>

<success_criteria>
- features.consulta_viabilidade e seguranca.throttle.consulta_viabilidade.por_minuto no catálogo, com fallback em config/sile.php.
- RateLimiter 'consulta-viabilidade' parametrizado, pronto para as rotas públicas do 07-06.
- Catálogo 35 travado nos DOIS testes de seeder.
- ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/07-consulta-previa-viabilidade/07-01-SUMMARY.md`: nomes exatos dos 2 parâmetros e defaults, o nome do RateLimiter ('consulta-viabilidade') e a nova contagem (35). Insumo direto de 07-06 (rotas/throttle/toggle).
</output>

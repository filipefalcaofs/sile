---
phase: 07-consulta-previa-viabilidade
plan: 06
type: execute
wave: 3
depends_on: ["07-01", "07-03", "07-05"]
files_modified:
  - app/Http/Controllers/Portal/ConsultaViabilidadeController.php
  - app/Http/Requests/Portal/ConsultaViabilidadeEnderecoRequest.php
  - app/Http/Requests/Portal/ConsultaViabilidadeCnaeRequest.php
  - app/Http/Requests/Portal/ConsultaViabilidadeInscricaoRequest.php
  - routes/portal.php
  - bootstrap/app.php
  - tests/Feature/Viabilidade/ConsultaViabilidadePublicaTest.php
autonomous: true
requirements: [HU-054, HU-055, HU-056]

must_haves:
  truths:
    - "A página de consulta e os 3 endpoints JSON (endereço/CNAE/inscrição) são PÚBLICOS (anônimo acessa) sob /portal/viabilidade, com throttle parametrizado"
    - "Cada endpoint retorna o ConsultaViabilidadeResult::toArray() (JSON) e é auditado (RN-002), com causer sistema/IP quando anônimo"
    - "Endereço não localizado / serviço indisponível devolve mensagem HONESTA (não resultado falso); inscrição indisponível devolve o resultado degradado + aviso (HU-055)"
    - "Toggle features.consulta_viabilidade desligado bloqueia ANTES de executar, com aviso comunicado (degradação controlada, nunca falha silenciosa)"
    - "Exceções dos endpoints renderizam JSON (não redirect) quando o cliente pede JSON — padrão do portal (shouldRenderJsonWhen)"
  artifacts:
    - path: "app/Http/Controllers/Portal/ConsultaViabilidadeController.php"
      provides: "Página pública (index) + endpoints endereco/cnae/inscricao chamando o ConsultaViabilidadeService"
      contains: "class ConsultaViabilidadeController"
    - path: "routes/portal.php"
      provides: "Rotas públicas /portal/viabilidade (page) e /portal/viabilidade/{endereco,cnae,inscricao} com throttle:consulta-viabilidade"
      contains: "viabilidade"
  key_links:
    - from: "app/Http/Controllers/Portal/ConsultaViabilidadeController.php"
      to: "App\\Services\\Viabilidade\\ConsultaViabilidadeService"
      via: "delega para consultarPorEndereco/Cnae/Inscricao (não reimplementa)"
      pattern: "ConsultaViabilidadeService"
    - from: "routes/portal.php"
      to: "throttle:consulta-viabilidade"
      via: "middleware do RateLimiter parametrizado (07-01)"
      pattern: "throttle:consulta-viabilidade"
    - from: "bootstrap/app.php"
      to: "shouldRenderJsonWhen"
      via: "portal/viabilidade/* renderiza JSON em exceção quando expectsJson"
      pattern: "portal/viabilidade"
---

<objective>
Expor a **consulta pública** de viabilidade pela HTTP: a página (Inertia) e os 3 endpoints JSON (endereço HU-054, CNAE HU-056, inscrição HU-055), todos públicos, throttled e auditados, delegando ao `ConsultaViabilidadeService`. Erros honestos (endereço não localizado, serviço indisponível, toggle off) — nunca resultado falso.

Purpose: o critério 1 do ROADMAP (cidadão consulta por endereço, inscrição ou CNAE, sem processo formal). Padrão da Fase 3.1: rota pública com throttle parametrizado + auditoria RN-002 + render JSON do portal (precedente consultar-cnpj). Sem fachada: o controller só orquestra via service e comunica a degradação.
Output: controller + 3 FormRequests + rotas públicas (page + endpoints) + shouldRenderJsonWhen + feature test cobrindo HU-054/055/056, toggle, throttle e auditoria anônima.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-consulta-previa-viabilidade/07-CONTEXT.md
@.planning/phases/07-consulta-previa-viabilidade/07-01-SUMMARY.md
@.planning/phases/07-consulta-previa-viabilidade/07-05-SUMMARY.md
@docs/superpowers/specs/2026-06-14-consulta-previa-viabilidade-design.md

# PADRÃO A ESPELHAR (endpoint público JSON + auditoria + toggle + throttle + render JSON + página) — ler antes de codar:
@app/Http/Controllers/Portal/CnpjLookupController.php
@app/Http/Controllers/Gestao/TerritoryController.php
@app/Http/Requests/Portal/LookupCnpjRequest.php
@routes/portal.php
@routes/web.php
@bootstrap/app.php
@app/Support/Settings.php
@app/Services/Geo/AddressNotFoundException.php
@app/Services/Geo/GeocoderException.php
@app/Services/Viabilidade/ConsultaViabilidadeService.php
@app/Services/Viabilidade/ConsultaViabilidadeResult.php
</context>

<rotas>
A consulta é PÚBLICA: as rotas NÃO entram no grupo `auth:web`. Acrescentar em routes/portal.php (fora dos grupos autenticados, ao estilo do grupo guest do GOV.BR), com prefixo/nome portal.* :
```php
// Consulta prévia de viabilidade (EP07) — PÚBLICA (cidadão anônimo acessa).
// Página sem throttle; endpoints JSON com throttle parametrizado (07-01).
Route::prefix('portal')->name('portal.')->group(function () {
    Route::get('viabilidade', [ConsultaViabilidadeController::class, 'index'])->name('viabilidade.index');

    Route::middleware('throttle:consulta-viabilidade')->group(function () {
        Route::post('viabilidade/endereco', [ConsultaViabilidadeController::class, 'endereco'])->name('viabilidade.endereco');
        Route::post('viabilidade/cnae', [ConsultaViabilidadeController::class, 'cnae'])->name('viabilidade.cnae');
        Route::post('viabilidade/inscricao', [ConsultaViabilidadeController::class, 'inscricao'])->name('viabilidade.inscricao');
    });
});
```
ATENÇÃO de ordenação: declarar ANTES de qualquer grupo que possa colidir; como o histórico autenticado (07-07) usará `viabilidade/historico` (rota literal distinta), não há conflito de binding.
</rotas>

<tasks>

<task type="auto">
  <name>Task 1: controller (index + endereco) + FormRequest endereço + rota + toggle + throttle + JSON render + auditoria</name>
  <read_first>app/Http/Controllers/Portal/CnpjLookupController.php, app/Http/Requests/Portal/LookupCnpjRequest.php, routes/portal.php, bootstrap/app.php, app/Http/Controllers/Gestao/TerritoryController.php</read_first>
  <files>app/Http/Controllers/Portal/ConsultaViabilidadeController.php, app/Http/Requests/Portal/ConsultaViabilidadeEnderecoRequest.php, routes/portal.php, bootstrap/app.php, tests/Feature/Viabilidade/ConsultaViabilidadePublicaTest.php</files>
  <action>
    TDD estrito (escrever os testes do endereço PRIMEIRO; RED→GREEN).

    1. `ConsultaViabilidadeController` (extends Controller), construtor injeta `ConsultaViabilidadeService $service` e `AuditService $audit` (a auditoria detalhada é do service; aqui registramos o bloqueio por toggle como no CnpjLookupController).
       - `index(): Response` → `Inertia::render('portal/viabilidade/consulta', ['consultaEnabled' => Settings::enabled('consulta_viabilidade')])` (página construída no 07-08; aqui só o backend da rota).
       - método privado `guardToggle(): ?JsonResponse` — se `! Settings::enabled('consulta_viabilidade')`: auditar bloqueio (log 'viabilidade', event 'consulta', result 'bloqueado', motivo 'toggle-desativado') e retornar `response()->json(['message' => 'A consulta de viabilidade está temporariamente desativada. Tente novamente mais tarde.'], 422)`. Caso contrário null.
       - `endereco(ConsultaViabilidadeEnderecoRequest $request): JsonResponse`:
         ```php
         if ($bloqueio = $this->guardToggle()) { return $bloqueio; }
         try {
             $result = $this->service->consultarPorEndereco(
                 $request->validated('endereco'),
                 $request->validated('cnae'),
                 $request->validated('area') !== null ? (float) $request->validated('area') : null,
             );
         } catch (AddressNotFoundException) {
             return response()->json(['message' => 'Endereço não localizado. Revise o endereço ou posicione a consulta por CNAE.'], 404);
         } catch (GeocoderException) {
             return response()->json(['message' => 'Serviço de geocodificação indisponível no momento. Tente novamente em instantes.'], 503);
         }
         return response()->json($result->toArray());
         ```
         (a auditoria de sucesso é interna ao service; o controller só trata as exceções honestas.)

    2. `ConsultaViabilidadeEnderecoRequest` (authorize true — público): rules
       - `endereco` => ['required','string','min:3','max:255']
       - `cnae` => ['required','string','max:14'] (formato livre — o service normaliza para dígitos)
       - `area` => ['nullable','numeric','min:0','max:9999999']
       `prepareForValidation`: trim do endereço. Mensagens pt-BR.

    3. `routes/portal.php`: acrescentar o grupo público (ver <rotas>) — por ora só `viabilidade.index` e `viabilidade.endereco` (cnae/inscricao entram nas tasks seguintes, mas pode já declarar as 3 rotas e implementar os métodos nas tasks 2/3; PREFERIR declarar as 3 de uma vez para não reescrever o arquivo).

    4. `bootstrap/app.php`: estender `shouldRenderJsonWhen` para incluir `($request->is('portal/viabilidade/*') && $request->expectsJson())` (ao lado de consultar-cnpj e gestao/territorio/*).

    Casos de teste em `tests/Feature/Viabilidade/ConsultaViabilidadePublicaTest.php` (RefreshDatabase; seeds reais dos Quadros/risco; Geocoder fake bind; SpatialRepository fake sem zona):
    - `test_pagina_de_consulta_e_publica` — GET /portal/viabilidade sem autenticar → 200 (assertInertia component 'portal/viabilidade/consulta' só depois do 07-08; por ora asserir status 200 e que NÃO redireciona para login).
    - `test_consulta_por_endereco_anonima_retorna_resultado_e_e_auditada` (HU-054/CA-01/CA-02) — POST JSON (`->postJson('/portal/viabilidade/endereco', [...])`) sem autenticar; asserir 200, json tem chaves veredito_locacional/risco/enquadramento; Activity log 'viabilidade'/'consulta' gravada (causer null — anônima).
    - `test_endereco_nao_localizado_retorna_mensagem_honesta` — Geocoder fake lança AddressNotFoundException → 404 com message; NUNCA resultado falso.
    - `test_toggle_desligado_bloqueia_antes_de_executar` (HU-014) — gravar Parameter features.consulta_viabilidade=0; POST → 422 com message; Activity result 'bloqueado'.
  </action>
  <verify>php artisan test --compact --filter=ConsultaViabilidadePublicaTest</verify>
  <acceptance_criteria>
    - grep "class ConsultaViabilidadeController" app/Http/Controllers/Portal/ConsultaViabilidadeController.php
    - grep "consultarPorEndereco" app/Http/Controllers/Portal/ConsultaViabilidadeController.php
    - grep "viabilidade.endereco" routes/portal.php
    - grep "throttle:consulta-viabilidade" routes/portal.php
    - grep "portal/viabilidade" bootstrap/app.php
    - Testes do endereço + toggle verdes.
  </acceptance_criteria>
  <done>Página pública + endpoint endereço (com erros honestos), toggle, throttle, render JSON e auditoria anônima.</done>
</task>

<task type="auto">
  <name>Task 2: endpoint CNAE (HU-056) + FormRequest + rota</name>
  <read_first>app/Http/Controllers/Portal/ConsultaViabilidadeController.php, app/Http/Requests/Portal/ConsultaViabilidadeEnderecoRequest.php</read_first>
  <files>app/Http/Controllers/Portal/ConsultaViabilidadeController.php, app/Http/Requests/Portal/ConsultaViabilidadeCnaeRequest.php, routes/portal.php, tests/Feature/Viabilidade/ConsultaViabilidadePublicaTest.php</files>
  <action>
    TDD. Acrescentar:
    - `cnae(ConsultaViabilidadeCnaeRequest $request): JsonResponse` — guardToggle; `$result = $this->service->consultarPorCnae($request->validated('cnae'), $request->validated('area') !== null ? (float) ... : null);` → json($result->toArray()). Sem geocode → sem AddressNotFoundException a tratar.
    - `ConsultaViabilidadeCnaeRequest`: rules cnae ['required','string','max:14'], area ['nullable','numeric','min:0','max:9999999']. authorize true.
    - rota viabilidade.cnae (já declarada na Task 1 se você optou por declarar as 3).

    Casos de teste:
    - `test_consulta_por_cnae_anonima_roda_risco_e_quadro7` (HU-056) — POST /portal/viabilidade/cnae {cnae:'4712-1/00', area:120} → 200; json risco.municipal.status 'classificado', enquadramento.quadro7.status 'identificado', veredito 'pendente', avisos com "não avalia o local".
    - `test_consulta_por_cnae_respeita_toggle` — toggle off → 422.
  </action>
  <verify>php artisan test --compact --filter=ConsultaViabilidadePublicaTest</verify>
  <acceptance_criteria>
    - grep "public function cnae" app/Http/Controllers/Portal/ConsultaViabilidadeController.php
    - grep "viabilidade.cnae" routes/portal.php
    - grep "consultarPorCnae" app/Http/Controllers/Portal/ConsultaViabilidadeController.php
    - Testes do CNAE verdes.
  </acceptance_criteria>
  <done>Endpoint CNAE público com request próprio, delegando ao service; testes verdes.</done>
</task>

<task type="auto">
  <name>Task 3: endpoint inscrição (HU-055, degradado honesto) + FormRequest + rota + throttle/auditoria</name>
  <read_first>app/Http/Controllers/Portal/ConsultaViabilidadeController.php, app/Services/Viabilidade/ConsultaViabilidadeService.php, app/Providers/FortifyServiceProvider.php</read_first>
  <files>app/Http/Controllers/Portal/ConsultaViabilidadeController.php, app/Http/Requests/Portal/ConsultaViabilidadeInscricaoRequest.php, routes/portal.php, tests/Feature/Viabilidade/ConsultaViabilidadePublicaTest.php</files>
  <action>
    TDD. Acrescentar:
    - `inscricao(ConsultaViabilidadeInscricaoRequest $request): JsonResponse` — guardToggle; `$result = $this->service->consultarPorInscricao($request->validated('inscricao'), $request->validated('cnae'), $request->validated('area') !== null ? (float) ... : null);` → json($result->toArray()). O service já degrada (provider indisponível → resultado por CNAE + aviso) — o controller NÃO trata exceção de unavailable (o service captura). Status 200 com o resultado degradado (a degradação é comunicada via `avisos`, não via erro HTTP — a consulta "funcionou", só não resolveu o ponto).
    - `ConsultaViabilidadeInscricaoRequest`: rules inscricao ['required','string','max:60'], cnae ['required','string','max:14'], area ['nullable','numeric','min:0','max:9999999']. authorize true.
    - rota viabilidade.inscricao.

    Casos de teste:
    - `test_consulta_por_inscricao_degrada_com_aviso_e_nunca_inventa_ponto` (HU-055/CA-03) — binding real (UnavailablePropertyRegistryLookup); POST /portal/viabilidade/inscricao {inscricao:'123', cnae:'4712-1/00', area:120} → 200; json.avisos contém "Resolução por inscrição imobiliária indisponível"; json.geocode null; json.territorio null; veredito 'pendente'; risco real presente. (sem ponto inventado).
    - `test_throttle_limita_consultas_publicas` (HU-014) — gravar Parameter seguranca.throttle.consulta_viabilidade.por_minuto=2; disparar 3 POSTs ao endpoint cnae/endereco → o 3º responde 429. (limpar o RateLimiter entre testes se necessário; usar `RateLimiter::clear` no setUp ou rota distinta.)
    - `test_consulta_anonima_nao_grava_historico` — após uma consulta anônima, asserir `ViabilityQuery::count() === 0` (a persistência só-quando-autenticado é do 07-07; aqui garantimos que anônima NÃO grava histórico pessoal, mas FOI auditada).
  </action>
  <verify>php artisan test --compact --filter=ConsultaViabilidadePublicaTest</verify>
  <acceptance_criteria>
    - grep "public function inscricao" app/Http/Controllers/Portal/ConsultaViabilidadeController.php
    - grep "viabilidade.inscricao" routes/portal.php
    - grep "consultarPorInscricao" app/Http/Controllers/Portal/ConsultaViabilidadeController.php
    - Testes inscrição/throttle/anônima verdes.
  </acceptance_criteria>
  <done>Endpoint inscrição degrada honestamente (aviso, sem ponto inventado); throttle parametrizado provado; consulta anônima auditada sem histórico.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=ConsultaViabilidadePublicaTest` — todos verdes.
- `php artisan route:list --path=portal/viabilidade` mostra a página + 3 endpoints (endpoints com throttle:consulta-viabilidade).
- `php artisan test --compact` — suíte completa CONTINUA verde.
</verification>

<success_criteria>
- 3 entradas públicas (endereço/CNAE/inscrição) sob /portal/viabilidade, throttled e auditadas.
- Erros honestos (endereço não localizado/serviço indisponível/toggle off); inscrição degrada com aviso (sem ponto inventado).
- Consulta anônima auditada (RN-002) sem histórico pessoal; render JSON do portal estendido.
</success_criteria>

<output>
Criar `.planning/phases/07-consulta-previa-viabilidade/07-06-SUMMARY.md`: rotas e nomes exatos (portal.viabilidade.index/endereco/cnae/inscricao), shape das requests, códigos HTTP de cada caminho (200/404/503/422/429) e a decisão de degradação por inscrição via avisos. Insumo direto de 07-07 (persistência quando autenticado, mesmos endpoints) e 07-08 (UI consome os endpoints).
</output>

---
phase: 07-consulta-previa-viabilidade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Realty/PropertyRegistryLookup.php
  - app/Services/Realty/PropertyRegistryResult.php
  - app/Services/Realty/PropertyRegistryUnavailableException.php
  - app/Services/Realty/PropertyNotFoundException.php
  - app/Services/Realty/UnavailablePropertyRegistryLookup.php
  - app/Providers/AppServiceProvider.php
  - tests/Feature/Viabilidade/PropertyRegistryLookupTest.php
autonomous: true
requirements: [HU-055]

must_haves:
  truths:
    - "Existe o contrato PropertyRegistryLookup::resolve(inscricao): PropertyRegistryResult, espelhando o padrão de CnpjLookup/Geocoder"
    - "O provider real (UnavailablePropertyRegistryLookup) SEMPRE lança PropertyRegistryUnavailableException — a base de lotes/Cadastro está pendente SEDUR; NUNCA inventa um ponto"
    - "O binding está registrado no AppServiceProvider e a Fase 13 troca SÓ o binding pelo provider conveniado (Cadastro/SEFAZ), sem tocar call sites"
    - "Um fake implementando o contrato resolve uma inscrição em coordenada — prova que a lógica roda de ponta a ponta quando a base chegar (sem fachada)"
  artifacts:
    - path: "app/Services/Realty/PropertyRegistryLookup.php"
      provides: "Interface do contrato de resolução de inscrição imobiliária → coordenada"
      contains: "interface PropertyRegistryLookup"
    - path: "app/Services/Realty/UnavailablePropertyRegistryLookup.php"
      provides: "Provider indisponível (base pendente SEDUR) — sempre lança unavailable, nunca inventa ponto"
      contains: "class UnavailablePropertyRegistryLookup"
    - path: "app/Services/Realty/PropertyRegistryResult.php"
      provides: "DTO readonly {latitude, longitude, inscricao, source, raw} com toArray snake_case"
      contains: "final readonly class PropertyRegistryResult"
  key_links:
    - from: "app/Providers/AppServiceProvider.php"
      to: "App\\Services\\Realty\\PropertyRegistryLookup"
      via: "bind do contrato no provider indisponível (trocado na Fase 13)"
      pattern: "PropertyRegistryLookup::class"
    - from: "app/Services/Realty/UnavailablePropertyRegistryLookup.php"
      to: "App\\Services\\Realty\\PropertyRegistryUnavailableException"
      via: "resolve() sempre lança unavailable (degradação honesta)"
      pattern: "throw new PropertyRegistryUnavailableException"
---

<objective>
Construir o **contrato de resolução por inscrição imobiliária** (HU-055), espelhando o padrão de `CnpjLookup`/`Geocoder`: interface + DTO readonly + exceções + provider INDISPONÍVEL + binding. A resolução do ponto pela inscrição depende da base de lotes/Cadastro Multifinalitário — BLOQUEADA pendente SEDUR (HU-033/HU-106). Aqui o provider real degrada honestamente; o contrato fica pronto para a Fase 13 trocar só o binding.

Purpose: HU-055 é o único bloqueio formal novo da fase. A regra do projeto (entrega-funcional / agentes-sile): integração indisponível = feature explicitamente bloqueada atrás de contrato, NUNCA adaptador falso e NUNCA ponto inventado. O teste com fake prova que a lógica roda quando a base chegar — sem fachada, mas pronta.
Output: `app/Services/Realty/` com interface, DTO, 2 exceções, provider indisponível e binding no AppServiceProvider; teste do contrato (fake resolve; real lança unavailable).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-consulta-previa-viabilidade/07-CONTEXT.md
@docs/superpowers/specs/2026-06-14-consulta-previa-viabilidade-design.md

# PADRÃO A ESPELHAR (contrato + DTO + exceções + provider + binding) — ler antes de codar:
@app/Services/Cnpj/CnpjLookup.php
@app/Services/Cnpj/CnpjData.php
@app/Services/Cnpj/CnpjLookupException.php
@app/Services/Cnpj/CnpjNotFoundException.php
@app/Services/Cnpj/BrasilApiCnpjLookup.php
@app/Services/Geo/Geocoder.php
@app/Services/Geo/GeocodeResult.php
@app/Providers/AppServiceProvider.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: interface PropertyRegistryLookup + DTO PropertyRegistryResult + exceções</name>
  <read_first>app/Services/Cnpj/CnpjLookup.php, app/Services/Cnpj/CnpjData.php, app/Services/Cnpj/CnpjLookupException.php, app/Services/Cnpj/CnpjNotFoundException.php, app/Services/Geo/GeocodeResult.php</read_first>
  <files>app/Services/Realty/PropertyRegistryLookup.php, app/Services/Realty/PropertyRegistryResult.php, app/Services/Realty/PropertyRegistryUnavailableException.php, app/Services/Realty/PropertyNotFoundException.php</files>
  <action>
    Espelhar o contrato de CnpjLookup. Namespace `App\Services\Realty`.

    1. `PropertyRegistryLookup` (interface):
       ```php
       interface PropertyRegistryLookup
       {
           /**
            * Resolve uma inscrição imobiliária em coordenada do imóvel (base de
            * lotes / Cadastro Multifinalitário). O provider concreto é trocável por
            * binding — a Fase 13 (HU-106) liga a base oficial sem tocar call sites.
            *
            * @throws PropertyNotFoundException quando a inscrição não existe na base
            * @throws PropertyRegistryUnavailableException quando a base está indisponível/pendente
            */
           public function resolve(string $inscricao): PropertyRegistryResult;
       }
       ```

    2. `PropertyRegistryResult` (final readonly, espelha GeocodeResult): campos
       - `float $latitude`, `float $longitude`, `string $inscricao`, `?string $source = null`, `array $raw = []` (payload bruto da base, p/ auditoria).
       - phpdoc da ordem [lat,lng] explícita (igual GeocodeResult — Pitfall de ordem de coordenadas).
       - `toArray(): array` snake_case: `['latitude'=>, 'longitude'=>, 'inscricao'=>, 'source'=>, 'raw'=>]`.

    3. `PropertyRegistryUnavailableException` (espelha CnpjLookupException): construtor `public function __construct(public readonly string $inscricao, public readonly ?string $motivo = null)`, mensagem pt-BR padrão 'Resolução por inscrição imobiliária indisponível: a base de lotes (Cadastro) está pendente da SEDUR.'

    4. `PropertyNotFoundException` (espelha CnpjNotFoundException): construtor `public function __construct(public readonly string $inscricao)`, mensagem 'Inscrição imobiliária {$inscricao} não encontrada na base de lotes.'
  </action>
  <verify>vendor/bin/pint --dirty --format agent</verify>
  <acceptance_criteria>
    - grep "interface PropertyRegistryLookup" app/Services/Realty/PropertyRegistryLookup.php
    - grep "final readonly class PropertyRegistryResult" app/Services/Realty/PropertyRegistryResult.php
    - grep "public function resolve(string \$inscricao): PropertyRegistryResult" app/Services/Realty/PropertyRegistryLookup.php
    - grep "class PropertyRegistryUnavailableException" app/Services/Realty/PropertyRegistryUnavailableException.php
  </acceptance_criteria>
  <done>Contrato + DTO + 2 exceções no namespace App\Services\Realty, espelhando CnpjLookup/Geocoder.</done>
</task>

<task type="auto">
  <name>Task 2: provider UnavailablePropertyRegistryLookup + binding + teste do contrato (fake prova a lógica)</name>
  <read_first>app/Providers/AppServiceProvider.php, app/Services/Cnpj/BrasilApiCnpjLookup.php</read_first>
  <files>app/Services/Realty/UnavailablePropertyRegistryLookup.php, app/Providers/AppServiceProvider.php, tests/Feature/Viabilidade/PropertyRegistryLookupTest.php</files>
  <action>
    TDD estrito (RED → verificar RED → GREEN → verificar GREEN).

    1. Escrever PRIMEIRO `tests/Feature/Viabilidade/PropertyRegistryLookupTest.php`:
       - `test_provider_real_esta_indisponivel_e_nunca_inventa_ponto`: resolver `app(PropertyRegistryLookup::class)` (binding real) e asserir que `resolve('123456')` lança `PropertyRegistryUnavailableException` (use `$this->expectException(...)`). Asserir também que a instância resolvida é `UnavailablePropertyRegistryLookup` (binding correto).
       - `test_contrato_resolve_inscricao_em_coordenada_com_provider_disponivel`: definir um fake anônimo `implements PropertyRegistryLookup` cujo `resolve()` devolve `new PropertyRegistryResult(latitude: -12.97, longitude: -38.50, inscricao: $inscricao, source: 'fake', raw: ['lote' => 'X'])`. Chamar `resolve('999')` e asserir latitude/longitude/inscricao/toArray() — PROVA que a lógica roda de ponta a ponta quando a base chegar (anti-fachada: o contrato é real, só o provider está indisponível).
       - `test_excecao_de_inexistencia_existe_no_contrato` (opcional, leve): asserir que `PropertyNotFoundException` é instanciável com a inscrição.

    2. `app/Services/Realty/UnavailablePropertyRegistryLookup.php`:
       ```php
       class UnavailablePropertyRegistryLookup implements PropertyRegistryLookup
       {
           public function resolve(string $inscricao): PropertyRegistryResult
           {
               // Base de lotes / Cadastro Multifinalitário pendente SEDUR
               // (HU-033/HU-106). Sem fonte oficial, NUNCA resolvemos um ponto —
               // degradação honesta. A Fase 13 troca este binding pelo provider real.
               throw new PropertyRegistryUnavailableException($inscricao);
           }
       }
       ```

    3. `app/Providers/AppServiceProvider.php` — no register(), ao lado dos binds de CnpjLookup/Geocoder, acrescentar:
       ```php
       // Resolução por inscrição imobiliária (HU-055) atrás de contrato. A base
       // de lotes/Cadastro está PENDENTE SEDUR — o provider degrada honestamente
       // (nunca inventa ponto). A Fase 13 (HU-106) troca SÓ este binding.
       $this->app->bind(PropertyRegistryLookup::class, UnavailablePropertyRegistryLookup::class);
       ```
       (incluir os `use App\Services\Realty\PropertyRegistryLookup;` e `use App\Services\Realty\UnavailablePropertyRegistryLookup;`)
  </action>
  <verify>php artisan test --compact --filter=PropertyRegistryLookupTest</verify>
  <acceptance_criteria>
    - grep "class UnavailablePropertyRegistryLookup implements PropertyRegistryLookup" app/Services/Realty/UnavailablePropertyRegistryLookup.php
    - grep "throw new PropertyRegistryUnavailableException" app/Services/Realty/UnavailablePropertyRegistryLookup.php
    - grep "bind(PropertyRegistryLookup::class, UnavailablePropertyRegistryLookup::class)" app/Providers/AppServiceProvider.php
    - PropertyRegistryLookupTest verde: provider real lança unavailable; fake resolve coordenada.
  </acceptance_criteria>
  <done>Provider indisponível lança unavailable (nunca inventa ponto); binding registrado; teste com fake prova a lógica de ponta a ponta.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=PropertyRegistryLookupTest` verde.
- `php artisan test --compact` — suíte completa CONTINUA verde (o binding novo não toca nenhum call site existente).
- Evidência anti-fachada: o provider real lança unavailable (zero ponto inventado) e o fake prova que o contrato resolve quando a base existir.
</verification>

<success_criteria>
- Contrato PropertyRegistryLookup + DTO + exceções + provider indisponível + binding, espelhando CnpjLookup/Geocoder.
- HU-055 fica explicitamente BLOQUEADA na resolução do ponto, atrás de contrato — nunca simulada.
- Teste com fake prova que a lógica roda quando a base oficial chegar (troca só de binding na Fase 13).
</success_criteria>

<output>
Criar `.planning/phases/07-consulta-previa-viabilidade/07-02-SUMMARY.md`: assinatura final de `resolve()`, shape do PropertyRegistryResult, nome das exceções e a decisão do binding (UnavailablePropertyRegistryLookup; trocado na Fase 13). Insumo direto de 07-05 (service consome o contrato na entrada por inscrição) e 07-06 (endpoint).
</output>

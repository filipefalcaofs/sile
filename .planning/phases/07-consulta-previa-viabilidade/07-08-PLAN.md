---
phase: 07-consulta-previa-viabilidade
plan: 08
type: execute
wave: 4
depends_on: ["07-06"]
files_modified:
  - resources/js/pages/portal/viabilidade/consulta.tsx
  - resources/js/components/viabilidade/resultado-viabilidade.tsx
  - resources/js/pages/home.tsx
  - tests/Feature/Viabilidade/ConsultaViabilidadePaginaTest.php
autonomous: true
requirements: [HU-054, HU-055, HU-056, HU-057, HU-058, HU-059]

must_haves:
  truths:
    - "A página pública /portal/viabilidade permite consultar por endereço, CNAE ou inscrição (3 entradas) sem login, consumindo os endpoints reais via useHttp"
    - "Para endereço/inscrição com ponto, o resultado mostra o imóvel no mapa Leaflet reusado (MapaSection/MapImovel da Fase 4)"
    - "O resultado mostra risco (badges municipal/sanitário + encaminhamento — HU-058), enquadramento Quadro 7 (HU-057), restrições/bairro/via (HU-059) e o veredito locacional"
    - "O veredito locacional pendente aparece COM o motivo (zona pendente SEDUR) — NUNCA Permitido/Não permitido sem zona (sem fachada na UI)"
    - "Avisos honestos (consulta por CNAE não avalia o local; inscrição indisponível) são exibidos; toggle desligado degrada com mensagem"
  artifacts:
    - path: "resources/js/pages/portal/viabilidade/consulta.tsx"
      provides: "Página pública de consulta (3 entradas + mapa + resultado), portal light standalone"
      contains: "viabilidade"
    - path: "resources/js/components/viabilidade/resultado-viabilidade.tsx"
      provides: "Componente reutilizável que renderiza o ConsultaViabilidadeResult (risco/enquadramento/restrições/veredito/avisos)"
      contains: "ResultadoViabilidade"
  key_links:
    - from: "resources/js/pages/portal/viabilidade/consulta.tsx"
      to: "/portal/viabilidade/{endereco,cnae,inscricao}"
      via: "useHttp POST consumindo os endpoints reais (sem simular no front)"
      pattern: "useHttp"
    - from: "resources/js/pages/portal/viabilidade/consulta.tsx"
      to: "@/components/geo/mapa-section"
      via: "reuso do mapa Leaflet da Fase 4 (SSR-safe)"
      pattern: "MapaSection"
---

<objective>
Construir a **página pública de consulta** (portal light) com as 3 entradas honestas, reusando o mapa Leaflet da Fase 4 e renderizando o resultado real dos motores — risco, enquadramento, restrições e o veredito locacional (pendente COM motivo quando sem zona). Nenhum resultado é simulado no front: tudo vem dos endpoints (useHttp).

Purpose: critério 1 do ROADMAP + as views HU-057/058/059. A UI deve ser HONESTA: nunca exibe Permitido/Não permitido sem zona; comunica avisos (CNAE não avalia local; inscrição indisponível) e a degradação do toggle. Acessível e mobile-first (portal do cidadão — obrigação legal eMAG/WCAG).
Output: página `portal/viabilidade/consulta.tsx` (standalone público, padrão home.tsx) + componente `ResultadoViabilidade` reutilizável (também usado no histórico 07-09) + link da landing; feature test do componente da página.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-consulta-previa-viabilidade/07-CONTEXT.md
@.planning/phases/07-consulta-previa-viabilidade/07-04-SUMMARY.md
@.planning/phases/07-consulta-previa-viabilidade/07-06-SUMMARY.md

# PADRÕES DE UI A REUSAR (mapa Fase 4; portal light; useHttp; resultado territorial honesto) — ler antes de codar:
@resources/js/pages/gestao/territorio/index.tsx
@resources/js/components/geo/mapa-section.tsx
@resources/js/components/geo/map-imovel.tsx
@resources/js/pages/portal/empresas/cadastrar.tsx
@resources/js/pages/home.tsx
@resources/js/components/ui/badge.tsx
@resources/js/components/ui/alert.tsx
@resources/js/components/ui/card.tsx
@resources/js/components/form/input.tsx
@resources/js/components/ui/button.tsx
</context>

<skill>
ATIVAR a skill `inertia-react-development` (Inertia v3 + React 19): useHttp para os endpoints JSON, onSuccess/onError/onHttpException, transform() síncrono (precedente do território). Páginas em resources/js/pages. Mapa é SSR-safe via MapaSection (lazy/mounted — Pitfall 9 do Leaflet).
</skill>

<contrato_json>
O JSON dos endpoints é o ConsultaViabilidadeResult::toArray() (ver 07-04-SUMMARY). Tipar no front (interface ResultadoConsulta) com as chaves: entrada, geocode (nullable {latitude, longitude, display_name, ...}), territorio (nullable; bairro/via/zona/lote/restricoes — cada dimensão {status, nome, motivo, ...}), enquadramento ({quadro7, quadro10, quadro11, quadro11a, consolidado, versoes}), risco ({municipal, sanitario, encaminhamento, fundamentacao, versoes}), restricoes (atalho), veredito_locacional ({resultado, label, motivo}), fundamentacao (string[]), avisos (string[]), versoes. Reusar os tipos de Dimensao/Restricoes do gestao/territorio/index.tsx como referência.
</contrato_json>

<tasks>

<task type="auto">
  <name>Task 1: componente ResultadoViabilidade (risco + enquadramento Q7 + restrições/bairro/via + veredito honesto + avisos)</name>
  <read_first>resources/js/pages/gestao/territorio/index.tsx, resources/js/components/ui/badge.tsx, resources/js/components/ui/alert.tsx, resources/js/components/ui/card.tsx</read_first>
  <files>resources/js/components/viabilidade/resultado-viabilidade.tsx</files>
  <action>
    Componente reutilizável `ResultadoViabilidade` (export nomeado) recebendo `{ result: ResultadoConsulta }` e renderizando, com os componentes do design system (Card, Badge, Alert):
    - VEREDITO LOCACIONAL (destaque): label + cor por resultado — `permitido` (success), `permitido_com_condicoes` (warning), `nao_permitido` (error), `pendente` (warning/info). Quando `pendente`, exibir o `motivo` SEMPRE (ex.: "Pendente de análise técnica — Permissão por zona pendente da base oficial (SEDUR)"). NUNCA renderizar Permitido/Não permitido se `resultado === 'pendente'`.
    - RISCO (HU-058): badges para `risco.municipal.nivel_label` (ou "Não classificado" quando status nao_classificado) e `risco.sanitario` (nivel_final), e o `risco.encaminhamento.fluxo` (expresso/analise) com `risco.encaminhamento.motivo`. Sem inventar nível (status nao_classificado → "Não classificado / segue para análise").
    - ENQUADRAMENTO (HU-057): Quadro 7 — grupo/subgrupo + faixa de área quando `enquadramento.quadro7.status === 'identificado'`; quando nao_encontrado, exibir "Sem enquadramento no Quadro 7 — segue para análise" com o motivo.
    - RESTRIÇÕES / TERRITÓRIO (HU-059): bairro, via e restrições do `territorio` (quando presente), reusando a lógica de status do gestao/territorio/index.tsx (identificado/nao_encontrado/indisponivel com Badge "Indisponível" + motivo). Zona/lote SEMPRE "Indisponível — pendente SEDUR" (nunca valor falso).
    - FUNDAMENTAÇÃO: lista `fundamentacao` (referências legais reais).
    - AVISOS: renderizar cada `avisos[]` como Alert variant="info"/"warning".
    - Estado vazio: quando `result` é null, o componente não é renderizado (o pai controla).
    Acessibilidade: headings semânticos, contraste, aria onde aplicável; mobile-first.
  </action>
  <verify>npx tsc --noEmit 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - grep "export function ResultadoViabilidade" resources/js/components/viabilidade/resultado-viabilidade.tsx
    - grep -i "pendente" resources/js/components/viabilidade/resultado-viabilidade.tsx (veredito pendente tratado com motivo)
    - O componente NÃO contém texto fixo "Permitido" exibido incondicionalmente — só sob `resultado === 'permitido'`.
    - typecheck sem erros novos no arquivo.
  </acceptance_criteria>
  <done>Componente honesto renderiza risco/enquadramento/restrições/veredito(pendente com motivo)/fundamentação/avisos; reutilizável pelo histórico.</done>
</task>

<task type="auto">
  <name>Task 2: página pública consulta.tsx (3 entradas + mapa reusado + useHttp) + link da landing</name>
  <read_first>resources/js/pages/home.tsx, resources/js/pages/gestao/territorio/index.tsx, resources/js/pages/portal/empresas/cadastrar.tsx, resources/js/components/geo/mapa-section.tsx</read_first>
  <files>resources/js/pages/portal/viabilidade/consulta.tsx, resources/js/pages/home.tsx</files>
  <action>
    Página `portal/viabilidade/consulta.tsx` — STANDALONE público (padrão home.tsx: header próprio com Logo + AccessibilityBar + ThemeProvider; NÃO usar PortalLayout, que é autenticado). Props: `{ consultaEnabled: boolean }` (do controller 07-06).

    Estrutura:
    - Seletor de tipo de entrada (3 abas/segmented: Endereço | CNAE | Inscrição imobiliária).
    - Form de ENDEREÇO: input endereço + input CNAE + input área (m², opcional) + botão Consultar. useHttp POST /portal/viabilidade/endereco.
    - Form de CNAE: input CNAE + input área (opcional) + botão. useHttp POST /portal/viabilidade/cnae.
    - Form de INSCRIÇÃO: input inscrição + input CNAE + input área + botão. useHttp POST /portal/viabilidade/inscricao. Exibir nota de que a resolução por inscrição pode estar indisponível (a resposta trará o aviso real).
    - Ao obter `result`: renderizar `<ResultadoViabilidade result={result} />`. Quando `result.geocode` (ou territorio com ponto) existir, exibir `<MapaSection lat={geocode.latitude} lng={geocode.longitude} draggable={false} />` (mapa só de exibição — a consulta pública não reposiciona).
    - Tratar erros via onError/onHttpException com mensagem honesta (mensagemDoErro/mensagemDaExcecao — reaproveitar o padrão do gestao/territorio/index.tsx): endereço não localizado (404), serviço indisponível (503), throttle (429 → "Muitas consultas; aguarde um instante"), toggle off (422 → mensagem do backend).
    - Quando `consultaEnabled === false`: desabilitar os forms e mostrar Alert "Consulta temporariamente desativada" (degradação comunicada).
    - CTA discreto: link "Entrar para salvar o histórico das suas consultas" → /portal/login (HU-060 é autenticada).
    - Mobile-first, acessível.

    `home.tsx`: apontar o card "Consulta de viabilidade" (ou criar um CTA no hero) para `/portal/viabilidade` (Link). Edição mínima — só o href do serviço de consulta de viabilidade.
  </action>
  <verify>npx tsc --noEmit 2>&1 | tail -20 && npm run build 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - grep "useHttp" resources/js/pages/portal/viabilidade/consulta.tsx
    - grep "/portal/viabilidade/endereco" resources/js/pages/portal/viabilidade/consulta.tsx
    - grep "/portal/viabilidade/inscricao" resources/js/pages/portal/viabilidade/consulta.tsx
    - grep "MapaSection" resources/js/pages/portal/viabilidade/consulta.tsx
    - grep "ResultadoViabilidade" resources/js/pages/portal/viabilidade/consulta.tsx
    - grep "/portal/viabilidade" resources/js/pages/home.tsx
    - typecheck e build verdes.
  </acceptance_criteria>
  <done>Página pública com 3 entradas via useHttp, mapa reusado para o ponto, resultado honesto, erros/throttle/toggle tratados; link da landing.</done>
</task>

<task type="auto">
  <name>Task 3: feature test da página pública (Inertia render) + verificação visual</name>
  <read_first>tests/Feature/Viabilidade/ConsultaViabilidadePublicaTest.php, app/Http/Controllers/Portal/ConsultaViabilidadeController.php</read_first>
  <files>tests/Feature/Viabilidade/ConsultaViabilidadePaginaTest.php</files>
  <action>
    TDD. Agora que a página existe em disco, o teste pode asserir o componente Inertia (o que não era possível no 07-06).

    `tests/Feature/Viabilidade/ConsultaViabilidadePaginaTest.php`:
    - `test_pagina_publica_renderiza_componente_de_consulta` — `$this->get('/portal/viabilidade')` sem autenticar → `assertInertia(fn (AssertableInertia $page) => $page->component('portal/viabilidade/consulta')->has('consultaEnabled'))`.
    - `test_pagina_reflete_toggle_desligado` — gravar Parameter features.consulta_viabilidade=0; GET → prop `consultaEnabled` false.
    (usar `use Inertia\Testing\AssertableInertia;`)

    Verificação visual (manual, registrar no SUMMARY): rodar `composer dev`, abrir /portal/viabilidade, consultar um endereço de Salvador e conferir: mapa renderiza o ponto; risco/enquadramento aparecem reais; veredito = "Pendente de análise técnica" com motivo de zona (sem zona oficial). Conferir mobile (375px) e dark mode.
  </action>
  <verify>php artisan test --compact --filter=ConsultaViabilidadePaginaTest</verify>
  <acceptance_criteria>
    - grep "component('portal/viabilidade/consulta')" tests/Feature/Viabilidade/ConsultaViabilidadePaginaTest.php
    - ConsultaViabilidadePaginaTest verde.
  </acceptance_criteria>
  <done>Teste assegura o render do componente público e o reflexo do toggle; verificação visual registrada.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` e `npm run build` verdes.
- `php artisan test --compact --filter="ConsultaViabilidadePaginaTest"` verde.
- `php artisan test --compact` — suíte completa CONTINUA verde.
- Verificação visual (composer dev): consulta por endereço com mapa + resultado honesto; veredito pendente com motivo; mobile + dark mode.
</verification>

<success_criteria>
- Página pública com 3 entradas reais (sem simular no front), mapa Leaflet reusado, resultado com risco/enquadramento/restrições/veredito.
- Veredito pendente sempre com motivo; nunca Permitido/Não permitido sem zona; avisos e toggle comunicados.
- Componente ResultadoViabilidade reutilizável pelo histórico (07-09).
</success_criteria>

<output>
Criar `.planning/phases/07-consulta-previa-viabilidade/07-08-SUMMARY.md`: nome/caminho da página e do componente reutilizável (ResultadoViabilidade), o contrato TS do resultado, e a decisão de layout standalone público. Insumo direto de 07-09 (reusa o componente) e do smoke do 07-10.
</output>

---
phase: 02-administracao-base
plan: 07
type: execute
wave: 5
depends_on: ["02-01", "02-02", "02-06"]
files_modified:
  - app/Http/Controllers/Gestao/ParameterController.php
  - app/Http/Requests/Gestao/UpdateParameterRequest.php
  - routes/gestao.php
  - app/Http/Controllers/Portal/ProcurationController.php
  - resources/js/pages/portal/procuracoes/index.tsx
  - resources/js/pages/gestao/parametros/index.tsx
  - resources/js/pages/gestao/parametros/historico.tsx
  - tests/Feature/Parameters/ManageParametersTest.php
  - tests/Feature/Parameters/ParameterEffectTest.php
  - tests/Feature/Parameters/ParameterHistoryTest.php
autonomous: true
requirements: ["HU-014"]

must_haves:
  truths:
    - "Administrador altera parâmetros por interface com validação do catálogo; valor inválido é rejeitado na gravação (CA-01, CA-03/RN-007)"
    - "Alteração de parâmetro tem efeito na aplicação sem novo deploy — comprovado em call site real da Fase 1 (CA-05/RN-006)"
    - "Toggle features.procuracoes desligado degrada o módulo de procurações de forma controlada e comunicada — novo vínculo bloqueado com aviso pt-BR, revogação preservada, nunca falha silenciosa (CA-06/RN-005/RN-011)"
    - "Histórico de alterações exibe valor anterior, valor novo, responsável e data/hora; parâmetro sensível aparece mascarado como [criptografado] (CA-07/RN-008/RN-009)"
    - "Parâmetro sensível nunca é devolvido em claro pela UI após gravado (RN-009)"
    - "Quem não tem manter-parametros recebe 403 auditado (CA-04)"
  artifacts:
    - path: "app/Http/Controllers/Gestao/ParameterController.php"
      provides: "index agrupado por group, update com validação dinâmica do catálogo, history paginado"
    - path: "app/Http/Requests/Gestao/UpdateParameterRequest.php"
      provides: "Validação dinâmica via validation_rules do próprio parâmetro (RN-007)"
    - path: "resources/js/pages/gestao/parametros/index.tsx"
      provides: "Tela agrupada com campo por tipo, sensível mascarado e link de histórico"
    - path: "app/Http/Controllers/Portal/ProcurationController.php"
      provides: "Degradação controlada do módulo de procurações via Settings::enabled (CA-06)"
  key_links:
    - from: "app/Http/Requests/Gestao/UpdateParameterRequest.php"
      to: "parameters.validation_rules"
      via: "Validator dinâmico com as regras do catálogo do próprio registro"
      pattern: "validation_rules"
    - from: "app/Http/Controllers/Gestao/ParameterController.php"
      to: "app/Support/Audit/AuditService.php"
      via: "auditoria explícita parametro-alterado com mascaramento de sensível"
      pattern: "\\[criptografado\\]"
    - from: "app/Http/Controllers/Portal/ProcurationController.php"
      to: "app/Support/Settings.php"
      via: "Settings::enabled('procuracoes') guardando store/index"
      pattern: "Settings::enabled\\('procuracoes'\\)"
    - from: "resources/js/pages/gestao/parametros/index.tsx"
      to: "/gestao/parametros/{key}/historico"
      via: "link de histórico por parâmetro"
      pattern: "historico"
---

<objective>
Entregar a face administrável da HU-014 sobre o núcleo do 02-02: tela de parâmetros agrupada com validação do catálogo, histórico auditado com mascaramento de sensíveis, e o primeiro feature toggle REAL do sistema (features.procuracoes) provando CA-06 de ponta a ponta — desativação controlada e comunicada de um módulo existente.

Propósito: fecha a HU-014 com os 7 CAs (CA-01..04 padrão + CA-05 efeito sem deploy + CA-06 degradação controlada + CA-07 histórico) — o mecanismo que todas as fases futuras (toggles de integrações, IA, fluxo expresso) reutilizam.
Output: /gestao/parametros navegável; procurações alternáveis por toggle sem deploy.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-administracao-base/02-CONTEXT.md
@.planning/phases/02-administracao-base/02-RESEARCH.md
@docs/SILE_HUs_Completas_MD/EP02-Administracao/HU-014-Manter-parâmetros-do-sistema.md
</context>

<important_notes>
- **Histórico via AuditService EXPLÍCITO, nunca HasAuditoria no Parameter** — `tapActivity` NÃO existe no activitylog v5 e o diff automático vazaria sensível em claro (Pitfall 4 do RESEARCH). Valores de sensível gravados no histórico como o marcador literal `[criptografado]` (anterior E novo).
- **CA-05 testado em call site REAL da Fase 1**: `ui.access_history.per_page` governa a paginação de `/portal/acessos` — alterar via PUT e ver o efeito na request seguinte (exemplo literal no RESEARCH, Code Examples). Nenhum call site é alterado.
- **CA-06 com toggle real**: `features.procuracoes` (decisão do CONTEXT/RESEARCH, Open Question 1 — recomendação aceita). Degradação definida: com toggle OFF, `store` de procuração responde `back()->with('status', ...)` SEM criar vínculo e a tela comunica a desativação e esconde o form de novo vínculo; `index` continua acessível e **revogação (destroy) continua funcionando** (segurança do outorgante prevalece sobre o toggle). Representação ativa existente não é tocada (escopo registrado).
- A UI NUNCA devolve valor sensível: prop `value` vai `null` para sensíveis + flag para a tela mostrar placeholder; gravação de sensível com campo vazio = manter valor atual.
- Binding por key na rota: `{parameter:key}` (keys contêm pontos — funcionam em segmento de rota).
- `sensitive` não é editável pela UI (fora do fillable — decisão do 02-02); o catálogo inicial não tem sensíveis, então os testes de RN-009 usam factory (fake permitido em teste; a UI exercita o caminho real quando a Fase 13 seedar credenciais).
- Wave 5, plano único na wave: fechar com a suíte completa. Os testes de procuração da Fase 1 (tests/Feature/Procuration) DEVEM continuar verdes sem edição — toggle default é ligado (config `features.procuracoes => true`).
</important_notes>

<tasks>

<task type="auto">
  <name>Task 1: Controller de parâmetros com validação dinâmica, mascaramento e auditoria (TDD)</name>
  <files>app/Http/Controllers/Gestao/ParameterController.php, app/Http/Requests/Gestao/UpdateParameterRequest.php, routes/gestao.php, tests/Feature/Parameters/ManageParametersTest.php</files>
  <read_first>
    - app/Models/Parameter.php (typedValue, mutator condicional, fillable — estado pós 02-02)
    - database/seeders/ParameterSeeder.php (catálogo: 10 chaves e suas validation_rules)
    - app/Support/Audit/AuditService.php (assinatura travada)
    - routes/gestao.php (estado pós 02-06 — adicionar rotas de parâmetros sem tocar nas existentes)
    - .planning/phases/02-administracao-base/02-RESEARCH.md (Padrão 4 — auditoria explícita mascarada; Padrão 2 — validação na gravação)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Parameters/ManageParametersTest.php` (`RefreshDatabase`, seed `RolesAndPermissionsSeeder` + `ParameterSeeder`; admin com termo aceito):
    - `test_administrador_ve_parametros_agrupados` (CA-01): GET `/gestao/parametros` → 200, component `gestao/parametros/index`, prop `groups` com chaves `features`, `seguranca`, `ui`; item de `security.login.max_attempts` contém `key`, `type = 'integer'`, `description` pt-BR, `value` (null — não administrado), `default_value = '5'`, `sensitive = false`.
    - `test_atualiza_parametro_valido` (CA-01): PUT `/gestao/parametros/ui.access_history.per_page` `['value' => '5']` → redirect; `Parameter::where('key', 'ui.access_history.per_page')->value('value') === '5'`.
    - `test_valor_invalido_e_rejeitado_pelas_regras_do_catalogo` (CA-03/RN-007): PUT `security.login.max_attempts` `['value' => '0']` (min:1 no catálogo) → `assertSessionHasErrors('value')`; banco inalterado (`value` continua null). Repetir com `['value' => 'abc']` (não inteiro).
    - `test_alteracao_e_auditada_com_valor_anterior_e_novo` (CA-02): PUT per_page para '5' e depois '10' → `Activity::where('log_name', 'parametros')->where('event', 'parametro-alterado')` com properties `key`, `valor_anterior` e `valor_novo` ('5' → '10' na segunda activity), `causer_id` = admin.
    - `test_parametro_sensivel_nunca_e_devolvido_em_claro` (RN-009): `Parameter::factory()->sensitive()->create(['key' => 'integracao.sefaz.token', 'group' => 'integracoes', 'value' => 'segredo-sefaz-001', 'validation_rules' => ['required', 'string']])` → GET `/gestao/parametros` → o JSON da página NÃO contém `segredo-sefaz-001` em lugar algum; o item correspondente tem `value === null` e `sensitive === true`.
    - `test_atualizar_sensivel_grava_criptografado` (RN-009): PUT na chave sensível `['value' => 'novo-token-002']` → `DB::table('parameters')...value('value')` NÃO contém `novo-token-002`; `Parameter::where('key', ...)->first()->value === 'novo-token-002'` (accessor decripta).
    - `test_sensivel_com_campo_vazio_mantem_o_valor_atual`: PUT `['value' => '']` na sensível → valor decriptado continua o anterior; nenhuma activity de alteração registrada.
    - `test_gestor_nao_mantem_parametros` (CA-04): gestor GET `/gestao/parametros` → 403; activity `log_name = 'seguranca'`, `result = 'bloqueado'`.
    Rodar `php artisan test --compact tests/Feature/Parameters` e CONFIRMAR RED.

    **GREEN**:
    1. `php artisan make:request Gestao/UpdateParameterRequest --no-interaction`:
       - rules: `'value' => ['nullable', 'string']` (validação REAL é dinâmica no after).
       - `after()`: resolver `$parameter = $this->route('parameter');` — se `$parameter->sensitive` e `$this->input('value') === '' || $this->input('value') === null` → pular validação dinâmica (manter atual); senão `Validator::make(['value' => $this->input('value')], ['value' => $parameter->validation_rules], [], ['value' => $parameter->description])` e copiar os erros para o validator pai (mensagens pt-BR do laravel-lang com o nome amigável do parâmetro).
    2. `php artisan make:controller Gestao/ParameterController --no-interaction`:
       - `index()`: `Parameter::query()->orderBy('group')->orderBy('key')->get()` → agrupar por `group` (`->groupBy('group')`) transformando cada item: `['key', 'type', 'description', 'sensitive', 'requires_connection_test', 'default_value', 'value' => $p->sensitive ? null : $p->value, 'has_admin_value' => $p->getRawOriginal('value') !== null, 'updated_at']`; render `gestao/parametros/index` com `groups`.
       - `update(UpdateParameterRequest $request, Parameter $parameter)`:
         ```php
         $input = $request->validated('value');

         if ($parameter->sensitive && ($input === null || $input === '')) {
             return back()->with('status', __('Valor mantido.'));
         }

         $old = $parameter->value;
         $parameter->update(['value' => $input]);

         app(AuditService::class)->log(
             logName: 'parametros',
             event: 'parametro-alterado',
             description: "Parâmetro {$parameter->key} alterado",
             properties: [
                 'key' => $parameter->key,
                 'valor_anterior' => $parameter->sensitive ? '[criptografado]' : $old,
                 'valor_novo' => $parameter->sensitive ? '[criptografado]' : $input,
             ],
             subject: $parameter,
         );

         return back()->with('status', __('Parâmetro atualizado com sucesso.'));
         ```
         (o `update` dispara `saved` → cache invalidado pelo booted do model — efeito imediato.)
       - `history(Parameter $parameter)`: implementado no Task 2 (deixar stub fora por enquanto — rota entra no Task 2).
    3. `routes/gestao.php` — DENTRO do grupo existente:
       ```php
       Route::middleware('permission:manter-parametros')->group(function () {
           Route::get('parametros', [ParameterController::class, 'index'])->name('parametros.index');
           Route::put('parametros/{parameter:key}', [ParameterController::class, 'update'])->name('parametros.update');
       });
       ```
    Rodar e CONFIRMAR GREEN.

    **REFACTOR** — `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>php artisan test --compact tests/Feature/Parameters</verify>
  <acceptance_criteria>
    - `php artisan test --compact tests/Feature/Parameters` termina com `"result":"passed"`
    - `app/Http/Requests/Gestao/UpdateParameterRequest.php` contém `validation_rules` (validação dinâmica do catálogo)
    - `app/Http/Controllers/Gestao/ParameterController.php` contém `[criptografado]` e `parametro-alterado`
    - O transform do index zera `value` quando `sensitive` (grep `sensitive ? null` presente)
    - `php artisan route:list --name=parametros` lista index e update
  </acceptance_criteria>
  <done>Parâmetros administráveis com validação do próprio catálogo (RN-007), sensível cifrado e jamais devolvido (RN-009), auditoria com anterior/novo mascarando sensíveis (RN-008) e CA-04 granular.</done>
</task>

<task type="auto">
  <name>Task 2: CA-05 efeito sem deploy + CA-06 toggle real de procurações + CA-07 histórico (TDD)</name>
  <files>app/Http/Controllers/Portal/ProcurationController.php, app/Http/Controllers/Gestao/ParameterController.php, routes/gestao.php, tests/Feature/Parameters/ParameterEffectTest.php, tests/Feature/Parameters/ParameterHistoryTest.php</files>
  <read_first>
    - app/Http/Controllers/Portal/ProcurationController.php (index/store/destroy da Fase 1 — adicionar guarda SEM alterar a lógica existente)
    - tests/Feature/Procuration (testes da Fase 1 que DEVEM continuar verdes sem edição)
    - app/Support/Settings.php (helper enabled criado no 02-02)
    - app/Models/Activity.php (model estendido da Fase 1 — colunas result/rules_version)
    - .planning/phases/02-administracao-base/02-RESEARCH.md (Padrão 5 — degradação controlada; Code Example — teste CA-05)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Parameters/ParameterEffectTest.php` (`RefreshDatabase`, seeds `RolesAndPermissionsSeeder` + `ParameterSeeder` + `LegalTermSeeder` conforme necessário):
    - `test_alteracao_de_parametro_tem_efeito_imediato_sem_deploy` (CA-05 — exemplo literal do RESEARCH): admin PUT `route('gestao.parametros.update', 'ui.access_history.per_page')` `['value' => '5']` → criar 6 AccessLogs do admin → GET `route('portal.acessos')` → prop `logs.data` com exatamente 5 itens (o call site da Fase 1 refletiu a mudança SEM deploy).
    - `test_toggle_desligado_bloqueia_novo_vinculo_com_aviso` (CA-06): cidadão com termo; admin desliga via PUT `route('gestao.parametros.update', 'features.procuracoes')` `['value' => '0']` → cidadão POST `/portal/procuracoes` (payload válido com e-mail de outro usuário) → redirect com `flash.status` (sessão `status`) contendo `'desativad'`; `assertDatabaseCount('procurations', 0)` (nada criado — degradação real, não silenciosa).
    - `test_toggle_desligado_preserva_revogacao` (CA-06 — degradação CONTROLADA): procuração ativa criada antes (factory) → toggle off → DELETE `/portal/procuracoes/{procuration}` pelo outorgante → procuração revogada no banco (segurança prevalece).
    - `test_toggle_desligado_comunica_na_tela`: GET `/portal/procuracoes` → prop `procuracoesEnabled === false`.
    - `test_toggle_ligado_mantem_fluxo_normal` (regressão Fase 1): sem mexer no parâmetro (default '1') → POST cria procuração normalmente; prop `procuracoesEnabled === true`.
    Criar `tests/Feature/Parameters/ParameterHistoryTest.php`:
    - `test_historico_exibe_anterior_novo_responsavel_e_data` (CA-07): admin altera `ui.access_history.per_page` 2x ('5', depois '10') → GET `route('gestao.parametros.historico', 'ui.access_history.per_page')` → 200, component `gestao/parametros/historico`, prop `entries.data` com 2 itens, o mais recente com `valor_anterior = '5'`, `valor_novo = '10'`, `responsavel` = nome do admin, `data` preenchida.
    - `test_historico_de_sensivel_mascara_valores` (CA-07/RN-009): parâmetro sensível via factory (`key => 'integracao.sefaz.token'`) → PUT com `['value' => 'token-secreto-003']` → histórico exibe `valor_anterior` e `valor_novo` iguais a `'[criptografado]'`; e `Activity::where('log_name', 'parametros')->get()` serializada NÃO contém `token-secreto-003`.
    - `test_historico_exige_permissao` (CA-04): gestor GET historico → 403 auditado.
    Rodar e CONFIRMAR RED.

    **GREEN**:
    1. `app/Http/Controllers/Portal/ProcurationController.php` — degradação controlada (CA-06), import `App\Support\Settings`:
       - `index`: adicionar prop `'procuracoesEnabled' => Settings::enabled('procuracoes')` ao render existente (lista continua visível — histórico íntegro).
       - `store`: PRIMEIRA linha:
         ```php
         if (! Settings::enabled('procuracoes')) {
             return back()->with('status', __('A funcionalidade de procurações está temporariamente desativada pelo administrador.'));
         }
         ```
       - `destroy`: SEM guarda (revogação sempre permitida — decisão registrada: segurança do outorgante prevalece).
    2. `app/Http/Controllers/Gestao/ParameterController.php` — método `history(Parameter $parameter)`:
       ```php
       $entries = Activity::query()
           ->where('log_name', 'parametros')
           ->where('subject_type', Parameter::class)
           ->where('subject_id', $parameter->id)
           ->with('causer:id,name')
           ->latest()
           ->paginate(15)
           ->withQueryString()
           ->through(fn (Activity $activity) => [
               'id' => $activity->id,
               'valor_anterior' => $activity->properties['valor_anterior'] ?? null,
               'valor_novo' => $activity->properties['valor_novo'] ?? null,
               'responsavel' => $activity->causer?->name,
               'data' => $activity->created_at->toIso8601String(),
           ]);

       return Inertia::render('gestao/parametros/historico', [
           'parameter' => $parameter->only('key', 'description', 'sensitive'),
           'entries' => $entries,
       ]);
       ```
    3. `routes/gestao.php` — dentro do grupo `permission:manter-parametros` criado no Task 1:
       ```php
       Route::get('parametros/{parameter:key}/historico', [ParameterController::class, 'history'])->name('parametros.historico');
       ```
       ATENÇÃO à ordem: registrar a rota `historico` ANTES de `parametros/{parameter:key}` se houver ambiguidade de match (GET `parametros/x/historico` vs PUT — verbos diferentes, sem conflito; manter mesmo assim a rota mais específica primeiro por clareza).
    Rodar e CONFIRMAR GREEN. Rodar também `php artisan test --compact tests/Feature/Procuration` (regressão da Fase 1 — DEVE passar sem edição).

    **REFACTOR** — `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>php artisan test --compact tests/Feature/Parameters tests/Feature/Procuration</verify>
  <acceptance_criteria>
    - `php artisan test --compact tests/Feature/Parameters` termina com `"result":"passed"`
    - `php artisan test --compact tests/Feature/Procuration` verde com `git diff --stat tests/Feature/Procuration` vazio (regressão sem edição)
    - `app/Http/Controllers/Portal/ProcurationController.php` contém `Settings::enabled('procuracoes')` e `temporariamente desativada` e o `destroy` NÃO contém a guarda
    - O histórico responde em `gestao.parametros.historico` (`php artisan route:list --name=parametros` lista 3 rotas)
    - Nenhuma string de segredo de teste aparece em `activity_log` (provado pelo teste de mascaramento)
  </acceptance_criteria>
  <done>CA-05 provado em call site real sem deploy; CA-06 provado com toggle real degradando procurações de forma comunicada (novo vínculo bloqueado, revogação preservada); CA-07 com histórico completo e sensível mascarado.</done>
</task>

<task type="auto">
  <name>Task 3: Telas de parâmetros (agrupada por domínio) e histórico + aviso na tela de procurações</name>
  <files>resources/js/pages/gestao/parametros/index.tsx, resources/js/pages/gestao/parametros/historico.tsx, resources/js/pages/portal/procuracoes/index.tsx</files>
  <read_first>
    - resources/js/pages/gestao/perfis/index.tsx (padrões de form/cards estabelecidos no 02-06)
    - resources/js/pages/gestao/acessos.tsx (padrão de tabela/paginação para o histórico)
    - resources/js/pages/portal/procuracoes/index.tsx (tela da Fase 1 — adicionar aviso/esconder form SEM reescrever)
    - app/Http/Controllers/Gestao/ParameterController.php (shape exato das props groups/entries)
  </read_first>
  <action>
    1. Criar `resources/js/pages/gestao/parametros/index.tsx` (GestaoLayout):
       - Props: `groups: Record<string, ParameterItem[]>` (item: key, type, description, sensitive, requires_connection_test, default_value, value, has_admin_value, updated_at).
       - Título "Parâmetros do sistema"; seções por grupo com títulos pt-BR mapeados: `seguranca` → "Segurança", `ui` → "Interface", `features` → "Funcionalidades", fallback capitalizado.
       - Cada parâmetro em card/linha: `description` em destaque + `key` em fonte mono pequena; valor padrão exibido ("Padrão: {default_value}"); badge "Administrado" quando `has_admin_value`.
       - Campo por tipo dentro de `<Form action={'/gestao/parametros/' + item.key} method="put">`:
         `boolean` → select "Ativado" (`value='1'`) / "Desativado" (`value='0'`);
         `integer`/`decimal` → `<input type="number">`;
         `text`/`json` → `<textarea>`;
         `string` → `<input type="text">`;
         `sensitive` → `<input type="password">` SEMPRE vazio com placeholder `'••••••'` e legenda "Deixe em branco para manter o valor atual. O valor gravado nunca é exibido.".
       - Valor inicial do campo: `value ?? default_value` (não sensível); botão "Salvar" por parâmetro (desabilitado em `processing`); erros `value` da render prop exibidos no card; sucesso via flash.status do layout.
       - Link "Histórico" por parâmetro → `/gestao/parametros/{key}/historico`.
       - Parâmetros com `requires_connection_test === true`: exibir nota "Teste de conexão disponível quando a integração for configurada (Fase 13)." — SEM botão (sem teste falso).
    2. Criar `resources/js/pages/gestao/parametros/historico.tsx` (GestaoLayout):
       - Props: `parameter` (key, description, sensitive), `entries` (paginator).
       - Título "Histórico — {parameter.description}"; subtítulo com a `key` em mono.
       - Tabela: "Data/hora" (toLocaleString pt-BR), "Responsável", "Valor anterior", "Valor novo" (renderizar `[criptografado]` como badge cinza quando for o marcador); paginação padrão por links; estado vazio "Nenhuma alteração registrada."; link "Voltar para parâmetros" → `/gestao/parametros`.
    3. Editar `resources/js/pages/portal/procuracoes/index.tsx` (mudança MÍNIMA):
       - Nova prop `procuracoesEnabled: boolean`.
       - Quando `false`: renderizar aviso âmbar no topo — "A funcionalidade de procurações está temporariamente desativada pelo administrador. Vínculos existentes permanecem visíveis e revogáveis." — e NÃO renderizar o form de novo vínculo (listas e botões de revogação permanecem).
    Rodar `npm run typecheck && npm run build`.
    (Lógica coberta nos Tasks 1–2; telas verificadas por typecheck/build + smoke humano no 02-08.)
  </action>
  <verify>npm run typecheck && npm run build</verify>
  <acceptance_criteria>
    - `npm run typecheck` e `npm run build` com exit code 0
    - `resources/js/pages/gestao/parametros/index.tsx` contém `Deixe em branco para manter`, `type="password"` e `/historico`
    - `resources/js/pages/gestao/parametros/historico.tsx` contém `[criptografado]` e `Valor anterior`
    - `resources/js/pages/portal/procuracoes/index.tsx` contém `procuracoesEnabled` e `temporariamente desativada`
    - `php artisan test --compact` — suíte completa verde (fechamento da wave 5)
  </acceptance_criteria>
  <done>HU-014 navegável de ponta a ponta: parâmetros agrupados e editáveis com validação, sensíveis mascarados, histórico consultável e o toggle de procurações comunicando a degradação na tela do portal.</done>
</task>

</tasks>

<verification>
1. `php artisan test --compact tests/Feature/Parameters tests/Feature/Procuration` verde (HU-014 + regressão do toggle).
2. `php artisan test --compact` — suíte completa verde (fechamento da wave 5).
3. `npm run typecheck && npm run build` verdes.
4. `vendor/bin/pint --dirty --format agent` sem pendências.
5. `git diff --stat tests/Feature/Procuration tests/Unit/Support/SettingsTest.php` vazio (testes da Fase 1 intocados).
</verification>

<success_criteria>
- HU-014 com os 7 CAs cobertos por testes: CA-01..04 padrão, CA-05 em call site real, CA-06 com toggle real e degradação comunicada, CA-07 com histórico mascarando sensíveis.
- RN-007 (validação do catálogo), RN-009 (cifrado, nunca devolvido), RN-010 (contrato presente, sem teste falso), RN-011 (sem falha silenciosa) atendidas.
- Zero edição em testes/call sites da Fase 1.
</success_criteria>

<output>
Após concluir, criar `.planning/phases/02-administracao-base/02-07-SUMMARY.md` registrando: o toggle real escolhido (features.procuracoes) e o comportamento degradado definido (store bloqueado com aviso, destroy preservado), e a nota de que toggles de integrações/IA/expresso nascem nas fases 9/11/13/14 sobre este mecanismo.
</output>

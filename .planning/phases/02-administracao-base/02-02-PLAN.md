---
phase: 02-administracao-base
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_06_10_120000_create_parameters_table.php
  - app/Models/Parameter.php
  - database/factories/ParameterFactory.php
  - database/seeders/ParameterSeeder.php
  - app/Support/Settings.php
  - config/sile.php
  - app/Providers/AppServiceProvider.php
  - tests/Feature/Parameters/ParameterRegistryTest.php
  - tests/Feature/Parameters/SettingsBackendTest.php
  - tests/Feature/Seeders/ParameterSeederTest.php
autonomous: true
requirements: ["HU-014"]

must_haves:
  truths:
    - "Settings::get resolve na ordem cache → banco (value ?? default do catálogo) → config/sile.php → default do call site, sem alterar NENHUM call site da Fase 1 (promessa da Fase 1 cumprida)"
    - "Sem banco migrado, todo artisan e a suíte Unit continuam funcionando (fallback por QueryException)"
    - "Alterar um Parameter no banco tem efeito na leitura seguinte (invalidação de cache no saved) — núcleo do CA-05"
    - "Parâmetro sensível é gravado criptografado no banco e lido em claro pelo accessor (RN-009)"
    - "Re-seed do catálogo NUNCA sobrescreve o value administrado (apenas metadados)"
    - "A política de senha (Password::defaults) passa a refletir parâmetros administrados no banco"
  artifacts:
    - path: "app/Models/Parameter.php"
      provides: "Registry: typedValue() por tipo, Crypt condicional em value, invalidação de cache no booted"
    - path: "app/Support/Settings.php"
      provides: "Backend banco+cache+fallback mantendo a assinatura get(key, default) + helper enabled(feature)"
    - path: "database/seeders/ParameterSeeder.php"
      provides: "Catálogo inicial de 10 parâmetros (seguranca/ui/features) idempotente que preserva value"
    - path: "database/migrations/2026_06_10_120000_create_parameters_table.php"
      provides: "Tabela parameters (key unique, group, type, value, default_value, validation_rules, sensitive, description, requires_connection_test)"
  key_links:
    - from: "app/Support/Settings.php"
      to: "app/Models/Parameter.php"
      via: "consulta por key dentro de Cache::remember com try/catch QueryException"
      pattern: "QueryException"
    - from: "app/Models/Parameter.php"
      to: "cache sile.parameters.*"
      via: "Cache::forget no saved/deleted (efeito sem deploy)"
      pattern: "Cache::forget"
    - from: "app/Providers/AppServiceProvider.php"
      to: "app/Support/Settings.php"
      via: "Password::defaults lendo security.password.* via Settings::get"
      pattern: "Settings::get\\('security\\.password\\."
---

<objective>
Entregar o núcleo estrutural da HU-014: tabela `parameters` (catálogo como dado), model com cast tipado/criptografia condicional/invalidação de cache, novo backend do `App\Support\Settings` (banco + cache + fallback resiliente) e o catálogo inicial seedado — tudo SEM tocar call sites nem os testes da Fase 1.

Propósito: é a promessa registrada da Fase 1 ([01-01]): trocar o backend do Settings sem tocar call sites. Os planos 02-04..02-07 consomem `Settings::get`/`Settings::enabled` e o catálogo daqui.
Output: registry funcional de ponta a ponta no nível de domínio (a UI administrativa entra no plano 02-07).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-administracao-base/02-CONTEXT.md
@.planning/phases/02-administracao-base/02-RESEARCH.md
@docs/SILE_HUs_Completas_MD/EP02-Administracao/HU-014-Manter-parâmetros-do-sistema.md
</context>

<important_notes>
- **PROIBIDO**: editar `tests/Unit/Support/SettingsTest.php` (regressão da promessa da Fase 1 — deve passar verde sem mudanças), editar as assinaturas de `Settings::get`, ou alterar os call sites existentes (`FortifyServiceProvider` linhas do rebinding/limiter, `AccessHistoryController`s, linha `password_reset_expire` do `AppServiceProvider`).
- A ÚNICA mudança permitida no `AppServiceProvider` é dentro da closure de `Password::defaults()`: trocar a leitura `config('sile.security.password')` por `Settings::get('security.password.*')` chave a chave — isso torna os parâmetros de senha REAIS (administráveis), evitando feature de fachada quando a UI (02-07) os exibir.
- `Settings::get` roda no `boot()` do AppServiceProvider — em build Docker/CI/testes Unit a tabela `parameters` NÃO existe. O try/catch `QueryException → fallback config` é obrigatório (Pitfall 1 do RESEARCH). NUNCA usar `Schema::hasTable` por chamada.
- Nome da tabela é `parameters` (NÃO `settings`) — o repositório já usa "Settings" para configurações pessoais do usuário (`tests/Feature/Settings`, `resources/js/pages/settings`).
- TTL do cache é constante técnica em `config/sile.php` (`parameters.cache_ttl`), não parâmetro do registry (recursão).
- Pitfall 3 do RESEARCH (ordem de atributos no mutator condicional): na factory, o state `sensitive()` deve definir `sensitive` ANTES de `value` no array de estado; produção sempre faz `update()` em registro já carregado.
- Plano roda em paralelo com 02-01 e 02-03 (Wave 1): verificações ESCOPADAS (`tests/Feature/Parameters`, `tests/Feature/Seeders/ParameterSeederTest.php`, `tests/Unit/Support/SettingsTest.php`); suíte completa só no fechamento da wave (orquestrador).
- O nome do arquivo de migration é gerado pelo `php artisan make:migration` com timestamp real — o nome em files_modified é indicativo.
</important_notes>

<tasks>

<task type="auto">
  <name>Task 1: Tabela parameters + model com cast tipado, criptografia condicional e invalidação de cache (TDD)</name>
  <files>database/migrations/2026_06_10_120000_create_parameters_table.php, app/Models/Parameter.php, database/factories/ParameterFactory.php, tests/Feature/Parameters/ParameterRegistryTest.php</files>
  <read_first>
    - .planning/phases/02-administracao-base/02-RESEARCH.md (Padrão 2 — esquema da tabela; Padrão 3 — Crypt condicional e Pitfall 3; Code Examples — typedValue e booted)
    - app/Models/AccessLog.php (convenção do projeto: atributo #[Fillable], método casts())
    - database/factories/AccessLogFactory.php (convenção de factory da Fase 1)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Parameters/ParameterRegistryTest.php` (`php artisan make:test Parameters/ParameterRegistryTest --no-interaction`), com `RefreshDatabase`:
    - `test_typed_value_faz_cast_por_tipo`: criar via factory parâmetros com `type`/`value`: (`integer`,`'15'`)→`15` (int), (`boolean`,`'1'`)→`true`, (`boolean`,`'0'`)→`false`, (`decimal`,`'2.5'`)→`2.5` (float), (`json`,`'{"a":1}'`)→`['a' => 1]`, (`string`,`'texto'`)→`'texto'` — assertar com `assertSame` em `->typedValue()`.
    - `test_typed_value_usa_default_quando_value_e_nulo`: factory com `value => null`, `default_value => '9'`, `type => 'integer'` → `typedValue()` retorna `9`.
    - `test_valor_sensivel_e_criptografado_no_banco`: `Parameter::factory()->sensitive()->create(['value' => 'segredo-001'])` → `DB::table('parameters')->where('id', $p->id)->value('value')` é diferente de `'segredo-001'` e não contém a substring; `$p->fresh()->value === 'segredo-001'` (accessor decripta).
    - `test_valor_nao_sensivel_permanece_em_claro`: factory padrão com `value => 'aberto'` → valor cru no banco é `'aberto'`.
    - `test_gravacao_invalida_o_cache_da_chave`: `Cache::put('sile.parameters.chave.x', 'velho', 60)` → criar/atualizar Parameter com `key => 'chave.x'` → `Cache::has('sile.parameters.chave.x')` é `false` (saved disparou forget); repetir para `delete()`.
    Rodar `php artisan test --compact tests/Feature/Parameters` e CONFIRMAR RED (classe Parameter inexistente).

    **GREEN**:
    1. `php artisan make:model Parameter -mf --no-interaction` (model + migration + factory).
    2. Migration `create_parameters_table`:
       ```php
       Schema::create('parameters', function (Blueprint $table) {
           $table->id();
           $table->string('key')->unique();
           $table->string('group')->index();
           $table->string('type', 20);
           $table->text('value')->nullable();
           $table->text('default_value')->nullable();
           $table->json('validation_rules');
           $table->boolean('sensitive')->default(false);
           $table->string('description');
           $table->boolean('requires_connection_test')->default(false);
           $table->timestamps();
       });
       ```
       (`value` é TEXT — ciphertext é maior que o texto plano; `requires_connection_test` é o contrato do RN-010, sem implementação de teste de conexão nesta fase — sem teste falso.)
    3. `app/Models/Parameter.php`:
       - `#[Fillable(['key', 'group', 'type', 'value', 'default_value', 'validation_rules', 'description', 'requires_connection_test'])]` — `sensitive` FORA do fillable (imutável por interface; definido só em seed/factory — mitigação do Pitfall 3).
       - `casts()`: `['validation_rules' => 'array', 'sensitive' => 'boolean', 'requires_connection_test' => 'boolean']`.
       - Accessor/mutator condicional (exatamente o Padrão 3 do RESEARCH):
         ```php
         protected function value(): Attribute
         {
             return Attribute::make(
                 get: fn (?string $value) => $value !== null && $this->sensitive
                     ? Crypt::decryptString($value)
                     : $value,
                 set: fn (?string $value) => $value !== null && $this->sensitive
                     ? Crypt::encryptString($value)
                     : $value,
             );
         }
         ```
       - `typedValue(): mixed` (exatamente o Code Example do RESEARCH): `$raw = $this->value ?? $this->default_value;` null→null; match em `type`: `integer`→(int), `decimal`→(float), `boolean`→`filter_var($raw, FILTER_VALIDATE_BOOLEAN)`, `json`→`json_decode($raw, true)`, default→string.
       - `booted()`: closure `$forget = fn (self $parameter) => Cache::forget("sile.parameters.{$parameter->key}");` registrada em `static::saved($forget)` e `static::deleted($forget)`.
       - SEM trait `HasAuditoria` (Pitfall 4 — o diff automático vazaria sensível; auditoria explícita entra no 02-07).
    4. `database/factories/ParameterFactory.php`:
       - `definition()`: `key` => `fake()->unique()->lexify('grupo.chave.????')`, `group` => `'geral'`, `type` => `'string'`, `value` => `null`, `default_value` => `fake()->word()`, `validation_rules` => `['required', 'string']`, `sensitive` => `false`, `description` => `fake()->sentence(3)`, `requires_connection_test` => `false`.
       - State `sensitive()`: retornar array com `'sensitive' => true` ANTES de qualquer `value` (ordem das chaves importa para o mutator — Pitfall 3); como `sensitive` está fora do fillable, o state usa `state(fn () => ['sensitive' => true])` e o teste passa `value` no create — confirmar no teste que a criptografia ocorreu (o assert de banco pega regressão de ordem).
       - State `integer(string $default = '10')`: `['type' => 'integer', 'default_value' => $default, 'validation_rules' => ['required', 'integer']]`.
    Rodar e CONFIRMAR GREEN.

    **REFACTOR** — `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>php artisan test --compact tests/Feature/Parameters</verify>
  <acceptance_criteria>
    - `php artisan test --compact tests/Feature/Parameters` termina com `"result":"passed"` (5 testes)
    - `app/Models/Parameter.php` contém `Crypt::encryptString`, `Crypt::decryptString`, `Cache::forget("sile.parameters.` e `typedValue`
    - `app/Models/Parameter.php` NÃO contém `HasAuditoria` e o atributo `#[Fillable]` NÃO contém `sensitive`
    - Migration contém `->unique()` em `key` e `text('value')->nullable()`
    - `vendor/bin/pint --dirty --format agent` sem pendências
  </acceptance_criteria>
  <done>Registry persistente com cast por tipo, criptografia condicional comprovada por assert de banco e invalidação de cache em qualquer gravação — base do CA-05 e do RN-009.</done>
</task>

<task type="auto">
  <name>Task 2: Settings com backend banco+cache+fallback, catálogo seedado e política de senha administrável (TDD)</name>
  <files>app/Support/Settings.php, config/sile.php, app/Providers/AppServiceProvider.php, database/seeders/ParameterSeeder.php, tests/Feature/Parameters/SettingsBackendTest.php, tests/Feature/Seeders/ParameterSeederTest.php</files>
  <read_first>
    - app/Support/Settings.php (assinatura atual — get(key, default) — que NÃO pode mudar)
    - config/sile.php (chaves atuais que viram defaults do catálogo)
    - app/Providers/AppServiceProvider.php (boot: Password::defaults lê config direto; password_reset_expire já usa Settings::get — esta linha NÃO muda)
    - tests/Unit/Support/SettingsTest.php (NÃO EDITAR — precisa continuar verde com o novo backend)
    - .planning/phases/02-administracao-base/02-RESEARCH.md (Padrão 1 — esqueleto exato do novo get; Padrão 2 — seeder que preserva value; Pitfall 2)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Parameters/SettingsBackendTest.php` (`RefreshDatabase`):
    - `test_le_valor_administrado_do_banco`: `Parameter::factory()->integer('5')->create(['key' => 'security.login.max_attempts', 'group' => 'seguranca', 'value' => '7'])` → `Settings::get('security.login.max_attempts')` retorna `7` (int).
    - `test_fallback_para_config_quando_chave_nao_existe_no_banco`: sem linha no banco → `Settings::get('security.login.max_attempts')` retorna `5` (de config/sile.php).
    - `test_fallback_para_default_do_catalogo_quando_value_e_nulo`: linha com `value => null`, `default_value => '12'`, type integer, key `ui.access_history.per_page` → retorna `12`.
    - `test_alteracao_tem_efeito_imediato_na_leitura_seguinte`: criar com value '7' → `Settings::get(...)` 7 → `$parameter->update(['value' => '9'])` → `Settings::get(...)` retorna `9` (invalidação no saved; núcleo do CA-05).
    - `test_enabled_le_toggle_do_grupo_features`: `Parameter::factory()->create(['key' => 'features.procuracoes', 'type' => 'boolean', 'value' => '0', 'default_value' => '1'])` → `Settings::enabled('procuracoes')` é `false`; deletar a linha → `true` (fallback `config('sile.features.procuracoes')`).
    - `test_politica_de_senha_respeita_parametro_do_banco`: criar `security.password.min_length` (integer) com `value => '12'` → `Validator::make(['password' => 'SenhaForte1'], ['password' => Password::defaults()])` FALHA (11 chars < 12) e `'SenhaForteMuito1'` passa.
    Criar `tests/Feature/Seeders/ParameterSeederTest.php` (`RefreshDatabase`):
    - `test_seeder_cria_catalogo_completo`: seed `ParameterSeeder` → `Parameter::count()` é `10`; grupos distintos são `['features', 'seguranca', 'ui']`; `Parameter::where('key', 'security.login.max_attempts')->first()` tem `type === 'integer'`, `default_value === '5'`, `validation_rules === ['required', 'integer', 'min:1', 'max:20']`, `value === null`.
    - `test_seeder_preserva_valor_administrado` (Pitfall 2): seed → `Parameter::where('key', 'ui.access_history.per_page')->first()->update(['value' => '5'])` → re-seed → `value` continua `'5'` e `description` continua a do catálogo.
    - `test_seeder_e_idempotente`: seed 2x → count `10`.
    Rodar `php artisan test --compact tests/Feature/Parameters tests/Feature/Seeders/ParameterSeederTest.php` e CONFIRMAR RED.

    **GREEN**:
    1. `config/sile.php` — acrescentar (preservando TUDO que existe):
       ```php
       'ui' => [
           'access_history' => ['per_page' => 15],
           'cnaes' => ['per_page' => 15],
           'users' => ['per_page' => 15],
       ],
       'features' => [
           'procuracoes' => true,
       ],
       'parameters' => [
           'cache_ttl' => 300,
       ],
       ```
    2. `app/Support/Settings.php` — novo backend mantendo a MESMA assinatura pública (Padrão 1 do RESEARCH, literal):
       ```php
       public static function get(string $key, mixed $default = null): mixed
       {
           $fallback = config("sile.{$key}", $default);

           try {
               return Cache::remember(
                   "sile.parameters.{$key}",
                   (int) config('sile.parameters.cache_ttl', 300),
                   function () use ($key, $fallback) {
                       $parameter = Parameter::query()->where('key', $key)->first();

                       return $parameter === null
                           ? $fallback
                           : ($parameter->typedValue() ?? $fallback);
                   },
               );
           } catch (QueryException) {
               return $fallback;
           }
       }

       public static function enabled(string $feature): bool
       {
           return (bool) static::get("features.{$feature}", false);
       }
       ```
       (imports: `App\Models\Parameter`, `Illuminate\Database\QueryException`, `Illuminate\Support\Facades\Cache`; manter o docblock da classe atualizado: backend banco+cache com fallback config — promessa da Fase 1 cumprida.)
    3. `app/Providers/AppServiceProvider.php` — APENAS dentro da closure de `Password::defaults()`: substituir `$config = config('sile.security.password');` e os acessos por:
       ```php
       $rule = Password::min((int) Settings::get('security.password.min_length', 8));

       if (Settings::get('security.password.require_mixed_case', true)) {
           $rule->mixedCase();
       }

       if (Settings::get('security.password.require_numbers', true)) {
           $rule->numbers();
       }

       if (Settings::get('security.password.require_symbols', false)) {
           $rule->symbols();
       }
       ```
       A linha `config(['auth.passwords.users.expire' => (int) Settings::get('security.password_reset_expire', 60)]);` permanece intocada.
    4. `php artisan make:seeder ParameterSeeder --no-interaction` — catálogo inicial; o array de update do `updateOrCreate` contém SÓ metadados (NUNCA `value` — Pitfall 2):
       ```php
       foreach (self::catalog() as $key => $meta) {
           Parameter::query()->updateOrCreate(['key' => $key], $meta);
       }
       ```
       `catalog()` (método estático privado, retorna array<string, array>) com EXATAMENTE estas 10 entradas — todas com `description` pt-BR:
       | key | group | type | default_value | validation_rules | description |
       |---|---|---|---|---|---|
       | security.password.min_length | seguranca | integer | '8' | ["required","integer","min:6","max:64"] | 'Tamanho mínimo da senha dos usuários' |
       | security.password.require_mixed_case | seguranca | boolean | '1' | ["required","boolean"] | 'Exigir letras maiúsculas e minúsculas na senha' |
       | security.password.require_numbers | seguranca | boolean | '1' | ["required","boolean"] | 'Exigir números na senha' |
       | security.password.require_symbols | seguranca | boolean | '0' | ["required","boolean"] | 'Exigir símbolos na senha' |
       | security.login.max_attempts | seguranca | integer | '5' | ["required","integer","min:1","max:20"] | 'Tentativas de login antes do bloqueio temporário' |
       | security.password_reset_expire | seguranca | integer | '60' | ["required","integer","min:10","max:1440"] | 'Validade em minutos do link de recuperação de senha' |
       | ui.access_history.per_page | ui | integer | '15' | ["required","integer","min:5","max:100"] | 'Itens por página no histórico de acessos' |
       | ui.cnaes.per_page | ui | integer | '15' | ["required","integer","min:5","max:100"] | 'Itens por página na listagem de CNAEs' |
       | ui.users.per_page | ui | integer | '15' | ["required","integer","min:5","max:100"] | 'Itens por página na listagem de usuários' |
       | features.procuracoes | features | boolean | '1' | ["required","boolean"] | 'Habilita o módulo de procurações no portal' |
       (`sensitive` e `requires_connection_test` ficam nos defaults da migration — `false`; nenhum parâmetro sensível no catálogo inicial: credenciais entram na Fase 13 sobre este mecanismo.)
    5. NÃO adicionar o ParameterSeeder ao DatabaseSeeder aqui — isso é responsabilidade do plano 02-08 (evita conflito de arquivo).
    Rodar e CONFIRMAR GREEN.

    **REFACTOR** — `vendor/bin/pint --dirty --format agent`. Rodar regressões da promessa da Fase 1 (sem editar nada nelas): `php artisan test --compact tests/Unit/Support/SettingsTest.php tests/Feature/Auth` — o Unit prova o fallback sem tabela; Auth prova lockout/senha intactos.
  </action>
  <verify>php artisan test --compact tests/Feature/Parameters tests/Feature/Seeders/ParameterSeederTest.php tests/Unit/Support/SettingsTest.php</verify>
  <acceptance_criteria>
    - `php artisan test --compact tests/Feature/Parameters tests/Feature/Seeders/ParameterSeederTest.php` termina com `"result":"passed"`
    - `php artisan test --compact tests/Unit/Support/SettingsTest.php` verde SEM nenhuma edição no arquivo (verificar com `git diff --stat tests/Unit/Support/SettingsTest.php` vazio)
    - `app/Support/Settings.php` contém `QueryException`, `Cache::remember` e `public static function enabled`
    - `app/Support/Settings.php` mantém a assinatura `public static function get(string $key, mixed $default = null): mixed`
    - `database/seeders/ParameterSeeder.php` NÃO contém `'value'` no array de metadados do updateOrCreate (grep `'value' =>` não aparece no segundo argumento)
    - `app/Providers/AppServiceProvider.php` contém `Settings::get('security.password.min_length'` e mantém a linha `auth.passwords.users.expire` inalterada
    - `config/sile.php` contém `cache_ttl` e `'procuracoes' => true` e continua contendo `'max_attempts' => 5`
  </acceptance_criteria>
  <done>Promessa da Fase 1 cumprida: Settings administrável por banco com cache e fallback resiliente, catálogo completo seedado preservando valores administrados, política de senha de verdade parametrizada — sem nenhum call site ou teste da Fase 1 alterado.</done>
</task>

</tasks>

<verification>
1. `php artisan test --compact tests/Feature/Parameters tests/Feature/Seeders/ParameterSeederTest.php` verde.
2. `php artisan test --compact tests/Unit/Support/SettingsTest.php` verde com `git diff --stat tests/Unit/Support/SettingsTest.php` vazio (arquivo intocado).
3. `vendor/bin/pint --dirty --format agent` sem pendências.
4. NÃO rodar a suíte completa aqui — Wave 1 tem planos paralelos; o orquestrador roda no fechamento da wave.
</verification>

<success_criteria>
- Settings::get com backend banco+cache+fallback; QueryException nunca derruba boot/artisan/testes Unit.
- Alteração de Parameter reflete na leitura seguinte (invalidação) — CA-05 provado no nível de domínio.
- Sensível criptografado no banco (assert contra valor cru) — RN-009 provado no nível de modelo.
- Catálogo de 10 parâmetros idempotente que preserva value administrado.
- Política de senha reflete o banco (parâmetros de senha deixam de ser fachada).
</success_criteria>

<output>
Após concluir, criar `.planning/phases/02-administracao-base/02-02-SUMMARY.md` registrando: a troca de backend sem tocar call sites (evidência: SettingsTest intocado e verde), o catálogo inicial (10 chaves) e a decisão de manter `sensitive` fora do fillable (Pitfall 3).
</output>

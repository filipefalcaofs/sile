---
phase: 02-administracao-base
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-03", "02-04"]
files_modified:
  - app/Http/Controllers/Gestao/UserManagementController.php
  - app/Http/Requests/Gestao/UpdateUserRoleRequest.php
  - app/Http/Requests/Gestao/ToggleUserActivationRequest.php
  - routes/gestao.php
  - resources/js/pages/gestao/usuarios/index.tsx
  - tests/Feature/Users/ManageUsersTest.php
autonomous: true
requirements: ["HU-012"]

must_haves:
  truths:
    - "Administrador lista e busca usuários da gestão com paginação parametrizada (HU-012 CA-01)"
    - "Administrador inativa e reativa contas pela interface, com auditoria explícita e efeito real no login (CA-01/CA-02)"
    - "Administrador vincula papel (perfil) ao usuário pela interface, com auditoria de papel anterior/novo (CA-01/CA-02)"
    - "Administrador NÃO consegue inativar a própria conta — bloqueio com mensagem pt-BR (CA-03, anti-lockout)"
    - "Quem não tem manter-usuarios recebe 403 auditado (CA-04)"
    - "A listagem linka o histórico de acessos de cada conta (gestao.acessos.show) — fecha o concern da Fase 1 registrado no STATE.md"
  artifacts:
    - path: "app/Http/Controllers/Gestao/UserManagementController.php"
      provides: "index (busca+paginação), updateRole (syncRoles), toggleActivation (forceFill auditado)"
    - path: "resources/js/pages/gestao/usuarios/index.tsx"
      provides: "Tela com busca, badges de papel/situação, ações e link para /gestao/acessos/{user}"
    - path: "app/Http/Requests/Gestao/ToggleUserActivationRequest.php"
      provides: "Bloqueio de auto-inativação com mensagem pt-BR"
  key_links:
    - from: "resources/js/pages/gestao/usuarios/index.tsx"
      to: "/gestao/acessos/{user}"
      via: "Link por linha da listagem (fecha o blocker da Fase 1)"
      pattern: "/gestao/acessos/"
    - from: "app/Http/Controllers/Gestao/UserManagementController.php"
      to: "app/Support/Audit/AuditService.php"
      via: "auditoria explícita usuario-inativado/usuario-reativado/papel-alterado"
      pattern: "usuario-inativado"
    - from: "routes/gestao.php"
      to: "permissão manter-usuarios"
      via: "middleware permission no grupo de rotas de usuários"
      pattern: "permission:manter-usuarios"
---

<objective>
Entregar a gestão de usuários da HU-012: listagem/busca com paginação parametrizada, ativação/inativação auditada (consumindo o mecanismo real do 02-03), vínculo de papel e navegação para o histórico de acessos de qualquer conta — fechando o concern da Fase 1 ("tela gestao/acessos alcançável apenas por URL direta").

Propósito: o administrador passa a controlar acesso sem depender de desenvolvedor; a inativação ganha interface sobre o efeito real já provado no 02-03.
Output: /gestao/usuarios navegável com os 4 CAs cobertos.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-administracao-base/02-CONTEXT.md
@.planning/phases/02-administracao-base/02-RESEARCH.md
@docs/SILE_HUs_Completas_MD/EP02-Administracao/HU-012-Manter-usuarios.md
</context>

<important_notes>
- A inativação usa `forceFill(['inactivated_at' => now()])` — `inactivated_at` está FORA do fillable por decisão do 02-03 (nunca mass assignment).
- Auditoria de usuários é EXPLÍCITA via AuditService (log 'usuarios') — o `HasAuditoria` do User loga só fillable, então inactivated_at e papéis não aparecem no diff automático (RESEARCH Padrão 4).
- Vínculo de papel: `$user->syncRoles([$role])` — um papel por usuário (modelo da Fase 1); atribuições a usuário não exigem reset de cache (doc spatie v8).
- O vínculo de papel pela UI oferece apenas os papéis existentes (estruturais + customizados criados via HU-013).
- `cpf` é dado sensível: a listagem NÃO expõe CPF completo (LGPD) — exibir mascarado `***.***.***-DD` (últimos 2 dígitos verificadores) apenas.
- Wave 3, plano único na wave: fechar com a suíte completa.
- Cadastro de usuário NOVO continua pelo fluxo público (HU-001) — fora do escopo (decisão do CONTEXT, seção deferred).
</important_notes>

<tasks>

<task type="auto">
  <name>Task 1: Controller de gestão de usuários com inativação, papel e auditoria (TDD)</name>
  <files>app/Http/Controllers/Gestao/UserManagementController.php, app/Http/Requests/Gestao/UpdateUserRoleRequest.php, app/Http/Requests/Gestao/ToggleUserActivationRequest.php, routes/gestao.php, tests/Feature/Users/ManageUsersTest.php</files>
  <read_first>
    - app/Models/User.php (isInactive(), casts — estado pós 02-03)
    - app/Support/Audit/AuditService.php (assinatura travada do log)
    - routes/gestao.php (estado pós 02-04 — rotas de cnaes já dentro do grupo; adicionar as de usuários SEM tocar nas existentes)
    - app/Http/Controllers/Gestao/CnaeController.php (convenção de index com busca+paginação estabelecida no 02-04)
    - .planning/phases/02-administracao-base/02-RESEARCH.md (Padrão 6 — vínculo de papel; Padrão 10 — rotas)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Users/ManageUsersTest.php` (`RefreshDatabase`, seed `RolesAndPermissionsSeeder`; admin: `administrador()->withAcceptedLgpdTerm()`):
    - `test_administrador_lista_usuarios_com_papeis_e_situacao` (CA-01): criar 1 analista e 1 cidadão inativo → GET `/gestao/usuarios` → 200, component `gestao/usuarios/index`, prop `users.data` com itens contendo `id`, `name`, `email`, `roles` (array de nomes), `inactivated_at`, `cpf_masked`; nenhum item contém a chave `cpf` em claro.
    - `test_busca_filtra_por_nome_ou_email` (CA-01): usuários 'Maria Souza'/'maria@x.dev' e 'João Lima' → GET `/gestao/usuarios?search=maria` → 1 item.
    - `test_inativa_usuario_com_auditoria` (CA-01/CA-02): PUT `/gestao/usuarios/{user}/inativacao` → redirect; `inactivated_at` preenchido no banco; `Activity` com `log_name = 'usuarios'`, `event = 'usuario-inativado'`, `causer_id = admin`, properties contendo `target_user_id`.
    - `test_reativa_usuario_com_auditoria`: usuário inativo → mesmo PUT → `inactivated_at === null`; activity `event = 'usuario-reativado'`.
    - `test_administrador_nao_inativa_a_propria_conta` (CA-03/anti-lockout): admin PUT na própria conta → `assertSessionHasErrors('user')` com mensagem `'Você não pode inativar a própria conta.'`; banco inalterado.
    - `test_altera_papel_do_usuario_com_auditoria` (CA-01/CA-02): analista → PUT `/gestao/usuarios/{user}/papel` `['role' => 'gestor']` → `$user->fresh()->hasRole('gestor')` true e `hasRole('analista')` false; activity `event = 'papel-alterado'` com properties `papel_anterior = ['analista']` e `papel_novo = 'gestor'`.
    - `test_papel_inexistente_e_rejeitado` (CA-03): PUT papel `['role' => 'papel-fantasma']` → `assertSessionHasErrors('role')`; papéis do usuário inalterados.
    - `test_gestor_sem_permissao_nao_gerencia_usuarios` (CA-04): gestor (tem acessar-gestao, NÃO tem manter-usuarios) GET `/gestao/usuarios` → 403; activity `log_name = 'seguranca'`, `result = 'bloqueado'`.
    - `test_inativacao_pela_interface_bloqueia_o_login_de_verdade` (ponta a ponta com 02-03): admin inativa o cidadão via PUT → **fazer logout explícito do admin antes de tentar o login** (`$this->post(route('logout'));` seguido de `$this->assertGuest();` — sem isso a sessão autenticada do admin cairia no redirect de guest e o POST /login não exercitaria o fluxo real) → POST `/login` com as credenciais do cidadão → `assertGuest()` e erro em `email` contendo `'inativa'`.
    Rodar `php artisan test --compact tests/Feature/Users` e CONFIRMAR RED.

    **GREEN**:
    1. `php artisan make:request Gestao/ToggleUserActivationRequest --no-interaction`:
       ```php
       public function rules(): array
       {
           return [];
       }

       public function after(): array
       {
           return [
               function (Validator $validator) {
                   if ($this->route('user')->is($this->user())) {
                       $validator->errors()->add('user', __('Você não pode inativar a própria conta.'));
                   }
               },
           ];
       }
       ```
    2. `php artisan make:request Gestao/UpdateUserRoleRequest --no-interaction`: rules `'role' => ['required', 'string', Rule::exists('roles', 'name')->where('guard_name', 'web')]`; `attributes()`: `['role' => 'papel']`.
    3. `php artisan make:controller Gestao/UserManagementController --no-interaction`:
       - `index(Request $request)`: `User::query()->with('roles:id,name')->when($search, fn ($q) => $q->where(fn ($w) => $w->where('name', 'like', "%{$term}%")->orWhere('email', 'like', "%{$term}%")))->orderBy('name')->paginate((int) Settings::get('ui.users.per_page', 15))->withQueryString()->through(fn (User $user) => ['id' => $user->id, 'name' => $user->name, 'email' => $user->email, 'roles' => $user->getRoleNames(), 'inactivated_at' => $user->inactivated_at?->toIso8601String(), 'cpf_masked' => '***.***.***-'.substr($user->cpf, -2)])`; render `gestao/usuarios/index` com `users`, `filters` e `roles` (lista `Role::orderBy('name')->pluck('name')` para o select de papel).
       - `toggleActivation(ToggleUserActivationRequest $request, User $user)`: alternar — se `isInactive()`: `forceFill(['inactivated_at' => null])->save()` + `AuditService::log('usuarios', 'usuario-reativado', "Conta de {$user->email} reativada", ['target_user_id' => $user->id], $user)`; senão `forceFill(['inactivated_at' => now()])->save()` + log `'usuario-inativado'` com a mesma estrutura. Redirect back `with('status', 'Situação da conta atualizada.')`.
       - `updateRole(UpdateUserRoleRequest $request, User $user)`: capturar `$previous = $user->getRoleNames()->all();` → `$user->syncRoles([$request->validated('role')]);` → `AuditService::log('usuarios', 'papel-alterado', "Papel de {$user->email} alterado", ['target_user_id' => $user->id, 'papel_anterior' => $previous, 'papel_novo' => $request->validated('role')], $user)` → back `with('status', 'Papel atualizado com sucesso.')`.
    4. `routes/gestao.php` — DENTRO do grupo existente, sem tocar rotas de cnaes/acessos:
       ```php
       Route::middleware('permission:manter-usuarios')->group(function () {
           Route::get('usuarios', [UserManagementController::class, 'index'])->name('usuarios.index');
           Route::put('usuarios/{user}/papel', [UserManagementController::class, 'updateRole'])->name('usuarios.papel.update');
           Route::put('usuarios/{user}/inativacao', [UserManagementController::class, 'toggleActivation'])->name('usuarios.inativacao.update');
       });
       ```
    Rodar e CONFIRMAR GREEN.

    **REFACTOR** — `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>php artisan test --compact tests/Feature/Users</verify>
  <acceptance_criteria>
    - `php artisan test --compact tests/Feature/Users` termina com `"result":"passed"` (15 testes nos 2 arquivos — 6 do 02-03 + 9 novos)
    - `app/Http/Controllers/Gestao/UserManagementController.php` contém `usuario-inativado`, `usuario-reativado`, `papel-alterado`, `syncRoles` e `Settings::get('ui.users.per_page'`
    - `app/Http/Requests/Gestao/ToggleUserActivationRequest.php` contém `Você não pode inativar a própria conta.`
    - `routes/gestao.php` contém `permission:manter-usuarios` e as rotas `usuarios.papel.update` e `usuarios.inativacao.update` (`php artisan route:list --name=usuarios` lista 3 rotas)
    - O transform do index NÃO inclui `'cpf' =>` (somente `cpf_masked`)
  </acceptance_criteria>
  <done>Gestão de usuários operacional: inativação/reativação com efeito real no login, vínculo de papel auditado com anterior/novo, anti-lockout de auto-inativação e CA-04 granular.</done>
</task>

<task type="auto">
  <name>Task 2: Tela de usuários com link para o histórico de acessos (fecha concern da Fase 1)</name>
  <files>resources/js/pages/gestao/usuarios/index.tsx</files>
  <read_first>
    - resources/js/pages/gestao/cnaes/index.tsx (padrão de busca debounced + tabela estabelecido no 02-04 — REUSAR)
    - resources/js/pages/gestao/acessos.tsx (página de destino do link — props targetUser)
    - .planning/STATE.md (Blockers/Concerns — o concern exato que este link fecha)
  </read_first>
  <action>
    Criar `resources/js/pages/gestao/usuarios/index.tsx` (GestaoLayout):
    - Props: `users` (paginator), `filters: { search: string }`, `roles: string[]`.
    - Título "Usuários" + busca server-side com debounce 350 ms via `router.get('/gestao/usuarios', { search }, { preserveState: true, replace: true })` (mesmo padrão do cnaes/index.tsx).
    - Tabela: "Nome", "E-mail", "CPF" (`cpf_masked`), "Papel" (badges com os nomes de `roles`), "Situação" (badge verde "Ativa" se `inactivated_at === null`; âmbar "Inativa" caso contrário), "Ações".
    - Ações por linha:
      - `<Link href={'/gestao/acessos/' + user.id}>Acessos</Link>` — navegação para o histórico de acessos da conta (FECHA o concern da Fase 1; estilo de link discreto).
      - Select de papel (options de `roles`) dentro de `<Form action={'/gestao/usuarios/' + user.id + '/papel'} method="put">` com botão "Alterar papel" (desabilitado enquanto `processing`).
      - Botão "Inativar" (conta ativa) ou "Reativar" (inativa) em `<Form action={'/gestao/usuarios/' + user.id + '/inativacao'} method="put">` com `window.confirm` pt-BR ("Inativar esta conta? O usuário perderá o acesso imediatamente." / "Reativar esta conta?").
    - Erros de validação (`user`, `role`) exibidos próximo à linha/topo; flash.status já vem do GestaoLayout.
    - Paginação pelos `links` (padrão acessos.tsx); estado vazio "Nenhum usuário encontrado.".
    Rodar `npm run typecheck && npm run build`.
    (Verificação de backend já coberta no Task 1; tela sem lógica de negócio própria — typecheck/build + smoke humano no 02-08.)
  </action>
  <verify>npm run typecheck && npm run build</verify>
  <acceptance_criteria>
    - `npm run typecheck` e `npm run build` com exit code 0
    - `resources/js/pages/gestao/usuarios/index.tsx` contém `/gestao/acessos/` (link que fecha o concern do STATE.md)
    - O arquivo contém `preserveState: true`, `Alterar papel`, `Inativar` e `Reativar`
    - `php artisan test --compact` — suíte completa verde (fechamento da wave 3)
  </acceptance_criteria>
  <done>Tela de usuários navegável: busca, papel, situação e ações funcionais — e o histórico de acessos finalmente alcançável por navegação, fechando o blocker registrado no STATE.md.</done>
</task>

</tasks>

<verification>
1. `php artisan test --compact tests/Feature/Users` verde.
2. `php artisan test --compact` — suíte completa verde (fechamento da wave 3).
3. `npm run typecheck && npm run build` verdes.
4. `vendor/bin/pint --dirty --format agent` sem pendências.
</verification>

<success_criteria>
- HU-012 com os 4 CAs cobertos: listagem/busca, inativação com efeito real (login bloqueado), papel auditado, anti-lockout e 403 auditado.
- Link "Acessos" presente em cada linha → concern da Fase 1 fechado (registrar no SUMMARY para o STATE.md remover o blocker).
- CPF nunca exposto em claro na listagem.
</success_criteria>

<output>
Após concluir, criar `.planning/phases/02-administracao-base/02-05-SUMMARY.md` registrando explicitamente que o concern "[Fase 2] Tela gestao/acessos alcançável apenas por URL direta" foi RESOLVIDO (para o orquestrador limpar o STATE.md).
</output>

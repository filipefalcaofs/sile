---
phase: 02-administracao-base
plan: 08
type: execute
wave: 6
depends_on: ["02-04", "02-05", "02-06", "02-07"]
files_modified:
  - database/seeders/DatabaseSeeder.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
  - resources/js/layouts/gestao-layout.tsx
autonomous: false
requirements: ["HU-011", "HU-012", "HU-013", "HU-014"]

must_haves:
  truths:
    - "php artisan migrate:fresh --seed prepara o ambiente completo: papéis/permissões, termo LGPD, catálogo de parâmetros, 1.331 CNAEs e admin dev"
    - "A navegação da gestão liga dashboard e as 4 telas administrativas, visível conforme as permissões do usuário (sem telas órfãs)"
    - "Suíte completa, pint, typecheck e build verdes — verificação integral da fase com evidência fresca"
    - "Smoke E2E navegável das 4 telas aprovado por humano (roteiro de 7 passos do RESEARCH)"
  artifacts:
    - path: "database/seeders/DatabaseSeeder.php"
      provides: "Orquestração completa dos seeders da fase (Parameter + Cnae incluídos)"
    - path: "resources/js/layouts/gestao-layout.tsx"
      provides: "Navegação da gestão com links permissionados para CNAEs, Usuários, Perfis e Parâmetros"
  key_links:
    - from: "database/seeders/DatabaseSeeder.php"
      to: "database/seeders/CnaeSeeder.php"
      via: "call na ordem correta (após RolesAndPermissions/LegalTerm/Parameter)"
      pattern: "CnaeSeeder::class"
    - from: "resources/js/layouts/gestao-layout.tsx"
      to: "auth.permissions (shared prop da Fase 1)"
      via: "renderização condicional dos links por permissão"
      pattern: "auth.permissions"
---

<objective>
Fechar a Fase 2: seeds de desenvolvimento completos, navegação da gestão ligando as quatro telas novas (visível por permissão), verificação integral com evidência fresca e smoke E2E humano do roteiro de 7 passos — o critério de pronto transversal do ROADMAP.

Propósito: garantir que a fase é navegável de ponta a ponta num ambiente recém-seedado, sem telas órfãs e sem regressão, antes do /gsd-verify-work.
Output: ambiente dev reproduzível, navegação fechada, fase verificada e aprovada por humano.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-administracao-base/02-CONTEXT.md
@.planning/phases/02-administracao-base/02-RESEARCH.md
@.planning/phases/02-administracao-base/02-VALIDATION.md
</context>

<important_notes>
- **DatabaseSeeder usa `WithoutModelEvents`**: o log explícito do CnaeSeeder (AuditService) NÃO depende de model events — persiste normalmente; o upsert idem. Comportamento já validado nos testes do 02-04 (seed direto). Manter o trait.
- Ordem dos seeders importa: `RolesAndPermissionsSeeder` → `LegalTermSeeder` → `ParameterSeeder` → `CnaeSeeder` → `DevAdminSeeder` (admin precisa do papel; Cnae audita — sem causer no console, ok).
- A navegação usa `auth.permissions` (shared prop tipada em `resources/js/types/index.d.ts` desde o 01-03) — links só aparecem para quem tem a permissão da tela (consultar-cnaes para CNAEs; manter-* para as demais).
- O checkpoint humano usa o ambiente dev real: `php artisan migrate:fresh --seed` + `composer run dev` (ou `npm run dev` + serve) e o roteiro de 7 passos da seção "Validação manual no browser" do 02-RESEARCH.md.
- Este plano NÃO é autônomo (`autonomous: false`) por causa do checkpoint final.
</important_notes>

<tasks>

<task type="auto">
  <name>Task 1: DatabaseSeeder completo + navegação da gestão permissionada (TDD)</name>
  <files>database/seeders/DatabaseSeeder.php, tests/Feature/Seeders/DatabaseSeederTest.php, resources/js/layouts/gestao-layout.tsx</files>
  <read_first>
    - database/seeders/DatabaseSeeder.php (ordem atual e trait WithoutModelEvents)
    - tests/Feature/Seeders/DatabaseSeederTest.php (asserções atuais — estender, não reescrever)
    - resources/js/layouts/gestao-layout.tsx (header atual sem nav)
    - resources/js/layouts/portal-layout.tsx (padrão de nav estabelecido na Fase 1 — seguir)
    - resources/js/types/index.d.ts (shape de SharedProps.auth.permissions)
  </read_first>
  <action>
    **RED** — estender `tests/Feature/Seeders/DatabaseSeederTest.php`:
    - Em `test_seed_completo_prepara_ambiente_de_desenvolvimento`: acrescentar `$this->assertSame(1331, Cnae::count());`, `$this->assertSame(10, Parameter::count());` e `$this->assertTrue(Activity::where('log_name', 'cnaes')->where('event', 'importacao-oficial')->exists());`.
    - Em `test_seed_e_idempotente`: acrescentar `Cnae::count() === 1331` e `Parameter::count() === 10` após o segundo seed.
    - Novo `test_seed_preserva_valor_de_parametro_administrado`: seed → `Parameter::where('key', 'ui.access_history.per_page')->first()->update(['value' => '7'])` → re-seed → value continua `'7'` (proteção de ponta a ponta do Pitfall 2).
    Rodar `php artisan test --compact tests/Feature/Seeders` e CONFIRMAR RED.

    **GREEN**:
    1. `database/seeders/DatabaseSeeder.php`:
       ```php
       $this->call([
           RolesAndPermissionsSeeder::class,
           LegalTermSeeder::class,
           ParameterSeeder::class,
           CnaeSeeder::class,
           DevAdminSeeder::class,
       ]);
       ```
    2. `resources/js/layouts/gestao-layout.tsx` — adicionar nav abaixo do header (padrão visual do portal-layout):
       - Links com renderização condicional por `auth.permissions` (array de strings da shared prop):
         "Painel" → `/gestao` (sempre);
         "CNAEs" → `/gestao/cnaes` (se inclui `consultar-cnaes`);
         "Usuários" → `/gestao/usuarios` (se inclui `manter-usuarios`);
         "Perfis" → `/gestao/perfis` (se inclui `manter-perfis`);
         "Parâmetros" → `/gestao/parametros` (se inclui `manter-parametros`).
       - Usar `<Link>` do Inertia; estilo consistente com o nav do portal-layout (mesmas classes de hover/ativo).
    Rodar e CONFIRMAR GREEN.

    **REFACTOR** — `vendor/bin/pint --dirty --format agent`; `npm run typecheck && npm run build`.
  </action>
  <verify>php artisan test --compact tests/Feature/Seeders && npm run typecheck && npm run build</verify>
  <acceptance_criteria>
    - `php artisan test --compact tests/Feature/Seeders` termina com `"result":"passed"` (incluindo o novo teste de preservação)
    - `database/seeders/DatabaseSeeder.php` contém `ParameterSeeder::class` e `CnaeSeeder::class` na ordem especificada
    - `resources/js/layouts/gestao-layout.tsx` contém `/gestao/cnaes`, `/gestao/usuarios`, `/gestao/perfis`, `/gestao/parametros` e `auth.permissions`
    - `npm run typecheck && npm run build` com exit code 0
  </acceptance_criteria>
  <done>Ambiente dev reproduzível com um comando e navegação da gestão fechada por permissão — nenhuma das 4 telas é órfã.</done>
</task>

<task type="auto">
  <name>Task 2: Verificação integral da fase com evidência fresca</name>
  <files>(nenhum arquivo novo — execução de verificações)</files>
  <read_first>
    - .planning/phases/02-administracao-base/02-VALIDATION.md (comandos por grupo e verificação do seed)
    - .planning/ROADMAP.md (critério de pronto transversal da fase)
  </read_first>
  <action>
    Executar e LER o output completo de cada comando (nenhuma afirmação sem evidência fresca):
    1. `php artisan test --compact` — suíte completa verde (todos os grupos da fase + 122 da Fase 1).
    2. `vendor/bin/pint --dirty --format agent` — sem pendências.
    3. `npm run typecheck && npm run build` — verdes.
    4. Evidência do seed em banco real de dev:
       ```bash
       php artisan migrate:fresh --seed
       php artisan tinker --execute 'echo App\Models\Cnae::count();'
       php artisan tinker --execute 'echo App\Models\Cnae::where("code","0111301")->value("description");'
       php artisan tinker --execute 'echo App\Models\Parameter::count();'
       php artisan tinker --execute 'echo json_encode(App\Models\Activity::where("log_name","cnaes")->latest()->value("properties"));'
       ```
       Outputs esperados: `1331`; `Cultivo de arroz`; `10`; JSON contendo `"esperado_publicacao":1332` e `"9900-8\/00"` na divergência.
    5. `php artisan route:list --path=gestao --except-vendor` — conferir as rotas das 4 telas presentes (cnaes, usuarios, perfis, parametros + acessos/dashboard da Fase 1).
    6. Confirmar testes da Fase 1 intocados: `git diff --stat tests/Unit/Support/SettingsTest.php tests/Feature/Procuration tests/Feature/Auth tests/Feature/AccessHistory` vazio.
    Registrar os outputs no SUMMARY (contagens e resultado da suíte).
  </action>
  <verify>php artisan test --compact && vendor/bin/pint --dirty --format agent && npm run typecheck && npm run build</verify>
  <acceptance_criteria>
    - `php artisan test --compact` termina com `"result":"passed"` (zero falhas, zero riscos)
    - Tinker: `Cnae::count()` imprime `1331`; `Parameter::count()` imprime `10`; properties da activity contém `esperado_publicacao`
    - `php artisan route:list --path=gestao --except-vendor` lista rotas de cnaes, usuarios, perfis e parametros
    - `git diff --stat` dos testes protegidos da Fase 1 retorna vazio
  </acceptance_criteria>
  <done>Fase verificada integralmente com evidência fresca: suíte, estilo, tipos, build, seed real e rotas — pronta para o smoke humano.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke E2E navegável das telas administrativas (roteiro de 7 passos)</name>
  <what-built>
    Fase 2 completa: CNAEs com carga oficial (1.331), gestão de usuários com inativação real e link para acessos, perfis com proteções anti-lockout, parâmetros administráveis com efeito sem deploy, histórico auditado e toggle real de procurações — tudo navegável sob /gestao com navegação permissionada.
  </what-built>
  <how-to-verify>
    Preparar: `php artisan migrate:fresh --seed && composer run dev` e abrir o app no browser.

    Roteiro (admin dev `admin@sile.dev` / `password` — aceitar o termo LGPD no primeiro acesso):
    1. Login em `/login` → redireciona para `/gestao`; conferir o nav com Painel, CNAEs, Usuários, Perfis e Parâmetros.
    2. `/gestao/cnaes` — tabela carregada; buscar "restaurante" (deve encontrar 5611-2/01); buscar "0111" por código; editar uma denominação; desativar um CNAE e ver o badge "Inativo".
    3. `/gestao/usuarios` — buscar usuário; inativar um cidadão de teste (criar antes em /register numa aba anônima, se necessário); em aba anônima tentar logar com ele → mensagem "Sua conta está inativa..."; reativar; clicar em "Acessos" → abre `/gestao/acessos/{id}`.
    4. `/gestao/perfis` — criar perfil "fiscal" com acessar-gestao + consultar-cnaes; tentar excluir "administrador" (bloqueado com mensagem); tentar remover acessar-gestao do administrador (bloqueado).
    5. `/gestao/parametros` — alterar "Itens por página no histórico de acessos" para 5 → abrir `/portal/acessos` e confirmar 5 itens por página (CA-05 sem deploy); tentar gravar 0 em "Tentativas de login..." → erro de validação pt-BR; abrir o "Histórico" do parâmetro alterado (valor anterior/novo/responsável).
    6. Em Parâmetros, desativar "Habilita o módulo de procurações" → no portal, `/portal/procuracoes` exibe o aviso âmbar e esconde o form de novo vínculo (CA-06); reativar e conferir que o form volta.
    7. Logar com um cidadão e acessar `/gestao/cnaes` direto pela URL → 403; (opcional) conferir registro bloqueado: `php artisan tinker --execute 'echo App\Models\Activity::where("log_name","seguranca")->latest()->value("description");'`.
  </how-to-verify>
  <resume-signal>Digite "aprovado" para fechar a fase, ou descreva os problemas encontrados (vira gap para /gsd-plan-phase 2 --gaps)</resume-signal>
</task>

</tasks>

<verification>
1. `php artisan test --compact` — suíte completa verde.
2. `npm run typecheck && npm run build` verdes; `vendor/bin/pint --dirty --format agent` sem pendências.
3. Evidência do seed real: contagens 1331/10 + divergência na auditoria.
4. Checkpoint humano aprovado (roteiro de 7 passos).
</verification>

<success_criteria>
- `migrate:fresh --seed` deixa o ambiente navegável com um comando.
- Navegação da gestão fechada e permissionada (sem telas órfãs — nav esconde o que o usuário não pode).
- Verificação integral verde com evidência fresca registrada no SUMMARY.
- Smoke E2E aprovado pelo usuário — HU-011 a HU-014 prontas para marcar Complete na traceability.
</success_criteria>

<output>
Após concluir, criar `.planning/phases/02-administracao-base/02-08-SUMMARY.md` registrando: outputs da verificação integral (suíte/contagens), aprovação do checkpoint e a confirmação de que o concern de navegação da Fase 1 foi fechado (link Acessos na listagem de usuários) — insumos para o /gsd-verify-work e atualização do STATE.md/REQUIREMENTS.md.
</output>

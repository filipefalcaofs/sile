---
phase: 02-administracao-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/convert-cnae-xlsx.py
  - database/data/cnaes-subclasses-2-3.csv
  - database/seeders/RolesAndPermissionsSeeder.php
  - tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
autonomous: true
requirements: ["HU-011", "HU-012", "HU-013", "HU-014"]

must_haves:
  truths:
    - "O dado oficial CNAE-Subclasses 2.3 está versionado no repositório como CSV com 1.331 subclasses, com proveniência documentada por script executável"
    - "As permissões granulares da fase (manter-cnaes, manter-usuarios, manter-perfis, manter-parametros, consultar-cnaes) existem e estão atribuídas aos papéis corretos"
    - "Re-executar o seeder de papéis NÃO remove permissões adicionadas por interface a papéis estruturais (seeder aditivo)"
  artifacts:
    - path: "database/data/cnaes-subclasses-2-3.csv"
      provides: "Dado oficial achatado (carry-forward resolvido): 1 cabeçalho + 1.331 linhas"
    - path: "scripts/convert-cnae-xlsx.py"
      provides: "Documentação executável da proveniência do CSV (conversão one-shot do xlsx IBGE/CONCLA)"
    - path: "database/seeders/RolesAndPermissionsSeeder.php"
      provides: "8 permissões seedadas de forma aditiva (givePermissionTo) nos 4 papéis estruturais"
  key_links:
    - from: "database/seeders/RolesAndPermissionsSeeder.php"
      to: "papel administrador"
      via: "givePermissionTo das permissões manter-*"
      pattern: "givePermissionTo"
    - from: "scripts/convert-cnae-xlsx.py"
      to: "database/data/cnaes-subclasses-2-3.csv"
      via: "conversão com carry-forward da árvore do xlsx"
      pattern: "cnaes-subclasses-2-3.csv"
---

<objective>
Preparar a fundação da Fase 2: converter o xlsx oficial do IBGE/CONCLA para CSV versionado (Wave 0 do 02-VALIDATION.md) e criar as permissões granulares da fase no seeder, tornando-o aditivo para não reverter ajustes feitos pelo administrador via HU-013.

Propósito: todos os planos seguintes dependem das permissões (CA-04 por recurso) e o plano 02-04 depende do CSV; o seeder aditivo evita o Pitfall 5 do 02-RESEARCH.md (re-seed desfazendo ajustes da UI).
Output: CSV oficial em `database/data/`, script de proveniência em `scripts/`, seeder com 8 permissões e testes atualizados.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-administracao-base/02-CONTEXT.md
@.planning/phases/02-administracao-base/02-RESEARCH.md
@.planning/STATE.md
</context>

<important_notes>
- **Wave 0 do 02-VALIDATION.md**: antes de qualquer mudança, rodar `php artisan test --compact` e confirmar a suíte atual verde (122 testes) — regressão de partida.
- **Divergência 1.331 × 1.332 já resolvida na pesquisa**: a publicação oficial cita 1.332 subclasses; a única ausente do arquivo de estrutura é a `9900-8/00` (confirmada existente na CONCLA). O CSV permanece fiel ao arquivo: **1.331 linhas de dados**. NÃO inventar a linha ausente — o registro da divergência acontece no relatório do import (plano 02-04).
- O script Python é conversão one-shot de DADO (transformação mecânica de formato, não código de produção PHP) — TDD não se aplica a ele; a lógica real de import (`CnaeImportService`) nasce com TDD estrito no plano 02-04. A verificação do CSV é feita por contagens executáveis (abaixo).
- Os papéis estruturais são exatamente `cidadao`, `analista`, `gestor`, `administrador` (decisão [01-03] do STATE.md) — não criar papéis novos aqui.
- Plano roda em paralelo com 02-02 e 02-03 (Wave 1): rodar SOMENTE verificações escopadas (`tests/Feature/Authorization`); a suíte completa é responsabilidade do orquestrador no fechamento da wave.
</important_notes>

<tasks>

<task type="auto">
  <name>Task 1: Converter o xlsx oficial IBGE/CONCLA para CSV versionado com script de proveniência</name>
  <files>scripts/convert-cnae-xlsx.py, database/data/cnaes-subclasses-2-3.csv</files>
  <read_first>
    - .planning/phases/02-administracao-base/02-RESEARCH.md (seção "Achados sobre o arquivo oficial de CNAEs" — layout, carry-forward, contagens esperadas)
    - docs/dados-oficiais/CNAE_Subclasses_2_3_Estrutura_Detalhada.xlsx (existência do arquivo fonte; NÃO editar)
    - .planning/phases/02-administracao-base/02-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>
    **Passo 0 (Wave 0):** rodar `php artisan test --compact` e confirmar suíte atual verde antes de qualquer mudança. Se houver falha pré-existente, PARAR e reportar.

    **Passo 1:** criar diretórios `scripts/` e `database/data/` (não existem ainda).

    **Passo 2:** criar `scripts/convert-cnae-xlsx.py` (python3 + openpyxl, ambos disponíveis na máquina de dev). O script:
    - Abre `docs/dados-oficiais/CNAE_Subclasses_2_3_Estrutura_Detalhada.xlsx`, aba `Estrutura Det. CNAE Subclass2.3`.
    - Ignora as linhas 1–4 (título/cabeçalho); dados começam na linha 5.
    - Layout em árvore com um único nível preenchido por linha: coluna A=Seção (`A`..`U`), B=Divisão (`01`), C=Grupo (`01.1`), D=Classe (`01.11-3`), E=Subclasse (`0111-3/01`), F=Denominação. A hierarquia é resolvida por **carry-forward** (cada subclasse pertence à última classe/grupo/divisão/seção vista acima); aplicar `str().strip()` em todos os valores.
    - Quando uma linha tem a coluna E (subclasse) preenchida, emite uma linha no CSV com o contexto corrente.
    - Emite `database/data/cnaes-subclasses-2-3.csv` via módulo `csv` (quoting padrão — denominações contêm vírgulas), com cabeçalho EXATAMENTE:
      `section_code,section_description,division_code,division_description,group_code,group_description,class_code,class_description,subclass_code,subclass_description`
    - Códigos permanecem **no formato oficial** (`0111-3/01`, `01.11-3`, `01.1`, `01`, `A`) — fidelidade à fonte; a normalização para dígitos é responsabilidade do `CnaeImportService` (plano 02-04).
    - Ao final imprime um resumo: total de subclasses emitidas, nº de seções/divisões/grupos/classes distintos vistos, e valida internamente: 1.331 subclasses, zero duplicatas de `subclass_code`, todos no padrão `^\d{4}-\d/\d{2}$`, denominação não vazia — saindo com exit code 1 se qualquer validação falhar.
    - Docstring no topo (pt-BR) documentando a proveniência: fonte oficial, data de download, contagem esperada da publicação (1.332) e a ausência conhecida da `9900-8/00` no arquivo de estrutura.

    **Passo 3:** executar `python3 scripts/convert-cnae-xlsx.py` e conferir o resumo (1.331 subclasses; 21 seções; 87 divisões; 285 grupos; 673 classes).
  </action>
  <verify>
    python3 scripts/convert-cnae-xlsx.py && python3 -c "
import csv
rows = list(csv.reader(open('database/data/cnaes-subclasses-2-3.csv')))
header, data = rows[0], rows[1:]
codes = [r[8] for r in data]
assert len(data) == 1331, f'esperado 1331, veio {len(data)}'
assert len(set(codes)) == 1331, 'duplicatas no subclass_code'
assert header[8] == 'subclass_code' and header[0] == 'section_code'
assert '9900-8/00' not in codes, '9900-8/00 nao deve estar no arquivo (fidelidade a fonte)'
sample = next(r for r in data if r[8] == '0111-3/01')
assert sample[9] == 'Cultivo de arroz', sample[9]
assert sample[6] == '01.11-3' and sample[0] == 'A'
print('CSV OK: 1331 subclasses, sem duplicatas, amostras corretas')
"
  </verify>
  <acceptance_criteria>
    - `php artisan test --compact` rodado ANTES das mudanças termina verde (Wave 0: regressão de partida confirmada)
    - `database/data/cnaes-subclasses-2-3.csv` existe com exatamente 1.332 linhas físicas de registro CSV (1 cabeçalho + 1.331 dados), verificado pelo comando python acima imprimindo `CSV OK`
    - O CSV NÃO contém `9900-8/00` e contém `0111-3/01` com denominação `Cultivo de arroz` e classe-pai `01.11-3`
    - `scripts/convert-cnae-xlsx.py` contém a string `9900-8/00` na docstring de proveniência e o cabeçalho `section_code,section_description,division_code,division_description,group_code,group_description,class_code,class_description,subclass_code,subclass_description`
    - O script sai com exit code 0
  </acceptance_criteria>
  <done>Dado oficial versionado e verificável: 1.331 subclasses no CSV com hierarquia resolvida, proveniência documentada de forma executável, pronto para o CnaeImportService (02-04).</done>
</task>

<task type="auto">
  <name>Task 2: Permissões granulares da fase + seeder aditivo (TDD)</name>
  <files>database/seeders/RolesAndPermissionsSeeder.php, tests/Feature/Authorization/RolesAndPermissionsSeederTest.php</files>
  <read_first>
    - database/seeders/RolesAndPermissionsSeeder.php (estado atual: 3 permissões, syncPermissions)
    - tests/Feature/Authorization/RolesAndPermissionsSeederTest.php (asserções atuais: contagens 4 papéis / 3 permissões)
    - .planning/phases/02-administracao-base/02-RESEARCH.md (Padrão 10 — permissões por recurso; Pitfall 5 — syncPermissions desfaz ajustes da UI)
  </read_first>
  <action>
    **RED** — atualizar `tests/Feature/Authorization/RolesAndPermissionsSeederTest.php` (estender, sem apagar testes existentes):
    - Em `test_seeder_cria_papeis_e_permissoes_da_fase`: acrescentar à lista de permissões verificadas: `manter-cnaes`, `manter-usuarios`, `manter-perfis`, `manter-parametros`, `consultar-cnaes`.
    - Em `test_atribuicoes_de_permissao_por_papel`: acrescentar asserções —
      administrador `hasPermissionTo` cada uma de `manter-cnaes`, `manter-usuarios`, `manter-perfis`, `manter-parametros`, `consultar-cnaes`;
      analista e gestor `hasPermissionTo('consultar-cnaes')` e `assertFalse(...->hasPermissionTo('manter-cnaes'))`;
      cidadao `assertFalse(...->hasPermissionTo('consultar-cnaes'))`.
    - Em `test_seeder_e_idempotente`: contagem de permissões passa de `3` para `8` (papéis seguem 4).
    - Novo `test_seeder_aditivo_preserva_ajustes_feitos_pela_interface`: seed → `Role::findByName('analista', 'web')->givePermissionTo(Permission::firstOrCreate(['name' => 'permissao-extra-ui', 'guard_name' => 'web']))` (simula ajuste do admin via HU-013) → re-seed → `assertTrue(Role::findByName('analista', 'web')->hasPermissionTo('permissao-extra-ui'))` (o re-seed NÃO removeu o ajuste).
    Rodar `php artisan test --compact tests/Feature/Authorization` e CONFIRMAR RED (novas asserções falham; o teste aditivo falha porque syncPermissions remove a extra).

    **GREEN** — editar `database/seeders/RolesAndPermissionsSeeder.php`:
    1. Lista `$permissions` passa a conter as 8: `acessar-gestao`, `consultar-acessos-de-qualquer-conta`, `gerenciar-procuracoes-proprias`, `manter-cnaes`, `manter-usuarios`, `manter-perfis`, `manter-parametros`, `consultar-cnaes` (todas `firstOrCreate` com `guard_name => 'web'` — padrão atual).
    2. Trocar TODOS os `->syncPermissions([...])` por `->givePermissionTo([...])` (aditivo — sync removeria ajustes do admin em re-seed; ver Pitfall 5 do RESEARCH):
       - `cidadao` → `givePermissionTo(['gerenciar-procuracoes-proprias'])`
       - `analista` → `givePermissionTo(['acessar-gestao', 'consultar-cnaes'])`
       - `gestor` → `givePermissionTo(['acessar-gestao', 'consultar-cnaes'])`
       - `administrador` → `givePermissionTo(['acessar-gestao', 'consultar-acessos-de-qualquer-conta', 'manter-cnaes', 'manter-usuarios', 'manter-perfis', 'manter-parametros', 'consultar-cnaes'])`
    3. Manter os `forgetCachedPermissions()` no início e no fim (inócuos na v8, mas inofensivos).
    Rodar e CONFIRMAR GREEN.

    **REFACTOR** — `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>php artisan test --compact tests/Feature/Authorization</verify>
  <acceptance_criteria>
    - `php artisan test --compact tests/Feature/Authorization` termina com `"result":"passed"` (5 testes, incluindo `test_seeder_aditivo_preserva_ajustes_feitos_pela_interface`)
    - `database/seeders/RolesAndPermissionsSeeder.php` NÃO contém `syncPermissions` e contém `givePermissionTo` e as strings `manter-cnaes`, `manter-usuarios`, `manter-perfis`, `manter-parametros`, `consultar-cnaes`
    - `test_seeder_e_idempotente` asserta `8` permissões e `4` papéis
    - `vendor/bin/pint --dirty --format agent` sem pendências
  </acceptance_criteria>
  <done>Permissões granulares da fase seedadas de forma aditiva e idempotente; CA-04 por recurso desbloqueado para os planos 02-04 a 02-07 sem risco de regressão administrativa em re-seed.</done>
</task>

</tasks>

<verification>
1. `python3 scripts/convert-cnae-xlsx.py` sai com 0 e o check python do Task 1 imprime `CSV OK`.
2. `php artisan test --compact tests/Feature/Authorization` verde.
3. `vendor/bin/pint --dirty --format agent` sem pendências.
4. NÃO rodar a suíte completa aqui — Wave 1 tem planos paralelos; o orquestrador roda `php artisan test --compact` no fechamento da wave.
</verification>

<success_criteria>
- CSV oficial com 1.331 subclasses versionado e verificado por contagem executável; proveniência documentada (incluindo a ausência da 9900-8/00).
- 8 permissões seedadas; administrador com todas as manter-*; analista/gestor com consultar-cnaes; seeder aditivo comprovado por teste.
- Nenhum teste existente da Fase 1 editado além das contagens/extensões do próprio RolesAndPermissionsSeederTest.
</success_criteria>

<output>
Após concluir, criar `.planning/phases/02-administracao-base/02-01-SUMMARY.md` registrando: contagens do CSV (1.331), confirmação da ausência da 9900-8/00 (fidelidade à fonte) e a mudança syncPermissions→givePermissionTo com a justificativa (Pitfall 5).
</output>

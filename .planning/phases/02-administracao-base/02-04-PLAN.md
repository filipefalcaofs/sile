---
phase: 02-administracao-base
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - database/migrations/2026_06_10_130000_create_cnaes_table.php
  - app/Models/Cnae.php
  - database/factories/CnaeFactory.php
  - app/Services/CnaeImportService.php
  - database/seeders/CnaeSeeder.php
  - app/Http/Controllers/Gestao/CnaeController.php
  - app/Http/Requests/Gestao/StoreCnaeRequest.php
  - app/Http/Requests/Gestao/UpdateCnaeRequest.php
  - routes/gestao.php
  - resources/js/pages/gestao/cnaes/index.tsx
  - tests/Feature/Cnae/CnaeImportTest.php
  - tests/Feature/Cnae/CnaeCrudTest.php
autonomous: true
requirements: ["HU-011"]

must_haves:
  truths:
    - "A tabela de CNAEs é carregada com as 1.331 subclasses oficiais (IBGE/CONCLA 2.3) por import real, idempotente e com relatório verificável (HU-011 CA-01)"
    - "A divergência com a publicação oficial (1.332 — ausência da 9900-8/00) fica registrada na auditoria do import, sem inserção silenciosa"
    - "Administrador cria, edita, desativa logicamente e exclui CNAEs pela interface; código imutável na edição (CA-01/CA-03)"
    - "Toda mudança em CNAE fica auditada com attribute_changes (CA-02)"
    - "Analista/gestor consultam CNAEs mas não mantêm; cidadão não acessa — 403 auditado (CA-04)"
    - "Busca por código ou denominação com paginação parametrizada funciona na tela"
  artifacts:
    - path: "app/Services/CnaeImportService.php"
      provides: "Import real do CSV com normalização, upsert por code e relatório de contadores + divergência"
    - path: "database/seeders/CnaeSeeder.php"
      provides: "Seed oficial delegando ao service e auditando o relatório (rules_version cnae-subclasses-2.3)"
    - path: "app/Http/Controllers/Gestao/CnaeController.php"
      provides: "index (busca+paginação), store, update (código imutável), destroy"
    - path: "resources/js/pages/gestao/cnaes/index.tsx"
      provides: "Tela administrativa com busca server-side (debounce), paginação, form e badge de inativo"
  key_links:
    - from: "database/seeders/CnaeSeeder.php"
      to: "app/Support/Audit/AuditService.php"
      via: "log do relatório do import na auditoria"
      pattern: "importacao-oficial"
    - from: "app/Services/CnaeImportService.php"
      to: "database/data/cnaes-subclasses-2-3.csv"
      via: "leitura do CSV versionado pelo 02-01"
      pattern: "cnaes-subclasses-2-3"
    - from: "routes/gestao.php"
      to: "permissões consultar-cnaes / manter-cnaes"
      via: "middleware permission por verbo (consulta separada de manutenção)"
      pattern: "permission:manter-cnaes"
    - from: "resources/js/pages/gestao/cnaes/index.tsx"
      to: "/gestao/cnaes"
      via: "router.get com debounce, preserveState e replace"
      pattern: "preserveState"
---

<objective>
Entregar a HU-011 completa: modelo de CNAEs pronto para as dimensões futuras (risco/condicionantes na Fase 6) sem retrabalho estrutural, import oficial real com relatório auditado (1.331 subclasses + divergência 9900-8/00 documentada) e CRUD administrativo navegável com busca e desativação lógica.

Propósito: CNAEs são cadastro estruturante — Fase 3 (vínculo empresa-CNAE) e Fase 6 (risco por CNAE) dependem deste modelo e desta carga.
Output: tabela cnaes populada e administrável de ponta a ponta, com os 4 CAs cobertos.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-administracao-base/02-CONTEXT.md
@.planning/phases/02-administracao-base/02-RESEARCH.md
@docs/SILE_HUs_Completas_MD/EP02-Administracao/HU-011-Manter-CNAEs.md
</context>

<important_notes>
- **Decisões já travadas (não rediscutir):** banco guarda código em dígitos (`0111301`), exibição `DDDD-D/SS` via accessor; PK surrogate `id` (código NÃO é PK — FKs futuras); hierarquia desnormalizada (section/division/group/class) vinda do CSV; flag `active` para desativação lógica; divergência 9900-8/00 NÃO entra no seed (caminho administrativo: CRUD).
- O import usa `upsert` (sem model events) — a auditoria do seed é o log EXPLÍCITO do relatório, não activities por linha (1.331 activities seriam ruído). Já o CRUD manual usa `HasAuditoria` (attribute_changes automático — CA-02).
- `DatabaseSeeder` NÃO é tocado aqui (pertence ao 02-08). Testes seedam `CnaeSeeder::class` diretamente.
- Paginação parametrizada: `Settings::get('ui.cnaes.per_page', 15)` — chave criada no catálogo/config pelo 02-02 (depends_on).
- Wave 2 com plano paralelo (02-05 NÃO — ele é wave 3; nesta wave roda sozinho): ao final, rodar a suíte completa como fechamento de wave.
- Coluna `class_code` etc.: nomes em inglês; `group_code` é seguro como coluna (grammar do Laravel cita identificadores).
- Nome do arquivo de migration gerado com timestamp real — o nome em files_modified é indicativo.
</important_notes>

<tasks>

<task type="auto">
  <name>Task 1: Migration + model + factory + CnaeImportService + CnaeSeeder com relatório auditado (TDD)</name>
  <files>database/migrations/2026_06_10_130000_create_cnaes_table.php, app/Models/Cnae.php, database/factories/CnaeFactory.php, app/Services/CnaeImportService.php, database/seeders/CnaeSeeder.php, tests/Feature/Cnae/CnaeImportTest.php</files>
  <read_first>
    - database/data/cnaes-subclasses-2-3.csv (cabeçalho e formato — produzido pelo 02-01)
    - app/Support/Audit/AuditService.php (assinatura travada: log(logName, event, description, properties, subject, result, rulesVersion))
    - app/Concerns/HasAuditoria.php (trait para o model)
    - .planning/phases/02-administracao-base/02-RESEARCH.md (Padrão 8 — import com relatório; "Achados sobre o arquivo oficial" — divergência resolvida; Code Examples — accessor formattedCode)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Cnae/CnaeImportTest.php` (`RefreshDatabase`):
    - `test_importa_as_1331_subclasses_oficiais`: `app(CnaeImportService::class)->import(database_path('data/cnaes-subclasses-2-3.csv'))` → `Cnae::count() === 1331`; report: `lidos === 1331`, `importados === 1331`, `atualizados === 0`, `rejeitados === []`.
    - `test_normaliza_codigo_e_preserva_hierarquia`: após import, `Cnae::where('code', '0111301')->first()` tem `description === 'Cultivo de arroz'`, `class_code === '01.11-3'`, `section_code === 'A'`, `active === true`; `formatted_code === '0111-3/01'`.
    - `test_import_e_idempotente`: importar 2x → `count === 1331`; segundo report: `importados === 0`, `atualizados === 1331`.
    - `test_relatorio_registra_divergencia_da_publicacao_oficial`: report contém `esperado_publicacao === 1332` e `divergencia` com chave `'9900-8/00'`; `Cnae::where('code', '9900800')->exists() === false` (sem inserção silenciosa).
    - `test_linha_malformada_e_rejeitada_sem_abortar_o_import`: gravar CSV temporário (`tempnam` + `file_put_contents`) com o cabeçalho oficial + 1 linha válida + 1 linha com `subclass_code => 'XXXX'` → report `rejeitados` tem 1 item identificando o código inválido; 1 registro importado.
    - `test_seeder_executa_import_e_audita_o_relatorio`: `$this->seed(CnaeSeeder::class)` → `Cnae::count() === 1331`; `Activity::where('log_name', 'cnaes')->where('event', 'importacao-oficial')->first()` existe com `rules_version === 'cnae-subclasses-2.3'` e `properties['divergencia']` contendo `9900-8/00`.
    Rodar `php artisan test --compact tests/Feature/Cnae` e CONFIRMAR RED.

    **GREEN**:
    1. `php artisan make:model Cnae -mf --no-interaction`.
    2. Migration `create_cnaes_table`:
       ```php
       Schema::create('cnaes', function (Blueprint $table) {
           $table->id();
           $table->string('code', 7)->unique();
           $table->string('description');
           $table->string('section_code', 1);
           $table->string('section_description');
           $table->string('division_code', 2);
           $table->string('division_description');
           $table->string('group_code', 5);
           $table->string('group_description');
           $table->string('class_code', 7);
           $table->string('class_description');
           $table->boolean('active')->default(true)->index();
           $table->timestamps();
       });
       ```
    3. `app/Models/Cnae.php`:
       - `#[Fillable(['code', 'description', 'section_code', 'section_description', 'division_code', 'division_description', 'group_code', 'group_description', 'class_code', 'class_description', 'active'])]`
       - `use HasAuditoria, HasFactory;`
       - `casts()`: `['active' => 'boolean']`
       - Accessor (Code Example do RESEARCH):
         ```php
         protected function formattedCode(): Attribute
         {
             return Attribute::make(
                 get: fn () => preg_replace('/^(\d{4})(\d)(\d{2})$/', '$1-$2/$3', $this->code),
             );
         }
         ```
    4. `database/factories/CnaeFactory.php`: `code` => `fake()->unique()->numerify('#######')`, `description` => `fake()->sentence(3)`, `section_code` => 'A', `section_description` => 'Seção de teste', `division_code` => '01', `division_description` => 'Divisão de teste', `group_code` => '01.1', `group_description` => 'Grupo de teste', `class_code` => '01.11-3', `class_description` => 'Classe de teste', `active` => `true`; state `inactive()` com `active => false`.
    5. `php artisan make:class Services/CnaeImportService --no-interaction`:
       ```php
       /**
        * @return array{lidos: int, importados: int, atualizados: int, rejeitados: array<int, string>, esperado_publicacao: int, divergencia: array<string, string>}
        */
       public function import(string $csvPath): array
       ```
       Lógica REAL (testável): abrir com `SplFileObject` (`READ_CSV`), validar cabeçalho esperado, iterar linhas; por linha: `trim` em todos os campos; normalizar `subclass_code` com `preg_replace('/\D/', '', $code)`; rejeitar (acumular em `rejeitados` com o valor original) se não casar `/^\d{7}$/`, se denominação vazia ou se código repetido no arquivo; acumular linhas válidas com TODOS os campos (código normalizado em `code`, denominação em `description`, hierarquia nas demais). Ao final: `$existing = Cnae::query()->pluck('code')->flip();` para particionar `importados` (não existia) × `atualizados` (existia); `Cnae::query()->upsert($rows, ['code'], ['description', 'section_code', 'section_description', 'division_code', 'division_description', 'group_code', 'group_description', 'class_code', 'class_description'])` em chunks de 500 (upsert NÃO toca `active` — desativação administrada sobrevive a re-import). Retornar o relatório com `'esperado_publicacao' => 1332` e `'divergencia' => ['9900-8/00' => 'Subclasse presente na publicação oficial IBGE/CONCLA, ausente do arquivo de estrutura; não consta do Decreto 32.636/2020. Cadastro manual via CRUD se necessário.']`.
    6. `php artisan make:seeder CnaeSeeder --no-interaction`:
       ```php
       public function run(): void
       {
           $report = app(CnaeImportService::class)->import(database_path('data/cnaes-subclasses-2-3.csv'));

           app(AuditService::class)->log(
               logName: 'cnaes',
               event: 'importacao-oficial',
               description: 'Importação da estrutura oficial CNAE-Subclasses 2.3 (IBGE/CONCLA)',
               properties: $report,
               rulesVersion: 'cnae-subclasses-2.3',
           );
       }
       ```
    Rodar e CONFIRMAR GREEN.

    **REFACTOR** — `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>php artisan test --compact tests/Feature/Cnae</verify>
  <acceptance_criteria>
    - `php artisan test --compact tests/Feature/Cnae` termina com `"result":"passed"` (6 testes)
    - `app/Services/CnaeImportService.php` contém `upsert`, `preg_replace('/\D/'` e `esperado_publicacao`
    - `database/seeders/CnaeSeeder.php` contém `importacao-oficial` e `cnae-subclasses-2.3`
    - `app/Models/Cnae.php` contém `HasAuditoria` e `formattedCode`
    - O array de colunas do upsert NÃO contém `'active'` (re-import preserva desativações administradas)
  </acceptance_criteria>
  <done>Carga oficial real e idempotente com relatório auditado: 1.331 subclasses no banco, divergência 9900-8/00 documentada na trilha — nada silencioso, nada inventado.</done>
</task>

<task type="auto">
  <name>Task 2: CRUD administrativo de CNAEs com permissões por verbo (TDD)</name>
  <files>app/Http/Controllers/Gestao/CnaeController.php, app/Http/Requests/Gestao/StoreCnaeRequest.php, app/Http/Requests/Gestao/UpdateCnaeRequest.php, routes/gestao.php, tests/Feature/Cnae/CnaeCrudTest.php</files>
  <read_first>
    - routes/gestao.php (grupo existente: auth, verified, permission:acessar-gestao, lgpd.accepted — as rotas novas entram DENTRO dele)
    - app/Http/Controllers/Gestao/AccessHistoryController.php (convenção de controller da gestão; paginação parametrizada via Settings)
    - app/Http/Requests/ProfileUpdateRequest.php (convenção: rules + attributes pt-BR; padrão CPF de campo imutável por omissão)
    - .planning/phases/02-administracao-base/02-RESEARCH.md (Padrão 10 — rotas/permissões; Code Example — controller index com filtro)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Cnae/CnaeCrudTest.php` (`RefreshDatabase`, seed `RolesAndPermissionsSeeder`; atores: `administrador()->withAcceptedLgpdTerm()`, `analista()->withAcceptedLgpdTerm()`, `cidadao()->withAcceptedLgpdTerm()`):
    - `test_administrador_lista_cnaes_paginados` (CA-01): 3 CNAEs via factory → GET `/gestao/cnaes` → 200, component `gestao/cnaes/index`, prop `cnaes.data` com 3 itens contendo `id`, `code`, `formatted_code`, `description`, `active`.
    - `test_busca_filtra_por_codigo_ou_denominacao` (CA-01): CNAEs com code `5611201`/description `Restaurantes e similares` e outro `0111301`/`Cultivo de arroz` → GET `/gestao/cnaes?search=restaurante` → 1 item; GET `/gestao/cnaes?search=0111-3` → 1 item (busca normaliza dígitos do termo).
    - `test_analista_consulta_mas_nao_mantem` (CA-04 granular): analista GET → 200; POST `/gestao/cnaes` → 403; activity `log_name = 'seguranca'`, `result = 'bloqueado'`.
    - `test_cidadao_nao_acessa_cnaes` (CA-04): GET → 403 auditado.
    - `test_cria_cnae_manual_normalizando_codigo` (CA-01 — caso real da divergência): admin POST `['code' => '9900-8/00', 'description' => 'Organismos internacionais e outras instituições extraterritoriais', 'section_code' => 'U', 'section_description' => 'Organismos internacionais...', 'division_code' => '99', 'division_description' => '...', 'group_code' => '99.0', 'group_description' => '...', 'class_code' => '99.00-8', 'class_description' => '...']` → redirect; `assertDatabaseHas('cnaes', ['code' => '9900800'])`.
    - `test_codigo_duplicado_e_malformado_bloqueados` (CA-03): POST com code já existente → `assertSessionHasErrors('code')`; POST com code `'123'` → erro em `code`; nada gravado.
    - `test_edicao_atualiza_dados_mas_nunca_o_codigo` (CA-03, padrão CPF): PUT `/gestao/cnaes/{cnae}` com `['code' => '9999999', 'description' => 'Denominação ajustada', 'active' => true]` → `code` INALTERADO no banco, `description` atualizada.
    - `test_desativacao_logica_preserva_o_registro` (CA-01): PUT com `active => false` → registro existe com `active === false`.
    - `test_mudancas_sao_auditadas_com_attribute_changes` (CA-02): após update de description → `Activity` com `event = 'updated'`, subject Cnae, `attribute_changes` contendo `description`.
    - `test_exclusao_de_cnae_e_auditada` (CA-01/CA-02): DELETE → `assertDatabaseMissing('cnaes', ...)`; activity `event = 'deleted'` subject Cnae.
    Rodar e CONFIRMAR RED.

    **GREEN**:
    1. `php artisan make:request Gestao/StoreCnaeRequest --no-interaction`:
       - `prepareForValidation()`: `$this->merge(['code' => preg_replace('/\D/', '', (string) $this->input('code'))]);`
       - rules: `'code' => ['required', 'regex:/^\d{7}$/', 'unique:cnaes,code']`, `'description' => ['required', 'string', 'max:255']`, `'section_code' => ['required', 'string', 'max:1']`, `'section_description' => ['required', 'string', 'max:255']`, `'division_code' => ['required', 'string', 'max:2']`, `'division_description' => ['required', 'string', 'max:255']`, `'group_code' => ['required', 'string', 'max:5']`, `'group_description' => ['required', 'string', 'max:255']`, `'class_code' => ['required', 'string', 'max:7']`, `'class_description' => ['required', 'string', 'max:255']`.
       - `attributes()`: `['code' => 'código', 'description' => 'denominação', ...]` (pt-BR para todos).
       - Mensagem custom para o regex do code: `'code.regex' => 'O código deve ter 7 dígitos no padrão DDDD-D/SS.'`.
    2. `php artisan make:request Gestao/UpdateCnaeRequest --no-interaction`: MESMAS rules de Store EXCETO `code` (omitido por completo — imutável, padrão CPF da Fase 1) e com `'active' => ['required', 'boolean']`.
    3. `php artisan make:controller Gestao/CnaeController --no-interaction`:
       - `index(Request $request)`: busca + paginação (Code Example do RESEARCH): `when` no `search` — `where('code', 'like', preg_replace('/\D/', '', $term).'%')->orWhere('description', 'like', "%{$term}%")` (wrapped em closure), `orderBy('code')`, `paginate((int) Settings::get('ui.cnaes.per_page', 15))->withQueryString()->through(fn (Cnae $cnae) => ['id' => $cnae->id, 'code' => $cnae->code, 'formatted_code' => $cnae->formatted_code, 'description' => $cnae->description, 'active' => $cnae->active, 'class_code' => $cnae->class_code])`; render `gestao/cnaes/index` com `cnaes` e `filters => ['search' => ...]`.
       - `store(StoreCnaeRequest $request)`: `Cnae::create($request->validated());` → `back()->with('status', 'CNAE cadastrado com sucesso.')`.
       - `update(UpdateCnaeRequest $request, Cnae $cnae)`: `$cnae->update($request->validated());` → `back()->with('status', 'CNAE atualizado com sucesso.')`.
       - `destroy(Cnae $cnae)`: `$cnae->delete();` → `back()->with('status', 'CNAE excluído.')`. (Sem vínculos nesta fase — a Fase 3 introduz o bloqueio por vínculo empresarial.)
    4. `routes/gestao.php` — DENTRO do grupo existente (Padrão 10):
       ```php
       Route::middleware('permission:consultar-cnaes')->group(function () {
           Route::get('cnaes', [CnaeController::class, 'index'])->name('cnaes.index');
       });

       Route::middleware('permission:manter-cnaes')->group(function () {
           Route::post('cnaes', [CnaeController::class, 'store'])->name('cnaes.store');
           Route::put('cnaes/{cnae}', [CnaeController::class, 'update'])->name('cnaes.update');
           Route::delete('cnaes/{cnae}', [CnaeController::class, 'destroy'])->name('cnaes.destroy');
       });
       ```
    Rodar e CONFIRMAR GREEN.

    **REFACTOR** — `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>php artisan test --compact tests/Feature/Cnae</verify>
  <acceptance_criteria>
    - `php artisan test --compact tests/Feature/Cnae` termina com `"result":"passed"` (16 testes nos 2 arquivos)
    - `routes/gestao.php` contém `permission:consultar-cnaes` e `permission:manter-cnaes`
    - `app/Http/Requests/Gestao/UpdateCnaeRequest.php` NÃO contém regra para `code`
    - `app/Http/Controllers/Gestao/CnaeController.php` contém `Settings::get('ui.cnaes.per_page'` e `withQueryString`
    - `php artisan route:list --name=cnaes` lista as 4 rotas (index/store/update/destroy)
  </acceptance_criteria>
  <done>CRUD de CNAEs com CA-01..04 cobertos: consulta granular separada de manutenção, código imutável na edição, desativação lógica e auditoria automática por model events.</done>
</task>

<task type="auto">
  <name>Task 3: Tela administrativa de CNAEs com busca server-side e formulários</name>
  <files>resources/js/pages/gestao/cnaes/index.tsx</files>
  <read_first>
    - resources/js/pages/gestao/acessos.tsx (template de tabela + paginação por links da Fase 1 — REUSAR padrões, não recriar)
    - resources/js/pages/portal/procuracoes/index.tsx (convenção de Form + render props do Inertia v3)
    - resources/js/layouts/gestao-layout.tsx (layout; flash.status já renderizado — NÃO editar o layout neste plano)
    - resources/js/types/index.d.ts (SharedProps — auth.permissions para esconder ações de quem só consulta)
    - .planning/phases/02-administracao-base/02-RESEARCH.md (Padrão 9 — busca com debounce; Code Example — useEffect com preserveState)
  </read_first>
  <action>
    Criar `resources/js/pages/gestao/cnaes/index.tsx` (GestaoLayout):
    - Props: `cnaes` (paginator: `data` + `links`), `filters: { search: string }`.
    - Título "CNAEs" + subtítulo "Estrutura oficial CNAE-Subclasses 2.3 (IBGE/CONCLA)".
    - **Busca server-side com debounce** (Code Example do RESEARCH, literal): `useState(filters.search ?? '')` + `useRef` para pular o primeiro render + `setTimeout` 350 ms + `router.get('/gestao/cnaes', { search }, { preserveState: true, replace: true })`; input com placeholder "Buscar por código ou denominação...".
    - Tabela com colunas: "Código" (`formatted_code`), "Denominação", "Classe" (`class_code`), "Situação" (badge: `active` → "Ativo" verde; inativo → "Inativo" âmbar — mesmo padrão de badge do acessos.tsx), "Ações".
    - Paginação reusando o padrão de `links` com `dangerouslySetInnerHTML` do acessos.tsx; estado vazio "Nenhum CNAE encontrado.".
    - Form de criação (visível apenas se `auth.permissions` inclui `manter-cnaes`): seção/painel recolhível "Cadastrar CNAE" com `<Form action="/gestao/cnaes" method="post">` e campos pt-BR: "Código (DDDD-D/SS)", "Denominação", "Seção (código e descrição)", "Divisão (código e descrição)", "Grupo (código e descrição)", "Classe (código e descrição)"; erros via render prop `errors`; botão "Salvar".
    - Edição inline por linha (apenas com `manter-cnaes`): botão "Editar" abre o form da linha com `<Form action={'/gestao/cnaes/' + cnae.id} method="put">` exibindo o código como TEXTO somente leitura (com legenda "O código não pode ser alterado.") e campos editáveis "Denominação" e "Situação" (select Ativo/Inativo enviando `active` `'1'`/`'0'`); botões "Desativar"/"Reativar" como atalho enviando o PUT com `active` invertido e description atual; botão "Excluir" com `<Form ... method="delete">` e `window.confirm('Excluir este CNAE?')` no onSubmit.
    - Sem permissão `manter-cnaes`: tabela somente leitura (sem coluna de ações de escrita).
    Rodar `npm run typecheck && npm run build` e corrigir erros de tipo.
    (TDD: a lógica de backend desta tela já está coberta pelos testes do Task 2 — assertions de component/props; tela React sem lógica de negócio própria segue verificação por typecheck/build + smoke humano no 02-08.)
  </action>
  <verify>npm run typecheck && npm run build</verify>
  <acceptance_criteria>
    - `npm run typecheck` e `npm run build` terminam com exit code 0
    - `resources/js/pages/gestao/cnaes/index.tsx` contém `preserveState: true`, `replace: true`, `/gestao/cnaes` e `O código não pode ser alterado`
    - O arquivo contém `manter-cnaes` (gate de UI por permissão) e os textos pt-BR `Cadastrar CNAE` e `Nenhum CNAE encontrado.`
    - `php artisan test --compact tests/Feature/Cnae && php artisan test --compact` verdes (fechamento da wave 2 — este é o único plano da wave)
  </acceptance_criteria>
  <done>Tela de CNAEs navegável com busca debounced que preserva foco e filtro na paginação, criação/edição/desativação para quem mantém e somente leitura para quem consulta.</done>
</task>

</tasks>

<verification>
1. `php artisan test --compact tests/Feature/Cnae` verde (import + CRUD).
2. `php artisan test --compact` — suíte completa verde (fechamento da wave 2).
3. `npm run typecheck && npm run build` verdes.
4. `vendor/bin/pint --dirty --format agent` sem pendências.
5. Evidência fresca do seed: `php artisan db:seed --class=CnaeSeeder` num banco migrado + `php artisan tinker --execute 'echo App\Models\Cnae::count();'` → `1331`.
</verification>

<success_criteria>
- HU-011 com os 4 CAs cobertos por testes: CRUD real, auditoria automática, validação bloqueante, 403 auditado e consulta granular.
- Import oficial: 1.331 registros, idempotente, relatório com divergência 9900-8/00 na auditoria (rules_version cnae-subclasses-2.3).
- Tela com busca/paginação parametrizada (`ui.cnaes.per_page` — nenhum valor hardcoded).
</success_criteria>

<output>
Após concluir, criar `.planning/phases/02-administracao-base/02-04-SUMMARY.md` registrando: contadores do import, o registro da divergência na auditoria (com id da activity), a decisão "upsert não toca active" e a estrutura do model pronta para as dimensões da Fase 6.
</output>

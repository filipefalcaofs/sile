---
phase: 02-administracao-base
plan: 06
type: execute
wave: 4
depends_on: ["02-01", "02-05"]
files_modified:
  - app/Support/Roles.php
  - app/Http/Controllers/Gestao/RoleController.php
  - app/Http/Requests/Gestao/StoreRoleRequest.php
  - app/Http/Requests/Gestao/UpdateRoleRequest.php
  - routes/gestao.php
  - resources/js/pages/gestao/perfis/index.tsx
  - tests/Feature/Roles/ManageRolesTest.php
autonomous: true
requirements: ["HU-013"]

must_haves:
  truths:
    - "Administrador cria perfis com permissões granulares por funcionalidade e o efeito é imediato (HU-013 CA-01)"
    - "Papéis estruturais (cidadao, analista, gestor, administrador) não podem ser excluídos nem renomeados; suas permissões PODEM ser ajustadas (CA-03)"
    - "A permissão acessar-gestao não pode ser removida do papel administrador (anti-lockout)"
    - "Exclusão de perfil com usuários vinculados é bloqueada com mensagem pt-BR (CA-03)"
    - "Operações de perfis ficam auditadas com permissões antes/depois (CA-02)"
    - "Quem não tem manter-perfis recebe 403 auditado (CA-04)"
  artifacts:
    - path: "app/Support/Roles.php"
      provides: "Constante única dos papéis estruturais (fonte de verdade no código)"
    - path: "app/Http/Controllers/Gestao/RoleController.php"
      provides: "index/store/update/destroy com proteções anti-lockout e auditoria explícita"
    - path: "resources/js/pages/gestao/perfis/index.tsx"
      provides: "Tela com permissões agrupadas por prefixo em checkboxes"
  key_links:
    - from: "app/Http/Requests/Gestao/UpdateRoleRequest.php"
      to: "app/Support/Roles.php"
      via: "validações de papel estrutural (rename proibido, acessar-gestao do administrador obrigatória)"
      pattern: "Roles::STRUCTURAL"
    - from: "app/Http/Controllers/Gestao/RoleController.php"
      to: "app/Support/Audit/AuditService.php"
      via: "auditoria explícita perfil-criado/atualizado/excluido"
      pattern: "perfil-criado"
    - from: "routes/gestao.php"
      to: "permissão manter-perfis"
      via: "middleware permission no grupo de rotas de perfis"
      pattern: "permission:manter-perfis"
---

<objective>
Entregar a HU-013: CRUD de perfis (roles spatie) com atribuição granular de permissões, proteções aos papéis estruturais e anti-lockout — garantindo que o administrador nunca consiga trancar a si mesmo nem quebrar os papéis que o código referencia.

Propósito: segregação de funções administrável por interface; perfis novos (ex.: "fiscal") podem nascer sem deploy, consumindo as permissões granulares seedadas no 02-01.
Output: /gestao/perfis navegável com os 4 CAs cobertos e as três proteções anti-lockout testadas.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-administracao-base/02-CONTEXT.md
@.planning/phases/02-administracao-base/02-RESEARCH.md
@docs/SILE_HUs_Completas_MD/EP02-Administracao/HU-013-Manter-perfis.md
</context>

<important_notes>
- **Proteções travadas no CONTEXT (implementar exatamente):** estruturais `['cidadao', 'analista', 'gestor', 'administrador']` não excluíveis e não renomeáveis; permissões deles ajustáveis EXCETO remover `acessar-gestao` do `administrador`; perfis custom totalmente editáveis; exclusão bloqueada com usuários vinculados.
- spatie v8: `Role::create`, `syncPermissions`, `delete` resetam o cache de permissões automaticamente — NÃO adicionar `forgetCachedPermissions` manual no controller (Padrão 7 / State of the Art do RESEARCH).
- No UPDATE de papéis estruturais usa-se `syncPermissions` (correto aqui: a intenção do admin é o conjunto exato marcado na tela) — diferente do SEEDER, que é aditivo (decisão do 02-01).
- Roles do spatie NÃO têm HasAuditoria (model do vendor) — auditoria explícita via AuditService (log 'perfis') com permissões antes/depois.
- Guard sempre `web` (único guard do projeto — decisão [01-03]).
- Wave 4, plano único na wave: fechar com a suíte completa.
</important_notes>

<tasks>

<task type="auto">
  <name>Task 1: CRUD de perfis com proteções estruturais e anti-lockout (TDD)</name>
  <files>app/Support/Roles.php, app/Http/Controllers/Gestao/RoleController.php, app/Http/Requests/Gestao/StoreRoleRequest.php, app/Http/Requests/Gestao/UpdateRoleRequest.php, routes/gestao.php, tests/Feature/Roles/ManageRolesTest.php</files>
  <read_first>
    - database/seeders/RolesAndPermissionsSeeder.php (estado pós 02-01: 8 permissões, givePermissionTo)
    - app/Support/Audit/AuditService.php (assinatura travada)
    - routes/gestao.php (estado pós 02-05 — adicionar rotas de perfis sem tocar nas existentes)
    - app/Http/Requests/Gestao/ToggleUserActivationRequest.php (convenção after() para validações de negócio, criada no 02-05)
    - .planning/phases/02-administracao-base/02-RESEARCH.md (Padrão 7 — spatie v8; Pitfall 6 — lockout)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Roles/ManageRolesTest.php` (`RefreshDatabase`, seed `RolesAndPermissionsSeeder`; admin com termo aceito):
    - `test_administrador_lista_perfis_com_permissoes_e_vinculos` (CA-01): GET `/gestao/perfis` → 200, component `gestao/perfis/index`, props `roles` (cada item com `id`, `name`, `permissions` array, `users_count`, `structural` boolean) e `permissions` (todas as 8 do seed).
    - `test_cria_perfil_com_permissoes_granulares` (CA-01): POST `/gestao/perfis` `['name' => 'fiscal', 'permissions' => ['acessar-gestao', 'consultar-cnaes']]` → redirect; `Role::findByName('fiscal', 'web')` existe com exatamente essas 2 permissões.
    - `test_permissoes_de_perfil_novo_tem_efeito_imediato` (CA-01): criar 'fiscal' via POST; `$user->assignRole('fiscal')` → GET `/gestao/cnaes` → 200; POST `/gestao/cnaes` (payload válido) → 403 (tem consultar, não tem manter — granularidade real).
    - `test_ajusta_permissoes_de_papel_estrutural`: PUT `/gestao/perfis/{idDoAnalista}` `['name' => 'analista', 'permissions' => ['acessar-gestao', 'consultar-cnaes', 'consultar-acessos-de-qualquer-conta']]` → analista passa a ter as 3.
    - `test_nao_remove_acessar_gestao_do_administrador` (anti-lockout): PUT no administrador com `permissions => ['manter-cnaes']` (sem acessar-gestao) → `assertSessionHasErrors('permissions')` com mensagem contendo `'acessar-gestao'`; permissões do administrador INALTERADAS.
    - `test_papel_estrutural_nao_pode_ser_renomeado` (CA-03): PUT no cidadao com `name => 'municipe'` → `assertSessionHasErrors('name')`; nome inalterado.
    - `test_papel_estrutural_nao_pode_ser_excluido` (CA-03): DELETE `/gestao/perfis/{idDoAdministrador}` → `assertSessionHasErrors('role')`; papel continua existindo.
    - `test_exclusao_bloqueada_com_usuarios_vinculados` (CA-03): criar role 'temporario' + user com esse papel → DELETE → `assertSessionHasErrors('role')` com mensagem contendo `'usuários vinculados'`; papel existe.
    - `test_exclui_perfil_custom_sem_usuarios` (CA-01): role 'descartavel' sem usuários → DELETE → removido do banco.
    - `test_nome_duplicado_e_rejeitado` (CA-03): POST `name => 'cidadao'` → `assertSessionHasErrors('name')`.
    - `test_operacoes_de_perfis_sao_auditadas` (CA-02): após create/update/delete → `Activity::where('log_name', 'perfis')` contém eventos `perfil-criado`, `perfil-atualizado`, `perfil-excluido`, com properties `permissoes_antes`/`permissoes_depois` no update.
    - `test_analista_nao_mantem_perfis` (CA-04): analista GET `/gestao/perfis` → 403; activity `result = 'bloqueado'`.
    Rodar `php artisan test --compact tests/Feature/Roles` e CONFIRMAR RED.

    **GREEN**:
    1. `php artisan make:class Support/Roles --no-interaction`:
       ```php
       final class Roles
       {
           /** Papéis referenciados pelo código — não excluíveis nem renomeáveis (HU-013). */
           public const array STRUCTURAL = ['cidadao', 'analista', 'gestor', 'administrador'];
       }
       ```
    2. `php artisan make:request Gestao/StoreRoleRequest --no-interaction`: rules `'name' => ['required', 'string', 'max:255', Rule::unique('roles', 'name')->where('guard_name', 'web')]`, `'permissions' => ['array']`, `'permissions.*' => ['string', Rule::exists('permissions', 'name')->where('guard_name', 'web')]`; `attributes()` pt-BR (`name` => 'nome', `permissions` => 'permissões').
    3. `php artisan make:request Gestao/UpdateRoleRequest --no-interaction`: mesmas rules de permissions; `name` com unique ignorando o próprio id; `after()` com DUAS validações de negócio (usar `$this->route('role')`):
       - rename de estrutural: `in_array($role->name, Roles::STRUCTURAL) && $this->input('name') !== $role->name` → erro em `name`: `'Papéis estruturais do sistema não podem ser renomeados.'`
       - anti-lockout: `$role->name === 'administrador' && ! in_array('acessar-gestao', (array) $this->input('permissions'))` → erro em `permissions`: `'O papel administrador não pode perder a permissão acessar-gestao.'`
    4. `php artisan make:controller Gestao/RoleController --no-interaction`:
       - `index()`: `Role::query()->withCount('users')->with('permissions:id,name')->orderBy('name')->get()` transformado para `['id', 'name', 'permissions' => nomes, 'users_count', 'structural' => in_array(name, Roles::STRUCTURAL)]`; `permissions` => `Permission::orderBy('name')->pluck('name')`; render `gestao/perfis/index`.
       - `store(StoreRoleRequest $request)`: `$role = Role::create(['name' => $validated['name'], 'guard_name' => 'web']);` `$role->syncPermissions($validated['permissions'] ?? []);` `AuditService::log('perfis', 'perfil-criado', "Perfil {$role->name} criado", ['permissoes' => $validated['permissions'] ?? []], $role)`; back com status 'Perfil criado com sucesso.'.
       - `update(UpdateRoleRequest $request, Role $role)`: capturar `$before = $role->permissions->pluck('name')->all();` → renomear apenas se NÃO estrutural (`$role->update(['name' => ...])`) → `$role->syncPermissions($validated['permissions'] ?? []);` → log `'perfil-atualizado'` com `['permissoes_antes' => $before, 'permissoes_depois' => ...]` → back status 'Perfil atualizado com sucesso.'.
       - `destroy(Role $role)`: se `in_array($role->name, Roles::STRUCTURAL)` → `throw ValidationException::withMessages(['role' => __('Papéis estruturais do sistema não podem ser excluídos.')]);` se `$role->users()->exists()` → `throw ValidationException::withMessages(['role' => __('Este perfil possui usuários vinculados e não pode ser excluído.')]);` senão capturar nome, `$role->delete();` log `'perfil-excluido'` → back status 'Perfil excluído.'.
    5. `routes/gestao.php` — DENTRO do grupo existente:
       ```php
       Route::middleware('permission:manter-perfis')->group(function () {
           Route::get('perfis', [RoleController::class, 'index'])->name('perfis.index');
           Route::post('perfis', [RoleController::class, 'store'])->name('perfis.store');
           Route::put('perfis/{role}', [RoleController::class, 'update'])->name('perfis.update');
           Route::delete('perfis/{role}', [RoleController::class, 'destroy'])->name('perfis.destroy');
       });
       ```
       (import `Spatie\Permission\Models\Role` para o binding implícito {role}.)
    Rodar e CONFIRMAR GREEN.

    **REFACTOR** — `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>php artisan test --compact tests/Feature/Roles</verify>
  <acceptance_criteria>
    - `php artisan test --compact tests/Feature/Roles` termina com `"result":"passed"` (12 testes)
    - `app/Support/Roles.php` contém `STRUCTURAL` com os 4 papéis exatos
    - `app/Http/Requests/Gestao/UpdateRoleRequest.php` contém `acessar-gestao` (anti-lockout) e `não podem ser renomeados`
    - `app/Http/Controllers/Gestao/RoleController.php` contém `perfil-criado`, `perfil-atualizado`, `perfil-excluido` e NÃO contém `forgetCachedPermissions`
    - `php artisan route:list --name=perfis` lista 4 rotas
  </acceptance_criteria>
  <done>HU-013 no backend: perfis granulares com efeito imediato, três proteções anti-lockout comprovadas por teste e trilha de auditoria com antes/depois.</done>
</task>

<task type="auto">
  <name>Task 2: Tela de perfis com permissões agrupadas em checkboxes</name>
  <files>resources/js/pages/gestao/perfis/index.tsx</files>
  <read_first>
    - resources/js/pages/gestao/usuarios/index.tsx (padrões de tabela/ações estabelecidos no 02-05)
    - resources/js/pages/portal/procuracoes/index.tsx (convenção de Form com render props)
    - .planning/phases/02-administracao-base/02-RESEARCH.md (Padrão 7 — agrupamento por prefixo)
  </read_first>
  <action>
    Criar `resources/js/pages/gestao/perfis/index.tsx` (GestaoLayout):
    - Props: `roles` (id, name, permissions, users_count, structural), `permissions: string[]`.
    - Título "Perfis e permissões".
    - Lista de perfis em cards/linhas: nome (badge "Estrutural" cinza quando `structural`), contagem "N usuário(s)", permissões atuais como chips.
    - Form "Criar perfil": input "Nome do perfil" + grade de checkboxes das permissões AGRUPADAS por prefixo (helper local: agrupar `permissions` pelo trecho antes do primeiro `-` → títulos "Manter", "Consultar", "Acessar", "Gerenciar"); `<Form action="/gestao/perfis" method="post">` com erros nas render props; botão "Criar perfil".
    - Editar (por perfil): expande o card com checkboxes pré-marcadas das permissões atuais e input de nome (readOnly + legenda "Papéis estruturais não podem ser renomeados." quando `structural`); `<Form action={'/gestao/perfis/' + role.id} method="put">`; para o administrador, a checkbox `acessar-gestao` renderiza `disabled` + sempre enviada (input hidden) com legenda "Permissão obrigatória do administrador." (a regra REAL está no backend — a UI apenas comunica).
    - Excluir: somente para perfis com `structural === false`; botão "Excluir" com `window.confirm('Excluir este perfil?')`; quando `users_count > 0`, botão desabilitado com tooltip/legenda "Há usuários vinculados a este perfil." (a regra real está no backend).
    - Erros `role`/`name`/`permissions` exibidos; estado vazio impossível (estruturais sempre existem).
    Rodar `npm run typecheck && npm run build`.
    (Regras de negócio todas no backend e já testadas no Task 1 — a tela comunica as restrições; verificação por typecheck/build + smoke humano no 02-08.)
  </action>
  <verify>npm run typecheck && npm run build</verify>
  <acceptance_criteria>
    - `npm run typecheck` e `npm run build` com exit code 0
    - `resources/js/pages/gestao/perfis/index.tsx` contém `Papéis estruturais não podem ser renomeados.`, `Permissão obrigatória do administrador.` e `Criar perfil`
    - O arquivo agrupa checkboxes por prefixo (contém lógica de agrupamento e os títulos de grupo)
    - `php artisan test --compact` — suíte completa verde (fechamento da wave 4)
  </acceptance_criteria>
  <done>Tela de perfis navegável com criação/edição/exclusão e comunicação clara das proteções estruturais e do anti-lockout.</done>
</task>

</tasks>

<verification>
1. `php artisan test --compact tests/Feature/Roles` verde.
2. `php artisan test --compact` — suíte completa verde (fechamento da wave 4).
3. `npm run typecheck && npm run build` verdes.
4. `vendor/bin/pint --dirty --format agent` sem pendências.
</verification>

<success_criteria>
- HU-013 com os 4 CAs cobertos: criação granular com efeito imediato, ajuste de estruturais permitido, rename/exclusão de estruturais bloqueados, anti-lockout do administrador, exclusão bloqueada com vínculos, tudo auditado.
- Nenhum reset manual de cache do spatie (v8 cuida nos métodos built-in).
</success_criteria>

<output>
Após concluir, criar `.planning/phases/02-administracao-base/02-06-SUMMARY.md` registrando: as três proteções anti-lockout com os nomes dos testes que as provam, e a distinção seeder aditivo (givePermissionTo) × update da UI (syncPermissions) com a justificativa.
</output>

---
phase: 15-relatorios-e-indicadores
plan: 08
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - app/Services/Relatorios/Export/XlsxExporter.php
  - app/Services/Relatorios/Export/ReportExporter.php
  - tests/Feature/Relatorios/XlsxExporterTest.php
autonomous: true
requirements: [HU-131]
must_haves:
  truths:
    - "XLSX é gerado por streaming de baixa memória (openspout ^4.0), legível ao reabrir com o Reader do openspout"
    - "O síncrono streama ao navegador (openToBrowser); o assíncrono grava em caminho de filesystem REAL (openToFile) — Pitfall 4"
    - "ReportExporter passa a aceitar 'xlsx' no match de drivers e o formato entra em relatorios.export.formatos_habilitados"
    - "XLSX reflete EXATAMENTE o conjunto filtrado (RN-005) via builder()->cursor(), sem all() em memória"
  artifacts:
    - path: "app/Services/Relatorios/Export/XlsxExporter.php"
      provides: "Driver XLSX por streaming (openspout)"
      contains: "implements ReportFormatExporter"
    - path: "app/Services/Relatorios/Export/ReportExporter.php"
      provides: "Orquestrador agora resolvendo o driver xlsx"
      contains: "XlsxExporter"
  key_links:
    - from: "app/Services/Relatorios/Export/ReportExporter.php"
      to: "app/Services/Relatorios/Export/XlsxExporter.php"
      via: "match('xlsx') => app(XlsxExporter::class)"
      pattern: "XlsxExporter"
---

<objective>
Completar o terceiro formato do contrato único (HU-131) com o ÚNICO código de export genuinamente novo: o `XlsxExporter` por streaming via openspout `^4.0` (instalado em 15-01) — baixa memória, encaixe no Job assíncrono — e ligá-lo ao `ReportExporter` (criado em 15-02). CSV/PDF já nascem em 15-02; aqui o XLSX entra e o `match` de drivers ganha o terceiro ramo.

Purpose: as três opções (CSV/XLSX/PDF) ficam disponíveis para todas as telas e relatórios.
Output: `XlsxExporter` (stream + write) + ramo 'xlsx' no `ReportExporter` + fallback de formatos ampliado, com teste que reabre o XLSX pelo Reader do openspout e prova as linhas do conjunto filtrado.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-relatorios-e-indicadores/15-CONTEXT.md
@.planning/phases/15-relatorios-e-indicadores/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: XlsxExporter (openspout streaming: stream→browser, write→file) + teste com Reader</name>
  <files>app/Services/Relatorios/Export/XlsxExporter.php, tests/Feature/Relatorios/XlsxExporterTest.php</files>
  <read_first>
    - app/Services/Relatorios/Export/ReportFormatExporter.php (interface de 15-02: stream(ReportDefinition): Response e write(ReportDefinition, string $absolutePath): void)
    - app/Services/Relatorios/Export/CsvExporter.php (15-02 — irmão: como itera o builder filtrado e usa columnLabels()/mapRow())
    - app/Services/Relatorios/Export/ReportDefinition.php (builder()/columnLabels()/mapRow()/fileName('xlsx'))
    - 15-RESEARCH.md "Code Examples — XlsxExporter com openspout" (API v4: new Writer(new Options()); $options->SHOULD_USE_INLINE_STRINGS; openToBrowser/openToFile; Row::fromValues; Style::setFontBold; $writer->close()) e "Pitfall 4" (openToFile precisa de caminho de filesystem REAL; v4 NÃO usa WriterEntityFactory)
  </read_first>
  <action>
    Criar `app/Services/Relatorios/Export/XlsxExporter.php` (`final class ... implements ReportFormatExporter`). Usar a API de OBJETO do openspout v4 (NUNCA `WriterEntityFactory`): `use OpenSpout\Writer\XLSX\{Writer,Options}; use OpenSpout\Common\Entity\Row; use OpenSpout\Common\Entity\Style\Style;`.
      - `stream(ReportDefinition $definition): \Symfony\Component\HttpFoundation\Response`: como o openspout escreve direto no output buffer, encapsular em `response()->streamDownload(function () use ($definition) { $writer = new Writer(new Options()); $writer->openToBrowser($definition->fileName('xlsx')); $writer->addRow(Row::fromValues($definition->columnLabels(), (new Style)->setFontBold())); foreach ($definition->builder()->cursor() as $m) { $writer->addRow(Row::fromValues(array_values($definition->mapRow($m)))); } $writer->close(); }, $definition->fileName('xlsx'), ['Content-Type' => 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'])` (confirmar a forma exata de combinar streamDownload + openToBrowser; se houver conflito de headers, usar `openToBrowser` direto retornando uma `StreamedResponse` própria).
      - `write(ReportDefinition $definition, string $absolutePath): void`: `new Writer()` → `openToFile($absolutePath)` (caminho local REAL — Pitfall 4) → cabeçalho + loop `cursor()` → `close()`.
    Teste `XlsxExporterTest`: usar `write()` para um caminho temporário (`tempnam`/`Storage::fake`) a partir de um `ReportDefinition` com builder de dataset cravado; REABRIR o arquivo com o `Reader` XLSX do openspout e asserir: 1ª linha = labels; nº de linhas de dados = nº de registros do builder; conteúdo de uma célula bate com `mapRow` (prova RN-005 — só linhas do conjunto filtrado).
  </action>
  <acceptance_criteria>
    - `grep -q "class XlsxExporter implements ReportFormatExporter" app/Services/Relatorios/Export/XlsxExporter.php`
    - `grep -q "openToFile" app/Services/Relatorios/Export/XlsxExporter.php` e `grep -q "Row::fromValues" ...` e NÃO há `WriterEntityFactory` (`! grep -q WriterEntityFactory app/Services/Relatorios/Export/XlsxExporter.php`)
    - `php artisan test --compact --filter=XlsxExporterTest` sai 0 (reabre com Reader e confere linhas)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Ligar o driver xlsx ao ReportExporter + ampliar o fallback de formatos</name>
  <files>app/Services/Relatorios/Export/ReportExporter.php, tests/Feature/Relatorios/XlsxExporterTest.php</files>
  <read_first>
    - app/Services/Relatorios/Export/ReportExporter.php (15-02: método `driver(string $formato)` com match csv/pdf e o fallback de formatos `['csv','pdf']`; a validação contra relatorios.export.formatos_habilitados)
    - config/sile.php (bloco relatorios.export.formatos_habilitados já contém 'xlsx' — 15-01; aqui só ajustar o FALLBACK em código que 15-02 deixou sem xlsx)
    - tests/Feature/Relatorios/ReportExporterTest.php (15-02 — onde adicionar o caso de xlsx síncrono/assíncrono)
  </read_first>
  <action>
    Em `ReportExporter`: adicionar ao `match($formato)` o ramo `'xlsx' => app(XlsxExporter::class)`; atualizar o fallback de formatos habilitados que 15-02 deixou como `['csv','pdf']` para `['csv','xlsx','pdf']` (mantendo a leitura primária de `Settings::get('relatorios.export.formatos_habilitados', ...)`). NÃO mudar a lógica de limiar sync/async nem a auditoria (já testadas em 15-02) — só habilitar o terceiro driver.
    Em `XlsxExporterTest` (ou estendendo o `ReportExporterTest` existente), adicionar: export `formato='xlsx'` abaixo do limiar → resposta de streaming (assertStatus 200 + header de Content-Type xlsx) e auditoria gravada (`event='exporta-xlsx'`); e acima do limiar (rebaixar `config(['sile.relatorios.export.assincrono_limiar_linhas' => 1])`) → `Queue::fake()` assertPushed(GerarExportacaoJob) com formato 'xlsx' (o write em disco é coberto pelo GerarExportacaoJobTest de 15-02 + o write() de Task 1).
  </action>
  <acceptance_criteria>
    - `grep -q "'xlsx' => app(XlsxExporter::class)" app/Services/Relatorios/Export/ReportExporter.php` (ou equivalente exato do match)
    - `php artisan test --compact --filter=ReportExporterTest` sai 0 e `--filter=XlsxExporterTest` sai 0 (xlsx sync + async)
    - `vendor/bin/pint --dirty --format agent` sem pendências
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=Xlsx` e `--filter=ReportExporter` verdes.
- XLSX legível pelo Reader do openspout; conteúdo == conjunto filtrado (RN-005).
</verification>

<success_criteria>
Os três formatos do contrato único (CSV/XLSX/PDF) disponíveis e testados; XLSX por streaming de baixa memória pronto para o caminho síncrono e o Job assíncrono.
</success_criteria>

<output>
Criar `.planning/phases/15-relatorios-e-indicadores/15-08-SUMMARY.md` registrando: a forma final de `stream()` (streamDownload vs StreamedResponse direta) decidida na implementação, a versão do openspout em uso e o Content-Type usado para xlsx.
</output>

---
phase: 15-relatorios-e-indicadores
plan: 10
type: execute
wave: 5
depends_on: ["15-02", "15-08", "15-09"]
files_modified:
  - app/Services/Relatorios/Export/Sources/AtividadesReportSource.php
  - app/Services/Relatorios/Export/Sources/AcessosReportSource.php
  - app/Http/Controllers/Gestao/AuditoriaController.php
  - app/Http/Controllers/Gestao/ProcessoController.php
  - tests/Feature/Relatorios/RetrofitCsvAuditoriaTest.php
autonomous: true
requirements: [HU-131]
must_haves:
  truths:
    - "Os DOIS CSVs existentes (AuditoriaController::export atividades/acessos e ProcessoController CSV) passam a usar o CsvExporter — uma fonte única, sem duas cópias divergentes (RN-009)"
    - "Colunas, nome de arquivo e a marca personalData/HU-101 são PRESERVADOS — os testes atuais permanecem verdes (anti-regressão, Pitfall 7)"
    - "Após o retrofit, as telas de auditoria e processos ganham também XLSX/PDF pelo mesmo ?formato= (HU-131)"
    - "A exportação continua auditada (meta-auditoria, personalData quando expõe PII)"
  artifacts:
    - path: "app/Services/Relatorios/Export/Sources/AtividadesReportSource.php"
      provides: "ReportSource da trilha de atividades (colunas atuais preservadas)"
      contains: "implements ReportSource"
    - path: "app/Http/Controllers/Gestao/AuditoriaController.php"
      provides: "export delegando ao ReportExporter (consolidado)"
      contains: "ReportExporter"
  key_links:
    - from: "app/Http/Controllers/Gestao/AuditoriaController.php"
      to: "app/Services/Relatorios/Export/ReportExporter.php"
      via: "export delega ?formato= ao exporter"
      pattern: "ReportExporter"
---

<objective>
Pagar a consolidação central da HU-131 (RN-009): migrar os DOIS streamings CSV já existentes — `AuditoriaController::export` (atividades + acessos) e `ProcessoController` (CSV de processos) — para o contrato único `ReportExporter`/`CsvExporter`, eliminando as cópias divergentes de `fputcsv`. O retrofit é incremental e SEM regressão: os testes atuais são a rede de proteção — colunas, nome de arquivo e `personalData`/HU-101 preservados. Como bônus, essas telas passam a ter XLSX/PDF pelo mesmo `?formato=`.

Purpose: uma única implementação de export para o sistema inteiro; telas antigas herdam os 3 formatos sem reimplementar.
Output: `AtividadesReportSource` + `AcessosReportSource` + `AuditoriaController`/`ProcessoController` delegando ao `ReportExporter`, com os testes existentes verdes e novos testes dos formatos adicionais.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-relatorios-e-indicadores/15-CONTEXT.md
@.planning/phases/15-relatorios-e-indicadores/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AtividadesReportSource + AcessosReportSource e migração de AuditoriaController::export</name>
  <files>app/Services/Relatorios/Export/Sources/AtividadesReportSource.php, app/Services/Relatorios/Export/Sources/AcessosReportSource.php, app/Http/Controllers/Gestao/AuditoriaController.php, tests/Feature/Relatorios/RetrofitCsvAuditoriaTest.php</files>
  <read_first>
    - app/Http/Controllers/Gestao/AuditoriaController.php (métodos `export`, `exportarAtividades`, `exportarAcessos`: as COLUNAS exatas via `$colunas`, o nome do arquivo, a guarda de volume e a marca personalData — copiar fielmente para os ReportSources)
    - tests/Feature/* da auditoria (12-04) que cobrem o export — a rede anti-regressão: as MESMAS colunas/arquivo precisam sair verdes
    - app/Services/Relatorios/Export/{ReportDefinition,ReportSource,ReportExporter}.php (contrato de 15-02; o controller passará a delegar)
    - 15-RESEARCH.md "Pitfall 7" (preservar colunas/arquivo/personalData; migrar 1 controller por vez, rodando o teste do controller a cada passo)
  </read_first>
  <action>
    Criar `AtividadesReportSource` e `AcessosReportSource` (`implements ReportSource`, classes SEPARADAS por fonte — assim o caminho assíncrono reconstrói a classe certa via `app($sourceClass)`, sem depender de args de construtor). Cada `definition(ReportFilters $f)` reproduz EXATAMENTE o Builder filtrado e as colunas/`mapRow` do `exportarAtividades`/`exportarAcessos` atuais (mesmos rótulos, mesma ordem), `personalData=true` (HU-101), `arquivoBase` igual ao nome de arquivo atual, `logName`/`event` iguais aos atuais da meta-auditoria.
    Refatorar `AuditoriaController::export` para montar o `ReportFilters` da request e delegar ao `ReportExporter` escolhendo o source por `fonte` (atividades vs acessos), passando o `?formato=` (default 'csv' para preservar o comportamento atual). REMOVER `exportarAtividades`/`exportarAcessos` SÓ depois que o teste existente passar pela nova via. NÃO alterar o `index` nem as rotas (a rota `auditoria.export` já existe).
    Teste `RetrofitCsvAuditoriaTest` (novo, complementar aos existentes): provar que `?formato=xlsx` e `?formato=pdf` agora funcionam na auditoria (status/headers) E que o CSV mantém as colunas atuais. Rodar TAMBÉM os testes de auditoria existentes (12-04) e confirmá-los verdes (anti-regressão).
  </action>
  <acceptance_criteria>
    - `grep -q "implements ReportSource" app/Services/Relatorios/Export/Sources/AtividadesReportSource.php` e idem `AcessosReportSource.php`
    - `grep -q "ReportExporter" app/Http/Controllers/Gestao/AuditoriaController.php` e NÃO há mais `fputcsv` no controller (`! grep -q fputcsv app/Http/Controllers/Gestao/AuditoriaController.php`)
    - `php artisan test --compact --filter=Auditoria` sai 0 (testes existentes + novos formatos) — anti-regressão
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migração de ProcessoController (CSV de processos) para o ReportExporter</name>
  <files>app/Http/Controllers/Gestao/ProcessoController.php, app/Services/Relatorios/Export/Sources/SolicitacoesReportSource.php, tests/Feature/Relatorios/RetrofitCsvAuditoriaTest.php</files>
  <read_first>
    - app/Http/Controllers/Gestao/ProcessoController.php (o método de CSV existente: colunas exatas, nome do arquivo, filtros aplicados — a rede anti-regressão)
    - app/Services/Relatorios/Export/Sources/SolicitacoesReportSource.php (15-03 — já alinhado às colunas do ProcessoController; se divergir, AJUSTAR aqui o source para BATER com o CSV atual, preservando o teste)
    - tests/Feature/* de processos (10-14) que cobrem o CSV — devem permanecer verdes
  </read_first>
  <action>
    Refatorar o branch de CSV do `ProcessoController` (hoje `exportarCsv`/branch `if formato==='csv'`) para delegar ao `ReportExporter` usando o `SolicitacoesReportSource` (15-03), passando `?formato=` (default 'csv'). Conferir que as colunas/arquivo do `SolicitacoesReportSource` BATEM com o CSV atual do ProcessoController; se houver divergência, ajustar o `SolicitacoesReportSource` (mapRow/colunas) para preservar EXATAMENTE o CSV atual (o teste de 10-14 é o juiz). Remover o `fputcsv`/`streamDownload` cru do controller só após o teste passar pela nova via. NÃO alterar o `index`/`fila`/`busca` nem as rotas.
    Adicionar ao `RetrofitCsvAuditoriaTest` (ou um teste dedicado de processos): `?formato=xlsx`/`pdf` no index de processos agora retornam streaming; o CSV mantém as colunas atuais. Rodar os testes de processos existentes (10-14) e confirmá-los verdes.
  </action>
  <acceptance_criteria>
    - `grep -q "ReportExporter" app/Http/Controllers/Gestao/ProcessoController.php` e o CSV cru (`fputcsv`) removido do controller
    - `php artisan test --compact --filter=Processo` sai 0 (existentes + novos formatos) — anti-regressão
    - `vendor/bin/pint --dirty --format agent` sem pendências
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=Auditoria` e `--filter=Processo` e `--filter=RetrofitCsv` verdes (anti-regressão + novos formatos).
- Nenhum `fputcsv` cru remanescente nesses dois controllers (consolidação RN-009).
</verification>

<success_criteria>
Os dois CSVs históricos consolidados no contrato único, sem regressão de colunas/arquivo/personalData, e as telas de auditoria/processos passam a oferecer CSV/XLSX/PDF pelo mesmo mecanismo.
</success_criteria>

<output>
Criar `.planning/phases/15-relatorios-e-indicadores/15-10-SUMMARY.md` registrando: as colunas exatas preservadas de cada export, qualquer ajuste feito no SolicitacoesReportSource para casar com o CSV de processos, e a confirmação de anti-regressão (12-04/10-14 verdes).
</output>

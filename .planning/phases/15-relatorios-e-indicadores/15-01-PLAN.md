---
phase: 15-relatorios-e-indicadores
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - package.json
  - database/seeders/ParameterSeeder.php
  - config/sile.php
  - database/seeders/RolesAndPermissionsSeeder.php
  - database/migrations/2026_06_15_000001_add_relatorios_indexes_to_viability_tables.php
  - tests/Feature/Seeders/ParameterSeederTest.php
  - tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
autonomous: true
requirements: [HU-131, HU-130, HU-145, HU-014]
must_haves:
  truths:
    - "openspout/openspout ^4.0 e echarts ^6.1 instalados sem quebrar o piso PHP ^8.3"
    - "Catálogo HU-014 tem 90 parâmetros (85 + 5 do grupo relatorios) e config/sile.php espelha todos com fallback"
    - "Existem as permissões consultar-relatorios e relatorios.produtividade.nominal (gestor/admin)"
    - "Colunas de data/decisão dos relatórios estão indexadas (protocoled_at, decided_at, flow+outcome, decided_by_user_id, transitions created_at)"
  artifacts:
    - path: "database/migrations/2026_06_15_000001_add_relatorios_indexes_to_viability_tables.php"
      provides: "Índices aditivos de desempenho dos relatórios (Pitfall 1)"
      contains: "index('protocoled_at')"
    - path: "config/sile.php"
      provides: "Bloco relatorios.* (parâmetros + constantes técnicas)"
      contains: "'relatorios' =>"
  key_links:
    - from: "config/sile.php"
      to: "App\\Support\\Settings::get"
      via: "fallback sile.relatorios.*"
      pattern: "relatorios"
---

<objective>
Fundação da Fase 15 (não toca lógica de negócio): instalar as dependências aprovadas (openspout, echarts), pagar a dívida de índices que o RESEARCH identificou (Pitfall 1 — as colunas de data/decisão dos relatórios NÃO estão indexadas hoje), e registrar os 5 parâmetros HU-014 do EP15 + as 2 permissões novas. (O Value Object `ReportFilters` foi MOVIDO para 15-02 — é o tipo de entrada do contrato de export e mora com ele, evitando que 15-02 dependa deste plano.)

Purpose: destravar todas as waves seguintes com catálogo/config/permissões/índices prontos, sob DONO ÚNICO (este plano), espelhando a disciplina das Fases 10–12.
Output: deps instaladas, migration de índices, 5 parâmetros (90 total), 2 permissões (29 total) e testes de seeder atualizados.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-relatorios-e-indicadores/15-CONTEXT.md
@.planning/phases/15-relatorios-e-indicadores/15-RESEARCH.md
@.planning/phases/15-relatorios-e-indicadores/15-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar openspout ^4.0 e echarts ^6.1 + migration aditiva de índices</name>
  <files>composer.json, package.json, database/migrations/2026_06_15_000001_add_relatorios_indexes_to_viability_tables.php</files>
  <read_first>
    - composer.json (confirmar `"php": "^8.3"` ANTES de instalar — openspout v5 exige ~8.4/8.5 e quebraria o piso; por isso ^4.0)
    - 15-RESEARCH.md seção "Standard Stack" (decisão de versão openspout ^4.0; echarts ^6.1) e "Pitfall 1" (colunas sem índice)
    - database/migrations/2026_06_14_235144_add_consulta_indexes_to_viability_requests_table.php (padrão de migration ADITIVA de índices, com up/down)
    - database/migrations/2026_06_14_163233_create_viability_decisions_table.php (índices existentes: viability_request_id unique, tvl_product_number unique — faltam decided_at, (flow,outcome), decided_by_user_id)
    - database/migrations/2026_06_14_104834_create_viability_request_transitions_table.php (só FK indexada — falta (viability_request_id, created_at))
  </read_first>
  <action>
    Rodar `composer require "openspout/openspout:^4.0"` e `npm install echarts@^6.1`. NÃO subir o piso PHP: o `composer.json` deve continuar com `"php": "^8.3"` (se o require alterar, reverter a linha do php manualmente). Confirmar que `composer.json` ganhou `openspout/openspout` em `require` e `package.json` ganhou `echarts` em `dependencies`.
    Criar a migration `add_relatorios_indexes_to_viability_tables` (`php artisan make:migration add_relatorios_indexes_to_viability_tables`), ADITIVA (sem alterar colunas — regra Laravel de repetir atributos não se aplica a addIndex):
      - `Schema::table('viability_requests', fn (Blueprint $t) => $t->index('protocoled_at'))`
      - `Schema::table('viability_decisions', function (Blueprint $t) { $t->index('decided_at'); $t->index(['flow', 'outcome']); $t->index('decided_by_user_id'); })`
      - `Schema::table('viability_request_transitions', fn (Blueprint $t) => $t->index(['viability_request_id', 'created_at']))`
    Implementar `down()` simétrico com `dropIndex` (usar os mesmos arrays de colunas). NÃO duplicar índices já existentes (protocol_number/status/sector_id/assigned_user_id/analysis_*/property_registration/address_neighborhood/external_reference já indexados).
  </action>
  <acceptance_criteria>
    - `grep -q 'openspout/openspout' composer.json` e `grep -q '"php": "\^8.3"' composer.json`
    - `grep -q '"echarts"' package.json`
    - `grep -q "index('protocoled_at')" database/migrations/2026_06_15_000001_add_relatorios_indexes_to_viability_tables.php`
    - `grep -q "index(\['flow', 'outcome'\])" database/migrations/2026_06_15_000001_add_relatorios_indexes_to_viability_tables.php`
    - `php artisan migrate --no-interaction` sai 0 e `php artisan migrate:rollback --no-interaction` sai 0 (up/down válidos); rodar `php artisan migrate --no-interaction` de novo ao final
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Registrar os 5 parâmetros HU-014 (grupo relatorios) + espelho config/sile.php + 2 permissões</name>
  <files>database/seeders/ParameterSeeder.php, config/sile.php, database/seeders/RolesAndPermissionsSeeder.php</files>
  <read_first>
    - database/seeders/ParameterSeeder.php (shape do catálogo: key => group/type/default_value(string)/validation_rules(array)/description; value NUNCA no array)
    - 15-RESEARCH.md "Entrada do catálogo de parâmetro (HU-014) — shape exato" e seção Parâmetros do 15-CONTEXT.md (chaves exatas; constantes técnicas FORA do catálogo)
    - config/sile.php (blocos `expresso`/`analise` como precedente de "parâmetros administráveis no catálogo + constantes técnicas só no config" — precedente [02-02])
    - database/seeders/RolesAndPermissionsSeeder.php (lista $permissions e atribuição aditiva givePermissionTo por papel — gestor/administrador)
  </read_first>
  <action>
    No `ParameterSeeder::catalog()`, adicionar 5 entradas no grupo `relatorios`:
      - `'relatorios.export.assincrono_limiar_linhas'` => group 'relatorios', type 'integer', default_value '5000', rules ['required','integer','min:100','max:1000000'], description 'Acima deste número de linhas a exportação roda em segundo plano'
      - `'relatorios.export.formatos_habilitados'` => group 'relatorios', type 'json', default_value '["csv","xlsx","pdf"]', rules ['required','json'], description 'Formatos de exportação habilitados nas telas de gestão'
      - `'relatorios.export.retencao_dias'` => group 'relatorios', type 'integer', default_value '7', rules ['required','integer','min:1','max:365'], description 'Dias de retenção dos arquivos de exportação gerados em segundo plano'
      - `'relatorios.expresso.meta_taxa'` => group 'relatorios', type 'string', default_value null, rules ['nullable','numeric','min:0','max:100'], description 'Meta (%) da taxa de resposta expressa (vazio = meta não definida)'
      - `'relatorios.expresso.janela_dias'` => group 'relatorios', type 'integer', default_value '30', rules ['required','integer','min:1','max:365'], description 'Janela (dias) da série temporal da taxa de resposta expressa'
    No `config/sile.php`, adicionar o bloco `'relatorios'` espelhando os parâmetros + as constantes técnicas (FORA do catálogo, precedente [02-02]):
      `'relatorios' => ['export' => ['assincrono_limiar_linhas' => 5000, 'formatos_habilitados' => ['csv','xlsx','pdf'], 'retencao_dias' => 7, 'max_linhas' => 100000, 'chunk' => 200, 'disk' => 'local', 'pdf' => ['paper' => 'a4', 'orientation' => 'portrait']], 'expresso' => ['meta_taxa' => null, 'janela_dias' => 30], 'cache_ttl_segundos' => 300, 'job' => ['tries' => 3, 'timeout' => 300, 'backoff' => [30, 60, 120], 'fila' => 'default'], 'tempo' => ['etapas' => []]]`
    No `RolesAndPermissionsSeeder`, adicionar à lista `$permissions`: `'consultar-relatorios'` e `'relatorios.produtividade.nominal'`. Conceder AMBAS a `gestor` e `administrador` (givePermissionTo aditivo). NÃO criar `relatorios.exportar` — a exportação herda a permissão de leitura da própria tela (RN-007); registrar essa decisão no SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -q "relatorios.export.assincrono_limiar_linhas" database/seeders/ParameterSeeder.php` e idem para as outras 4 chaves
    - `grep -q "'relatorios' =>" config/sile.php` e `php artisan config:show sile.relatorios.export.assincrono_limiar_linhas` imprime 5000
    - `grep -q "consultar-relatorios" database/seeders/RolesAndPermissionsSeeder.php` e `grep -q "relatorios.produtividade.nominal" database/seeders/RolesAndPermissionsSeeder.php`
    - `php artisan tinker --execute 'echo App\Support\Settings::get("relatorios.expresso.meta_taxa", "indefinida");'` imprime "indefinida" (default null cai no fallback do call site)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Atualizar testes de seeder (90 params / 29 perms)</name>
  <files>tests/Feature/Seeders/ParameterSeederTest.php, tests/Feature/Authorization/RolesAndPermissionsSeederTest.php</files>
  <read_first>
    - tests/Feature/Seeders/ParameterSeederTest.php (asserts `assertSame(85, ...)` em 2 lugares e a lista de groups; o teste de idempotência também conta 85)
    - tests/Feature/Authorization/RolesAndPermissionsSeederTest.php (`test_seeder_e_idempotente` assere `assertSame(27, Permission::query()->count())`)
  </read_first>
  <action>
    Atualizar `ParameterSeederTest`: trocar `assertSame(85, ...)` por `assertSame(90, ...)` (nos 2 testes), incluir `'relatorios'` na lista ordenada de groups, e adicionar um teste `test_seeder_registra_parametros_de_relatorios` cobrindo as 5 chaves (group/type/default/rules). Atualizar `RolesAndPermissionsSeederTest::test_seeder_e_idempotente` para `assertSame(29, ...)` e adicionar `test_papeis_recebem_permissoes_de_relatorios` (gestor/admin têm consultar-relatorios + relatorios.produtividade.nominal; analista/cidadao não).
  </action>
  <acceptance_criteria>
    - `php artisan test --compact --filter=ParameterSeederTest` sai 0 e o arquivo contém `assertSame(90`
    - `php artisan test --compact --filter=RolesAndPermissionsSeederTest` sai 0 e o arquivo contém `assertSame(29`
    - `vendor/bin/pint --dirty --format agent` sem pendências
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=Seeder` e `--filter=RolesAndPermissions` verdes.
- `php artisan migrate:fresh --seed --no-interaction` sai 0 (catálogo 90, permissões 29 aplicados).
- `vendor/bin/pint --dirty --format agent` limpo.
</verification>

<success_criteria>
Deps instaladas sem quebrar `^8.3`; índices aditivos aplicados; 90 parâmetros / 29 permissões com testes verdes. (O `ReportFilters` é entregue em 15-02 junto ao contrato de export.)
</success_criteria>

<output>
Criar `.planning/phases/15-relatorios-e-indicadores/15-01-SUMMARY.md` registrando: contagem final de parâmetros/permissões, decisão de NÃO criar `relatorios.exportar` (export herda a permissão da tela — RN-007), versão do openspout/echarts instalada, o set exato de índices adicionados, e a nota de que `ReportFilters` foi movido para 15-02.
</output>

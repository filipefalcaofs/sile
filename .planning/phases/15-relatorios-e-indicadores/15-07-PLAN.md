---
phase: 15-relatorios-e-indicadores
plan: 07
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - database/migrations/2026_06_15_000004_create_expresso_quedas_table.php
  - app/Models/ExpressoQueda.php
  - database/factories/ExpressoQuedaFactory.php
  - app/Services/Expresso/FluxoExpressoService.php
  - app/Services/Relatorios/ExpressoQuedaService.php
  - app/Services/Relatorios/Export/Sources/ExpressoQuedaReportSource.php
  - tests/Feature/Relatorios/ExpressoQuedaCapturaTest.php
  - tests/Feature/Relatorios/ExpressoQuedaServiceTest.php
autonomous: true
requirements: [HU-145, HU-131]
must_haves:
  truths:
    - "No momento da queda ao analista, o motivo/gatilho estruturado por CNAE é persistido (tipo_gatilho/dimensao/motivo) — RN-001, motivo estruturado nunca texto livre"
    - "Quando o motor degradou ($resolved null) grava 1 linha de nível-processo com cnae/tipo_gatilho NULL (honesto — gatilho NUNCA inventado)"
    - "A captura é ADITIVA em FluxoExpressoService::encaminharAnalise — Fases 9/10 verdes (anti-regressão)"
    - "Taxa de resposta expressa = flow='expresso' & decided_by_user_id IS NULL ÷ elegíveis, em série temporal com meta parametrizável (RN-004)"
    - "Drill-down chega às divergências analista×motor reusando ProcessoQueryService::filtered"
  artifacts:
    - path: "app/Models/ExpressoQueda.php"
      provides: "Registro estruturado da queda por CNAE (HU-145)"
      contains: "class ExpressoQueda"
    - path: "app/Services/Relatorios/ExpressoQuedaService.php"
      provides: "Taxa de resposta expressa + ranking de motivos + drill-down"
      contains: "class ExpressoQuedaService"
  key_links:
    - from: "app/Services/Expresso/FluxoExpressoService.php"
      to: "app/Models/ExpressoQueda.php"
      via: "ExpressoQueda::create dentro de encaminharAnalise (após audit->log)"
      pattern: "ExpressoQueda::create"
---

<objective>
HU-145 — fechar o ciclo de melhoria contínua do expresso (medir → parametrizar HU-143 → medir). Pré-requisito anti-fachada: capturar o motivo/gatilho ESTRUTURADO da queda no momento em que ela ocorre (hoje só 3 categorias grossas vão para `transitions.reason` e o gatilho fica aninhado em `engine_snapshot`, com cobertura parcial). Persistir por CNAE em tabela dedicada, gravada em `FluxoExpressoService::encaminharAnalise` (ajuste ADITIVO com anti-regressão das Fases 9/10). Depois, o serviço de relatório: taxa de resposta expressa em série temporal com meta parametrizável e drill-down até as divergências analista×motor.

Purpose: o relatório de quedas mede sobre dado real e estruturado; sem a captura, a HU-145 seria fachada.
Output: tabela/model/factory `ExpressoQueda` + captura aditiva + `ExpressoQuedaService` + `ExpressoQuedaReportSource`, com testes que provam a captura honesta (incl. gatilho null quando degradado) e a taxa correta.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-relatorios-e-indicadores/15-CONTEXT.md
@.planning/phases/15-relatorios-e-indicadores/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tabela/model ExpressoQueda + captura ADITIVA em encaminharAnalise (anti-regressão 9/10)</name>
  <files>database/migrations/2026_06_15_000004_create_expresso_quedas_table.php, app/Models/ExpressoQueda.php, database/factories/ExpressoQuedaFactory.php, app/Services/Expresso/FluxoExpressoService.php, tests/Feature/Relatorios/ExpressoQuedaCapturaTest.php</files>
  <read_first>
    - app/Services/Expresso/FluxoExpressoService.php (método `encaminharAnalise` ~linha 147: a captura entra DENTRO da DB::transaction existente, APÓS o `$this->audit->log(...)`, usando `$resolved`; `perCnaeResumo`/`rulesVersionRepresentativa` já existem)
    - app/Services/Risco/RiscoClassificationService.php (shape de `encaminhamento`: fluxo/dimensao_decisiva/motivo/gatilhos_acionados[]) e app/Enums/TipoGatilho.php (códigos do gatilho)
    - app/Services/Solicitacao/ResolvedViability.php (estrutura de $resolved: por_cnae[*] com fluxo/consulta_array.risco.encaminhamento)
    - 15-RESEARCH.md "Captura estruturada da queda (HU-145)" (código de referência da captura — copiar a lógica $resolved!==null vs null)
    - tests/Feature/Expresso/* (testes de FluxoExpressoService das Fases 9/10 — a rede anti-regressão: continuam verdes após a captura aditiva)
  </read_first>
  <action>
    Criar migration/model/factory via `php artisan make:model ExpressoQueda -mf --no-interaction`.
    Migration `create_expresso_quedas_table`: `id`, `viability_request_id` (FK constrained, cascadeOnDelete), `cnae` (string, nullable), `tipo_gatilho` (string, nullable), `dimensao` (string, nullable), `motivo` (text), `timestamps`. Índices: `viability_request_id` (FK) e `(tipo_gatilho, created_at)` para o ranking/série.
    Model `ExpressoQueda`: `$fillable = ['viability_request_id','cnae','tipo_gatilho','dimensao','motivo']`; relação `viabilityRequest()`.
    Em `FluxoExpressoService::encaminharAnalise`, DENTRO da `DB::transaction` existente e APÓS `$this->audit->log(...)`, adicionar a captura (espelhando 15-RESEARCH): se `$resolved !== null`, iterar `$resolved->por_cnae` e, para cada item cujo `fluxo === Fluxo::Analise->value`, ler `consulta_array.risco.encaminhamento` e `ExpressoQueda::create(['viability_request_id'=>$request->id,'cnae'=>$item['cnae'],'tipo_gatilho'=>$enc['gatilhos_acionados'][0]['codigo'] ?? null,'dimensao'=>$enc['dimensao_decisiva'] ?? null,'motivo'=>$enc['motivo'] ?? $reason])`; se `$resolved === null` (toggle off/degradado), gravar 1 linha de nível-processo com `cnae/tipo_gatilho/dimensao` NULL e `motivo=$reason`. NUNCA inventar gatilho. (Verificar o nome exato dos campos do array em RiscoClassificationService antes de cravar as chaves.)
    Teste `ExpressoQuedaCapturaTest`: (a) encaminhamento com `$resolved` real e 1 CNAE caindo por gatilho → grava ExpressoQueda com `tipo_gatilho` correto (do TipoGatilho); (b) encaminhamento com `$resolved === null` → grava 1 linha com `tipo_gatilho IS NULL` (anti-fachada); (c) ANTI-REGRESSÃO: o comportamento de encaminhamento (transição em_analise, auditoria, evento) permanece — rodar os testes existentes de FluxoExpresso e confirmá-los verdes.
  </action>
  <acceptance_criteria>
    - `grep -q "class ExpressoQueda" app/Models/ExpressoQueda.php` e `grep -q "tipo_gatilho" database/migrations/2026_06_15_000004_create_expresso_quedas_table.php`
    - `grep -q "ExpressoQueda::create" app/Services/Expresso/FluxoExpressoService.php`
    - `php artisan test --compact --filter=ExpressoQuedaCapturaTest` sai 0 (gatilho real + null honesto)
    - `php artisan test --compact --filter=FluxoExpresso` sai 0 (anti-regressão Fases 9/10)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ExpressoQuedaService (taxa expressa + ranking + drill-down) + ExpressoQuedaReportSource</name>
  <files>app/Services/Relatorios/ExpressoQuedaService.php, app/Services/Relatorios/Export/Sources/ExpressoQuedaReportSource.php, tests/Feature/Relatorios/ExpressoQuedaServiceTest.php</files>
  <read_first>
    - app/Services/Relatorios/ReportFilters.php (período/cnae/categoria; VO de 15-02) e app/Support/Settings.php (ler relatorios.expresso.meta_taxa/janela_dias)
    - app/Models/ViabilityDecision.php (flow/decided_by_user_id/decided_at) e app/Models/ViabilityRequestTransition.php (elegíveis: protocolada→deferida/indeferida vs →em_analise)
    - app/Services/Analise/ProcessoQueryService.php (filtered — o drill-down cai aqui) e app/Models/AnalysisDivergence.php (divergências analista×motor — drill-down final)
    - 15-CONTEXT.md seção HU-145 (taxa = flow='expresso' & decided_by_user_id IS NULL ÷ elegíveis; série temporal + meta parametrizável RN-004)
  </read_first>
  <action>
    Criar `app/Services/Relatorios/ExpressoQuedaService.php` (route-free). Métodos (recebem `ReportFilters $f`, agregação em SQL):
      - `taxaRespostaExpressa(ReportFilters $f): array` (RN-002): `respondidas = count(viability_decisions where flow='expresso' and decided_by_user_id is null and decided_at in período)`; `elegiveis = count(processos que entraram no expresso = transições protocolada→{deferida|indeferida|em_analise})`; `taxa = elegiveis>0 ? round(respondidas/elegiveis*100,1) : null` (null = "sem base no período", honesto). Incluir `meta` de `Settings::get('relatorios.expresso.meta_taxa')` (null = "meta não definida", RN-004) — NUNCA inventar meta.
      - `serieTemporal(ReportFilters $f): array` — taxa por dia/semana dentro de `relatorios.expresso.janela_dias` (`selectRaw("date(decided_at) ...")` portável), lista ordenada.
      - `rankingMotivos(ReportFilters $f): array` — `ExpressoQueda` `groupBy('tipo_gatilho')` (e dimensao) com `count(*)`, desc; null vira rótulo 'não classificado' (honesto).
      - `drillDown(ReportFilters $f): \Illuminate\Database\Eloquent\Builder` — Builder dos processos que caíram (reusando `ProcessoQueryService::filtered`) para chegar às divergências analista×motor; o detalhe de divergência usa `AnalysisDivergence` no recorte.
    Criar `ExpressoQuedaReportSource implements ReportSource`: exporta o ranking/lista de quedas filtradas (colunas [protocolo, cnae, tipo_gatilho, dimensao, motivo, caiu_em]) sobre o Builder de `ExpressoQueda` join requests; `personalData=false` (sem PII direta); `event='exporta-quedas-expresso'`, `arquivoBase='quedas-expresso'`.
    Teste `ExpressoQuedaServiceTest`: dataset cravado (ex.: 8 elegíveis, 6 respondidas pelo expresso) → `taxa=75.0`; período sem base → `taxa=null`; `meta` reflete o parâmetro (setar via Settings) e fica null quando não definido; `rankingMotivos` agrupa por tipo_gatilho com `count` correto e rotula null como 'não classificado'.
  </action>
  <acceptance_criteria>
    - `grep -q "class ExpressoQuedaService" app/Services/Relatorios/ExpressoQuedaService.php` e `grep -q "function taxaRespostaExpressa" ...`
    - `grep -q "implements ReportSource" app/Services/Relatorios/Export/Sources/ExpressoQuedaReportSource.php`
    - `php artisan test --compact --filter=ExpressoQuedaServiceTest` sai 0 (taxa 75.0, taxa null, meta parametrizável null/definida)
    - `vendor/bin/pint --dirty --format agent` sem pendências
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=ExpressoQueda` verde (captura + serviço) e `--filter=FluxoExpresso` verde (anti-regressão).
- Anti-fachada: gatilho null quando degradado; meta null quando não definida; taxa null sem base.
</verification>

<success_criteria>
Captura estruturada da queda honesta e aditiva (Fases 9/10 verdes) e o relatório de quedas com taxa de resposta expressa, série temporal, meta parametrizável e drill-down — fechando o ciclo de melhoria do expresso sobre dado real.
</success_criteria>

<output>
Criar `.planning/phases/15-relatorios-e-indicadores/15-07-SUMMARY.md` com: as chaves exatas lidas de `encaminhamento` (confirmadas no RiscoClassificationService), o local exato da captura em `encaminharAnalise`, as fórmulas de taxa/elegíveis e a confirmação anti-regressão das Fases 9/10.
</output>

---
phase: 15-relatorios-e-indicadores
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - app/Services/Relatorios/IndicadoresViabilidadeService.php
  - app/Services/Relatorios/Export/Sources/SolicitacoesReportSource.php
  - tests/Feature/Relatorios/IndicadoresViabilidadeServiceTest.php
  - tests/Feature/Relatorios/SolicitacoesReportSourceTest.php
autonomous: true
requirements: [HU-123, HU-124, HU-125, HU-126, HU-127, HU-128, HU-131]
must_haves:
  truths:
    - "Solicitações por período/zona(→bairro)/CNAE/risco são contadas por agregação SQL real sobre viability_requests"
    - "Taxas de deferimento/indeferimento são deferida÷decididas e indeferida÷decididas no período (HU-127/128)"
    - "Zona urbanística indisponível degrada para bairro com rótulo honesto (HU-124 — anti-fachada)"
    - "Sem dados no período retorna zeros/listas vazias honestos, NUNCA número inventado (CA-03)"
    - "SolicitacoesReportSource produz o ReportDefinition do conjunto filtrado (RN-005) reusando ProcessoQueryService::filtered"
  artifacts:
    - path: "app/Services/Relatorios/IndicadoresViabilidadeService.php"
      provides: "Agregações HU-123/124/125/126/127/128 em SQL"
      contains: "class IndicadoresViabilidadeService"
    - path: "app/Services/Relatorios/Export/Sources/SolicitacoesReportSource.php"
      provides: "ReportSource das solicitações filtradas (export CSV/XLSX/PDF)"
      contains: "implements ReportSource"
  key_links:
    - from: "app/Services/Relatorios/IndicadoresViabilidadeService.php"
      to: "viability_requests / viability_decisions"
      via: "groupBy + count/selectRaw com ReportFilters"
      pattern: "groupBy"
---

<objective>
Calcular os indicadores de solicitações (HU-123 período, HU-124 zona→bairro, HU-125 CNAE, HU-126 risco, HU-127 taxa de deferimento, HU-128 taxa de indeferimento) por AGREGAÇÃO SQL real sobre os dados das Fases 8–10, espelhando `ProcessoQueryService` (Builder + `when()` + `groupBy`/`selectRaw`, NUNCA loop PHP). Entregar também o `SolicitacoesReportSource` que transforma o conjunto filtrado no `ReportDefinition` para o export transversal (RN-005), reusando a query da consulta.

Purpose: 6 dos 11 indicadores da fase, todos sobre dado real, com degradação honesta (zona→bairro; sem dados→vazio).
Output: `IndicadoresViabilidadeService` + `SolicitacoesReportSource` + testes com datasets factory de valores cravados.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-relatorios-e-indicadores/15-CONTEXT.md
@.planning/phases/15-relatorios-e-indicadores/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: IndicadoresViabilidadeService (HU-123/124/125/126)</name>
  <files>app/Services/Relatorios/IndicadoresViabilidadeService.php, tests/Feature/Relatorios/IndicadoresViabilidadeServiceTest.php</files>
  <read_first>
    - app/Services/Analise/ProcessoQueryService.php (padrão `when()` + `whereLike(caseSensitive:false)` + `DB::raw('count(*) as total')` + `->map()` tipado em visaoSetor — REUSAR o estilo, agregação SEMPRE em SQL)
    - app/Services/Relatorios/ReportFilters.php (VO de 15-02 — acessores tipados `from()/to()/setorId()/bairro()/cnae()/categoria()/analistaId()` sobre o bag; usar os acessores como métodos)
    - database/migrations/2026_06_14_104830_create_viability_requests_table.php e add_analysis_columns (colunas: protocoled_at, status, sector_id, analysis_category, address_neighborhood, is_virtual_office)
    - database/migrations/2026_06_14_104832_create_viability_request_cnaes_table.php (HU-125: join por cnae) e a migration de risk_classifications/risk_sanitary_classifications (HU-126: nível de risco real por CNAE)
    - 15-CONTEXT.md (HU-124 degrada para bairro — zona bloqueada SEDUR; "número sempre real")
  </read_first>
  <action>
    Criar `app/Services/Relatorios/IndicadoresViabilidadeService.php` (route-free). Métodos (todos recebem `ReportFilters $f` e agregam em SQL com `when()`):
      - `porPeriodo(ReportFilters $f): array` (HU-123): conta `viability_requests` protocoladas por dia/mês no intervalo (`whereBetween('protocoled_at', [$f->from(), $f->to()])` quando informado), `groupBy` na data truncada portável (usar `selectRaw("date(protocoled_at) as dia, count(*) as total")` — válido em SQLite e pgsql) → lista `[dia, total]` ordenada.
      - `porZona(ReportFilters $f): array` (HU-124): `groupBy('address_neighborhood')` com `count(*)`; retornar `['degradacao' => 'bairro', 'rotulo' => 'Zona urbanística indisponível — agrupado por bairro', 'itens' => [...]]` (anti-fachada: rótulo honesto de degradação, nunca zona inventada).
      - `porCnae(ReportFilters $f): array` (HU-125): join `viability_request_cnaes` → `groupBy('cnae')` com `count(distinct viability_request_id)`; top N ordenado desc.
      - `porRisco(ReportFilters $f): array` (HU-126): PREFERIR o nível de risco REAL — join `viability_request_cnaes` → `risk_classifications` (risco municipal vigente por CNAE) e `groupBy` no nível real (baixo_a/baixo_b/alto), agregando por processo (DISTINCT viability_request_id) em SQL. Onde não houver classificação do CNAE, cair no FALLBACK documentado `analysis_category` (categoria derivada consolidada na Fase 10) e, na ausência dela, rotular 'não classificado' (honesto). NUNCA inventar nível de risco. Retornar a fonte usada por linha (real vs derivada) para transparência. Se o join de risco encarecer demais no volume atual, manter o `analysis_category` como base e DOCUMENTAR a limitação no SUMMARY (decisão honesta).
    Aplicar os filtros comuns (setor/bairro/cnae/categoria/analista/período) via `when()` espelhando `ProcessoQueryService`. Dataset vazio → arrays vazios (sem número fabricado).
    Teste: montar `ViabilityRequest::factory()` com protocoled_at/bairro/cnae cravados e asserir contagens EXATAS por período/bairro/cnae/categoria; um teste prova que `porZona` traz o rótulo de degradação para bairro; um teste prova lista vazia quando não há dados no período (CA-03).
  </action>
  <acceptance_criteria>
    - `grep -q "class IndicadoresViabilidadeService" app/Services/Relatorios/IndicadoresViabilidadeService.php`
    - `grep -q "groupBy" app/Services/Relatorios/IndicadoresViabilidadeService.php` e NÃO há `->get()->groupBy(` (agregação em SQL, não em coleção)
    - `php artisan test --compact --filter=IndicadoresViabilidadeServiceTest` sai 0
    - O teste contém um caso de período vazio asserindo array vazio (anti-fachada)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Taxas de deferimento/indeferimento (HU-127/128) + SolicitacoesReportSource (RN-005)</name>
  <files>app/Services/Relatorios/IndicadoresViabilidadeService.php, app/Services/Relatorios/Export/Sources/SolicitacoesReportSource.php, tests/Feature/Relatorios/SolicitacoesReportSourceTest.php, tests/Feature/Relatorios/IndicadoresViabilidadeServiceTest.php</files>
  <read_first>
    - database/migrations/2026_06_14_163233_create_viability_decisions_table.php (outcome deferida/indeferida; decided_at; flow)
    - app/Services/Relatorios/Export/ReportDefinition.php e ReportSource.php (contrato de 15-02: definition(ReportFilters): ReportDefinition com builder/colunas/mapRow)
    - app/Http/Controllers/Gestao/ProcessoController.php::exportarCsv (colunas reais usadas no CSV de processos — alinhar mapRow do source)
    - app/Http/Resources/ProcessoResource.php (campos protocol_number/empresa/cnpj/status_label/categoria/analista/analysis_due_at)
  </read_first>
  <action>
    Em `IndicadoresViabilidadeService`, adicionar:
      - `taxaDeferimento(ReportFilters $f): array` (HU-127): sobre `viability_decisions` join `viability_requests` no intervalo (`decided_at`), `deferidas = count(outcome=deferida)`, `total = count(decididas)`, `taxa = total>0 ? round(deferidas/total*100,1) : null` (null = "sem decisões no período", honesto). Retornar `['deferidas'=>..,'total'=>..,'taxa'=>..]`.
      - `taxaIndeferimento(ReportFilters $f): array` (HU-128): simétrico (indeferidas/total). NUNCA somar a 100 artificialmente — cada uma é sua razão real (em_analise não vira decisão).
    Criar `app/Services/Relatorios/Export/Sources/SolicitacoesReportSource.php` (`implements ReportSource`): `definition(ReportFilters $f)` retorna `ReportDefinition` cujo `builder` = `app(ProcessoQueryService::class)->filtered($f->toProcessoFiltros())` — `toProcessoFiltros()` JÁ EXISTE no `ReportFilters` (15-02) e devolve as 17 chaves do `ProcessoQueryService::filtered`, então o conjunto exportado é EXATAMENTE o filtrado da tela (RN-005), tanto no síncrono quanto no assíncrono (o bag faz round-trip pelo Job). `colunas` = [protocolo, empresa, cnpj, status, categoria, bairro, analista, protocolado_em, decidido_em, resultado]; `mapRow($req)` lê os campos do ProcessoResource (cpf/cnpj minimizado — usar o já mascarado da Resource quando houver PII); `logName='relatorios'`, `event='exporta-solicitacoes'`, `personalData=true`, `arquivoBase='solicitacoes'`. NÃO recortar manualmente o filtro — `toProcessoFiltros()` é a fonte única.
    Testes: `IndicadoresViabilidadeServiceTest` ganha casos de taxa (dataset com 3 deferidas + 1 indeferida → taxa deferimento 75.0, indeferimento 25.0; e período sem decisão → taxa null). `SolicitacoesReportSourceTest`: aplicar um filtro do VOCABULÁRIO DE PROCESSO fora dos 7 do indicador (ex.: `status` ou `protocolo`) via `ReportFilters::fromArray([...])` e provar que `builder()->get()` traz só as linhas do filtro (RN-005 — o bag carrega o vocabulário completo, não só os 7 campos) e que `mapRow` devolve as colunas na ordem de `colunas`.
  </action>
  <acceptance_criteria>
    - `grep -q "function taxaDeferimento(" app/Services/Relatorios/IndicadoresViabilidadeService.php` e `function taxaIndeferimento(`
    - `grep -q "implements ReportSource" app/Services/Relatorios/Export/Sources/SolicitacoesReportSource.php`
    - `php artisan test --compact --filter=IndicadoresViabilidadeServiceTest` sai 0 (inclui taxa 75.0/25.0 e taxa null sem decisão)
    - `php artisan test --compact --filter=SolicitacoesReportSourceTest` sai 0 (prova RN-005)
    - `vendor/bin/pint --dirty --format agent` sem pendências
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=Indicadores` e `--filter=SolicitacoesReportSource` verdes.
- Nenhuma agregação em loop PHP (grep não acha `->get()->groupBy(`).
</verification>

<success_criteria>
HU-123/124/125/126/127/128 calculados sobre dado real com degradação honesta e o ReportSource das solicitações pronto para o export transversal.
</success_criteria>

<output>
Criar `.planning/phases/15-relatorios-e-indicadores/15-03-SUMMARY.md` com as assinaturas dos métodos do serviço e o mapeamento ReportFilters→ProcessoQueryService usado pelo SolicitacoesReportSource.
</output>

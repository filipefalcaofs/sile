---
phase: 15-relatorios-e-indicadores
plan: 05
type: execute
wave: 3
depends_on: ["15-01", "15-02", "15-04"]
files_modified:
  - app/Services/Relatorios/TempoAnaliseService.php
  - app/Services/Relatorios/Export/Sources/TempoAnaliseReportSource.php
  - app/Services/Relatorios/Export/Sources/EscritorioVirtualReportSource.php
  - tests/Feature/Relatorios/TempoAnaliseServiceTest.php
  - tests/Feature/Relatorios/TempoAnaliseReportSourceTest.php
autonomous: true
requirements: [HU-129, HU-131]
must_haves:
  truths:
    - "Tempo por etapa (preenchimento, espera, análise, pendência) é medido em tempo ÚTIL a partir de viability_request_transitions, descontando fins de semana/feriados (HU-129) — elimina a distorção do legado"
    - "Relatório SAPS Tempo de Emissão de TVL = protocoled_at→decided_at onde tvl_product_number IS NOT NULL (RN-006)"
    - "Relatório SAPS Sedes de Escritório Virtual = recorte is_virtual_office=true"
    - "Sem transições/decisões no período retorna vazio honesto, NUNCA tempo inventado (CA-03)"
    - "Os relatórios de tempo são exportáveis pelo contrato único (ReportSource → ReportDefinition, RN-005)"
  artifacts:
    - path: "app/Services/Relatorios/TempoAnaliseService.php"
      provides: "Tempo por etapa (HU-129) + Tempo de Emissão de TVL (SAPS) em tempo útil"
      contains: "class TempoAnaliseService"
    - path: "app/Services/Relatorios/Export/Sources/TempoAnaliseReportSource.php"
      provides: "ReportSource do detalhamento de tempo por processo (export)"
      contains: "implements ReportSource"
  key_links:
    - from: "app/Services/Relatorios/TempoAnaliseService.php"
      to: "app/Services/Expresso/BusinessDeadlineCalculator.php"
      via: "businessDurationBetween por par de transições"
      pattern: "businessDurationBetween"
---

<objective>
HU-129 — medir o tempo POR ETAPA da timeline (não só o total) com a regra de prazo correta (tempo útil, descontando fins de semana/feriados via `businessDurationBetween` de 15-04). O legado reporta 19 dias onde foram 42h reais porque conta calendário e mistura etapas; aqui cada etapa é isolada e o tempo de espera (ex.: BAP) que não é trabalho da SEDUR fica visível. Entregar também os dois relatórios equivalentes do SAPS (RN-006): Tempo de Emissão de TVL e Sedes de Escritório Virtual.

Purpose: indicador de tempo honesto e acionável + paridade com os relatórios administrativos do SAPS, exportáveis pelo contrato único.
Output: `TempoAnaliseService` (tempo por etapa + SAPS) + `TempoAnaliseReportSource` + `EscritorioVirtualReportSource` com testes de datasets cravados (incluindo o caso fim de semana/feriado).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-relatorios-e-indicadores/15-CONTEXT.md
@.planning/phases/15-relatorios-e-indicadores/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TempoAnaliseService — tempo por etapa (tempo útil) + Tempo de Emissão de TVL</name>
  <files>app/Services/Relatorios/TempoAnaliseService.php, tests/Feature/Relatorios/TempoAnaliseServiceTest.php</files>
  <read_first>
    - app/Models/ViabilityRequestTransition.php (colunas from_status/to_status/created_at/viability_request_id — fonte das etapas; ordenar por created_at)
    - app/Services/Expresso/BusinessDeadlineCalculator.php (businessDurationBetween de 15-04 — minutos úteis entre dois instantes)
    - app/Services/Relatorios/ReportFilters.php (período/setor/categoria/analista — from()/to(); VO de 15-02)
    - app/Services/Analise/ProcessoQueryService.php (estilo when()/whereLike; NÃO recriar filtros)
    - database/migrations/2026_06_14_104830_create_viability_requests_table.php + add_analysis_columns (protocoled_at, status; ViabilityDecision tvl_product_number/decided_at/is_virtual_office)
    - 15-RESEARCH.md "Tempo por etapa via transições consecutivas" + Open Questions #3 (caminho portável: parear transições no serviço e aplicar businessDurationBetween — o desconto de tempo útil NÃO é SQL portável; a contagem/listagem segue em SQL)
  </read_first>
  <action>
    Criar `app/Services/Relatorios/TempoAnaliseService.php` (route-free), injetando `BusinessDeadlineCalculator`. Métodos (recebem `ReportFilters $f`):
      - `tempoPorEtapa(ReportFilters $f): array` (HU-129): carregar (via query escopada por período em `protocoled_at`/`created_at`, com `cursor()` para volume) as transições ordenadas por `viability_request_id, created_at`; parear transições consecutivas por processo e classificar a etapa pelo par (from→to): PREENCHIMENTO=`created_at`→`protocoled_at`; ESPERA/ENCAMINHAMENTO=`protocolada`→`em_analise`; ANÁLISE=`em_analise`→decisão (descontando intervalos `em_pendencia`); PENDÊNCIA=somatório `em_pendencia`→`em_analise`. Para cada par aplicar `calculator->businessDurationBetween(inicio, fim)` (tempo ÚTIL). Agregar média/mediana por etapa. IMPORTANTE: o desconto de tempo útil é PHP (não há SQL portável SQLite+pgsql para isso — Open Q #3); a varredura é escopada por período e processada em cursor, NÃO é "loop para contar o que o SQL faria". Retornar `['etapas' => [ ['etapa' => 'analise', 'media_minutos' => .., 'amostras' => ..], ... ]]`.
      - `tempoEmissaoTvl(ReportFilters $f): array` (SAPS, RN-006): sobre `viability_decisions` join `viability_requests` onde `tvl_product_number IS NOT NULL` e `decided_at` no período; duração = `businessDurationBetween(protocoled_at, decided_at)`; retornar média + amostras. Sem registros ⇒ `['media_minutos' => null, 'amostras' => 0]` (honesto, não 0 disfarçado).
    Dataset vazio ⇒ etapas com `amostras=0` e `media_minutos=null`. NUNCA tempo fabricado.
    Teste `TempoAnaliseServiceTest`: montar transições factory cravadas (incl. um par que cruza sábado/domingo) e asserir a média por etapa EXATA em minutos úteis (provando o desconto do fim de semana — anti-distorção do legado); um caso de TVL com `protocoled_at`/`decided_at` cravados → `tempoEmissaoTvl` esperado; um caso de período vazio → `media_minutos=null`/`amostras=0` (CA-03).
  </action>
  <acceptance_criteria>
    - `grep -q "class TempoAnaliseService" app/Services/Relatorios/TempoAnaliseService.php` e `grep -q "businessDurationBetween" app/Services/Relatorios/TempoAnaliseService.php`
    - `grep -q "function tempoPorEtapa" app/Services/Relatorios/TempoAnaliseService.php` e `function tempoEmissaoTvl`
    - `php artisan test --compact --filter=TempoAnaliseServiceTest` sai 0 (inclui o caso fim de semana e o caso vazio com null)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sedes de Escritório Virtual + ReportSources de tempo/SAPS (export RN-005)</name>
  <files>app/Services/Relatorios/TempoAnaliseService.php, app/Services/Relatorios/Export/Sources/TempoAnaliseReportSource.php, app/Services/Relatorios/Export/Sources/EscritorioVirtualReportSource.php, tests/Feature/Relatorios/TempoAnaliseReportSourceTest.php</files>
  <read_first>
    - app/Services/Relatorios/Export/ReportDefinition.php e ReportSource.php (contrato de 15-02: definition(ReportFilters): ReportDefinition com builder/colunas/mapRow/personalData)
    - app/Services/Relatorios/Export/Sources/SolicitacoesReportSource.php (15-03 — modelo de ReportSource concreto: builder filtrado + colunas + mapRow + logName/event)
    - app/Models/ViabilityDecision.php (is_virtual_office, tvl_product_number, decided_at, outcome) e ProcessoQueryService::filtered (reuso da query da tela)
  </read_first>
  <action>
    Em `TempoAnaliseService`, adicionar `sedesEscritorioVirtual(ReportFilters $f): \Illuminate\Database\Eloquent\Builder` — Builder de `viability_requests` (ou decisions join requests) com `where('is_virtual_office', true)` + filtros comuns via `when()`. (Retornar o Builder permite o ReportSource reusar exatamente o mesmo recorte.)
    Criar `EscritorioVirtualReportSource implements ReportSource`: `definition(ReportFilters $f)` usa `app(TempoAnaliseService::class)->sedesEscritorioVirtual($f)` como `builder`; `colunas` = [protocolo, empresa, cnpj, bairro, protocolado_em, decidido_em, resultado]; `mapRow` minimiza PII (cnpj/cpf da Resource); `logName='relatorios'`, `event='exporta-escritorio-virtual'`, `personalData=true`, `arquivoBase='sedes-escritorio-virtual'`.
    Criar `TempoAnaliseReportSource implements ReportSource`: exporta o DETALHAMENTO por processo (protocolo + minutos úteis por etapa + total) sobre o conjunto filtrado — `builder` escopado por período; `colunas` = [protocolo, preenchimento_min, espera_min, analise_min, pendencia_min, total_min]; `mapRow` calcula os minutos por etapa do processo via o mesmo pareamento (reusar um helper privado de Task 1 exposto ao source, ou recomputar por processo); `personalData=false` (sem PII — só protocolo e tempos); `event='exporta-tempo-analise'`, `arquivoBase='tempo-por-etapa'`.
    Teste `TempoAnaliseReportSourceTest`: aplicar um filtro (período/categoria) e provar que `definition($f)->builder()->get()` traz só as linhas do filtro (RN-005) e que `mapRow` devolve as colunas na ordem declarada; provar `EscritorioVirtualReportSource` traz só `is_virtual_office=true`.
  </action>
  <acceptance_criteria>
    - `grep -q "implements ReportSource" app/Services/Relatorios/Export/Sources/TempoAnaliseReportSource.php` e idem `EscritorioVirtualReportSource.php`
    - `grep -q "function sedesEscritorioVirtual" app/Services/Relatorios/TempoAnaliseService.php`
    - `php artisan test --compact --filter=TempoAnaliseReportSourceTest` sai 0 (prova RN-005 e o recorte is_virtual_office)
    - `vendor/bin/pint --dirty --format agent` sem pendências
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=TempoAnalise` verde (serviço + source).
- Nenhuma agregação de CONTAGEM em loop PHP (o loop existente é só para o desconto de tempo útil, justificado).
</verification>

<success_criteria>
HU-129 com tempo por etapa em tempo útil (desconto de fim de semana/feriado provado) e os dois relatórios SAPS, todos exportáveis pelo contrato único, sobre dado real com degradação honesta.
</success_criteria>

<output>
Criar `.planning/phases/15-relatorios-e-indicadores/15-05-SUMMARY.md` com as assinaturas dos métodos do serviço, a classificação exata de cada etapa por par (from→to) e os `arquivoBase`/`event` dos ReportSources de tempo e escritório virtual.
</output>

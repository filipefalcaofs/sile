---
phase: 15-relatorios-e-indicadores
plan: 06
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - app/Services/Relatorios/ProdutividadeAnalistaService.php
  - app/Services/Relatorios/Export/Sources/ProdutividadeReportSource.php
  - tests/Feature/Relatorios/ProdutividadeAnalistaServiceTest.php
  - tests/Feature/Relatorios/ProdutividadeReportSourceTest.php
autonomous: true
requirements: [HU-130, HU-131]
must_haves:
  truths:
    - "Produtividade por analista é contada por agregação SQL real (decisões/processos por decided_by_user_id e assigned_user_id) sobre dado real"
    - "Default é AGREGADO/ANONIMIZADO (sensibilidade RH/LGPD); a visão NOMINAL é um parâmetro do serviço, liberada só com a permissão relatorios.produtividade.nominal (gate no HTTP de 15-09)"
    - "Escopo do próprio analista (scopeUserId) retorna só o recorte dele; gestor vê todos"
    - "Sem decisões no período retorna vazio honesto, NUNCA produtividade inventada (CA-03)"
    - "A exportação respeita a mesma minimização: anônima por default, nominal só sob permissão (RN-007)"
    - "O ProdutividadeReportSource é SyncOnlyReportSource (estado de construtor nominal/escopo) — o ReportExporter força síncrono e nunca perde o estado no Job"
  artifacts:
    - path: "app/Services/Relatorios/ProdutividadeAnalistaService.php"
      provides: "Produtividade por analista (agregado/nominal/escopado)"
      contains: "class ProdutividadeAnalistaService"
    - path: "app/Services/Relatorios/Export/Sources/ProdutividadeReportSource.php"
      provides: "ReportSource da produtividade (anônimo por default)"
      contains: "implements ReportSource"
  key_links:
    - from: "app/Services/Relatorios/ProdutividadeAnalistaService.php"
      to: "viability_decisions / analysis_records"
      via: "groupBy decided_by_user_id / assigned_user_id com count"
      pattern: "groupBy"
---

<objective>
HU-130 — produtividade por analista sobre dado real, em modo CONSERVADOR por sensibilidade RH/LGPD: o default é agregado/anonimizado; a visão NOMINAL (com nome do analista) é liberada só sob a permissão dedicada `relatorios.produtividade.nominal` (criada em 15-01); o analista vê apenas o próprio recorte e o gestor vê todos. Toda consulta é auditada (gate/auditoria no HTTP de 15-09). A política definitiva de quem vê nominal é confirmação SEDUR (registrada; o default conservador não bloqueia).

Purpose: indicador de produtividade real sem expor ranking nominal indevidamente — anti-fachada e LGPD-aware.
Output: `ProdutividadeAnalistaService` (agregado/nominal/escopado) + `ProdutividadeReportSource` (anônimo por default) com testes cravados.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-relatorios-e-indicadores/15-CONTEXT.md
@.planning/phases/15-relatorios-e-indicadores/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProdutividadeAnalistaService (agregado por default, nominal/escopo parametrizados)</name>
  <files>app/Services/Relatorios/ProdutividadeAnalistaService.php, tests/Feature/Relatorios/ProdutividadeAnalistaServiceTest.php</files>
  <read_first>
    - app/Services/Analise/ProcessoQueryService.php (padrão when()/groupBy/DB::raw('count(*)')/->map() tipado — agregação SEMPRE em SQL)
    - app/Services/Relatorios/ReportFilters.php (VO de 15-02 — acessores `from()/to()/setorId()/categoria()/analistaId()`)
    - app/Models/ViabilityDecision.php (decided_by_user_id, outcome, decided_at, flow) e app/Models/AnalysisRecord.php (assigned_user_id/analista da ficha) — fontes da produtividade
    - 15-CONTEXT.md seção "HU-130 — produtividade (modo conservador)" (default anonimizado; nominal sob permissão; analista vê só o próprio)
  </read_first>
  <action>
    Criar `app/Services/Relatorios/ProdutividadeAnalistaService.php` (route-free). Método principal:
      `porAnalista(ReportFilters $f, bool $nominal = false, ?int $scopeUserId = null): array` — agregar em SQL as decisões da análise técnica por analista: `viability_decisions` no período (`decided_at`), `where('flow', 'analise_tecnica')`, `groupBy('decided_by_user_id')`, `selectRaw('decided_by_user_id, count(*) as decididas, sum(case when outcome = ? then 1 else 0 end) as deferidas, ...')` (bind dos outcomes); complementar com contagem de fichas/processos atribuídos via `assigned_user_id` (analysis_records) quando fizer sentido. Aplicar filtros comuns via `when()`. 
      Quando `$scopeUserId !== null` (analista vendo o próprio), filtrar `decided_by_user_id = $scopeUserId` (e ignorar nominal — é o próprio). 
      Quando `$nominal === false`, NÃO expor nome/identidade: rotular cada linha como `Analista #N` (ordinal estável, ex.: índice por ordem de volume) ou hash não reversível; quando `$nominal === true`, incluir `nome` do usuário (join users). 
      Sem decisões no período ⇒ array vazio (não fabricar linha/zero disfarçado).
    Teste `ProdutividadeAnalistaServiceTest`: dataset com 2 analistas e decisões cravadas → contagens EXATAS por analista; `nominal=false` ⇒ as linhas NÃO contêm `nome` (assert ausência da chave/identidade) e SIM um rótulo anônimo; `nominal=true` ⇒ contém `nome`; `scopeUserId` ⇒ só o recorte do próprio; período vazio ⇒ array vazio (CA-03).
  </action>
  <acceptance_criteria>
    - `grep -q "class ProdutividadeAnalistaService" app/Services/Relatorios/ProdutividadeAnalistaService.php` e `grep -q "groupBy" ...` sem `->get()->groupBy(`
    - `grep -q "function porAnalista" app/Services/Relatorios/ProdutividadeAnalistaService.php`
    - `php artisan test --compact --filter=ProdutividadeAnalistaServiceTest` sai 0 (inclui anônimo-por-default, nominal, escopo e vazio)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ProdutividadeReportSource (export anônimo por default; nominal sob permissão)</name>
  <files>app/Services/Relatorios/Export/Sources/ProdutividadeReportSource.php, tests/Feature/Relatorios/ProdutividadeReportSourceTest.php</files>
  <read_first>
    - app/Services/Relatorios/Export/ReportDefinition.php, ReportSource.php e SyncOnlyReportSource.php (contrato de 15-02; o ReportExporter FORÇA síncrono para SyncOnlyReportSource — não despacha o Job, preservando o estado de construtor)
    - app/Services/Relatorios/Export/Sources/SolicitacoesReportSource.php (15-03 — modelo de ReportSource concreto)
    - 15-02-SUMMARY.md (guarda SyncOnly: sources com estado de construtor implementam o marcador; ReportExporter força síncrono)
  </read_first>
  <action>
    Criar `ProdutividadeReportSource implements ReportSource, SyncOnlyReportSource` (marcador de 15-02) com construtor `(bool $nominal = false, ?int $scopeUserId = null)` (property promotion). Implementar `SyncOnlyReportSource` é a CORREÇÃO do checker para o estado de construtor: o `ReportExporter` força o caminho síncrono e NUNCA despacha o Job (que reconstruiria via `app($sourceClass)` e perderia `nominal`/`scopeUserId`). `definition(ReportFilters $f)` monta um `ReportDefinition` cujo `builder` reproduz a MESMA agregação de `porAnalista` em forma de Builder (ou um Builder de `viability_decisions` agregado equivalente) respeitando `nominal`/`scopeUserId`; `colunas` anônimas por default ([analista_rotulo, decididas, deferidas, indeferidas]) e com [analista_nome] só quando `nominal=true`; `mapRow` espelha; `personalData = $this->nominal` (nominal expõe identidade → herda meta-auditoria PII); `logName='relatorios'`, `event='exporta-produtividade'`, `arquivoBase='produtividade-analistas'`. (Volume = nº de analistas, pequeno; o marcador SyncOnly garante o síncrono independentemente do volume.)
    Teste `ProdutividadeReportSourceTest`: `new ProdutividadeReportSource(nominal:false)` → `definition($f)` produz colunas SEM nome (assert ausência de `analista_nome` nas colunas) e `personalData=false`; `new ProdutividadeReportSource(nominal:true)` → inclui `analista_nome` e `personalData=true`; com `scopeUserId` → builder traz só o recorte do próprio (RN-005/RN-007); e que o source é `instanceof SyncOnlyReportSource` (garante o caminho síncrono no ReportExporter — preserva nominal/escopo).
  </action>
  <acceptance_criteria>
    - `grep -q "implements ReportSource" app/Services/Relatorios/Export/Sources/ProdutividadeReportSource.php` e `grep -q "SyncOnlyReportSource" app/Services/Relatorios/Export/Sources/ProdutividadeReportSource.php`
    - `php artisan test --compact --filter=ProdutividadeReportSourceTest` sai 0 (anônimo default, nominal sob flag, escopo, marcador SyncOnly)
    - `vendor/bin/pint --dirty --format agent` sem pendências
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=Produtividade` verde (serviço + source).
- Anti-fachada/LGPD: default sem nome; nominal só sob flag; vazio quando sem dado.
</verification>

<success_criteria>
HU-130 calculada sobre dado real com default anonimizado, visão nominal parametrizada e escopo do próprio analista, exportável com a mesma minimização — pronto para o gate de permissão e a auditoria no HTTP (15-09).
</success_criteria>

<output>
Criar `.planning/phases/15-relatorios-e-indicadores/15-06-SUMMARY.md` com a assinatura de `porAnalista` (nominal/scopeUserId), como o rótulo anônimo é gerado, e a nota de que o `ProdutividadeReportSource` implementa `SyncOnlyReportSource` (estado de construtor nominal/escopo preservado pelo caminho síncrono forçado; 15-09 resolve nominal pela permissão).
</output>

---
phase: 15-relatorios-e-indicadores
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Relatorios/Export/ReportDefinition.php
  - app/Services/Relatorios/Export/ReportSource.php
  - app/Services/Relatorios/Export/ReportFormatExporter.php
  - app/Services/Relatorios/Export/CsvExporter.php
  - app/Services/Relatorios/Export/PdfExporter.php
  - app/Services/Relatorios/Export/ReportExporter.php
  - resources/views/relatorios/relatorio.blade.php
  - app/Models/ExportFile.php
  - database/migrations/2026_06_15_000002_create_export_files_table.php
  - database/factories/ExportFileFactory.php
  - app/Jobs/GerarExportacaoJob.php
  - app/Notifications/ExportacaoPronta.php
  - tests/Feature/Relatorios/ReportExporterTest.php
  - tests/Feature/Relatorios/GerarExportacaoJobTest.php
autonomous: true
requirements: [HU-131]
must_haves:
  truths:
    - "Um único contrato de exportação (ReportDefinition + ReportSource + ReportExporter + drivers) paga a dívida transversal da HU-131"
    - "CSV e PDF geram conteúdo REAL do conjunto filtrado; o PDF traz 'Total de registros: N' + data/hora + filtros (RN-010/CA-07)"
    - "Abaixo do limiar a exportação é síncrona (streaming); acima dispara GerarExportacaoJob (CA-06/RN-006)"
    - "Toda exportação é auditada via AuditService (RN-008); export falho chama failed() auditado e NÃO gera link válido"
  artifacts:
    - path: "app/Services/Relatorios/Export/ReportExporter.php"
      provides: "Orquestrador sync/async + auditoria"
      contains: "function export("
    - path: "app/Jobs/GerarExportacaoJob.php"
      provides: "Job de export assíncrono (tries/timeout/backoff/failed audita)"
      contains: "public function failed("
    - path: "resources/views/relatorios/relatorio.blade.php"
      provides: "Blade genérico de relatório PDF com rodapé de total"
      contains: "Total de registros"
  key_links:
    - from: "app/Services/Relatorios/Export/ReportExporter.php"
      to: "app/Jobs/GerarExportacaoJob.php"
      via: "dispatch acima do limiar"
      pattern: "GerarExportacaoJob::dispatch"
    - from: "app/Jobs/GerarExportacaoJob.php"
      to: "App\\Notifications\\ExportacaoPronta"
      via: "notify após gravar o arquivo"
      pattern: "ExportacaoPronta"
---

<objective>
Pagar a HU-131 como CONTRATO ÚNICO de exportação — o coração da fase e a dívida que as Fases 8–12 vinham diferindo. `ReportDefinition` (DTO do que exportar) + `ReportSource` (quem constrói a definition a partir de `ReportFilters`) + `ReportFormatExporter` (1 driver/formato) + `ReportExporter` (orquestra síncrono streaming OU `GerarExportacaoJob` acima do limiar) + `ExportFile` (arquivo gerado, baixável por URL assinada, prunable). CSV e PDF nascem aqui; XLSX (openspout) entra em 15-08; o endpoint de download e as rotas em 15-09.

Purpose: qualquer tela ganha export CSV/XLSX/PDF do conjunto filtrado adicionando um `ReportSource` + branch `?formato=` no controller, sem reimplementar (RN-009).
Output: contrato + drivers CSV/PDF + Blade genérico + ReportExporter + Job + ExportFile + Notification, com testes provando RN-005/006/008 e o anti-fachada (export falho não vira "pronto").
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-relatorios-e-indicadores/15-CONTEXT.md
@.planning/phases/15-relatorios-e-indicadores/15-RESEARCH.md
@docs/SILE_HUs_Completas_MD/EP15-Relatórios-e-Indicadores/HU-131-Exportar-relatorios.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contrato — ReportDefinition + ReportSource + ReportFormatExporter + ExportFile</name>
  <files>app/Services/Relatorios/Export/ReportDefinition.php, app/Services/Relatorios/Export/ReportSource.php, app/Services/Relatorios/Export/ReportFormatExporter.php, app/Models/ExportFile.php, database/migrations/2026_06_15_000002_create_export_files_table.php, database/factories/ExportFileFactory.php</files>
  <read_first>
    - app/Services/Relatorios/ReportFilters.php (VO de 15-01 — entrada do ReportSource)
    - app/Services/Analise/TvlPdfService.php (padrão de disco NÃO-público parametrizado + guarda anti-'public'; modelo TvlDocument como precedente do ExportFile)
    - app/Http/Controllers/Gestao/AuditoriaController.php::export (colunas + streaming CSV existente — o CsvExporter consolida este padrão)
    - 15-RESEARCH.md "Pattern 2" e "Code Examples" (assinaturas builder()/columnLabels()/mapRow()/fileName())
  </read_first>
  <action>
    Criar `app/Services/Relatorios/Export/ReportDefinition.php` como `final readonly class` carregando: `string $titulo`, `array $colunas` (lista de `['key' => ..., 'label' => ...]`), `\Closure $builder` (() => Builder filtrado da tela — RN-005), `\Closure $mapRow` (fn (Model): array<scalar> alinhado a $colunas), `array $filtrosAplicados`, `string $logName`, `string $event`, `bool $personalData`, `string $arquivoBase`. Métodos: `columnLabels(): array`, `builder(): \Illuminate\Database\Eloquent\Builder`, `mapRow($model): array`, `fileName(string $ext): string` (arquivoBase + data + ext).
    Criar `app/Services/Relatorios/Export/ReportSource.php` (interface): `public function definition(ReportFilters $filtros): ReportDefinition;`. Os ReportSources concretos (Solicitacoes/Quedas) vêm nas waves seguintes; o Job reconstrói via `app($sourceClass)->definition(ReportFilters::fromArray($filtros))` (closures NUNCA são serializadas — só sourceClass + filtros).
    Criar `app/Services/Relatorios/Export/ReportFormatExporter.php` (interface): `public function stream(ReportDefinition $definition): \Symfony\Component\HttpFoundation\Response;` (síncrono, ao navegador) e `public function write(ReportDefinition $definition, string $absolutePath): void;` (assíncrono, grava no caminho local).
    Criar o model `ExportFile` (`php artisan make:model ExportFile -mf`): migration `create_export_files_table` com `user_id` (nullable, nullOnDelete), `disk`, `path`, `filename`, `format`, `row_count` (int), `filtros` (jsonb), `timestamps`; model com `#[Fillable([...])]` e cast `filtros => 'array'`; factory plausível. `ExportFile` é o registro real do arquivo gerado (espelha TvlDocument) — fonte do download assinado (15-09) e do pruning de retenção (15-15).
  </action>
  <acceptance_criteria>
    - `grep -q "final readonly class ReportDefinition" app/Services/Relatorios/Export/ReportDefinition.php`
    - `grep -q "interface ReportFormatExporter" app/Services/Relatorios/Export/ReportFormatExporter.php` e contém `function stream(` e `function write(`
    - `grep -q "interface ReportSource" app/Services/Relatorios/Export/ReportSource.php`
    - `php artisan migrate --no-interaction` sai 0 e `php artisan tinker --execute 'echo \Schema::hasColumn("export_files","row_count") ? "ok" : "no";'` imprime ok
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Drivers síncronos — CsvExporter + PdfExporter + Blade genérico (rodapé Total de registros: N)</name>
  <files>app/Services/Relatorios/Export/CsvExporter.php, app/Services/Relatorios/Export/PdfExporter.php, resources/views/relatorios/relatorio.blade.php</files>
  <read_first>
    - app/Http/Controllers/Gestao/AuditoriaController.php::exportarAtividades (streamDownload + fputcsv + chunk(200) — padrão CSV a consolidar)
    - app/Services/Analise/TvlPdfService.php (Pdf::loadView()->setPaper(config('sile...paper'),(...orientation))->output(); disco não-público)
    - resources/views/tvl/documento.blade.php (estrutura de Blade de PDF do projeto — espelhar layout/typografia)
    - 15-RESEARCH.md "Pitfall 4" (openspout precisa de caminho real — só relevante para XLSX em 15-08; CSV usa php://output no stream e file handle no write)
  </read_first>
  <action>
    `CsvExporter implements ReportFormatExporter`: `stream()` → `response()->streamDownload(...)` escrevendo cabeçalho (`$definition->columnLabels()`) e `$definition->builder()->chunk(config('sile.relatorios.export.chunk', 200), ...)` com `fputcsv` de `$definition->mapRow($model)`; `write($definition, $path)` → abre `fopen($path,'w')`, mesmo loop com `cursor()`/`chunk`, `fclose`. Sem linha de total no corpo (RN-010 — CSV/XLSX preservam integridade tabular).
    `PdfExporter implements ReportFormatExporter`: `stream()` → `Pdf::loadView('relatorios.relatorio', $this->dados($definition))->setPaper(config('sile.relatorios.export.pdf.paper','a4'), config('sile.relatorios.export.pdf.orientation','portrait'))->stream($definition->fileName('pdf'))`; `write()` → `...->output()` gravado em `$path` via `file_put_contents`. O `dados()` carrega `titulo`, `colunas` (labels), `linhas` (map de todas as linhas do builder — usar `->get()` no PDF pois precisa do total e o PDF é abaixo do limiar), `total` (count), `geradoEm` (now), `filtros` ($definition->filtrosAplicados).
    Criar `resources/views/relatorios/relatorio.blade.php`: tabela com cabeçalho e linhas, e ao FINAL do documento o rodapé "Total de registros: {{ $total }}" + "Gerado em: {{ $geradoEm }}" + a lista de filtros aplicados (CA-07/RN-010).
  </action>
  <acceptance_criteria>
    - `grep -q "class CsvExporter implements ReportFormatExporter" app/Services/Relatorios/Export/CsvExporter.php`
    - `grep -q "class PdfExporter implements ReportFormatExporter" app/Services/Relatorios/Export/PdfExporter.php`
    - `grep -q "Total de registros" resources/views/relatorios/relatorio.blade.php`
    - teste (Task 3) prova: CSV começa com a linha de cabeçalho esperada; PDF começa com `%PDF` e contém "Total de registros: " coerente
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: ReportExporter (sync/async + auditoria) + GerarExportacaoJob + ExportacaoPronta + testes</name>
  <files>app/Services/Relatorios/Export/ReportExporter.php, app/Jobs/GerarExportacaoJob.php, app/Notifications/ExportacaoPronta.php, tests/Feature/Relatorios/ReportExporterTest.php, tests/Feature/Relatorios/GerarExportacaoJobTest.php</files>
  <read_first>
    - app/Jobs/DecidirFluxoExpressoJob.php (padrão tries/timeout/backoff de config('sile...job.*'), onQueue, failed() que AUDITA via AuditService — espelhar)
    - app/Support/Audit/AuditService.php (assinatura log(logName,event,description,properties,subject,result,rulesVersion,personalData))
    - app/Http/Controllers/Gestao/TvlDocumentController.php (download por URL assinada — o link da Notification aponta para a rota gestao.relatorios.exportacoes.download criada em 15-09)
    - 15-RESEARCH.md "Open Questions" #1 (Notification STANDALONE database+mail — NÃO usar NotificationDispatcher, que é process-bound)
  </read_first>
  <action>
    `ReportExporter`: `public function export(ReportSource $source, ReportFilters $filtros, string $formato, ?User $user = null): \Symfony\Component\HttpFoundation\Response`. Passos: valida `$formato` contra `relatorios.export.formatos_habilitados` (Settings, fallback ['csv','pdf'] enquanto xlsx não existe — 15-08 amplia); `$definition = $source->definition($filtros)`; `$total = (clone)$definition->builder()->count()`; AUDITA sempre (logName do definition, event 'exporta-'.$formato, properties: tela/filtros/formato/volume, personalData do definition). Se `$total <= Settings::get('relatorios.export.assincrono_limiar_linhas', config('sile.relatorios.export.assincrono_limiar_linhas', 5000))` → retorna `$this->driver($formato)->stream($definition)` (síncrono). Senão → `GerarExportacaoJob::dispatch($source::class, $filtros->toArray(), $formato, $user?->id)` e retorna `back()->with('flash', ...)` ou `response()->noContent(202)` (CA-06: tela não trava). Resolver driver por `match($formato){ 'csv'=>app(CsvExporter::class), 'pdf'=>app(PdfExporter::class), default=>throw new \InvalidArgumentException(...) }` (15-08 adiciona 'xlsx').
    `GerarExportacaoJob implements ShouldQueue` (espelha DecidirFluxoExpressoJob): ctor `(string $sourceClass, array $filtros, string $formato, ?int $userId)` setando tries/timeout/backoff/fila de `config('sile.relatorios.job.*')`; `handle()`: reconstrói `$source = app($sourceClass)`, `$definition = $source->definition(ReportFilters::fromArray($filtros))`, resolve disco `relatorios.export.disk` (NÃO público), grava num caminho local (`Storage::disk($disk)->path($rel)` quando local; senão `tempnam()` + `Storage::put`), driver->write, cria `ExportFile` (row_count = count) e notifica o usuário com `ExportacaoPronta` (se userId); `failed(?Throwable)`: AUDITA result='falha' (RN-002), SEM criar ExportFile/link (anti-fachada — Pitfall 6).
    `ExportacaoPronta` (`php artisan make:notification ExportacaoPronta`): canais `['database','mail']`; `toMail`/`toArray` com link `URL::temporarySignedRoute('gestao.relatorios.exportacoes.download', now()->addMinutes(...), ['exportFile' => $this->exportFile->id])` (a rota nasce em 15-09 — nos testes deste plano usar `Notification::fake()`).
    Testes: `ReportExporterTest` — (a) dataset pequeno → resposta de streaming (assertStatus 200, header Content-Type) e auditoria gravada; (b) dataset acima do limiar (rebaixar via `config(['sile.relatorios.export.assincrono_limiar_linhas' => 1])`) → `Queue::fake()` assertPushed(GerarExportacaoJob); (c) CSV contém exatamente as linhas do builder filtrado (RN-005: filtrar e provar que linha fora do filtro NÃO aparece); (d) PDF começa com '%PDF' e contém 'Total de registros: '. `GerarExportacaoJobTest` — handle grava arquivo no disco fake + cria ExportFile + dispara ExportacaoPronta (Notification::fake); e que `failed()` audita 'falha' e NÃO cria ExportFile.
  </action>
  <acceptance_criteria>
    - `grep -q "function export(" app/Services/Relatorios/Export/ReportExporter.php` e `grep -q "GerarExportacaoJob::dispatch" app/Services/Relatorios/Export/ReportExporter.php`
    - `grep -q "public function failed(" app/Jobs/GerarExportacaoJob.php`
    - `grep -q "ExportacaoPronta" app/Jobs/GerarExportacaoJob.php`
    - `php artisan test --compact --filter=ReportExporterTest` sai 0 e `php artisan test --compact --filter=GerarExportacaoJobTest` sai 0
    - `vendor/bin/pint --dirty --format agent` sem pendências
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=Relatorios` verde (ReportExporter + GerarExportacaoJob + ReportFilters).
- `vendor/bin/pint --dirty --format agent` limpo.
- Dimensão 3 (RESEARCH/VALIDATION): CSV/PDF válidos; limiar assíncrono com Queue::fake; export falho não gera link.
</verification>

<success_criteria>
Contrato de exportação único e testado (CSV+PDF), com decisão sync/async parametrizada, auditoria de toda exportação e job resiliente que audita falha sem fabricar "pronto". XLSX e endpoint de download entram em 15-08/15-09.
</success_criteria>

<output>
Criar `.planning/phases/15-relatorios-e-indicadores/15-02-SUMMARY.md` registrando: assinaturas finais de `ReportDefinition`/`ReportSource`/`ReportFormatExporter`/`ReportExporter::export`, o nome da rota de download que 15-09 deve criar (`gestao.relatorios.exportacoes.download`), e a decisão de Notification standalone (não NotificationDispatcher).
</output>

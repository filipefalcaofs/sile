---
phase: 15-relatorios-e-indicadores
plan: 12
type: execute
wave: 5
depends_on: ["15-09"]
files_modified:
  - resources/js/components/ui/chart/echarts-core.ts
  - resources/js/components/ui/chart/chart-impl.tsx
  - resources/js/components/ui/chart/chart.tsx
  - resources/js/components/ui/data-table/export-menu.tsx
  - resources/js/components/ui/data-table/use-server-table.ts
autonomous: true
requirements: [HU-122, HU-131]
must_haves:
  truths:
    - "O wrapper de gráfico ECharts é CLIENT-ONLY e SSR-safe (lazy + mounted + skeleton) — não importa echarts no caminho do servidor do Inertia v3"
    - "O tema do gráfico acompanha o DS (classe dark no <html>), trocando claro/escuro sem recriar a instância"
    - "O bundle usa import tree-shakeable (echarts/core + echarts.use), não o pacote cheio"
    - "O <ExportMenu> monta {url do index}?formato=csv|xlsx|pdf preservando os filtros atuais (RN-004) e é reusável por qualquer tela com useServerTable+DataTable"
  artifacts:
    - path: "resources/js/components/ui/chart/chart.tsx"
      provides: "Wrapper de gráfico client-only SSR-safe"
      contains: "useState"
    - path: "resources/js/components/ui/data-table/export-menu.tsx"
      provides: "Dropdown de exportação CSV/XLSX/PDF do conjunto filtrado"
      contains: "formato"
  key_links:
    - from: "resources/js/components/ui/chart/chart.tsx"
      to: "resources/js/components/ui/chart/chart-impl.tsx"
      via: "lazy() import só no cliente"
      pattern: "lazy("
    - from: "resources/js/components/ui/data-table/export-menu.tsx"
      to: "resources/js/components/ui/data-table/use-server-table.ts"
      via: "lê os params atuais para montar ?formato="
      pattern: "formato"
---

<objective>
Fundações de frontend reusáveis da fase: o wrapper de gráfico Apache ECharts client-only SSR-safe (decisão travada — única lib de chart do projeto) espelhando o padrão do mapa react-leaflet da Fase 4, e o `<ExportMenu>` (irmão de `table-toolbar`/`per-page-select`) que dá a qualquer tela o dropdown CSV/XLSX/PDF apontando para `{url do index}?formato=` com os filtros atuais. Com estes dois prontos, o dashboard (15-13) e as páginas de relatório (15-14) só montam telas.

Purpose: gráficos sem quebrar o SSR + export de 1 linha em qualquer datatable.
Output: `ui/chart/*` (core tree-shake + impl + guarda SSR) + `ui/data-table/export-menu.tsx` + exposição dos params atuais no `useServerTable`, com `tsc`/`build` verdes.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-relatorios-e-indicadores/15-CONTEXT.md
@.planning/phases/15-relatorios-e-indicadores/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrapper ECharts client-only SSR-safe (core tree-shake + impl + guarda) com tema do DS</name>
  <files>resources/js/components/ui/chart/echarts-core.ts, resources/js/components/ui/chart/chart-impl.tsx, resources/js/components/ui/chart/chart.tsx</files>
  <read_first>
    - resources/js/components/geo/mapa-section.tsx (padrão EXATO de client-only do projeto: lazy() + flag mounted + skeleton de mesma altura — Pitfall 9; espelhar)
    - resources/js/components/geo/map-imovel.tsx (como o componente client-only inicializa a lib que toca o DOM e limpa no unmount)
    - 15-RESEARCH.md "Code Examples — Wrapper ECharts" (echarts-core.ts com echarts.use([...]); chart-impl com echarts.init/setOption/ResizeObserver/dispose; chart.tsx com lazy+Suspense+mounted) e "Pitfall 5" (tema preso ao SO; ler classe `dark` do <html> e observar com MutationObserver)
    - resources/js/app.tsx (confirmar SSR ativo do Inertia v3 — por isso o gráfico precisa ser client-only)
  </read_first>
  <action>
    Criar `resources/js/components/ui/chart/echarts-core.ts`: importar de `echarts/core` + os charts/components/renderers usados (`BarChart, LineChart, PieChart`, `GridComponent, TooltipComponent, LegendComponent, TitleComponent`, `CanvasRenderer`) e `echarts.use([...])`; exportar `{ echarts }`. (Tree-shake — NÃO importar o pacote `echarts` cheio.)
    Criar `resources/js/components/ui/chart/chart-impl.tsx`: componente que recebe `option: EChartsOption` + `theme: 'dark'|'light'`; em `useEffect` faz `echarts.init(el, theme === 'dark' ? 'dark' : undefined)`, `setOption`, observa resize com `ResizeObserver`, e no cleanup `dispose()`. Determinar o tema lendo `document.documentElement.classList.contains('dark')` e re-aplicar via `MutationObserver` na classe do `<html>` (Pitfall 5 — NUNCA prefers-color-scheme).
    Criar `resources/js/components/ui/chart/chart.tsx`: `const ChartImpl = lazy(() => import('./chart-impl')...)`; estado `mounted` (useEffect setMounted true); enquanto `!mounted` renderiza skeleton `animate-pulse` de mesma altura; depois `<Suspense fallback={skeleton}><ChartImpl .../></Suspense>`. Exportar `<Chart option={...} />` (theme resolvido internamente pela classe dark).
  </action>
  <acceptance_criteria>
    - `grep -q "echarts.use(" resources/js/components/ui/chart/echarts-core.ts` e `grep -q "from 'echarts/core'" resources/js/components/ui/chart/echarts-core.ts`
    - `grep -q "lazy(" resources/js/components/ui/chart/chart.tsx` e `grep -q "mounted" resources/js/components/ui/chart/chart.tsx`
    - `grep -q "classList.contains('dark')\|MutationObserver" resources/js/components/ui/chart/chart-impl.tsx`
    - NÃO há import do pacote cheio em chart.tsx no caminho do servidor (`! grep -q "from 'echarts'" resources/js/components/ui/chart/chart.tsx`)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: <ExportMenu> (CSV/XLSX/PDF do conjunto filtrado) + exposição dos params do useServerTable + smoke build</name>
  <files>resources/js/components/ui/data-table/export-menu.tsx, resources/js/components/ui/data-table/use-server-table.ts</files>
  <read_first>
    - resources/js/components/ui/data-table/use-server-table.ts (o objeto `params` montado em `visit` — sort/direction/per_page/search/filtros; EXPOR esse snapshot para o ExportMenu montar a URL de export)
    - resources/js/components/ui/data-table/table-toolbar.tsx e per-page-select.tsx (irmãos — estilo/props do DS TailAdmin a espelhar no ExportMenu)
    - resources/js/components/ui/dropdown.tsx (ou o componente de dropdown do DS, se houver — reusar para o menu)
    - 15-CONTEXT.md (ExportMenu monta {url do index}?formato={fmt}&...paramsAtuais; qualquer tela com useServerTable+DataTable ganha export)
  </read_first>
  <action>
    No `useServerTable`, expor os parâmetros atuais para reuso na exportação: retornar adicionalmente `currentParams` (o MESMO objeto montado em `visit`: sort/direction/per_page + search + filtros não-vazios) — sem mudar o comportamento de navegação existente.
    Criar `resources/js/components/ui/data-table/export-menu.tsx`: `<ExportMenu url={indexUrl} params={currentParams} formatos={['csv','xlsx','pdf']} />` — dropdown (estilo do DS) onde cada item é um link/âncora para `${url}?${new URLSearchParams({ ...params, formato })}` (CSV/XLSX baixam direto; PDF idem; acima do limiar o backend responde 202 e a notificação leva ao download — comportamento do ReportExporter). Acessível (aria), com rótulos pt-BR ("Exportar CSV/Excel/PDF").
    Smoke de build: rodar `npx tsc --noEmit` e `npm run build` com os novos componentes — verdes (o wrapper de chart não pode quebrar o SSR/typecheck).
  </action>
  <acceptance_criteria>
    - `grep -q "currentParams" resources/js/components/ui/data-table/use-server-table.ts`
    - `grep -q "formato" resources/js/components/ui/data-table/export-menu.tsx` e `grep -q "URLSearchParams" resources/js/components/ui/data-table/export-menu.tsx`
    - `npx tsc --noEmit` sai 0 e `npm run build` sai 0 (SSR/typecheck verdes com chart + export-menu)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` e `npm run build` verdes (Dimensão 6 — SSR/Front: chart client-only não quebra o build).
- Wrapper de chart espelha o client-only do mapa (lazy+mounted+skeleton); ExportMenu reusável montando ?formato= com filtros atuais.
</verification>

<success_criteria>
Gráfico ECharts SSR-safe com tema do DS e dropdown de exportação reusável prontos — base para o dashboard (15-13) e as páginas de relatório (15-14) sem reimplementar chart/export.
</success_criteria>

<output>
Criar `.planning/phases/15-relatorios-e-indicadores/15-12-SUMMARY.md` com: a API final de `<Chart>` e `<ExportMenu>` (props), o conjunto de módulos ECharts registrados em echarts-core, e como o `currentParams` é exposto pelo useServerTable.
</output>

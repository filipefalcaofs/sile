---
phase: 15-relatorios-e-indicadores
plan: 15
type: execute
wave: 7
depends_on: ["15-09", "15-10", "15-11", "15-13", "15-14"]
files_modified:
  - app/Models/ExportFile.php
  - routes/console.php
  - app/Console/Commands/RelatoriosExportarCommand.php
  - database/seeders/RelatoriosDevSeeder.php
  - database/seeders/DatabaseSeeder.php
  - tests/Feature/Relatorios/ExportRetencaoPruningTest.php
  - tests/Feature/Relatorios/RelatoriosGoldenSmokeTest.php
autonomous: false
requirements: [HU-131, HU-122, HU-129, HU-145]
must_haves:
  truths:
    - "Os arquivos de exportação são podados por retenção parametrizada (relatorios.export.retencao_dias) em rotina agendada idempotente — sem crescimento ilimitado do disco"
    - "Um comando de evidência gera um arquivo REAL de cada formato (CSV/XLSX/PDF) a partir de seeds, provando o export end-to-end (lógica real, não fachada)"
    - "Seeds de dev populam relatórios pelo FLUXO REAL (decisões/transições/quedas), nunca números cravados na tela"
    - "Verificação integral fresca: composer test (2 processos) verde, pint limpo, npm run build verde; baselines de parâmetros/permissões/testes atualizados"
    - "Guardião-entrega dá veredito APROVADO com evidência fresca antes de marcar a fase concluída"
  artifacts:
    - path: "app/Console/Commands/RelatoriosExportarCommand.php"
      provides: "Evidência end-to-end de export real (1 arquivo por formato)"
      contains: "relatorios:exportar"
    - path: "routes/console.php"
      provides: "Agendamento do pruning de retenção dos exports"
      contains: "retencao_dias"
  key_links:
    - from: "routes/console.php"
      to: "app/Models/ExportFile.php"
      via: "pruning por retenção (MassPrunable)"
      pattern: "prune"
---

<objective>
Fechamento da Fase 15: pruning de retenção dos arquivos de exportação (padrão Fase 3.1), comando de evidência que gera um arquivo REAL de cada formato a partir de seeds (prova anti-fachada do export end-to-end), seeds de dev pelo fluxo real, e a verificação integral fresca nos 2 processos + veredito do guardião-entrega. DONO ÚNICO de `routes/console.php` na fase.

Purpose: garantir que o export não cresce sem limite, que a lógica de export é real ponta-a-ponta, e que a fase está honestamente pronta.
Output: `ExportFile` prunable + agendamento + `relatorios:exportar` + seeds dev + golden/smoke + verificação integral + guardião + SUMMARY de fechamento.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-relatorios-e-indicadores/15-CONTEXT.md
@.planning/phases/15-relatorios-e-indicadores/15-RESEARCH.md
@.planning/phases/15-relatorios-e-indicadores/15-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pruning de retenção dos exports (ExportFile MassPrunable + agendamento)</name>
  <files>app/Models/ExportFile.php, routes/console.php, tests/Feature/Relatorios/ExportRetencaoPruningTest.php</files>
  <read_first>
    - routes/console.php (padrão Fase 3.1: pruning agendado de access_logs com retenção parametrizada — espelhar para os exports; DONO ÚNICO deste arquivo na fase)
    - app/Models/ExportFile.php (15-02: disk/path; aplicar MassPrunable + prunable() por created_at < now()-retencao; ao podar, REMOVER o arquivo do Storage também — não deixar órfão)
    - app/Models/AccessLog.php (modelo de MassPrunable do projeto, se for o usado na 3.1; espelhar a estratégia)
    - app/Support/Settings.php (ler relatorios.export.retencao_dias com fallback config)
  </read_first>
  <action>
    Em `ExportFile`, aplicar `Illuminate\Database\Eloquent\MassPrunable` (ou `Prunable` se precisar remover o arquivo físico — preferir `Prunable` com `pruning()` que faz `Storage::disk($this->disk)->delete($this->path)` para não deixar arquivo órfão) e `prunable()` => `where('created_at', '<', now()->subDays((int) Settings::get('relatorios.export.retencao_dias', config('sile.relatorios.export.retencao_dias', 7))))`.
    Em `routes/console.php`, agendar `model:prune --model=...ExportFile` (ou um comando dedicado) diariamente, idempotente, espelhando o pruning de access_logs da Fase 3.1.
    Teste `ExportRetencaoPruningTest`: criar ExportFiles antigos (> retenção) e recentes em `Storage::fake`; rodar o prune; provar que os antigos somem (registro E arquivo) e os recentes permanecem; mudar `relatorios.export.retencao_dias` via Settings muda o corte (parametrização sem deploy — Dimensão 5).
  </action>
  <acceptance_criteria>
    - `grep -q "Prunable\|MassPrunable" app/Models/ExportFile.php` e `grep -q "retencao_dias" app/Models/ExportFile.php`
    - `grep -q "ExportFile\|prune" routes/console.php`
    - `php artisan test --compact --filter=ExportRetencaoPruningTest` sai 0 (poda por retenção parametrizada + remove arquivo)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Seeds dev (fluxo real) + comando de evidência relatorios:exportar (golden/smoke)</name>
  <files>database/seeders/RelatoriosDevSeeder.php, database/seeders/DatabaseSeeder.php, app/Console/Commands/RelatoriosExportarCommand.php, tests/Feature/Relatorios/RelatoriosGoldenSmokeTest.php</files>
  <read_first>
    - database/seeders/DatabaseSeeder.php (como os seeders de dev das fases anteriores são encadeados; adicionar RelatoriosDevSeeder no bloco de dev)
    - database/seeders/*DevSeeder*.php das Fases 9/10 (modelo de seed que cria processos/decisões pelo FLUXO REAL — reusar para gerar dados de relatório plausíveis)
    - app/Console/Commands/* (ex.: o comando expresso:decidir / analise:decidir — modelo de comando de evidência) e app/Services/Relatorios/Export/ReportExporter.php
    - 15-VALIDATION.md "Comando de evidência" (relatorios:exportar gera 1 arquivo real de cada formato a partir de seeds — golden/smoke end-to-end)
  </read_first>
  <action>
    Criar `RelatoriosDevSeeder` que gere, pelo FLUXO REAL (factories de ViabilityRequest/Decision/Transition/ExpressoQueda em estados variados — deferidas/indeferidas/em análise/quedas por gatilho), uma massa de dev suficiente para os relatórios mostrarem números (NUNCA números cravados na tela; a tela calcula sobre o dado). Encadear no `DatabaseSeeder` no bloco de ambiente dev.
    Criar `app/Console/Commands/RelatoriosExportarCommand.php` (`relatorios:exportar {source?} {--formato=}`): a partir dos dados semeados, monta um `ReportFilters` e chama o `ReportExporter` para gravar UM arquivo REAL de cada formato (CSV/XLSX/PDF) num caminho de evidência, imprimindo o caminho e a contagem de linhas — prova a lógica real de export ponta a ponta (entrega-funcional).
    Teste `RelatoriosGoldenSmokeTest`: rodar o comando (ou o ReportExporter direto) para CSV/XLSX/PDF sobre um dataset conhecido e asserir: CSV tem cabeçalho + N linhas; XLSX reabre pelo Reader do openspout com N linhas; PDF começa com `%PDF` e contém "Total de registros: N" coerente (CA-07/RN-010). Um caso anti-fachada: export de conjunto vazio gera arquivo com 0 linhas de dado (cabeçalho/total 0), nunca número inventado.
  </action>
  <acceptance_criteria>
    - `grep -q "relatorios:exportar" app/Console/Commands/RelatoriosExportarCommand.php`
    - `grep -q "RelatoriosDevSeeder" database/seeders/DatabaseSeeder.php`
    - `php artisan test --compact --filter=RelatoriosGoldenSmokeTest` sai 0 (3 formatos válidos + vazio honesto)
    - `php artisan migrate:fresh --seed --no-interaction` sai 0 e `php artisan relatorios:exportar --formato=pdf` gera um PDF real (sai 0)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verificação integral fresca (2 processos) + guardião-entrega + smoke navegável</name>
  <what-built>
    Fase 15 completa: serviços de relatório, contrato de export (CSV/XLSX/PDF), dashboard/telas com gráficos ECharts, retrofit das listagens, feriados e captura de quedas — tudo sobre dado real com degradação honesta.
  </what-built>
  <read_first>
    - .planning/phases/15-relatorios-e-indicadores/15-VALIDATION.md (Test Infrastructure + as 6 dimensões; CA-03 anti-fachada prioritário)
    - .cursor/agents/guardiao-entrega.md (critério de veredito anti-fachada + auditoria + parametrização + TDD com evidência fresca)
    - .planning/STATE.md (baselines a atualizar: ~1315 SQLite + 29 postgis ANTES da fase; parâmetros 85→90; permissões 27→29)
  </read_first>
  <action>
    Rodar a VERIFICAÇÃO INTEGRAL FRESCA (evidência, não "deve passar"):
      1. `composer test` (2 processos: `--exclude-group postgis` e `--group postgis`) — registrar a contagem fresca (baseline ~1315 SQLite + 29 postgis + os novos da fase); todos verdes.
      2. `vendor/bin/pint --dirty --format agent` — limpo.
      3. `npx tsc --noEmit` e `npm run build` — verdes (chart client-only não quebra o SSR).
      4. `php artisan migrate:fresh --seed` + `relatorios:exportar` para cada formato — arquivos reais gerados (evidência anti-fachada).
    Invocar o agente `guardiao-entrega` com a evidência fresca para o veredito (anti-fachada CA-03: KPI sem delta inventado; zona→bairro com ressalva; feriado nunca inventado; gatilho null quando degradado; export == conjunto filtrado RN-005; export falho NÃO vira "pronto"). Veredito REPROVADO bloqueia a conclusão.
    Smoke navegável (checkpoint humano): abrir o dashboard, uma página de relatório, alternar tema do gráfico (claro/escuro) e gerar um export real de cada formato no browser.
  </action>
  <how-to-verify>
    1. Rodar `composer test` e confirmar os 2 processos verdes com a contagem registrada.
    2. Rodar `vendor/bin/pint --dirty --format agent`, `npx tsc --noEmit`, `npm run build` — todos verdes.
    3. `php artisan migrate:fresh --seed && php artisan relatorios:exportar --formato=xlsx` — abrir o XLSX gerado e conferir linhas reais.
    4. No browser: dashboard com KPIs reais; página de Indicadores com gráfico re-temando ao alternar dark mode; exportar CSV/XLSX/PDF do conjunto filtrado.
  </how-to-verify>
  <resume-signal>Responder "aprovado" após o guardião-entrega APROVAR e o smoke navegável passar, ou descrever os problemas encontrados.</resume-signal>
</task>

</tasks>

<verification>
- `composer test` (2 processos) verde; `vendor/bin/pint --dirty --format agent` limpo; `npx tsc --noEmit` e `npm run build` verdes.
- `relatorios:exportar` gera CSV/XLSX/PDF reais; pruning de retenção testado.
- Guardião-entrega: APROVADO com evidência fresca.
</verification>

<success_criteria>
Fase 15 honestamente pronta: export end-to-end real (evidência), retenção parametrizada, todos os indicadores sobre dado real com degradação honesta, suíte verde nos 2 processos e veredito APROVADO do guardião.
</success_criteria>

<output>
Criar `.planning/phases/15-relatorios-e-indicadores/15-15-SUMMARY.md` (fechamento) com: contagem fresca da suíte (SQLite + postgis), parâmetros/permissões finais (90/29), evidência do `relatorios:exportar`, o veredito do guardião e os bloqueios honestos remanescentes (painel público deferido; zona→bairro até GIS/Fase 13; lista oficial de feriados/metas/produtividade nominal/colunas SAPS/PII por coluna = pendências SEDUR/DPO registradas, nunca simuladas).
</output>

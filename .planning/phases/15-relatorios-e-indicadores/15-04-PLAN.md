---
phase: 15-relatorios-e-indicadores
plan: 04
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - app/Models/Holiday.php
  - database/migrations/2026_06_15_000003_create_holidays_table.php
  - database/factories/HolidayFactory.php
  - database/seeders/HolidaySeeder.php
  - app/Services/Expresso/HolidayProvider.php
  - app/Services/Expresso/DatabaseHolidayProvider.php
  - app/Services/Expresso/BusinessDeadlineCalculator.php
  - app/Providers/AppServiceProvider.php
  - tests/Feature/Relatorios/HolidayCadastroTest.php
  - tests/Unit/Expresso/BusinessDeadlineCalculatorTest.php
autonomous: true
requirements: [HU-137, HU-129]
must_haves:
  truths:
    - "Feriados são cadastrados como dado versionado/auditado (HU-137); lista oficial ausente degrada honesto (dias úteis sem feriados, com ressalva)"
    - "BusinessDeadlineCalculator calcula DURAÇÃO em tempo útil entre dois instantes (businessDurationBetween) descontando fins de semana e feriados — o que HU-129 precisa e o dueAt forward não faz"
    - "Nenhum feriado é inventado: provider sem feriados ⇒ só fins de semana descontados, com flag de ressalva (hasOfficialCalendar=false)"
    - "O prazo SLA/BAP (dueAt/isOverdue) permanece inalterado — Fases 9/10 verdes (anti-regressão)"
  artifacts:
    - path: "app/Models/Holiday.php"
      provides: "Cadastro de feriado auditado (HU-137)"
      contains: "class Holiday"
    - path: "app/Services/Expresso/BusinessDeadlineCalculator.php"
      provides: "Duração em tempo útil entre instantes (HU-129)"
      contains: "function businessDurationBetween"
    - path: "app/Services/Expresso/HolidayProvider.php"
      provides: "Contrato de fonte de feriados (cacheável)"
      contains: "interface HolidayProvider"
  key_links:
    - from: "app/Services/Expresso/BusinessDeadlineCalculator.php"
      to: "app/Services/Expresso/HolidayProvider.php"
      via: "injeção do provider para descontar feriados"
      pattern: "HolidayProvider"
---

<objective>
Entregar o habilitador da HU-129 sem fachada: o cadastro de feriados (HU-137) como dado versionado/auditado e a operação NOVA de duração em tempo útil no `BusinessDeadlineCalculator` — a causa-raiz da distorção do legado (19 dias reportados vs 42h reais). O `dueAt` forward atual só faz prazo (addHours); HU-129 mede DURAÇÃO decorrida descontando fins de semana/feriados, que é um método novo (`businessDurationBetween`).

Purpose: o relatório de tempo por etapa (15-05) e os relatórios SAPS medem tempo correto; o seam de feriados fica pronto e centralizado num único lugar.
Output: `Holiday` + migration + factory + seeder honesto + `HolidayProvider`/`DatabaseHolidayProvider` + `businessDurationBetween` com testes cravados, SEM tocar a semântica de prazo SLA/BAP (anti-regressão das Fases 9/10).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-relatorios-e-indicadores/15-CONTEXT.md
@.planning/phases/15-relatorios-e-indicadores/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cadastro de feriados (Holiday) auditado + migration + factory + seeder honesto</name>
  <files>app/Models/Holiday.php, database/migrations/2026_06_15_000003_create_holidays_table.php, database/factories/HolidayFactory.php, database/seeders/HolidaySeeder.php, tests/Feature/Relatorios/HolidayCadastroTest.php</files>
  <read_first>
    - app/Models/Sector.php (model recente do projeto: trait de auditoria HasAuditoria/LogsActivity, casts(), $fillable — espelhar o estilo de cadastro auditado)
    - database/migrations/2026_06_14_163233_create_viability_decisions_table.php (estilo de migration do projeto: Schema::create, índices, comentários)
    - database/seeders/RolesAndPermissionsSeeder.php (estilo de seeder idempotente — updateOrCreate; NÃO usar create cru)
    - 15-CONTEXT.md seção "HU-129 — tempo por etapa + feriados" (lista oficial é pendência SEDUR → degrada honesto, sem feriado inventado)
  </read_first>
  <action>
    Criar o model + migration via `php artisan make:model Holiday -mf --no-interaction`.
    Migration `create_holidays_table` (ADITIVA, nova tabela): colunas `id`, `date` (date, `unique`), `name` (string), `recurring_annually` (boolean, default false — feriado fixo anual tipo 25/12), `active` (boolean, default true), `timestamps`. Índice em `date` já vem do unique.
    Model `Holiday`: `protected $fillable = ['date', 'name', 'recurring_annually', 'active'];`, `casts()` com `'date' => 'date'`, `'recurring_annually' => 'boolean'`, `'active' => 'boolean'`. Auditoria (HU-137 "versionado/auditado"): aplicar o MESMO mecanismo de auditoria dos cadastros irmãos (trait `HasAuditoria`/`LogsActivity` conforme `Sector` faz) para registrar create/update/delete na trilha (RN-002).
    `HolidaySeeder`: idempotente (`Holiday::updateOrCreate(['date' => ...], [...])`). Como a LISTA OFICIAL de feriados municipais é pendência SEDUR (degradação honesta), semear APENAS os feriados nacionais fixos inquestionáveis como dado de partida substituível (ex.: 01/01 Confraternização, 25/12 Natal, 07/09, 21/04, 01/05) marcados `recurring_annually=true` — NUNCA inventar feriado municipal de Salvador. Registrar no SUMMARY que a lista municipal completa é pendência SEDUR.
    `HolidayCadastroTest` (`php artisan make:test --phpunit Relatorios/HolidayCadastroTest`): provar que o seeder é idempotente (rodar 2x mantém a contagem), que `date` é único (segunda inserção da mesma data falha) e que criar/editar um feriado gera linha de auditoria.
  </action>
  <acceptance_criteria>
    - `grep -q "class Holiday" app/Models/Holiday.php` e `grep -q "recurring_annually" database/migrations/2026_06_15_000003_create_holidays_table.php`
    - `php artisan migrate --no-interaction` sai 0 e `php artisan tinker --execute 'echo \Schema::hasColumn("holidays","date") ? "ok" : "no";'` imprime ok
    - `php artisan test --compact --filter=HolidayCadastroTest` sai 0 (idempotência + unique + auditoria)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: HolidayProvider (contrato + impl cacheada) + binding + businessDurationBetween (aditivo, dueAt intacto)</name>
  <files>app/Services/Expresso/HolidayProvider.php, app/Services/Expresso/DatabaseHolidayProvider.php, app/Services/Expresso/BusinessDeadlineCalculator.php, app/Providers/AppServiceProvider.php</files>
  <read_first>
    - app/Services/Expresso/BusinessDeadlineCalculator.php (estado atual: só `dueAt(from,hours)`=addHours e `isOverdue`; docblock declara o seam HU-137 — NÃO mudar a semântica de dueAt/isOverdue)
    - app/Services/Analise/AnalysisSlaService.php (consumidor de `dueAt` via `dueAtFor` — prazo da fila HU-144; é a rede anti-regressão: dueAt NÃO pode mudar de comportamento)
    - app/Providers/AppServiceProvider.php (onde registrar o binding interface→impl; ÚNICO editor deste arquivo na Wave 2)
    - app/Support/Settings.php (padrão de cache de leitura; o provider de feriados cacheia a lista com TTL técnico de config)
  </read_first>
  <action>
    Criar `app/Services/Expresso/HolidayProvider.php` (interface): `public function isHoliday(\DateTimeInterface $date): bool;`, `public function hasOfficialCalendar(): bool;` (true só quando há feriado municipal cadastrado — base para a ressalva honesta do relatório). 
    Criar `app/Services/Expresso/DatabaseHolidayProvider.php` (`implements HolidayProvider`): carrega os feriados ativos via `Holiday` (considerando `recurring_annually` por mês/dia e datas específicas), com `Cache::remember(...)` usando TTL técnico de `config('sile.relatorios.cache_ttl_segundos', 300)`; `isHoliday` compara mês/dia (recorrentes) e data exata; `hasOfficialCalendar` = existe ao menos 1 feriado NÃO-recorrente (municipal específico) cadastrado.
    No `AppServiceProvider::register()`, bind `HolidayProvider::class => DatabaseHolidayProvider::class` (singleton).
    Em `BusinessDeadlineCalculator`: injetar `HolidayProvider` via construtor (property promotion). ADICIONAR (sem alterar `dueAt`/`isOverdue`): `public function businessDurationBetween(\DateTimeInterface $from, \DateTimeInterface $to): int` → retorna a duração em MINUTOS úteis decorridos de `$from` a `$to`, descontando integralmente sábados/domingos e dias marcados `isHoliday()`. Implementar percorrendo por dia (Carbon) somando os minutos de cada dia útil dentro do intervalo (parcial nas pontas), `isWeekend()` || `isHoliday()` ⇒ 0 no dia. Se `$to <= $from` ⇒ 0. NÃO inventar feriado: sem calendário oficial, só fins de semana são descontados. Atualizar o docblock do seam para refletir que dueAt SEGUE em horas-calendário (prazo SLA/BAP HU-134/144 inalterado) e que a duração útil entra por `businessDurationBetween` (HU-129).
  </action>
  <acceptance_criteria>
    - `grep -q "interface HolidayProvider" app/Services/Expresso/HolidayProvider.php` e `grep -q "function hasOfficialCalendar" app/Services/Expresso/HolidayProvider.php`
    - `grep -q "function businessDurationBetween" app/Services/Expresso/BusinessDeadlineCalculator.php` e o `dueAt` original continua `addHours` (`grep -q "addHours" app/Services/Expresso/BusinessDeadlineCalculator.php`)
    - `grep -q "HolidayProvider::class" app/Providers/AppServiceProvider.php`
    - `php artisan test --compact --filter=AnalysisSlaService` sai 0 (anti-regressão: prazo SLA inalterado)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Testes cravados de businessDurationBetween (fim de semana, feriado, degradação honesta)</name>
  <files>tests/Unit/Expresso/BusinessDeadlineCalculatorTest.php</files>
  <read_first>
    - tests/Unit/Expresso/BusinessDeadlineCalculatorTest.php (se existir — ADITIVO, manter os testes de dueAt/isOverdue intactos; senão criar via `php artisan make:test --phpunit --unit Expresso/BusinessDeadlineCalculatorTest`)
    - app/Services/Expresso/BusinessDeadlineCalculator.php (assinatura de businessDurationBetween — minutos úteis)
    - 15-VALIDATION.md Dimensão 1 (HU-137 feriado nunca inventado: lista vazia ⇒ dias úteis sem feriados + ressalva) e Dimensão 2 (tempo por etapa com desconto de fim de semana/feriado)
  </read_first>
  <action>
    Adicionar ao `BusinessDeadlineCalculatorTest` (usando um `HolidayProvider` fake/stub controlável no teste — NÃO o banco, salvo onde precisar do DatabaseHolidayProvider):
      - `test_duracao_util_desconta_fim_de_semana`: intervalo de uma sexta 14h a uma segunda 14h (cruzando sáb/dom) deve dar EXATAMENTE 1 dia útil (sex 14h→sex 24h + seg 00h→seg 14h) — assert do total de minutos cravado; provar que sábado/domingo não contam.
      - `test_duracao_util_desconta_feriado_cadastrado`: com o provider retornando isHoliday=true para uma terça, intervalo seg 00h→qua 00h dá 1 dia útil (seg) — a terça-feriado não conta.
      - `test_sem_calendario_oficial_desconta_so_fim_de_semana`: provider `hasOfficialCalendar()=false` ⇒ um intervalo num feriado NÃO cadastrado (ex.: um feriado municipal não semeado) conta como dia útil normal (anti-fachada: nenhum feriado inventado), e `hasOfficialCalendar()` é falso.
      - `test_intervalo_invertido_ou_nulo_retorna_zero`.
    Garantir cobertura adicional pelo `DatabaseHolidayProvider` real em ao menos 1 caso (semear 1 feriado e provar o desconto end-to-end).
  </action>
  <acceptance_criteria>
    - `php artisan test --compact --filter=BusinessDeadlineCalculatorTest` sai 0 (inclui os 4 casos novos)
    - O teste contém um caso com `hasOfficialCalendar` falso provando que feriado não cadastrado NÃO é descontado (anti-fachada)
    - `vendor/bin/pint --dirty --format agent` sem pendências
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=Holiday` e `--filter=BusinessDeadlineCalculator` e `--filter=AnalysisSla` verdes (o último prova anti-regressão).
- `php artisan migrate:fresh --seed --no-interaction` sai 0 com os feriados de partida semeados.
- `vendor/bin/pint --dirty --format agent` limpo.
</verification>

<success_criteria>
Calendário de feriados auditado e substituível (lista municipal = pendência SEDUR honesta), duração em tempo útil disponível para a HU-129 e seam de feriados centralizado, sem regressão no prazo SLA/BAP das Fases 9/10.
</success_criteria>

<output>
Criar `.planning/phases/15-relatorios-e-indicadores/15-05-... NÃO`. Criar `.planning/phases/15-relatorios-e-indicadores/15-04-SUMMARY.md` registrando: a assinatura de `businessDurationBetween` (minutos úteis), a decisão de MANTER `dueAt` em horas-calendário (prazo SLA/BAP HU-134/144 inalterado — anti-regressão; tornar o prazo em dias úteis depende de confirmação SEDUR do critério legal e fica registrado como pendência), e que a lista oficial de feriados municipais é pendência SEDUR (degradação honesta com `hasOfficialCalendar`).
</output>

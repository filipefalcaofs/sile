---
phase: 15-relatorios-e-indicadores
plan: 09
type: execute
wave: 4
depends_on: ["15-03", "15-05", "15-06", "15-07", "15-08"]
files_modified:
  - routes/gestao.php
  - app/Http/Requests/Gestao/RelatorioFiltersRequest.php
  - app/Http/Controllers/Gestao/RelatorioController.php
  - app/Http/Controllers/Gestao/ExportacaoController.php
  - app/Http/Controllers/Gestao/HolidayController.php
  - app/Http/Requests/Gestao/HolidayRequest.php
  - tests/Feature/Relatorios/RelatorioControllerTest.php
  - tests/Feature/Relatorios/ExportacaoDownloadTest.php
  - tests/Feature/Relatorios/HolidayControllerTest.php
autonomous: true
requirements: [HU-122, HU-123, HU-124, HU-125, HU-126, HU-127, HU-128, HU-129, HU-130, HU-131, HU-145, HU-137]
must_haves:
  truths:
    - "As telas de relatório (indicadores/tempo/produtividade/quedas) são servidas com dado REAL dos serviços, gated por consultar-relatorios, e toda consulta é auditada (RN-002)"
    - "Qualquer relatório exporta via ?formato=csv|xlsx|pdf delegando ao ReportExporter (RN-004/009); a produtividade nominal só sai com relatorios.produtividade.nominal (RN-007)"
    - "O download do arquivo assíncrono é por URL TEMPORÁRIA ASSINADA servindo disco NÃO-público por streaming (nunca URL pública)"
    - "Feriados (HU-137) têm CRUD administrável auditado; sem consultar-relatorios → 403 auditado no ponto único"
    - "Rotas estáticas (exportacoes/download) vêm ANTES de qualquer wildcard; RelatorioController é o ÚNICO editor de routes/gestao.php nesta fase"
  artifacts:
    - path: "app/Http/Controllers/Gestao/RelatorioController.php"
      provides: "Endpoints dos relatórios (Inertia) + export ?formato="
      contains: "class RelatorioController"
    - path: "app/Http/Controllers/Gestao/ExportacaoController.php"
      provides: "Download assinado do ExportFile (disco não-público)"
      contains: "temporarySignedRoute"
    - path: "routes/gestao.php"
      provides: "Rotas relatorios.* + feriados.* + exportacoes.download"
      contains: "relatorios"
  key_links:
    - from: "app/Http/Controllers/Gestao/RelatorioController.php"
      to: "app/Services/Relatorios/Export/ReportExporter.php"
      via: "branch ?formato= delega ao exporter"
      pattern: "ReportExporter"
    - from: "app/Http/Controllers/Gestao/ExportacaoController.php"
      to: "app/Models/ExportFile.php"
      via: "Storage::disk(...)->download do disco não-público"
      pattern: "Storage::disk"
---

<objective>
Camada HTTP da fase (DONO ÚNICO de `routes/gestao.php`): expor os relatórios (HU-122 dados via serviços, HU-123–130/145) como páginas Inertia com dado real, dar a TODA tela o export transversal via `?formato=` (HU-131 RN-004/009), entregar o endpoint de download assinado do arquivo assíncrono (espelhando o TVL HU-132) e o CRUD de feriados (HU-137). Os componentes React das páginas chegam em 15-13/15-14; aqui os testes provam o controller (componente Inertia + props reais + export + auditoria + gates).

Purpose: ligar os serviços route-free (15-03/05/06/07) e os drivers (15-02/08) à retaguarda, com permissão e auditoria.
Output: `RelatorioController` + `RelatorioFiltersRequest` + `ExportacaoController` (download assinado) + `HolidayController` + rotas, com testes de Inertia/props/export/gate/auditoria.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-relatorios-e-indicadores/15-CONTEXT.md
@.planning/phases/15-relatorios-e-indicadores/15-RESEARCH.md
@.planning/phases/15-relatorios-e-indicadores/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RelatorioController (Inertia + ?formato=) + RelatorioFiltersRequest + rotas relatorios.*</name>
  <files>routes/gestao.php, app/Http/Requests/Gestao/RelatorioFiltersRequest.php, app/Http/Controllers/Gestao/RelatorioController.php, tests/Feature/Relatorios/RelatorioControllerTest.php</files>
  <read_first>
    - routes/gestao.php (padrão dos grupos com `permission:` + prefix + name; ordenação rota estática ANTES do wildcard — ver grupo `auditoria` (export antes do index) e `processos` (fila/busca antes do {viabilityRequest}))
    - app/Http/Controllers/Gestao/AuditoriaController.php (modelo: index renderiza Inertia + branch `if formato==='csv' return $this->export(...)`; consulta auditada via AuditService; paginação server-side)
    - app/Services/Relatorios/{IndicadoresViabilidadeService,TempoAnaliseService,ProdutividadeAnalistaService,ExpressoQuedaService}.php (os métodos a consumir) e ReportFilters::fromArray
    - app/Services/Relatorios/Export/{ReportExporter,Sources/SolicitacoesReportSource,Sources/TempoAnaliseReportSource,Sources/EscritorioVirtualReportSource,Sources/ProdutividadeReportSource,Sources/ExpressoQuedaReportSource}.php
  </read_first>
  <action>
    Criar `app/Http/Requests/Gestao/RelatorioFiltersRequest.php` (authorize via `can:consultar-relatorios` ou true — o gate é a rota; rules nullable para data_de/data_ate (date), setor (int), bairro/cnae/categoria (string), analista (int)); `toReportFilters(): ReportFilters` => `ReportFilters::fromArray($this->validated())`.
    Criar `app/Http/Controllers/Gestao/RelatorioController.php` com métodos finos:
      - `indicadores(RelatorioFiltersRequest $r)`: `$f = $r->toReportFilters()`; se `$r->query('formato')` informado → `return app(ReportExporter::class)->export(app(SolicitacoesReportSource::class), $f, $formato, $r->user())`; senão AUDITA a consulta (AuditService logName 'relatorios', event 'consulta-indicadores', filtros aplicados) e `Inertia::render('gestao/relatorios/indicadores', [...dados de IndicadoresViabilidadeService: porPeriodo/porZona/porCnae/porRisco/taxaDeferimento/taxaIndeferimento...])`.
      - `tempo(RelatorioFiltersRequest $r)`: idem com TempoAnaliseService + sources de tempo/escritório virtual (escolher o source pelo `?relatorio=` quando houver mais de um; default tempo).
      - `produtividade(RelatorioFiltersRequest $r)`: resolve `$nominal = $r->user()->can('relatorios.produtividade.nominal')`; `$scope = $nominal ? null : $r->user()->id`; dados de `ProdutividadeAnalistaService::porAnalista($f, $nominal, $scope)`; export via `new ProdutividadeReportSource($nominal, $scope)`.
      - `quedas(RelatorioFiltersRequest $r)`: ExpressoQuedaService (taxa/serie/ranking) + export via ExpressoQuedaReportSource.
    Em `routes/gestao.php`, dentro do grupo `auth:gestao`/`acessar-gestao`, adicionar o grupo gated por `permission:consultar-relatorios` com prefix `relatorios` name `relatorios.`: rotas GET `indicadores`/`tempo`/`produtividade`/`quedas` (cada uma serve a tela E o export por `?formato=`). Colocar este grupo e o de exportacoes/feriados de forma que rotas estáticas venham ANTES de wildcards (não há wildcard `{relatorio}` — cada relatório é rota estática nomeada).
    Teste `RelatorioControllerTest`: usuário com `consultar-relatorios` → cada GET retorna 200 com `Inertia::assertComponent('gestao/relatorios/...')` e props com os números reais (dataset factory cravado); `?formato=csv` retorna streaming (200) e grava auditoria; sem `consultar-relatorios` → 403 (auditado — assert da linha de auditoria do 403); produtividade sem `relatorios.produtividade.nominal` → props/colunas anônimas (sem nome).
  </action>
  <acceptance_criteria>
    - `grep -q "class RelatorioController" app/Http/Controllers/Gestao/RelatorioController.php` e `grep -q "ReportExporter" ...`
    - `php artisan route:list --path=gestao/relatorios` lista indicadores/tempo/produtividade/quedas (name `gestao.relatorios.*`)
    - `php artisan test --compact --filter=RelatorioControllerTest` sai 0 (Inertia + props reais + export + 403 auditado + nominal gated)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ExportacaoController — download assinado do ExportFile (disco não-público)</name>
  <files>routes/gestao.php, app/Http/Controllers/Gestao/ExportacaoController.php, tests/Feature/Relatorios/ExportacaoDownloadTest.php</files>
  <read_first>
    - app/Http/Controllers/Gestao/TvlDocumentController.php (padrão EXATO: download por URL temporária assinada (middleware 'signed') servindo disco NÃO-público por streaming — copiar a abordagem)
    - app/Models/ExportFile.php (15-02: disk/path/filename/user_id — o arquivo gerado pelo Job) e app/Notifications/ExportacaoPronta.php (15-02: aponta para a rota gestao.relatorios.exportacoes.download)
    - 15-02-SUMMARY.md (nome confirmado da rota de download: gestao.relatorios.exportacoes.download)
  </read_first>
  <action>
    Criar `app/Http/Controllers/Gestao/ExportacaoController.php` com `download(Request $request, ExportFile $exportFile)`: valida que o `ExportFile` pertence ao usuário (ou que o usuário tem `consultar-relatorios`), AUDITA o download (RN-008), e retorna `Storage::disk($exportFile->disk)->download($exportFile->path, $exportFile->filename)` (disco NÃO-público — guarda anti-'public' como no TvlDocumentController). Se o arquivo não existir mais (pruning/retenção) → 404 honesto (sem fabricar). 
    Em `routes/gestao.php`, dentro do grupo `relatorios` (mesmo dono desta wave), adicionar a rota estática `GET exportacoes/{exportFile}/download` com middleware `signed`, name `exportacoes.download` (= `gestao.relatorios.exportacoes.download`), ANTES de qualquer wildcard do grupo.
    Teste `ExportacaoDownloadTest`: criar um `ExportFile` em `Storage::fake(disk)` com conteúdo; gerar a URL via `URL::temporarySignedRoute('gestao.relatorios.exportacoes.download', ...)`; baixar com assinatura válida → 200 + conteúdo; sem assinatura → 403; arquivo ausente → 404; download auditado.
  </action>
  <acceptance_criteria>
    - `grep -q "temporarySignedRoute\|->download(" app/Http/Controllers/Gestao/ExportacaoController.php` e `grep -q "Storage::disk" ...`
    - `php artisan route:list --name=gestao.relatorios.exportacoes.download` lista a rota com middleware signed
    - `php artisan test --compact --filter=ExportacaoDownloadTest` sai 0 (assinada ok, sem assinatura 403, ausente 404)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: HolidayController CRUD (HU-137 admin) + rotas feriados.* + auditoria</name>
  <files>routes/gestao.php, app/Http/Controllers/Gestao/HolidayController.php, app/Http/Requests/Gestao/HolidayRequest.php, tests/Feature/Relatorios/HolidayControllerTest.php</files>
  <read_first>
    - app/Http/Controllers/Gestao/SectorController.php (modelo de CRUD de cadastro: index Inertia + store/update/toggle; auditoria; FormRequest) — espelhar
    - app/Models/Holiday.php (15-04: fillable date/name/recurring_annually/active)
    - routes/gestao.php (grupo `setores`/`textos-padrao` reusam `manter-parametros` — feriados segue o mesmo: sem permissão nova)
  </read_first>
  <action>
    Criar `app/Http/Requests/Gestao/HolidayRequest.php` (rules: `date` required date unique (ignorando o próprio no update), `name` required string, `recurring_annually` boolean, `active` boolean).
    Criar `app/Http/Controllers/Gestao/HolidayController.php` espelhando `SectorController`: `index` (Inertia `gestao/feriados/index` com lista paginada server-side), `store`, `update`, `toggleActivation` — cada ação auditada (RN-002) via o mecanismo do model/AuditService. SEM destroy (preserva histórico — só ativa/inativa).
    Em `routes/gestao.php`, adicionar o grupo `feriados` gated por `permission:manter-parametros` (reuso, como setores/textos-padrao — sem permissão nova; registrar no SUMMARY) com index/store/update/{holiday}/ativacao.
    Teste `HolidayControllerTest`: admin/gestor com `manter-parametros` cria/edita/ativa-inativa um feriado (cada ação audita); `date` duplicada → 422; sem `manter-parametros` → 403 auditado; index renderiza `gestao/feriados/index` com a lista.
  </action>
  <acceptance_criteria>
    - `grep -q "class HolidayController" app/Http/Controllers/Gestao/HolidayController.php`
    - `php artisan route:list --path=gestao/feriados` lista index/store/update/ativacao (name `gestao.feriados.*`)
    - `php artisan test --compact --filter=HolidayControllerTest` sai 0 (CRUD auditado + 422 unique + 403 gate)
    - `vendor/bin/pint --dirty --format agent` sem pendências
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=Relatorio` e `--filter=Exportacao` e `--filter=HolidayController` verdes.
- `php artisan route:list --path=gestao/relatorios` e `--path=gestao/feriados` corretos; download com middleware signed.
- `vendor/bin/pint --dirty --format agent` limpo.
</verification>

<success_criteria>
Relatórios servidos com dado real, exportáveis por ?formato= nas três opções, download assinado de disco não-público, CRUD de feriados auditado — tudo gated e auditado, com RelatorioController como único editor de routes/gestao.php na fase.
</success_criteria>

<output>
Criar `.planning/phases/15-relatorios-e-indicadores/15-09-SUMMARY.md` com: os nomes finais das rotas (`gestao.relatorios.indicadores/tempo/produtividade/quedas`, `gestao.relatorios.exportacoes.download`, `gestao.feriados.*`), os nomes dos componentes Inertia que 15-13/15-14 devem criar, o mapeamento `?relatorio=` quando um endpoint serve mais de um ReportSource, e a decisão de reusar `manter-parametros` para feriados (sem permissão nova).
</output>

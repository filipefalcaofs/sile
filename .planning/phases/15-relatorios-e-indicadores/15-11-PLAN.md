---
phase: 15-relatorios-e-indicadores
plan: 11
type: execute
wave: 5
depends_on: ["15-02", "15-08", "15-09"]
files_modified:
  - app/Services/Relatorios/Export/Sources/CnaesReportSource.php
  - app/Services/Relatorios/Export/Sources/ParametrosReportSource.php
  - app/Services/Relatorios/Export/Sources/UsuariosReportSource.php
  - app/Services/Relatorios/Export/Sources/PerfisReportSource.php
  - app/Services/Relatorios/Export/Sources/AcessosUsuarioReportSource.php
  - app/Http/Controllers/Gestao/CnaeController.php
  - app/Http/Controllers/Gestao/ParameterController.php
  - app/Http/Controllers/Gestao/UserManagementController.php
  - app/Http/Controllers/Gestao/RoleController.php
  - app/Http/Controllers/Gestao/AccessHistoryController.php
  - tests/Feature/Relatorios/RetrofitListagensTest.php
autonomous: true
requirements: [HU-131]
must_haves:
  truths:
    - "As listagens das Fases 1–2 (CNAEs, parâmetros, usuários, perfis, acessos) ganham exportação CSV/XLSX/PDF do conjunto filtrado pelo componente compartilhado (critério de pronto transversal 8)"
    - "Cada export reflete EXATAMENTE a listagem filtrada (search/active/sort) — RN-005"
    - "Os ReportSources das listagens leem os filtros do BAG do ReportFilters (search/active/pii/user no bag, NÃO no construtor) — reconstrutíveis via fromArray, refletindo o filtro no síncrono E no assíncrono"
    - "Usuários exporta cpf_masked por default; PII só sob permissão específica (RN-007)"
    - "Cada exportação é auditada; nenhuma listagem reimplementa export (RN-009)"
  artifacts:
    - path: "app/Services/Relatorios/Export/Sources/UsuariosReportSource.php"
      provides: "ReportSource de usuários com cpf_masked"
      contains: "cpf_masked"
    - path: "app/Http/Controllers/Gestao/CnaeController.php"
      provides: "index com branch ?formato= delegando ao ReportExporter"
      contains: "ReportExporter"
  key_links:
    - from: "app/Http/Controllers/Gestao/CnaeController.php"
      to: "app/Services/Relatorios/Export/ReportExporter.php"
      via: "branch ?formato= no index"
      pattern: "ReportExporter"
---

<objective>
Cumprir o critério de pronto transversal 8 do ROADMAP: as listagens já entregues nas Fases 1–2 recebem o retrofit de exportação CSV/XLSX/PDF do conjunto filtrado pelo componente compartilhado (HU-131). Sem rotas novas — cada `index` ganha um branch `?formato=` (~3 linhas) que monta o ReportSource da tela e delega ao `ReportExporter`. Um controller por vez, com teste a cada passo.

Purpose: paridade de export em toda a gestão; nenhuma tela reimplementa CSV (RN-009).
Output: 5 ReportSources (cnaes/parâmetros/usuários/perfis/acessos) + branches `?formato=` nos respectivos controllers, com testes de RN-005 e minimização de PII (usuários).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-relatorios-e-indicadores/15-CONTEXT.md
@.planning/phases/15-relatorios-e-indicadores/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export de CNAEs e Parâmetros (1 controller por vez)</name>
  <files>app/Services/Relatorios/Export/Sources/CnaesReportSource.php, app/Services/Relatorios/Export/Sources/ParametrosReportSource.php, app/Http/Controllers/Gestao/CnaeController.php, app/Http/Controllers/Gestao/ParameterController.php, tests/Feature/Relatorios/RetrofitListagensTest.php</files>
  <read_first>
    - app/Http/Controllers/Gestao/CnaeController.php (método index: a query filtrada atual — search/active/sort/paginação server-side; o ReportSource deve reproduzir a MESMA query) e ParameterController.php (index: filtros do catálogo)
    - app/Services/Relatorios/Export/Sources/SolicitacoesReportSource.php (15-03 — modelo de ReportSource concreto que lê do ReportFilters) + ReportExporter (15-02)
    - app/Services/Relatorios/ReportFilters.php (15-02 — bag serializável + `get()`/`only()`; os filtros da listagem (search/active) viajam no BAG, NÃO no construtor — reconstrutível pelo Job)
    - app/Services/Relatorios/Export/ReportExporter.php (assinatura export(ReportSource,$filtros,$formato,$user); volume pequeno → caminho síncrono na prática, mas o source é reconstrutível mesmo se async)
  </read_first>
  <action>
    Criar `CnaesReportSource` e `ParametrosReportSource` (`implements ReportSource`). Correção do blocker: os filtros da listagem (search/active/sort) viajam no BAG do `ReportFilters` — o `definition(ReportFilters $f)` LÊ `$f->get('search')`/`$f->get('active')`/`$f->get('sort')` e reproduz EXATAMENTE a query do `index` (mesmos `when()`/`whereLike(caseSensitive:false)`/sort), com as colunas visíveis da tela; `personalData=false`; `event='exporta-cnaes'`/`'exporta-parametros'`; `arquivoBase` adequado. NÃO usar estado de construtor (seria perdido no `app($sourceClass)` do Job) — o bag é a fonte, reconstrutível via `fromArray` (RN-005 sync E async).
    No `index` de cada controller, adicionar o branch: `if ($formato = $request->query('formato')->toString()) { return app(ReportExporter::class)->export(app(CnaesReportSource::class), ReportFilters::fromArray($request->only(['search','active','sort','direction'])), $formato, $request->user()); }` — o source é resolvido por `app()` (sem args) e lê tudo do `ReportFilters`. Auditar a exportação (o ReportExporter já audita — confirmar). NÃO mexer no resto do index nem nas rotas.
    Teste `RetrofitListagensTest`: aplicar `?search=` + `?formato=csv` em cnaes e provar que o conteúdo == a listagem filtrada (RN-005: registro fora do filtro NÃO aparece) e que `?formato=xlsx`/`pdf` retornam streaming; um caso provando que `CnaesReportSource->definition(ReportFilters::fromArray(['search'=>...]))` (reconstrução sem construtor) traz o mesmo recorte; idem 1 caso para parâmetros.
  </action>
  <acceptance_criteria>
    - `grep -q "implements ReportSource" app/Services/Relatorios/Export/Sources/CnaesReportSource.php` e idem ParametrosReportSource
    - `grep -q "ReportExporter" app/Http/Controllers/Gestao/CnaeController.php` e em ParameterController
    - `php artisan test --compact --filter=RetrofitListagensTest` sai 0 (cnaes/parâmetros: RN-005 + formatos)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Export de Usuários (cpf_masked) e Perfis</name>
  <files>app/Services/Relatorios/Export/Sources/UsuariosReportSource.php, app/Services/Relatorios/Export/Sources/PerfisReportSource.php, app/Http/Controllers/Gestao/UserManagementController.php, app/Http/Controllers/Gestao/RoleController.php, tests/Feature/Relatorios/RetrofitListagensTest.php</files>
  <read_first>
    - app/Http/Controllers/Gestao/UserManagementController.php (index: query filtrada de usuários; campos de PII — CPF) e RoleController.php (index: perfis + contagem de permissões)
    - app/Models/User.php (acessor de cpf mascarado, se houver — usar o cpf_masked; senão mascarar no mapRow) e a regra LGPD (RN-007: cpf_masked por default, PII só sob permissão específica)
    - 15-CONTEXT.md (HU-131 RN-007: usuários exporta cpf_masked salvo permissão de PII)
  </read_first>
  <action>
    Criar `UsuariosReportSource` (`implements ReportSource`): reproduz a query do index de usuários (lendo `search`/`active` do BAG do ReportFilters); colunas [nome, email, papel, cpf, status, criado_em]; `mapRow` emite CPF MASCARADO (`cpf_masked`) por default; o flag de liberação de PII viaja no BAG (`$f->get('pii')`), NÃO no construtor — quando `true` (o controller setou a partir da permissão de PII), emite o CPF completo; `personalData=true` (HU-101). Assim o source é reconstrutível via `fromArray` (sem estado de construtor) e o caminho assíncrono honra o mesmo gate. `PerfisReportSource`: colunas [perfil, permissoes_total, usuarios_total]; `personalData=false`; `event='exporta-perfis'`.
    Adicionar o branch `?formato=` nos índices de `UserManagementController` e `RoleController`: `ReportFilters::fromArray($request->only(['search','active','sort','direction']) + ['pii' => $request->user()->can('<permissão-de-PII-existente>') ? '1' : ''])` e delegar ao `ReportExporter` com `app(UsuariosReportSource::class)`. Usar a permissão de PII já existente no projeto se houver; caso não exista, NUNCA setar `pii` (CPF sempre mascarado) e registrar a permissão de PII como pendência DPO no SUMMARY.
    Teste: usuários `?formato=csv` default → CPF MASCARADO no conteúdo (assert que o CPF cru NÃO aparece); com `pii` liberado no bag → CPF completo; RN-005 com `?search=`; perfis `?formato=xlsx` retorna streaming.
  </action>
  <acceptance_criteria>
    - `grep -q "cpf_masked" app/Services/Relatorios/Export/Sources/UsuariosReportSource.php`
    - `grep -q "ReportExporter" app/Http/Controllers/Gestao/UserManagementController.php` e em RoleController
    - `php artisan test --compact --filter=RetrofitListagensTest` sai 0 (usuários mascarado por default + perfis)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Export do histórico de acessos + verificação consolidada do retrofit</name>
  <files>app/Services/Relatorios/Export/Sources/AcessosUsuarioReportSource.php, app/Http/Controllers/Gestao/AccessHistoryController.php, tests/Feature/Relatorios/RetrofitListagensTest.php</files>
  <read_first>
    - app/Http/Controllers/Gestao/AccessHistoryController.php (a listagem de acessos por usuário — query filtrada; é por {user})
    - app/Services/Relatorios/Export/Sources/SolicitacoesReportSource.php (15-03 — modelo de ReportSource que lê do ReportFilters) e app/Services/Relatorios/ReportFilters.php (15-02 — bag)
    - app/Models/AccessLog.php (event/ip/created_at/user_agent — colunas do histórico)
  </read_first>
  <action>
    Criar `AcessosUsuarioReportSource` (`implements ReportSource`): o `user` alvo e os filtros viajam no BAG (`$f->get('user')`), NÃO no construtor — reconstrutível via `fromArray`; reproduz a query do `AccessHistoryController` (acessos daquele usuário; escopo/permissão `consultar-acessos-de-qualquer-conta` já garantida pela rota); colunas [evento, ip, user_agent, data_hora]; `personalData=true` (IP/UA são dado pessoal). Adicionar o branch `?formato=` no `AccessHistoryController` montando `ReportFilters::fromArray(['user' => $user->id] + $request->only([...filtros...]))` e delegando ao `ReportExporter` com `app(AcessosUsuarioReportSource::class)` (a rota `acessos.show` já existe; só o branch).
    Teste: `?formato=csv` no histórico de um usuário retorna só os acessos daquele usuário (RN-005, `user` lido do bag); auditado. Rodar `--filter=RetrofitListagensTest` completo (cnaes/parâmetros/usuários/perfis/acessos) verde.
  </action>
  <acceptance_criteria>
    - `grep -q "implements ReportSource" app/Services/Relatorios/Export/Sources/AcessosUsuarioReportSource.php`
    - `grep -q "ReportExporter" app/Http/Controllers/Gestao/AccessHistoryController.php`
    - `php artisan test --compact --filter=RetrofitListagensTest` sai 0 (todas as 5 listagens)
    - `vendor/bin/pint --dirty --format agent` sem pendências
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=RetrofitListagens` verde (5 listagens, RN-005 + formatos + cpf_masked).
- Nenhuma listagem reimplementa export (todas via ReportExporter) — RN-009.
</verification>

<success_criteria>
As listagens das Fases 1–2 oferecem CSV/XLSX/PDF do conjunto filtrado pelo componente compartilhado, com minimização de PII e auditoria, cumprindo o critério de pronto transversal 8.
</success_criteria>

<output>
Criar `.planning/phases/15-relatorios-e-indicadores/15-11-SUMMARY.md` registrando: as colunas de cada listagem, a permissão de PII usada (ou a pendência DPO registrada se inexistente — CPF sempre mascarado até lá), e a confirmação de que os índices não ganharam rotas novas (só branch ?formato=).
</output>

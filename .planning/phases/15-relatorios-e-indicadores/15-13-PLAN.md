---
phase: 15-relatorios-e-indicadores
plan: 13
type: execute
wave: 6
depends_on: ["15-09", "15-12"]
files_modified:
  - app/Http/Controllers/Gestao/DashboardController.php
  - resources/js/pages/gestao/dashboard.tsx
  - resources/js/pages/gestao/relatorios/indicadores.tsx
  - resources/js/components/app/app-sidebar.tsx
  - resources/js/components/app/command-search.tsx
  - tests/Feature/Relatorios/DashboardKpisTest.php
autonomous: true
requirements: [HU-122, HU-123, HU-124, HU-125, HU-126, HU-127, HU-128, HU-131]
must_haves:
  truths:
    - "O dashboard executivo (HU-122) mostra KPIs reais do EP15 (volume, taxas, tempo médio, taxa expressa) gated por consultar-relatorios; sem janela histórica os comparativos degradam SEM delta inventado"
    - "A página de Indicadores de Viabilidade (HU-123–128) mostra período/zona(→bairro)/CNAE/risco/taxas com gráficos ECharts e tabela exportável (<ExportMenu>)"
    - "Os gráficos usam o wrapper client-only (15-12); zona indisponível exibe o rótulo honesto de degradação para bairro (HU-124)"
    - "A navegação ganha o grupo Relatórios (sidebar + Cmd+K) gated por consultar-relatorios"
  artifacts:
    - path: "resources/js/pages/gestao/relatorios/indicadores.tsx"
      provides: "Página de indicadores de viabilidade com gráficos + export"
      contains: "ExportMenu"
    - path: "app/Http/Controllers/Gestao/DashboardController.php"
      provides: "KPIs do EP15 no dashboard (gated)"
      contains: "consultar-relatorios"
  key_links:
    - from: "resources/js/pages/gestao/relatorios/indicadores.tsx"
      to: "resources/js/components/ui/chart/chart.tsx"
      via: "render dos gráficos"
      pattern: "Chart"
---

<objective>
HU-122 — dashboard executivo com indicadores reais, e a primeira página de relatório detalhado (Indicadores de Viabilidade, HU-123–128). Estender o `DashboardController` (padrão Fase 2.4: KPI gated por permissão, sem delta inventado) com os KPIs do EP15 e montar as telas consumindo os gráficos (15-12) e os dados reais de 15-09. Adicionar o grupo "Relatórios" à navegação (sidebar + Cmd+K).

Purpose: o gestor abre o sistema e vê a operação real; os indicadores de viabilidade têm tela navegável com gráfico e export.
Output: KPIs do EP15 no `DashboardController` + `dashboard.tsx` atualizado + `relatorios/indicadores.tsx` + grupo Relatórios na nav, com teste de KPIs reais e build verde.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-relatorios-e-indicadores/15-CONTEXT.md
@.planning/phases/15-relatorios-e-indicadores/15-RESEARCH.md
@.planning/phases/15-relatorios-e-indicadores/15-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: KPIs do EP15 no DashboardController (gated, dado real, sem delta inventado)</name>
  <files>app/Http/Controllers/Gestao/DashboardController.php, tests/Feature/Relatorios/DashboardKpisTest.php</files>
  <read_first>
    - app/Http/Controllers/Gestao/DashboardController.php (padrão atual: cada bloco de KPI condicionado a `$user->can(...)`; sem delta inventado — adicionar o bloco `relatorios` gated por `consultar-relatorios`)
    - app/Services/Relatorios/{IndicadoresViabilidadeService,TempoAnaliseService,ExpressoQuedaService}.php (métodos para os KPIs: volume no período, taxa de deferimento/indeferimento, tempo médio por etapa, taxa de resposta expressa)
    - resources/js/components/ui/kpi-card.tsx (KpiCard trata `delta` como OPCIONAL — "variação real, nunca inventada"; sem janela histórica → sem delta)
  </read_first>
  <action>
    No `DashboardController::__invoke`, adicionar ao array `kpis` o bloco `'relatorios' => $user->can('consultar-relatorios') ? [...] : null` com KPIs REAIS do período corrente (default últimos N dias, janela técnica de config): volume de solicitações, taxa de deferimento e indeferimento (IndicadoresViabilidadeService), tempo médio da etapa de análise (TempoAnaliseService), taxa de resposta expressa (ExpressoQuedaService). NÃO incluir `delta`/comparativo (sem série histórica persistida → degradar honesto, sem "+X%"). Cada número vem do serviço (dado real); sem dados ⇒ valor honesto (null/0 real, com rótulo claro), nunca fabricado.
    Teste `DashboardKpisTest`: usuário com `consultar-relatorios` + dataset factory cravado → o bloco `relatorios` traz os números EXATOS e NÃO traz chave `delta`; usuário sem a permissão → bloco `relatorios` é null (gated); período sem dados → números honestos (taxa null), sem invenção.
  </action>
  <acceptance_criteria>
    - `grep -q "consultar-relatorios" app/Http/Controllers/Gestao/DashboardController.php` e `grep -q "'relatorios' =>" app/Http/Controllers/Gestao/DashboardController.php`
    - `php artisan test --compact --filter=DashboardKpisTest` sai 0 (números reais, sem delta, gated, vazio honesto)
    - O teste asserta a AUSÊNCIA de `delta` no payload de KPI (anti-fachada)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Páginas dashboard.tsx (KPIs + gráficos) e relatorios/indicadores.tsx (tabelas/gráficos + ExportMenu)</name>
  <files>resources/js/pages/gestao/dashboard.tsx, resources/js/pages/gestao/relatorios/indicadores.tsx</files>
  <read_first>
    - resources/js/pages/gestao/dashboard.tsx (estrutura atual de KpiCards por permissão — adicionar a seção Relatórios com os KPIs novos e 1–2 gráficos)
    - resources/js/components/ui/chart/chart.tsx (15-12 — <Chart option={...}/> client-only) e resources/js/components/ui/data-table/export-menu.tsx (15-12 — <ExportMenu>)
    - resources/js/pages/gestao/auditoria/index.tsx (modelo de página de relatório do console: PageHeader + filtros + DataTable + useServerTable — espelhar para indicadores)
    - 15-09-SUMMARY.md (nome do componente Inertia que o RelatorioController renderiza: 'gestao/relatorios/indicadores' + shape das props)
  </read_first>
  <action>
    Atualizar `resources/js/pages/gestao/dashboard.tsx`: quando `kpis.relatorios` presente, renderizar os KpiCards do EP15 (sem delta quando ausente) + 1–2 gráficos `<Chart>` (ex.: distribuição por risco/categoria; série de volume) usando `option` ECharts montado a partir das props.
    Criar `resources/js/pages/gestao/relatorios/indicadores.tsx`: PageHeader "Indicadores de Viabilidade"; filtros (período/setor/bairro/CNAE/categoria) via os controles existentes do DS; gráficos `<Chart>` para porPeriodo/porZona/porCnae/porRisco e cards para taxaDeferimento/taxaIndeferimento; uma DataTable do detalhamento com `useServerTable` + `<ExportMenu url={route('gestao.relatorios.indicadores')} params={currentParams} />`. Exibir o RÓTULO honesto de degradação da zona→bairro vindo das props (HU-124). Tipar as props conforme o payload de 15-09.
  </action>
  <acceptance_criteria>
    - `grep -q "Chart" resources/js/pages/gestao/relatorios/indicadores.tsx` e `grep -q "ExportMenu" resources/js/pages/gestao/relatorios/indicadores.tsx`
    - `grep -q "relatorios" resources/js/pages/gestao/dashboard.tsx`
    - `npx tsc --noEmit` sai 0 (tipagem das props de indicadores/dashboard)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Navegação — grupo Relatórios na sidebar + Cmd+K (gated) + build</name>
  <files>resources/js/components/app/app-sidebar.tsx, resources/js/components/app/command-search.tsx</files>
  <read_first>
    - resources/js/components/app/app-sidebar.tsx (como os grupos/itens são declarados e gated por permissão — ex.: Auditoria/LGPD/Abuso da Fase 12; adicionar o grupo Relatórios)
    - resources/js/components/app/command-search.tsx (como as entradas do Cmd+K são declaradas/gated — adicionar as entradas dos relatórios)
    - resources/js/layouts/gestao-layout.tsx (como a permissão do usuário chega aos componentes de nav — shared props)
  </read_first>
  <action>
    No `app-sidebar.tsx`, adicionar o grupo "Relatórios" (gated por `consultar-relatorios`) com os itens: Dashboard (já existe — não duplicar), Indicadores de Viabilidade (`gestao.relatorios.indicadores`), Tempo de Análise (`gestao.relatorios.tempo`), Produtividade (`gestao.relatorios.produtividade`), Quedas do Expresso (`gestao.relatorios.quedas`); e Feriados (`gestao.feriados.index`, gated por `manter-parametros`). As páginas de tempo/produtividade/quedas/feriados são criadas em 15-14 — os links já apontam para as rotas reais (criadas em 15-09).
    No `command-search.tsx`, adicionar as mesmas entradas (gated) para a busca global Cmd+K.
    Build smoke: `npm run build` verde com a nav atualizada.
  </action>
  <acceptance_criteria>
    - `grep -q "consultar-relatorios" resources/js/components/app/app-sidebar.tsx` e `grep -q "relatorios" resources/js/components/app/command-search.tsx`
    - `npm run build` sai 0
    - `vendor/bin/pint --dirty --format agent` sem pendências (DashboardController/teste PHP)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=DashboardKpis` verde; `npx tsc --noEmit` e `npm run build` verdes.
- Anti-fachada: KPI sem delta inventado; zona→bairro com rótulo honesto na tela.
</verification>

<success_criteria>
Dashboard executivo com KPIs reais (sem delta fabricado), página de Indicadores de Viabilidade navegável com gráficos ECharts e export, e o grupo Relatórios na navegação — tudo gated por consultar-relatorios.
</success_criteria>

<output>
Criar `.planning/phases/15-relatorios-e-indicadores/15-13-SUMMARY.md` com: os KPIs do EP15 adicionados ao dashboard, o shape das props de `indicadores.tsx`, e a lista de itens de nav/Cmd+K adicionados (que 15-14 completa com as páginas restantes).
</output>

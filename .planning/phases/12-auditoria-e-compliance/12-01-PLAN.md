---
phase: 12-auditoria-e-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/XXXX_XX_XX_XXXXXX_add_audit_query_indexes_and_personal_data_to_activity_log.php
  - app/Support/Audit/AuditService.php
  - tests/Feature/Auditoria/AuditServicePersonalDataTest.php
autonomous: true
requirements: [HU-100, HU-102]

must_haves:
  truths:
    - "A trilha activity_log fica consultável em escala: a migration ADITIVA cria os índices que a consulta por período/fonte/ação da Fase 12 usa — created_at, composto (log_name, created_at) e event — sem tabela denormalizada nem materialized view (decisão CONTEXT: consultar a espinha direto)"
    - "activity_log ganha a coluna ADITIVA personal_data (boolean nullable, INDEXADA): marca os registros de auditoria que tocaram dado pessoal (LGPD HU-102), default null/false — não altera nenhum registro existente"
    - "AuditService::log aceita o parâmetro ADITIVO bool \\$personalData=false e grava activity_log.personal_data via forceFill (junto de result/rules_version), sem mudar a assinatura posicional usada hoje (parâmetro nomeado/no fim)"
    - "ANTI-REGRESSÃO transversal: TODOS os call sites atuais de AuditService::log (Fases 1–11) continuam VERDES sem alteração — personalData tem default false e nada é marcado como dado pessoal por engano"
    - "É o ÚNICO editor de AuditService e o ÚNICO dono da migration de índices/personal_data do activity_log na Fase 12 (a marcação real dos call sites sensíveis é da 12-06 LGPD)"
  artifacts:
    - path: "database/migrations/XXXX_XX_XX_XXXXXX_add_audit_query_indexes_and_personal_data_to_activity_log.php"
      provides: "Índices de consulta (created_at; (log_name,created_at); event) + coluna personal_data boolean nullable indexada em activity_log"
      contains: "personal_data"
    - path: "app/Support/Audit/AuditService.php"
      provides: "log(..., bool \\$personalData=false) gravando personal_data (aditivo)"
      contains: "personalData"
  key_links:
    - from: "app/Support/Audit/AuditService.php"
      to: "activity_log.personal_data"
      via: "forceFill grava personal_data junto de result/rules_version após o log()"
      pattern: "personal_data"
    - from: "database/migrations/XXXX_XX_XX_XXXXXX_add_audit_query_indexes_and_personal_data_to_activity_log.php"
      to: "activity_log"
      via: "Schema::table aditivo (índices + coluna), reversível no down()"
      pattern: "->index\\(|boolean\\('personal_data'\\)"
---

<objective>
Fundação de LEITURA e LGPD sobre a espinha `activity_log` (Wave 1, par paralelo com 12-02/12-03): migration ADITIVA com os índices que a consulta unificada (HU-100) precisa para paginar por período/fonte/ação em escala, mais a coluna `personal_data` (HU-102) que marca acessos a dado pessoal; e a extensão ADITIVA do `AuditService::log` para gravar essa marca.

Purpose: o CONTEXT decidiu que `activity_log` JÁ É a trilha unificada — consultá-la direto (server-driven), sem materializar. Para isso, índices de consulta são pré-requisito (sem eles a paginação por data/fonte degrada). A coluna `personal_data` + o parâmetro no `AuditService` são a infraestrutura mínima para o painel LGPD (12-06) marcar e medir acessos a PII — tudo ADITIVO, sem tocar a lógica de auditoria existente nem nenhum call site (anti-regressão transversal das Fases 1–11).
Output: 1 migration aditiva (índices + personal_data) + `AuditService::log(..., bool $personalData=false)` gravando a coluna, coberto por feature test e com a suíte de auditoria existente VERDE.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-auditoria-e-compliance/12-CONTEXT.md
@docs/superpowers/specs/2026-06-15-auditoria-compliance-design.md

# PADRÕES A ESPELHAR (migration aditiva de coluna/índice + serviço de auditoria) — ler antes de codar:
@database/migrations/2026_06_09_234627_create_activity_log_table.php
@app/Support/Audit/AuditService.php
@app/Models/Activity.php
@docs/SILE_HUs_Completas_MD/EP12-Auditoria-e-Compliance/HU-100-Consultar-trilha-de-auditoria.md
@docs/SILE_HUs_Completas_MD/EP12-Auditoria-e-Compliance/HU-102-Monitorar-conformidade-LGPD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration aditiva — índices de consulta + personal_data em activity_log</name>
  <read_first>database/migrations/2026_06_09_234627_create_activity_log_table.php (estrutura atual: log_name/result/rules_version já indexados; event e created_at NÃO), app/Models/Activity.php ($guarded=[] no pai — coluna nova já mass-assignable)</read_first>
  <files>database/migrations/XXXX_XX_XX_XXXXXX_add_audit_query_indexes_and_personal_data_to_activity_log.php</files>
  <action>
    TDD: como é schema puro, a prova é `php artisan migrate` + a verificação da coluna no Task 2 (o teste do AuditService grava/lê personal_data). Gerar via `php artisan make:migration add_audit_query_indexes_and_personal_data_to_activity_log --table=activity_log --no-interaction`.

    `up()` (Schema::table, ADITIVO — não recria nada existente):
    - `personal_data` boolean nullable (default null) — marca acesso a dado pessoal (HU-102). NÃO usar default true; nada é PII por engano.
    - índices ADITIVOS para a consulta da trilha (HU-100): `index('created_at')` (ordenação/janela por período), `index(['log_name', 'created_at'])` (filtro por fonte + ordenação), `index('event')` (filtro por ação) e `index('personal_data')` (recorte LGPD). NÃO duplicar índices já existentes (log_name single, result, rules_version já têm).
    - Atenção SQLite (banco de teste): adicionar coluna nullable + índices é suportado; não combinar com mudança de coluna existente.

    `down()`: dropar os índices criados e a coluna `personal_data` (reversível). Use nomes de índice explícitos se o driver exigir para o drop.

    NÃO mexer em colunas existentes (RN do Laravel: alterar coluna exige repetir todos os atributos — aqui só ADICIONAMOS).
  </action>
  <verify>php artisan migrate --no-interaction && vendor/bin/pint --dirty --format agent</verify>
  <acceptance_criteria>
    - `php artisan migrate` aplica sem erro (SQLite de teste e Postgres).
    - grep "personal_data" e os índices (created_at; ['log_name','created_at']; event) na migration; `down()` reverte coluna e índices.
  </acceptance_criteria>
  <done>activity_log preparada para consulta em escala (índices) e para LGPD (personal_data) — tudo aditivo, sem tocar registros/colunas existentes.</done>
</task>

<task type="auto">
  <name>Task 2: AuditService::log com personalData (aditivo) + anti-regressão dos call sites</name>
  <read_first>app/Support/Audit/AuditService.php (forceFill pós-log de result/rules_version), app/Models/Activity.php</read_first>
  <files>app/Support/Audit/AuditService.php, tests/Feature/Auditoria/AuditServicePersonalDataTest.php</files>
  <action>
    TDD estrito (RED primeiro). Estender `AuditService::log` com o parâmetro ADITIVO `bool $personalData = false` (último parâmetro, mantendo a compatibilidade total — todos os call sites atuais usam parâmetros nomeados/posicionais que NÃO mudam). No bloco `forceFill` existente, gravar também `'personal_data' => $personalData` junto de `result`/`rules_version`. NÃO mudar o `logBlocked` (continua false por padrão).

    `AuditServicePersonalDataTest` (SQLite, novo arquivo em tests/Feature/Auditoria/):
    - `log(..., personalData: true)` grava `activity_log.personal_data` = true (assertDatabaseHas).
    - `log(...)` sem o parâmetro grava `personal_data` falsy (false/null) — o default NÃO marca PII (anti-fachada LGPD: nada é dado pessoal por engano).
    - opcional: confirmar que o registro continua gravando result/rules_version como antes (não houve regressão no forceFill).

    ANTI-REGRESSÃO: rode a suíte de auditoria existente (filtro abaixo) — todos os call sites das Fases 1–11 continuam verdes (o parâmetro é aditivo com default false).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="AuditService"</verify>
  <acceptance_criteria>
    - grep "personalData" e "personal_data" em app/Support/Audit/AuditService.php.
    - AuditServicePersonalDataTest verde (true grava; default não marca).
    - `php artisan test --compact --filter=AuditService` verde (sem regressão nos consumidores existentes).
  </acceptance_criteria>
  <done>AuditService grava personal_data de forma aditiva; trilha pronta para a marcação real dos call sites sensíveis (12-06), sem regressão transversal.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste / Evidência |
|---|---|
| HU-100 — trilha consultável em escala (índices de período/fonte/ação) | `php artisan migrate` + grep índices |
| HU-102 — acesso a dado pessoal é marcável (personal_data) | AuditServicePersonalDataTest |
| RN-002 / anti-regressão — AuditService aditivo, call sites 1–11 verdes | `--filter=AuditService` |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan migrate --no-interaction` aplica a migration aditiva sem erro.
- `php artisan test --compact --filter=AuditService` verde (AuditServicePersonalDataTest + consumidores existentes).
- Suíte completa permanece verde (nada de lógica de negócio mudou; só schema aditivo + parâmetro com default).
</verification>

<success_criteria>
- Índices de consulta da trilha (created_at; (log_name,created_at); event) e coluna personal_data criados de forma ADITIVA.
- AuditService::log grava personal_data (aditivo, default false) sem regressão dos call sites das Fases 1–11.
- ZERO dependência nova; ZERO alteração de coluna existente; ÚNICO dono de AuditService e da migration de activity_log na fase.
</success_criteria>

<output>
Criar `.planning/phases/12-auditoria-e-compliance/12-01-SUMMARY.md`: nome final da migration, a assinatura final de `AuditService::log` (posição do `personalData`) e os índices criados. Insumo direto de 12-04 (AuditTrailQueryService consulta com os índices), 12-06 (LGPD marca personal_data nos call sites reais e mede) e 12-11 (verificação integral).
</output>

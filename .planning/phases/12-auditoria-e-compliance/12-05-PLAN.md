---
phase: 12-auditoria-e-compliance
plan: 05
type: execute
wave: 2
depends_on: ["12-02"]
files_modified:
  - app/Services/Auditoria/DecisionExplanationService.php
  - app/Http/Resources/DecisionExplanationResource.php
  - app/Http/Controllers/Gestao/ResultadoExpressoController.php
  - app/Http/Controllers/Gestao/ProcessoController.php
  - tests/Feature/Auditoria/DecisionExplanationTest.php
autonomous: true
requirements: [HU-099]

must_haves:
  truths:
    - "HU-099 RN-004: DecisionExplanationService::explain(ViabilityDecision) devolve a explicação passo a passo (por CNAE, na ordem entrada→risco→louos quadro7→10→11→11a→consolidação→desfecho, cada passo com entrada/resultado_parcial/motivo/versao_regra) projetada do decision_trace + rules_versions + fundamentacao GRAVADOS"
    - "HU-099 RN-005 — PROJEÇÃO PURA: o serviço NUNCA chama o motor (resolver/LOUOS/risco). Um spy do motor registra ZERO chamadas durante explain()"
    - "Decisão LEGADA (decision_trace null) degrada HONESTO: monta a explicação compacta de per_cnae/fundamentacao/rules_versions e MARCA explicitamente os passos não snapshotados como 'não registrado nesta decisão' — nunca inventa nem reexecuta"
    - "A explicação é exposta no detalhe da decisão na retaguarda: ResultadoExpressoController::show e ProcessoController::show passam a enviar a prop 'explicacao' (DecisionExplanationResource), sob as permissões que já existem (consultar-solicitacoes) — SEM rota nova e SEM permissão nova"
    - "É um par paralelo de 12-04/12-06 sem sobreposição: NÃO toca routes/gestao.php, config/sile.php, AuditTrailQueryService nem o motor de abuso"
  artifacts:
    - path: "app/Services/Auditoria/DecisionExplanationService.php"
      provides: "explain(ViabilityDecision): array — projeção pura do trace; legado degrada honesto"
      contains: "explain"
    - path: "app/Http/Resources/DecisionExplanationResource.php"
      provides: "Shape de apresentação dos passos (com marca 'não registrado' p/ legado)"
      contains: "passos"
    - path: "app/Http/Controllers/Gestao/ResultadoExpressoController.php"
      provides: "show passa a enviar a prop explicacao (HU-099)"
      contains: "explicacao"
  key_links:
    - from: "app/Http/Controllers/Gestao/ResultadoExpressoController.php"
      to: "app/Services/Auditoria/DecisionExplanationService.php"
      via: "show() injeta o service e envia DecisionExplanationResource como prop 'explicacao'"
      pattern: "DecisionExplanationService|explicacao"
    - from: "app/Services/Auditoria/DecisionExplanationService.php"
      to: "ViabilityDecision (decision_trace/rules_versions/fundamentacao)"
      via: "lê SOMENTE campos persistidos; nunca instancia/chama o motor (RN-005)"
      pattern: "decision_trace"
---

<objective>
Explicabilidade passo a passo das decisões (HU-099) como PROJEÇÃO PURA (Wave 2, par paralelo com 12-04/12-06): `DecisionExplanationService::explain(ViabilityDecision)` que LÊ o `decision_trace` (gravado na 12-02) + `rules_versions` + `fundamentacao` e devolve a visualização ordenada — sem NUNCA chamar o motor (RN-005) — mais o `DecisionExplanationResource` e a exposição da prop `explicacao` no detalhe da decisão (`ResultadoExpressoController::show` e `ProcessoController::show`), reusando as permissões existentes.

Purpose: a 12-02 fez a decisão passar a GRAVAR o trace; este plano o TORNA LEGÍVEL para analista/gestor de forma reproduzível (RN-005 — fonte é o registro, nunca a reexecução). Decisão legada (sem trace) degrada honesta, marcando passos não snapshotados como "não registrado" — anti-fachada: nunca inventa nem recomputa. Sem rota/permissão nova: a explicação aparece no detalhe da decisão que o gestor já consulta (consultar-solicitacoes). A integração na trilha (12-04/12-10) é por link ao detalhe — sem acoplar este plano ao AuditoriaController.
Output: service de projeção + resource + exposição em 2 controllers existentes, com testes de ordem, de spy=0 (RN-005) e de degradação honesta do legado.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-auditoria-e-compliance/12-CONTEXT.md
@docs/superpowers/specs/2026-06-15-auditoria-compliance-design.md
@.planning/phases/12-auditoria-e-compliance/12-02-SUMMARY.md

# FONTE (trace) + PONTOS DE EXPOSIÇÃO (ler antes de codar):
@app/Models/ViabilityDecision.php
@app/Http/Controllers/Gestao/ResultadoExpressoController.php
@app/Http/Controllers/Gestao/ProcessoController.php
@app/Http/Resources/ViabilityDecisionResource.php
@docs/SILE_HUs_Completas_MD/EP12-Auditoria-e-Compliance/HU-099-Consultar-regras-aplicadas.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DecisionExplanationService (projeção pura) + Resource — RN-004/005</name>
  <read_first>app/Models/ViabilityDecision.php (decision_trace/per_cnae/rules_versions/fundamentacao já são array), 12-02-SUMMARY (shape do decision_trace), app/Services/Solicitacao/SolicitacaoViabilityResolver.php (o motor que NÃO pode ser chamado — para o spy)</read_first>
  <files>app/Services/Auditoria/DecisionExplanationService.php, app/Http/Resources/DecisionExplanationResource.php, tests/Feature/Auditoria/DecisionExplanationTest.php</files>
  <action>
    TDD estrito (RED primeiro). `DecisionExplanationService::explain(ViabilityDecision $decision): array` — PROJEÇÃO PURA:
    - Quando `decision_trace` existe: projeta por CNAE, na ordem do trace (entrada → risco → louos quadro7→10→11→11a → consolidação → desfecho), cada passo com {entrada, resultado_parcial, motivo, versao_regra} lidos do trace; anexa fundamentacao consolidada e o mapa rules_versions. NUNCA instancia/chama o resolver ou os motores LOUOS/risco.
    - Quando `decision_trace` é null (legado): monta uma explicação COMPACTA a partir de per_cnae/fundamentacao/rules_versions e MARCA cada passo não snapshotado como "não registrado nesta decisão" (flag explícita no payload). Sem inventar valores.

    `DecisionExplanationResource` (ou um array DTO + resource fino): expõe `por_cnae` (lista de passos ordenados com a flag registrado/não-registrado), `fundamentacao`, `rules_versions`, `legado` (bool) e o desfecho. pt-BR nos rótulos.

    `DecisionExplanationTest` (SQLite):
    - decisão com decision_trace (factory) → explain devolve os passos na ORDEM correta, com versao_regra/motivo do trace.
    - RN-005 (spy=0): faça bind de um SPY/fake do `SolicitacaoViabilityResolver` (e/ou dos motores) que INCREMENTA um contador ou LANÇA se chamado; rode explain() e asserir ZERO chamadas (o teste passa só se o motor não foi tocado).
    - legado (decision_trace null) → explain marca passos como "não registrado" e NÃO lança; nada inventado.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=DecisionExplanationTest</verify>
  <acceptance_criteria>
    - grep "explain" em DecisionExplanationService; "passos"/"nao_registrado" no resource/payload.
    - DecisionExplanationTest verde: ordem dos passos, spy=0 (RN-005), legado degrada honesto.
  </acceptance_criteria>
  <done>Explicação passo a passo reproduzível (projeção pura do trace), com legado honesto — sem recomputo.</done>
</task>

<task type="auto">
  <name>Task 2: Expor a explicação no detalhe da decisão (ResultadoExpresso + Processo show)</name>
  <read_first>app/Http/Controllers/Gestao/ResultadoExpressoController.php (show já carrega decision + audita), app/Http/Controllers/Gestao/ProcessoController.php (show já carrega decision + audita)</read_first>
  <files>app/Http/Controllers/Gestao/ResultadoExpressoController.php, app/Http/Controllers/Gestao/ProcessoController.php, tests/Feature/Auditoria/DecisionExplanationTest.php</files>
  <action>
    TDD estrito. Injetar `DecisionExplanationService` nos dois controllers e, no `show`, quando houver decisão, enviar a prop ADITIVA `explicacao` = `(new DecisionExplanationResource($service->explain($decision)))->resolve()` (ou o array do service). NÃO mudar gates, rotas, auditoria nem as demais props (aditivo):
    - `ResultadoExpressoController::show`: já carrega `decision`; adicionar `explicacao` ao Inertia::render('gestao/resultados-expresso/show', ...). Sem decisão → 404 já existente (sem explicação inventada).
    - `ProcessoController::show`: adicionar `explicacao` quando `decision` existir (a UI renderiza só se presente); manter gate consultar-solicitacoes e a auditoria atual. (NOTA: a marcação personal_data do audit deste show é da 12-07 — aqui só adicionamos a prop; 12-07 depende deste plano para serializar a edição do mesmo método.)

    Estender `DecisionExplanationTest`: o show de resultados-expresso e o de processos retornam a prop `explicacao` com os passos (Inertia assertion); decisão legada → prop presente com a marca "não registrado"; processo sem decisão → sem explicação (não inventa).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="DecisionExplanationTest|ResultadoExpresso|Processo"</verify>
  <acceptance_criteria>
    - grep "explicacao" em ResultadoExpressoController e ProcessoController (prop no show).
    - DecisionExplanationTest verde (prop presente nos dois show; legado marcado; sem decisão sem explicação).
    - Suítes ResultadoExpresso/Processo CONTINUAM verdes (mudança aditiva).
  </acceptance_criteria>
  <done>A explicação passo a passo aparece no detalhe da decisão (expresso e análise), sem rota/permissão nova e sem regressão.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-099 RN-004 — passo a passo legível na ordem | DecisionExplanationTest |
| HU-099 RN-005 — projeção pura, motor com 0 chamadas | DecisionExplanationTest (spy=0) |
| HU-099 — legado degrada honesto ('não registrado') | DecisionExplanationTest |
| HU-099 — exposto no detalhe (consultar-solicitacoes, sem rota/permissão nova) | DecisionExplanationTest (show) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="DecisionExplanationTest|ResultadoExpresso|Processo"` verde.
- Suíte completa CONTINUA verde; este plano NÃO toca routes/gestao.php nem config/sile.php (sem conflito com 12-04/12-06).
</verification>

<success_criteria>
- explain() projeta o trace passo a passo de forma reproduzível (RN-004/005), sem chamar o motor (spy=0).
- Legado degrada honesto; explicação exposta no detalhe da decisão sem rota/permissão nova.
- ZERO dependência nova; par paralelo limpo (sem sobreposição de arquivos com 12-04/12-06).
</success_criteria>

<output>
Criar `.planning/phases/12-auditoria-e-compliance/12-05-SUMMARY.md`: a assinatura de explain(), o shape do DecisionExplanationResource (passos + flag registrado/legado), e as props 'explicacao' adicionadas aos dois show. Insumo direto de 12-10 (componente de explicabilidade no detalhe) e de 12-07 (que edita o MESMO ProcessoController::show para marcar personal_data — depende deste plano).
</output>

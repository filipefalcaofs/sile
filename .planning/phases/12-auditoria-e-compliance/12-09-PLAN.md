---
phase: 12-auditoria-e-compliance
plan: 09
type: execute
wave: 4
depends_on: ["12-03", "12-06", "12-07", "12-08"]
files_modified:
  - app/Http/Controllers/Gestao/AbusoController.php
  - app/Http/Resources/AbuseAlertResource.php
  - routes/gestao.php
  - tests/Feature/Abuso/AbusoPainelTest.php
autonomous: true
requirements: [HU-149]

must_haves:
  truths:
    - "HU-149 CA-03: o gestor/admin (gerenciar-alertas-abuso) lista os abuse_alerts com filtros (rule_key/severity/status/período) paginados, e vê o indicador de EFETIVIDADE = confirmados ÷ gerados (por regra e geral) — calculado sobre os alertas reais (RN-005)"
    - "Confirmar e descartar exigem justification OBRIGATÓRIA, gravam resolved_by/resolved_at + status, e são AUDITADOS (RN-002/RN-003) — espelha o motivo obrigatório da malha fina"
    - "HU-149 CA-02 anti-fachada: confirmar/descartar mudam SÓ o status do ALERTA — NUNCA indeferem/cassam nem transicionam o status do PROCESSO (a malha fina é ortogonal e já foi acionada pelo motor quando acima do limiar)"
    - "É o ÚNICO editor de routes/gestao.php na Wave 4 (par paralelo de 12-10, que só mexe no frontend); somente leitura+resolução de alertas, sem tocar o motor (12-06/12-08)"
    - "403 sem gerenciar-alertas-abuso é auditado no ponto único; a consulta do painel é auditada"
  artifacts:
    - path: "app/Http/Controllers/Gestao/AbusoController.php"
      provides: "index (lista+filtros+efetividade) + confirmar/descartar (justification obrigatória, auditados)"
      contains: "efetividade"
    - path: "app/Http/Resources/AbuseAlertResource.php"
      provides: "Shape do alerta (rule_key/severity/status/evidence/processo/malha fina/resolução)"
      contains: "rule_key"
    - path: "routes/gestao.php"
      provides: "gestao.abuso.* (index + confirmar + descartar) sob gerenciar-alertas-abuso"
      contains: "abuso"
  key_links:
    - from: "app/Http/Controllers/Gestao/AbusoController.php"
      to: "app/Models/AbuseAlert.php"
      via: "lista/efetividade sobre abuse_alerts; confirmar/descartar muda status do alerta (NUNCA do processo)"
      pattern: "AbuseAlert"
    - from: "routes/gestao.php"
      to: "Gestao\\AbusoController"
      via: "grupo permission:gerenciar-alertas-abuso → index/confirmar/descartar"
      pattern: "gerenciar-alertas-abuso"
---

<objective>
Painel humano de alertas de abuso (HU-149) — revisão, NUNCA punição (Wave 4; ÚNICO route-owner de `gestao.php` na wave, par paralelo de 12-10): `Gestao\AbusoController` (lista filtrável + indicador de efetividade + confirmar/descartar com justificativa obrigatória, tudo auditado e gated por `gerenciar-alertas-abuso`), `AbuseAlertResource` e as rotas `gestao.abuso.*`.

Purpose: os detectores (12-06/12-08) geram alertas e encaminham à malha fina acima do limiar; falta a superfície humana que CA-02/CA-03 exigem — consultar, medir efetividade (confirmados÷gerados, para calibrar regras — RN-005) e confirmar/descartar com justificativa. A defesa anti-fachada central: confirmar/descartar muda SÓ o status do ALERTA — nunca decide o processo (a malha fina é insumo de revisão, ortogonal ao status). Somente leitura + resolução; o motor não é tocado.
Output: controller + resource + rotas, cobertos por testes (lista/filtros/efetividade, justificativa obrigatória, NUNCA muda status do processo, auditoria, 403).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-auditoria-e-compliance/12-CONTEXT.md
@docs/superpowers/specs/2026-06-15-auditoria-compliance-design.md
@.planning/phases/12-auditoria-e-compliance/12-03-SUMMARY.md
@.planning/phases/12-auditoria-e-compliance/12-06-SUMMARY.md

# PADRÕES A ESPELHAR (controller server-driven + ação auditada com justificativa + resource + rota gated) — ler antes de codar:
@app/Http/Controllers/Gestao/MalhaFinaController.php
@app/Http/Controllers/Gestao/ResultadoExpressoController.php
@app/Models/AbuseAlert.php
@routes/gestao.php
@docs/SILE_HUs_Completas_MD/EP12-Auditoria-e-Compliance/HU-149-Detectar-padroes-de-abuso-e-fraude.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AbusoController@index (lista + filtros + efetividade) + AbuseAlertResource + rota</name>
  <read_first>app/Models/AbuseAlert.php (status/severity/evidence/relations), app/Http/Controllers/Gestao/ResultadoExpressoController.php (index server-driven: filtros/paginação/auditoria), routes/gestao.php (grupo permission + ordem estática antes de wildcard)</read_first>
  <files>app/Http/Controllers/Gestao/AbusoController.php, app/Http/Resources/AbuseAlertResource.php, routes/gestao.php, tests/Feature/Abuso/AbusoPainelTest.php</files>
  <action>
    TDD estrito (RED primeiro). `Gestao\AbusoController@index(Request)`: lista `abuse_alerts` com filtros (rule_key, severity, status, período por detected_at) — `when()`/whereLike caseSensitive:false — paginados server-side (reusar um per_page do estilo PER_PAGE_OPTIONS), eager-load viabilityRequest/fineMeshReferral, via `AbuseAlertResource`. Calcula EFETIVIDADE = confirmados ÷ gerados (geral e por rule_key) sobre os alertas reais (RN-005). AUDITA a consulta ('abuso','consulta-alertas'). Inertia::render('gestao/abuso/index', props: alertas paginados, filtros, efetividade, ruleKeyOptions/severityOptions/statusOptions).

    `AbuseAlertResource`: rule_key, severity (value+label), status (value+label), detected_at, window, evidence (minimizado/legível), processo (protocol_number/link quando há viability_request_id), encaminhado_malha_fina (bool via fine_mesh_referral_id), resolução (resolved_by/resolved_at/justification quando houver).

    Rotas em `routes/gestao.php` (ÚNICO editor na Wave 4) sob `permission:gerenciar-alertas-abuso`: `GET gestao/abuso` → index (name `gestao.abuso.index`). (As ações de confirmar/descartar entram no Task 2 — mesmo grupo.)

    `AbusoPainelTest` (SQLite): semeia alertas (factory) variados; index lista/filtra/pagina; efetividade calcula confirmados÷gerados correto (geral e por regra); consulta auditada; SEM gerenciar-alertas-abuso → 403.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=AbusoPainelTest && php artisan route:list --path=gestao/abuso</verify>
  <acceptance_criteria>
    - grep "efetividade" no controller; "rule_key" no resource; "gerenciar-alertas-abuso" na rota.
    - AbusoPainelTest verde (lista/filtros/efetividade/403/auditoria); route:list mostra gestao.abuso.index.
  </acceptance_criteria>
  <done>Painel de alertas real (lista + filtros + efetividade), gated e auditado — base da revisão humana.</done>
</task>

<task type="auto">
  <name>Task 2: confirmar/descartar (justificativa obrigatória, auditados, NUNCA muda status do processo)</name>
  <read_first>app/Services/Analise/MalhaFinaService.php (motivo obrigatório — padrão de justificativa), app/Models/AbuseAlert.php (status/resolved_by/resolved_at/justification)</read_first>
  <files>app/Http/Controllers/Gestao/AbusoController.php, routes/gestao.php, tests/Feature/Abuso/AbusoPainelTest.php</files>
  <action>
    TDD estrito. Adicionar ao `AbusoController` as ações `confirmar(AbuseAlert)` e `descartar(AbuseAlert)` (ou um `resolver` com parâmetro): validam justification OBRIGATÓRIA (Form Request ou validação inline — string não-vazia, espelhando o motivo obrigatório da malha fina), gravam `status` (confirmado/descartado) + `resolved_by_user_id` + `resolved_at` + `justification`, e AUDITAM (RN-002/RN-003). CA-02 ANTI-FACHADA: NÃO transicionar o status do PROCESSO, NÃO indeferir/cassar, NÃO mexer na malha fina já criada — só o status do ALERTA muda.

    Rotas (mesmo grupo gerenciar-alertas-abuso): `POST gestao/abuso/{abuseAlert}/confirmar` (name `gestao.abuso.confirmar`) e `POST gestao/abuso/{abuseAlert}/descartar` (name `gestao.abuso.descartar`). Rotas estáticas/dois-segmentos não colidem com index.

    Estender `AbusoPainelTest`: confirmar/descartar sem justificativa → erro de validação (nada muda); com justificativa → status/resolved_by/resolved_at/justification gravados e AUDITADO; o status do PROCESSO vinculado permanece INALTERADO (CA-02); efetividade reflete o confirmado. SEM gerenciar-alertas-abuso → 403.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=AbusoPainelTest</verify>
  <acceptance_criteria>
    - grep "confirmar"/"descartar" no controller e nas rotas; validação de justification obrigatória.
    - AbusoPainelTest verde (justificativa obrigatória; status do alerta muda; status do PROCESSO inalterado; auditoria; 403).
  </acceptance_criteria>
  <done>Revisão humana real: confirmar/descartar com justificativa auditada, sem NUNCA punir o processo (anti-fachada CA-02).</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-149 CA-03 — lista + efetividade (confirmados÷gerados) | AbusoPainelTest |
| HU-149 CA-02 — confirmar/descartar NUNCA muda status do processo | AbusoPainelTest |
| HU-149 RN-002/003 — justificativa obrigatória + auditoria | AbusoPainelTest |
| Segurança — 403 sem gerenciar-alertas-abuso (auditado) | AbusoPainelTest |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=AbusoPainelTest` verde.
- `php artisan route:list --path=gestao/abuso` lista index/confirmar/descartar sob gerenciar-alertas-abuso.
- Suíte completa CONTINUA verde; ÚNICO editor de routes/gestao.php na Wave 4.
</verification>

<success_criteria>
- Painel de alertas real: lista/filtros/efetividade + confirmar/descartar com justificativa auditada.
- CA-02 anti-fachada garantida por teste: resolução do alerta NUNCA altera o status do processo.
- ZERO dependência nova; ÚNICO route-owner da Wave 4.
</success_criteria>

<output>
Criar `.planning/phases/12-auditoria-e-compliance/12-09-SUMMARY.md`: as rotas gestao.abuso.* (names), o shape do AbuseAlertResource, o cálculo da efetividade e as props do Inertia 'gestao/abuso/index'. Insumo direto de 12-11 (página do painel de abuso) e 12-12 (smoke/seed dev de alertas).
</output>

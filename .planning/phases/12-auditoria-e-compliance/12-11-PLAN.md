---
phase: 12-auditoria-e-compliance
plan: 11
type: execute
wave: 5
depends_on: ["12-09", "12-10"]
files_modified:
  - resources/js/pages/gestao/abuso/index.tsx
  - resources/js/components/app/app-sidebar.tsx
  - resources/js/components/app/command-search.tsx
autonomous: true
requirements: [HU-149, HU-098, HU-100, HU-102]

must_haves:
  truths:
    - "A página gestao/abuso/index consome as props da 12-09: lista filtrável de alertas, indicador de efetividade (confirmados÷gerados) e as ações confirmar/descartar com justificativa OBRIGATÓRIA (modal/form) — deixando explícito que NUNCA pune o processo"
    - "A navegação da retaguarda (app-sidebar) ganha as entradas Auditoria/Trilha, LGPD e Alertas de Abuso, cada uma gated pela permissão correta (consultar-auditoria, monitorar-lgpd, gerenciar-alertas-abuso) — quem não tem a permissão não vê o item"
    - "O Cmd+K (command-search) ganha os destinos das 3 superfícies (trilha, LGPD, abuso), respeitando as mesmas permissões"
    - "É a ÚNICA dona de app-sidebar.tsx e command-search.tsx na fase; depende da 12-10 (páginas auditoria/lgpd já existem) e da 12-09 (backend do abuso) — os links apontam para páginas reais"
    - "Compila limpo (npm run build) e a feature test AbusoPainelTest (12-09) segue verde; o render é validado no smoke da 12-12"
  artifacts:
    - path: "resources/js/pages/gestao/abuso/index.tsx"
      provides: "Painel de alertas (lista+filtros+efetividade+confirmar/descartar com justificativa)"
      contains: "efetividade"
    - path: "resources/js/components/app/app-sidebar.tsx"
      provides: "Entradas de navegação Auditoria/LGPD/Abuso gated por permissão"
      contains: "auditoria"
    - path: "resources/js/components/app/command-search.tsx"
      provides: "Destinos da trilha/LGPD/abuso no Cmd+K (gated)"
      contains: "auditoria"
  key_links:
    - from: "resources/js/components/app/app-sidebar.tsx"
      to: "rotas gestao.auditoria.index / gestao.lgpd.index / gestao.abuso.index"
      via: "itens de menu gated por can(consultar-auditoria/monitorar-lgpd/gerenciar-alertas-abuso)"
      pattern: "auditoria|lgpd|abuso"
    - from: "resources/js/pages/gestao/abuso/index.tsx"
      to: "rotas gestao.abuso.confirmar / gestao.abuso.descartar"
      via: "form/modal com justificativa obrigatória (router.post)"
      pattern: "confirmar|descartar"
---

<objective>
Frontend do painel de abuso e a navegação/Cmd+K das três superfícies (Wave 5; ÚNICA dona de `app-sidebar.tsx` e `command-search.tsx`): a página `gestao/abuso/index` (lista + efetividade + confirmar/descartar com justificativa) e as entradas de navegação + Cmd+K para Trilha, LGPD e Abuso, todas gated pelas permissões da fase.

Purpose: fecha a camada de UI — o backend do abuso (12-09) e as páginas de trilha/LGPD (12-10) já existem; faltam a página do painel de abuso e a forma de CHEGAR nas três (sidebar + Cmd+K), respeitando as permissões (quem não tem, não vê). Tudo server-driven; confirmar/descartar deixa explícito que é revisão humana, nunca punição do processo. Ativar `inertia-react-development` + `tailwindcss-development`.
Output: 1 página + nav + Cmd+K, compilando limpo, com AbusoPainelTest verde e o render validado no smoke (12-12).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-auditoria-e-compliance/12-CONTEXT.md
@.planning/phases/12-auditoria-e-compliance/12-09-SUMMARY.md
@.planning/phases/12-auditoria-e-compliance/12-10-SUMMARY.md

# PADRÕES A ESPELHAR (página de lista+ação com modal; nav gated; Cmd+K) — ler antes de codar:
@resources/js/pages/gestao/resultados-expresso/index.tsx
@resources/js/components/app/app-sidebar.tsx
@resources/js/components/app/command-search.tsx
@resources/js/pages/gestao/processos/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Página gestao/abuso/index (lista + efetividade + confirmar/descartar)</name>
  <read_first>resources/js/pages/gestao/resultados-expresso/index.tsx (lista server-driven), 12-09-SUMMARY (props: alertas paginados, filtros, efetividade; rotas confirmar/descartar e a obrigatoriedade da justificativa)</read_first>
  <files>resources/js/pages/gestao/abuso/index.tsx</files>
  <action>
    Skills `inertia-react-development` + `tailwindcss-development`. `gestao/abuso/index.tsx` server-driven: filtros (rule_key/severity/status/período) que disparam visita Inertia; tabela paginada dos alertas (rule_key, severity, status, processo/protocolo, encaminhado à malha fina, detectado em, evidência resumida); cartão/realce do indicador de EFETIVIDADE (confirmados÷gerados, geral e por regra). Ações confirmar/descartar via modal/form com campo de justificativa OBRIGATÓRIO (`router.post` para gestao.abuso.confirmar/descartar; exibir erros de validação). Deixar explícito na UI que confirmar/descartar é revisão humana e NÃO altera o processo (anti-fachada). Estado vazio honesto (ex.: "detecção desligada" quando não há alertas e o toggle está off).
  </action>
  <verify>npm run build && php artisan test --compact --filter=AbusoPainelTest</verify>
  <acceptance_criteria>
    - Existe resources/js/pages/gestao/abuso/index.tsx; grep "efetividade" e "confirmar"/"descartar".
    - `npm run build` compila; AbusoPainelTest segue verde.
  </acceptance_criteria>
  <done>Painel de alertas navegável (lista+efetividade+resolução com justificativa), anti-fachada explícito na UI.</done>
</task>

<task type="auto">
  <name>Task 2: Navegação (app-sidebar) + Cmd+K (command-search) das 3 superfícies, gated por permissão</name>
  <read_first>resources/js/components/app/app-sidebar.tsx (como os itens existentes são gated por permissão/can), resources/js/components/app/command-search.tsx (como os destinos são registrados/gated)</read_first>
  <files>resources/js/components/app/app-sidebar.tsx, resources/js/components/app/command-search.tsx</files>
  <action>
    Skills `inertia-react-development` + `tailwindcss-development`. Adicionar, espelhando o gating de permissão já usado:
    - app-sidebar: itens "Trilha de auditoria" (gestao.auditoria.index, can consultar-auditoria), "Conformidade LGPD" (gestao.lgpd.index, can monitorar-lgpd) e "Alertas de abuso" (gestao.abuso.index, can gerenciar-alertas-abuso). Agrupar de forma coerente com a navegação atual (ex.: seção de auditoria/compliance). Quem NÃO tem a permissão NÃO vê o item.
    - command-search (Cmd+K): destinos das 3 superfícies com os mesmos gates de permissão.
    NÃO duplicar itens; reusar ícones/estilos do design system. Estas são as ÚNICAS edições destes 2 arquivos na fase.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - grep "auditoria"/"lgpd"/"abuso" em app-sidebar.tsx e command-search.tsx, com o gating de permissão (can/permissões).
    - `npm run build` compila sem erro.
  </acceptance_criteria>
  <done>As 3 superfícies são alcançáveis pela navegação e pelo Cmd+K, gated por permissão — a fase fica navegável de ponta a ponta.</done>
</task>

</tasks>

## Mapa CA → evidência
| HU / RN | Evidência |
|---|---|
| HU-149 — painel de abuso navegável (lista/efetividade/resolução) | npm run build + AbusoPainelTest (12-09) + smoke (12-12) |
| HU-098/100/102/149 — navegação + Cmd+K gated por permissão | npm run build + smoke (12-12) |

<verification>
- `npm run build` compila sem erro (tipos/manifest).
- `php artisan test --compact --filter=AbusoPainelTest` verde.
- ÚNICA dona de app-sidebar.tsx e command-search.tsx; SÓ frontend (sem conflito com qualquer plano).
</verification>

<success_criteria>
- Painel de abuso navegável; as 3 superfícies acessíveis por sidebar + Cmd+K, gated por permissão.
- Compila limpo; AbusoPainelTest verde; anti-fachada explícito na UI de resolução.
- ZERO dependência nova; SÓ frontend.
</success_criteria>

<output>
Criar `.planning/phases/12-auditoria-e-compliance/12-11-SUMMARY.md`: a página de abuso, as entradas de navegação/Cmd+K adicionadas (com os gates) e quaisquer componentes compartilhados. Insumo direto de 12-12 (roteiro do smoke navegável).
</output>

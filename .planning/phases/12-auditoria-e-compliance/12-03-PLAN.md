---
phase: 12-auditoria-e-compliance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/XXXX_XX_XX_XXXXXX_create_abuse_alerts_table.php
  - app/Enums/AbuseSeverity.php
  - app/Enums/AbuseAlertStatus.php
  - app/Models/AbuseAlert.php
  - database/factories/AbuseAlertFactory.php
  - database/seeders/ParameterSeeder.php
  - database/seeders/RolesAndPermissionsSeeder.php
  - tests/Feature/Auditoria/AbuseAlertModelTest.php
  - tests/Feature/Seeders/ParameterSeederTest.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
  - tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
  - tests/Feature/Roles/ManageRolesTest.php
autonomous: true
requirements: [HU-149, HU-014]

must_haves:
  truths:
    - "Existe a tabela DEDICADA abuse_alerts (model AbuseAlert + HasAuditoria + factory): rule_key, severity (baixa/media/alta), status (aberto/confirmado/descartado), fingerprint, evidence json, viability_request_id nullable, subject morphs nullable, window_start/end, detected_at, resolved_by_user_id/resolved_at, justification, fine_mesh_referral_id nullable"
    - "A idempotência do detector é estrutural: índice ÚNICO PARCIAL (rule_key, fingerprint) restrito aos alertas em status 'aberto' — reprocessar a mesma janela NÃO duplica um alerta aberto"
    - "Os 7 parâmetros HU-014 da fase nascem no ParameterSeeder com tipo/default/validação: ui.auditoria.per_page (20), features.deteccao_abuso (0, toggle OFF default), abuso.janela_dias (30), abuso.volume_cnpj.limite (5), abuso.volume_contador.limite (20), abuso.escritorio_virtual.limite (3), abuso.severidade_malha_fina (alta) — reusa retencao.access_logs.dias"
    - "As 3 permissões aditivas existem e são atribuídas: consultar-auditoria (gestor+admin), monitorar-lgpd (admin), gerenciar-alertas-abuso (gestor+admin) — givePermissionTo aditivo, nunca sync (preserva ajustes da UI)"
    - "As contagens-baseline são atualizadas e VERDES no MESMO plano (dono único por wave): ParameterSeederTest/DatabaseSeederTest 78→85 (+ lista de grupos com 'abuso'); RolesAndPermissionsSeederTest/DatabaseSeederTest/ManageRolesTest 24→27 permissões"
  artifacts:
    - path: "database/migrations/XXXX_XX_XX_XXXXXX_create_abuse_alerts_table.php"
      provides: "Tabela abuse_alerts com índice único parcial (rule_key,fingerprint) WHERE status='aberto'"
      contains: "abuse_alerts"
    - path: "app/Models/AbuseAlert.php"
      provides: "Model AbuseAlert (HasAuditoria, casts enums+evidence array+datas, relações)"
      contains: "HasAuditoria"
    - path: "database/seeders/ParameterSeeder.php"
      provides: "7 parâmetros novos (auditoria/abuso + toggle features.deteccao_abuso)"
      contains: "features.deteccao_abuso"
    - path: "database/seeders/RolesAndPermissionsSeeder.php"
      provides: "consultar-auditoria, monitorar-lgpd, gerenciar-alertas-abuso + atribuições"
      contains: "gerenciar-alertas-abuso"
  key_links:
    - from: "database/migrations/XXXX_XX_XX_XXXXXX_create_abuse_alerts_table.php"
      to: "abuse_alerts (unicidade de alerta aberto)"
      via: "índice único parcial (rule_key, fingerprint) WHERE status = 'aberto' (driver-portável)"
      pattern: "fingerprint"
    - from: "database/seeders/RolesAndPermissionsSeeder.php"
      to: "Spatie Permission"
      via: "Permission::firstOrCreate + givePermissionTo aditivo das 3 permissões"
      pattern: "consultar-auditoria|monitorar-lgpd|gerenciar-alertas-abuso"
---

<objective>
Fundação de dados, parâmetros e permissões da fase (Wave 1, par paralelo com 12-01/12-02): a tabela `abuse_alerts` (model + enums + factory) que recebe os alertas da detecção (HU-149), os 7 parâmetros HU-014 administráveis (incluindo o toggle `features.deteccao_abuso` OFF por default) e as 3 permissões aditivas (`consultar-auditoria`, `monitorar-lgpd`, `gerenciar-alertas-abuso`) — com as contagens-baseline atualizadas no MESMO plano (dono único de ParameterSeeder/RolesAndPermissionsSeeder e das suítes de contagem nesta wave).

Purpose: HU-149 nasce DESLIGADA (toggle off) e nunca pune — só registra alertas e encaminha à malha fina. A tabela `abuse_alerts` é o ledger desses alertas, com idempotência estrutural (índice único parcial por alerta aberto) para o scheduler reprocessar sem duplicar. Os parâmetros e permissões são pré-requisito de toda a superfície de consulta/abuso da fase; centralizá-los aqui evita conflito de arquivo com os pares paralelos e respeita a lição da Fase 10 (atualizar TODAS as suítes de contagem junto da mudança).
Output: migration + enums + AbuseAlert + factory + 7 parâmetros + 3 permissões, com AbuseAlertModelTest e as 4 suítes de contagem (Parameter/DatabaseSeeder/RolesAndPermissions/ManageRoles) verdes.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-auditoria-e-compliance/12-CONTEXT.md
@docs/superpowers/specs/2026-06-15-auditoria-compliance-design.md

# PADRÕES A ESPELHAR (model com HasAuditoria + factory; catálogo de parâmetros; permissões aditivas) — ler antes de codar:
@app/Concerns/HasAuditoria.php
@app/Models/AccessLog.php
@database/seeders/ParameterSeeder.php
@database/seeders/RolesAndPermissionsSeeder.php
@tests/Feature/Seeders/ParameterSeederTest.php
@tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
@docs/SILE_HUs_Completas_MD/EP12-Auditoria-e-Compliance/HU-149-Detectar-padroes-de-abuso-e-fraude.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: abuse_alerts (migration + enums + model + factory)</name>
  <read_first>app/Concerns/HasAuditoria.php (logFillable/logOnlyDirty), app/Models/AccessLog.php (Fillable attribute + casts), app/Enums/* (estilo string-backed, ex.: AnalysisPendencyStatus)</read_first>
  <files>database/migrations/XXXX_XX_XX_XXXXXX_create_abuse_alerts_table.php, app/Enums/AbuseSeverity.php, app/Enums/AbuseAlertStatus.php, app/Models/AbuseAlert.php, database/factories/AbuseAlertFactory.php, tests/Feature/Auditoria/AbuseAlertModelTest.php</files>
  <action>
    TDD estrito (RED primeiro). Criar via Artisan (`make:enum`, `make:migration`, `make:model AbuseAlert -f`).

    Enums string-backed: `AbuseSeverity` (Baixa='baixa'/Media='media'/Alta='alta', com ordenação/peso para comparar com abuso.severidade_malha_fina) e `AbuseAlertStatus` (Aberto='aberto'/Confirmado='confirmado'/Descartado='descartado'). Valores em snake/lower honestos.

    Migration `abuse_alerts`:
    - `rule_key` (string, indexado), `severity` (string), `status` (string default 'aberto', indexado),
    - `fingerprint` (string) — assinatura determinística da ocorrência (idempotência),
    - `evidence` (json nullable) — as evidências do detector,
    - `viability_request_id` (FK nullable, índice), `nullableMorphs('subject')` (alvo opcional: empresa/usuário/contador),
    - `window_start`/`window_end` (timestamps nullable), `detected_at` (timestamp),
    - `resolved_by_user_id` (FK users nullable), `resolved_at` (timestamp nullable), `justification` (text nullable),
    - `fine_mesh_referral_id` (FK fine_mesh_referrals nullable — vínculo com a malha fina), timestamps.
    - **Idempotência estrutural:** índice ÚNICO PARCIAL `(rule_key, fingerprint)` restrito a `status = 'aberto'`. Crie de forma DRIVER-PORTÁVEL (Postgres e SQLite suportam índice parcial) — via `DB::statement("CREATE UNIQUE INDEX ... WHERE status = 'aberto'")` no up() e drop no down(), à sua discrição quanto ao nome. Isso garante "1 alerta ABERTO por (regra, fingerprint)"; confirmados/descartados não bloqueiam novo alerta futuro.

    `AbuseAlert` (model): `use HasAuditoria` (RN-002 — a própria criação/baixa de alerta é auditável), `#[Fillable([...])]` dos campos, `casts()` com os 2 enums + `evidence` array + window/detected/resolved datetimes; relações `viabilityRequest()`, `resolvedBy()` (User), `fineMeshReferral()` (FineMeshReferral) e `subject()` (morphTo). `AbuseAlertFactory` com defaults sensatos (status Aberto, severity Media, detected_at now) e states por status/severity.

    `AbuseAlertModelTest` (SQLite): factory cria e os casts batem (enums tipados, evidence array, datas Carbon); o índice único parcial barra um 2º alerta ABERTO com mesmo (rule_key, fingerprint) e PERMITE novo quando o anterior está confirmado/descartado.
  </action>
  <verify>php artisan migrate --no-interaction && vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=AbuseAlertModelTest</verify>
  <acceptance_criteria>
    - grep "abuse_alerts" e "fingerprint" na migration; índice único parcial WHERE status='aberto'; grep "HasAuditoria" em AbuseAlert.
    - AbuseAlertModelTest verde (casts + idempotência do alerta aberto).
  </acceptance_criteria>
  <done>Ledger abuse_alerts real, auditável e idempotente por desenho — pronto para os detectores (12-07).</done>
</task>

<task type="auto">
  <name>Task 2: 7 parâmetros HU-014 (ParameterSeeder) + contagem 78→85</name>
  <read_first>database/seeders/ParameterSeeder.php (formato do catálogo: group/type/default_value/validation_rules/description; convenção key≠group), tests/Feature/Seeders/ParameterSeederTest.php (assert 78 + lista ordenada de grupos), tests/Feature/Seeders/DatabaseSeederTest.php (assert 78)</read_first>
  <files>database/seeders/ParameterSeeder.php, tests/Feature/Seeders/ParameterSeederTest.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <action>
    TDD estrito (ajustar a asserção de contagem para 85 e RODAR — RED — antes de adicionar os parâmetros). Adicionar ao catálogo (cada um com type/default/validation_rules/description honestos):
    - `ui.auditoria.per_page` — group `ui`, integer, default '20', rules [required, integer, min:5, max:100].
    - `features.deteccao_abuso` — group `features`, boolean, default '0' (OFF — HU-149 nasce desligada), rules [required, boolean].
    - `abuso.janela_dias` — group `abuso`, integer, default '30', rules [required, integer, min:1, max:365].
    - `abuso.volume_cnpj.limite` — group `abuso`, integer, default '5', rules [required, integer, min:1, max:1000].
    - `abuso.volume_contador.limite` — group `abuso`, integer, default '20', rules [required, integer, min:1, max:1000].
    - `abuso.escritorio_virtual.limite` — group `abuso`, integer, default '3', rules [required, integer, min:1, max:1000].
    - `abuso.severidade_malha_fina` — group `abuso`, string, default 'alta', rules [required, in:baixa,media,alta].
    REUSA `retencao.access_logs.dias` (já existe — NÃO recriar). Constantes técnicas (auditoria.export.max_linhas, TTLs) ficam em config/sile.php, NÃO aqui (12-04/12-07 as adicionam ao config).

    Atualizar `ParameterSeederTest`: `test_seeder_cria_catalogo_completo` → contagem 85 e a LISTA ordenada de grupos passa a incluir `'abuso'` (novo grupo) — nova lista: ['abuso','analise','expresso','features','geo','integracoes','louos','notificacoes','retencao','risco','seguranca','solicitacao','ui']; `test_seeder_e_idempotente` → 85. Adicionar um teste curto para os parâmetros novos (ex.: features.deteccao_abuso default '0'/group features; abuso.severidade_malha_fina default 'alta'/group abuso). Atualizar `DatabaseSeederTest` (78→85 nos dois pontos).

    NOTA (decisão a validar): os 5 `abuso.*` vão para o grupo NOVO `abuso` (seguindo a convenção prefixo→grupo); per_page fica em `ui`; o toggle em `features`. Se a SEDUR preferir um grupo `auditoria` dedicado, ajuste o group e a lista de grupos do teste de forma consistente.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="ParameterSeederTest|DatabaseSeederTest"</verify>
  <acceptance_criteria>
    - grep dos 7 keys em ParameterSeeder; "features.deteccao_abuso" default '0'.
    - ParameterSeederTest verde com 85 e a lista de grupos com 'abuso'; DatabaseSeederTest 85.
  </acceptance_criteria>
  <done>Parâmetros HU-014 da fase administráveis (toggle de abuso OFF default), contagem-baseline atualizada e verde.</done>
</task>

<task type="auto">
  <name>Task 3: 3 permissões aditivas (RolesAndPermissionsSeeder) + contagem 24→27</name>
  <read_first>database/seeders/RolesAndPermissionsSeeder.php (array de permissões + givePermissionTo aditivo por papel), tests/Feature/Authorization/RolesAndPermissionsSeederTest.php (assert 24 + testes por grupo), tests/Feature/Roles/ManageRolesTest.php (has('permissions', 24)), tests/Feature/Seeders/DatabaseSeederTest.php (24)</read_first>
  <files>database/seeders/RolesAndPermissionsSeeder.php, tests/Feature/Authorization/RolesAndPermissionsSeederTest.php, tests/Feature/Roles/ManageRolesTest.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <action>
    TDD estrito (ajustar as asserções de contagem para 27 e RODAR — RED — antes de adicionar). Adicionar ao array `$permissions` as 3 novas e atribuir de forma ADITIVA (givePermissionTo, nunca sync):
    - `consultar-auditoria` → gestor + administrador (trilha/histórico/regras/export — HU-097..101).
    - `monitorar-lgpd` → administrador (painel LGPD — HU-102; DPO/admin).
    - `gerenciar-alertas-abuso` → gestor + administrador (painel de alertas — HU-149).
    Analista NÃO recebe nenhuma das 3 (decisão CONTEXT: default gestor/admin; sem papel auditor dedicado). Cidadão nenhuma.

    Atualizar testes (lição Fase 10 — TODAS as suítes de contagem juntas):
    - `RolesAndPermissionsSeederTest`: `test_seeder_e_idempotente` 24→27; adicionar `test_papeis_recebem_permissoes_de_auditoria_e_compliance` asserindo a existência das 3 e as atribuições (gestor/admin têm consultar-auditoria e gerenciar-alertas-abuso; só admin tem monitorar-lgpd; analista/cidadão não têm).
    - `ManageRolesTest`: `has('permissions', 24)` → 27.
    - `DatabaseSeederTest`: `assertSame(24, Permission...)` → 27 (e atualizar o comentário das contagens).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="RolesAndPermissionsSeederTest|ManageRolesTest|DatabaseSeederTest"</verify>
  <acceptance_criteria>
    - grep das 3 permissões em RolesAndPermissionsSeeder + atribuições por papel.
    - RolesAndPermissionsSeederTest/ManageRolesTest/DatabaseSeederTest verdes com 27 permissões; novo teste de atribuição verde.
  </acceptance_criteria>
  <done>Permissões da fase criadas e atribuídas (gestor/admin/admin) de forma aditiva; contagem-baseline 27 atualizada e verde nas 3 suítes.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-149 — ledger de alertas + idempotência (alerta aberto único) | AbuseAlertModelTest |
| HU-014 — parâmetros administráveis (toggle abuso OFF default) | ParameterSeederTest |
| HU-014 / RN-003 — permissões consultar-auditoria/monitorar-lgpd/gerenciar-alertas-abuso | RolesAndPermissionsSeederTest |
| Contagens-baseline (85 parâmetros, 27 permissões) | ParameterSeederTest, DatabaseSeederTest, ManageRolesTest |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan migrate --no-interaction` aplica abuse_alerts.
- `php artisan test --compact --filter="AbuseAlertModelTest|ParameterSeederTest|RolesAndPermissionsSeederTest|ManageRolesTest|DatabaseSeederTest"` verde (85 parâmetros, 27 permissões).
- Suíte completa permanece verde (schema/dados aditivos; nenhuma lógica de negócio alterada).
</verification>

<success_criteria>
- abuse_alerts real (model+enums+factory+idempotência estrutural) — pronto para os detectores.
- 7 parâmetros HU-014 (abuso OFF default) e 3 permissões aditivas criados; contagens 85/27 atualizadas no mesmo plano (dono único).
- ZERO dependência nova; givePermissionTo aditivo preserva ajustes da UI; ÚNICO dono de ParameterSeeder/RolesAndPermissionsSeeder e das suítes de contagem na Wave 1.
</success_criteria>

<output>
Criar `.planning/phases/12-auditoria-e-compliance/12-03-SUMMARY.md`: schema final de abuse_alerts (colunas + índice parcial), enums AbuseSeverity/AbuseAlertStatus, os 7 keys de parâmetros (com grupos) e as 3 permissões + atribuições, e as novas baselines (85/27). Insumo direto de 12-04 (per_page/permissão consultar-auditoria), 12-06 (monitorar-lgpd), 12-07 (parâmetros de detecção + abuse_alerts) e 12-08 (gerenciar-alertas-abuso + AbuseAlert).
</output>

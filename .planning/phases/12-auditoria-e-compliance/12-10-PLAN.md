---
phase: 12-auditoria-e-compliance
plan: 10
type: execute
wave: 4
depends_on: ["12-04", "12-05", "12-07"]
files_modified:
  - resources/js/pages/gestao/auditoria/index.tsx
  - resources/js/pages/gestao/lgpd/index.tsx
  - resources/js/components/auditoria/decision-explanation.tsx
  - resources/js/pages/gestao/resultados-expresso/show.tsx
  - resources/js/pages/gestao/processos/show.tsx
autonomous: true
requirements: [HU-098, HU-099, HU-100, HU-101, HU-102]

must_haves:
  truths:
    - "A página da trilha (gestao/auditoria/index) consome as props server-driven da 12-04: seletor de fonte (atividade/alteracoes/acessos), filtros (período/usuário/entidade/ação/resultado), tabela paginada e botão de export CSV que preserva os filtros — sem recalcular nada no cliente"
    - "A explicabilidade (HU-099) é renderizada no detalhe da decisão (resultados-expresso/show e processos/show) por um componente que mostra o passo a passo por CNAE na ordem (entrada→risco→louos→consolidação→desfecho) e marca visualmente os passos 'não registrado' do legado — sem inventar dado"
    - "O painel LGPD (gestao/lgpd/index) mostra as 3 seções reais da 12-07: consentimentos (% aceite/pendentes), retenção (dias + último pruning, decisões fora) e acessos a dado pessoal (métrica), minimizado (sem PII crua)"
    - "É um par paralelo de 12-09 (backend do abuso) sem sobreposição: SÓ toca frontend (.tsx); NÃO edita routes/gestao.php nem controllers; a página do abuso e a navegação/Cmd+K ficam para a 12-11"
    - "Compila limpo (npm run build) e as feature tests dos controllers (12-04/05/07) seguem verdes — o render fino é validado no smoke da 12-12"
  artifacts:
    - path: "resources/js/pages/gestao/auditoria/index.tsx"
      provides: "Trilha unificada: fonte+filtros+tabela paginada+export CSV (HU-098/100/101)"
      contains: "auditoria"
    - path: "resources/js/components/auditoria/decision-explanation.tsx"
      provides: "Passo a passo da decisão (HU-099) com marca de legado"
      contains: "passos"
    - path: "resources/js/pages/gestao/lgpd/index.tsx"
      provides: "Painel LGPD (consentimentos/retenção/acessos a PII)"
      contains: "lgpd"
  key_links:
    - from: "resources/js/pages/gestao/resultados-expresso/show.tsx"
      to: "resources/js/components/auditoria/decision-explanation.tsx"
      via: "renderiza a prop 'explicacao' (DecisionExplanationResource) quando presente"
      pattern: "explicacao|DecisionExplanation"
    - from: "resources/js/pages/gestao/auditoria/index.tsx"
      to: "rota gestao.auditoria.export"
      via: "Link/href com os filtros atuais + ?formato=csv (export server-driven)"
      pattern: "auditoria.export|formato=csv"
---

<objective>
Frontend da consulta da trilha, da explicabilidade e do painel LGPD (Wave 4; par paralelo de 12-09, SÓ frontend): as páginas `gestao/auditoria/index`, `gestao/lgpd/index`, o componente de explicabilidade passo a passo embutido em `resultados-expresso/show` e `processos/show`, consumindo as props server-driven já entregues (12-04/12-05/12-07).

Purpose: o backend das três superfícies de consulta já existe e está testado; falta a UI navegável. Tudo é server-driven (filtros/paginação/export vêm do servidor — o cliente não recalcula). A explicabilidade renderiza o passo a passo da decisão marcando honestamente o legado ("não registrado"). O painel LGPD mostra métricas minimizadas. A página do abuso e a navegação/Cmd+K ficam para a 12-11 (que depende desta). Ativar as skills `inertia-react-development` e `tailwindcss-development`.
Output: 2 páginas novas + 1 componente + 2 detalhes ajustados, compilando limpo, com as feature tests dos controllers verdes e o render validado no smoke (12-12).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-auditoria-e-compliance/12-CONTEXT.md
@.planning/phases/12-auditoria-e-compliance/12-04-SUMMARY.md
@.planning/phases/12-auditoria-e-compliance/12-05-SUMMARY.md
@.planning/phases/12-auditoria-e-compliance/12-07-SUMMARY.md

# PADRÕES A ESPELHAR (página server-driven com filtros/paginação + detalhe) — ler antes de codar:
@resources/js/pages/gestao/resultados-expresso/index.tsx
@resources/js/pages/gestao/resultados-expresso/show.tsx
@resources/js/pages/gestao/processos/show.tsx
@resources/js/pages/gestao/emails/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Páginas gestao/auditoria/index (trilha + CSV) e gestao/lgpd/index (painel LGPD)</name>
  <read_first>resources/js/pages/gestao/resultados-expresso/index.tsx (filtros server-driven + paginação + per_page; Link router), resources/js/pages/gestao/emails/index.tsx, 12-04-SUMMARY (props da trilha) e 12-07-SUMMARY (props do LGPD)</read_first>
  <files>resources/js/pages/gestao/auditoria/index.tsx, resources/js/pages/gestao/lgpd/index.tsx</files>
  <action>
    Skills `inertia-react-development` + `tailwindcss-development`. Páginas server-driven (espelham resultados-expresso/index — Inertia router com preserveState/replace nos filtros; nada de recompute no cliente).
    - `gestao/auditoria/index.tsx`: seletor de FONTE (atividade/alteracoes/acessos), filtros (período, usuário, entidade, ação log_name+event, resultado) que disparam visita Inertia, tabela paginada dos registros (colunas do ActivityResource: data, fonte/ação, usuário, entidade, resultado, versão de regra; mostrar attribute_changes quando fonte=alteracoes), paginação server-side, e botão "Exportar CSV" como link para `route('gestao.auditoria.export')` com os MESMOS filtros (?formato=csv). Vazio com estado honesto.
    - `gestao/lgpd/index.tsx`: 3 cartões/seções — consentimentos (% aceite da versão vigente, pendentes), retenção (dias + data do último pruning; nota "decisões fora do pruning"), acessos a dado pessoal (métrica/série) — minimizado (sem PII crua). Seção informativa da pendência DPO (eliminação/anonimização) honesta.

    NÃO adicionar entradas de navegação aqui (a 12-11 é dona do app-sidebar/Cmd+K). As páginas são alcançáveis pela rota; a nav vem depois.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Existem resources/js/pages/gestao/auditoria/index.tsx e gestao/lgpd/index.tsx; grep "formato=csv"/"auditoria.export" na página da trilha.
    - `npm run build` compila sem erro de tipos/manifest.
  </acceptance_criteria>
  <done>Trilha (consulta+CSV) e painel LGPD navegáveis (server-driven), prontos para o smoke.</done>
</task>

<task type="auto">
  <name>Task 2: Componente de explicabilidade + integração nos detalhes (resultados-expresso/show, processos/show)</name>
  <read_first>resources/js/pages/gestao/resultados-expresso/show.tsx e processos/show.tsx (onde a decisão é exibida), 12-05-SUMMARY (shape da prop 'explicacao': passos por CNAE + flag registrado/legado)</read_first>
  <files>resources/js/components/auditoria/decision-explanation.tsx, resources/js/pages/gestao/resultados-expresso/show.tsx, resources/js/pages/gestao/processos/show.tsx</files>
  <action>
    Skills `inertia-react-development` + `tailwindcss-development`. Criar `decision-explanation.tsx` (componente): recebe a prop `explicacao` e renderiza, por CNAE, o passo a passo na ORDEM (entrada → risco → louos quadro7→10→11→11a → consolidação → desfecho), cada passo com entrada/resultado_parcial/motivo/versão; quando `legado`/passo `nao_registrado`, exibir um badge honesto "não registrado nesta decisão" (sem inventar). Mostrar fundamentação e versões de regra.

    Integrar o componente nos dois detalhes, renderizando SÓ quando a prop `explicacao` está presente (decisão existe):
    - `resultados-expresso/show.tsx`: abaixo da decisão imutável.
    - `processos/show.tsx`: numa aba/seção do detalhe (quando há decisão).
    NÃO alterar a lógica/props existentes além de consumir `explicacao` (aditivo no frontend).
  </action>
  <verify>npm run build && php artisan test --compact --filter="ResultadoExpresso|Processo|DecisionExplanation"</verify>
  <acceptance_criteria>
    - Existe resources/js/components/auditoria/decision-explanation.tsx; grep "explicacao"/"DecisionExplanation" nos dois show.tsx.
    - `npm run build` compila; as feature tests de ResultadoExpresso/Processo/DecisionExplanation seguem verdes.
  </acceptance_criteria>
  <done>Explicabilidade passo a passo visível no detalhe da decisão, com legado honesto — HU-099 navegável.</done>
</task>

</tasks>

## Mapa CA → teste / evidência
| HU / RN | Evidência |
|---|---|
| HU-098/100/101 — trilha+fonte+filtros+CSV navegável | npm run build + smoke (12-12) + AuditoriaConsultaTest/ExportTest (12-04) |
| HU-099 — passo a passo no detalhe, legado marcado | npm run build + DecisionExplanationTest (12-05) + smoke |
| HU-102 — painel LGPD (3 seções) navegável | npm run build + LgpdMonitorTest (12-07) + smoke |

<verification>
- `npm run build` compila sem erro (tipos/manifest).
- `php artisan test --compact --filter="ResultadoExpresso|Processo|DecisionExplanation"` verde (mudança só de frontend; props já testadas no backend).
- Par paralelo limpo: NÃO toca routes/gestao.php nem controllers (sem conflito com 12-09).
</verification>

<success_criteria>
- Trilha (consulta+CSV), explicabilidade no detalhe e painel LGPD navegáveis, server-driven e honestos (legado marcado, LGPD minimizado).
- Compila limpo; feature tests verdes; sem navegação/Cmd+K aqui (dono é a 12-11).
- ZERO dependência nova; SÓ frontend.
</success_criteria>

<output>
Criar `.planning/phases/12-auditoria-e-compliance/12-10-SUMMARY.md`: as páginas/componentes criados, como cada um consome as props (trilha/explicabilidade/LGPD) e onde o componente de explicabilidade foi embutido. Insumo direto de 12-11 (navegação aponta para estas páginas) e 12-12 (smoke navegável).
</output>

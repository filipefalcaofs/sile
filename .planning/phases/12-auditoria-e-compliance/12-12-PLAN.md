---
phase: 12-auditoria-e-compliance
plan: 12
type: execute
wave: 6
depends_on: ["12-01", "12-02", "12-03", "12-04", "12-05", "12-06", "12-07", "12-08", "12-09", "12-10", "12-11"]
files_modified:
  - database/seeders/AuditoriaDevSeeder.php
  - database/seeders/DatabaseSeeder.php
  - tests/Feature/Seeders/AuditoriaDevSeederTest.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
autonomous: false
requirements: [HU-097, HU-098, HU-099, HU-100, HU-101, HU-102, HU-149]

must_haves:
  truths:
    - "O ambiente de desenvolvimento tem dados de auditoria/abuso/LGPD REAIS (dados fictícios, LÓGICA real): o AuditoriaDevSeeder semeia um padrão de abuso DEDICADO e roda a detecção REAL (habilita o toggle só durante o seed, gera abuse_alerts + 1 encaminhamento à malha fina pelo caminho de sistema) e produz acessos com personal_data=true pelo caminho REAL — tornando trilha, explicabilidade, LGPD e abuso navegáveis"
    - "A suíte INTEGRAL passa fresca via `composer test` (2 processos: SQLite --exclude-group postgis + --group postgis), com a baseline crescida pelos testes da fase (era 1205 SQLite + 29 postgis); pint limpo; 85 parâmetros / 27 permissões"
    - "Anti-fachada confirmada por evidência fresca: abuso OFF default → abuso:detectar no-op e NENHUM alerta altera o status do PROCESSO (RN-001); explicabilidade é projeção pura (spy do motor = 0, RN-005) e legado degrada honesto; personal_data só nos call sites reais; decision_trace é aditivo (suítes 9/10 verdes); export pleno XLSX/PDF segue bloqueado honesto (HU-131/Fase 15)"
    - "Smoke navegável de ponta a ponta validado por humano: trilha (fonte/filtros/CSV), explicabilidade passo a passo no detalhe, painel LGPD, painel de abuso (efetividade + confirmar/descartar sem punir o processo), navegação + Cmd+K gated por permissão"
    - "Veredito do guardião-entrega REGISTRADO (anti-fachada + auditoria RN-002 + parametrização HU-014 + TDD com evidência fresca); REPROVADO bloqueia a conclusão da fase; pendências SEDUR/DPO (limiares de fraude, retenção/eliminação LGPD, papel auditor, EscritorioVirtualEncadeado 2ª onda) REGISTRADAS, nunca simuladas"
  artifacts:
    - path: "database/seeders/AuditoriaDevSeeder.php"
      provides: "Dados dev via fluxo REAL (detecção de abuso + acessos personal_data + decisão com trace), idempotente por marcador estável"
      contains: "AbuseDetectionService"
    - path: "tests/Feature/Seeders/AuditoriaDevSeederTest.php"
      provides: "Prova de presença (alertas reais + acessos personal_data), não fabricação; idempotência"
      contains: "abuse_alerts"
  key_links:
    - from: "database/seeders/AuditoriaDevSeeder.php"
      to: "app/Services/Abuso/AbuseDetectionService.php"
      via: "habilita o toggle só no seed e roda a detecção REAL sobre o padrão dedicado (gera alertas/malha fina de verdade)"
      pattern: "AbuseDetectionService|deteccao_abuso"
    - from: "database/seeders/DatabaseSeeder.php"
      to: "database/seeders/AuditoriaDevSeeder.php"
      via: "encadeado no seed de desenvolvimento (após os exemplos das fases anteriores)"
      pattern: "AuditoriaDevSeeder"
---

<objective>
Fechamento da Fase 12 (Wave 6, depende de tudo; autonomous=false por conter checkpoint humano): seed de desenvolvimento que produz dados de auditoria/abuso/LGPD REAIS pelo fluxo de verdade, verificação INTEGRAL fresca (`composer test` 2 processos + pint + evidência anti-fachada) e smoke navegável de ponta a ponta com checkpoint humano, fechado pelo veredito do guardião-entrega.

Purpose: a regra entrega-funcional exige a feature operando ponta a ponta com evidência fresca. O seed usa dados fictícios mas LÓGICA REAL (semeia um padrão de abuso e roda a detecção real — gerando alertas e malha fina de verdade; gera acessos a dado pessoal pelo caminho real; decide um processo para tornar a explicabilidade navegável), deixando trilha/explicabilidade/LGPD/abuso navegáveis no dev. A verificação roda a suíte inteira (incl. @group postgis) e confirma os invariantes anti-fachada da fase (abuso nunca pune; explicabilidade não recomputa; personal_data só onde é real; decision_trace aditivo). O guardião dá o parecer final; REPROVADO trava a fase.
Output: AuditoriaDevSeeder + encadeamento + teste de presença; relatório de verificação integral; checkpoint humano do smoke; veredito do guardião.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-auditoria-e-compliance/12-CONTEXT.md
@docs/superpowers/specs/2026-06-15-auditoria-compliance-design.md
@.planning/phases/12-auditoria-e-compliance/12-06-SUMMARY.md
@.planning/phases/12-auditoria-e-compliance/12-08-SUMMARY.md
@.planning/phases/12-auditoria-e-compliance/12-09-SUMMARY.md

# PADRÕES A ESPELHAR (dev seeder idempotente real + verificação + guardião) — ler antes de codar:
@database/seeders/DatabaseSeeder.php
@tests/Feature/Seeders/DatabaseSeederTest.php
@.cursor/agents/guardiao-entrega.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuditoriaDevSeeder (dados dev via fluxo REAL) + encadeamento + teste de presença</name>
  <read_first>database/seeders/DatabaseSeeder.php (ordem dos seeders dev; marcadores estáveis dos exemplos), app/Services/Abuso/AbuseDetectionService.php, app/Support/Settings.php (habilitar o toggle só no seed), tests/Feature/Seeders/DatabaseSeederTest.php (contagens do cidadao — usar marcador DEDICADO p/ não perturbar)</read_first>
  <files>database/seeders/AuditoriaDevSeeder.php, database/seeders/DatabaseSeeder.php, tests/Feature/Seeders/AuditoriaDevSeederTest.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <action>
    TDD estrito. `AuditoriaDevSeeder` (dados fictícios, LÓGICA REAL; idempotente por marcador estável próprio — NÃO mutar os exemplos já asseridos):
    - Semeia um PADRÃO de abuso DEDICADO (ex.: N+1 solicitações do mesmo CNPJ/contador num requerente/empresa de exemplo próprios), habilita `features.deteccao_abuso` SÓ durante o seed (Settings), roda `AbuseDetectionService::detectar()` REAL (gera abuse_alerts e, acima do limiar, 1 encaminhamento à malha fina pelo caminho de sistema) e RESTAURA o toggle para OFF ao final (a feature nasce desligada — degradação honesta).
    - Produz alguns acessos com `personal_data=true` pelo caminho REAL (ex.: registrar via AuditService no fluxo de leitura sensível ou simular a consulta real) para o painel LGPD ter dado.
    - (Opcional, p/ explicabilidade navegável no dev) leva um processo dedicado à decisão pelo fluxo REAL da análise técnica (ficha finalizada → decide) — a ViabilityDecision nasce com decision_trace.
    Idempotente: re-seed NÃO duplica (marcador estável). Encadear no `DatabaseSeeder` (perfil dev), após os exemplos das fases anteriores.

    CUIDADO ANTI-REGRESSÃO: o `DatabaseSeederTest` assere contagens dos exemplos do cidadao (ex.: 7 ViabilityRequest do cidadao). Use requerente/empresa DEDICADOS (não o cidadao@sile.dev) para o padrão de abuso, de modo a NÃO alterar essas contagens; se algum entity contado mudar, ATUALIZE o DatabaseSeederTest de forma consistente (este plano é o dono dele na Wave 6). Prefira asserções de PRESENÇA.
    ATENÇÃO ESPECÍFICA (warning do plan-checker): se o seed LEVAR um processo à decisão pelo fluxo real (para a explicabilidade navegável), o `tests/Feature/Seeders/DatabaseSeederTest.php` linha ~242 `assertSame(0, ViabilityDecision::query()->count())` (e o comentário das linhas ~234-240 "sem ViabilityDecision") DEIXAM de valer — ATUALIZE essa asserção de 0 para o N correto (e o comentário). A decisão dev DEVE usar o caminho análise-técnica-a-partir-da-ficha (SQLite-safe, sem PostGIS — não o expresso que exige zona); e, se quiser exercitar o sub-passo "não registrado" da HU-099, semeie uma decisão LEGADA via factory SEM decision_trace. Confirme a contagem real rodando o teste.

    `AuditoriaDevSeederTest` (SQLite): após `$this->seed()`, existem abuse_alerts REAIS (status aberto, ≥1) ligados ao padrão dedicado, ≥1 com fine_mesh_referral_id (acima do limiar), e ≥1 activity com personal_data=true; o toggle `features.deteccao_abuso` permanece OFF após o seed (degradação honesta restaurada). Idempotência: re-seed não duplica alertas/processos dedicados.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="AuditoriaDevSeederTest|DatabaseSeederTest"</verify>
  <acceptance_criteria>
    - grep "AbuseDetectionService"/"deteccao_abuso" em AuditoriaDevSeeder; "AuditoriaDevSeeder" em DatabaseSeeder.
    - AuditoriaDevSeederTest verde (alertas reais + malha fina + personal_data; toggle OFF restaurado; idempotente); DatabaseSeederTest verde (85 parâmetros, 27 permissões).
  </acceptance_criteria>
  <done>Dev navegável com alertas/acessos/decisão REAIS produzidos pelo fluxo de verdade (toggle restaurado OFF), idempotente.</done>
</task>

<task type="auto">
  <name>Task 2: Verificação INTEGRAL fresca (composer test 2 processos + pint + evidência anti-fachada) + guardião-entrega</name>
  <read_first>.cursor/agents/guardiao-entrega.md, composer.json (script test: --exclude-group postgis e depois --group postgis)</read_first>
  <files>(sem código — verificação/relatório no SUMMARY)</files>
  <action>
    Verificação com EVIDÊNCIA FRESCA (regra superpowers):
    1. `vendor/bin/pint --dirty --format agent` — sem pendências.
    2. `composer test` — roda os DOIS processos (SQLite `--exclude-group postgis` e `--group postgis`). Confirmar TUDO verde e a baseline crescida pelos testes da fase (referência anterior: 1205 SQLite + 29 postgis). Ler o output inteiro.
    3. Evidência anti-fachada (grep/inspeção + testes que já provam):
       - Abuso: `features.deteccao_abuso` OFF por default; `abuso:detectar` no-op honesto; NENHUM alerta transiciona o status do PROCESSO (RN-001 — AbusoPainelTest/AbuseDetectionServiceTest); idempotência do alerta aberto.
       - Explicabilidade: projeção pura — spy do motor = 0 chamadas (RN-005); decisão legada marca "não registrado" (DecisionExplanationTest).
       - LGPD: personal_data marcado SÓ nos call sites reais (PersonalDataMarkingTest); decisões fora do pruning.
       - decision_trace ADITIVO: suítes das Fases 9/10 (incl. @group postgis) verdes; AuditService aditivo (consumidores verdes).
       - Bloqueios honestos REGISTRADOS: export pleno XLSX/PDF → HU-131/Fase 15; EscritorioVirtualEncadeado → 2ª onda; limiares de fraude/retenção/eliminação LGPD/papel auditor → SEDUR/DPO.
    4. Acionar o agent `guardiao-entrega` com a evidência fresca para o veredito final (anti-fachada + auditoria RN-002 + parametrização HU-014 + TDD). Registrar o veredito; REPROVADO bloqueia a conclusão (corrigir e repetir).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && composer test</verify>
  <acceptance_criteria>
    - `composer test` verde nos 2 processos (SQLite + postgis), baseline crescida pelos testes da fase; 85 parâmetros / 27 permissões.
    - Evidência anti-fachada registrada (abuso OFF/no-op/nunca pune; explicabilidade spy=0; personal_data real; decision_trace aditivo 9/10 verdes).
    - Veredito do guardião-entrega APROVADO registrado no SUMMARY.
  </acceptance_criteria>
  <done>Suíte integral fresca verde + invariantes anti-fachada confirmados + parecer do guardião APROVADO.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Auditoria e Compliance do EP12 ponta a ponta: trilha unificada consultável/exportável (HU-098/100/101), explicabilidade passo a passo das decisões (HU-099), painel LGPD (HU-102) e detecção de abuso com alerta + malha fina, NUNCA punição (HU-149) — com navegação e Cmd+K gated por permissão.
  </what-built>
  <how-to-verify>
    Com o ambiente dev rodando (`composer run dev`) e o seed aplicado (`php artisan migrate:fresh --seed`):
    1. Logar como gestor@sile.dev (gestão): abrir a Trilha de auditoria — filtrar por período/usuário/ação, alternar a FONTE (atividade → alterações → acessos), e Exportar CSV (conferir que o arquivo respeita os filtros). Conferir que a própria consulta aparece na trilha (meta-auditoria).
    2. Abrir um resultado expresso/processo DECIDIDO e ver a EXPLICABILIDADE passo a passo por CNAE (entrada→risco→louos→consolidação→desfecho, com versões); numa decisão legada (sem trace), conferir a marca "não registrado nesta decisão".
    3. Logar como admin@sile.dev: abrir o painel LGPD — consentimentos (% aceite/pendentes), retenção (dias + último pruning; decisões fora) e acessos a dado pessoal (métrica).
    4. Abrir o painel de Alertas de abuso: ver os alertas do seed, o indicador de efetividade, e confirmar/descartar um alerta com justificativa — CONFIRMAR que o status do PROCESSO vinculado NÃO muda (revisão, não punição). Conferir o estado "detecção desligada" (toggle off default).
    5. Conferir a navegação (sidebar) e o Cmd+K para as 3 superfícies; logar com um perfil SEM as permissões e confirmar que os itens NÃO aparecem (403 ao forçar a URL, auditado).
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados (cada problema vira gap a corrigir antes de concluir a fase).</resume-signal>
</task>

</tasks>

## Mapa CA → teste / evidência
| HU / RN | Teste / Evidência |
|---|---|
| HU-098/099/100/101/102/149 — ponta a ponta navegável | Checkpoint humano (smoke) + AuditoriaDevSeederTest |
| Entrega-funcional — lógica real ponta a ponta (sem fachada) | composer test (2 processos) + evidência anti-fachada + guardião |
| HU-149 — alerta nunca pune; toggle OFF; explicabilidade spy=0 | Evidência anti-fachada (Task 2) + checkpoint (passos 2/4) |
| Suíte integral fresca verde (SQLite + postgis); 85/27 | `composer test` (Task 2) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `composer test` verde nos DOIS processos (SQLite + @group postgis), baseline crescida; 85 parâmetros / 27 permissões.
- AuditoriaDevSeederTest + DatabaseSeederTest verdes (dev real, idempotente).
- Checkpoint humano APROVADO + veredito do guardião-entrega APROVADO.
</verification>

<success_criteria>
- Dev navegável com auditoria/abuso/LGPD REAIS (fluxo de verdade), idempotente, toggle de abuso restaurado OFF.
- Suíte INTEGRAL fresca verde (2 processos); invariantes anti-fachada confirmados (abuso nunca pune; explicabilidade não recomputa; personal_data real; decision_trace aditivo 9/10 verdes).
- Smoke ponta a ponta aprovado por humano; guardião-entrega APROVADO. Bloqueios SEDUR/DPO (limiares de fraude, retenção/eliminação LGPD, papel auditor, EscritorioVirtualEncadeado 2ª onda, export pleno HU-131) registrados no STATE/ROADMAP — nunca simulados.
</success_criteria>

<output>
Criar `.planning/phases/12-auditoria-e-compliance/12-12-SUMMARY.md`: o resultado do `composer test` (contagens finais SQLite/postgis), o veredito do guardião-entrega, o que o seed dev produz e os bloqueios SEDUR/DPO registrados. Fecha a Fase 12.
</output>

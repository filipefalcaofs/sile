---
phase: 12-auditoria-e-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/XXXX_XX_XX_XXXXXX_add_decision_trace_to_viability_decisions.php
  - app/Models/ViabilityDecision.php
  - app/Services/Expresso/FluxoExpressoService.php
  - app/Services/Analise/AnaliseTecnicaDecisionService.php
  - tests/Feature/Auditoria/DecisionTraceEnrichmentTest.php
autonomous: true
requirements: [HU-099]

must_haves:
  truths:
    - "viability_decisions ganha a coluna ADITIVA decision_trace (JSON nullable, IMUTÁVEL — gravada uma vez na criação da decisão): o snapshot passo a passo que a explicabilidade (HU-099) projeta, sem nunca recomputar (RN-005)"
    - "O fluxo expresso (Fase 9) passa a gravar decision_trace na MESMA transação de FluxoExpressoService::emitir, a partir do consulta_array JÁ em memória ($resolved->por_cnae[].consulta_array) — entrada→risco→louos(quadro7→10→11→11a, cada {entrada, resultado_parcial, motivo, versao_regra})→consolidação→desfecho por CNAE; é ADITIVO, NÃO muda o veredito nem qualquer outro campo"
    - "A decisão técnica humana (Fase 10) passa a gravar decision_trace em AnaliseTecnicaDecisionService a partir da FICHA finalizada (per_cnae + fundamentação + engine_rules_versions do analista) — sem recomputar o motor; ADITIVO"
    - "ANTI-REGRESSÃO 9/10: as suítes do fluxo expresso (incl. @group postgis) e da análise técnica seguem 100% VERDES — o enriquecimento só AMPLIA o snapshot persistido, sem alterar outcome/per_cnae/rules_versions/fundamentacao/transição/auditoria existentes"
    - "Decisão LEGADA (sem trace) é honesta: decision_trace fica null e a projeção (12-05) marca os passos como 'não registrado nesta decisão' — nunca inventa nem reexecuta"
  artifacts:
    - path: "database/migrations/XXXX_XX_XX_XXXXXX_add_decision_trace_to_viability_decisions.php"
      provides: "Coluna decision_trace JSON nullable em viability_decisions (aditiva, imutável por convenção)"
      contains: "decision_trace"
    - path: "app/Services/Expresso/FluxoExpressoService.php"
      provides: "decision_trace por CNAE gravado no create da ViabilityDecision (a partir do consulta_array)"
      contains: "decision_trace"
    - path: "app/Services/Analise/AnaliseTecnicaDecisionService.php"
      provides: "decision_trace gravado a partir da ficha finalizada"
      contains: "decision_trace"
  key_links:
    - from: "app/Services/Expresso/FluxoExpressoService.php"
      to: "viability_decisions.decision_trace"
      via: "ViabilityDecision::create recebe decision_trace montado de $resolved->por_cnae[].consulta_array (sem recomputo)"
      pattern: "decision_trace"
    - from: "app/Models/ViabilityDecision.php"
      to: "decision_trace (cast array + Fillable)"
      via: "cast 'array' e atributo no #[Fillable]"
      pattern: "decision_trace"
---

<objective>
Fundação da explicabilidade passo a passo (HU-099) por FORWARD SNAPSHOT (Wave 1, par paralelo com 12-01/12-03): migration ADITIVA `decision_trace` (JSON nullable, imutável) em `viability_decisions` e ENRIQUECIMENTO dos DOIS pontos de escrita da decisão — `FluxoExpressoService::emitir` (Fase 9) e `AnaliseTecnicaDecisionService` (Fase 10) — para persistir o trace completo que hoje é DESCARTADO.

Purpose: RN-004 exige visualização passo a passo; RN-005 proíbe recomputar. Hoje `ViabilityDecision.per_cnae` guarda só a forma compacta; o trace completo (Quadro 7/10/11/11A + risco, cada um com versão e motivo) vive no `consulta_array` (ConsultaViabilidadeResult::toArray) mas é jogado fora. A solução é gravar esse trace JÁ em memória no momento da decisão — ADITIVO, sem mudar a lógica de decisão (ANTI-REGRESSÃO das suítes 9/10). A projeção pura que LÊ esse trace é a 12-05; este plano só garante que a fonte passa a existir, e que decisões legadas (sem trace) degradam honestas.
Output: 1 migration aditiva + cast/Fillable na `ViabilityDecision` + enriquecimento nos 2 serviços, com teste do shape e as suítes 9/10 verdes.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-auditoria-e-compliance/12-CONTEXT.md
@docs/superpowers/specs/2026-06-15-auditoria-compliance-design.md

# FONTE DO TRACE + PONTOS DE ESCRITA (ler antes de codar — o trace é o consulta_array já em memória):
@app/Models/ViabilityDecision.php
@app/Services/Expresso/FluxoExpressoService.php
@app/Services/Analise/AnaliseTecnicaDecisionService.php
@app/Services/Solicitacao/ResolvedViability.php
@docs/SILE_HUs_Completas_MD/EP12-Auditoria-e-Compliance/HU-099-Consultar-regras-aplicadas.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration decision_trace + cast/Fillable na ViabilityDecision</name>
  <read_first>app/Models/ViabilityDecision.php (#[Fillable] + casts() — per_cnae/rules_versions/fundamentacao já são 'array'), a migration original de viability_decisions (convenção)</read_first>
  <files>database/migrations/XXXX_XX_XX_XXXXXX_add_decision_trace_to_viability_decisions.php, app/Models/ViabilityDecision.php</files>
  <action>
    TDD: a prova vem no Task 2 (enriquecimento grava e o cast devolve array). Gerar `php artisan make:migration add_decision_trace_to_viability_decisions --table=viability_decisions --no-interaction`.

    Migration (ADITIVA): `decision_trace` JSON nullable (default null). NÃO alterar colunas existentes. `down()` dropa só a coluna. A imutabilidade é por convenção (gravada uma vez no create, como per_cnae) — não há trigger.

    `ViabilityDecision`: adicionar `'decision_trace'` ao `#[Fillable([...])]` e `'decision_trace' => 'array'` ao `casts()` (espelha per_cnae/rules_versions/fundamentacao).
  </action>
  <verify>php artisan migrate --no-interaction && vendor/bin/pint --dirty --format agent</verify>
  <acceptance_criteria>
    - `php artisan migrate` aplica sem erro; grep "decision_trace" na migration e em ViabilityDecision (Fillable + cast 'array').
  </acceptance_criteria>
  <done>viability_decisions guarda decision_trace (JSON nullable, array no model) — a fonte da explicabilidade passa a existir, aditiva.</done>
</task>

<task type="auto">
  <name>Task 2: Enriquecimento decision_trace no FluxoExpressoService::emitir (ANTI-REGRESSÃO Fase 9)</name>
  <read_first>app/Services/Expresso/FluxoExpressoService.php (emitir: ViabilityDecision::create; perCnae() já lê $item['consulta']->fundamentacao()), app/Services/Solicitacao/ResolvedViability.php (por_cnae traz 'consulta' objeto e 'consulta_array' = toArray completo)</read_first>
  <files>app/Services/Expresso/FluxoExpressoService.php, tests/Feature/Auditoria/DecisionTraceEnrichmentTest.php</files>
  <action>
    TDD estrito (RED primeiro). No `emitir`, adicionar `'decision_trace' => $this->decisionTrace($resolved)` ao array do `ViabilityDecision::create` — SÓ isso muda no fluxo (nada de outcome/per_cnae/rules_versions/fundamentacao/transição/auditoria). Criar o método privado `decisionTrace(ResolvedViability $resolved): array` que monta, por CNAE e na ORDEM, a partir do `consulta_array` (item['consulta']->toArray() ou item['consulta_array'] já disponível): entrada (área/ponto/CNAE) → risco (municipal/sanitário/encaminhamento + versão) → louos (quadro7 → quadro10 → quadro11 → quadro11a, cada um {entrada, resultado_parcial, motivo, versao_regra}) → consolidação (veredito) → desfecho. O shape EXATO dos campos fica à sua discrição, mas deve refletir o que o motor JÁ produziu (nunca inventar) e ser estável/serializável.

    `DecisionTraceEnrichmentTest` (SQLite, sem PostGIS): para o caminho expresso, injete um `SolicitacaoViabilityResolver` FAKE (via `$this->app->instance(...)` ou bind) que devolve um `ResolvedViability` com `por_cnae` contendo um `consulta_array` conhecido e `elegivelExpresso()=true`/consolidado permitido — assim `decide()→emitir()` roda sem reexecutar motores territoriais (PostGIS). Asserir: a `ViabilityDecision` criada tem `decision_trace` não-nulo, com um item por CNAE na ordem e os passos louos (quadro7→10→11→11a) com versao_regra/motivo refletindo o consulta_array. NÃO asserir recomputo (o resolver é chamado 1× pela decisão — comportamento atual).

    ANTI-REGRESSÃO Fase 9: rode a suíte do expresso (SQLite + @group postgis) — outcome, per_cnae, rules_versions, fundamentacao, transição e auditoria seguem idênticos; só decision_trace é novo.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="DecisionTraceEnrichmentTest|FluxoExpresso|Expresso"</verify>
  <acceptance_criteria>
    - grep "decision_trace" em app/Services/Expresso/FluxoExpressoService.php (no create + método privado).
    - DecisionTraceEnrichmentTest verde (shape por CNAE na ordem, passos louos com versão/motivo).
    - Suíte do fluxo expresso VERDE (SQLite e @group postgis) — sem regressão.
  </acceptance_criteria>
  <done>Toda decisão expressa NOVA grava o trace passo a passo (RN-005), sem mudar a decisão — fonte real da explicabilidade.</done>
</task>

<task type="auto">
  <name>Task 3: Enriquecimento decision_trace na AnaliseTecnicaDecisionService (ANTI-REGRESSÃO Fase 10)</name>
  <read_first>app/Services/Analise/AnaliseTecnicaDecisionService.php (registrarDecisao: ViabilityDecision::create; perCnaeDaFicha()/fundamentacao() já existem; engine_rules_versions da ficha)</read_first>
  <files>app/Services/Analise/AnaliseTecnicaDecisionService.php, tests/Feature/Auditoria/DecisionTraceEnrichmentTest.php</files>
  <action>
    TDD estrito. No `registrarDecisao`, adicionar `'decision_trace' => $this->decisionTrace($record, $perCnae)` ao `ViabilityDecision::create` — SÓ isso muda (outcome/per_cnae/rules_versions/fundamentacao/transição/auditoria intactos). O trace da decisão HUMANA nasce da FICHA (não recomputa motor): por CNAE, a escolha do analista (status_sugerido × status_escolhido), a fundamentação registrada na ficha e as engine_rules_versions; quando a ficha tem o snapshot do motor por CNAE, reaproveitar a mesma ordenação de passos do Task 2 (entrada→risco→louos→consolidação→desfecho); quando não tem (caso pendente decidido pelo humano), o trace registra os passos disponíveis e marca os ausentes como "não registrado" — honesto, nunca inventado.

    Adicionar ao `DecisionTraceEnrichmentTest` (mesmo arquivo) o caso da análise técnica (SQLite, SEM PostGIS): criar uma `AnalysisRecord` finalizada (factory) com per_cnae/fundamentação, processo em em_analise, chamar `decide($record, $analista)` e asserir que a `ViabilityDecision` tem `decision_trace` coerente com a ficha. Cobrir também o caso legado: uma `ViabilityDecision` factory SEM decision_trace continua válida (null) — a degradação honesta é exercida na 12-05.

    ANTI-REGRESSÃO Fase 10: rode a suíte da análise técnica/decisão — tudo verde; só decision_trace é novo.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="DecisionTraceEnrichmentTest|AnaliseTecnica|AnaliseDecisao"</verify>
  <acceptance_criteria>
    - grep "decision_trace" em app/Services/Analise/AnaliseTecnicaDecisionService.php.
    - DecisionTraceEnrichmentTest verde (caso expresso + caso análise técnica + legado null).
    - Suíte da análise técnica VERDE — sem regressão.
  </acceptance_criteria>
  <done>A decisão humana também grava o trace (da ficha, sem recomputo); decisão legada degrada honesta — fonte completa para HU-099.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste / Evidência |
|---|---|
| HU-099 RN-004 — passo a passo persistido (expresso) | DecisionTraceEnrichmentTest (caso expresso) |
| HU-099 RN-004 — passo a passo persistido (análise humana, da ficha) | DecisionTraceEnrichmentTest (caso análise) |
| HU-099 RN-005 — sem recomputo; legado degrada honesto | DecisionTraceEnrichmentTest (resolver chamado 1×; legado null) |
| ANTI-REGRESSÃO Fases 9/10 — enriquecimento aditivo | suítes Expresso (SQLite+postgis) e Análise verdes |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan migrate --no-interaction` aplica a migration aditiva.
- `php artisan test --compact --filter="DecisionTraceEnrichmentTest|Expresso|AnaliseTecnica"` verde.
- Suítes das Fases 9 e 10 (incluindo @group postgis do expresso) CONTINUAM verdes — o enriquecimento é estritamente aditivo.
</verification>

<success_criteria>
- decision_trace existe (JSON nullable, imutável) e é gravado pelos DOIS pontos de escrita a partir do snapshot já em memória, sem recomputo (RN-005).
- Suítes 9/10 verdes (anti-regressão); decisão legada degrada honesta (trace null).
- ZERO mudança no veredito/per_cnae/rules_versions/fundamentacao/transição/auditoria existentes; ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/12-auditoria-e-compliance/12-02-SUMMARY.md`: o SHAPE final do decision_trace por CNAE (campos de cada passo), como o expresso e a análise técnica o montam, e o contrato do caso legado (trace null). Insumo direto de 12-05 (DecisionExplanationService projeta exatamente este shape) e de 12-09 (UI passo a passo).
</output>

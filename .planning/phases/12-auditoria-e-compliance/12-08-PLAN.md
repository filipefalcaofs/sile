---
phase: 12-auditoria-e-compliance
plan: 08
type: execute
wave: 3
depends_on: ["12-06"]
files_modified:
  - app/Services/Abuso/Detectors/PoligonoRepetidoDetector.php
  - app/Services/Abuso/Detectors/InscricaoAtividadesIncompativeisDetector.php
  - app/Services/Abuso/Detectors/CondicionanteEvasaoDetector.php
  - app/Providers/AppServiceProvider.php
  - tests/Feature/Abuso/AbuseDetectorsEstruturaisTest.php
autonomous: true
requirements: [HU-149]

must_haves:
  truths:
    - "Os 3 detectores ESTRUTURAIS determinísticos existem e implementam AbuseDetector sobre dado REAL: PoligonoRepetidoDetector (mesmo polígono em endereços distintos), InscricaoAtividadesIncompativeisDetector (mesma inscrição imobiliária com atividades incompatíveis/simultâneas) e CondicionanteEvasaoDetector (respostas de condicionante sempre na opção que evita análise)"
    - "Os 3 ficam registrados na MESMA tag 'abuse.detectors' (AppServiceProvider) — o AbuseDetectionService da 12-06 passa a iterar os 5 detectores sem qualquer outra mudança; a EscritorioVirtualEncadeado permanece FORA (2ª onda — HU-139 texto livre)"
    - "Cada detector usa critério/limiar DETERMINÍSTICO e honesto (reusa parâmetros abuso.* existentes/constantes; NÃO adiciona parâmetro novo — 12-03 é dona do ParameterSeeder), com fingerprint estável (idempotência) e evidence real"
    - "PoligonoRepetido funciona em SQLite por comparação de geometria NORMALIZADA (hash do geojson), sem depender de PostGIS; a igualdade espacial plena (ST_Equals) fica como refinamento futuro registrado"
    - "Nenhum detector pune ou transiciona status — só produz AbuseFinding (RN-001); o gate real continua sendo o toggle OFF default (HU-149 nasce desligada)"
  artifacts:
    - path: "app/Services/Abuso/Detectors/PoligonoRepetidoDetector.php"
      provides: "Detector de polígono repetido em endereços distintos (hash normalizado)"
      contains: "implements AbuseDetector"
    - path: "app/Services/Abuso/Detectors/CondicionanteEvasaoDetector.php"
      provides: "Detector de respostas de condicionante evasivas (sempre evita análise)"
      contains: "implements AbuseDetector"
    - path: "app/Providers/AppServiceProvider.php"
      provides: "3 detectores estruturais acrescentados à tag abuse.detectors"
      contains: "abuse.detectors"
  key_links:
    - from: "app/Providers/AppServiceProvider.php"
      to: "AbuseDetectionService (tag abuse.detectors)"
      via: "tag aditiva inclui os 3 detectores estruturais (o serviço passa a iterar 5)"
      pattern: "PoligonoRepetidoDetector|InscricaoAtividadesIncompativeisDetector|CondicionanteEvasaoDetector"
    - from: "app/Services/Abuso/Detectors/PoligonoRepetidoDetector.php"
      to: "AbuseFinding"
      via: "detect(DetectionWindow) devolve findings com fingerprint estável e evidence"
      pattern: "AbuseFinding"
---

<objective>
Os 3 detectores ESTRUTURAIS de abuso (HU-149) que completam o conjunto mínimo da fase (Wave 3, par paralelo com 12-07; dono único de `AppServiceProvider` na wave): `PoligonoRepetidoDetector`, `InscricaoAtividadesIncompativeisDetector` e `CondicionanteEvasaoDetector`, implementando o contrato `AbuseDetector` da 12-06 e registrados na tag `abuse.detectors`.

Purpose: a 12-06 entregou o pipeline + os 2 detectores de volume; este plano adiciona os 3 estruturais (polígono repetido, inscrição com atividades incompatíveis, evasão por condicionante) — todos determinísticos sobre dado real, com fingerprint estável e evidence honesta, sem adicionar parâmetro novo (reusa abuso.*/constantes). O `EscritorioVirtualEncadeadoDetector` fica para a 2ª onda (depende de marcação estruturada — HU-139 é texto livre). Como o toggle nasce OFF, não há risco em produção até a SEDUR validar.
Output: 3 detectores + registro na tag, cobertos por testes determinísticos (acima/abaixo do critério, fingerprint estável, janela).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-auditoria-e-compliance/12-CONTEXT.md
@docs/superpowers/specs/2026-06-15-auditoria-compliance-design.md
@.planning/phases/12-auditoria-e-compliance/12-06-SUMMARY.md

# CONTRATO + DADO REAL (ler antes de codar):
@app/Services/Abuso/Contracts/AbuseDetector.php
@app/Services/Abuso/Detectors/VolumeCnpjDetector.php
@app/Providers/AppServiceProvider.php
@docs/SILE_HUs_Completas_MD/EP12-Auditoria-e-Compliance/HU-149-Detectar-padroes-de-abuso-e-fraude.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PoligonoRepetidoDetector + InscricaoAtividadesIncompativeisDetector</name>
  <read_first>app/Services/Abuso/Detectors/VolumeCnpjDetector.php (forma do detector: key/detect/fingerprint/evidence), o schema real de ViabilityRequest (property_polygon_geojson, property_registration/inscrição, CNAEs), 12-06-SUMMARY (AbuseFinding/DetectionWindow)</read_first>
  <files>app/Services/Abuso/Detectors/PoligonoRepetidoDetector.php, app/Services/Abuso/Detectors/InscricaoAtividadesIncompativeisDetector.php, tests/Feature/Abuso/AbuseDetectorsEstruturaisTest.php</files>
  <action>
    TDD estrito (RED primeiro). Espelhar a forma dos detectores de volume (12-06).
    - `PoligonoRepetidoDetector` (key 'poligono_repetido'): na janela, agrupa por uma CHAVE NORMALIZADA do polígono (hash do geojson normalizado — funciona em SQLite, sem PostGIS) e emite finding quando o MESMO polígono aparece em ENDEREÇOS DISTINTOS acima de um mínimo (reusar abuso.escritorio_virtual.limite ou um mínimo determinístico de 2 endereços distintos — à discrição, honesto). evidence: hash, endereços/ids distintos, total. Registrar (comentário/SUMMARY) que ST_Equals (igualdade espacial plena) é refinamento futuro.
    - `InscricaoAtividadesIncompativeisDetector` (key 'inscricao_atividades_incompativeis'): na janela, por `property_registration` (inscrição imobiliária), detecta atividades incompatíveis/simultâneas. Como a matriz oficial de incompatibilidade é pendência SEDUR, use um PROXY determinístico e honesto (ex.: mesma inscrição com ≥N processos ativos simultâneos de CNAEs primários distintos / requerentes distintos) — documente o critério no SUMMARY como provisório (SEDUR refina). evidence: inscrição, CNAEs/requerentes, ids. fingerprint estável.

    `AbuseDetectorsEstruturaisTest` (SQLite): semeia o padrão real (mesmo polígono em 2+ endereços; mesma inscrição com atividades distintas simultâneas) e o caso negativo; cada detector emite finding SÓ acima do critério, com fingerprint estável e respeitando a janela. SEM punição/transição (só finding).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=AbuseDetectorsEstruturaisTest</verify>
  <acceptance_criteria>
    - grep "implements AbuseDetector" nos 2 detectores; "poligono_repetido"/"inscricao_atividades_incompativeis" nas keys.
    - AbuseDetectorsEstruturaisTest verde (polígono repetido + inscrição; fingerprint estável; janela).
  </acceptance_criteria>
  <done>2 detectores estruturais determinísticos reais (polígono/inscrição), prontos para o pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: CondicionanteEvasaoDetector + registro dos 3 na tag (pipeline com 5 detectores)</name>
  <read_first>o schema real das respostas de condicionante (RiskCondicionante e como a solicitação registra as respostas), app/Providers/AppServiceProvider.php (tag 'abuse.detectors' da 12-06), app/Services/Abuso/AbuseDetectionService.php (itera a tag)</read_first>
  <files>app/Services/Abuso/Detectors/CondicionanteEvasaoDetector.php, app/Providers/AppServiceProvider.php, tests/Feature/Abuso/AbuseDetectorsEstruturaisTest.php</files>
  <action>
    TDD estrito. `CondicionanteEvasaoDetector` (key 'condicionante_evasao'): na janela, identifica requerentes/CNPJs cujas respostas de condicionante SEMPRE escolhem a opção que evita a análise (a "resposta certa" estatisticamente improvável) acima de um mínimo de processos — critério determinístico e honesto a partir do schema real das respostas (leia como a resposta evasiva é representada; documente o critério como provisório → SEDUR). evidence: requerente/CNPJ, processos, padrão. fingerprint estável.

    Registrar os 3 detectores estruturais na tag `abuse.detectors` do AppServiceProvider (tag é ADITIVA — a 12-06 já registrou os 2 de volume; aqui acrescentam-se os 3). Resultado: AbuseDetectionService passa a iterar os 5, sem outra mudança. EscritorioVirtualEncadeado NÃO entra (2ª onda).

    Estender `AbuseDetectorsEstruturaisTest`: o detector de condicionante emite finding no padrão evasivo e não no caso normal; e um teste de PIPELINE (com toggle ON) confirma que o AbuseDetectionService agora resolve/itera os 5 detectores (ex.: todos presentes na coleção tagueada; uma execução produz alertas dos detectores cujo padrão foi semeado, idempotente).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="AbuseDetectorsEstruturaisTest|AbuseDetectionServiceTest"</verify>
  <acceptance_criteria>
    - grep "condicionante_evasao" no detector; os 3 detectores na tag em AppServiceProvider.
    - Testes verdes: detector de condicionante + pipeline com 5 detectores (idempotente).
  </acceptance_criteria>
  <done>Conjunto mínimo de 5 detectores determinísticos completo e plugado no motor; EscritorioVirtual fica registrado para a 2ª onda.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-149 — polígono repetido em endereços distintos | AbuseDetectorsEstruturaisTest |
| HU-149 — inscrição com atividades incompatíveis/simultâneas | AbuseDetectorsEstruturaisTest |
| HU-149 — evasão por condicionante (sempre evita análise) | AbuseDetectorsEstruturaisTest |
| HU-149 — pipeline itera os 5 detectores (idempotente, sem punição) | AbuseDetectionServiceTest |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="AbuseDetectorsEstruturaisTest|AbuseDetectionServiceTest"` verde.
- Suíte completa CONTINUA verde; ÚNICO editor de AppServiceProvider na Wave 3; NÃO toca routes/gestao.php, ParameterSeeder nem config/sile.php.
</verification>

<success_criteria>
- 3 detectores estruturais determinísticos reais + os 2 de volume = 5 detectores plugados no motor.
- Critérios honestos (proxies determinísticos onde a regra oficial é pendência SEDUR), fingerprint estável, sem punição/transição; toggle OFF default.
- ZERO dependência/parâmetro novo; EscritorioVirtualEncadeado registrado para a 2ª onda.
</success_criteria>

<output>
Criar `.planning/phases/12-auditoria-e-compliance/12-08-SUMMARY.md`: os 3 detectores (keys, critério/limiar usado, evidence) com os critérios PROVISÓRIOS a validar com a SEDUR, e a confirmação de que a tag agora tem 5. Insumo de 12-09 (painel mostra a efetividade por rule_key) e 12-12 (smoke/seed dev de alertas).
</output>

---
phase: 12-auditoria-e-compliance
plan: 04
type: execute
wave: 2
depends_on: ["12-01", "12-03"]
files_modified:
  - app/Services/Auditoria/AuditTrailQueryService.php
  - app/Http/Controllers/Gestao/AuditoriaController.php
  - app/Http/Resources/ActivityResource.php
  - config/sile.php
  - routes/gestao.php
  - tests/Feature/Auditoria/AuditoriaConsultaTest.php
  - tests/Feature/Auditoria/AuditoriaExportTest.php
autonomous: true
requirements: [HU-098, HU-100, HU-101]

must_haves:
  truths:
    - "HU-100: o gestor/admin (consultar-auditoria) consulta a trilha unificada server-driven sobre activity_log com filtros REAIS — período, usuário (causer), entidade (subject_type+id), ação (log_name+event), resultado — paginada no servidor, do mais recente ao mais antigo, sem materializar nada"
    - "HU-098: a fonte 'alteracoes' lista SÓ os registros com attribute_changes preenchido (histórico de mudanças de dados), reusando o mesmo filtro/paginação"
    - "A fonte 'acessos' lista o histórico GLOBAL de access_logs (login/logout/falha/bloqueio) na MESMA UI, por merge na aplicação (não UNION SQL) — fonte secundária prunável"
    - "HU-101: o export CSV (?formato=csv) respeita os MESMOS filtros do index, em streaming (chunk 200), com guarda de volume técnico (auditoria.export.max_linhas em config); export pleno XLSX/PDF fica bloqueado honesto → HU-131/Fase 15"
    - "Meta-auditoria CA-02: a PRÓPRIA consulta e o PRÓPRIO export são auditados (RN-002) — quem consultou a trilha de quem fica registrado; 403 sem consultar-auditoria é auditado no ponto único"
    - "É o ÚNICO editor de routes/gestao.php e de config/sile.php na Wave 2 (os pares 12-05/12-06 não tocam esses arquivos)"
  artifacts:
    - path: "app/Services/Auditoria/AuditTrailQueryService.php"
      provides: "filtered(filtros) sobre activity_log (espelha ProcessoQueryService) + apenasAlteracoes + acessos (access_logs)"
      contains: "whereNotNull"
    - path: "app/Http/Controllers/Gestao/AuditoriaController.php"
      provides: "index roteia fonte (atividade/alteracoes/acessos), pagina, AUDITA; export CSV streaming auditado"
      contains: "streamDownload"
    - path: "app/Http/Resources/ActivityResource.php"
      provides: "Resource da trilha (causer/acting_for/subject/result/rules_version/ip/channel + attribute_changes)"
      contains: "attribute_changes"
  key_links:
    - from: "app/Http/Controllers/Gestao/AuditoriaController.php"
      to: "app/Services/Auditoria/AuditTrailQueryService.php"
      via: "index/export chamam filtered/apenasAlteracoes/acessos conforme a fonte e auditam a consulta"
      pattern: "AuditTrailQueryService"
    - from: "routes/gestao.php"
      to: "Gestao\\AuditoriaController"
      via: "grupo permission:consultar-auditoria → gestao.auditoria.index + gestao.auditoria.export"
      pattern: "auditoria"
---

<objective>
Superfície de consulta e exportação da trilha (Wave 2, par paralelo com 12-05/12-06; ÚNICO route-owner de `gestao.php` e dono de `config/sile.php` na wave): `AuditTrailQueryService` (espelha o `ProcessoQueryService`) sobre `activity_log`, `Gestao\AuditoriaController` (index roteando por fonte atividade/alteracoes/acessos + export CSV, ambos auditados — meta-auditoria CA-02), `ActivityResource` e as rotas `gestao.auditoria.*`.

Purpose: o CONTEXT decidiu que `activity_log` JÁ É a trilha unificada — consultar direto, server-driven, com os índices da 12-01. HU-100 é a consulta filtrável; HU-098 é a variante "só alterações" (whereNotNull attribute_changes); HU-101 é o CSV do mesmo conjunto (reusando o padrão streamDownload+chunk da Fase 10). `access_logs` entra como fonte secundária por merge na aplicação. A consulta da trilha É auditável (quem viu o quê — relevante p/ LGPD). Export pleno XLSX/PDF permanece bloqueado honesto (HU-131/Fase 15).
Output: service + controller + resource + rotas + guarda de volume em config, cobertos por feature tests (filtros/fontes/paginação/403 auditado/CSV auditado).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-auditoria-e-compliance/12-CONTEXT.md
@docs/superpowers/specs/2026-06-15-auditoria-compliance-design.md
@.planning/phases/12-auditoria-e-compliance/12-01-SUMMARY.md
@.planning/phases/12-auditoria-e-compliance/12-03-SUMMARY.md

# PADRÕES A ESPELHAR (query service server-driven + controller + CSV streaming + resource + rota auditada) — ler antes de codar:
@app/Services/Analise/ProcessoQueryService.php
@app/Http/Controllers/Gestao/ProcessoController.php
@app/Http/Controllers/Gestao/AccessHistoryController.php
@app/Models/Activity.php
@app/Models/AccessLog.php
@routes/gestao.php
@docs/SILE_HUs_Completas_MD/EP12-Auditoria-e-Compliance/HU-100-Consultar-trilha-de-auditoria.md
@docs/SILE_HUs_Completas_MD/EP12-Auditoria-e-Compliance/HU-098-Consultar-historico-de-alteracões.md
@docs/SILE_HUs_Completas_MD/EP12-Auditoria-e-Compliance/HU-101-Exportar-auditoria.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuditTrailQueryService + ActivityResource</name>
  <read_first>app/Services/Analise/ProcessoQueryService.php (filtered: when()/whereLike caseSensitive:false/orderByDesc/eager-load; helpers valor/inteiro/data), app/Models/Activity.php (causer/actingFor/subject + colunas SILE), app/Models/AccessLog.php</read_first>
  <files>app/Services/Auditoria/AuditTrailQueryService.php, app/Http/Resources/ActivityResource.php, tests/Feature/Auditoria/AuditoriaConsultaTest.php</files>
  <action>
    TDD estrito (RED primeiro). `AuditTrailQueryService` espelhando o `ProcessoQueryService`:
    - `filtered(array $filtros): Builder<Activity>` sobre `Activity::query()` com `when()` por filtro (só entra se informado): período (`whereDate created_at >= data_de` / `<= data_ate`), usuário (`causer_id`/`causer_type` ou busca por nome via whereHas causer), entidade (`subject_type` + `subject_id`), ação (`log_name` e/ou `event`), resultado (`result`). `whereLike(..., caseSensitive: false)` onde for texto. `orderByDesc('id')` e eager-load `causer`, `actingFor`, `subject` (evitar N+1).
    - `apenasAlteracoes(array $filtros): Builder` = `filtered()->whereNotNull('attribute_changes')` (HU-098).
    - `acessos(array $filtros): Builder<AccessLog>` = variante GLOBAL sobre `AccessLog` (não por usuário, ≠ AccessHistoryController), com os filtros aplicáveis (período/usuário/evento) — fonte secundária por merge na aplicação.
    - helpers de normalização (valor/inteiro/data) no estilo do ProcessoQueryService.

    `ActivityResource` (HU-098/100): created_at (ISO), log_name, event, description, causer (id/nome ou null=sistema), acting_for (id/nome), subject (tipo legível + id/rótulo), result, rules_version, ip_address, channel e `attribute_changes` (diff p/ HU-098). NÃO expor properties sensíveis cruas indevidamente (minimização LGPD).

    `AuditoriaConsultaTest` (SQLite): semeia activities variadas (via AuditService/factory de Activity) — por período/causer/subject/log_name/event/result; `filtered` aplica cada filtro; `apenasAlteracoes` traz só com attribute_changes; `acessos` lista access_logs globais. Asserções de presença/exclusão e ordenação desc.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=AuditoriaConsultaTest</verify>
  <acceptance_criteria>
    - grep "whereNotNull" (apenasAlteracoes) e "caseSensitive" em AuditTrailQueryService; "attribute_changes" em ActivityResource.
    - AuditoriaConsultaTest verde (filtros, fonte alteracoes, fonte acessos, ordenação).
  </acceptance_criteria>
  <done>Consulta unificada real (HU-100) + histórico de alterações (HU-098) + fonte de acessos, server-driven sobre a espinha activity_log.</done>
</task>

<task type="auto">
  <name>Task 2: AuditoriaController (index por fonte + export CSV) + config + rotas (ÚNICO route-owner W2)</name>
  <read_first>app/Http/Controllers/Gestao/ProcessoController.php (index server-driven + exportarCsv streamDownload+fputcsv+chunk(200) + filtrosPreenchidos + auditoria da consulta), routes/gestao.php (grupos permission:...; ordem rota estática antes de wildcard), config/sile.php (estrutura dos blocos)</read_first>
  <files>app/Http/Controllers/Gestao/AuditoriaController.php, config/sile.php, routes/gestao.php, tests/Feature/Auditoria/AuditoriaExportTest.php</files>
  <action>
    TDD estrito. `Gestao\AuditoriaController`:
    - `index(Request)`: lê `fonte` (atividade|alteracoes|acessos; default atividade) e os filtros; chama o método correspondente do `AuditTrailQueryService`; pagina server-side (`ui.auditoria.per_page`, opções fixas no estilo PER_PAGE_OPTIONS) via `ActivityResource` (ou mapeamento mínimo para acessos); com `?formato=csv` delega ao export. AUDITA a própria consulta — `audit->log('auditoria','consulta-trilha', ..., personalData: true)` (a trilha pode conter PII — meta-auditoria CA-02, relevante p/ LGPD). Inertia::render('gestao/auditoria/index', props: registros paginados, filtros, fonteOptions, perPageOptions).
    - `export(Request)` (ou index ?formato=csv): CSV do conjunto filtrado em `streamDownload` + `fputcsv` + `->chunk(200)` (padrão Fase 10), respeitando os MESMOS filtros e a fonte; guarda de volume `config('sile.auditoria.export.max_linhas')` (limita/avisa honesto); AUDITA o export (`event 'exporta-trilha-csv'`, personalData: true). Export pleno XLSX/PDF → comentar como HU-131/Fase 15 (bloqueado honesto).

    `config/sile.php`: adicionar bloco `'auditoria' => ['export' => ['max_linhas' => 50000]]` (constante técnica — NÃO é parâmetro de negócio). Ajustar à sua discrição o valor default.

    Rotas em `routes/gestao.php` (ÚNICO editor na Wave 2) sob `permission:consultar-auditoria`:
    - `GET gestao/auditoria` → index (name `gestao.auditoria.index`)
    - `GET gestao/auditoria/export` → export (name `gestao.auditoria.export`) — rota estática ANTES de qualquer wildcard do grupo.

    `AuditoriaExportTest` (SQLite): com consultar-auditoria, o CSV respeita os filtros (linhas esperadas) e a exportação é auditada; SEM a permissão → 403 (auditado no ponto único); a consulta do index também grava auditoria (meta-auditoria). Cobrir as 3 fontes no index (atividade/alteracoes/acessos) retornando Inertia ok.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=AuditoriaExportTest && php artisan route:list --path=gestao/auditoria</verify>
  <acceptance_criteria>
    - grep "streamDownload" e "consultar-auditoria"/"auditoria" em AuditoriaController/routes; "auditoria" em config/sile.php.
    - `php artisan route:list --path=gestao/auditoria` lista index + export.
    - AuditoriaExportTest verde (CSV filtrado + auditado; 403 sem permissão; meta-auditoria do index).
  </acceptance_criteria>
  <done>Trilha consultável e exportável (CSV) na retaguarda, gated e auto-auditada (meta-auditoria CA-02); export pleno bloqueado honesto.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-100 — consulta filtrável (período/usuário/entidade/ação/resultado), paginada | AuditoriaConsultaTest |
| HU-098 — fonte 'alteracoes' (só attribute_changes) | AuditoriaConsultaTest |
| HU-100 — fonte 'acessos' (access_logs global, merge na app) | AuditoriaConsultaTest |
| HU-101 — CSV respeita filtros, streaming, auditado | AuditoriaExportTest |
| CA-02 / RN-002 — meta-auditoria da consulta/export + 403 auditado | AuditoriaExportTest |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="AuditoriaConsultaTest|AuditoriaExportTest"` verde.
- `php artisan route:list --path=gestao/auditoria` lista index + export sob consultar-auditoria.
- Suíte completa CONTINUA verde; ÚNICO editor de routes/gestao.php e config/sile.php na Wave 2.
</verification>

<success_criteria>
- HU-098/100/101 reais: trilha unificada filtrável + histórico de alterações + fonte de acessos + CSV, tudo server-driven sobre activity_log/access_logs, sem materializar.
- Meta-auditoria (consulta/export auditados, personalData) e 403 auditado; guarda de volume técnica em config.
- ZERO dependência nova; ÚNICO route-owner/config-owner da Wave 2.
</success_criteria>

<output>
Criar `.planning/phases/12-auditoria-e-compliance/12-04-SUMMARY.md`: a API do AuditTrailQueryService (filtered/apenasAlteracoes/acessos), o shape do ActivityResource, as rotas gestao.auditoria.* (names), as props do Inertia 'gestao/auditoria/index' e a chave config auditoria.export.max_linhas. Insumo direto de 12-10 (página da trilha) e 12-12 (smoke/verificação).
</output>

---
phase: 12-auditoria-e-compliance
plan: 06
type: execute
wave: 2
depends_on: ["12-03"]
files_modified:
  - app/Services/Abuso/Contracts/AbuseDetector.php
  - app/Services/Abuso/AbuseFinding.php
  - app/Services/Abuso/DetectionWindow.php
  - app/Services/Abuso/Detectors/VolumeCnpjDetector.php
  - app/Services/Abuso/Detectors/VolumeContadorDetector.php
  - app/Services/Abuso/AbuseDetectionService.php
  - app/Services/Analise/MalhaFinaService.php
  - app/Console/Commands/AbusoDetectarCommand.php
  - app/Providers/AppServiceProvider.php
  - routes/console.php
  - tests/Feature/Abuso/AbuseDetectionServiceTest.php
  - tests/Feature/Abuso/AbuseDetectorsTest.php
autonomous: true
requirements: [HU-149]

must_haves:
  truths:
    - "Existe o contrato Strategy AbuseDetector { key(): string; detect(DetectionWindow): iterable<AbuseFinding> } com 2 detectores determinísticos reais sobre dado REAL: VolumeCnpjDetector (mesmo CNPJ acima de abuso.volume_cnpj.limite na janela) e VolumeContadorDetector (mesmo criador/contador acima de abuso.volume_contador.limite)"
    - "AbuseDetectionService só roda quando features.deteccao_abuso está ON (no-op HONESTO quando OFF — default); itera os detectores habilitados na janela abuso.janela_dias, faz upsert IDEMPOTENTE em abuse_alerts (não duplica alerta aberto) e AUDITA"
    - "HU-149 RN-001 — NUNCA pune: acima de abuso.severidade_malha_fina o serviço encaminha à malha fina (MalhaFinaService, ator=sistema) e grava fine_mesh_referral_id no alerta; JAMAIS indefere/cassa nem transiciona status (malha fina é ortogonal — Fase 10)"
    - "abuso:detectar roda no scheduler (daily, withoutOverlapping, onOneServer) e é no-op honesto com o toggle OFF — espelha o padrão idempotente das Fases 9/11 (ExpressoIndeferirSemBap/EscalonarSla)"
    - "ANTI-REGRESSÃO Fase 10: a malha fina ganha SÓ um caminho ADITIVO de sistema (encaminharSistema, referred_by null) — encaminhar(User) e a suíte de malha fina seguem VERDES; é par paralelo de 12-04/12-05 (dono único de routes/console.php e AppServiceProvider na wave)"
  artifacts:
    - path: "app/Services/Abuso/Contracts/AbuseDetector.php"
      provides: "Interface Strategy dos detectores (key + detect(Janela))"
      contains: "interface AbuseDetector"
    - path: "app/Services/Abuso/AbuseDetectionService.php"
      provides: "Orquestra detectores (Settings-gated), upsert idempotente, malha fina acima do limiar, audita"
      contains: "deteccao_abuso"
    - path: "app/Console/Commands/AbusoDetectarCommand.php"
      provides: "abuso:detectar idempotente; no-op honesto com toggle off"
      contains: "abuso:detectar"
    - path: "app/Services/Analise/MalhaFinaService.php"
      provides: "encaminharSistema(request, motivo) aditivo (referred_by null = sistema)"
      contains: "encaminharSistema"
  key_links:
    - from: "app/Services/Abuso/AbuseDetectionService.php"
      to: "app/Services/Analise/MalhaFinaService.php"
      via: "severity >= abuso.severidade_malha_fina → encaminharSistema(...) e grava fine_mesh_referral_id"
      pattern: "encaminharSistema|fine_mesh_referral_id"
    - from: "routes/console.php"
      to: "AbusoDetectarCommand"
      via: "Schedule::command('abuso:detectar')->daily()->withoutOverlapping()->onOneServer()"
      pattern: "abuso:detectar"
---

<objective>
Motor de detecção de abuso (HU-149) — ALERTA + malha fina, NUNCA punição (Wave 2, par paralelo com 12-04/12-05; dono único de `routes/console.php` e `AppServiceProvider` na wave): contrato `AbuseDetector` (Strategy) + 2 detectores de volume determinísticos sobre dado real, `AbuseDetectionService` (toggle-gated, upsert idempotente em `abuse_alerts`, encaminhamento à malha fina acima do limiar, auditado), o caminho ADITIVO `MalhaFinaService::encaminharSistema` (ator=sistema) e o comando idempotente `abuso:detectar` no scheduler.

Purpose: com o expresso decidindo sozinho, a autodeclaração vira o ponto fraco — a defesa é vigiar com regras determinísticas + amostragem dirigida à malha fina (HU-136), nunca frear/punir automaticamente (RN-001). O motor nasce DESLIGADO (features.deteccao_abuso=0) até a SEDUR validar limiares; quando ligado, gera alertas idempotentes e, acima de `abuso.severidade_malha_fina`, encaminha à malha fina (ortogonal ao status — não decide nada). Este plano entrega o pipeline + 2 detectores de referência; os 3 detectores estruturais vêm na 12-08. O painel humano é a 12-09.
Output: contrato + 2 detectores + service + comando/scheduler + caminho de sistema na malha fina, cobertos por testes (idempotência, no-op toggle off, malha fina acima do limiar, NUNCA muda status).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-auditoria-e-compliance/12-CONTEXT.md
@docs/superpowers/specs/2026-06-15-auditoria-compliance-design.md
@.planning/phases/12-auditoria-e-compliance/12-03-SUMMARY.md

# PADRÕES A ESPELHAR (comando idempotente no scheduler + serviço Settings-gated + malha fina + binding) — ler antes de codar:
@app/Console/Commands/EscalonarSlaCommand.php
@app/Console/Commands/ExpressoIndeferirSemBapCommand.php
@routes/console.php
@app/Services/Analise/MalhaFinaService.php
@app/Providers/AppServiceProvider.php
@app/Support/Settings.php
@docs/SILE_HUs_Completas_MD/EP12-Auditoria-e-Compliance/HU-149-Detectar-padroes-de-abuso-e-fraude.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contrato AbuseDetector + DTOs + 2 detectores de volume + binding</name>
  <read_first>app/Services/Abuso (não existe — criar), app/Models/AbuseAlert.php (campos rule_key/fingerprint/severity/evidence), app/Providers/AppServiceProvider.php (estilo de bind/tag), app/Support/Settings.php (Settings::get com fallback config)</read_first>
  <files>app/Services/Abuso/Contracts/AbuseDetector.php, app/Services/Abuso/AbuseFinding.php, app/Services/Abuso/DetectionWindow.php, app/Services/Abuso/Detectors/VolumeCnpjDetector.php, app/Services/Abuso/Detectors/VolumeContadorDetector.php, app/Providers/AppServiceProvider.php, tests/Feature/Abuso/AbuseDetectorsTest.php</files>
  <action>
    TDD estrito (RED primeiro). Contrato e DTOs imutáveis:
    - `interface AbuseDetector { public function key(): string; /** @return iterable<AbuseFinding> */ public function detect(DetectionWindow $window): iterable; }`
    - `AbuseFinding` (readonly): rule_key, severity (AbuseSeverity), fingerprint (string determinístico), evidence (array), viability_request_id (?int), subject (?Model), window_start/window_end.
    - `DetectionWindow` (readonly): start/end (CarbonImmutable) — derivado de `abuso.janela_dias`.

    2 detectores determinísticos sobre dado REAL (queries Eloquent; SEM IA):
    - `VolumeCnpjDetector` (key 'volume_cnpj'): agrupa solicitações por CNPJ da empresa na janela; quando o total > `abuso.volume_cnpj.limite`, emite um finding (severity à sua discrição por faixa; fingerprint estável = ex. hash(rule_key+cnpj+janela)); evidence com cnpj/total/ids.
    - `VolumeContadorDetector` (key 'volume_contador'): idem por criador/representante (o "contador" — usar o ator que cria as solicitações, ex. created_by/requester conforme o schema real), limiar `abuso.volume_contador.limite`.
    O fingerprint deve ser ESTÁVEL para a mesma ocorrência na janela (idempotência da 12-03). Limiares e janela vêm de Settings (HU-014), com fallback em config.

    Binding em `AppServiceProvider`: registrar os 2 detectores numa TAG (ex. `$this->app->tag([VolumeCnpjDetector::class, VolumeContadorDetector::class], 'abuse.detectors')`) — a 12-08 ACRESCENTA os demais à mesma tag (tag é aditiva). O AbuseDetectionService receberá `iterable<AbuseDetector>` resolvido por essa tag.

    `AbuseDetectorsTest` (SQLite): semeia dado real do padrão (N+1 solicitações do mesmo CNPJ/contador na janela) e dado fora do padrão; cada detector emite finding SÓ acima do limiar, com fingerprint estável (mesma entrada → mesmo fingerprint) e evidence correta; respeita a janela (fora da janela não conta).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=AbuseDetectorsTest</verify>
  <acceptance_criteria>
    - grep "interface AbuseDetector" e os 2 detectores em app/Services/Abuso; tag 'abuse.detectors' em AppServiceProvider.
    - AbuseDetectorsTest verde (acima/abaixo do limiar, fingerprint estável, janela).
  </acceptance_criteria>
  <done>Contrato Strategy + 2 detectores determinísticos reais (volume) com fingerprint idempotente, registrados por tag.</done>
</task>

<task type="auto">
  <name>Task 2: AbuseDetectionService (toggle-gated, idempotente, malha fina) + MalhaFinaService::encaminharSistema (aditivo)</name>
  <read_first>app/Models/AbuseAlert.php (índice único parcial p/ aberto), app/Services/Analise/MalhaFinaService.php (encaminhar/registrar — referred_by_user_id já é nullable), app/Support/Audit/AuditService.php</read_first>
  <files>app/Services/Abuso/AbuseDetectionService.php, app/Services/Analise/MalhaFinaService.php, tests/Feature/Abuso/AbuseDetectionServiceTest.php</files>
  <action>
    TDD estrito. `MalhaFinaService::encaminharSistema(ViabilityRequest $request, string $motivo): FineMeshReferral` — método ADITIVO (NÃO alterar encaminhar/encaminharLote/registrar existentes): cria o fine_mesh_referrals com `referred_by_user_id => null` (coluna já nullable) e liga in_fine_mesh via forceFill, auditando com marcador "ator=sistema" (RN-002). ANTI-REGRESSÃO: a suíte de malha fina (Fase 10) segue verde.

    `AbuseDetectionService::detectar(): array` (ou run()):
    - se `features.deteccao_abuso` (Settings, fallback config) OFF → NO-OP honesto (retorna resumo zerado, NÃO grava nada).
    - monta a `DetectionWindow` de `abuso.janela_dias`; itera os `iterable<AbuseDetector>` (tag); para cada finding, faz UPSERT IDEMPOTENTE em abuse_alerts: se já existe alerta ABERTO com (rule_key, fingerprint) → no-op (atualiza evidence/window se quiser, sem duplicar); senão cria (status aberto, detected_at, evidence, subject/viability_request_id).
    - se `finding.severity >= abuso.severidade_malha_fina` (comparar pelo peso do enum) → `MalhaFinaService::encaminharSistema(request, "suspeita de abuso: {rule_key}")` e gravar `fine_mesh_referral_id` no alerta. NUNCA transiciona status (RN-001).
    - AUDITA o ciclo (quantos alertas criados/encaminhados).

    `AbuseDetectionServiceTest` (SQLite):
    - toggle OFF → detectar() é no-op (0 alertas; nada auditado de criação).
    - toggle ON + padrão presente → cria alerta(s); 2ª execução NÃO duplica (idempotência via índice parcial).
    - severity alta (>= limiar) → cria fine_mesh_referrals (encaminharSistema), grava fine_mesh_referral_id, e o STATUS do processo NÃO muda (CA-02 anti-fachada); severity baixa → alerta sem malha fina.
    - injeta um detector FAKE controlado (via tag/instance) para o caso determinístico sem depender dos 2 reais.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="AbuseDetectionServiceTest|MalhaFina"</verify>
  <acceptance_criteria>
    - grep "deteccao_abuso" e "encaminharSistema"/"fine_mesh_referral_id" em AbuseDetectionService; "encaminharSistema" em MalhaFinaService.
    - AbuseDetectionServiceTest verde (no-op off, idempotência, malha fina acima do limiar, status NÃO muda); suíte MalhaFina verde (anti-regressão).
  </acceptance_criteria>
  <done>Detecção real: alerta idempotente + malha fina acima do limiar, NUNCA punição/transição; toggle off no-op honesto.</done>
</task>

<task type="auto">
  <name>Task 3: abuso:detectar (comando idempotente) + scheduler</name>
  <read_first>app/Console/Commands/EscalonarSlaCommand.php e ExpressoIndeferirSemBapCommand.php (estrutura do comando + no-op honesto), routes/console.php (Schedule::command ... withoutOverlapping/onOneServer)</read_first>
  <files>app/Console/Commands/AbusoDetectarCommand.php, routes/console.php, tests/Feature/Abuso/AbuseDetectionServiceTest.php</files>
  <action>
    TDD estrito. `AbusoDetectarCommand` (signature `abuso:detectar`): injeta `AbuseDetectionService`, chama detectar(), imprime o resumo (criados/encaminhados ou "detecção desligada" no no-op). NUNCA decide — só orquestra o serviço (RN-001/RN-003).

    `routes/console.php`: `Schedule::command('abuso:detectar')->daily()->withoutOverlapping()->onOneServer();` com comentário honesto (no-op enquanto features.deteccao_abuso=0; SEDUR liga). É o ÚNICO editor de routes/console.php na Wave 2.

    Estender `AbuseDetectionServiceTest` (ou um teste de comando): `php artisan abuso:detectar` com toggle OFF → no-op honesto (exit 0, nada criado); com toggle ON + padrão → cria alertas; 2ª chamada idempotente (não duplica).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=AbuseDetectionServiceTest && php artisan abuso:detectar</verify>
  <acceptance_criteria>
    - grep "abuso:detectar" no comando e em routes/console.php (daily/withoutOverlapping/onOneServer).
    - `php artisan abuso:detectar` roda sem erro (no-op honesto com toggle off por default).
  </acceptance_criteria>
  <done>Detecção agendada idempotente e segura em multi-instância; no-op honesto enquanto desligada.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-149 CA-01 — alerta por padrão (volume) + malha fina acima do limiar | AbuseDetectionServiceTest + AbuseDetectorsTest |
| HU-149 CA-02 / RN-001 — NUNCA pune/transiciona status | AbuseDetectionServiceTest |
| HU-149 — idempotência (não duplica alerta aberto) | AbuseDetectionServiceTest |
| HU-149 RN-002 — toggle OFF no-op honesto; auditado | AbuseDetectionServiceTest / comando |
| ANTI-REGRESSÃO Fase 10 — encaminharSistema aditivo | suíte MalhaFina verde |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="AbuseDetectorsTest|AbuseDetectionServiceTest|MalhaFina"` verde.
- `php artisan abuso:detectar` no-op honesto com o toggle off.
- Suíte completa CONTINUA verde; ÚNICO editor de routes/console.php e AppServiceProvider na Wave 2; NÃO toca routes/gestao.php nem config/sile.php.
</verification>

<success_criteria>
- Pipeline de detecção real: contrato + 2 detectores de volume + serviço idempotente toggle-gated + comando/scheduler.
- Malha fina acima do limiar via caminho de sistema (aditivo), NUNCA punição/transição (RN-001); toggle OFF default.
- ZERO dependência nova; anti-regressão Fase 10; par paralelo limpo.
</success_criteria>

<output>
Criar `.planning/phases/12-auditoria-e-compliance/12-06-SUMMARY.md`: o contrato AbuseDetector + shape de AbuseFinding/DetectionWindow, como a tag 'abuse.detectors' é consumida, a API do AbuseDetectionService, a assinatura de MalhaFinaService::encaminharSistema e o comando/scheduler. Insumo direto de 12-08 (acrescenta 3 detectores à tag) e 12-09 (painel lê abuse_alerts + efetividade).
</output>

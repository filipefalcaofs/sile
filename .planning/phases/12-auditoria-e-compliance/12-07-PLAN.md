---
phase: 12-auditoria-e-compliance
plan: 07
type: execute
wave: 3
depends_on: ["12-01", "12-03", "12-04", "12-05"]
files_modified:
  - app/Services/Lgpd/LgpdMonitorService.php
  - app/Http/Controllers/Gestao/LgpdMonitorController.php
  - app/Http/Controllers/Gestao/ProcessoController.php
  - app/Http/Controllers/Gestao/AccessHistoryController.php
  - app/Http/Controllers/Gestao/UserManagementController.php
  - routes/gestao.php
  - tests/Feature/Lgpd/LgpdMonitorTest.php
  - tests/Feature/Lgpd/PersonalDataMarkingTest.php
autonomous: true
requirements: [HU-102]

must_haves:
  truths:
    - "HU-102: o admin (monitorar-lgpd) vê um painel que AGREGA 3 fontes REAIS — consentimentos (LegalTerm::current('lgpd') × LegalTermAcceptance: % de aceite da versão vigente, pendentes de re-aceite), retenção (retencao.access_logs.dias + data do último pruning lido da trilha) e acessos a dado pessoal (contagem/série de activity_log onde personal_data=true)"
    - "Os call sites REAIS de leitura sensível passam a marcar personal_data=true via AuditService: detalhe de processo (ProcessoController::show), histórico de acessos de terceiro (Gestao\\AccessHistoryController) e gestão de usuários (UserManagementController) — de forma ADITIVA (só acrescenta o parâmetro, sem mudar a auditoria existente)"
    - "O painel LGPD é minimizado (LGPD): mostra MÉTRICAS e contagens, nunca despeja PII; a própria consulta ao painel é auditada (RN-002) e marcada personal_data quando lista acessos a dado pessoal"
    - "A retenção é honesta: as decisões (viability_decisions) ficam FORA do pruning (compliance) — o painel deixa isso explícito; só access_logs é prunável (retencao.access_logs.dias)"
    - "É o ÚNICO editor de routes/gestao.php na Wave 3; pendência DPO (eliminação/anonimização do titular) fica REGISTRADA, não inventada"
  artifacts:
    - path: "app/Services/Lgpd/LgpdMonitorService.php"
      provides: "consentimentos()/acessosDadoPessoal()/retencao() agregando fontes reais"
      contains: "LegalTermAcceptance"
    - path: "app/Http/Controllers/Gestao/LgpdMonitorController.php"
      provides: "Painel LGPD (gate monitorar-lgpd, auditado, minimizado)"
      contains: "monitorar-lgpd"
    - path: "app/Http/Controllers/Gestao/ProcessoController.php"
      provides: "audit do show marcado personalData: true (leitura de dado pessoal do processo)"
      contains: "personalData"
  key_links:
    - from: "app/Services/Lgpd/LgpdMonitorService.php"
      to: "activity_log.personal_data"
      via: "acessosDadoPessoal() conta/agrupa registros com personal_data=true"
      pattern: "personal_data"
    - from: "routes/gestao.php"
      to: "Gestao\\LgpdMonitorController"
      via: "grupo permission:monitorar-lgpd → gestao.lgpd.index"
      pattern: "lgpd"
---

<objective>
Monitoramento de conformidade LGPD (HU-102) sobre fontes REAIS (Wave 3; ÚNICO route-owner de `gestao.php` na wave): `LgpdMonitorService` (consentimentos × LegalTermAcceptance, retenção + último pruning, acessos a dado pessoal via `personal_data`), `Gestao\LgpdMonitorController` (gate `monitorar-lgpd`, auditado, minimizado) e a MARCAÇÃO `personal_data=true` nos call sites reais de leitura sensível (detalhe de processo, histórico de acessos de terceiro, gestão de usuários).

Purpose: a 12-01 criou a coluna `personal_data` e o parâmetro no `AuditService`; este plano (a) liga essa marca nos pontos onde o sistema realmente lê PII e (b) entrega o painel que agrega consentimentos/retenção/acessos a dado pessoal — tudo sobre dado real, minimizado (métricas, não PII crua) e auditado. A retenção é honesta: decisões ficam fora do pruning (compliance); só access_logs é prunável. Direitos do titular (eliminação/anonimização) são pendência DPO — registrada, nunca um rito inventado.
Output: service + controller + rota + marcação dos call sites, cobertos por testes (cobertura de consentimento da versão vigente, contagem de acessos marcados, retenção + último pruning, 403 sem permissão).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-auditoria-e-compliance/12-CONTEXT.md
@docs/superpowers/specs/2026-06-15-auditoria-compliance-design.md
@.planning/phases/12-auditoria-e-compliance/12-01-SUMMARY.md
@.planning/phases/12-auditoria-e-compliance/12-03-SUMMARY.md
@.planning/phases/12-auditoria-e-compliance/12-05-SUMMARY.md

# PADRÕES + FONTES (ler antes de codar):
@app/Models/LegalTerm.php
@app/Models/LegalTermAcceptance.php
@app/Models/AccessLog.php
@app/Http/Controllers/Gestao/AccessHistoryController.php
@app/Http/Controllers/Gestao/ProcessoController.php
@app/Support/Audit/AuditService.php
@routes/gestao.php
@docs/SILE_HUs_Completas_MD/EP12-Auditoria-e-Compliance/HU-102-Monitorar-conformidade-LGPD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Marcar personal_data nos call sites reais de leitura sensível (aditivo)</name>
  <read_first>app/Http/Controllers/Gestao/ProcessoController.php (show audita 'consulta-processo'; este plano edita o MESMO método que a 12-05 — por isso depende dela), app/Http/Controllers/Gestao/AccessHistoryController.php (audita 'consulta-acessos' de terceiro), app/Http/Controllers/Gestao/UserManagementController.php (consulta/gestão de usuários), 12-01-SUMMARY (posição do parâmetro personalData)</read_first>
  <files>app/Http/Controllers/Gestao/ProcessoController.php, app/Http/Controllers/Gestao/AccessHistoryController.php, app/Http/Controllers/Gestao/UserManagementController.php, tests/Feature/Lgpd/PersonalDataMarkingTest.php</files>
  <action>
    TDD estrito (RED primeiro). Marcar `personalData: true` (parâmetro aditivo da 12-01) nas chamadas `AuditService::log` dos pontos onde o sistema REALMENTE lê dado pessoal de terceiro:
    - `Gestao\ProcessoController::show` — consulta do processo de um cidadão (dados da empresa/requerente). Acrescentar `personalData: true` ao log existente 'consulta-processo' (SÓ isso muda; não tocar a prop explicacao da 12-05 nem o resto). É o mesmo método editado pela 12-05 — por isso este plano depende da 12-05 (serialização).
    - `Gestao\AccessHistoryController` — consulta administrativa do histórico de acessos de OUTRO usuário ('consulta-acessos'): `personalData: true`.
    - `UserManagementController` — leitura/gestão dos dados de usuários: marcar `personalData: true` no(s) ponto(s) reais de leitura de PII (confirme a chamada exata lendo o controller; se a listagem não expõe PII sensível, marque só onde há leitura de dado pessoal — honesto, sem marcar por excesso).
    NÃO criar marcação fictícia: só os pontos REAIS de leitura sensível (CONTEXT). NADA além de acrescentar o parâmetro.

    `PersonalDataMarkingTest` (SQLite): ao acessar o show de um processo de terceiro / o histórico de acessos de outro usuário / a gestão de usuários, o registro de auditoria correspondente tem `personal_data` = true. ANTI-REGRESSÃO: as suítes existentes desses controllers seguem verdes (mudança aditiva).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="PersonalDataMarkingTest|Processo|AccessHistory|UserManagement"</verify>
  <acceptance_criteria>
    - grep "personalData" em ProcessoController/AccessHistoryController/UserManagementController.
    - PersonalDataMarkingTest verde (personal_data=true nos call sites); suítes dos controllers verdes (anti-regressão).
  </acceptance_criteria>
  <done>Leitura de PII passa a ser marcada personal_data=true nos pontos reais — a base de medição do painel LGPD existe de verdade.</done>
</task>

<task type="auto">
  <name>Task 2: LgpdMonitorService + LgpdMonitorController + rota (ÚNICO route-owner W3)</name>
  <read_first>app/Models/LegalTerm.php (current('lgpd')), app/Models/LegalTermAcceptance.php (aceites), app/Models/AccessLog.php (retenção/prunable retencao.access_logs.dias), routes/gestao.php (grupo permission), como o pruning é auditado (listener AuditModelsPruned → confirmar log_name do último pruning, ex. 'retencao')</read_first>
  <files>app/Services/Lgpd/LgpdMonitorService.php, app/Http/Controllers/Gestao/LgpdMonitorController.php, routes/gestao.php, tests/Feature/Lgpd/LgpdMonitorTest.php</files>
  <action>
    TDD estrito. `LgpdMonitorService` (3 agregações reais, minimizadas):
    - `consentimentos()`: versão vigente do termo (LegalTerm::current('lgpd')) × LegalTermAcceptance — total de usuários, % que aceitou a versão vigente, pendentes de (re)aceite, e (à discrição) série/contagem por versão.
    - `retencao()`: `retencao.access_logs.dias` (Settings) + a data do ÚLTIMO pruning lido da trilha (activity_log do pruning de access_logs — confirme o log_name/event reais do AuditModelsPruned); deixa explícito que as DECISÕES ficam FORA do pruning (compliance).
    - `acessosDadoPessoal()`: contagem/série de activity_log onde `personal_data = true` (janela à discrição, reusando ui.dashboard.* se fizer sentido) — quem acessou PII de quem, em MÉTRICA (não despeja PII).

    `Gestao\LgpdMonitorController@index` (gate `permission:monitorar-lgpd`): monta as 3 seções via o service, AUDITA a consulta (`audit->log('lgpd','consulta-painel', ..., personalData: true)` — listar acessos a PII é, em si, acesso a dado de auditoria sensível), Inertia::render('gestao/lgpd/index', props minimizadas). Registrar como PENDÊNCIA DPO (no payload/seção informativa, honesto) os direitos do titular de eliminação/anonimização — sem inventar rito.

    Rota em `routes/gestao.php` (ÚNICO editor na Wave 3): `GET gestao/lgpd` → index (name `gestao.lgpd.index`) sob `permission:monitorar-lgpd`.

    `LgpdMonitorTest` (SQLite): cobertura de consentimento da versão vigente (aceites/pendentes corretos ao mudar a versão do termo); acessosDadoPessoal conta SÓ os personal_data=true (integra com o Task 1); retenção devolve os dias + o último pruning (semear um activity de pruning) e marca decisões fora do pruning; SEM monitorar-lgpd → 403 (auditado no ponto único); a consulta do painel é auditada.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=LgpdMonitorTest && php artisan route:list --path=gestao/lgpd</verify>
  <acceptance_criteria>
    - grep "LegalTermAcceptance"/"personal_data" em LgpdMonitorService; "monitorar-lgpd" em controller/rota.
    - `php artisan route:list --path=gestao/lgpd` lista o index.
    - LgpdMonitorTest verde (consentimentos, acessos PII, retenção + pruning, 403, auditoria).
  </acceptance_criteria>
  <done>Painel LGPD real (consentimentos/retenção/acessos a PII), minimizado e auditado; pendência DPO registrada honesta.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-102 — consentimentos (versão vigente, aceites/pendentes) | LgpdMonitorTest |
| HU-102 — acessos a dado pessoal (personal_data=true) | LgpdMonitorTest + PersonalDataMarkingTest |
| HU-102 — retenção (dias + último pruning; decisões fora) | LgpdMonitorTest |
| HU-102 / RN-002 — painel gated/auditado/minimizado; 403 | LgpdMonitorTest |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="PersonalDataMarkingTest|LgpdMonitorTest"` verde.
- `php artisan route:list --path=gestao/lgpd` lista o painel sob monitorar-lgpd.
- Suíte completa CONTINUA verde; ÚNICO editor de routes/gestao.php na Wave 3; marcação personal_data aditiva (sem regressão).
</verification>

<success_criteria>
- HU-102 real: painel agrega consentimentos/retenção/acessos a PII sobre dado real, minimizado e auditado.
- personal_data marcado nos call sites REAIS (sem fachada, sem marcar por excesso); retenção honesta (decisões fora do pruning).
- ZERO dependência nova; ÚNICO route-owner da Wave 3; pendência DPO registrada, nunca inventada.
</success_criteria>

<output>
Criar `.planning/phases/12-auditoria-e-compliance/12-07-SUMMARY.md`: a API do LgpdMonitorService, os call sites marcados personal_data, a rota gestao.lgpd.index e as props do Inertia 'gestao/lgpd/index'. Insumo direto de 12-10 (página do painel LGPD) e 12-12 (smoke/verificação).
</output>

---
phase: 09-fluxo-expresso
plan: 10
type: execute
wave: 5
depends_on: ["09-05", "09-08", "09-09"]
files_modified:
  - app/Services/Expresso/BusinessDeadlineCalculator.php
  - app/Services/Expresso/IndeferirSemBapService.php
  - app/Console/Commands/ExpressoIndeferirSemBapCommand.php
  - routes/console.php
  - database/factories/ViabilityRequestFactory.php
  - tests/Feature/Expresso/IndeferirSemBapTest.php
  - tests/Feature/Expresso/ExpressoIndeferirSemBapCommandTest.php
autonomous: true
requirements: [HU-134]

must_haves:
  truths:
    - "HU-134 está DORMENTE: a rotina, o parâmetro (expresso.bap.prazo_horas) e o estado (aguardando_bap) existem, mas NADA coloca processos em aguardando_bap hoje (BapRegistry indisponível) — em produção a varredura é no-op (zero processos)"
    - "O comando expresso:indeferir-sem-bap varre aguardando_bap com bap_due_at vencido (calculado por BusinessDeadlineCalculator, prazo parametrizável) e indefere: ViabilityDecision outcome indeferida + reason 'indeferido sem atuação' + transição aguardando_bap→indeferida + ResultadoEmitido"
    - "O indeferimento por BAP comunica ao Regin (via ResultadoEmitido → ComunicarResultadoRegin) e NÃO envia à SEFAZ (HU-134 RN-003 — EnviarViabilidadeSefaz ignora indeferida)"
    - "É idempotente (withoutOverlapping/onOneServer) e reprocessa casos falhos (RN-004); o seam BusinessDeadlineCalculator usa horas-calendário até a HU-137 (feriados) — parametrizável"
  artifacts:
    - path: "app/Console/Commands/ExpressoIndeferirSemBapCommand.php"
      provides: "Rotina dormente: indefere aguardando_bap vencidas (no-op em produção até o Regin)"
      contains: "expresso:indeferir-sem-bap"
    - path: "app/Services/Expresso/IndeferirSemBapService.php"
      provides: "Indeferimento por prazo BAP (decisão + transição + evento, sem SEFAZ)"
      contains: "indeferido sem atuação"
    - path: "app/Services/Expresso/BusinessDeadlineCalculator.php"
      provides: "Seam de prazo (horas-calendário hoje; dias úteis na HU-137)"
      contains: "class BusinessDeadlineCalculator"
  key_links:
    - from: "app/Services/Expresso/IndeferirSemBapService.php"
      to: "app/Events/ResultadoEmitido.php"
      via: "indefere → ResultadoEmitido (→ Regin audita; SEFAZ ignora indeferida)"
      pattern: "ResultadoEmitido::dispatch"
    - from: "app/Console/Commands/ExpressoIndeferirSemBapCommand.php"
      to: "app/Services/Expresso/BusinessDeadlineCalculator.php"
      via: "prazo expresso.bap.prazo_horas → bap_due_at vencido"
      pattern: "expresso.bap.prazo_horas"
---

<objective>
HU-134 (Wave 5) como rotina DORMENTE honesta: o comando/scheduler `expresso:indeferir-sem-bap` que indefere processos parados em `aguardando_bap` além do prazo administrável (`expresso.bap.prazo_horas`), com o seam `BusinessDeadlineCalculator` (horas-calendário até a HU-137 de feriados). A rotina, o parâmetro e o estado existem prontos — mas NADA entra em `aguardando_bap` hoje (o `BapRegistry` está indisponível, Fase 13), então em produção a varredura é no-op.

Purpose: refletir a regra operacional do SAPS sem fachada — o motor é real e testado AGORA (com seed de processo `aguardando_bap` vencido no teste), e liga sozinho quando o conector Regin (Fase 13) alimentar `bap_due_at`. O indeferimento por BAP comunica ao Regin e NÃO envia à SEFAZ (RN-003), reusando o `ResultadoEmitido` (os listeners da Wave 4 já tratam isso). Idempotente/reprocessável (RN-004).
Output: `BusinessDeadlineCalculator` (seam) + `IndeferirSemBapService` + comando `expresso:indeferir-sem-bap` (no scheduler) + state da factory `awaitingBap()`, cobertos por testes (indefere vencida com seed; no-op sem BAP; Regin sim/SEFAZ não).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-fluxo-expresso/09-CONTEXT.md
@docs/superpowers/specs/2026-06-14-fluxo-expresso-design.md
@.planning/phases/09-fluxo-expresso/09-01-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-02-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-03-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-05-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-08-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-09-SUMMARY.md

# PADRÕES A ESPELHAR (scheduler idempotente; transação+evento; factory state) — ler antes de codar:
@routes/console.php
@app/Services/Expresso/FluxoExpressoService.php
@app/Services/Solicitacao/ViabilityRequestStateMachine.php
@app/Events/ResultadoEmitido.php
@database/factories/ViabilityRequestFactory.php
@app/Support/Settings.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: BusinessDeadlineCalculator + IndeferirSemBapService + factory state awaitingBap</name>
  <read_first>app/Services/Expresso/FluxoExpressoService.php, app/Services/Solicitacao/ViabilityRequestStateMachine.php, database/factories/ViabilityRequestFactory.php</read_first>
  <files>app/Services/Expresso/BusinessDeadlineCalculator.php, app/Services/Expresso/IndeferirSemBapService.php, database/factories/ViabilityRequestFactory.php, tests/Feature/Expresso/IndeferirSemBapTest.php</files>
  <action>
    TDD estrito (RED primeiro).

    `BusinessDeadlineCalculator` (seam de prazo — RN-001):
    - `dueAt(\DateTimeInterface $from, int $hours): \Illuminate\Support\Carbon` — hoje retorna `Carbon::instance($from)->addHours($hours)` (horas-calendário). PHPDoc: a HU-137 (feriados/fins de semana) substituirá por dias úteis SEM tocar os call sites (seam). 
    - `isOverdue(\DateTimeInterface $dueAt, ?\DateTimeInterface $now = null): bool`.

    `IndeferirSemBapService` (reusa o caminho de emissão; espelha o FluxoExpressoService na transação):
    - `indeferir(ViabilityRequest $request): ViabilityDecision` — guarda status===AguardandoBap (senão no-op/exceção honesta); DB::transaction: cria ViabilityDecision (flow 'expresso', outcome Indeferida, consolidated_result 'nao_permitido' ou um marcador 'sem_atuacao_bap', reason 'indeferido sem atuação', per_cnae/rules_versions do que houver — pode ser vazio honesto, decided_by_user_id null, decided_at now); transição aguardando_bap→indeferida pela StateMachine; auditoria SÍNCRONA 'expresso'/'decisao' result 'indeferida' motivo BAP. Após commit: ResultadoEmitido::dispatch (→ Regin audita; SEFAZ ignora indeferida — Wave 4 já garante).
    - NÃO chama SEFAZ direto (RN-003) — o gateway SEFAZ é acionado só por deferimento via listener, que ignora indeferida.

    `ViabilityRequestFactory`: adicionar state `awaitingBap(?Carbon $dueAt = null)` — status AguardandoBap, protocol_number setado, bap_due_at = $dueAt ?? now()->subHours(72) (vencido por padrão para os testes), bap_linked_at null. (Escrita única do state aqui.)

    `IndeferirSemBapTest` (SQLite):
    - request awaitingBap vencida → indeferir(): cria ViabilityDecision outcome indeferida + reason 'indeferido sem atuação'; status indeferida; transição gravada; Event::fake assertDispatched(ResultadoEmitido).
    - BusinessDeadlineCalculator: dueAt(+48h) e isOverdue corretos.
    - guarda: indeferir() numa solicitação que NÃO está aguardando_bap → não cria decisão (no-op/exceção).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=IndeferirSemBapTest</verify>
  <acceptance_criteria>
    - grep "indeferido sem atuação" app/Services/Expresso/IndeferirSemBapService.php.
    - grep "addHours" app/Services/Expresso/BusinessDeadlineCalculator.php (seam horas-calendário) + PHPDoc citando HU-137.
    - grep "awaitingBap" database/factories/ViabilityRequestFactory.php.
    - IndeferirSemBapTest verde (indefere vencida + evento; seam de prazo; guarda de estado).
  </acceptance_criteria>
  <done>Indeferimento por prazo BAP real (decisão+transição+evento, sem SEFAZ) + seam de prazo + factory state; testado com seed de aguardando_bap.</done>
</task>

<task type="auto">
  <name>Task 2: comando expresso:indeferir-sem-bap + scheduler + no-op dormente + Regin/SEFAZ</name>
  <read_first>routes/console.php, app/Console/Commands/ExpressoReavaliarCommand.php (se já existir; senão SolicitacaoProtocolarCommand), app/Listeners/EnviarViabilidadeSefaz.php</read_first>
  <files>app/Console/Commands/ExpressoIndeferirSemBapCommand.php, routes/console.php, tests/Feature/Expresso/ExpressoIndeferirSemBapCommandTest.php</files>
  <action>
    TDD estrito.

    `ExpressoIndeferirSemBapCommand` (auto-descoberto; signature `expresso:indeferir-sem-bap`):
    - prazo = `Settings::get('expresso.bap.prazo_horas', config('sile.expresso.bap.prazo_horas', 48))`.
    - varre `status===aguardando_bap` cujo `bap_due_at` está vencido (BusinessDeadlineCalculator::isOverdue) — para cada uma chama IndeferirSemBapService::indeferir. Imprime quantas indeferiu.
    - NO-OP honesto: como nada coloca em aguardando_bap hoje (BapRegistry indisponível), em produção a varredura encontra ZERO → imprime "nenhum processo aguardando BAP vencido" e sai 0 (prova de dormência, não de fachada).

    Registrar no `routes/console.php`: `Schedule::command('expresso:indeferir-sem-bap')->hourly()->withoutOverlapping()->onOneServer();` (comentar: DORMENTE até o Regin alimentar bap_due_at — Fase 13; reprocessa falhos a cada hora, RN-004).

    `ExpressoIndeferirSemBapCommandTest` (SQLite):
    - NO-OP (dormente): sem nenhuma aguardando_bap (estado real de produção) → comando exit 0, mensagem de no-op, ZERO ViabilityDecision criada (prova de que nada é indeferido sem BAP de verdade).
    - caminho REAL (com seed awaitingBap vencida): cria 1 awaitingBap vencida + 1 awaitingBap dentro do prazo → comando indefere SÓ a vencida (1 decisão indeferida); a dentro do prazo permanece aguardando_bap.
    - integração Regin/SEFAZ: com Event NÃO fakeado (listeners reais auto-descobertos), após indeferir → Activity 'integracoes'/'regin-parecer' (bloqueado) presente E 'sefaz-viabilidade' result 'ignorado' (indeferimento não vai à SEFAZ — HU-134 RN-003). (Reusa os listeners da Wave 4.)
  </action>
  <verify>php artisan test --compact --filter=ExpressoIndeferirSemBapCommandTest && php artisan expresso:indeferir-sem-bap --help</verify>
  <acceptance_criteria>
    - grep "expresso:indeferir-sem-bap" no comando e em routes/console.php.
    - grep "withoutOverlapping" e "onOneServer" na agenda da rotina.
    - grep "expresso.bap.prazo_horas" no comando (prazo parametrizável).
    - ExpressoIndeferirSemBapCommandTest verde — INCLUINDO o no-op dormente (zero decisões sem BAP) e SEFAZ 'ignorado' no indeferimento.
  </acceptance_criteria>
  <done>Rotina dormente expresso:indeferir-sem-bap: no-op em produção (zero) e indefere vencidas quando houver seed; Regin comunicado, SEFAZ ignorado; idempotente/reprocessável.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-134 RN-001 — prazo 48h parametrizável (seam) | IndeferirSemBapTest (BusinessDeadlineCalculator) + comando lê expresso.bap.prazo_horas |
| HU-134 RN-002 — motivo 'indeferido sem atuação' | IndeferirSemBapTest (reason) |
| HU-134 RN-003 — Regin sim, SEFAZ não | ExpressoIndeferirSemBapCommandTest (regin 'bloqueado' + sefaz 'ignorado') |
| HU-134 RN-004 — reprocessa/idempotente | ExpressoIndeferirSemBapCommandTest (agenda withoutOverlapping; só vencidas) |
| Anti-fachada / dormência — nada em aguardando_bap hoje | ExpressoIndeferirSemBapCommandTest (no-op zero decisões) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="IndeferirSemBapTest|ExpressoIndeferirSemBapCommandTest"` verde.
- `php artisan expresso:indeferir-sem-bap` em dev → no-op honesto (zero aguardando_bap, pois nada vincula BAP hoje).
- Suíte completa CONTINUA verde — reusa decisão/estado/evento/listeners existentes.
</verification>

<success_criteria>
- HU-134 pronta porém DORMENTE: rotina+parâmetro+estado existem; no-op em produção até o Regin (Fase 13) alimentar bap_due_at — sem fachada.
- Indeferimento por BAP real (com seed): decisão+transição+evento; Regin comunicado, SEFAZ ignorado (RN-003); seam de prazo parametrizável.
- ZERO dependência nova; idempotente/reprocessável.
</success_criteria>

<output>
Criar `.planning/phases/09-fluxo-expresso/09-10-SUMMARY.md`: assinatura de IndeferirSemBapService::indeferir e de BusinessDeadlineCalculator, a agenda do expresso:indeferir-sem-bap, o state awaitingBap da factory e a prova de dormência (no-op). Registrar a pendência: ativação real depende do Regin (Fase 13) e a contagem em dias úteis depende da HU-137. Insumo do fechamento (09-12) e do STATE/ROADMAP (bloqueio honesto).
</output>

---
phase: 09-fluxo-expresso
plan: 11
type: execute
wave: 6
depends_on: ["09-05", "09-10"]
files_modified:
  - app/Http/Controllers/Gestao/ResultadoExpressoController.php
  - app/Http/Resources/ViabilityDecisionResource.php
  - routes/gestao.php
  - resources/js/pages/gestao/resultados-expresso/index.tsx
  - resources/js/pages/gestao/resultados-expresso/show.tsx
  - tests/Feature/Expresso/ResultadoExpressoConsoleTest.php
autonomous: true
requirements: [HU-076, HU-078]

must_haves:
  truths:
    - "A retaguarda tem uma tela de resultado expresso (lista + detalhe da ViabilityDecision) — o analista/gestor consulta as decisões automáticas; o cidadão acompanha via Regin (sem PDF)"
    - "A lista mostra as solicitações decididas (deferida/indeferida) com filtros (status/resultado, número, período) e paginação server-side; o detalhe mostra outcome, número TVL, consolidated_result, por_cnae, rules_versions, fundamentação, motivo e decided_at"
    - "O acesso é gated por consultar-solicitacoes (reuso — a decisão é parte da solicitação; sem permissão nova) e segue o padrão visual do console (TailAdmin: Table/Badge/PageBreadcrumb)"
    - "A tela é somente leitura (decisão imutável) — não há ação de re-decidir/editar na UI"
  artifacts:
    - path: "app/Http/Controllers/Gestao/ResultadoExpressoController.php"
      provides: "index (lista filtrável) + show (detalhe da decisão) da retaguarda"
      contains: "consultar-solicitacoes"
    - path: "resources/js/pages/gestao/resultados-expresso/show.tsx"
      provides: "Detalhe navegável da ViabilityDecision (fundamentação, por CNAE, versões, TVL)"
      contains: "ViabilityDecision"
  key_links:
    - from: "routes/gestao.php"
      to: "app/Http/Controllers/Gestao/ResultadoExpressoController.php"
      via: "Route::middleware('permission:consultar-solicitacoes') (reuso da permissão da Fase 8)"
      pattern: "consultar-solicitacoes"
    - from: "app/Http/Controllers/Gestao/ResultadoExpressoController.php"
      to: "app/Models/ViabilityDecision.php"
      via: "lista/detalha as decisões (com a solicitação) via Eloquent API Resource"
      pattern: "ViabilityDecision"
---

<objective>
UI de retaguarda (Wave 6): a tela do console SEDUR para consultar o resultado do fluxo expresso — lista filtrável das solicitações decididas (deferida/indeferida) e o detalhe da `ViabilityDecision` (outcome, número TVL, consolidado, por CNAE, versões de regra, fundamentação, motivo, data). Somente leitura — a decisão é imutável; o cidadão acompanha pelo Regin (sem PDF/TVL nesta fase; HU-132 é Fase 10).

Purpose: dar visibilidade auditável da decisão automática à SEDUR (insumo do PDF da Fase 10 e da explicabilidade da Fase 12). Reusa a permissão `consultar-solicitacoes` (a decisão é parte da solicitação — decisão de NÃO criar permissão nova, a validar com a SEDUR) e o design system do console (TailAdmin: Table/Badge/PageBreadcrumb/Pagination/EmptyState). Anti-fachada: a tela exibe SÓ o que existe na ViabilityDecision real (incl. o caso 'indeferido sem atuação' da HU-134).
Output: controller index/show + Eloquent API Resource + 2 páginas Inertia/React, gated por permissão, cobertos por feature tests (acesso/listagem/detalhe/Inertia props).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-fluxo-expresso/09-CONTEXT.md
@docs/superpowers/specs/2026-06-14-fluxo-expresso-design.md
@.planning/phases/09-fluxo-expresso/09-02-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-05-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-10-SUMMARY.md

# PADRÕES A ESPELHAR (controller+resource+rota gestão; página lista/detalhe TailAdmin) — ler antes de codar:
@routes/gestao.php
@app/Http/Controllers/Gestao/RiscoController.php
@resources/js/pages/gestao/risco/index.tsx
@resources/js/pages/gestao/tipos-servico/index.tsx
@app/Models/ViabilityDecision.php
@app/Models/ViabilityRequest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: rota + ResultadoExpressoController (index/show) + ViabilityDecisionResource</name>
  <read_first>routes/gestao.php, app/Http/Controllers/Gestao/RiscoController.php, app/Models/ViabilityDecision.php</read_first>
  <files>app/Http/Controllers/Gestao/ResultadoExpressoController.php, app/Http/Resources/ViabilityDecisionResource.php, routes/gestao.php, tests/Feature/Expresso/ResultadoExpressoConsoleTest.php</files>
  <action>
    TDD estrito (RED primeiro).

    Rotas em `routes/gestao.php`, sob `permission:consultar-solicitacoes` (REUSO — não criar permissão; comentar a decisão), prefixo `resultados-expresso`:
    - `GET resultados-expresso` → index (`gestao.resultados-expresso.index`).
    - `GET resultados-expresso/{viabilityRequest}` → show (`gestao.resultados-expresso.show`).

    `ResultadoExpressoController`:
    - index(Request): lista as solicitações COM decisão (whereHas('decision') OU joina viability_decisions), filtros por outcome (deferida/indeferida), número de protocolo/TVL e período (decided_at), ordenado por decided_at desc; paginação server-side (`solicitacao` ou `ui.*` per_page — reusar um parâmetro existente ou config). Retorna Inertia::render('gestao/resultados-expresso/index', [...]) com a coleção (Resource) + filtros + meta (from/to/total — padrão da casa).
    - show(ViabilityRequest): carrega a request + decision + cnaes; retorna Inertia::render('gestao/resultados-expresso/show', ['decisao' => ViabilityDecisionResource, ...]). 404 se não houver decisão.
    - SOMENTE LEITURA (sem store/update/destroy).

    `ViabilityDecisionResource` (Eloquent API Resource): expõe outcome (+label), consolidated_result (+label), tvl_product_number, per_cnae, rules_versions, fundamentacao, reason, decided_at, e dados da solicitação (protocolo, empresa, endereço resumido) — payload de leitura honesto (sem dados sensíveis desnecessários).

    `ResultadoExpressoConsoleTest` (SQLite):
    - sem permissão → 403 (usuário sem consultar-solicitacoes); com a permissão → 200.
    - index lista só decididas (uma deferida + uma indeferida via factory; uma protocolada sem decisão NÃO aparece); filtro por outcome funciona.
    - show de uma deferida retorna o detalhe com tvl_product_number; show de uma sem decisão → 404.
    - assertInertia component + props (decisao com outcome/per_cnae).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=ResultadoExpressoConsoleTest</verify>
  <acceptance_criteria>
    - grep "resultados-expresso" routes/gestao.php sob permission:consultar-solicitacoes.
    - grep "whereHas('decision')" (ou join) no controller — só decididas.
    - ResultadoExpressoConsoleTest verde (403/200, lista só decididas, filtro, show/404, Inertia props).
  </acceptance_criteria>
  <done>Rota+controller index/show (somente leitura) + Resource, gated por consultar-solicitacoes (reuso); só decididas, com filtros/paginação.</done>
</task>

<task type="auto">
  <name>Task 2: páginas Inertia/React (lista + detalhe) no padrão do console</name>
  <read_first>resources/js/pages/gestao/risco/index.tsx, resources/js/pages/gestao/tipos-servico/index.tsx</read_first>
  <files>resources/js/pages/gestao/resultados-expresso/index.tsx, resources/js/pages/gestao/resultados-expresso/show.tsx</files>
  <action>
    Seguir a skill inertia-react-development e o padrão TailAdmin do console (AppShell/PageBreadcrumb, componentes ui/Table, ui/Badge, ui/pagination, ui/empty-state). ZERO dependência nova.

    `index.tsx`: DataTable em card único (PageBreadcrumb "Resultados do fluxo expresso"); colunas: número de protocolo, empresa, resultado (Badge por outcome: deferida=success, indeferida=error), número TVL (quando houver), decidido em; filtros (resultado, busca por número, período) que fazem GET (router/Link preservando estado); paginação "Mostrando X–Y de Z"; empty-state distinguindo busca vazia de lista vazia; cada linha tem "ver detalhe" → show. Mobile-first/acessível.
    `show.tsx`: detalhe da decisão — cabeçalho com número de protocolo + Badge de outcome + número TVL; seções: resultado consolidado, decisão por CNAE (tendência + encaminhamento por atividade), fundamentação legal (lista), versões de regra (território/louos/risco), motivo (quando houver — ex.: 'indeferido sem atuação'), decidido em / por (sistema quando null). Nota explícita ao operador: "Canal oficial ao cidadão: Regin/SEFAZ. PDF/TVL é função da retaguarda na próxima fase (HU-132)." Somente leitura (sem botões de ação).

    Não criar teste novo de JS aqui (o teste Inertia do controller cobre props); garantir `npx tsc --noEmit` e `npm run build` verdes.
  </action>
  <verify>npx tsc --noEmit && npm run build</verify>
  <acceptance_criteria>
    - As páginas usam os componentes do console (Table/Badge/PageBreadcrumb/pagination/empty-state) — ZERO dependência nova.
    - grep "Regin" em show.tsx (nota de canal oficial + ressalva do PDF na Fase 10).
    - `npx tsc --noEmit` e `npm run build` verdes.
  </acceptance_criteria>
  <done>Lista filtrável + detalhe da decisão no padrão TailAdmin, somente leitura, com a ressalva honesta de canal/PDF; build/typecheck verdes.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-076 — retaguarda exibe dados consolidados da decisão | ResultadoExpressoConsoleTest (show com TVL/por_cnae) |
| HU-078 — decisão auditável consultável (regras/versões/resultado) | ResultadoExpressoConsoleTest (props rules_versions/fundamentacao) |
| HU-073/074/075 CA-04 — segurança de acesso | ResultadoExpressoConsoleTest (403 sem permissão; 200 com) |
| Imutabilidade — somente leitura | ResultadoExpressoConsoleTest (sem rotas store/update) + ausência de ações na UI |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=ResultadoExpressoConsoleTest` verde.
- `npx tsc --noEmit` e `npm run build` verdes.
- Suíte completa CONTINUA verde; permissões permanecem 19 (reuso de consultar-solicitacoes).
</verification>

<success_criteria>
- Tela de resultado expresso (lista filtrável + detalhe somente leitura) gated por consultar-solicitacoes, no padrão do console.
- Exibe a decisão real (outcome/TVL/por CNAE/versões/fundamentação/motivo) com a ressalva honesta de canal/PDF.
- ZERO dependência nova; sem permissão nova (decisão a validar com a SEDUR).
</success_criteria>

<output>
Criar `.planning/phases/09-fluxo-expresso/09-11-SUMMARY.md`: as rotas/nomes, a forma do ViabilityDecisionResource, as páginas (lista/detalhe) e a decisão de reuso de permissão (consultar-solicitacoes). Insumo do smoke navegável (09-12) e da Fase 10 (HU-132 emite o PDF a partir desta mesma fonte).
</output>

---
phase: 09-fluxo-expresso
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Solicitacao/SolicitacaoViabilityResolver.php
  - app/Services/Solicitacao/ResolvedViability.php
  - app/Services/Solicitacao/SimulacaoSolicitacaoService.php
  - tests/Feature/Expresso/SolicitacaoViabilityResolverTest.php
autonomous: true
requirements: [HU-073, HU-074, HU-075, HU-141]

must_haves:
  truths:
    - "Existe o SolicitacaoViabilityResolver que, dada uma ViabilityRequest, itera os CNAEs (principal+complementares) via ConsultaViabilidadeService::consultarPorPontoConhecido (centroide do polígono) e devolve um ResolvedViability (por_cnae[] + consolidado + rules_versions) SEM persistir"
    - "Cada item por_cnae carrega o ConsultaViabilidadeResult bruto — o consumidor lê numa passada o risco.encaminhamento.fluxo (elegibilidade HU-073) e o vereditoLocacional().resultado (decisão HU-074/075)"
    - "A consolidação é o pior caso (mais restritivo) entre os CNAEs — mesma severidade da Fase 8 (nao_permitido > pendente > permitido_com_condicoes > permitido); sem polígono degrada para a via CNAE (território null → pendente)"
    - "SimulacaoSolicitacaoService (Fase 8) passa a CONSUMIR o resolver e mantém seu comportamento (persiste snapshot orientativo + audita) — TODOS os testes da Fase 8 seguem verdes"
    - "O resolver NÃO decide nem persiste — é insumo puro; a decisão autoritativa (09-05) o reexecuta fresco, nunca confia no simulation_snapshot pré-protocolo"
  artifacts:
    - path: "app/Services/Solicitacao/SolicitacaoViabilityResolver.php"
      provides: "Resolução por-CNAE + consolidação reutilizável (sem persistir), insumo da simulação e da decisão"
      contains: "class SolicitacaoViabilityResolver"
    - path: "app/Services/Solicitacao/ResolvedViability.php"
      provides: "DTO readonly: por_cnae[] (com ConsultaViabilidadeResult), consolidado, rules_versions"
      contains: "final readonly class ResolvedViability"
  key_links:
    - from: "app/Services/Solicitacao/SolicitacaoViabilityResolver.php"
      to: "app/Services/Viabilidade/ConsultaViabilidadeService.php"
      via: "consultarPorPontoConhecido por CNAE (reusa o motor Fase 7; sem polígono → consultarPorCnae)"
      pattern: "consultarPorPontoConhecido"
    - from: "app/Services/Solicitacao/SimulacaoSolicitacaoService.php"
      to: "app/Services/Solicitacao/SolicitacaoViabilityResolver.php"
      via: "simulate() delega a iteração/consolidação ao resolver e só persiste o snapshot orientativo"
      pattern: "SolicitacaoViabilityResolver"
---

<objective>
Extrair de `SimulacaoSolicitacaoService::simulate` o núcleo reutilizável de resolução de viabilidade por solicitação: o `SolicitacaoViabilityResolver` (itera CNAEs, propaga o veredito do motor LOUOS por CNAE, consolida o pior caso, coleta as versões das regras) e o DTO `ResolvedViability`, SEM persistir. A simulação da Fase 8 passa a consumi-lo (orientativa, persiste snapshot — testes verdes); a decisão autoritativa (09-05) o reexecuta fresco.

Purpose: a decisão vinculante REEXECUTA os motores (entrada/regras/versões da época) — NÃO confia no `simulation_snapshot` pré-protocolo (orientativo, pode estar `markSimulationStale`). Para ter uma única fonte de iteração/consolidação (DRY, sem lógica de decisão paralela — RN-001) consumida tanto pela simulação quanto pela decisão, extraímos o resolver. Cada item por_cnae carrega o `ConsultaViabilidadeResult` para o consumidor ler numa passada o `risco.encaminhamento.fluxo` (elegibilidade) e o `vereditoLocacional().resultado` (decisão).
Output: `SolicitacaoViabilityResolver` + `ResolvedViability` (DTO), refactor mínimo do `SimulacaoSolicitacaoService` para delegar ao resolver, com a suíte da Fase 8 mantida verde.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-fluxo-expresso/09-CONTEXT.md
@docs/superpowers/specs/2026-06-14-fluxo-expresso-design.md

# A REFATORAR / REUSAR (extrair sem mudar comportamento; manter Fase 8 verde) — ler antes de codar:
@app/Services/Solicitacao/SimulacaoSolicitacaoService.php
@app/Services/Viabilidade/ConsultaViabilidadeService.php
@app/Services/Viabilidade/ConsultaViabilidadeResult.php
@app/Services/Risco/RiscoResult.php
@app/Enums/ResultadoViabilidade.php
@app/Enums/Fluxo.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: SolicitacaoViabilityResolver + ResolvedViability (extração, sem persistir)</name>
  <read_first>app/Services/Solicitacao/SimulacaoSolicitacaoService.php, app/Services/Viabilidade/ConsultaViabilidadeResult.php, app/Services/Risco/RiscoResult.php</read_first>
  <files>app/Services/Solicitacao/SolicitacaoViabilityResolver.php, app/Services/Solicitacao/ResolvedViability.php, tests/Feature/Expresso/SolicitacaoViabilityResolverTest.php</files>
  <action>
    TDD estrito (RED primeiro). Espelhar a lógica HOJE em SimulacaoSolicitacaoService (orderedCnaes, consolidar com a const SEVERIDADE, centroid) — extraída, não reinventada.

    `ResolvedViability` (DTO `final readonly`):
    - `por_cnae`: list<array> — cada item: `cnae`, `cnae_formatado`, `is_primary` (bool), `tendencia` (string = veredito), `tendencia_label`, `fluxo` (string = risco.encaminhamento.fluxo: 'expresso'|'analise'), `consulta` (o ConsultaViabilidadeResult — objeto, não só toArray, para a decisão ler fresco) e `consulta_array` (toArray para o snapshot).
    - `consolidado` (string — pior caso ResultadoViabilidade).
    - `rules_versions` (array — versões representativas).
    - `ponto` (array{lat,lng}|null), `area_m2` (float|null).
    - método `elegivelExpresso(): bool` — true SE todos os itens têm `fluxo === Fluxo::Expresso->value` (HU-073: qualquer 'analise' → inelegível). Sem CNAEs → false (nada a decidir).
    - método `toSnapshot(): array` — shape idêntico ao snapshot que a Fase 8 persiste hoje (`ponto`, `area_m2`, `por_cnae` com `consulta` = toArray), para o SimulacaoSolicitacaoService manter o mesmo registro.

    `SolicitacaoViabilityResolver`:
    - construtor: `private ConsultaViabilidadeService $consulta`.
    - `resolve(ViabilityRequest $request): ResolvedViability`:
      - centroide do property_polygon_geojson (mover o método centroid privado para cá) + área (used_area_m2).
      - orderedCnaes (principal primeiro, depois por código — mover de SimulacaoSolicitacaoService).
      - por CNAE: ponto null → `consultarPorCnae(code, area)`; senão `consultarPorPontoConhecido(lat,lng,code,area)`.
      - de cada result: `tendencia = $result->vereditoLocacional()['resultado']`; `fluxo = $result->risco->encaminhamento['fluxo'] ?? Fluxo::Analise->value`.
      - consolidar pior caso (mesma const SEVERIDADE da Fase 8).
      - rules_versions = versoes() do primeiro result (idênticas entre CNAEs do mesmo ponto/vigência).
      - NÃO persiste, NÃO audita — só resolve.

    `SolicitacaoViabilityResolverTest` (SQLite + fake SpatialRepository como nos testes dos motores — ver ConsultaViabilidade*Test): 
    - request com 1 CNAE expresso (risco baixo) sem zona → consolidado pendente; fluxo 'expresso'; elegivelExpresso() true (elegibilidade é por encaminhamento de risco, NÃO pelo veredito locacional).
    - request com CNAE de risco alto/gatilho → fluxo 'analise'; elegivelExpresso() false.
    - múltiplos CNAEs: um nao_permitido consolida nao_permitido (pior caso); todos permitido → permitido.
    - sem polígono → usa consultarPorCnae (território null → pendente), sem geocodificar.
    - rules_versions presentes (território/louos/risco).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=SolicitacaoViabilityResolverTest</verify>
  <acceptance_criteria>
    - grep "class SolicitacaoViabilityResolver" e "consultarPorPontoConhecido".
    - grep "final readonly class ResolvedViability" e "elegivelExpresso" + "toSnapshot".
    - SolicitacaoViabilityResolverTest verde (elegibilidade por encaminhamento; consolidação pior caso; sem polígono degrada; rules_versions).
  </acceptance_criteria>
  <done>Resolver + DTO ResolvedViability (por_cnae com ConsultaViabilidadeResult, consolidado, rules_versions, elegibilidade) — sem persistir nem decidir.</done>
</task>

<task type="auto">
  <name>Task 2: SimulacaoSolicitacaoService consome o resolver (Fase 8 verde)</name>
  <read_first>app/Services/Solicitacao/SimulacaoSolicitacaoService.php, tests/Feature/Solicitacao (os testes da simulação e do wizard que tocam simulate)</read_first>
  <files>app/Services/Solicitacao/SimulacaoSolicitacaoService.php</files>
  <action>
    Refatoração SEM mudar comportamento (skill receiving/refactor): `SimulacaoSolicitacaoService::simulate` passa a:
    - injetar `SolicitacaoViabilityResolver` (além do AuditService; pode manter ConsultaViabilidadeService se algo ainda usar, mas idealmente a iteração sai toda para o resolver).
    - chamar `$resolved = $this->resolver->resolve($request);`
    - montar o snapshot a partir de `$resolved->toSnapshot()` (mesmo shape de hoje), persistir `simulation_snapshot/simulation_rules_versions/simulation_resultado/simulated_at` e AUDITAR ('solicitacoes','simulacao') exatamente como hoje (RN-002/RN-003 — orientativa, não bloqueia).
    - devolver o mesmo array de retorno (resultado/resultado_label/por_cnae/rules_versions/simulated_at) para o wizard (08-13).
    - remover os métodos privados que foram movidos (centroid/orderedCnaes/consolidar/SEVERIDADE) — agora vivem no resolver.

    NÃO escrever teste novo aqui (a Fase 8 já cobre): RODAR os testes da Fase 8 da simulação/wizard e confirmar VERDES (verificação antes de completar). Se algum teste asseria um shape interno que mudou, ajustar o resolver para preservar o shape — NUNCA relaxar o teste.
  </action>
  <verify>php artisan test --compact --filter="Simulacao|Wizard|SolicitacaoSmoke|SolicitacaoGoldenCase"</verify>
  <acceptance_criteria>
    - grep "SolicitacaoViabilityResolver" app/Services/Solicitacao/SimulacaoSolicitacaoService.php.
    - grep -L "private function consolidar" SimulacaoSolicitacaoService.php (movido para o resolver) — a lógica não está duplicada.
    - Testes da simulação/wizard/smoke/golden da Fase 8 VERDES (comportamento preservado).
  </acceptance_criteria>
  <done>Simulação delega ao resolver e mantém snapshot orientativo + auditoria; suíte da Fase 8 verde (sem duplicar lógica).</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-073 RN-007/008 — elegibilidade por encaminhamento de risco | SolicitacaoViabilityResolverTest (expresso→elegível; analise→inelegível) |
| HU-074/075 RN-009 — consolidação pior caso por CNAE | SolicitacaoViabilityResolverTest (nao_permitido consolida; todos permitido) |
| HU-141 — simulação orientativa preservada (reuso) | testes da Fase 8 (Simulacao/Wizard/Smoke/Golden) verdes |
| RN-001 — sem lógica de decisão paralela / sem persistir | SolicitacaoViabilityResolverTest (resolver não persiste) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=SolicitacaoViabilityResolverTest` verde.
- `php artisan test --compact` — suíte completa VERDE, com ênfase nos testes da Fase 8 (simulação/wizard/smoke/golden) intactos (refator sem mudança de comportamento).
</verification>

<success_criteria>
- Resolver reutilizável (por_cnae com ConsultaViabilidadeResult, consolidação, rules_versions, elegibilidade) consumido pela simulação e, depois, pela decisão.
- SimulacaoSolicitacaoService mantém comportamento (orientativo) — Fase 8 verde, sem duplicação de lógica.
- ZERO dependência nova; sem fachada (a decisão autoritativa reexecutará fresco, nunca o snapshot).
</success_criteria>

<output>
Criar `.planning/phases/09-fluxo-expresso/09-04-SUMMARY.md`: assinatura de SolicitacaoViabilityResolver::resolve, o shape EXATO de ResolvedViability (por_cnae item, consolidado, rules_versions, elegivelExpresso, toSnapshot) e a confirmação de que a Fase 8 segue verde. Insumo direto de 09-05 (a decisão reexecuta o resolver fresco e lê elegibilidade + veredito por CNAE).
</output>

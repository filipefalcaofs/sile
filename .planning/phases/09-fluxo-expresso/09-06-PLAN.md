---
phase: 09-fluxo-expresso
plan: 06
type: execute
wave: 3
depends_on: ["09-05"]
files_modified:
  - app/Listeners/AvaliarFluxoExpresso.php
  - app/Jobs/DecidirFluxoExpressoJob.php
  - app/Console/Commands/ExpressoReavaliarCommand.php
  - routes/console.php
  - tests/Feature/Expresso/AvaliarFluxoExpressoListenerTest.php
  - tests/Feature/Expresso/DecidirFluxoExpressoJobTest.php
  - tests/Feature/Expresso/ExpressoReavaliarCommandTest.php
autonomous: true
requirements: [HU-073, HU-074, HU-075, HU-076]

must_haves:
  truths:
    - "O listener AUTO-DESCOBERTO AvaliarFluxoExpresso pendura no SolicitacaoProtocolada (Fase 8) e DESPACHA o DecidirFluxoExpressoJob — não decide inline (mantém o protocolo rápido) — registrado SÓ por auto-descoberta, NUNCA Event::listen (lição Fase 8)"
    - "DecidirFluxoExpressoJob (ShouldQueue, tries/timeout/backoff de config/sile.php) chama FluxoExpressoService::decide; falha esgotada vai para failed e audita (RN-002), nunca silenciosa"
    - "Exatamente UM job é despachado por protocolo (travado por CONTAGEM) — sem duplicação de decisão/auditoria"
    - "O scheduler expresso:reavaliar é rede de segurança: redecide solicitações protocoladas SEM decisão (órfãs); é no-op quando todas já decidiram; idempotente (withoutOverlapping/onOneServer)"
  artifacts:
    - path: "app/Listeners/AvaliarFluxoExpresso.php"
      provides: "Gatilho auto-descoberto que despacha o job de decisão no protocolo"
      contains: "DecidirFluxoExpressoJob::dispatch"
    - path: "app/Jobs/DecidirFluxoExpressoJob.php"
      provides: "Job resiliente (retry/backoff) que executa a decisão expressa"
      contains: "FluxoExpressoService"
    - path: "app/Console/Commands/ExpressoReavaliarCommand.php"
      provides: "Rede de segurança: reavalia protocoladas sem decisão"
      contains: "expresso:reavaliar"
  key_links:
    - from: "app/Listeners/AvaliarFluxoExpresso.php"
      to: "app/Events/SolicitacaoProtocolada.php"
      via: "handle(SolicitacaoProtocolada) auto-descoberto (sem Event::listen) → DecidirFluxoExpressoJob::dispatch"
      pattern: "SolicitacaoProtocolada"
    - from: "routes/console.php"
      to: "app/Console/Commands/ExpressoReavaliarCommand.php"
      via: "Schedule::command('expresso:reavaliar')->withoutOverlapping()->onOneServer()"
      pattern: "expresso:reavaliar"
---

<objective>
Gatilho e orquestração (Wave 3): o listener AUTO-DESCOBERTO `AvaliarFluxoExpresso` no evento `SolicitacaoProtocolada` (Fase 8) que DESPACHA o `DecidirFluxoExpressoJob` (fila, retry/backoff) — a decisão roda fora do request do protocolo (resiliência da Fase 3.1). O comando/scheduler `expresso:reavaliar` é a rede de segurança que redecide protocoladas órfãs (sem decisão).

Purpose: o protocolo (Fase 8) dispara o primeiro evento de domínio; a Fase 9 só ADICIONA um listener ao MESMO evento, sem tocar o protocolo. CRÍTICO (lição da Fase 8 — RN-002): listeners em app/Listeners são auto-descobertos; registrar também via Event::listen DUPLICA a execução (auditoria 2×) — o registro é único (auto-descoberta) e travamos por CONTAGEM. O scheduler (ativado na Fase 3.1) é rede de segurança idempotente para casos de falha (não o gatilho principal).
Output: listener + job + comando de scheduler `expresso:reavaliar` (registrado em routes/console.php), cobertos por feature tests (Bus/Queue fake; contagem do disparo; reavaliação de órfãs; no-op).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-fluxo-expresso/09-CONTEXT.md
@docs/superpowers/specs/2026-06-14-fluxo-expresso-design.md
@.planning/phases/09-fluxo-expresso/09-01-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-05-SUMMARY.md

# PADRÕES A ESPELHAR (listener auto-descoberto; job resiliente; scheduler; comando) — ler antes de codar:
@app/Listeners/RegistrarTrilhaProtocolo.php
@app/Events/SolicitacaoProtocolada.php
@app/Jobs/ImportRedesimJob.php
@routes/console.php
@app/Console/Commands/SolicitacaoProtocolarCommand.php
@app/Providers/AppServiceProvider.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: listener AvaliarFluxoExpresso (auto-descoberto) + DecidirFluxoExpressoJob</name>
  <read_first>app/Listeners/RegistrarTrilhaProtocolo.php, app/Jobs/ImportRedesimJob.php, app/Services/Expresso/FluxoExpressoService.php, app/Providers/AppServiceProvider.php</read_first>
  <files>app/Listeners/AvaliarFluxoExpresso.php, app/Jobs/DecidirFluxoExpressoJob.php, tests/Feature/Expresso/AvaliarFluxoExpressoListenerTest.php, tests/Feature/Expresso/DecidirFluxoExpressoJobTest.php</files>
  <action>
    TDD estrito (RED primeiro).

    `AvaliarFluxoExpresso` (app/Listeners — AUTO-DESCOBERTO; type-hint do evento no handle; NÃO registrar em Event::listen/AppServiceProvider):
    - `handle(SolicitacaoProtocolada $event): void` → `DecidirFluxoExpressoJob::dispatch($event->request->id);` (despachar pelo ID, não o model, para serialização segura). Despachar na fila parametrizada `config('sile.expresso.fila')` se != default (`->onQueue(...)`).
    - PHPDoc: gatilho principal da decisão; despacha (não decide inline) para manter o protocolo rápido e ganhar resiliência; auto-descoberta é o registro único (lição Fase 8 — Event::listen duplicaria).

    `DecidirFluxoExpressoJob` (ShouldQueue, espelha ImportRedesimJob):
    - `public int $tries`/`timeout`/`array $backoff` lidos de config/sile.php (expresso.job.*) — ou propriedades com default e método que lê config; manter simples (propriedades inicializadas a partir de config no construtor OU constantes que espelham o config — preferir ler config no handle/retryUntil para refletir parametrização técnica).
    - construtor: `public readonly int $viabilityRequestId`.
    - `handle(FluxoExpressoService $service): void` → carrega a ViabilityRequest pelo id; se não existir mais ou não estiver protocolada, retorna (idempotente — o service também re-checa sob lock); senão `$service->decide($request)`.
    - `failed(?Throwable $e)`: audita 'expresso'/'decisao-falha' result 'falha' com o id + erro (RN-002 — nunca silenciosa), espelhando ImportRedesimJob::failed.

    Testes:
    - `AvaliarFluxoExpressoListenerTest` (SQLite): Bus::fake; protocolar uma solicitação (ou disparar SolicitacaoProtocolada) → assertDispatched(DecidirFluxoExpressoJob) exatamente 1× (CONTAGEM — trava a não-duplicação por auto-descoberta vs Event::listen). Asserir que o job carrega o id correto.
    - `DecidirFluxoExpressoJobTest` (SQLite): instanciar o job e handle() com um FluxoExpressoService real (ou mock) → chama decide() na solicitação protocolada; solicitação já decidida → no-op (não chama decide / chama e o service re-checa). Testar failed() audita 'falha'.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="AvaliarFluxoExpressoListenerTest|DecidirFluxoExpressoJobTest"</verify>
  <acceptance_criteria>
    - grep "DecidirFluxoExpressoJob::dispatch" app/Listeners/AvaliarFluxoExpresso.php.
    - grep -L "AvaliarFluxoExpresso" app/Providers/AppServiceProvider.php (NÃO registrado via Event::listen — auto-descoberta).
    - grep "ShouldQueue" e "FluxoExpressoService" em app/Jobs/DecidirFluxoExpressoJob.php.
    - AvaliarFluxoExpressoListenerTest verde com a asserção por CONTAGEM (1 job por protocolo).
    - DecidirFluxoExpressoJobTest verde (decide + no-op + failed audita).
  </acceptance_criteria>
  <done>Gatilho auto-descoberto despacha 1 job por protocolo (sem duplicar); job resiliente decide e audita falha — sem Event::listen.</done>
</task>

<task type="auto">
  <name>Task 2: comando + scheduler expresso:reavaliar (rede de segurança idempotente)</name>
  <read_first>routes/console.php, app/Console/Commands/SolicitacaoProtocolarCommand.php</read_first>
  <files>app/Console/Commands/ExpressoReavaliarCommand.php, routes/console.php, tests/Feature/Expresso/ExpressoReavaliarCommandTest.php</files>
  <action>
    TDD estrito.

    `ExpressoReavaliarCommand` (auto-descoberto; signature `expresso:reavaliar {--limit=}`):
    - busca solicitações `status === protocolada` SEM ViabilityDecision (whereDoesntHave('decision')) — as "órfãs" que o gatilho não decidiu (falha/perda).
    - para cada uma, `DecidirFluxoExpressoJob::dispatch($id)` (reusa o caminho real; não decide inline no comando) — ou chama o service direto se preferir evidência imediata; preferir despachar o job (consistência com o gatilho). Imprime quantas foram reenfileiradas.
    - no-op honesto: zero órfãs → imprime "nenhuma solicitação pendente de decisão" e sai 0.

    Registrar no `routes/console.php` (espelhar a rotina de retenção existente):
    `Schedule::command('expresso:reavaliar')->everyTenMinutes()->withoutOverlapping()->onOneServer();` (cadência técnica — comentar que é rede de segurança, não gatilho principal).

    `ExpressoReavaliarCommandTest` (SQLite): Bus::fake; criar 2 protocoladas sem decisão + 1 já decidida (com ViabilityDecision via factory) → comando despacha 2 jobs (assertDispatchedTimes 2), não toca a já decidida; cenário sem órfãs → 0 jobs, exit 0, mensagem de no-op.
  </action>
  <verify>php artisan test --compact --filter=ExpressoReavaliarCommandTest && php artisan expresso:reavaliar --help</verify>
  <acceptance_criteria>
    - grep "expresso:reavaliar" app/Console/Commands/ExpressoReavaliarCommand.php e routes/console.php.
    - grep "withoutOverlapping" e "onOneServer" na agenda do expresso:reavaliar.
    - grep "whereDoesntHave('decision')" (ou equivalente) no comando — só órfãs.
    - ExpressoReavaliarCommandTest verde (reenfileira órfãs; no-op sem órfãs).
  </acceptance_criteria>
  <done>Rede de segurança expresso:reavaliar redecide protocoladas órfãs e é no-op idempotente; agendada com withoutOverlapping/onOneServer.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-073/074/075 — decisão acionada no protocolo (gatilho) | AvaliarFluxoExpressoListenerTest (1 job por protocolo) |
| RN-002 (lição Fase 8) — sem duplicação de auditoria | AvaliarFluxoExpressoListenerTest (CONTAGEM) + grep sem Event::listen |
| HU-076 FA-03 — resiliência/retentativa e falha auditada | DecidirFluxoExpressoJobTest (failed audita 'falha') |
| HU-134 RN-004 / robustez — reprocessar órfãs | ExpressoReavaliarCommandTest (reenfileira órfãs; no-op) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="AvaliarFluxoExpressoListenerTest|DecidirFluxoExpressoJobTest|ExpressoReavaliarCommandTest"` verde.
- `php artisan event:list` (ou inspeção) mostra AvaliarFluxoExpresso ligado a SolicitacaoProtocolada por auto-descoberta; AppServiceProvider sem Event::listen para ele.
- Suíte completa CONTINUA verde — o protocolo da Fase 8 NÃO foi alterado (só um listener novo no mesmo evento).
</verification>

<success_criteria>
- Decisão acionada de forma desacoplada e resiliente: 1 job por protocolo (auto-descoberta, sem duplicar), retry/backoff parametrizados, falha auditada.
- Rede de segurança expresso:reavaliar idempotente (withoutOverlapping/onOneServer) redecide órfãs.
- ZERO dependência nova; protocolo da Fase 8 intacto.
</success_criteria>

<output>
Criar `.planning/phases/09-fluxo-expresso/09-06-SUMMARY.md`: o gatilho (listener auto-descoberto → job), a assinatura/propriedades do DecidirFluxoExpressoJob (tries/backoff de config), a agenda do expresso:reavaliar (cadência/withoutOverlapping/onOneServer) e a confirmação de que NÃO há Event::listen. Insumo do fechamento (09-12 smoke do fluxo automático protocolar→decidir).
</output>

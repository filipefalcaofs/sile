---
phase: 09-fluxo-expresso
plan: 12
type: execute
wave: 7
depends_on: ["09-05", "09-06", "09-07", "09-08", "09-09", "09-10", "09-11"]
files_modified:
  - database/seeders/ExpressoDevSeeder.php
  - database/seeders/ZonaFicticiaDevSeeder.php
  - database/seeders/DatabaseSeeder.php
  - app/Console/Commands/ExpressoDecidirCommand.php
  - tests/Feature/Expresso/ExpressoGoldenCaseTest.php
  - tests/Feature/Expresso/ExpressoSmokeTest.php
  - tests/Fixtures/golden/expresso/*.json
  - tests/Feature/Seeders/DatabaseSeederTest.php
autonomous: false
requirements: [HU-073, HU-074, HU-075, HU-076, HU-077, HU-078, HU-134]

must_haves:
  truths:
    - "Há um seed de ZONA FICTÍCIA SÓ dev/teste (claramente rotulado) que faz a LOUOS Quadro 10 resolver sobre uma área de Salvador, permitindo demonstrar um DEFERIMENTO navegável — em produção, sem zona oficial, a degradação honesta (em_analise) permanece"
    - "Há um comando expresso:decidir {solicitacao} que dá EVIDÊNCIA REAL da decisão (roda FluxoExpressoService e imprime status/decisão/TVL) — reprocesso/evidência manual"
    - "Golden/smoke travam a regressão do fluxo automático: protocolar → (gatilho) decidir → deferida (com zona fictícia) | em_analise (sem zona, sem evento) | indeferida (CNAE não permitido); a auditoria síncrona e os efeitos (notifica/Regin pendência/SEFAZ só deferida) são verificados"
    - "Verificação integral FRESCA: pint + suíte SQLite (baseline 848 + novos) + @group postgis (com container) + tsc + build, todos verdes, com as contagens atualizadas (parâmetros 56)"
    - "Smoke navegável aprovado (checkpoint humano): protocolar sobre a zona fictícia → deferimento automático visível na retaguarda (com TVL) + notificação; protocolar sem zona → em_analise honesto (sem decisão/evento)"
  artifacts:
    - path: "database/seeders/ZonaFicticiaDevSeeder.php"
      provides: "Zona urbanística fictícia SÓ dev/teste para demonstrar um deferimento navegável (não afeta produção)"
      contains: "ficticia"
    - path: "app/Console/Commands/ExpressoDecidirCommand.php"
      provides: "Evidência real da decisão: roda o motor e imprime status/decisão/TVL"
      contains: "expresso:decidir"
    - path: "tests/Feature/Expresso/ExpressoSmokeTest.php"
      provides: "Smoke do fluxo automático protocolar→decidir (deferida/em_analise/indeferida) com efeitos"
      contains: "protocol"
  key_links:
    - from: "database/seeders/DatabaseSeeder.php"
      to: "database/seeders/ExpressoDevSeeder.php"
      via: "seeds da fase 9 registrados (zona fictícia + exemplos decididos) no seed de dev"
      pattern: "ExpressoDevSeeder"
    - from: "app/Console/Commands/ExpressoDecidirCommand.php"
      to: "app/Services/Expresso/FluxoExpressoService.php"
      via: "decide(request) e imprime o resultado real"
      pattern: "FluxoExpressoService"
---

<objective>
Fechamento da Fase 9 (Wave 7): o seed de DESENVOLVIMENTO (incl. uma ZONA FICTÍCIA só dev/teste para tornar um deferimento navegável), o comando de evidência `expresso:decidir`, os golden/smoke do fluxo automático de ponta a ponta, e a VERIFICAÇÃO INTEGRAL FRESCA — encerrando com o smoke navegável (checkpoint humano).

Purpose: critério de pronto do ROADMAP com EVIDÊNCIA FRESCA (verificação antes de completar). O motor de decisão é real; em produção, sem a zona oficial (Quadro 10/SEDUR), a maioria cai honestamente em `em_analise`. Para DEMONSTRAR o core value (um deferimento navegável) sem fachada, adicionamos uma zona fictícia EXCLUSIVA de dev/teste (dados fictícios, lógica real — entrega-funcional): muda só a carga, nunca o comportamento; em produção a degradação honesta permanece. Golden/smoke provam os três caminhos (deferida/em_analise/indeferida) e os efeitos.
Output: 2 seeders dev + comando expresso:decidir + golden/smoke + fixtures + verificação integral + checkpoint humano. Atualiza STATE.md e ROADMAP no fechamento.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-fluxo-expresso/09-CONTEXT.md
@docs/superpowers/specs/2026-06-14-fluxo-expresso-design.md
@.planning/phases/09-fluxo-expresso/09-01-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-02-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-05-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-06-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-10-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-11-SUMMARY.md

# PADRÕES A ESPELHAR (seeds dev; comando de evidência; golden/smoke; verificação) — ler antes de codar:
@database/seeders/DatabaseSeeder.php
@database/seeders/SolicitacaoDevSeeder.php
@database/seeders/GeoLayerSeeder.php
@database/seeders/LouosQuadro10Seeder.php
@app/Console/Commands/SolicitacaoProtocolarCommand.php
@tests/Feature/Solicitacao/SolicitacaoGoldenCaseTest.php
@tests/Feature/Solicitacao/SolicitacaoSmokeTest.php
@.planning/phases/08-solicitacao-de-viabilidade/08-16-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: seed de zona fictícia (SÓ dev/teste) + ExpressoDevSeeder + comando expresso:decidir</name>
  <read_first>database/seeders/GeoLayerSeeder.php, database/seeders/LouosQuadro10Seeder.php, database/seeders/SolicitacaoDevSeeder.php, app/Console/Commands/SolicitacaoProtocolarCommand.php</read_first>
  <files>database/seeders/ZonaFicticiaDevSeeder.php, database/seeders/ExpressoDevSeeder.php, database/seeders/DatabaseSeeder.php, app/Console/Commands/ExpressoDecidirCommand.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <action>
    TDD onde houver lógica; seeders cobertos pelo DatabaseSeederTest.

    `ZonaFicticiaDevSeeder` (DEV/TESTE — NUNCA produção real): cria uma geo_feature de ZONA (a camada/atributo que a LOUOS Quadro 10 lê — ver GeoLayerSeeder/LouosQuadro10Seeder) cobrindo um polígono de Salvador, com NOME/descrição EXPLÍCITOS de fictício (ex.: zona 'ZPR-FICTICIA-DEV' / "Zona fictícia de demonstração — substituir pela base oficial SEDUR"). Idempotente. PHPDoc deixando claro: existe SÓ para tornar um deferimento navegável em dev; em produção, sem a zona oficial, o motor degrada honesto para em_analise — muda a carga, não a lógica (entrega-funcional). Requer PostGIS p/ a geometria (espelhar GeoLayerSeeder driver-aware); em SQLite o seed da zona não roda a geometria (consistente com a decisão da Fase 8 de não simular no seed SQLite).

    `ExpressoDevSeeder`: a partir de solicitações de exemplo do cidadão dev (reusar/estender o SolicitacaoDevSeeder) DENTRO da zona fictícia + CNAE de risco baixo, protocola (ProtocolarSolicitacaoService) e roda a decisão real (FluxoExpressoService::decide) para gerar um DEFERIMENTO de exemplo (com número TVL/timeline/auditoria reais); e um exemplo SEM zona → em_analise. Dados fictícios, lógica real. Idempotente. Registrar no `DatabaseSeeder` (após GeoLayerSeeder/SolicitacaoDevSeeder; ZonaFicticiaDevSeeder antes do ExpressoDevSeeder).

    `ExpressoDecidirCommand` (auto-descoberto; signature `expresso:decidir {solicitacao}`): carrega a solicitação por id, roda FluxoExpressoService::decide e imprime status final + outcome + número TVL (se deferida) + motivo (se em_analise/indeferida). Útil para evidência/reprocesso manual; exit 0 e mensagem honesta quando cai em em_analise (não é erro).

    `DatabaseSeederTest`: acrescentar asserções — a zona fictícia é criada (em ambiente pgsql/postgis; em SQLite, pular a parte geométrica como na Fase 8) e os exemplos do expresso existem (um deferido com ViabilityDecision+TVL, um em_analise) com idempotência (contagens estáveis no re-seed). Confirmar a contagem de parâmetros 56 (já ajustada em 09-01).
  </action>
  <verify>php artisan test --compact --filter=DatabaseSeederTest && php artisan expresso:decidir --help</verify>
  <acceptance_criteria>
    - grep "ExpressoDevSeeder" e "ZonaFicticiaDevSeeder" em database/seeders/DatabaseSeeder.php.
    - grep "ficticia" (rótulo explícito) em database/seeders/ZonaFicticiaDevSeeder.php + PHPDoc de dev/teste.
    - grep "expresso:decidir" em app/Console/Commands/ExpressoDecidirCommand.php.
    - DatabaseSeederTest verde (zona fictícia + exemplos decididos; idempotência; parâmetros 56).
  </acceptance_criteria>
  <done>Zona fictícia dev/teste (rotulada, idempotente) + exemplos decididos via serviços reais + comando expresso:decidir; produção mantém a degradação honesta.</done>
</task>

<task type="auto">
  <name>Task 2: golden + smoke do fluxo automático (deferida / em_analise sem evento / indeferida)</name>
  <read_first>tests/Feature/Solicitacao/SolicitacaoSmokeTest.php, tests/Feature/Solicitacao/SolicitacaoGoldenCaseTest.php, app/Services/Expresso/FluxoExpressoService.php</read_first>
  <files>tests/Feature/Expresso/ExpressoGoldenCaseTest.php, tests/Feature/Expresso/ExpressoSmokeTest.php, tests/Fixtures/golden/expresso/*.json</files>
  <action>
    TDD/golden (#[DataProvider], padrão Fases 5/6/7/8).

    `ExpressoSmokeTest` (escolher SQLite com fake SpatialRepository para os caminhos sem geometria; usar @group postgis para o caminho que exige zona REAL/PostGIS — ou injetar um SpatialRepository fake que devolve a zona, espelhando os testes dos motores): dirige o fluxo AUTOMÁTICO de ponta a ponta pelos serviços reais —
    - protocolar (ProtocolarSolicitacaoService) → o gatilho/DecidirFluxoExpressoJob (ou chamar FluxoExpressoService::decide direto, documentando) →
    - caso DEFERIDA (zona presente, CNAE baixo, todos permitido): status deferida + ViabilityDecision + número TVL + ResultadoEmitido + auditoria síncrona + notificação enviada (Notification::fake) + Regin pendência auditada + SEFAZ pendência auditada (só deferida).
    - caso EM_ANALISE (sem zona — CRÍTICO anti-fachada): status em_analise, NENHUMA ViabilityDecision, ResultadoEmitido NÃO disparado (Event::fake assertNotDispatched), nenhuma notificação.
    - caso INDEFERIDA (CNAE/área que consolida nao_permitido com zona): status indeferida + decisão sem TVL + ResultadoEmitido + Regin pendência + SEFAZ ignorado.

    `ExpressoGoldenCaseTest` (#[DataProvider] + fixtures JSON em tests/Fixtures/golden/expresso/): casos-âncora entrada→esperado executando o motor real sobre o seed (CNAE baixo + zona fictícia → deferida+TVL; sem zona → em_analise sem evento; CNAE não permitido → indeferida). Espelhar o estilo dos golden das fases anteriores; travar a regressão de domínio.
  </action>
  <verify>php artisan test --compact --filter="ExpressoSmokeTest|ExpressoGoldenCaseTest" && php artisan test --compact --group=postgis --filter="ExpressoSmokeTest|ExpressoGoldenCaseTest"</verify>
  <acceptance_criteria>
    - grep "assertNotDispatched" (ou equivalente) no caso em_analise sem zona (anti-fachada).
    - grep "DataProvider" em tests/Feature/Expresso/ExpressoGoldenCaseTest.php + fixtures JSON presentes.
    - ExpressoSmokeTest e ExpressoGoldenCaseTest verdes (deferida/em_analise/indeferida + efeitos), incl. @group postgis onde exige zona real.
  </acceptance_criteria>
  <done>Golden/smoke travam o fluxo automático real e a degradação honesta (sem zona → em_analise sem evento); efeitos verificados.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: verificação integral fresca + smoke navegável (deferimento via zona fictícia + em_analise honesto)</name>
  <what-built>
    A Fase 9 completa: parâmetros HU-014; schema da decisão (viability_decisions/tvl_sequences/colunas BAP) + DecisionOutcome + ViabilityDecision + TvlNumberGenerator + transições da StateMachine; contratos Regin/Sefaz/Bap bloqueados + ResultadoEmitido; SolicitacaoViabilityResolver (extração); FluxoExpressoService (elegibilidade + defere/indefere + lock + auditoria síncrona + TVL + evento); gatilho (listener auto-descoberto + job + expresso:reavaliar); efeitos (notificação HU-077 + Regin/SEFAZ pendência); HU-134 BAP dormente; UI de resultado na retaguarda. Reuso real das Fases 5/6/7/8.
  </what-built>
  <how-to-verify>
    EVIDÊNCIA FRESCA — rodar e LER o output inteiro antes de afirmar (regra de verificação antes de completar):
    1. `vendor/bin/pint --dirty --format agent` — sem pendências.
    2. `php artisan test --compact` — suíte SQLite verde (baseline 848 da Fase 8 + os novos da fase; Seeders agora 56 parâmetros / 19 permissões). Registrar o total final no SUMMARY.
    3. `php artisan test --compact --group=postgis` com o container PostGIS de pé — verde (19 da Fase 8 + os novos: TVL concorrência, idempotência da decisão, smoke/golden com zona real). Registrar o total.
    4. `npx tsc --noEmit` e `npm run build` — verdes.
    5. Evidência do comando: `php artisan db:seed` (popula a zona fictícia + exemplos) e `php artisan expresso:decidir <id de uma solicitação protocolada sobre a zona fictícia>` — imprime status deferida + número TVL; e `expresso:decidir <id sem zona>` → em_analise honesto (sem decisão). Colar o output no SUMMARY.
    6. SMOKE NAVEGÁVEL (`composer dev`):
       a. DEFERIMENTO NAVEGÁVEL (dev): logar como cidadão, protocolar uma solicitação sobre a ZONA FICTÍCIA (CNAE de risco baixo) → o gatilho decide automaticamente → conferir na retaguarda (consultar-solicitacoes) a tela de Resultado do fluxo expresso: a decisão DEFERIDA com número TVL, decisão por CNAE, fundamentação e versões; conferir a notificação por e-mail (log/mailtrap) SEM anexo; conferir na trilha as pendências Regin ('bloqueado') e SEFAZ ('bloqueado'). Mobile (375px) + dark mode.
       b. EM_ANALISE HONESTO (sem zona — CRÍTICO): protocolar uma solicitação FORA da zona fictícia → conferir que vai para em_analise (NÃO deferida/indeferida), SEM ViabilityDecision e SEM e-mail de resultado — a degradação honesta visível.
       c. (Opcional) BAP dormente: `php artisan expresso:indeferir-sem-bap` → no-op (zero), provando a dormência.
    7. Auditoria: `php artisan tinker --execute 'echo App\Models\Activity::where("log_name","expresso")->count();'` > 0 (decisões auditadas síncronas).
    8. Registrar no SUMMARY/STATE/ROADMAP as PENDÊNCIAS/bloqueios que permanecem (sem fachada): transmissão Regin (HU-104) e SEFAZ (HU-110) → Fase 13; HU-134 ativa depende do Regin → Fase 13; TVL PDF (HU-132) → Fase 10; zona oficial (Quadro 10/SEDUR) — sem ela a maioria cai em em_analise honestamente; HU-137 (feriados) para o prazo BAP em dias úteis; canais plenos de notificação → EP11; política do "médio risco" e numeração oficial do TVL (SEDUR).
  </how-to-verify>
  <resume-signal>Responder "aprovado" (anexando outputs/prints do deferimento navegável + em_analise honesto) ou descrever divergências para correção antes de fechar a fase.</resume-signal>
</task>

</tasks>

## Mapa CA → teste (fechamento integral)
| HU / RN | Teste / evidência |
|---|---|
| HU-073/074/075 — três caminhos reais (deferida/em_analise/indeferida) | ExpressoSmokeTest + ExpressoGoldenCaseTest |
| HU-076 RN-007 — TVL no deferimento | ExpressoSmokeTest (deferida) + expresso:decidir |
| HU-077 — notificação ao deferir | ExpressoSmokeTest (Notification::fake) + smoke navegável |
| HU-078 — auditoria síncrona consultável | tinker log_name 'expresso' > 0 + UI de resultado |
| HU-104/110 — pendência Regin/SEFAZ honesta (deferida; SEFAZ ignora indeferida) | ExpressoSmokeTest (auditorias 'bloqueado'/'ignorado') |
| HU-134 — dormente (no-op) | expresso:indeferir-sem-bap (no-op) |
| Anti-fachada — sem zona → em_analise sem evento | ExpressoSmokeTest (assertNotDispatched) + smoke navegável (b) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact` (SQLite) verde; `--group=postgis` verde (container de pé).
- `npx tsc --noEmit` e `npm run build` verdes.
- Comando expresso:decidir dá evidência real (deferida com TVL / em_analise honesto); smoke navegável aprovado (deferimento via zona fictícia + em_analise sem zona).
</verification>

<success_criteria>
- Critérios 1, 2, 5 e 6 do ROADMAP validados com evidência fresca; critério 3 (Regin/SEFAZ) e 4 (BAP) ficam com a DECISÃO/auditoria reais e a transmissão/ativação BLOQUEADAS → Fase 13 (registrado, não simulado); TVL PDF → Fase 10.
- Zona fictícia SÓ dev/teste demonstra o deferimento navegável sem fachada; produção mantém a degradação honesta (em_analise sem zona).
- ZERO dependência nova; pendências SEDUR/bloqueios registrados em STATE/ROADMAP.
</success_criteria>

<output>
Criar `.planning/phases/09-fluxo-expresso/09-12-SUMMARY.md`: total final da suíte (SQLite + postgis), output do expresso:decidir (deferida+TVL e em_analise), os seeds (zona fictícia + exemplos), golden/smoke criados, o registro do smoke navegável aprovado e a lista explícita das pendências bloqueadas/adiadas (Regin/SEFAZ HU-104/110 → Fase 13, HU-134 ativa → Fase 13, TVL PDF HU-132 → Fase 10, zona oficial Quadro 10/SEDUR, HU-137 feriados, canais EP11, médio risco/numeração TVL SEDUR). Atualizar STATE.md e ROADMAP.md no fechamento da fase.
</output>

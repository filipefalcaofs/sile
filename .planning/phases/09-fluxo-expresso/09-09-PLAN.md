---
phase: 09-fluxo-expresso
plan: 09
type: execute
wave: 4
depends_on: ["09-03", "09-05"]
files_modified:
  - app/Listeners/EnviarViabilidadeSefaz.php
  - tests/Feature/Expresso/EnviarViabilidadeSefazListenerTest.php
autonomous: true
requirements: [HU-076, HU-110, HU-134]

must_haves:
  truths:
    - "O listener AUTO-DESCOBERTO EnviarViabilidadeSefaz pendura no ResultadoEmitido e SÓ age quando a decisão é DEFERIDA (indeferimento NÃO envia à SEFAZ — HU-134 RN-003)"
    - "Como o contrato está BLOQUEADO (UnavailableSefazViabilidadeGateway lança SefazUnavailableException), o listener CAPTURA e AUDITA pendência (log 'integracoes', result 'bloqueado') — NUNCA finge envio (anti-fachada)"
    - "Indeferimento é ignorado de forma auditável (sem tentar enviar) — registro claro de que SEFAZ não se aplica"
    - "Registro único por auto-descoberta (NÃO Event::listen) — sem duplicar (lição Fase 8)"
  artifacts:
    - path: "app/Listeners/EnviarViabilidadeSefaz.php"
      provides: "Listener que envia a viabilidade à SEFAZ só no deferimento; bloqueado → audita pendência (HU-110)"
      contains: "SefazUnavailableException"
  key_links:
    - from: "app/Listeners/EnviarViabilidadeSefaz.php"
      to: "app/Services/Sefaz/SefazViabilidadeGateway.php"
      via: "sendViabilidade só se decision->isDeferida(); try/catch SefazUnavailableException → audita 'bloqueado'"
      pattern: "sendViabilidade"
    - from: "app/Listeners/EnviarViabilidadeSefaz.php"
      to: "app/Models/ViabilityDecision.php"
      via: "guarda isDeferida() — indeferida não envia (HU-134 RN-003)"
      pattern: "isDeferida"
---

<objective>
Efeito desacoplado HU-110 (Wave 4, track ‖): o listener auto-descoberto `EnviarViabilidadeSefaz` consome o `ResultadoEmitido` e, SÓ quando a decisão é DEFERIDA, envia os dados da viabilidade à SEFAZ municipal via o contrato `SefazViabilidadeGateway`. Bloqueado (Fase 13) → audita pendência; indeferimento → não envia (HU-134 RN-003), de forma auditável.

Purpose: o canal oficial de dados ao município (SEFAZ) só faz sentido no deferimento. A transmissão está bloqueada (sem contrato/homologação) → degrada honesto (entrega-funcional): a abstração existe, a Fase 13 liga o binding; nunca adaptador falso. A guarda "só deferida" é regra de negócio (HU-110/HU-134 RN-003) testada agora.
Output: `EnviarViabilidadeSefaz` (listener auto-descoberto) cobre deferida-bloqueada (audita pendência), deferida-sucesso (fake) e indeferida (ignora, auditado).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-fluxo-expresso/09-CONTEXT.md
@docs/superpowers/specs/2026-06-14-fluxo-expresso-design.md
@.planning/phases/09-fluxo-expresso/09-03-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-05-SUMMARY.md

# PADRÕES A ESPELHAR (listener auto-descoberto; contrato bloqueado + fake; auditoria de pendência) — ler antes de codar:
@app/Listeners/RegistrarTrilhaProtocolo.php
@app/Events/ResultadoEmitido.php
@app/Services/Sefaz/SefazViabilidadeGateway.php
@app/Services/Sefaz/UnavailableSefazViabilidadeGateway.php
@app/Support/Audit/AuditService.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: listener EnviarViabilidadeSefaz (só deferida; bloqueado → audita pendência; indeferida ignora)</name>
  <read_first>app/Listeners/RegistrarTrilhaProtocolo.php, app/Services/Sefaz/SefazViabilidadeGateway.php, app/Services/Sefaz/UnavailableSefazViabilidadeGateway.php, app/Models/ViabilityDecision.php</read_first>
  <files>app/Listeners/EnviarViabilidadeSefaz.php, tests/Feature/Expresso/EnviarViabilidadeSefazListenerTest.php</files>
  <action>
    TDD estrito (RED primeiro). `EnviarViabilidadeSefaz` (app/Listeners — AUTO-DESCOBERTO; type-hint ResultadoEmitido; ShouldQueue):
    - construtor injeta `SefazViabilidadeGateway $sefaz` e `AuditService $audit`.
    - `handle(ResultadoEmitido $event)`:
      - guarda HU-134 RN-003: `if (! $event->decision->isDeferida()) { $this->audit->log('integracoes','sefaz-viabilidade','Envio à SEFAZ não se aplica (indeferimento)', properties: [protocolo,outcome], result: 'ignorado', subject: $event->request); return; }`
      - `try { $this->sefaz->sendViabilidade($event->request, $event->decision); audit ... result 'sucesso'; }`
      - `catch (SefazUnavailableException $e) { audit 'integracoes'/'sefaz-viabilidade' 'Envio à SEFAZ pendente (integração bloqueada — Fase 13)' result 'bloqueado'; }`
      - NÃO relançar (a decisão não falha pela integração bloqueada).
    - PHPDoc: só deferimento; bloqueado → pendência; auto-descoberta única (NÃO Event::listen).

    `EnviarViabilidadeSefazListenerTest` (SQLite):
    - deferida + binding default (bloqueado): event(ResultadoEmitido) → Activity 'integracoes'/'sefaz-viabilidade' result 'bloqueado' (1×, CONTAGEM); sem exceção que quebre o fluxo.
    - deferida + fake gateway (app()->instance) → chama sendViabilidade e audita 'sucesso' (prova que liga).
    - INDEFERIDA → NÃO chama o gateway (com um fake/spy, asserir zero chamadas) e audita 'ignorado' (HU-134 RN-003).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=EnviarViabilidadeSefazListenerTest</verify>
  <acceptance_criteria>
    - grep "SefazUnavailableException" e "isDeferida" em app/Listeners/EnviarViabilidadeSefaz.php.
    - grep "result: 'bloqueado'" e "result: 'ignorado'" no listener.
    - grep -L "EnviarViabilidadeSefaz" app/Providers/AppServiceProvider.php (auto-descoberta).
    - EnviarViabilidadeSefazListenerTest verde (deferida bloqueada audita; sucesso com fake; indeferida ignora sem chamar gateway).
  </acceptance_criteria>
  <done>Listener envia à SEFAZ só no deferimento; bloqueado → pendência auditada; indeferimento ignorado auditável; sem fachada, sem duplicar.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-110 / HU-076 RN-008 — envia viabilidade à SEFAZ no deferimento | EnviarViabilidadeSefazListenerTest (sucesso com fake) |
| HU-134 RN-003 — indeferimento NÃO envia à SEFAZ | EnviarViabilidadeSefazListenerTest (indeferida ignora, zero chamadas) |
| HU-076 FA-03 / anti-fachada — bloqueado audita pendência | EnviarViabilidadeSefazListenerTest (result 'bloqueado') |
| RN-002 (lição Fase 8) — sem duplicação | EnviarViabilidadeSefazListenerTest (CONTAGEM) + grep sem Event::listen |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=EnviarViabilidadeSefazListenerTest` verde.
- Suíte completa CONTINUA verde — listener novo no ResultadoEmitido, sem tocar a decisão.
</verification>

<success_criteria>
- Envio à SEFAZ só no deferimento (provado com fake); indeferimento ignorado auditável (HU-134 RN-003); bloqueado → pendência honesta.
- Registro único por auto-descoberta; a decisão nunca falha pela integração bloqueada.
- ZERO dependência nova; sem adaptador falso.
</success_criteria>

<output>
Criar `.planning/phases/09-fluxo-expresso/09-09-SUMMARY.md`: o comportamento do listener (deferida envia / bloqueado audita / indeferida ignora), o logName/event/result das auditorias e a confirmação de auto-descoberta. Insumo de 09-10 (indeferimento BAP NÃO deve acionar SEFAZ — este listener ignora) e 09-12 (smoke registra a pendência SEFAZ só no deferimento).
</output>

---
phase: 09-fluxo-expresso
plan: 05
type: execute
wave: 2
depends_on: ["09-01", "09-02", "09-03", "09-04"]
files_modified:
  - app/Services/Expresso/FluxoExpressoService.php
  - app/Services/Expresso/DecisionResult.php
  - tests/Feature/Expresso/FluxoExpressoElegibilidadeTest.php
  - tests/Feature/Expresso/FluxoExpressoDecisaoTest.php
  - tests/Feature/Expresso/FluxoExpressoAuditoriaTest.php
  - tests/Feature/Expresso/FluxoExpressoIdempotenciaPostgisTest.php
autonomous: true
requirements: [HU-073, HU-074, HU-075, HU-076, HU-078]

must_haves:
  truths:
    - "FluxoExpressoService::decide(request) é o MOTOR de decisão: sob Cache::lock + re-check status===protocolada (idempotência), reexecuta o SolicitacaoViabilityResolver FRESCO e decide — nunca confia no simulation_snapshot"
    - "Elegibilidade HU-073: toggle features.fluxo_expresso off → em_analise; algum CNAE fora do expresso (resolved->elegivelExpresso() false) → em_analise (semi-expresso, motivo auditado) — SEM ResultadoEmitido"
    - "CRÍTICO (anti-fachada): veredito consolidado == pendente (sem zona) → em_analise, SEM criar ViabilityDecision e SEM ResultadoEmitido — o motor NÃO defere/indefere sem a zona"
    - "Decisão HU-074/075 (RN-009): consolidado nao_permitido → INDEFERE; permitido/permitido_com_condicoes → DEFERE — numa DB::transaction cria ViabilityDecision (per_cnae/rules_versions/fundamentacao/reason), gera número TVL só no deferimento, transiciona pela StateMachine e AUDITA SÍNCRONO (HU-078, rules_version)"
    - "ResultadoEmitido é disparado SÓ após o commit das decisões (deferida/indeferida); a auditoria autoritativa NÃO depende do evento (Event::fake e a auditoria continua gravada)"
    - "Idempotência real (@group postgis): duas decisões concorrentes da mesma solicitação → 1 ViabilityDecision, 1 número TVL, 1 evento"
  artifacts:
    - path: "app/Services/Expresso/FluxoExpressoService.php"
      provides: "Motor de decisão: elegibilidade + defere/indefere + lock + transação + auditoria síncrona + evento"
      contains: "Cache::lock"
    - path: "app/Services/Expresso/DecisionResult.php"
      provides: "Retorno da decisão (status final + decisão criada|null + motivo) para comando/job/UI"
      contains: "final readonly class DecisionResult"
  key_links:
    - from: "app/Services/Expresso/FluxoExpressoService.php"
      to: "app/Services/Solicitacao/SolicitacaoViabilityResolver.php"
      via: "resolve(request) FRESCO dentro do lock (autoritativo, não o snapshot)"
      pattern: "resolver->resolve"
    - from: "app/Services/Expresso/FluxoExpressoService.php"
      to: "app/Support/Audit/AuditService.php"
      via: "log('expresso','decisao', rulesVersion=...) SÍNCRONO na transação (HU-078) — independe do evento"
      pattern: "logName: 'expresso'"
    - from: "app/Services/Expresso/FluxoExpressoService.php"
      to: "app/Events/ResultadoEmitido.php"
      via: "ResultadoEmitido::dispatch(request, decision) APÓS o commit, só quando defere/indefere"
      pattern: "ResultadoEmitido::dispatch"
---

<objective>
O CORE VALUE do produto: `FluxoExpressoService::decide(request)` — o motor real que, sob lock idempotente, reexecuta os motores frescos (via `SolicitacaoViabilityResolver`), identifica elegibilidade (HU-073), e DEFERE (HU-074) / INDEFERE (HU-075) / encaminha para análise — registrando a decisão imutável com fundamentação, versões de regra e número TVL (HU-076), auditando SÍNCRONO (HU-078) e disparando `ResultadoEmitido` após o commit.

Purpose: é a entrega da fase. A decisão é AUTORITATIVA (reexecuta, não confia no snapshot orientativo). A degradação herdada é honesta e CRÍTICA: sem a zona (Quadro 10), o veredito consolida `pendente` → a solicitação vai para `em_analise` (NÃO defere/indefere) e NÃO emite `ResultadoEmitido` — o motor liga sozinho quando a base oficial entrar (muda a carga, não a lógica). A idempotência é real (Cache::lock + re-check + unique do TVL), provada em @group postgis.
Output: `FluxoExpressoService` + `DecisionResult` (DTO), cobertos por feature tests de elegibilidade, decisão, auditoria síncrona e idempotência concorrente (@group postgis).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-fluxo-expresso/09-CONTEXT.md
@docs/superpowers/specs/2026-06-14-fluxo-expresso-design.md
@.planning/phases/09-fluxo-expresso/09-01-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-02-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-03-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-04-SUMMARY.md

# PADRÕES A ESPELHAR (transação+auditoria+evento; lock; concorrência postgis) — ler antes de codar:
@app/Services/Solicitacao/ProtocolarSolicitacaoService.php
@app/Services/Solicitacao/ViabilityRequestStateMachine.php
@app/Support/Audit/AuditService.php
@app/Enums/ResultadoViabilidade.php
@app/Enums/Fluxo.php
@app/Enums/ViabilityRequestStatus.php
@tests/Feature/Solicitacao/ProtocolarConcorrenciaPostgisTest.php
@tests/PostgisTestCase.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: elegibilidade + roteamento (HU-073) com a degradação CRÍTICA sem zona (em_analise sem evento)</name>
  <read_first>app/Services/Solicitacao/SolicitacaoViabilityResolver.php, app/Services/Solicitacao/ViabilityRequestStateMachine.php, app/Enums/ViabilityRequestStatus.php, app/Support/Settings.php</read_first>
  <files>app/Services/Expresso/FluxoExpressoService.php, app/Services/Expresso/DecisionResult.php, tests/Feature/Expresso/FluxoExpressoElegibilidadeTest.php</files>
  <action>
    TDD estrito (RED primeiro). Criar `DecisionResult` (readonly): `status` (ViabilityRequestStatus final), `decision` (?ViabilityDecision), `reason` (?string), `emitted` (bool — se ResultadoEmitido foi/seria disparado). Helper `paraAnalise(reason)`, `decidida(decision)`.

    `FluxoExpressoService` (construtor injeta SolicitacaoViabilityResolver, ViabilityRequestStateMachine, TvlNumberGenerator, AuditService). `decide(ViabilityRequest $request, ?User $actor = null): DecisionResult`:
    - `Cache::lock("expresso:decisao:{$request->id}", (int) config('sile.expresso.lock.ttl_segundos', 10))->block(...)` — dentro do lock, RECARREGAR e re-checar `status === Protocolada` (idempotência: se já saiu de protocolada, retorna sem redecidir).
    - toggle: `Settings::get('features.fluxo_expresso', config('sile.features.fluxo_expresso', true))` false → encaminhar em_analise (motivo "fluxo expresso desativado") via helper `encaminharAnalise()`.
    - `$resolved = $this->resolver->resolve($request);` (FRESCO — autoritativo).
    - elegibilidade HU-073: `! $resolved->elegivelExpresso()` → em_analise (motivo "atividade fora do fluxo expresso (análise técnica)", semi-expresso) — RN-008.
    - veredito consolidado `=== ResultadoViabilidade::Pendente->value` (CRÍTICO sem zona) → em_analise (motivo "veredito locacional pendente — zona urbanística pendente SEDUR"), **SEM** criar decisão, **SEM** evento.
    - senão, segue para a emissão (Task 2): nao_permitido → indefere; permitido/permitido_com_condicoes → defere.

    `encaminharAnalise(request, reason, actor)`: numa DB::transaction transiciona protocolada→em_analise pela StateMachine (timeline + auditoria 'solicitacoes/transicao'), grava auditoria 'expresso/decisao' result 'analise' com o motivo + rules_versions (quando houver resolução), e devolve DecisionResult::paraAnalise(reason) (emitted=false). NÃO dispara ResultadoEmitido.

    `FluxoExpressoElegibilidadeTest` (SQLite + fake SpatialRepository):
    - toggle off → em_analise; sem ViabilityDecision; Event::fake assert NOT dispatched ResultadoEmitido.
    - CNAE de risco alto/gatilho (fluxo analise) → em_analise (semi-expresso) auditado; sem decisão; sem evento.
    - **CRÍTICO**: CNAE expresso porém SEM zona (consolidado pendente) → em_analise; NENHUMA ViabilityDecision; Event::fake assertNotDispatched(ResultadoEmitido). (anti-fachada — o motor NÃO defere/indefere sem zona).
    - idempotência lógica: chamar decide() numa solicitação já em_analise → no-op (não cria 2ª transição/decisão).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=FluxoExpressoElegibilidadeTest</verify>
  <acceptance_criteria>
    - grep "Cache::lock" e "status" (re-check) em app/Services/Expresso/FluxoExpressoService.php.
    - grep "Pendente" (degradação sem zona → em_analise sem evento) no service.
    - FluxoExpressoElegibilidadeTest verde — INCLUINDO o teste crítico sem-zona (em_analise, sem decisão, sem ResultadoEmitido).
  </acceptance_criteria>
  <done>Elegibilidade + roteamento com lock/idempotência e a degradação CRÍTICA honesta (sem zona → em_analise, sem evento). Sem fachada.</done>
</task>

<task type="auto">
  <name>Task 2: defere/indefere (HU-074/075/RN-009) + ViabilityDecision + TVL + auditoria síncrona (HU-078) + ResultadoEmitido</name>
  <read_first>app/Services/Solicitacao/ProtocolarSolicitacaoService.php, app/Models/ViabilityDecision.php, app/Services/Expresso/TvlNumberGenerator.php, app/Events/ResultadoEmitido.php</read_first>
  <files>app/Services/Expresso/FluxoExpressoService.php, tests/Feature/Expresso/FluxoExpressoDecisaoTest.php, tests/Feature/Expresso/FluxoExpressoAuditoriaTest.php</files>
  <action>
    TDD estrito. Completar o `decide()` com a emissão (espelhar a transação do ProtocolarSolicitacaoService: validar fora, transacionar, evento após commit):
    - decidir o `DecisionOutcome` a partir do consolidado: nao_permitido → Indeferida; permitido/permitido_com_condicoes → Deferida (RN-009 — já consolidado pelo pior caso no resolver).
    - `DB::transaction`:
      - criar a `ViabilityDecision` (flow 'expresso', outcome, consolidated_result, per_cnae do resolved (toArray por CNAE com tendência+fluxo+fundamentação), rules_versions, fundamentacao consolidada (união das fundamentações dos CNAEs), reason (null no caminho normal), decided_by_user_id = $actor?->id (null=sistema), decided_at now).
      - SÓ no deferimento: `tvl_product_number = $this->tvl->generate()` (gravado na decisão; unique é a defesa final).
      - transicionar pela StateMachine: protocolada→deferida | protocolada→indeferida (timeline + auditoria 'solicitacoes/transicao').
      - AUDITORIA SÍNCRONA HU-078: `audit->log('expresso','decisao', "...", properties: [por_cnae resumido, consolidado, outcome, tvl], rulesVersion: <versão representativa das regras>, subject: $request, result: 'deferida'|'indeferida')` — DENTRO da transação (não depende do evento).
    - APÓS o commit: `ResultadoEmitido::dispatch($request, $decision)`.
    - devolver DecisionResult::decidida($decision) (emitted=true).
    - ROBUSTEZ idempotente (corrida rara que escape do Cache::lock — ex.: cache não compartilhado): capturar `Illuminate\Database\QueryException` de violação de `unique(viability_request_id)` na criação da ViabilityDecision e tratar como NO-OP idempotente — recarregar e devolver a decisão existente (DecisionResult::decidida(decisão já gravada), emitted=false para não redisparar o evento), em vez de deixar o job falhar. O caminho normal continua protegido por lock+re-check; isto só fecha a corrida rara de forma limpa (não é fachada — a decisão real já existe).

    `FluxoExpressoDecisaoTest` (SQLite + fake SpatialRepository):
    - todos os CNAEs permitido (com zona fake identificada) → Deferida: cria 1 ViabilityDecision (outcome deferida, tvl_product_number TVL-AAAA-000001 não-nulo), status deferida, transição gravada; Event::fake assertDispatched(ResultadoEmitido) com a decisão.
    - um CNAE nao_permitido → Indeferida: 1 ViabilityDecision (outcome indeferida, tvl_product_number NULL), status indeferida, evento disparado.
    - permitido_com_condicoes → Deferida (defere com condições; fundamentação carrega as condicionantes).
    - per_cnae/rules_versions/fundamentacao persistidos na decisão (RN-005).

    `FluxoExpressoAuditoriaTest` (SQLite):
    - Event::fake([ResultadoEmitido::class]) (isola o evento) → após decide() deferido: existe Activity log_name 'expresso' event 'decisao' result 'deferida' com rules_version preenchida e properties por_cnae — PROVANDO que a auditoria é SÍNCRONA e independe do evento (HU-078).
    - a transição (solicitacoes/transicao) também auditada.
  </action>
  <verify>php artisan test --compact --filter="FluxoExpressoDecisaoTest|FluxoExpressoAuditoriaTest"</verify>
  <acceptance_criteria>
    - grep "ResultadoEmitido::dispatch" (após o commit) e "DB::transaction" no service.
    - grep "logName: 'expresso'" (auditoria síncrona) e "tvl->generate" (TVL só deferimento).
    - FluxoExpressoDecisaoTest verde (defere/indefere, TVL só deferida, evento disparado).
    - FluxoExpressoAuditoriaTest verde (auditoria síncrona com rules_version, independente do evento).
  </acceptance_criteria>
  <done>Defere/indefere reais criando ViabilityDecision (+TVL no deferimento), transição auditada e auditoria SÍNCRONA da decisão; ResultadoEmitido após o commit.</done>
</task>

<task type="auto">
  <name>Task 3: idempotência sob concorrência real (@group postgis)</name>
  <read_first>tests/Feature/Solicitacao/ProtocolarConcorrenciaPostgisTest.php, tests/PostgisTestCase.php</read_first>
  <files>tests/Feature/Expresso/FluxoExpressoIdempotenciaPostgisTest.php</files>
  <action>
    TDD. `FluxoExpressoIdempotenciaPostgisTest extends PostgisTestCase` (#[Group('postgis')] herdado) — espelha o ProtocolarConcorrenciaPostgisTest:
    - Event::fake([ResultadoEmitido::class]) para isolar a concorrência da decisão.
    - preparar uma solicitação protocolada elegível e DEFERÍVEL (com zona fake identificada de modo que o consolidado seja permitido — usar o mesmo setup dos testes de decisão sobre PostGIS, ou um SpatialRepository fake injetado que devolve zona).
    - chamar `decide()` DUAS vezes (sequenciais provando o lock + re-check; a 2ª recai no re-check status!==protocolada).
    - asserir: exatamente 1 ViabilityDecision para a solicitação; tvl_sequences avançou exatamente 1 (sem buraco); status deferida; Event::fake assertDispatched(ResultadoEmitido) UMA vez (a 2ª chamada não redispara).
    - assertSame('pgsql', DB::connection()->getDriverName()).
    Documentar que o Cache::lock real exige cache compartilhado em produção (Redis); em teste, o re-check de status dentro da transação é a garantia adicional.
  </action>
  <verify>php artisan test --compact --group=postgis --filter=FluxoExpressoIdempotenciaPostgisTest</verify>
  <acceptance_criteria>
    - grep "Group('postgis')" (ou extends PostgisTestCase) no arquivo.
    - FluxoExpressoIdempotenciaPostgisTest verde (1 decisão, 1 TVL, 1 evento) com o container de pé; skip honesto sem servidor.
  </acceptance_criteria>
  <done>Idempotência real sob concorrência: 1 decisão/1 TVL/1 evento por solicitação (@group postgis).</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-073 CA-01/RN-008 — elegibilidade (expresso vs análise) | FluxoExpressoElegibilidadeTest (toggle off; gatilho→análise) |
| HU-073 CA-03 / anti-fachada — sem zona → em_analise SEM evento | FluxoExpressoElegibilidadeTest (teste CRÍTICO sem zona) |
| HU-074 CA-01 / RN-009 — defere quando todos permitido (+TVL) | FluxoExpressoDecisaoTest (deferida + TVL) |
| HU-075 CA-01 / RN-009 — indefere quando nao_permitido | FluxoExpressoDecisaoTest (indeferida, sem TVL) |
| HU-076 RN-007 — número TVL no deferimento | FluxoExpressoDecisaoTest (tvl_product_number não-nulo só deferida) |
| HU-078 CA-02 / RN-005 — auditoria síncrona com versões/por-CNAE | FluxoExpressoAuditoriaTest (independe do evento) |
| HU-076 — idempotência da emissão | FluxoExpressoIdempotenciaPostgisTest (1 decisão/TVL/evento) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="FluxoExpressoElegibilidadeTest|FluxoExpressoDecisaoTest|FluxoExpressoAuditoriaTest"` verde.
- `php artisan test --compact --group=postgis --filter=FluxoExpressoIdempotenciaPostgisTest` verde (container) ou skip honesto.
- Suíte completa CONTINUA verde (a Fase 8 não foi tocada; o resolver e o schema são da Wave 1).
</verification>

<success_criteria>
- Motor de decisão real: elegibilidade + defere/indefere reexecutando os motores frescos; ViabilityDecision imutável + TVL no deferimento; auditoria SÍNCRONA (HU-078); ResultadoEmitido após o commit.
- Degradação CRÍTICA honesta: sem zona → em_analise, SEM decisão e SEM evento (anti-fachada provado em teste).
- Idempotência real (lock + re-check + unique TVL) provada em @group postgis. ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/09-fluxo-expresso/09-05-SUMMARY.md`: assinatura de FluxoExpressoService::decide e de DecisionResult, a regra de elegibilidade/decisão (toggle, semi-expresso, pendente→análise sem evento, nao_permitido→indefere, permitido(_com_condicoes)→defere), o log de auditoria síncrona (logName/event/result/rulesVersion) e o ponto de disparo do ResultadoEmitido. Insumo direto de 09-06 (job chama decide), 09-07/08/09 (consomem o evento), 09-10 (reusa a emissão para o indeferimento BAP) e 09-11 (exibe a decisão).
</output>

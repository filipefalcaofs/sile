---
phase: 09-fluxo-expresso
plan: 07
type: execute
wave: 4
depends_on: ["09-01", "09-03", "09-05"]
files_modified:
  - app/Notifications/ResultadoExpressoNotification.php
  - app/Listeners/NotificarResultadoExpresso.php
  - tests/Feature/Expresso/ResultadoExpressoNotificationTest.php
  - tests/Feature/Expresso/NotificarResultadoExpressoListenerTest.php
autonomous: true
requirements: [HU-077]

must_haves:
  truths:
    - "Existe a notificação ResultadoExpressoNotification (mail, ShouldQueue) que reusa o padrão EmailLog/VerifyEmailQueued/LogNotificationSent — SEM anexo de TVL (canal oficial é Regin/SEFAZ)"
    - "O assunto é parametrizável: deferida usa expresso.notificacao.assunto_deferida; indeferida usa expresso.notificacao.assunto_indeferida (HU-077 RN-005)"
    - "O listener AUTO-DESCOBERTO NotificarResultadoExpresso pendura no ResultadoEmitido e notifica o requerente; toggle features.notificacao_resultado_expresso off → NÃO envia e AUDITA a degradação (nunca falha silenciosa)"
    - "Registro único por auto-descoberta (NÃO Event::listen) — sem duplicar notificação/auditoria (lição Fase 8)"
  artifacts:
    - path: "app/Notifications/ResultadoExpressoNotification.php"
      provides: "E-mail do resultado expresso ao requerente, sem anexo de TVL, assunto parametrizável"
      contains: "ShouldQueue"
    - path: "app/Listeners/NotificarResultadoExpresso.php"
      provides: "Listener auto-descoberto do ResultadoEmitido que notifica (respeitando o toggle)"
      contains: "ResultadoEmitido"
  key_links:
    - from: "app/Listeners/NotificarResultadoExpresso.php"
      to: "app/Events/ResultadoEmitido.php"
      via: "handle(ResultadoEmitido) auto-descoberto → notifica o requester (toggle off → audita e não envia)"
      pattern: "ResultadoEmitido"
    - from: "app/Notifications/ResultadoExpressoNotification.php"
      to: "App\\Support\\Settings"
      via: "assunto via expresso.notificacao.assunto_deferida/_indeferida (HU-014)"
      pattern: "expresso.notificacao"
---

<objective>
Efeito desacoplado HU-077 (Wave 4, track ‖): notificar o requerente do resultado do fluxo expresso por e-mail complementar — SEM anexo de TVL (o canal oficial é Regin/SEFAZ) —, reusando a infra de e-mail (`EmailLog`/`VerifyEmailQueued`/`LogNotificationSent`). O listener auto-descoberto `NotificarResultadoExpresso` consome o `ResultadoEmitido` e respeita o toggle `features.notificacao_resultado_expresso` (off → não envia e audita).

Purpose: ciência ao cidadão pelos canais parametrizados, sem entregar PDF/TVL pelo Simplifica (decisão SEDUR). É efeito colateral desacoplado: a decisão e a auditoria já aconteceram (09-05) — a notificação é plugada no evento. Anti-fachada: toggle off degrada comunicado (auditado), nunca finge envio. Canais plenos (WhatsApp/in-app) são EP11.
Output: `ResultadoExpressoNotification` (mail/ShouldQueue, assunto parametrizável, sem anexo) + `NotificarResultadoExpresso` (listener auto-descoberto com toggle), cobertos por feature tests (Notification::fake; toggle off audita; auto-descoberta sem duplicar).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-fluxo-expresso/09-CONTEXT.md
@docs/superpowers/specs/2026-06-14-fluxo-expresso-design.md
@.planning/phases/09-fluxo-expresso/09-01-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-03-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-05-SUMMARY.md

# PADRÕES A ESPELHAR (notificação mail + EmailLog; listener auto-descoberto; toggle) — ler antes de codar:
@app/Notifications/VerifyEmailQueued.php
@app/Listeners/LogNotificationSent.php
@app/Listeners/RegistrarTrilhaProtocolo.php
@app/Models/EmailLog.php
@app/Support/Settings.php
@app/Support/Audit/AuditService.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: ResultadoExpressoNotification (mail, ShouldQueue, sem anexo, assunto parametrizável)</name>
  <read_first>app/Notifications/VerifyEmailQueued.php, app/Listeners/LogNotificationSent.php, app/Models/EmailLog.php</read_first>
  <files>app/Notifications/ResultadoExpressoNotification.php, tests/Feature/Expresso/ResultadoExpressoNotificationTest.php</files>
  <action>
    TDD estrito (RED primeiro). `ResultadoExpressoNotification` (mail, ShouldQueue, espelha o padrão VerifyEmailQueued + EmailLog):
    - construtor carrega o necessário (ViabilityRequest + ViabilityDecision, ou os dados já desnormalizados: protocol_number, outcome, public label).
    - `via()` = ['mail']; `toMail($notifiable): MailMessage` com:
      - subject: deferida → `Settings::get('expresso.notificacao.assunto_deferida', config('sile.expresso.notificacao.assunto_deferida'))`; indeferida → assunto_indeferida.
      - corpo pt-BR: informa o número de protocolo, o resultado (deferida/indeferida) e orienta acompanhar pelo Regin/Junta; SEM anexo de TVL (NÃO chamar ->attach(); deixar claro no PHPDoc o motivo — canal oficial é Regin/SEFAZ).
    - integrar com EmailLog/`emailLogId` como VerifyEmailQueued (o LogNotificationSent marca como enviado; failed() marca como falha) — criar o EmailLog no disparo (no listener) e setar emailLogId na notificação, espelhando o fluxo existente.

    `ResultadoExpressoNotificationTest` (SQLite): Notification::fake OU Mail::fake — asserir o assunto correto por outcome (deferida/indeferida) lendo os parâmetros (gravar Parameter para um caso e checar override); asserir que NÃO há anexo (toMail sem attachments). Pode testar a renderização do MailMessage diretamente (subject + linhas) sem enfileirar.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=ResultadoExpressoNotificationTest</verify>
  <acceptance_criteria>
    - grep "ShouldQueue" e "expresso.notificacao.assunto" em app/Notifications/ResultadoExpressoNotification.php.
    - grep -L "->attach" na notificação (SEM anexo de TVL).
    - ResultadoExpressoNotificationTest verde (assunto por outcome parametrizado; sem anexo).
  </acceptance_criteria>
  <done>Notificação mail/ShouldQueue do resultado, assunto parametrizável, SEM anexo de TVL, integrada ao EmailLog.</done>
</task>

<task type="auto">
  <name>Task 2: listener NotificarResultadoExpresso (auto-descoberto, com toggle + auditoria da degradação)</name>
  <read_first>app/Listeners/RegistrarTrilhaProtocolo.php, app/Events/ResultadoEmitido.php, app/Support/Audit/AuditService.php</read_first>
  <files>app/Listeners/NotificarResultadoExpresso.php, tests/Feature/Expresso/NotificarResultadoExpressoListenerTest.php</files>
  <action>
    TDD estrito. `NotificarResultadoExpresso` (app/Listeners — AUTO-DESCOBERTO; type-hint ResultadoEmitido; ShouldQueue opcional — o envio já é ShouldQueue pela notificação; manter o listener síncrono e deixar a notificação enfileirar, espelhando o padrão da casa):
    - `handle(ResultadoEmitido $event)`: 
      - toggle `Settings::get('features.notificacao_resultado_expresso', config('sile.features.notificacao_resultado_expresso', true))` false → `audit->log('notificacoes','resultado-expresso', '...desativada...', result: 'desativado', subject: request)` e RETURN (degradação comunicada, não envia).
      - resolver o destinatário (o requester/User da solicitação; sem e-mail válido → audita 'sem-destinatario' e return — honesto).
      - criar EmailLog + montar a ResultadoExpressoNotification (set emailLogId) + `Notification::send($requester, $notification)` (ou `$requester->notify(...)`).
    - PHPDoc: efeito desacoplado do ResultadoEmitido; auto-descoberta é o registro único (NÃO Event::listen — duplicaria); a auditoria/decisão NÃO dependem deste listener.

    `NotificarResultadoExpressoListenerTest` (SQLite): 
    - Notification::fake; disparar `event(new ResultadoEmitido($request, $decision))` (decisão deferida via factory) → assertSentTo o requester com ResultadoExpressoNotification, 1× (CONTAGEM — auto-descoberta não duplica).
    - toggle off → assertNothingSent + Activity 'notificacoes' result 'desativado' gravada (degradação auditada).
    - decisão indeferida → notifica com o assunto de indeferida.
  </action>
  <verify>php artisan test --compact --filter=NotificarResultadoExpressoListenerTest</verify>
  <acceptance_criteria>
    - grep "ResultadoEmitido" app/Listeners/NotificarResultadoExpresso.php.
    - grep "features.notificacao_resultado_expresso" no listener (toggle).
    - grep -L "NotificarResultadoExpresso" app/Providers/AppServiceProvider.php (auto-descoberta, sem Event::listen).
    - NotificarResultadoExpressoListenerTest verde (envia 1×; toggle off audita degradação; indeferida).
  </acceptance_criteria>
  <done>Listener auto-descoberto notifica o requerente (1×), respeita o toggle e audita a degradação — sem duplicar, sem falsa notificação.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-077 CA-01 — notifica o resultado ao requerente | NotificarResultadoExpressoListenerTest (assertSentTo) |
| HU-077 RN-004 — sem anexo de PDF/TVL | ResultadoExpressoNotificationTest (sem ->attach) |
| HU-077 RN-005 — status/mensagem parametrizáveis | ResultadoExpressoNotificationTest (assunto por outcome) |
| HU-077 — toggle off degrada comunicado (auditado) | NotificarResultadoExpressoListenerTest (assertNothingSent + auditoria) |
| RN-002 (lição Fase 8) — sem duplicação | NotificarResultadoExpressoListenerTest (CONTAGEM) + grep sem Event::listen |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="ResultadoExpressoNotificationTest|NotificarResultadoExpressoListenerTest"` verde.
- Suíte completa CONTINUA verde — efeito novo plugado no ResultadoEmitido (09-03/05), sem tocar a decisão.
</verification>

<success_criteria>
- E-mail do resultado expresso (sem anexo de TVL), assunto parametrizável, integrado ao EmailLog.
- Listener auto-descoberto notifica 1× e degrada comunicado quando o toggle está off (auditado).
- ZERO dependência nova; sem fachada (nunca finge envio).
</success_criteria>

<output>
Criar `.planning/phases/09-fluxo-expresso/09-07-SUMMARY.md`: a assinatura da ResultadoExpressoNotification (assunto por outcome, sem anexo, EmailLog), o listener auto-descoberto e o comportamento do toggle (degradação auditada). Insumo do fechamento (09-12 smoke: deferimento navegável dispara a notificação).
</output>

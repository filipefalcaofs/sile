---
phase: 09-fluxo-expresso
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/seeders/ParameterSeeder.php
  - config/sile.php
  - tests/Feature/Seeders/ParameterSeederTest.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
autonomous: true
requirements: [HU-014, HU-073, HU-076, HU-077, HU-134]

must_haves:
  truths:
    - "O catálogo HU-014 ganha os 7 parâmetros do fluxo expresso: features.fluxo_expresso, features.notificacao_resultado_expresso, expresso.bap.prazo_horas, expresso.notificacao.assunto_deferida/_indeferida, expresso.tvl.prefixo/.padding — administráveis com tipo, default, validação e histórico"
    - "O grupo novo 'expresso' aparece no catálogo (e os 2 toggles ficam no grupo 'features' existente) — total do catálogo passa de 49 para 56"
    - "config/sile.php espelha os parâmetros expresso.* / features.* (fallback do Settings::get quando o banco está indisponível) e guarda as CONSTANTES TÉCNICAS fora do catálogo: timeout do lock, fila, tries/timeout/backoff do job (precedente [02-02])"
    - "Reusa risco.mapa_encaminhamento (Fase 6) para a elegibilidade — NÃO cria parâmetro novo de encaminhamento"
    - "ParameterSeederTest e DatabaseSeederTest seguem verdes com as contagens atualizadas (56) e o grupo 'expresso' inserido na lista ordenada"
  artifacts:
    - path: "database/seeders/ParameterSeeder.php"
      provides: "7 novos parâmetros administráveis do fluxo expresso (HU-014)"
      contains: "expresso.tvl.prefixo"
    - path: "config/sile.php"
      provides: "Fallback dos parâmetros + constantes técnicas do expresso (lock/fila/job)"
      contains: "'expresso' =>"
  key_links:
    - from: "database/seeders/ParameterSeeder.php"
      to: "config/sile.php"
      via: "cada parâmetro do catálogo tem o espelho de fallback em sile.expresso.* / sile.features.* (Settings::get lê config quando o banco está indisponível)"
      pattern: "expresso\\."
    - from: "tests/Feature/Seeders/ParameterSeederTest.php"
      to: "database/seeders/ParameterSeeder.php"
      via: "asserção de contagem 56 + grupo 'expresso' + asserts por chave dos novos parâmetros"
      pattern: "assertSame\\(56"
---

<objective>
Fundação de parametrização (Track A da Wave 1): registrar no catálogo HU-014 os parâmetros administráveis do fluxo expresso e espelhá-los em `config/sile.php` (fallback + constantes técnicas). Nada de valor de negócio hardcoded — prazo BAP, assuntos de e-mail, formato do número TVL e os toggles do fluxo/notificação nascem administráveis, com efeito sem deploy.

Purpose: a Fase 9 inteira lê parâmetros via `Settings::get(..., config('sile....'))`. Garantir o catálogo + fallback ANTES dos consumidores (FluxoExpressoService, TvlNumberGenerator, notificação, BAP) evita acoplamento ao seeder e mantém os serviços testáveis com default inline. Constantes técnicas (lock/fila/retry) ficam só no config, fora do painel — parametrizar isso seria ruído.
Output: 7 parâmetros novos no `ParameterSeeder` (catálogo 49→56, grupo novo `expresso`), bloco `expresso` + 2 toggles `features.*` em `config/sile.php`, e os testes de contagem (ParameterSeederTest/DatabaseSeederTest) atualizados e verdes.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-fluxo-expresso/09-CONTEXT.md
@docs/superpowers/specs/2026-06-14-fluxo-expresso-design.md

# PADRÕES A ESPELHAR (catálogo HU-014; fallback config; testes de contagem) — ler antes de codar:
@database/seeders/ParameterSeeder.php
@config/sile.php
@app/Support/Settings.php
@tests/Feature/Seeders/ParameterSeederTest.php
@tests/Feature/Seeders/DatabaseSeederTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: 7 parâmetros novos no catálogo HU-014 + asserts no ParameterSeederTest</name>
  <read_first>database/seeders/ParameterSeeder.php, tests/Feature/Seeders/ParameterSeederTest.php</read_first>
  <files>database/seeders/ParameterSeeder.php, tests/Feature/Seeders/ParameterSeederTest.php</files>
  <action>
    TDD: PRIMEIRO atualizar `ParameterSeederTest` (RED), depois adicionar os parâmetros ao `catalog()`.

    No `ParameterSeederTest`:
    - `test_seeder_cria_catalogo_completo`: trocar `assertSame(49, ...)` por `assertSame(56, ...)`; atualizar a lista ordenada de grupos para incluir `'expresso'`: `['expresso', 'features', 'geo', 'integracoes', 'louos', 'retencao', 'risco', 'seguranca', 'solicitacao', 'ui']`.
    - `test_seeder_e_idempotente`: trocar `assertSame(49, ...)` por `assertSame(56, ...)`.
    - Acrescentar `test_seeder_registra_parametros_do_fluxo_expresso`: asserir, por chave, group/type/default_value/validation_rules dos 7 novos (e `value` null).

    No `ParameterSeeder::catalog()`, acrescentar (nomes de chave EXATOS — risco.mapa_encaminhamento NÃO é recriado, já existe):
    1. `features.fluxo_expresso` — group `features`, type `boolean`, default `'1'`, rules `['required','boolean']`, description "Habilita o deferimento/indeferimento automático (fluxo expresso); desligado, toda solicitação protocolada vai para análise técnica (degradação comunicada)".
    2. `features.notificacao_resultado_expresso` — group `features`, type `boolean`, default `'1'`, rules `['required','boolean']`, description "Habilita a notificação por e-mail do resultado do fluxo expresso ao requerente (sem anexo de TVL; canais plenos no EP11)".
    3. `expresso.bap.prazo_horas` — group `expresso`, type `integer`, default `'48'`, rules `['required','integer','min:1','max:720']`, description "Prazo (horas) sem BAP vinculado antes do indeferimento automático por prazo (HU-134; ativação bloqueada até o Regin — Fase 13)".
    4. `expresso.notificacao.assunto_deferida` — group `expresso`, type `string`, default `'Resultado da sua solicitação de viabilidade: deferida'`, rules `['required','string','max:150']`, description "Assunto do e-mail de notificação de deferimento (HU-077 RN-005)".
    5. `expresso.notificacao.assunto_indeferida` — group `expresso`, type `string`, default `'Resultado da sua solicitação de viabilidade: indeferida'`, rules `['required','string','max:150']`, description "Assunto do e-mail de notificação de indeferimento (HU-077 RN-005)".
    6. `expresso.tvl.prefixo` — group `expresso`, type `string`, default `'TVL'`, rules `['required','string','max:10']`, description "Prefixo do número de produto TVL do deferimento ({prefixo}-AAAA-NNNNNN) — numeração oficial do SAPS a confirmar SEDUR".
    7. `expresso.tvl.padding` — group `expresso`, type `integer`, default `'6'`, rules `['required','integer','min:4','max:10']`, description "Quantidade de dígitos da sequência do número de produto TVL".

    Manter o padrão do seeder (upsert por key SÓ de metadados; `value` nunca no update — preserva o que o admin gravou).
  </action>
  <verify>php artisan test --compact --filter=ParameterSeederTest</verify>
  <acceptance_criteria>
    - grep "expresso.tvl.prefixo" e "features.fluxo_expresso" em database/seeders/ParameterSeeder.php.
    - grep "assertSame(56" em tests/Feature/Seeders/ParameterSeederTest.php (catálogo + idempotência).
    - grep "'expresso'" na lista de grupos do ParameterSeederTest.
    - ParameterSeederTest verde (catálogo 56, grupo expresso, asserts por chave).
  </acceptance_criteria>
  <done>7 parâmetros novos no catálogo (grupo expresso + 2 toggles features), catálogo 49→56, ParameterSeederTest verde.</done>
</task>

<task type="auto">
  <name>Task 2: fallback + constantes técnicas em config/sile.php + contagem do DatabaseSeederTest</name>
  <read_first>config/sile.php, tests/Feature/Seeders/DatabaseSeederTest.php</read_first>
  <files>config/sile.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <action>
    TDD: se o `DatabaseSeederTest` asseria a contagem de parâmetros (grep `49`), atualizar para `56` ANTES de rodar; confirmar RED→GREEN.

    Em `config/sile.php`:
    - No bloco `features`, acrescentar `'fluxo_expresso' => true` e `'notificacao_resultado_expresso' => true`.
    - Criar o bloco `'expresso'` espelhando os parâmetros de negócio E guardando as constantes técnicas (precedente [02-02] — constantes técnicas ficam SÓ no config, nunca no catálogo):
      ```php
      'expresso' => [
          'bap' => ['prazo_horas' => 48],
          'notificacao' => [
              'assunto_deferida' => 'Resultado da sua solicitação de viabilidade: deferida',
              'assunto_indeferida' => 'Resultado da sua solicitação de viabilidade: indeferida',
          ],
          'tvl' => ['prefixo' => 'TVL', 'padding' => 6],
          // Constantes técnicas (fora do catálogo HU-014): idempotência/concorrência
          // da emissão e resiliência do job de decisão. Comentário explicando o porquê.
          'lock' => ['ttl_segundos' => 10],
          'fila' => 'default',
          'job' => ['tries' => 3, 'timeout' => 120, 'backoff' => [30, 60, 120]],
      ],
      ```
    - Manter o comentário-padrão do arquivo (chaves pt-BR espelham os parâmetros HU-014; Settings::get lê config no fallback).

    No `DatabaseSeederTest`: atualizar qualquer asserção de contagem de parâmetros para 56 (manter as demais asserções da fase 8 intactas). Não alterar a contagem de permissões (19 — esta fase reusa `consultar-solicitacoes`, não cria permissão).
  </action>
  <verify>php artisan test --compact --filter="DatabaseSeederTest|ParameterSeederTest"</verify>
  <acceptance_criteria>
    - grep "'expresso' =>" config/sile.php e "fluxo_expresso" no bloco features.
    - grep "'lock'" e "'job'" no bloco expresso (constantes técnicas com comentário do porquê).
    - php artisan config:show sile.expresso.tvl.prefixo retorna TVL.
    - DatabaseSeederTest e ParameterSeederTest verdes (contagem 56).
  </acceptance_criteria>
  <done>config/sile.php com fallback dos parâmetros + constantes técnicas do expresso; DatabaseSeederTest verde com contagem 56.</done>
</task>

</tasks>

## Mapa CA → teste (HU-014)
| CA / RN | Teste |
|---|---|
| HU-014 — parâmetro com tipo/default/validação/histórico | ParameterSeederTest::test_seeder_registra_parametros_do_fluxo_expresso |
| HU-014 — catálogo completo e idempotente | ParameterSeederTest::test_seeder_cria_catalogo_completo / test_seeder_e_idempotente (56) |
| HU-014 — fallback sem banco (config) | config:show sile.expresso.* + DatabaseSeederTest |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="ParameterSeederTest|DatabaseSeederTest"` verde (catálogo 56, grupo expresso, permissões 19 inalteradas).
- `php artisan config:show sile.expresso` mostra o bloco completo (negócio + técnico).
- Suíte completa CONTINUA verde (só parâmetros novos + config; nenhum consumidor alterado).
</verification>

<success_criteria>
- 7 parâmetros administráveis novos (catálogo 49→56, grupo expresso), com fallback em config/sile.php.
- Constantes técnicas (lock/fila/job) só no config, fora do catálogo (parametrização do que faz sentido).
- Reusa risco.mapa_encaminhamento (não recria); permissões permanecem 19. ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/09-fluxo-expresso/09-01-SUMMARY.md`: as 7 chaves novas (group/type/default/rules), a estrutura do bloco `expresso` no config (negócio + constantes técnicas com seus valores), a nova contagem do catálogo (56) e a decisão de NÃO criar permissão (reuso de consultar-solicitacoes). Insumo direto de 09-05 (toggle/TVL/assuntos), 09-07 (assuntos/toggle), 09-10 (prazo BAP) e 09-06 (fila/tries/backoff/lock).
</output>

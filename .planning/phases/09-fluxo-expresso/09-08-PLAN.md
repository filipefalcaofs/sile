---
phase: 09-fluxo-expresso
plan: 08
type: execute
wave: 4
depends_on: ["09-03", "09-05"]
files_modified:
  - app/Listeners/ComunicarResultadoRegin.php
  - tests/Feature/Expresso/ComunicarResultadoReginListenerTest.php
autonomous: true
requirements: [HU-076, HU-104]

must_haves:
  truths:
    - "O listener AUTO-DESCOBERTO ComunicarResultadoRegin pendura no ResultadoEmitido e tenta comunicar o parecer (deferido OU indeferido) ao integrador via ReginParecerNotifier"
    - "Como o contrato está BLOQUEADO (UnavailableReginParecerNotifier lança ReginUnavailableException), o listener CAPTURA a exceção e AUDITA pendência (log 'integracoes', result 'bloqueado') — NUNCA finge sucesso (anti-fachada)"
    - "Registro único por auto-descoberta (NÃO Event::listen) — sem duplicar (lição Fase 8)"
  artifacts:
    - path: "app/Listeners/ComunicarResultadoRegin.php"
      provides: "Listener que comunica o parecer ao Regin; bloqueado → audita pendência (HU-104)"
      contains: "ReginUnavailableException"
  key_links:
    - from: "app/Listeners/ComunicarResultadoRegin.php"
      to: "app/Services/Regin/ReginParecerNotifier.php"
      via: "notifyParecer(request, decision) dentro de try/catch; ReginUnavailableException → audita 'bloqueado'"
      pattern: "notifyParecer"
    - from: "app/Listeners/ComunicarResultadoRegin.php"
      to: "App\\Support\\Audit\\AuditService"
      via: "log('integracoes','regin-parecer', result: 'bloqueado'|'sucesso')"
      pattern: "logName: 'integracoes'"
---

<objective>
Efeito desacoplado HU-104 (Wave 4, track ‖): o listener auto-descoberto `ComunicarResultadoRegin` consome o `ResultadoEmitido` e tenta comunicar o parecer (deferido/indeferido) ao integrador Regin/Junta via o contrato `ReginParecerNotifier`. Como a integração está BLOQUEADA (Fase 13), o provider `Unavailable` lança e o listener AUDITA a pendência — nunca simula sucesso.

Purpose: a decisão é tomada/registrada/auditada de verdade (09-05); a TRANSMISSÃO ao Regin fica como pendência honesta (entrega-funcional) até a Fase 13 ligar o binding. O parecer (deferido E indeferido) deve ir ao Regin (HU-076 RN-008 / HU-104). Anti-fachada: o listener captura `ReginUnavailableException` e registra `result: 'bloqueado'` (outbox/trilha), sem nenhum adaptador falso.
Output: `ComunicarResultadoRegin` (listener auto-descoberto) cobre o caminho bloqueado (audita pendência) e o caminho de sucesso (com um fake do notifier nos testes), provando que a lógica liga quando o contrato existir.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-fluxo-expresso/09-CONTEXT.md
@docs/superpowers/specs/2026-06-14-fluxo-expresso-design.md
@.planning/phases/09-fluxo-expresso/09-03-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-05-SUMMARY.md

# PADRÕES A ESPELHAR (listener auto-descoberto; contrato bloqueado + fake nos testes; auditoria de pendência) — ler antes de codar:
@app/Listeners/RegistrarTrilhaProtocolo.php
@app/Events/ResultadoEmitido.php
@app/Services/Regin/ReginParecerNotifier.php
@app/Services/Regin/UnavailableReginParecerNotifier.php
@app/Services/Viabilidade/ConsultaViabilidadeService.php
@app/Support/Audit/AuditService.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: listener ComunicarResultadoRegin (auto-descoberto; bloqueado → audita pendência)</name>
  <read_first>app/Listeners/RegistrarTrilhaProtocolo.php, app/Services/Regin/ReginParecerNotifier.php, app/Services/Regin/UnavailableReginParecerNotifier.php, app/Support/Audit/AuditService.php</read_first>
  <files>app/Listeners/ComunicarResultadoRegin.php, tests/Feature/Expresso/ComunicarResultadoReginListenerTest.php</files>
  <action>
    TDD estrito (RED primeiro). `ComunicarResultadoRegin` (app/Listeners — AUTO-DESCOBERTO; type-hint ResultadoEmitido; ShouldQueue — integração externa roda na fila):
    - construtor injeta `ReginParecerNotifier $regin` e `AuditService $audit`.
    - `handle(ResultadoEmitido $event)`:
      - `try { $this->regin->notifyParecer($event->request, $event->decision); $this->audit->log('integracoes','regin-parecer','Parecer comunicado ao Regin', properties: [protocolo, outcome], result: 'sucesso', subject: $event->request); }`
      - `catch (ReginUnavailableException $e) { $this->audit->log('integracoes','regin-parecer','Comunicação do parecer ao Regin pendente (integração bloqueada — Fase 13)', properties: [protocolo, outcome, erro], result: 'bloqueado', subject: $event->request); }`
      - NÃO relançar (a decisão não pode falhar por causa da integração bloqueada); o registro 'bloqueado' é a pendência visível.
    - PHPDoc: efeito desacoplado; bloqueado → audita pendência, nunca sucesso falso; registro único por auto-descoberta (NÃO Event::listen).

    `ComunicarResultadoReginListenerTest` (SQLite):
    - caminho REAL (bloqueado): com o binding default (Unavailable), disparar `event(new ResultadoEmitido($request, $decision))` → Activity 'integracoes'/'regin-parecer' result 'bloqueado' (1×, CONTAGEM); NÃO lança exceção que quebre o fluxo.
    - caminho de SUCESSO (prova que a lógica liga): `app()->instance(ReginParecerNotifier::class, <fake que registra a chamada>)` → o listener chama notifyParecer e audita 'sucesso'. (fake/spy SÓ no teste — prática padrão.)
    - indeferida também comunica (RN-008 — parecer vai ao Regin nos dois casos).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=ComunicarResultadoReginListenerTest</verify>
  <acceptance_criteria>
    - grep "ReginUnavailableException" e "logName: 'integracoes'" em app/Listeners/ComunicarResultadoRegin.php.
    - grep "result: 'bloqueado'" no listener (pendência, sem sucesso falso).
    - grep -L "ComunicarResultadoRegin" app/Providers/AppServiceProvider.php (auto-descoberta).
    - ComunicarResultadoReginListenerTest verde (bloqueado audita 1×; sucesso com fake; indeferida também comunica).
  </acceptance_criteria>
  <done>Listener comunica o parecer ao Regin; bloqueado → audita pendência (nunca sucesso falso); liga com o contrato real (provado com fake).</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-104 / HU-076 RN-008 — comunica parecer ao Regin (deferido e indeferido) | ComunicarResultadoReginListenerTest (sucesso com fake; indeferida) |
| HU-076 FA-03 / anti-fachada — bloqueado audita pendência, sem simular | ComunicarResultadoReginListenerTest (result 'bloqueado') |
| RN-002 (lição Fase 8) — sem duplicação | ComunicarResultadoReginListenerTest (CONTAGEM) + grep sem Event::listen |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=ComunicarResultadoReginListenerTest` verde.
- Suíte completa CONTINUA verde — listener novo no ResultadoEmitido, sem tocar a decisão.
</verification>

<success_criteria>
- Parecer comunicado ao Regin quando o contrato existir (provado com fake); bloqueado → pendência auditada honesta (sem adaptador falso).
- Registro único por auto-descoberta; a decisão nunca falha por causa da integração bloqueada.
- ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/09-fluxo-expresso/09-08-SUMMARY.md`: o comportamento do listener (sucesso vs bloqueado), o logName/event/result da auditoria de pendência e a confirmação de auto-descoberta. Insumo de 09-10 (indeferimento BAP → ResultadoEmitido também aciona este listener → Regin) e 09-12 (smoke registra a pendência Regin).
</output>

---
phase: 09-fluxo-expresso
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Regin/ReginParecerNotifier.php
  - app/Services/Regin/UnavailableReginParecerNotifier.php
  - app/Services/Regin/ReginUnavailableException.php
  - app/Services/Regin/BapRegistry.php
  - app/Services/Regin/UnavailableBapRegistry.php
  - app/Services/Sefaz/SefazViabilidadeGateway.php
  - app/Services/Sefaz/UnavailableSefazViabilidadeGateway.php
  - app/Services/Sefaz/SefazUnavailableException.php
  - app/Events/ResultadoEmitido.php
  - app/Providers/AppServiceProvider.php
  - tests/Feature/Expresso/BlockedIntegrationContractsTest.php
  - tests/Feature/Expresso/ResultadoEmitidoEventTest.php
autonomous: true
requirements: [HU-076, HU-104, HU-110, HU-134]

must_haves:
  truths:
    - "Os contratos das integrações bloqueadas existem como interfaces (ReginParecerNotifier, SefazViabilidadeGateway, BapRegistry) com providers Unavailable que degradam honesto — Regin/Sefaz LANÇAM exceção própria; BapRegistry::findLinkage retorna null (nada entra em aguardando_bap hoje)"
    - "Os bindings em AppServiceProvider::register() apontam para os providers Unavailable — a Fase 13 troca SÓ o binding (padrão PropertyRegistryLookup), sem tocar call sites"
    - "Existe o SEGUNDO evento de domínio ResultadoEmitido (ShouldDispatchAfterCommit, carrega ViabilityRequest + ViabilityDecision) — base desacoplada dos efeitos (HU-077/104/110)"
    - "Nenhum adaptador falso: o Unavailable nunca simula sucesso; quem captura a exceção (listeners 09-08/09) audita pendência, nunca resultado fictício"
  artifacts:
    - path: "app/Services/Regin/UnavailableReginParecerNotifier.php"
      provides: "Provider bloqueado do parecer Regin (lança ReginUnavailableException)"
      contains: "throw new ReginUnavailableException"
    - path: "app/Services/Sefaz/UnavailableSefazViabilidadeGateway.php"
      provides: "Provider bloqueado do envio SEFAZ (lança SefazUnavailableException)"
      contains: "throw new SefazUnavailableException"
    - path: "app/Events/ResultadoEmitido.php"
      provides: "Segundo evento de domínio (after-commit) com a solicitação + a decisão"
      contains: "ShouldDispatchAfterCommit"
  key_links:
    - from: "app/Providers/AppServiceProvider.php"
      to: "app/Services/Regin/UnavailableReginParecerNotifier.php"
      via: "bind(ReginParecerNotifier::class, UnavailableReginParecerNotifier::class) — troca única na Fase 13"
      pattern: "ReginParecerNotifier::class"
    - from: "app/Events/ResultadoEmitido.php"
      to: "app/Models/ViabilityDecision.php"
      via: "construtor carrega ViabilityRequest + ViabilityDecision (payload dos listeners)"
      pattern: "ViabilityDecision"
---

<objective>
Fundação de integração desacoplada (Track C da Wave 1): os CONTRATOS das integrações bloqueadas (Regin/Junta HU-104, SEFAZ HU-110, vínculo BAP HU-134) atrás de interface com provider `Unavailable` (degradação honesta, padrão `PropertyRegistryLookup`), os bindings no `AppServiceProvider`, e o SEGUNDO evento de domínio `ResultadoEmitido` (after-commit, carregando a solicitação + a decisão).

Purpose: a Fase 9 toma e registra a decisão de verdade, mas a TRANSMISSÃO externa está bloqueada (sem contrato/homologação) → degrada honesto (entrega-funcional): a abstração existe e a Fase 13 liga o binding sem tocar call sites; nunca adaptador falso. O `ResultadoEmitido` mantém a Fase 9 independente das Fases 10/11/13 — os efeitos (notificar, comunicar Regin, enviar SEFAZ) são listeners auto-descobertos plugados depois.
Output: 3 interfaces + 3 providers Unavailable + 2 exceções, bindings no provider, e o evento `ResultadoEmitido` — cobertos por feature tests (Unavailable degrada; binding resolve; evento after-commit carrega o payload).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-fluxo-expresso/09-CONTEXT.md
@docs/superpowers/specs/2026-06-14-fluxo-expresso-design.md

# PADRÕES A ESPELHAR (contrato bloqueado + Unavailable + binding; evento de domínio after-commit) — ler antes de codar:
@app/Services/Realty/PropertyRegistryLookup.php
@app/Services/Realty/UnavailablePropertyRegistryLookup.php
@app/Services/Realty/PropertyRegistryUnavailableException.php
@app/Providers/AppServiceProvider.php
@app/Events/SolicitacaoProtocolada.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: contratos Regin/Sefaz/Bap + providers Unavailable + exceções + bindings</name>
  <read_first>app/Services/Realty/PropertyRegistryLookup.php, app/Services/Realty/UnavailablePropertyRegistryLookup.php, app/Services/Realty/PropertyRegistryUnavailableException.php, app/Providers/AppServiceProvider.php</read_first>
  <files>app/Services/Regin/ReginParecerNotifier.php, app/Services/Regin/UnavailableReginParecerNotifier.php, app/Services/Regin/ReginUnavailableException.php, app/Services/Regin/BapRegistry.php, app/Services/Regin/UnavailableBapRegistry.php, app/Services/Sefaz/SefazViabilidadeGateway.php, app/Services/Sefaz/UnavailableSefazViabilidadeGateway.php, app/Services/Sefaz/SefazUnavailableException.php, app/Providers/AppServiceProvider.php, tests/Feature/Expresso/BlockedIntegrationContractsTest.php</files>
  <action>
    TDD: `BlockedIntegrationContractsTest` (RED) antes dos providers.

    Contratos (interfaces) — assinaturas mínimas e honestas (a forma fina do payload é Fase 13):
    1. `ReginParecerNotifier` (HU-104): `notifyParecer(ViabilityRequest $request, ViabilityDecision $decision): void` — comunica o parecer (deferido/indeferido) ao integrador Regin/Junta.
    2. `SefazViabilidadeGateway` (HU-110): `sendViabilidade(ViabilityRequest $request, ViabilityDecision $decision): void` — envia os dados de viabilidade à SEFAZ municipal (SÓ deferimento — quem chama decide).
    3. `BapRegistry` (HU-134/HU-133): `findLinkage(ViabilityRequest $request): ?BapLinkage` (ou `?string`/array simples) — resolve o vínculo BAP do processo. Pode ser null (sem vínculo).

    Providers Unavailable (degradação honesta — espelham UnavailablePropertyRegistryLookup):
    - `UnavailableReginParecerNotifier implements ReginParecerNotifier` → `throw new ReginUnavailableException(...)`.
    - `UnavailableSefazViabilidadeGateway implements SefazViabilidadeGateway` → `throw new SefazUnavailableException(...)`.
    - `UnavailableBapRegistry implements BapRegistry` → `findLinkage(): null` (NUNCA inventa vínculo — nada entra em aguardando_bap hoje; sem exceção, é "não há vínculo" honesto).
    - `ReginUnavailableException` e `SefazUnavailableException` (RuntimeException com mensagem clara "pendente Fase 13 / contrato indisponível").

    Bindings em `AppServiceProvider::register()` (junto aos existentes CnpjLookup/Geocoder/PropertyRegistryLookup, com comentário "Fase 13 troca SÓ este binding"):
    - `$this->app->bind(ReginParecerNotifier::class, UnavailableReginParecerNotifier::class);`
    - `$this->app->bind(SefazViabilidadeGateway::class, UnavailableSefazViabilidadeGateway::class);`
    - `$this->app->bind(BapRegistry::class, UnavailableBapRegistry::class);`

    `BlockedIntegrationContractsTest` (SQLite):
    - app(ReginParecerNotifier::class) instanceof UnavailableReginParecerNotifier; ao chamar notifyParecer → expectException(ReginUnavailableException).
    - app(SefazViabilidadeGateway::class) instanceof Unavailable...; sendViabilidade → expectException(SefazUnavailableException).
    - app(BapRegistry::class) instanceof Unavailable...; findLinkage(request) === null.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=BlockedIntegrationContractsTest</verify>
  <acceptance_criteria>
    - grep "interface ReginParecerNotifier", "interface SefazViabilidadeGateway", "interface BapRegistry".
    - grep "ReginParecerNotifier::class" app/Providers/AppServiceProvider.php (3 bindings novos).
    - grep "throw new ReginUnavailableException" e "throw new SefazUnavailableException".
    - BlockedIntegrationContractsTest verde (Unavailable degrada; binding resolve; BAP null).
  </acceptance_criteria>
  <done>3 contratos + 3 providers Unavailable + 2 exceções + bindings — degradação honesta, sem adaptador falso.</done>
</task>

<task type="auto">
  <name>Task 2: evento de domínio ResultadoEmitido (after-commit, carrega request + decisão)</name>
  <read_first>app/Events/SolicitacaoProtocolada.php, app/Models/ViabilityDecision.php</read_first>
  <files>app/Events/ResultadoEmitido.php, tests/Feature/Expresso/ResultadoEmitidoEventTest.php</files>
  <action>
    TDD. `ResultadoEmitido` ESPELHA `SolicitacaoProtocolada`:
    - implements `ShouldDispatchAfterCommit`; use `Dispatchable`.
    - construtor: `public function __construct(public ViabilityRequest $request, public ViabilityDecision $decision) {}`.
    - PHPDoc explicando: SEGUNDO evento de domínio; disparado pelo FluxoExpressoService (09-05) APÓS o commit; carrega a solicitação + a decisão imutável; listeners AUTO-DESCOBERTOS (NÃO Event::listen) — NotificarResultadoExpresso (HU-077), ComunicarResultadoRegin (HU-104), EnviarViabilidadeSefaz (HU-110). A auditoria autoritativa da decisão (HU-078) NÃO depende deste evento (é síncrona na transação) — o evento é só efeitos colaterais desacoplados.

    `ResultadoEmitidoEventTest` (SQLite):
    - Event::fake; dispatch dentro de DB::transaction e asserir que só é observado APÓS o commit (ShouldDispatchAfterCommit) — espelhar como a Fase 8 testou SolicitacaoProtocolada (ou, se não houver teste de after-commit lá, asserir o contrato: implements ShouldDispatchAfterCommit + o payload request/decision acessível).
    - Asserir que o evento carrega a ViabilityRequest e a ViabilityDecision passadas.
  </action>
  <verify>php artisan test --compact --filter=ResultadoEmitidoEventTest</verify>
  <acceptance_criteria>
    - grep "implements ShouldDispatchAfterCommit" app/Events/ResultadoEmitido.php.
    - grep "ViabilityDecision" app/Events/ResultadoEmitido.php (payload).
    - ResultadoEmitidoEventTest verde.
  </acceptance_criteria>
  <done>ResultadoEmitido (after-commit) com request+decisão — base desacoplada dos efeitos da Wave 4.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-104 — parecer Regin atrás de contrato bloqueado | BlockedIntegrationContractsTest (Unavailable lança; binding resolve) |
| HU-110 — envio SEFAZ atrás de contrato bloqueado | BlockedIntegrationContractsTest (Unavailable lança) |
| HU-134 — vínculo BAP indisponível (nada em aguardando_bap) | BlockedIntegrationContractsTest (findLinkage === null) |
| HU-076 — emissão dispara evento de domínio | ResultadoEmitidoEventTest (after-commit, payload) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="BlockedIntegrationContractsTest|ResultadoEmitidoEventTest"` verde.
- Suíte completa CONTINUA verde (só serviços/evento novos + 3 bindings; nenhum call site existente alterado).
</verification>

<success_criteria>
- Contratos Regin/Sefaz/Bap com Unavailable (degradação honesta, sem fachada) + bindings trocáveis na Fase 13.
- ResultadoEmitido (after-commit) carregando request + decisão — base dos efeitos desacoplados.
- ZERO dependência nova; nenhum adaptador falso.
</success_criteria>

<output>
Criar `.planning/phases/09-fluxo-expresso/09-03-SUMMARY.md`: assinaturas exatas das 3 interfaces e dos 3 Unavailable, os nomes das 2 exceções, os 3 bindings adicionados, e o construtor/contrato do ResultadoEmitido. Insumo direto de 09-05 (dispara o evento), 09-08 (consome ReginParecerNotifier), 09-09 (consome SefazViabilidadeGateway) e 09-10 (consome BapRegistry + emite ResultadoEmitido → Regin).
</output>

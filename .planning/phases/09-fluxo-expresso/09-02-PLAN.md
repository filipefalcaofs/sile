---
phase: 09-fluxo-expresso
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/*_create_viability_decisions_table.php
  - database/migrations/*_create_tvl_sequences_table.php
  - database/migrations/*_add_bap_columns_to_viability_requests_table.php
  - app/Enums/DecisionOutcome.php
  - app/Models/ViabilityDecision.php
  - app/Models/ViabilityRequest.php
  - app/Models/TvlSequence.php
  - database/factories/ViabilityDecisionFactory.php
  - database/factories/TvlSequenceFactory.php
  - app/Services/Solicitacao/ViabilityRequestStateMachine.php
  - app/Services/Expresso/TvlNumberGenerator.php
  - tests/Feature/Expresso/ExpressoSchemaTest.php
  - tests/Feature/Expresso/ViabilityDecisionTest.php
  - tests/Feature/Solicitacao/ViabilityRequestStateMachineTest.php
  - tests/Feature/Expresso/TvlNumberGeneratorTest.php
  - tests/Feature/Expresso/TvlNumberGeneratorPostgisTest.php
autonomous: true
requirements: [HU-074, HU-075, HU-076, HU-078, HU-134]

must_haves:
  truths:
    - "Existe a tabela IMUTÁVEL viability_decisions (1:1 com viability_requests, unique(viability_request_id)): flow, outcome, consolidated_result, tvl_product_number (unique, nullable — só no deferimento), per_cnae jsonb, rules_versions jsonb (RN-005), fundamentacao jsonb, reason, decided_by_user_id (null=sistema), decided_at (append-only) — fonte do PDF/TVL (Fase 10) e da explicabilidade (Fase 12)"
    - "tvl_sequences (year, last_number) espelha protocol_sequences; viability_requests ganha bap_due_at e bap_linked_at nullable (relógio do BAP — dormente até o Regin)"
    - "DecisionOutcome (deferida/indeferida) é enum dedicado; ViabilityDecision tem casts/relations e é append-only (sem updates de negócio)"
    - "TvlNumberGenerator gera {prefixo}-{AAAA}-{NNNNNN} concorrência-seguro (tvl_sequences->lockForUpdate na transação; parametrizável expresso.tvl.* com default inline TVL/6) — unicidade em SQLite e concorrência real em @group postgis"
    - "ViabilityRequestStateMachine só GANHA transições (protocolada→{em_analise,deferida,indeferida,aguardando_bap}; aguardando_bap→{deferida,indeferida,em_analise}) sem tocar o protocolo — os testes da Fase 8 seguem verdes"
  artifacts:
    - path: "database/migrations"
      provides: "viability_decisions (imutável 1:1) + tvl_sequences + colunas bap_* driver-agnósticas (jsonb portável SQLite/pgsql)"
      contains: "viability_decisions"
    - path: "app/Models/ViabilityDecision.php"
      provides: "Registro imutável da decisão (append-only) com casts jsonb e relação 1:1 com a solicitação"
      contains: "class ViabilityDecision"
    - path: "app/Services/Expresso/TvlNumberGenerator.php"
      provides: "Gerador de número de produto TVL concorrência-seguro e parametrizado"
      contains: "lockForUpdate"
    - path: "app/Services/Solicitacao/ViabilityRequestStateMachine.php"
      provides: "Mapa de transições estendido com os estados de decisão (sem tocar o protocolo)"
      contains: "'aguardando_bap'"
  key_links:
    - from: "app/Services/Expresso/TvlNumberGenerator.php"
      to: "App\\Support\\Settings"
      via: "lê expresso.tvl.prefixo/.padding com default inline TVL/6 (não depende do seeder)"
      pattern: "expresso.tvl"
    - from: "app/Models/ViabilityDecision.php"
      to: "app/Models/ViabilityRequest.php"
      via: "belongsTo viabilityRequest + unique(viability_request_id) (1:1)"
      pattern: "viability_request_id"
    - from: "app/Services/Solicitacao/ViabilityRequestStateMachine.php"
      to: "tests/Feature/Solicitacao/ViabilityRequestStateMachineTest.php"
      via: "só ADICIONA entradas no mapa; protocolada→rascunho e rascunho→em_analise continuam inválidas (Fase 8 verde)"
      pattern: "protocolada"
---

<objective>
Fundação de dados e domínio da decisão (Track B da Wave 1): a tabela IMUTÁVEL `viability_decisions` (1:1 com a solicitação) que registra deferimento/indeferimento com fundamentação, versões de regras e número TVL; o `TvlNumberGenerator` concorrência-seguro (espelhando o `ProtocolNumberGenerator`); o enum `DecisionOutcome`; e as transições novas da `ViabilityRequestStateMachine` (só ADIÇÃO, sem tocar o protocolo).

Purpose: a decisão expressa precisa ser registro auditável, imutável e fonte do PDF/TVL (Fase 10) e da explicabilidade (Fase 12) — RN-005/RN-007. A persistência e o número são a "verdade gravada"; o `FluxoExpressoService` (09-05) só orquestra. Anti-fachada: o número TVL é concorrência-seguro de verdade (lockForUpdate + unique), provado em @group postgis; a geometria não entra aqui (a decisão usa o resultado dos motores, não SQL espacial), então as tabelas são portáveis (jsonb) e rodam em SQLite.
Output: 2 tabelas + 2 colunas, 1 enum, 2 models + 2 factories, `TvlNumberGenerator`, transições novas na StateMachine — cobertos por feature tests (SQLite + 1 @group postgis de concorrência).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-fluxo-expresso/09-CONTEXT.md
@docs/superpowers/specs/2026-06-14-fluxo-expresso-design.md

# PADRÕES A ESPELHAR (gerador concorrência-seguro; enum; model imutável; @group postgis) — ler antes de codar:
@app/Services/Solicitacao/ProtocolNumberGenerator.php
@app/Models/ProtocolSequence.php
@database/factories/ProtocolSequenceFactory.php
@app/Enums/ResultadoViabilidade.php
@app/Models/ViabilityRequest.php
@app/Services/Solicitacao/ViabilityRequestStateMachine.php
@tests/Feature/Solicitacao/ViabilityRequestStateMachineTest.php
@tests/Feature/Solicitacao/ProtocolNumberGeneratorPostgisTest.php
@tests/PostgisTestCase.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: migrations (viability_decisions + tvl_sequences + colunas BAP) + ExpressoSchemaTest</name>
  <read_first>app/Models/ViabilityRequest.php, database/migrations (a migration de create_viability_requests_table para os tipos de coluna jsonb portáveis)</read_first>
  <files>database/migrations/*_create_viability_decisions_table.php, database/migrations/*_create_tvl_sequences_table.php, database/migrations/*_add_bap_columns_to_viability_requests_table.php, tests/Feature/Expresso/ExpressoSchemaTest.php</files>
  <action>
    TDD: escrever `ExpressoSchemaTest` (Schema::hasTable/hasColumns) ANTES, confirmar RED, depois `php artisan make:migration --no-interaction`.

    1. `viability_decisions` (IMUTÁVEL, 1:1 com a solicitação):
       - id; viability_request_id (FK viability_requests, cascadeOnDelete, **unique** — 1:1);
       - flow (string, default 'expresso');
       - outcome (string — deferida/indeferida; NUNCA grava em_analise/pendente: esses não criam decisão);
       - consolidated_result (string — o ResultadoViabilidade consolidado: permitido/permitido_com_condicoes/nao_permitido);
       - tvl_product_number (string nullable, **unique** — preenchido SÓ no deferimento);
       - per_cnae (jsonb/json) — veredito + encaminhamento por CNAE (RN-009/explicabilidade);
       - rules_versions (jsonb/json) — versões território/louos/risco da decisão (RN-005);
       - fundamentacao (jsonb/json) — referências legais consolidadas;
       - reason (text nullable) — motivo (ex.: 'indeferido sem atuação' na HU-134);
       - decided_by_user_id (FK users nullable — **null = sistema**);
       - decided_at (timestamp);
       - timestamps.
       - Usar `json` (portável SQLite/pgsql) como nas tabelas da Fase 8 — NÃO há geometria aqui.
    2. `tvl_sequences` (espelha protocol_sequences): id; year (integer, unique); last_number (unsignedBigInteger default 0); timestamps.
    3. `add_bap_columns_to_viability_requests`: adicionar `bap_due_at` (timestamp nullable) e `bap_linked_at` (timestamp nullable) à `viability_requests` (relógio do BAP — HU-134; dormente até o Regin alimentar). Migration aditiva, sem tocar colunas existentes (Pitfall: ao alterar coluna repetir atributos — aqui só ADICIONA).

    `ExpressoSchemaTest`: Schema::hasTable das 2 novas + hasColumns dos campos críticos (viability_request_id/outcome/tvl_product_number/per_cnae/rules_versions/decided_at em viability_decisions; year/last_number em tvl_sequences; bap_due_at/bap_linked_at em viability_requests). Asserir o índice unique de viability_request_id e de tvl_product_number (via tentativa de duplicar OU listagem de índices — preferir provar 1:1 num teste de model na Task 2).
  </action>
  <verify>php artisan test --compact --filter=ExpressoSchemaTest</verify>
  <acceptance_criteria>
    - php artisan migrate:fresh roda limpo em SQLite (json portável, sem geometry).
    - grep "unique" na migration de viability_decisions (viability_request_id E tvl_product_number).
    - grep "bap_due_at" na migration de colunas BAP.
    - ExpressoSchemaTest verde.
  </acceptance_criteria>
  <done>2 tabelas + 2 colunas BAP (portáveis jsonb), unicidade 1:1 e do TVL; schema test verde em SQLite.</done>
</task>

<task type="auto">
  <name>Task 2: DecisionOutcome + ViabilityDecision/TvlSequence (models+factories) + transições da StateMachine</name>
  <read_first>app/Enums/ResultadoViabilidade.php, app/Models/ViabilityRequest.php, app/Services/Solicitacao/ViabilityRequestStateMachine.php, tests/Feature/Solicitacao/ViabilityRequestStateMachineTest.php</read_first>
  <files>app/Enums/DecisionOutcome.php, app/Models/ViabilityDecision.php, app/Models/TvlSequence.php, database/factories/ViabilityDecisionFactory.php, database/factories/TvlSequenceFactory.php, app/Services/Solicitacao/ViabilityRequestStateMachine.php, app/Models/ViabilityRequest.php, tests/Feature/Expresso/ViabilityDecisionTest.php, tests/Feature/Solicitacao/ViabilityRequestStateMachineTest.php</files>
  <action>
    TDD estrito (RED→GREEN para cada peça).

    1. `DecisionOutcome` (string enum + label()): Deferida='deferida', Indeferida='indeferida'. label() pt-BR ('Deferida'/'Indeferida'). Helper `fromResultado(ResultadoViabilidade $r): ?self` opcional (permitido/permitido_com_condicoes→Deferida; nao_permitido→Indeferida; pendente→null) — ou deixar a regra no service (09-05). Manter mínimo: só os 2 casos + label.
    2. `ViabilityDecision` (model IMUTÁVEL — NÃO usa HasAuditoria; a auditoria da decisão é SÍNCRONA no service, HU-078):
       - `$fillable` com os campos da gravação única (viability_request_id, flow, outcome, consolidated_result, tvl_product_number, per_cnae, rules_versions, fundamentacao, reason, decided_by_user_id, decided_at).
       - casts(): outcome→DecisionOutcome, per_cnae→array, rules_versions→array, fundamentacao→array, decided_at→datetime.
       - relations: viabilityRequest() belongsTo; decidedBy() belongsTo(User) nullable.
       - helpers de leitura: `isDeferida(): bool` (outcome===Deferida).
    3. `TvlSequence` (#[Fillable] year/last_number) — espelha ProtocolSequence.
    4. `ViabilityRequest`: adicionar a relação `decision(): HasOne` (ViabilityDecision) e os casts de bap_due_at/bap_linked_at→datetime + incluir bap_* no fillable do domínio (escritos pela HU-134/Regin futuro, não pelo cidadão — mas como já há forceFill no fluxo, manter fora do fillable e gravar via forceFill no 09-10; decisão à discrição, documentar). Edição mínima, sem quebrar a Fase 8.
    5. Factories: `ViabilityDecisionFactory` definition() = deferida (flow 'expresso', outcome Deferida, consolidated_result 'permitido', tvl_product_number 'TVL-'.year.'-000001', per_cnae/rules_versions/fundamentacao arrays plausíveis, decided_by_user_id null, decided_at now) + states `indeferida()` (outcome Indeferida, consolidated_result 'nao_permitido', tvl_product_number null) e `semAtuacaoBap()` (indeferida, reason 'indeferido sem atuação'). `TvlSequenceFactory` simples. Associar a uma ViabilityRequest::factory()->protocoled() por default.
    6. `ViabilityRequestStateMachine`: ADICIONAR ao mapa `TRANSITIONS` (sem remover nada):
       - `'protocolada' => ['cancelada', 'em_analise', 'deferida', 'indeferida', 'aguardando_bap']` (era só `['cancelada']`).
       - `'aguardando_bap' => ['deferida', 'indeferida', 'em_analise']` (entrada nova).
       - Manter `'rascunho' => ['protocolada', 'cancelada']` intacto. NÃO adicionar saídas para rascunho→em_analise (a Fase 8 trava isso).

    Testes:
    - `ViabilityDecisionTest` (SQLite): factory deferida cria com TVL + casts array; state indeferida sem TVL; **1:1** — criar duas decisions para a mesma request lança QueryException (unique). isDeferida() correto.
    - `ViabilityRequestStateMachineTest`: ACRESCENTAR testes novos sem apagar os da Fase 8 — protocolada→deferida/indeferida/em_analise/aguardando_bap válidas e auditadas; aguardando_bap→deferida/indeferida/em_analise válidas; aguardando_bap→protocolada inválida. CONFIRMAR que os testes existentes (protocolada→rascunho inválida; rascunho→em_analise inválida) seguem verdes.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="ViabilityDecisionTest|ViabilityRequestStateMachineTest"</verify>
  <acceptance_criteria>
    - grep "enum DecisionOutcome" app/Enums/DecisionOutcome.php (2 casos + label).
    - grep "'aguardando_bap'" app/Services/Solicitacao/ViabilityRequestStateMachine.php (mapa estendido).
    - grep "decision" (HasOne) app/Models/ViabilityRequest.php.
    - ViabilityDecisionTest verde (1:1, TVL só deferida, casts); ViabilityRequestStateMachineTest verde (novas transições + os antigos intactos).
  </acceptance_criteria>
  <done>Enum DecisionOutcome; ViabilityDecision imutável 1:1 + factory; TvlSequence; StateMachine só ESTENDIDA (Fase 8 verde).</done>
</task>

<task type="auto">
  <name>Task 3: TvlNumberGenerator (unicidade SQLite + concorrência @group postgis)</name>
  <read_first>app/Services/Solicitacao/ProtocolNumberGenerator.php, tests/Feature/Solicitacao/ProtocolNumberGeneratorPostgisTest.php, app/Support/Settings.php</read_first>
  <files>app/Services/Expresso/TvlNumberGenerator.php, tests/Feature/Expresso/TvlNumberGeneratorTest.php, tests/Feature/Expresso/TvlNumberGeneratorPostgisTest.php</files>
  <action>
    TDD estrito. `TvlNumberGenerator::generate(?int $year = null): string` — ESPELHA o ProtocolNumberGenerator:
    - `$year ??= (int) now()->year;`
    - `DB::transaction`: `TvlSequence::query()->where('year',$year)->lockForUpdate()->first() ?? ::create(['year'=>$year,'last_number'=>0])`; `increment('last_number')`; retorna o número.
    - Parametrização HU-014 com DEFAULT INLINE (não depende do seeder 09-01): `$prefix = (string) Settings::get('expresso.tvl.prefixo', config('sile.expresso.tvl.prefixo', 'TVL'));` e `$padding = (int) Settings::get('expresso.tvl.padding', config('sile.expresso.tvl.padding', 6));`. `sprintf('%s-%d-%s', $prefix, $year, str_pad((string)$number, $padding, '0', STR_PAD_LEFT))`.
    - O gerador NÃO grava em viability_decisions — só sequencia; o caller (09-05) usa o número dentro da transação da decisão; `unique(tvl_product_number)` é a defesa final.

    `TvlNumberGeneratorTest` (SQLite, RefreshDatabase):
    - test_gera_no_formato_parametrizado: generate(2026)==='TVL-2026-000001'; segundo==='...000002'.
    - test_reinicia_sequencia_por_ano.
    - test_respeita_prefixo_e_padding_parametrizados: gravar Parameter expresso.tvl.prefixo='TVLX' e .padding='4' (limpar cache do Settings) → 'TVLX-2026-0001'. (Gravar Parameter direto no teste — não depender de plano.)
    - test_numeros_unicos_em_sequencia (50 → count unique 50).

    `TvlNumberGeneratorPostgisTest extends PostgisTestCase` (#[Group('postgis')] herdado): test_concorrencia_nao_duplica_sob_lock — espelhar o ProtocolarConcorrenciaPostgisTest (duas gerações sob o lock avançam a sequência sem buraco; asserir last_number==2 e dois números distintos; assertSame('pgsql', DB::connection()->getDriverName())).
  </action>
  <verify>php artisan test --compact --filter=TvlNumberGeneratorTest && php artisan test --compact --group=postgis --filter=TvlNumberGeneratorPostgisTest</verify>
  <acceptance_criteria>
    - grep "lockForUpdate" app/Services/Expresso/TvlNumberGenerator.php.
    - grep "expresso.tvl.prefixo" app/Services/Expresso/TvlNumberGenerator.php.
    - TvlNumberGeneratorTest verde (formato/ano/prefixo+padding/unicidade).
    - TvlNumberGeneratorPostgisTest verde (container de pé) ou skip honesto (FALHA com POSTGIS_TESTS_REQUIRED=true).
  </acceptance_criteria>
  <done>Gerador TVL concorrência-seguro e parametrizado (default inline TVL/6); unicidade SQLite + concorrência @group postgis.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-076 RN-007 — número TVL no deferimento | TvlNumberGeneratorTest::test_gera_no_formato_parametrizado |
| HU-076 — TVL concorrência-segura | TvlNumberGeneratorPostgisTest::test_concorrencia_nao_duplica_sob_lock |
| HU-076/078 RN-005 — registro imutável com versões/por-CNAE | ViabilityDecisionTest (casts array, 1:1, TVL só deferida) |
| HU-074/075 — estados de decisão na máquina | ViabilityRequestStateMachineTest (protocolada→deferida/indeferida; aguardando_bap→...) |
| HU-134 — relógio BAP (colunas) | ExpressoSchemaTest (bap_due_at/bap_linked_at) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- Suíte SQLite migra limpo (json portável; sem geometry). NUNCA `migrate:fresh --env=testing` (rodaria no Postgres de dev).
- `php artisan test --compact --filter="ExpressoSchemaTest|ViabilityDecisionTest|ViabilityRequestStateMachineTest|TvlNumberGeneratorTest"` verde.
- `php artisan test --compact --group=postgis --filter=TvlNumberGeneratorPostgisTest` verde (container) ou skip honesto.
- Suíte completa CONTINUA verde — a Fase 8 (StateMachine/Protocolo) não regrediu (transições só ADICIONADAS).
</verification>

<success_criteria>
- viability_decisions imutável 1:1 (TVL único só no deferimento; per_cnae/rules_versions/fundamentacao jsonb) + tvl_sequences + colunas BAP.
- DecisionOutcome; TvlNumberGenerator concorrência-seguro e parametrizado; StateMachine ESTENDIDA sem regredir a Fase 8.
- ZERO dependência nova; sem fachada (concorrência real provada em @group postgis).
</success_criteria>

<output>
Criar `.planning/phases/09-fluxo-expresso/09-02-SUMMARY.md`: nomes EXATOS de tabelas/colunas, do enum DecisionOutcome, dos models (casts/relations, incl. ViabilityRequest::decision()), das factories e states, a assinatura de TvlNumberGenerator::generate e o mapa FINAL da StateMachine (entradas novas). Insumo direto de 09-05 (cria a decisão+TVL+transição), 09-07/08/09 (carregam a ViabilityDecision), 09-10 (aguardando_bap→indeferida) e 09-11 (exibe a decisão).
</output>

# PLAN 14-03 — Onda 2: Síntese (HU-116 a 119)

> Onda 2 do AI-SPEC. Reusa a fundação de execução da Onda 1 (`RunAiAgentJob` + `AiSuggestion` + `AiFeatureGate` + guardrails + auditoria). Funções de GERAÇÃO DE TEXTO (sem visão), todas como SUGESTÃO revisável — nunca decisão (Failure Mode #1). Testes com fake do SDK.

## Objetivo
Acelerar a comunicação: resumir a solicitação para o cidadão (pré-protocolo) e o processo para o analista, sugerir minuta de parecer (não-decisão) e explicar o resultado ao cidadão em linguagem acessível.

## Critério de pronto (anti-fachada)
- Cada função: Agent (`app/Ai/Agents`) com saída estruturada + Job (subclasse de `RunAiAgentJob`, `capability='text'`, sem attachments) + Service (gate `features.ia_*`). Toggle OFF ⇒ não chama provedor.
- **Citação de fonte obrigatória** no schema (resumo/parecer/explicação); sem fonte ⇒ escala (guardrail já no base).
- **HU-118 NÃO-DECISÃO (crítico):** a minuta NUNCA é gravada como decisão; a fundamentação vem do `engine_snapshot`/motor (não inventa artigo/quadro); sem motor/fonte ⇒ não sugere (escala). Entra na ficha como rascunho EDITÁVEL (a ficha finalizada é imutável — RN-003).
- **HU-119 acessibilidade:** linguagem cidadã, eMAG/WCAG; fiel à decisão registrada (HU-099); não promete o que a lei não garante.
- Minimização de PII no input ao provedor. Auditoria RN-002. Testes `#[Group('ia')]` com fake. tsc/build verdes.

## Dependências
- Onda 1 (fundação + RunAiAgentJob + AiSuggestion). Toggles `features.ia_resumo` (116/117), `features.ia_parecer` (118), `features.ia_explicacao` (119) — já existem (Onda 0).
- Dados: ficha de análise (Fase 10 `AnalysisRecord`/`AnalysisRecordController` — `engine_snapshot`/per_cnae/inconsistências da Onda 1/precedentes HU-142); `ViabilityDecision` + explicabilidade (HU-099); solicitação (Fase 8); portal (wizard `portal/solicitacoes/*`, protocolo).
- Enum `AiSuggestionType` precisa de: `ResumoSolicitacao`, `ResumoProcesso`, `Parecer`, `Explicacao` (adicionar se faltarem — verificar `app/Enums/AiSuggestionType.php`).

## Tasks (TDD — teste primeiro)

### Task A1 — HU-117 Resumo para o analista (ficha)
- `ResumoProcessoAgent` (schema `{ resumo: string, pontos_chave?: array<string>, fonte: string(req) }`); `ResumoProcessoJob` (`function='resumo_processo'`, type `ResumoProcesso`); `ResumoProcessoService` (gate `ia_resumo`). Input: síntese do motor/per_cnae + inconsistências (Onda 1) + pendências + precedentes — MINIMIZAR PII.
- Integração: prop `sugestoesIa`/seção de resumo no topo da ficha (`gestao/ficha-analise/show.tsx`) via `Inertia::optional` (reusa o padrão da Onda 1). Read-only. TDD com fake.

### Task A2 — HU-118 Sugerir minuta de parecer (ficha) — NÃO-DECISÃO
- `SugestaoParecerAgent` (schema `{ minuta: string(req), fundamentacao: string(req — citando o motor/LOUOS real), confianca: enum, fonte: string(req) }`; instructions: NÃO decidir desfecho; fundamentação vem do `engine_snapshot`; nunca inventar artigo/quadro). `SugestaoParecerJob` (`function='parecer'`, type `Parecer`). `SugestaoParecerService` (gate `ia_parecer`; sem `engine_snapshot`/fonte ⇒ não despacha/escala).
- Integração: botão **"Sugerir minuta"** no card "Parecer técnico" da ficha (ao lado de "Inserir texto-padrão"), só com `editavel`; preenche o `textarea parecer` como RASCUNHO editável (nunca finaliza, nunca grava decisão). A sugestão fica em `ai_suggestions` (status sugerida/escalada). TDD: minuta marcada como sugestão; sem motor → escala; nunca grava em `AnalysisRecord`/`ViabilityDecision`.

### Task B1 — HU-116 Resumo da solicitação (portal, pré-protocolo)
- `ResumoSolicitacaoAgent` (schema `{ resumo: string, fonte: string(req) }`); `ResumoSolicitacaoJob` (`function='resumo_solicitacao'`? — usa toggle `ia_resumo`; type `ResumoSolicitacao`); `ResumoSolicitacaoService` (gate `ia_resumo`). Input: dados declarados (empresa/CNAEs/imóvel) — sem CPF desnecessário. Nunca afirma desfecho.
- Integração: etapa de revisão do wizard (`portal/solicitacoes/wizard.tsx` ou equivalente) — sugestão de conferência. TDD com fake.

### Task B2 — HU-119 Explicar resultado ao cidadão (portal) — acessível
- `ExplicacaoCidadaoAgent` (schema `{ explicacao: string(req), fonte: string(req) }`; instructions: traduzir a decisão técnica para linguagem cidadã clara; fiel à decisão/fundamentação registradas (HU-099); não prometer). `ExplicacaoCidadaoJob` (`function='explicacao'`, type `Explicacao`). `ExplicacaoCidadaoService` (gate `ia_explicacao`; só quando há `ViabilityDecision`).
- Integração: card no portal/protocolo (`portal/solicitacoes/protocolo.tsx`) quando há decisão — versão cidadã da explicabilidade. Acessível (eMAG/WCAG), flag "sugestão". TDD com fake.

## Verificação
- `php artisan test --compact --group ia` verde; filtros das novas funções verdes; `--exclude-group postgis` sem regressão; `npx tsc --noEmit`/`npm run build` verdes; `vendor/bin/pint` nos arquivos.

## Bloqueios/observações (registrar, nunca simular)
- VALIDAÇÃO real (chamada ao provedor) já provada na fundação (texto); calibração de prompts (resumo/parecer/explicação) = datasets/rubrica SEDUR (golden provisórios até lá).
- HU-118 é Failure Mode #1 — testes de guardrail de não-decisão são prioritários.
- Acessibilidade da HU-119 (cidadão) pode pedir parecer do `especialista-acessibilidade` na verificação.

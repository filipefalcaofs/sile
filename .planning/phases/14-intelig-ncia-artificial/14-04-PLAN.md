# PLAN 14-04 — Onda 3: Assistentes (HU-120, HU-121) + RAG/pgvector

> Onda 3 (final) do AI-SPEC. Assistentes conversacionais com RAG. O `SimilaritySearch`/pgvector do `laravel/ai` v0.8.1 está INCOMPLETO (`whereVectorSimilarTo` inexistente) ⇒ busca por similaridade é DIY (extensão pgvector + SQL cosine). Reusa a fundação das Ondas 1-2.

## Objetivo
- **HU-120 Assistente do cidadão:** orienta o preenchimento/dúvidas no portal — NUNCA decide viabilidade, escala quando incerto.
- **HU-121 Assistente do analista:** apoia a busca de regras (LOUOS/HU-099) e precedentes (HU-142) na retaguarda — cita a fonte, nunca inventa fundamentação.

## Critério de pronto (anti-fachada)
- Assistentes atrás de toggle `features.ia_assistente`; OFF/sem provedor ⇒ indisponível (não chama). RAG só sobre dado REAL (precedentes/regras/contexto do próprio processo); resposta sem fonte ⇒ não responde/escala.
- **pgvector DIY:** migration driver-aware (pgsql: `CREATE EXTENSION IF NOT EXISTS vector` + coluna `vector` + índice cosine; sqlite/teste: coluna json, sem índice). Embeddings via `Laravel\Ai\Embeddings::for()` (SDK real); busca por similaridade via SQL próprio (`<=>`). Embeddar conteúdo ANONIMIZADO (LGPD).
- Conversa persistida (`RemembersConversations` + migration `agent_conversations` do SDK via `vendor:publish --tag=ai-migrations`).
- Não-decisão (cidadão) / citação de fonte (analista). Auditoria RN-002. Testes com fake; busca vetorial `#[Group('postgis')]` (skip em sqlite, espelha o padrão geoespacial). tsc/build verdes.

## Dependências
- Ondas 0-2 (fundação `RunAiAgentJob`/`AiSuggestion`/`AiFeatureGate`/embeddings config). Toggle `features.ia_assistente` (já existe). Precedentes (HU-142), regras LOUOS (HU-099/EP05), `ViabilityRequest`/trilha.
- PostgreSQL com extensão `pgvector` (imagem `postgis-pgvector` — confirmar `CREATE EXTENSION` no banco de teste pgsql; em produção é gate de infra/Portainer).
- API real do SDK: `Embeddings::for([...])->generate()->first()` (vetor); `RemembersConversations` (NÃO definir `messages()` junto — precedência quebra histórico); streaming `stream()` síncrono no request (UX). Fake por agente.

## Tasks (TDD)

### Task A — Infra de RAG (pgvector DIY)
- Migration driver-aware `ai_precedent_embeddings` (ou coluna em tabela de precedentes): `pgsql` → extensão vector + `vector(N)` + índice `hnsw (... vector_cosine_ops)`; `sqlite` → `json`/`text`. up/down validados ISOLADOS (`--path`).
- `app/Services/Ai/Embeddings/PrecedentIndexer.php`: gera embedding (via `Embeddings::for` do SDK, capability embeddings, gate de config ativa) do RESUMO ANONIMIZADO do precedente; persiste o vetor. Idempotente. Comando `ai:indexar-precedentes` (evidência).
- `app/Services/Ai/Embeddings/PrecedentVectorSearch.php`: busca os K precedentes mais similares por SQL cosine (`ORDER BY embedding <=> :q LIMIT :k`) no pgsql; em sqlite degrada (sem índice — fallback honesto ou skip). 
- TDD: indexação gera vetor (fake do Embeddings); busca por similaridade `#[Group('postgis')]` (skip sqlite) retorna ordenado; sem config de embeddings ⇒ indisponível (não chama).

### Task B — HU-121 Assistente do analista (RAG)
- `app/Ai/Agents/AssistenteAnalistaAgent.php` (`RemembersConversations`): instructions: segunda opinião assistida; cita regra/precedente REAL recuperado; sem fonte ⇒ não responde. Tools/contexto: `PrecedentVectorSearch` (precedentes), regras LOUOS (HU-099), trilha do processo — só leitura.
- `AssistenteAnalistaService` + controller/rota (retaguarda, gate `ia_assistente` + permissão de análise). Streaming síncrono OU resposta única; conversa persistida. Auditoria RN-002.
- UI: painel/chat na ficha/processo (gestão). Acessível, pt-BR, flag "sugestão".
- TDD com fake: responde citando precedente recuperado; incerto/sem fonte ⇒ escala/orienta; toggle off ⇒ indisponível.

### Task C — HU-120 Assistente do cidadão
- `app/Ai/Agents/AssistenteCidadaoAgent.php` (`RemembersConversations`): orienta preenchimento/dúvidas; NUNCA decide viabilidade nem afirma desfecho; incerto ⇒ encaminha atendimento. RAG sobre conhecimento público/requisitos (HU-067) + contexto do próprio cidadão.
- `AssistenteCidadaoService` + controller/rota (portal, gate `ia_assistente`). Conversa persistida. 
- UI: widget no portal (wizard/painel). ACESSÍVEL (eMAG/WCAG), pt-BR.
- TDD com fake: orienta sem decidir; incerto ⇒ encaminha; toggle off ⇒ indisponível.

## Verificação
- `php artisan test --compact --group ia` verde; busca vetorial `--group postgis` verde (pgsql) ou skip honesto (sqlite); tsc/build/pint verdes.
- `ai:indexar-precedentes` + uma busca real (evidência), se o pgsql de teste tiver pgvector.

## Bloqueios/observações (registrar, nunca simular)
- **pgvector em PRODUÇÃO:** extensão precisa estar habilitada no PostgreSQL de produção (imagem `postgis-pgvector` já inclui; confirmar no deploy/Portainer) — gate de infra.
- Streaming conversacional do SDK v0.x: se `stream()`/`RemembersConversations` apresentar imaturidade, degradar para resposta única (não-streaming) — registrar, nunca fingir.
- Validação real (embeddings + chat) com o provedor cadastrado = smoke; calibração com datasets SEDUR.

---
phase: 10-analise-tecnica-sedur
plan: 15
type: execute
wave: 7
depends_on: ["10-09", "10-10", "10-11", "10-12", "10-13"]
files_modified:
  - app/Http/Controllers/Gestao/ProcessoDecisaoController.php
  - app/Http/Controllers/Gestao/ProcessoPendenciaController.php
  - app/Http/Controllers/Gestao/MalhaFinaController.php
  - app/Http/Controllers/Gestao/TvlDocumentController.php
  - app/Http/Requests/Gestao/AbrirPendenciaRequest.php
  - app/Http/Requests/Gestao/EncaminharMalhaFinaRequest.php
  - routes/gestao.php
  - tests/Feature/Analise/ProcessoDecisaoEndpointTest.php
  - tests/Feature/Analise/ProcessoPendenciaEndpointTest.php
  - tests/Feature/Analise/MalhaFinaEndpointTest.php
  - tests/Feature/Analise/TvlDocumentEndpointTest.php
autonomous: true
requirements: [HU-085, HU-086, HU-087, HU-088, HU-089, HU-083, HU-136, HU-132]

must_haves:
  truths:
    - "Os endpoints gestao das ações do analista wireiam os serviços das Waves 5/6 (thin controllers): decidir (deferir/indeferir/encerrar via AnaliseTecnicaDecisionService, gate analisar-processos), abrir pendência (PendenciaService::abrir, gate analisar-processos), encaminhar malha fina single+lote (MalhaFinaService, gate encaminhar-malha-fina), emitir/baixar TVL (TvlPdfService + URL temporária assinada, gate emitir-tvl)"
    - "HU-132: download do TVL por temporarySignedRoute (TTL analise.tvl.download.ttl_minutos) servindo o arquivo do disco NÃO público via streaming — nunca URL pública; emitir cria/audita tvl_documents (reuso do serviço)"
    - "Cada endpoint valida permissão (403 auditado — CA-04), valida o estado do processo (decidir exige ficha finalizada; abrir pendência exige em_analise) e audita; ÚNICO editor de routes/gestao.php na Wave 7"
    - "Os controllers NÃO reimplementam regra — só orquestram os serviços já testados (10-10/10-11/10-12/10-13); a decisão dispara ResultadoEmitido (Regin/SEFAZ bloqueados → Fase 13)"
  artifacts:
    - path: "app/Http/Controllers/Gestao/ProcessoDecisaoController.php"
      provides: "Endpoints deferir/indeferir/encerrar (via AnaliseTecnicaDecisionService), gate analisar-processos"
      contains: "AnaliseTecnicaDecisionService"
    - path: "app/Http/Controllers/Gestao/TvlDocumentController.php"
      provides: "Emitir TVL (emitir-tvl) + download por URL temporária assinada (disco não público)"
      contains: "temporarySignedRoute"
  key_links:
    - from: "app/Http/Controllers/Gestao/ProcessoDecisaoController.php"
      to: "app/Services/Analise/AnaliseTecnicaDecisionService.php"
      via: "decide(record, analista) — controller fino, só orquestra (regra testada em 10-10)"
      pattern: "->decide("
    - from: "app/Http/Controllers/Gestao/TvlDocumentController.php"
      to: "app/Services/Analise/TvlPdfService.php"
      via: "emitir → TvlPdfService::generate; download → Storage do disco não público por URL assinada"
      pattern: "TvlPdfService"
---

<objective>
Backend das ações do console (Wave 7, gestao route-owner): os endpoints HTTP finos que expõem os serviços das Waves 5/6 — decidir (deferir/indeferir/encerrar), abrir pendência (analista), encaminhar malha fina (single+lote), emitir/baixar TVL. ÚNICO editor de `routes/gestao.php` na Wave 7.

Purpose: as regras já estão prontas e testadas em serviços (10-10 decisão, 10-11 pendência, 10-12 malha fina, 10-13 TVL); aqui só falta a casca HTTP gated/auditada que as telas (10-16/10-17) acionam. Concentrar todas as rotas de ação num plano mantém o ÚNICO editor de routes/gestao.php por wave e deixa as páginas (10-16/10-17) file-disjuntas (consomem os nomes de rota). O download do TVL por URL temporária assinada (disco não público) fecha a HU-132 com segurança real.
Output: 4 controllers finos + requests + rotas, cobertos por feature tests (gates 403, orquestração dos serviços, validações de estado, URL assinada do TVL).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md
@.planning/phases/10-analise-tecnica-sedur/10-09-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-10-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-11-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-12-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-13-SUMMARY.md

# PADRÕES A ESPELHAR (controller fino + permissão + request; download por URL assinada/streaming; rota gestão) — ler antes de codar:
@app/Http/Controllers/Gestao/ResultadoExpressoController.php
@routes/gestao.php
@app/Http/Controllers/Gestao/CaixaSetorController.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: decisão (deferir/indeferir/encerrar) + abrir pendência (analista) — endpoints + rotas</name>
  <read_first>app/Services/Analise/AnaliseTecnicaDecisionService.php, app/Services/Analise/PendenciaService.php, routes/gestao.php</read_first>
  <files>app/Http/Controllers/Gestao/ProcessoDecisaoController.php, app/Http/Controllers/Gestao/ProcessoPendenciaController.php, app/Http/Requests/Gestao/AbrirPendenciaRequest.php, routes/gestao.php, tests/Feature/Analise/ProcessoDecisaoEndpointTest.php, tests/Feature/Analise/ProcessoPendenciaEndpointTest.php</files>
  <action>
    TDD estrito (RED primeiro). Rotas em `routes/gestao.php` (ÚNICO route-owner da Wave 7), prefixo `processos/{viabilityRequest}`:
    - POST `processos/{viabilityRequest}/decidir` → ProcessoDecisaoController (gate `permission:analisar-processos`): resolve a ficha finalizada (currentAnalysisRecord) e chama `AnaliseTecnicaDecisionService::decide(record, $analista)`; o outcome (defere/indefere) sai da ficha (RN-004 — HU-086/087/088); encerramento (HU-089) é a própria transição final. Trata DomainException (ficha rascunho → 422 com mensagem).
    - POST `processos/{viabilityRequest}/pendencias` → ProcessoPendenciaController@store (gate `permission:analisar-processos`): `AbrirPendenciaRequest` valida descricao; chama `PendenciaService::abrir($request, $analista, $descricao)`.

    Controllers FINOS (sem regra — só orquestram + auditam o acesso/erro). 403 auditado sem permissão.

    `ProcessoDecisaoEndpointTest` (SQLite + fake SpatialRepository):
    - sem analisar-processos → 403; com ficha finalizada (todas deferidas) → 200/redirect, status deferida + ViabilityDecision (flow analise_tecnica) + ResultadoEmitido (Event::fake).
    - ficha rascunho → 422 (não decide).
    `ProcessoPendenciaEndpointTest` (SQLite):
    - abrir pendência (em_analise) → status em_pendencia + analysis_pendencies; sem permissão → 403; fora de em_analise → 422.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="ProcessoDecisaoEndpointTest|ProcessoPendenciaEndpointTest"</verify>
  <acceptance_criteria>
    - grep "processos/{viabilityRequest}/decidir" e "pendencias" em routes/gestao.php sob analisar-processos.
    - grep "AnaliseTecnicaDecisionService" no ProcessoDecisaoController (controller fino).
    - testes verdes (403, decidir defere/indefere, bloqueio rascunho, abrir pendência).
  </acceptance_criteria>
  <done>Endpoints de decisão (HU-086/087/088/089) e de abrir pendência (HU-083) — controllers finos orquestrando os serviços, gated e auditados.</done>
</task>

<task type="auto">
  <name>Task 2: malha fina (encaminhar single+lote) + emitir/baixar TVL (URL assinada) — endpoints + rotas</name>
  <read_first>app/Services/Analise/MalhaFinaService.php, app/Services/Analise/TvlPdfService.php, routes/gestao.php</read_first>
  <files>app/Http/Controllers/Gestao/MalhaFinaController.php, app/Http/Controllers/Gestao/TvlDocumentController.php, app/Http/Requests/Gestao/EncaminharMalhaFinaRequest.php, routes/gestao.php, tests/Feature/Analise/MalhaFinaEndpointTest.php, tests/Feature/Analise/TvlDocumentEndpointTest.php</files>
  <action>
    TDD estrito. Rotas em `routes/gestao.php`:
    - POST `processos/malha-fina` → MalhaFinaController@store (gate `permission:encaminhar-malha-fina`): `EncaminharMalhaFinaRequest` valida request_ids (array — single ou lote da consulta, HU-136 RN-004) + motivo (required); chama MalhaFinaService::encaminhar/encaminharLote. Funciona em qualquer status (RN-001).
    - POST `processos/{viabilityRequest}/tvl` → TvlDocumentController@store (gate `permission:emitir-tvl`): resolve a ViabilityDecision deferida do processo; chama TvlPdfService::generate; retorna o TvlDocument (com link de download). Bloqueio FA-01 (não deferida → 422).
    - GET `processos/tvl/{tvlDocument}/download` → TvlDocumentController@download por `temporarySignedRoute` (gate `permission:emitir-tvl` + `signed`): valida assinatura/TTL (analise.tvl.download.ttl_minutos) e faz streaming do arquivo do disco NÃO público (Storage::disk(...)->download) — nunca expõe URL pública.

    `MalhaFinaEndpointTest` (SQLite): sem encaminhar-malha-fina → 403; encaminhar single (em_analise) e lote (3, incl. um deferido — RN-001) com motivo → fine_mesh_referrals criados + in_fine_mesh; motivo vazio → 422.
    `TvlDocumentEndpointTest` (SQLite, Storage::fake): sem emitir-tvl → 403; emitir sobre deferida → 200 + tvl_documents (auditado); sobre indeferida → 422 (FA-01); download com URL assinada válida → 200 streaming; URL não assinada/expirada → 403.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="MalhaFinaEndpointTest|TvlDocumentEndpointTest"</verify>
  <acceptance_criteria>
    - grep "malha-fina" e "tvl" em routes/gestao.php (gates encaminhar-malha-fina / emitir-tvl); grep "temporarySignedRoute" no TvlDocumentController.
    - MalhaFinaEndpointTest verde (single/lote/deferido/motivo) e TvlDocumentEndpointTest verde (403, FA-01, download assinado).
  </acceptance_criteria>
  <done>Endpoints de malha fina (HU-136 single+lote) e TVL (HU-132 emitir + download por URL temporária assinada do disco não público), gated e auditados.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-086/087/088/089 — decidir/encerrar | ProcessoDecisaoEndpointTest |
| HU-083 — abrir pendência (analista) | ProcessoPendenciaEndpointTest |
| HU-136 RN-001/004 — malha fina qualquer status + lote | MalhaFinaEndpointTest |
| HU-132 CA-01/FA-01/RN-002 — emitir só deferida + auditoria | TvlDocumentEndpointTest |
| HU-132 — download por URL assinada (não público) | TvlDocumentEndpointTest (signed/expirada) |
| CA-04 — segurança de acesso (gates) | todos (403 por permissão) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="ProcessoDecisaoEndpointTest|ProcessoPendenciaEndpointTest|MalhaFinaEndpointTest|TvlDocumentEndpointTest"` verde.
- Suíte completa CONTINUA verde; ÚNICO editor de routes/gestao.php na Wave 7 (10-16/10-17 só páginas).
</verification>

<success_criteria>
- Endpoints finos das ações do analista (decidir/encerrar, abrir pendência, malha fina single+lote, emitir/baixar TVL) gated por permissão e auditados, orquestrando os serviços já testados.
- TVL por URL temporária assinada do disco não público (HU-132); decisão dispara ResultadoEmitido (Regin/SEFAZ bloqueados → Fase 13).
- ÚNICO route-owner da Wave 7; ZERO regra reimplementada; ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-15-SUMMARY.md`: as rotas de ação (processos.decidir/pendencias/malha-fina/tvl/tvl.download), os gates por permissão e o esquema do download assinado do TVL. Insumo direto de 10-16 (botões da consulta/detalhe: malha fina lote, Cmd+K) e 10-17 (ficha: decidir/encerrar, abrir pendência, emitir/baixar TVL).
</output>

---
phase: 10-analise-tecnica-sedur
plan: 09
type: execute
wave: 4
depends_on: ["10-02", "10-06", "10-08"]
files_modified:
  - app/Services/Analise/AnalysisRecordService.php
  - app/Services/Analise/AnalysisRecordDiff.php
  - app/Http/Controllers/Gestao/AnalysisRecordController.php
  - app/Http/Controllers/Gestao/PrecedenteController.php
  - app/Http/Requests/Gestao/AnalysisRecordRequest.php
  - app/Http/Resources/AnalysisRecordResource.php
  - routes/gestao.php
  - tests/Feature/Analise/AnalysisRecordAutosaveTest.php
  - tests/Feature/Analise/AnalysisRecordFinalizeTest.php
  - tests/Feature/Analise/AnalysisRecordRevisionDiffTest.php
  - tests/Feature/Analise/PrecedenteEndpointTest.php
autonomous: true
requirements: [HU-135, HU-140, HU-142, HU-085]

must_haves:
  truths:
    - "HU-135: a ficha abre a partir da revisão pré-analisada (10-08); o analista preenche por CNAE (status escolhido × sugerido, condicionantes, vagas), salva rascunho por autosave (PATCH, debounce config) e registra parecer (lê textos-padrão ativos) — gated por analisar-processos"
    - "HU-135 RN-003: ao FINALIZAR, a revisão fica IMUTÁVEL (status finalizada) — autosave/edição numa revisão finalizada é bloqueado (422); recalcular/editar depois = NOVA revisão (status rascunho, copiada da anterior)"
    - "HU-140 divergências: ao finalizar, cada campo onde o valor FINAL diverge do SUGERIDO pelo motor gera analysis_divergences (cnae, field, suggested_value, final_value, justification) — insumo do HU-145"
    - "HU-135 RN-007 diff: comparação entre 2 revisões (campos, condicionantes, status por CNAE, parecer) — o que mudou; HU-142 precedentes endpoint serve o painel da ficha (PrecedentService)"
    - "ÚNICO editor de routes/gestao.php na Wave 4; toda ação audita (RN-002); a decisão (deferir/indeferir) NÃO está aqui — é 10-10 a partir da ficha finalizada"
  artifacts:
    - path: "app/Services/Analise/AnalysisRecordService.php"
      provides: "Ficha: abrir/autosave/finalizar (imutável)/nova revisão + divergências na finalização"
      contains: "finalized_at"
    - path: "app/Services/Analise/AnalysisRecordDiff.php"
      provides: "Diff entre duas revisões (campos/condicionantes/status por CNAE/parecer)"
      contains: "diff"
    - path: "app/Http/Controllers/Gestao/AnalysisRecordController.php"
      provides: "show/autosave(PATCH)/finalizar/nova-revisão/diff da ficha (gated analisar-processos)"
      contains: "analisar-processos"
  key_links:
    - from: "app/Services/Analise/AnalysisRecordService.php"
      to: "app/Models/AnalysisRecord.php"
      via: "finalizar grava finalized_at + status finalizada (imutável RN-003); nova revisão copia a anterior"
      pattern: "AnalysisRecordStatus::Finalizada"
    - from: "app/Http/Controllers/Gestao/PrecedenteController.php"
      to: "app/Services/Analise/PrecedentService.php"
      via: "endpoint de precedentes da ficha (HU-142) consome PrecedentService::forRecord"
      pattern: "PrecedentService"
---

<objective>
Backend da ficha de análise (Wave 4, ÚNICO route-owner): `AnalysisRecordService` (abrir/autosave/finalizar imutável/nova revisão + divergências), `AnalysisRecordDiff` (HU-135 RN-007), o controller com os endpoints da ficha e o endpoint de precedentes (HU-142, via PrecedentService). A decisão fica em 10-10.

Purpose: a ficha é onde a análise humana acontece (HU-135) — o analista parte da revisão pré-analisada (10-08), confirma/diverge do motor (gera divergências HU-140, insumo do HU-145), salva por autosave e finaliza. A imutabilidade da revisão finalizada (RN-003) e o diff entre revisões (RN-007) são regras centrais; o painel de precedentes (HU-142) e a biblioteca de textos-padrão (HU-085, leitura) entram aqui. Reúno tudo num plano por ser a superfície de rotas da ficha (route-owner da Wave 4).
Output: AnalysisRecordService + Diff + controller (show/autosave/finalizar/nova-revisão/diff) + PrecedenteController + rotas + Resource, cobertos por feature tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md
@.planning/phases/10-analise-tecnica-sedur/10-02-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-06-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-08-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-04-SUMMARY.md

# PADRÕES A ESPELHAR (controller server-driven + request + resource + rota; transação+auditoria) — ler antes de codar:
@app/Http/Controllers/Gestao/ResultadoExpressoController.php
@app/Http/Resources/ViabilityDecisionResource.php
@app/Models/AnalysisRecord.php
@app/Support/Audit/AuditService.php
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-135-Preencher-ficha-de-análise.md
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-142-Exibir-historico-decisorio-do-imovel-e-CNAE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AnalysisRecordService (abrir/autosave) + AnalysisRecordController show/PATCH + rotas + Resource</name>
  <read_first>app/Models/AnalysisRecord.php, app/Http/Resources/ViabilityDecisionResource.php, routes/gestao.php</read_first>
  <files>app/Services/Analise/AnalysisRecordService.php, app/Http/Controllers/Gestao/AnalysisRecordController.php, app/Http/Requests/Gestao/AnalysisRecordRequest.php, app/Http/Resources/AnalysisRecordResource.php, routes/gestao.php, tests/Feature/Analise/AnalysisRecordAutosaveTest.php</files>
  <action>
    TDD estrito (RED primeiro). Rotas em `routes/gestao.php` (ÚNICO route-owner da Wave 4), sob `permission:analisar-processos`, prefixo `processos/{viabilityRequest}/ficha`:
    - GET `processos/{viabilityRequest}/ficha` → show (revisão atual + textos-padrão ativos + flag editável).
    - PATCH `processos/{viabilityRequest}/ficha` → autosave (atualiza o rascunho da revisão atual).

    `AnalysisRecordService`:
    - `current(ViabilityRequest $request): AnalysisRecord` — devolve a revisão atual (maior), criando rev 1 vazia se não houver (defensivo; normalmente a pré-análise já criou).
    - `autosave(AnalysisRecord $record, array $data): AnalysisRecord` — só se status rascunho (senão lança/422 — não edita finalizada RN-003); atualiza per_cnae (status escolhido por CNAE), conditions, parking, parecer; audita leve ('analise'/'ficha-autosave') ou sem auditoria a cada tecla (debounce é client-side; a constante analise.autosave.debounce_ms é config — Task do 10-17). Persiste só campos do rascunho.
    - `AnalysisRecordRequest` valida o payload da ficha (per_cnae array com status in deferida/indeferida/analise por cnae; conditions array; parking array; parecer string nullable).
    - `AnalysisRecordResource` expõe a ficha (revisão, status, engine_available, per_cnae sugerido×escolhido, conditions, parking, parecer, finalized_at) — payload de leitura honesto.
    - controller show: 403 sem analisar-processos; carrega a revisão atual + textos-padrão ativos (StandardText where active, gated leitura analisar-processos) para o picker do parecer (HU-085 RN-004); Inertia props (página em 10-17).

    `AnalysisRecordAutosaveTest` (SQLite):
    - sem analisar-processos → 403; com → 200.
    - PATCH atualiza per_cnae/parecer no rascunho (recarrega e confere).
    - PATCH numa revisão finalizada → 422 (RN-003, não edita).
    - show retorna a revisão atual + textos-padrão ativos no payload.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=AnalysisRecordAutosaveTest</verify>
  <acceptance_criteria>
    - grep "processos/{viabilityRequest}/ficha" + "analisar-processos" em routes/gestao.php.
    - grep "rascunho" (bloqueio de edição de finalizada) em app/Services/Analise/AnalysisRecordService.php.
    - AnalysisRecordAutosaveTest verde (403, autosave no rascunho, bloqueio na finalizada, textos-padrão no payload).
  </acceptance_criteria>
  <done>Ficha abre da revisão atual + autosave do rascunho (PATCH), com textos-padrão para o parecer; finalizada não edita.</done>
</task>

<task type="auto">
  <name>Task 2: finalizar (imutável RN-003) + divergências (HU-140) + nova revisão</name>
  <read_first>app/Services/Analise/AnalysisRecordService.php, app/Models/AnalysisDivergence.php, app/Services/Analise/PreAnaliseService.php</read_first>
  <files>app/Services/Analise/AnalysisRecordService.php, app/Http/Controllers/Gestao/AnalysisRecordController.php, routes/gestao.php, tests/Feature/Analise/AnalysisRecordFinalizeTest.php</files>
  <action>
    TDD estrito. Rotas (mesmo prefixo, analisar-processos):
    - POST `processos/{viabilityRequest}/ficha/finalizar` → finalizar.
    - POST `processos/{viabilityRequest}/ficha/nova-revisao` → novaRevisao (recalcular/editar após finalizar).

    `AnalysisRecordService`:
    - `finalizar(AnalysisRecord $record, ?User $ator = null): AnalysisRecord` — numa transação: marca status Finalizada + finalized_at=now() (imutável a partir daqui — RN-003); COMPARA o per_cnae final com o engine_snapshot/per_cnae SUGERIDO e grava `analysis_divergences` para cada campo divergente (cnae, field, suggested_value, final_value, justification quando informada — HU-140 RN-002); audita SÍNCRONO ('analise'/'ficha-finalizar', rulesVersion da ficha). NÃO decide o processo (a decisão é 10-10).
    - `novaRevisao(ViabilityRequest $request, ?User $ator = null): AnalysisRecord` — cria a próxima revisão (revision+1, status rascunho) COPIANDO os campos da última finalizada (base para ajuste); "Recalcular" (reexecutar o motor) é uma variação documentada que repõe engine_snapshot (reusa PreAnaliseService numa nova revisão) — manter a escolha à discrição, mas registrar a diferença entre "nova revisão de edição" e "recalcular".

    `AnalysisRecordFinalizeTest` (SQLite):
    - finalizar muda status→finalizada + finalized_at; autosave subsequente → 422 (imutável).
    - divergência: per_cnae final difere do sugerido → analysis_divergences criado (assert cnae/field/suggested/final).
    - novaRevisao cria revision 2 (rascunho) copiando a finalizada; currentAnalysisRecord passa a ser a rev 2.
    - auditoria 'analise'/'ficha-finalizar' gravada.
  </action>
  <verify>php artisan test --compact --filter=AnalysisRecordFinalizeTest</verify>
  <acceptance_criteria>
    - grep "finalized_at" e "analysis_divergences" (ou AnalysisDivergence::create) em app/Services/Analise/AnalysisRecordService.php.
    - AnalysisRecordFinalizeTest verde (imutabilidade, divergências, nova revisão, auditoria).
  </acceptance_criteria>
  <done>Finalizar torna a revisão imutável (RN-003) e grava divergências (HU-140); nova revisão para ajuste/recalcular.</done>
</task>

<task type="auto">
  <name>Task 3: diff entre revisões (HU-135 RN-007) + endpoint de precedentes (HU-142)</name>
  <read_first>app/Services/Analise/PrecedentService.php, app/Models/AnalysisRecord.php</read_first>
  <files>app/Services/Analise/AnalysisRecordDiff.php, app/Http/Controllers/Gestao/AnalysisRecordController.php, app/Http/Controllers/Gestao/PrecedenteController.php, routes/gestao.php, tests/Feature/Analise/AnalysisRecordRevisionDiffTest.php, tests/Feature/Analise/PrecedenteEndpointTest.php</files>
  <action>
    TDD estrito. Rotas (analisar-processos):
    - GET `processos/{viabilityRequest}/ficha/diff?de={rev}&para={rev}` → diff entre duas revisões.
    - GET `processos/{viabilityRequest}/precedentes` → precedentes (PrecedentController@show via PrecedentService::forRecord da revisão atual).

    `AnalysisRecordDiff::between(AnalysisRecord $a, AnalysisRecord $b): array` — compara campos/condicionantes/status por CNAE/parecer e devolve só o que MUDOU (estrutura {campo, de, para}), incluindo per_cnae por CNAE. Puro, testável.

    `PrecedenteController@show(ViabilityRequest)`: gated analisar-processos; resolve a revisão atual; `PrecedentService::forRecord` → payload {imovel:[...], cnae_zona:{...|indisponivel}} (LGPD sem CPF); audita a consulta ('analise'/'precedentes' — RN-004 HU-142). Retorna Inertia/JSON consumido pelo painel da ficha (10-17).

    `AnalysisRecordRevisionDiffTest` (SQLite): rev 1 (status X no CNAE, parecer A) e rev 2 (status Y, parecer B) → diff retorna só as mudanças (status do CNAE, parecer); campos iguais não aparecem.
    `PrecedenteEndpointTest` (SQLite, FakePrecedentRepository): 403 sem permissão; com permissão → payload com precedentes do imóvel (sem CPF) e cnae_zona (disponível/indisponível conforme zona); auditoria gravada.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="AnalysisRecordRevisionDiffTest|PrecedenteEndpointTest"</verify>
  <acceptance_criteria>
    - grep "ficha/diff" e "precedentes" em routes/gestao.php; grep "PrecedentService" em app/Http/Controllers/Gestao/PrecedenteController.php.
    - AnalysisRecordRevisionDiffTest verde (só mudanças) e PrecedenteEndpointTest verde (403, payload sem CPF, auditoria).
  </acceptance_criteria>
  <done>Diff entre revisões (RN-007) e endpoint de precedentes (HU-142) servindo o painel da ficha, gated e auditado.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-135 RN-008 — autosave do rascunho | AnalysisRecordAutosaveTest (PATCH) |
| HU-135 RN-003 — revisão imutável após finalizar | AnalysisRecordFinalizeTest (422 em finalizada; nova revisão) |
| HU-140 RN-002 — divergência sugerido×final | AnalysisRecordFinalizeTest (analysis_divergences) |
| HU-135 RN-007 — diff entre revisões | AnalysisRecordRevisionDiffTest |
| HU-135 RN-009/HU-085 — textos-padrão no parecer | AnalysisRecordAutosaveTest (show com textos ativos) |
| HU-142 — painel de precedentes | PrecedenteEndpointTest (imóvel sem CPF + cnae_zona) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="AnalysisRecordAutosaveTest|AnalysisRecordFinalizeTest|AnalysisRecordRevisionDiffTest|PrecedenteEndpointTest"` verde.
- Suíte completa CONTINUA verde; ÚNICO editor de routes/gestao.php na Wave 4.
</verification>

<success_criteria>
- Ficha real: abrir da revisão pré-analisada, autosave do rascunho, finalizar imutável (RN-003), nova revisão, diff (RN-007) e divergências (HU-140).
- Painel de precedentes (HU-142) e biblioteca de textos-padrão (HU-085 leitura) integrados; gated por analisar-processos e auditado.
- Decisão NÃO está aqui (é 10-10, a partir da ficha finalizada). ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-09-SUMMARY.md`: as rotas da ficha (show/autosave/finalizar/nova-revisao/diff/precedentes), as assinaturas de AnalysisRecordService (autosave/finalizar/novaRevisao) e AnalysisRecordDiff, o shape do AnalysisRecordResource e do payload de precedentes, e o ponto onde as divergências são gravadas. Insumo direto de 10-10 (decide a partir da ficha finalizada), 10-13 (TVL lê condicionantes da ficha) e 10-17 (ficha SAPS UI).
</output>

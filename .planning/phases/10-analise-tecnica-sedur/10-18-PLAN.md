---
phase: 10-analise-tecnica-sedur
plan: 18
type: execute
wave: 8
depends_on: ["10-04", "10-05", "10-06", "10-07", "10-08", "10-09", "10-10", "10-11", "10-12", "10-13", "10-14", "10-15", "10-16", "10-17"]
files_modified:
  - database/seeders/SectorSeeder.php
  - database/seeders/StandardTextSeeder.php
  - database/seeders/AnaliseDevSeeder.php
  - database/seeders/DatabaseSeeder.php
  - app/Console/Commands/AnaliseDecidirCommand.php
  - tests/Feature/Analise/AnaliseGoldenCaseTest.php
  - tests/Feature/Analise/AnaliseSmokeTest.php
  - tests/Feature/Analise/AnalisePrecedentesPostgisTest.php
  - tests/Fixtures/golden/analise/*.json
  - tests/Feature/Seeders/DatabaseSeederTest.php
autonomous: false
requirements: [HU-079, HU-080, HU-081, HU-082, HU-083, HU-084, HU-085, HU-086, HU-087, HU-088, HU-089, HU-132, HU-135, HU-136, HU-138, HU-140, HU-142, HU-144]

must_haves:
  truths:
    - "Seeds dev: SectorSeeder (setores + analistas vinculados) + StandardTextSeeder (textos-padrão por categoria) + AnaliseDevSeeder que, via os SERVIÇOS REAIS, monta processos em cada estágio: distribuído, em análise (ficha pré-analisada), em pendência, deferido humano (com ViabilityDecision flow analise_tecnica + TVL), em malha fina — dados fictícios, lógica real (entrega-funcional)"
    - "Comando analise:decidir {solicitacao} dá EVIDÊNCIA REAL: finaliza/decide via AnaliseTecnicaDecisionService e imprime status/outcome/TVL/fundamentação (em_analise/rascunho sai honesto, não erro)"
    - "Golden/smoke travam a regressão do fluxo humano de ponta a ponta: encaminhar→pré-análise→distribuir→assumir→ficha→finalizar(divergência)→deferir/indeferir→(TVL); ciclo de pendência (abrir→responder→reabre); malha fina (qualquer status, lote); precedentes @group postgis (ST_Intersects real)"
    - "Verificação integral FRESCA: pint + suíte SQLite (baseline + novos) + @group postgis (com container) + tsc + build, todos verdes, com as contagens atualizadas (parâmetros 67 / permissões 24) — registrar o total final"
    - "Smoke navegável aprovado (checkpoint humano): fila com SLA → abrir ficha pré-analisada → finalizar com divergência → deferir → emitir TVL (download por URL assinada, NÃO ao cidadão) → consulta/detalhe/timeline/mini-mapa; pendência ida-e-volta pelo portal; malha fina sobre um deferido"
  artifacts:
    - path: "database/seeders/AnaliseDevSeeder.php"
      provides: "Processos de exemplo em cada estágio via serviços reais (dados fictícios, lógica real)"
      contains: "AnaliseTecnicaDecisionService"
    - path: "app/Console/Commands/AnaliseDecidirCommand.php"
      provides: "Evidência real da decisão humana (finaliza/decide e imprime status/TVL)"
      contains: "analise:decidir"
    - path: "tests/Feature/Analise/AnaliseSmokeTest.php"
      provides: "Smoke do fluxo humano ponta a ponta (encaminhar→ficha→decidir→TVL) + pendência + malha fina"
      contains: "analysis_records"
  key_links:
    - from: "database/seeders/DatabaseSeeder.php"
      to: "database/seeders/AnaliseDevSeeder.php"
      via: "seeds da Fase 10 registrados (setores/textos-padrão + processos em estágios) após os seeds da Fase 9"
      pattern: "AnaliseDevSeeder"
    - from: "app/Console/Commands/AnaliseDecidirCommand.php"
      to: "app/Services/Analise/AnaliseTecnicaDecisionService.php"
      via: "decide(record) e imprime o resultado real"
      pattern: "AnaliseTecnicaDecisionService"
---

<objective>
Fechamento da Fase 10 (Wave 8): seeds dev (setores/textos-padrão + processos em cada estágio via serviços reais), o comando de evidência `analise:decidir`, os golden/smoke do fluxo humano de ponta a ponta (incl. precedentes @group postgis), e a VERIFICAÇÃO INTEGRAL FRESCA — encerrando com o smoke navegável (checkpoint humano).

Purpose: critério de pronto do ROADMAP com EVIDÊNCIA FRESCA (verificação antes de completar). A decisão humana é real (ViabilityDecision flow analise_tecnica), a pré-análise reusa o motor real, a malha fina/pendência/TVL são reais; só a transmissão Regin/SEFAZ na conclusão segue BLOQUEADA → Fase 13 (auditam pendência, nunca "enviado"). Os seeds usam dados fictícios mas LÓGICA REAL (entrega-funcional — muda a carga, não o comportamento). Golden/smoke provam os caminhos e a degradação honesta (deferir sem zona pelo humano; engine_available=false).
Output: 3 seeders + comando + golden/smoke + fixtures + verificação integral + checkpoint humano. Atualiza STATE.md e ROADMAP no fechamento.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md
@.planning/phases/10-analise-tecnica-sedur/10-02-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-08-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-09-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-10-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-13-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-14-SUMMARY.md

# PADRÕES A ESPELHAR (seeds dev via serviços reais; comando de evidência; golden/smoke; verificação integral) — ler antes de codar:
@database/seeders/DatabaseSeeder.php
@database/seeders/ExpressoDevSeeder.php
@database/seeders/ZonaFicticiaDevSeeder.php
@app/Console/Commands/ExpressoDecidirCommand.php
@tests/Feature/Expresso/ExpressoGoldenCaseTest.php
@tests/Feature/Expresso/ExpressoSmokeTest.php
@.planning/phases/09-fluxo-expresso/09-12-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: seeds dev (setores, textos-padrão, processos em estágios) + comando analise:decidir</name>
  <read_first>database/seeders/DatabaseSeeder.php, database/seeders/ExpressoDevSeeder.php, app/Console/Commands/ExpressoDecidirCommand.php</read_first>
  <files>database/seeders/SectorSeeder.php, database/seeders/StandardTextSeeder.php, database/seeders/AnaliseDevSeeder.php, database/seeders/DatabaseSeeder.php, app/Console/Commands/AnaliseDecidirCommand.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <action>
    TDD onde houver lógica; seeders cobertos pelo DatabaseSeederTest.

    `SectorSeeder`: cria 1-2 setores (ex.: "Análise Locacional") e vincula os analistas dev (reusa os usuários dev). Idempotente.
    `StandardTextSeeder`: cria textos-padrão por categoria (fundamentações exemplo, pt-BR). Idempotente.
    `AnaliseDevSeeder`: a partir de solicitações em `em_analise` (reusa o ExpressoDevSeeder/zona fictícia da Fase 9 — alguns processos caem em em_analise sem zona; outros sobre a zona fictícia podem ser encaminhados via toggle/gatilho) monta exemplos navegáveis usando os SERVIÇOS REAIS: distribuir (DistribuicaoService) → assumir → a ficha já vem pré-analisada (PreAnaliseService via o evento) → preencher/finalizar (AnalysisRecordService, gerando ao menos uma divergência) → deferir (AnaliseTecnicaDecisionService → ViabilityDecision flow analise_tecnica + TVL) num exemplo; deixar outro em em_pendencia (PendenciaService::abrir) e outro em malha fina (MalhaFinaService). Dados fictícios, lógica real. Idempotente. Registrar no `DatabaseSeeder` após os seeds da Fase 9.

    `AnaliseDecidirCommand` (auto-descoberto; `analise:decidir {solicitacao}`): carrega a solicitação, resolve a currentAnalysisRecord; se rascunho, opção --finalizar para finalizar antes (ou exigir finalizada); chama AnaliseTecnicaDecisionService::decide e imprime status final + outcome + número TVL (se deferida) + fundamentação. em_analise/rascunho sem --finalizar sai honesto (exit 0, mensagem), inexistente exit 1.

    `DatabaseSeederTest`: acrescentar asserções — setores/textos-padrão criados; existem processos de exemplo em em_analise (com ficha rev 1), em_pendencia, deferido humano (ViabilityDecision flow 'analise_tecnica' + TvlDocument? — TVL é sob demanda; pelo menos a decisão) e em malha fina; idempotência (re-seed estável). Confirmar contagens parâmetros 67 / permissões 24 (já ajustadas em 10-01).
  </action>
  <verify>php artisan test --compact --filter=DatabaseSeederTest && php artisan analise:decidir --help</verify>
  <acceptance_criteria>
    - grep "AnaliseDevSeeder" + "SectorSeeder" + "StandardTextSeeder" em database/seeders/DatabaseSeeder.php.
    - grep "analise:decidir" em app/Console/Commands/AnaliseDecidirCommand.php.
    - DatabaseSeederTest verde (setores/textos/processos em estágios; idempotência; 67/24).
  </acceptance_criteria>
  <done>Seeds dev (setores/textos-padrão + processos em cada estágio via serviços reais) + comando analise:decidir; dados fictícios, lógica real.</done>
</task>

<task type="auto">
  <name>Task 2: golden + smoke do fluxo humano (ponta a ponta) + precedentes @group postgis</name>
  <read_first>tests/Feature/Expresso/ExpressoSmokeTest.php, tests/Feature/Expresso/ExpressoGoldenCaseTest.php, tests/Feature/Analise/PrecedentRepositoryPostgisTest.php</read_first>
  <files>tests/Feature/Analise/AnaliseGoldenCaseTest.php, tests/Feature/Analise/AnaliseSmokeTest.php, tests/Feature/Analise/AnalisePrecedentesPostgisTest.php, tests/Fixtures/golden/analise/*.json</files>
  <action>
    TDD/golden (#[DataProvider], padrão Fases 5-9).

    `AnaliseSmokeTest` (SQLite + fake SpatialRepository): dirige o fluxo HUMANO de ponta a ponta pelos serviços reais —
    - encaminhar (FluxoExpressoService::decide sem zona → em_analise) → o listener PreAnalisarProcesso cria a ficha rev 1 → distribuir/assumir (DistribuicaoService) → preencher/finalizar a ficha com uma DIVERGÊNCIA (AnalysisRecordService) → DEFERIR (AnaliseTecnicaDecisionService): ViabilityDecision flow 'analise_tecnica', decided_by analista, status deferida, TVL gerado; ResultadoEmitido disparado (Event::fake) + Regin/SEFAZ pendência 'bloqueado' auditada (reuso dos listeners; Fase 13); analysis_divergences gravada.
    - INDEFERIR: ficha com uma CNAE indeferida → status indeferida, sem TVL.
    - CICLO DE PENDÊNCIA: abrir (em_analise→em_pendencia) → responder pelo portal (em_pendencia→em_analise).
    - MALHA FINA: encaminhar um processo DEFERIDO (qualquer status) → fine_mesh_referrals + in_fine_mesh, status inalterado.
    - DEGRADAÇÃO: engine_available=false (FA-01) → ficha modo manual, o humano decide assim mesmo.

    `AnaliseGoldenCaseTest` (#[DataProvider] + fixtures JSON em tests/Fixtures/golden/analise/): casos-âncora entrada→esperado executando os serviços reais (ficha todas deferidas → deferida+TVL; uma indeferida → indeferida; deferir sem zona pelo humano → deferida com fundamentação da ficha). Travar a regressão do fluxo humano.

    `AnalisePrecedentesPostgisTest extends PostgisTestCase` (#[Group('postgis')]): semear decisões em imóveis que INTERCEPTAM o polígono atual + abrir a ficha → o painel de precedentes (PrecedentService sobre PostgisPrecedentRepository) retorna os precedentes reais (ST_Intersects); assertSame('pgsql', ...). Skip honesto sem container.
  </action>
  <verify>php artisan test --compact --filter="AnaliseSmokeTest|AnaliseGoldenCaseTest" && php artisan test --compact --group=postgis --filter=AnalisePrecedentesPostgisTest</verify>
  <acceptance_criteria>
    - grep "'analise_tecnica'" e "analysis_divergences" no AnaliseSmokeTest; grep "DataProvider" + fixtures JSON no AnaliseGoldenCaseTest.
    - grep "Group('postgis')" no AnalisePrecedentesPostgisTest.
    - AnaliseSmokeTest/AnaliseGoldenCaseTest verdes (deferir/indeferir/pendência/malha fina/degradação); AnalisePrecedentesPostgisTest verde (container) ou skip honesto.
  </acceptance_criteria>
  <done>Golden/smoke travam o fluxo humano real (encaminhar→ficha→decidir→TVL + pendência + malha fina + degradação) e os precedentes @group postgis.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: verificação integral fresca + smoke navegável da análise técnica (checkpoint humano)</name>
  <what-built>
    A Fase 10 completa: parâmetros/permissões (67/24); schema (setores+pivot, ficha versionada, divergências, pendências, malha fina, textos-padrão, tvl_documents) + enums/models; StateMachine (em_analise/em_pendencia) + evento EncaminhadoParaAnalise; setores (HU-138) + textos-padrão (HU-085); AnalysisSlaService; PrecedentRepository (@group postgis); distribuição (HU-079/080/081) + pré-análise (HU-140); ficha (HU-135) + divergências + diff + precedentes (HU-142); decisão humana (HU-085/086/087/088/089 → ViabilityDecision flow analise_tecnica + ResultadoEmitido); pendências (HU-083/084 parcial); malha fina (HU-136); TVL PDF (HU-132, dompdf); consulta (HU-082) + fila (HU-144); UI do console (fila/consulta/detalhe/ficha SAPS/admin/nav + Cmd+K). Reuso real das Fases 5/6/8/9.
  </what-built>
  <how-to-verify>
    EVIDÊNCIA FRESCA — rodar e LER o output inteiro antes de afirmar (verificação antes de completar):
    1. `vendor/bin/pint --dirty --format agent` — sem pendências.
    2. `php artisan test --compact` — suíte SQLite verde (baseline da Fase 9 + os novos da Fase 10; Seeders 67 parâmetros / 24 permissões). Registrar o total final no SUMMARY.
    3. `php artisan test --compact --group=postgis` com o container PostGIS de pé — verde (baseline + PrecedentRepositoryPostgisTest + AnalisePrecedentesPostgisTest). Registrar o total.
    4. `npx tsc --noEmit` e `npm run build` — verdes.
    5. Evidência do comando: `php artisan migrate:fresh --seed` e `php artisan analise:decidir <id de um processo em análise com ficha finalizável> --finalizar` — imprime status deferida/indeferida + número TVL + fundamentação. Colar o output no SUMMARY.
    6. SMOKE NAVEGÁVEL (`composer dev`), logado como analista/gestor (`acessar-gestao` + permissões da fase):
       a. FILA: abrir "Fila" → ver os processos ordenados por prazo com semáforo + contadores; abas Meus/Setor.
       b. FICHA: abrir um processo em análise → ficha PRÉ-ANALISADA pelo motor (sugestão por CNAE + gatilhos destacados); preencher/divergir, autosave; ver precedentes (ou "sem precedentes") + mini-mapa; ver diff de revisão; FINALIZAR.
       c. DECIDIR: deferir → status deferida + ViabilityDecision (flow analise_tecnica); EMITIR TVL → baixar o PDF por URL assinada (confirmar que NÃO vai ao cidadão); conferir na trilha Regin/SEFAZ 'bloqueado' (Fase 13).
       d. PENDÊNCIA: abrir pendência num processo → status em_pendencia + e-mail; logar como o cidadão dono no portal → responder → volta a em_analise.
       e. MALHA FINA: encaminhar um processo DEFERIDO à malha fina (motivo) → flag ligada, status inalterado; lote pela consulta.
       f. CONSULTA: filtros (analista/categoria/status) + busca global Cmd+K levando ao processo + exportar CSV. Mobile (375px) + dark mode.
    7. Auditoria: `php artisan tinker --execute 'echo App\Models\Activity::where("log_name","analise")->count();'` > 0 (ações auditadas síncronas).
    8. Registrar no SUMMARY/STATE/ROADMAP as PENDÊNCIAS/bloqueios que PERMANECEM (sem fachada): transmissão Regin (HU-104)/SEFAZ (HU-110) na conclusão → Fase 13; convite Simplifica/Regin + multicanal (HU-083/084 pleno) → EP11; exportação plena XLSX/PDF (HU-131) → Fase 15; zona oficial Quadro 10/SEDUR (sem ela o humano decide o caso pendente); HU-137 feriados (seam pronto); assinatura digital gov.br/ICP do TVL → SEDUR; quadros/planilhas/ficha SAPS oficiais, gatilhos, valor TLL monetário (DAM bloqueado) e recurso administrativo → SEDUR.
  </how-to-verify>
  <resume-signal>Responder "aprovado" (anexando outputs/prints do fluxo: fila→ficha→deferir→TVL + pendência ida-e-volta + malha fina sobre deferido) ou descrever divergências para correção antes de fechar a fase.</resume-signal>
</task>

</tasks>

## Mapa CA → teste (fechamento integral)
| HU / RN | Teste / evidência |
|---|---|
| HU-079/080/081/140 — encaminhar→pré-análise→distribuir→assumir | AnaliseSmokeTest + smoke navegável (a/b) |
| HU-135/140 — ficha pré-analisada + divergência + diff | AnaliseSmokeTest + FichaUiSmokeTest + navegável (b) |
| HU-085/086/087/088/089 — decidir/encerrar | AnaliseGoldenCaseTest + analise:decidir + navegável (c) |
| HU-132 — TVL PDF interno (URL assinada, não ao cidadão) | TvlDocumentEndpointTest + navegável (c) |
| HU-083/084 — pendência ida-e-volta | AnaliseSmokeTest + navegável (d) |
| HU-136 — malha fina (deferido + lote) | AnaliseSmokeTest + navegável (e) |
| HU-082/144 — consulta/fila/busca/CSV | ProcessoConsultaTest/ProcessoFilaTest + navegável (a/f) |
| HU-142 — precedentes reais | AnalisePrecedentesPostgisTest + navegável (b) |
| Anti-fachada — Regin/SEFAZ pendência honesta | AnaliseSmokeTest (auditoria 'bloqueado') |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact` (SQLite) verde; `--group=postgis` verde (container de pé).
- `npx tsc --noEmit` e `npm run build` verdes.
- Comando analise:decidir dá evidência real (deferida/indeferida + TVL); smoke navegável aprovado (fila→ficha→deferir→TVL + pendência + malha fina).
</verification>

<success_criteria>
- Critérios 1, 1b, 2, 2b, 4, 5, 6, 7 do ROADMAP validados com evidência fresca; a TRANSMISSÃO Regin/SEFAZ na conclusão (critério 4 parcial) fica com a DECISÃO/TVL/auditoria REAIS e a transmissão BLOQUEADA → Fase 13 (registrado, não simulado); HU-083/084 entregue PARCIAL (ciclo interno) com convite/multicanal → EP11.
- Seeds dev com dados fictícios e LÓGICA REAL; golden/smoke + precedentes @group postgis; verificação integral fresca (67/24).
- ZERO dependência nova além do dompdf (10-13); pendências SEDUR/bloqueios registrados em STATE/ROADMAP.
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-18-SUMMARY.md`: total final da suíte (SQLite + postgis), output do analise:decidir (deferida/indeferida + TVL), os seeds (setores/textos/processos em estágios), golden/smoke criados, o registro do smoke navegável aprovado e a lista explícita das pendências bloqueadas/adiadas (Regin/SEFAZ → Fase 13, HU-083/084 pleno → EP11, HU-131 → Fase 15, zona Quadro 10/SEDUR, HU-137 feriados, assinatura gov.br/ICP do TVL, ficha SAPS oficial/gatilhos/TLL/recurso → SEDUR). Atualizar STATE.md e ROADMAP.md no fechamento da fase.
</output>

---
phase: 10-analise-tecnica-sedur
plan: 06
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - app/Services/Analise/PrecedentRepository.php
  - app/Services/Analise/PostgisPrecedentRepository.php
  - tests/Support/Analise/FakePrecedentRepository.php
  - app/Services/Analise/PrecedentService.php
  - app/Providers/AppServiceProvider.php
  - tests/Feature/Analise/PrecedentServiceTest.php
  - tests/Feature/Analise/PrecedentRepositoryPostgisTest.php
autonomous: true
requirements: [HU-142]

must_haves:
  truths:
    - "Contrato PrecedentRepository isola TODO o SQL espacial (espelha SpatialRepository): propertyPrecedents(geojson, limit) usa ST_Intersects(vr.property_polygon, ST_GeomFromGeoJSON(:current)) JOIN viability_decisions, ordenado por decided_at desc, limit parametrizável; cnaeZoneStats(cnae, zona, desde) agrega viability_decisions por outcome na janela"
    - "PostgisPrecedentRepository é a implementação real (PostGIS, @group postgis); o binding no AppServiceProvider é INCONDICIONAL (bind(PrecedentRepository::class, PostgisPrecedentRepository::class)) — espelha EXATAMENTE o bind(SpatialRepository::class, PostgisSpatialRepository::class) existente, NÃO condicional a pgsql"
    - "FakePrecedentRepository é HELPER DE TESTE em tests/Support/Analise/ (não código de produção, igual ao tests/Support/Geo/FakeSpatialRepository); os testes SQLite injetam o fake POPULADO via \\$this->app->instance(PrecedentRepository::class, \\$fake) — assim PrecedentServiceTest (aqui), PrecedenteEndpointTest (10-09) e AnaliseSmokeTest (10-18) NÃO retornam precedentes vazios silenciosamente"
    - "PrecedentService aplica os parâmetros (analise.precedentes.janela_meses/max_itens), lê a zona do engine_snapshot da ficha no caminho EXATO engine_snapshot['por_cnae'][i]['consulta']['territorio']['zona'] (shape do ResolvedViability::toSnapshot da Fase 9: zona = {status,nome,...}; usa zona['nome'] quando status==='identificado', senão degrada honesto — estatística de CNAE 'indisponível', nunca inventada) e MINIMIZA dados pessoais (LGPD: NUNCA CPF — RN-004)"
    - "Fallback portável (street+number) quando não há polígono/PostGIS — precedentes do imóvel por endereço; a estatística do CNAE na zona exige a zona (sem ela, retorna 'indisponível')"
  artifacts:
    - path: "app/Services/Analise/PrecedentRepository.php"
      provides: "Contrato dos precedentes (imóvel por geometria; CNAE na zona por agregação)"
      contains: "interface PrecedentRepository"
    - path: "app/Services/Analise/PostgisPrecedentRepository.php"
      provides: "SQL espacial real (ST_Intersects sobre property_polygon) — @group postgis"
      contains: "ST_Intersects"
    - path: "app/Services/Analise/PrecedentService.php"
      provides: "Precedentes parametrizados + LGPD (nunca CPF) + degradação honesta sem zona"
      contains: "analise.precedentes"
  key_links:
    - from: "app/Services/Analise/PostgisPrecedentRepository.php"
      to: "viability_requests.property_polygon + viability_decisions"
      via: "ST_Intersects(property_polygon, ST_GeomFromGeoJSON(:current)) JOIN decisões (geometry derivada da Fase 8)"
      pattern: "ST_Intersects"
    - from: "app/Providers/AppServiceProvider.php"
      to: "app/Services/Analise/PrecedentRepository.php"
      via: "bind INCONDICIONAL PrecedentRepository→PostgisPrecedentRepository (espelha o bind incondicional do SpatialRepository); fake injetado via instance() nos testes SQLite"
      pattern: "PrecedentRepository::class"
---

<objective>
Fundação dos precedentes (Wave 2, @group postgis): o contrato `PrecedentRepository` + implementação real `PostgisPrecedentRepository` (ST_Intersects sobre a geometry `property_polygon` da Fase 8) + `FakePrecedentRepository` + `PrecedentService` (parâmetros + LGPD + degradação honesta). Serviço puro, sem rota — o endpoint da ficha consome em 10-09.

Purpose: HU-142 dá consistência decisória — o analista decide vendo os precedentes (decisões anteriores no mesmo imóvel; estatística do CNAE na zona), não no vácuo. O dado JÁ existe (viability_decisions + geometry property_polygon, gancho deixado em 08-06); falta apresentá-lo. Isolar o SQL espacial atrás do contrato (igual ao SpatialRepository) mantém os consumidores testáveis com fake e a concorrência/espacialidade provada em @group postgis. Anti-fachada: sem a zona oficial (Quadro 10/SEDUR), a estatística do CNAE na zona degrada para "indisponível" — nunca uma estatística inventada; LGPD: jamais CPF.
Output: contrato + Postgis + fake + binding + PrecedentService, cobertos por feature tests (fake/portável em SQLite) + ST_Intersects real (@group postgis).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md
@.planning/phases/10-analise-tecnica-sedur/10-01-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-02-SUMMARY.md

# PADRÕES A ESPELHAR (contrato + Postgis + fake + binding; @group postgis; geometry property_polygon) — ler antes de codar:
@app/Services/Geo/SpatialRepository.php
@app/Services/Geo/PostgisSpatialRepository.php
@app/Models/ViabilityDecision.php
@app/Models/ViabilityRequest.php
@tests/PostgisTestCase.php
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-142-Exibir-historico-decisorio-do-imovel-e-CNAE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: contrato PrecedentRepository + FakePrecedentRepository + binding + PrecedentService (parâmetros + LGPD)</name>
  <read_first>app/Services/Geo/SpatialRepository.php, app/Models/ViabilityDecision.php, app/Providers/AppServiceProvider.php, app/Support/Settings.php</read_first>
  <files>app/Services/Analise/PrecedentRepository.php, tests/Support/Analise/FakePrecedentRepository.php, app/Services/Analise/PrecedentService.php, app/Providers/AppServiceProvider.php, tests/Feature/Analise/PrecedentServiceTest.php</files>
  <action>
    TDD estrito (RED primeiro).

    `PrecedentRepository` (interface; espelhar SpatialRepository — o ponto/geojson na ordem [lng,lat]):
    - `propertyPrecedents(array $currentGeojson, int $limit, ?string $fallbackStreet = null, ?string $fallbackNumber = null): array` — decisões anteriores no mesmo imóvel (lista de {viability_request_id, protocol_number, outcome, decided_at, service_type, analyst}).
    - `cnaeZoneStats(string $cnae, string $zona, DateTimeInterface $since): array` — agregação {deferidos, indeferidos, total} das decisões do CNAE na zona desde a data.

    `FakePrecedentRepository` em `tests/Support/Analise/` (HELPER DE TESTE, não produção — igual ao `tests/Support/Geo/FakeSpatialRepository`): recebe arrays setáveis em memória, implementa o contrato sem SQL espacial. Os testes SQLite injetam o fake POPULADO via `$this->app->instance(PrecedentRepository::class, $fake)`.

    Binding no `AppServiceProvider` INCONDICIONAL (espelhar `bind(SpatialRepository::class, PostgisSpatialRepository::class)`): `bind(PrecedentRepository::class, PostgisPrecedentRepository::class)`. NÃO condicionar a pgsql; NÃO registrar o fake no container de produção (ele é só de teste, injetado via instance()).

    `PrecedentService` (construtor injeta PrecedentRepository):
    - `forRecord(AnalysisRecord $record): array` — lê o polígono/endereço da request e a ZONA do `engine_snapshot` no caminho EXATO `engine_snapshot['por_cnae'][i]['consulta']['territorio']['zona']` (= {status,nome,...}; usa `zona['nome']` só quando `zona['status']==='identificado'`, senão indisponível); aplica `analise.precedentes.max_itens` (limit) e `analise.precedentes.janela_meses` (janela) via Settings (default inline 10/12); monta {imovel: [...], cnae_zona: {...|indisponivel}}.
    - LGPD (RN-004): o payload NUNCA inclui CPF; expõe protocolo/outcome/data/serviço/analista, não dados pessoais do requerente.
    - Degradação honesta: sem zona no engine_snapshot → `cnae_zona = {disponivel: false, motivo: 'zona urbanística pendente'}` (não inventa estatística); sem polígono → usa fallback street+number; sem precedentes → lista vazia explícita (HU-142 CA-03 "sem precedentes", não ocultar).

    `PrecedentServiceTest` (SQLite, FakePrecedentRepository):
    - imóvel com 2 decisões anteriores → retorna as 2 (sem CPF no payload — assert ausência da chave/valor de CPF).
    - max_itens=1 (parametrizado) → limita a 1.
    - sem zona no engine_snapshot → cnae_zona disponivel=false (degradação honesta).
    - sem precedentes → listas vazias explícitas (não erro).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=PrecedentServiceTest</verify>
  <acceptance_criteria>
    - grep "interface PrecedentRepository" + "cnaeZoneStats" no contrato; grep "PrecedentRepository::class" no AppServiceProvider (binding INCONDICIONAL → PostgisPrecedentRepository); FakePrecedentRepository em tests/Support/Analise/ (não em app/).
    - grep "analise.precedentes" e ausência de CPF no payload do PrecedentService.
    - PrecedentServiceTest verde (parâmetros, LGPD sem CPF, degradação sem zona, sem precedentes).
  </acceptance_criteria>
  <done>Contrato + fake + binding + PrecedentService parametrizado, LGPD (nunca CPF) e degradação honesta sem zona; testável sem PostGIS.</done>
</task>

<task type="auto">
  <name>Task 2: PostgisPrecedentRepository (ST_Intersects sobre property_polygon) — @group postgis</name>
  <read_first>app/Services/Geo/PostgisSpatialRepository.php, tests/PostgisTestCase.php, app/Services/Analise/PrecedentRepository.php</read_first>
  <files>app/Services/Analise/PostgisPrecedentRepository.php, tests/Feature/Analise/PrecedentRepositoryPostgisTest.php</files>
  <action>
    TDD (@group postgis). `PostgisPrecedentRepository implements PrecedentRepository` (espelhar PostgisSpatialRepository — SQL nativo, sem cálculo geométrico em PHP):
    - `propertyPrecedents`: `DB::table('viability_requests as vr')->join('viability_decisions as vd','vd.viability_request_id','vr.id')->whereRaw('ST_Intersects(vr.property_polygon, ST_GeomFromGeoJSON(?))', [json do geojson do anel atual])->orderByDesc('vd.decided_at')->limit($limit)` projetando os campos do contrato (sem CPF). Fallback street+number quando geojson nulo.
    - `cnaeZoneStats`: agrega viability_decisions por outcome onde a zona (lida do per_cnae/engine_snapshot do processo da decisão OU de uma coluna derivada) == :zona e decided_at >= :since (count deferida/indeferida). Documentar como a zona é resolvida (sem zona, o consumidor degrada — esta query só roda com zona).

    `PrecedentRepositoryPostgisTest extends PostgisTestCase` (#[Group('postgis')] herdado): semear 2 requests com property_polygon que INTERCEPTAM um polígono atual + decisões → propertyPrecedents retorna 2 ordenadas por decided_at; um request que NÃO intercepta → não retorna. assertSame('pgsql', DB::connection()->getDriverName()). Skip honesto sem container.
  </action>
  <verify>php artisan test --compact --group=postgis --filter=PrecedentRepositoryPostgisTest</verify>
  <acceptance_criteria>
    - grep "ST_Intersects" + "ST_GeomFromGeoJSON" em app/Services/Analise/PostgisPrecedentRepository.php.
    - grep "Group('postgis')" (ou extends PostgisTestCase) no teste.
    - PrecedentRepositoryPostgisTest verde com o container de pé; skip honesto sem servidor.
  </acceptance_criteria>
  <done>Implementação real ST_Intersects sobre property_polygon (geometry da Fase 8), provada em @group postgis.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-142 CA-01 — precedentes do imóvel | PrecedentRepositoryPostgisTest (ST_Intersects) + PrecedentServiceTest |
| HU-142 CA-02 — estatística do CNAE na zona | PrecedentServiceTest (cnae_zona com zona) |
| HU-142 CA-03 — sem precedentes (não ocultar) | PrecedentServiceTest (listas vazias explícitas) |
| HU-142 RN-003 — janela/itens parametrizáveis | PrecedentServiceTest (max_itens/janela_meses) |
| HU-142 RN-004 — LGPD (sem CPF) + degradação sem zona | PrecedentServiceTest (sem CPF; cnae_zona indisponível) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=PrecedentServiceTest` verde.
- `php artisan test --compact --group=postgis --filter=PrecedentRepositoryPostgisTest` verde (container) ou skip honesto.
- Suíte completa CONTINUA verde (serviço/contrato novos, sem rota).
</verification>

<success_criteria>
- Precedentes do imóvel (ST_Intersects real) e do CNAE na zona (agregação), atrás de contrato com fake portável e binding condicional.
- Parametrizado (janela/itens), LGPD (nunca CPF) e degradação honesta sem zona; concorrência espacial provada em @group postgis.
- ZERO dependência nova; reusa a geometry property_polygon da Fase 8.
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-06-SUMMARY.md`: a assinatura do contrato PrecedentRepository, do PostgisPrecedentRepository (SQL), do fake, do binding e do PrecedentService (parâmetros/LGPD/degradação), e o shape do payload de precedentes. Insumo direto de 10-09 (endpoint de precedentes na ficha) e 10-16/10-17 (painel de precedentes na ficha SAPS).
</output>

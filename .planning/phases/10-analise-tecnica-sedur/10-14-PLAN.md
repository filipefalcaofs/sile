---
phase: 10-analise-tecnica-sedur
plan: 14
type: execute
wave: 6
depends_on: ["10-02", "10-05"]
files_modified:
  - database/migrations/*_add_consulta_indexes_to_viability_requests_table.php
  - app/Http/Controllers/Gestao/ProcessoController.php
  - app/Http/Controllers/Gestao/ProcessoBuscaController.php
  - app/Http/Resources/ProcessoResource.php
  - app/Services/Analise/ProcessoQueryService.php
  - routes/gestao.php
  - tests/Feature/Analise/ProcessoConsultaTest.php
  - tests/Feature/Analise/ProcessoFilaTest.php
  - tests/Feature/Analise/ProcessoBuscaGlobalTest.php
autonomous: true
requirements: [HU-082, HU-144]

must_haves:
  truths:
    - "HU-082: consulta de processos server-driven (espelha ResultadoExpressoController) com os filtros do SAPS (print 16: grupo de status, status, número do processo/produto TVL/BAP, serviço, setor, zona, datas, inscrição, nome, CNPJ, CEP, logradouro, bairro) + filtro por ANALISTA + CATEGORIA (Expresso/Semi-Expresso/Malha Fina/Sede de Escritório, derivadas) — paginação server-side + whereLike caseSensitive:false (PostgreSQL)"
    - "HU-082 RN-008 — desempenho: índices nos campos filtrados (migration dedicada, aditiva, sobre colunas existentes/da análise) — resposta da engenharia à reclamação 'sistema muito lento' do legado"
    - "HU-082 RN-009 — busca global (endpoint leve Cmd+K): número de processo/BAP/produto/CNPJ/nome leva ao processo; HU-082 RN-011 — exportação CSV SIMPLES do conjunto filtrado (export pleno XLSX/PDF → HU-131/Fase 15)"
    - "HU-144: fila do analista — index com modos 'meus processos' e 'caixa do setor' ordenado por analysis_due_at + semáforo (AnalysisSlaService) + contadores por status (aguardando análise, em análise, em pendência, vencendo hoje); gestor vê visão do setor"
    - "Gated por consultar-solicitacoes (reuso — sem permissão nova, decisão de 10-01); 403 auditado; toda consulta relevante auditada (CA-02); ÚNICO editor de routes/gestao.php na Wave 6 (paralelo file-disjunto com 10-13)"
  artifacts:
    - path: "app/Http/Controllers/Gestao/ProcessoController.php"
      provides: "index (consulta filtrável HU-082 + fila HU-144) + show (detalhe) server-driven, gated consultar-solicitacoes"
      contains: "consultar-solicitacoes"
    - path: "app/Services/Analise/ProcessoQueryService.php"
      provides: "Construção da query filtrada/paginada (filtros SAPS + analista + categoria) + contadores + semáforo"
      contains: "analysis_due_at"
  key_links:
    - from: "app/Http/Controllers/Gestao/ProcessoController.php"
      to: "app/Services/Analise/AnalysisSlaService.php"
      via: "fila ordenada por analysis_due_at + statusFor (semáforo) on-the-fly (HU-144)"
      pattern: "AnalysisSlaService"
    - from: "routes/gestao.php"
      to: "app/Http/Controllers/Gestao/ProcessoController.php"
      via: "Route::middleware('permission:consultar-solicitacoes')->prefix('processos') (reuso da permissão)"
      pattern: "processos"
---

<objective>
Consulta de processos (HU-082) e fila do analista (HU-144) (Wave 6, gestao route-owner): `ProcessoController` server-driven com filtros completos do SAPS + analista + categoria, índices, busca global e CSV simples; e a fila priorizada por SLA (meus processos / caixa do setor + semáforo + contadores). Detalhe (show) com timeline/dados; o mini-mapa/abas é renderizado na UI (10-16).

Purpose: HU-082 supera o legado ("filtros insuficientes" + "sistema muito lento") com filtros completos + filtro por analista/categoria + paginação server-side e índices (RN-008) + busca global (RN-009). HU-144 entrega o trabalho ordenado por prazo (corrige a distorção do legado) com semáforo via AnalysisSlaService (10-05). Reusa consultar-solicitacoes (decisão de 10-01). CSV simples agora; export pleno (XLSX/PDF) → HU-131/Fase 15 (registrado). Este é o ÚNICO editor de routes/gestao.php da Wave 6.
Output: migration de índices + ProcessoController (index consulta+fila, show) + ProcessoBuscaController (global) + ProcessoQueryService + Resource + rotas + CSV, cobertos por feature tests (filtros, fila ordenada/contadores, busca global, 403, índices).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md
@.planning/phases/10-analise-tecnica-sedur/10-02-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-05-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-11-SUMMARY.md

# PADRÕES A ESPELHAR (controller server-driven + filtros + paginação + whereLike caseSensitive:false; Resource; rota gestão) — ler antes de codar:
@app/Http/Controllers/Gestao/ResultadoExpressoController.php
@app/Http/Resources/ViabilityDecisionResource.php
@app/Models/ViabilityRequest.php
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-082-Consultar-processo.md
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-144-Fila-de-trabalho-do-analista-com-SLA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: índices de consulta + ProcessoQueryService + ProcessoController index/show (filtros SAPS + analista + categoria) + rotas</name>
  <read_first>app/Http/Controllers/Gestao/ResultadoExpressoController.php, app/Models/ViabilityRequest.php, routes/gestao.php</read_first>
  <files>database/migrations/*_add_consulta_indexes_to_viability_requests_table.php, app/Services/Analise/ProcessoQueryService.php, app/Http/Controllers/Gestao/ProcessoController.php, app/Http/Resources/ProcessoResource.php, routes/gestao.php, tests/Feature/Analise/ProcessoConsultaTest.php</files>
  <action>
    TDD estrito (RED primeiro). Rotas em `routes/gestao.php` (ÚNICO route-owner da Wave 6), sob `permission:consultar-solicitacoes` (reuso — decisão de 10-01), prefixo `processos`:
    - GET `processos` → index (consulta filtrável + paginação; CSV quando ?formato=csv).
    - GET `processos/{viabilityRequest}` → show (detalhe: dados/timeline/decisão/ficha/precedentes resumido — props para a UI 10-16).

    Migration `add_consulta_indexes_to_viability_requests` (ADITIVA — índices nas colunas filtradas que ainda não tenham: protocol_number, property_registration, address_neighborhood, status; as de análise sector_id/assigned_user_id/analysis_due_at/analysis_category/in_fine_mesh já indexadas em 10-02 — NÃO duplicar). Repetir atributos só se ALTERAR coluna; aqui só ADICIONA índices.

    `ProcessoQueryService::filtered(array $filtros): Builder` — aplica os filtros do SAPS (grupo de status/status; protocol_number; tvl_product_number via decision; bap; service_type_id; sector_id; zona; data_de/data_ate; property_registration; nome/CNPJ via company/requester; CEP/logradouro/bairro) + analista (assigned_user_id) + categoria (deriva: in_fine_mesh→Malha Fina; is_virtual_office→Sede de Escritório; analysis_category Expresso/Semi-Expresso). whereLike com caseSensitive:false (PostgreSQL — padrão 2.4). Eager-load company/decision para evitar N+1.

    `ProcessoController@index`: ProcessoQueryService->filtered + paginação server-side (reusa um per_page existente/config); Inertia::render('gestao/processos/index', [coleção via ProcessoResource + filtros + meta]); audita a consulta ('analise'/'consulta-processos'). CSV simples (?formato=csv) do conjunto filtrado (cabeçalho + linhas; export pleno → HU-131). show: carrega request + decision + currentAnalysisRecord + transitions; Inertia props (404 honesto se necessário); audita.
    `ProcessoResource`: payload de leitura (3 identificadores RN-007: número do processo, BAP, produto TVL; empresa; imóvel; status; categoria derivada; analista; SLA resumido).

    `ProcessoConsultaTest` (SQLite):
    - sem consultar-solicitacoes → 403 auditado; com → 200.
    - filtro por status, por analista, por categoria (in_fine_mesh→Malha Fina) retornam o subconjunto certo; busca por protocolo (case-insensitive) acha.
    - paginação server-side (meta from/to/total); CSV (?formato=csv) retorna text/csv com as linhas filtradas.
    - assertInertia component + props.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=ProcessoConsultaTest</verify>
  <acceptance_criteria>
    - grep "processos" + "consultar-solicitacoes" em routes/gestao.php; grep "caseSensitive" ou whereLike no ProcessoQueryService.
    - migration de índices aditiva (sem duplicar os de 10-02).
    - ProcessoConsultaTest verde (403, filtros SAPS+analista+categoria, paginação, CSV).
  </acceptance_criteria>
  <done>Consulta HU-082 server-driven (filtros completos + analista + categoria + paginação + índices + CSV), gated por consultar-solicitacoes e auditada.</done>
</task>

<task type="auto">
  <name>Task 2: fila do analista (HU-144) — meus processos / caixa do setor + semáforo + contadores</name>
  <read_first>app/Services/Analise/AnalysisSlaService.php, app/Services/Analise/ProcessoQueryService.php</read_first>
  <files>app/Http/Controllers/Gestao/ProcessoController.php, app/Services/Analise/ProcessoQueryService.php, routes/gestao.php, tests/Feature/Analise/ProcessoFilaTest.php</files>
  <action>
    TDD estrito. Rota (consultar-solicitacoes): GET `processos/fila` → fila (`gestao.processos.fila`).

    `ProcessoController@fila` + `ProcessoQueryService`:
    - modos `meus` (assigned_user_id = usuário) e `setor` (sectors do usuário; HU-144 RN-005 respeita o vínculo) — em_analise/em_pendencia, ordenado por analysis_due_at asc.
    - cada item recebe o semáforo (AnalysisSlaService::statusFor com analysis_due_at + analysis_stage_started_at) + tempo restante.
    - contadores por status (aguardando análise = em_analise sem assigned; em análise = assigned; em pendência; vencendo hoje = analysis_due_at <= fim do dia).
    - gestor (distribuir-processos) vê a visão agregada do setor (carga por analista, vermelhos) — props extras.
    - Inertia props (página em 10-16); audita acesso.

    `ProcessoFilaTest` (SQLite):
    - fila 'meus' lista só os do analista, ordenada por prazo; semáforo correto (verde/amarelo/vermelho conforme due/started); contadores corretos.
    - fila 'setor' respeita o vínculo (analista de outro setor não vê).
    - sem consultar-solicitacoes → 403.
  </action>
  <verify>php artisan test --compact --filter=ProcessoFilaTest</verify>
  <acceptance_criteria>
    - grep "processos/fila" em routes/gestao.php; grep "AnalysisSlaService" e "analysis_due_at" no controller/service da fila.
    - ProcessoFilaTest verde (meus/setor; ordenação por prazo; semáforo; contadores; vínculo de setor).
  </acceptance_criteria>
  <done>Fila HU-144 (meus/setor) ordenada por SLA com semáforo on-the-fly e contadores, respeitando o vínculo de setor.</done>
</task>

<task type="auto">
  <name>Task 3: busca global (Cmd+K) — endpoint leve</name>
  <read_first>app/Http/Controllers/Gestao/ProcessoController.php</read_first>
  <files>app/Http/Controllers/Gestao/ProcessoBuscaController.php, routes/gestao.php, tests/Feature/Analise/ProcessoBuscaGlobalTest.php</files>
  <action>
    TDD estrito. Rota (consultar-solicitacoes): GET `processos/busca?q=` → ProcessoBuscaController (`gestao.processos.busca`).
    Endpoint LEVE (HU-082 RN-009): casa q contra protocol_number, BAP (external_reference/bap), tvl_product_number (decision), CNPJ (company), nome (requester) — whereLike caseSensitive:false; retorna até N resultados {id, protocolo, empresa, link para o processo} em JSON (consumido pelo Cmd+K da UI 10-16). Sem paginação pesada (limite pequeno). Audita opcionalmente (consulta leve — à discrição; preferir não poluir a trilha).

    `ProcessoBuscaGlobalTest` (SQLite): busca por protocolo/CNPJ/nome retorna o processo certo; q vazio → vazio; sem permissão → 403.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=ProcessoBuscaGlobalTest</verify>
  <acceptance_criteria>
    - grep "processos/busca" em routes/gestao.php.
    - ProcessoBuscaGlobalTest verde (busca por protocolo/CNPJ/nome; vazio; 403).
  </acceptance_criteria>
  <done>Busca global leve (Cmd+K) por protocolo/BAP/produto/CNPJ/nome, gated; base do atalho da UI.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-082 RN-004/005 — filtros SAPS + analista + categoria | ProcessoConsultaTest (filtros/derivações) |
| HU-082 RN-008 — paginação + índices (desempenho) | ProcessoConsultaTest (paginação) + migration de índices |
| HU-082 RN-009 — busca global | ProcessoBuscaGlobalTest |
| HU-082 RN-011 — exportação CSV (pleno → HU-131) | ProcessoConsultaTest (?formato=csv) |
| HU-082 CA-04 — segurança | ProcessoConsultaTest (403) |
| HU-144 CA-01/RN-003 — fila por prazo + semáforo | ProcessoFilaTest |
| HU-144 RN-005/CA-03 — caixa do setor / visão do gestor | ProcessoFilaTest (vínculo de setor) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="ProcessoConsultaTest|ProcessoFilaTest|ProcessoBuscaGlobalTest"` verde.
- Suíte completa CONTINUA verde; ÚNICO editor de routes/gestao.php na Wave 6 (paralelo file-disjunto com 10-13).
</verification>

<success_criteria>
- HU-082 consulta server-driven (filtros completos + analista + categoria + índices + busca global + CSV) e HU-144 fila por SLA (meus/setor + semáforo + contadores), gated por consultar-solicitacoes.
- Export pleno (XLSX/PDF) registrado → HU-131/Fase 15 (CSV simples agora). ZERO dependência nova.
- ÚNICO route-owner da Wave 6; detalhe/mini-mapa/timeline visual na UI (10-16).
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-14-SUMMARY.md`: as rotas (processos.index/show/fila/busca), o ProcessoQueryService (filtros/derivações de categoria), o ProcessoResource (3 identificadores RN-007), os contadores/semáforo da fila e a decisão CSV-agora/export-pleno-depois. Insumo direto de 10-16 (telas de fila/consulta/detalhe + Cmd+K) e 10-15 (ações usam o mesmo controller/escopo).
</output>

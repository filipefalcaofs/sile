---
phase: 10-analise-tecnica-sedur
plan: 07
type: execute
wave: 3
depends_on: ["10-02", "10-03", "10-05"]
files_modified:
  - app/Services/Expresso/FluxoExpressoService.php
  - app/Services/Analise/DistribuicaoService.php
  - app/Http/Controllers/Gestao/CaixaSetorController.php
  - app/Http/Requests/Gestao/DistribuirProcessoRequest.php
  - routes/gestao.php
  - tests/Feature/Analise/EncaminhamentoAnaliseTest.php
  - tests/Feature/Analise/DistribuicaoServiceTest.php
  - tests/Feature/Analise/CaixaSetorTest.php
autonomous: true
requirements: [HU-079, HU-080, HU-081]

must_haves:
  truths:
    - "HU-079: ao encaminhar à análise (FluxoExpressoService::encaminharAnalise), além da transição/auditoria SÍNCRONA já existentes, o sistema MATERIALIZA analysis_due_at (AnalysisSlaService, etapa distribuição) e DISPARA EncaminhadoParaAnalise APÓS o commit — sem mudar a regra de roteamento da Fase 9 (toggle/semi-expresso/pendente continuam → em_analise)"
    - "HU-080: caixa do setor — DistribuicaoService.distribuir(request|requests, analista, ator) atribui assigned_user_id/assigned_at SEM o processo sair do setor (sector_id intacto), recalcula analysis_due_at (etapa análise) e audita por processo; suporta LOTE (RN-007) com auditoria por item"
    - "HU-081: assumir — DistribuicaoService.assumir(request, analista) deixa o analista do setor pegar um processo da caixa para si (auditado); só analista vinculado ao setor pode assumir/ser distribuído"
    - "CaixaSetorController lista a caixa do setor (processos em_analise do setor, atribuídos e não atribuídos), gated por distribuir-processos (distribuir/lote) e analisar-processos (assumir); 403 auditado sem permissão; ÚNICO editor de routes/gestao.php na Wave 3"
    - "A auditoria do histórico de atribuição (quem/quando/de qual setor) é SÍNCRONA (RN-006) — não depende de evento"
  artifacts:
    - path: "app/Services/Analise/DistribuicaoService.php"
      provides: "Caixa do setor: distribuir (single+lote), assumir, com SLA recalculado e auditoria por processo"
      contains: "assigned_user_id"
    - path: "app/Services/Expresso/FluxoExpressoService.php"
      provides: "encaminharAnalise ganha materialização do SLA + dispatch EncaminhadoParaAnalise after-commit"
      contains: "EncaminhadoParaAnalise"
  key_links:
    - from: "app/Services/Expresso/FluxoExpressoService.php"
      to: "app/Events/EncaminhadoParaAnalise.php"
      via: "EncaminhadoParaAnalise::dispatch($request) APÓS o commit do encaminhamento (pré-análise reage em 10-08)"
      pattern: "EncaminhadoParaAnalise::dispatch"
    - from: "app/Services/Analise/DistribuicaoService.php"
      to: "app/Services/Analise/AnalysisSlaService.php"
      via: "dueAtFor(Analise) recalcula analysis_due_at ao atribuir (HU-144 RN-001 — fonte única de prazo)"
      pattern: "AnalysisSlaService"
---

<objective>
Distribuição e gatilho da pré-análise (Wave 3, backend): completar o `encaminharAnalise` (HU-079 — materializa o SLA e dispara `EncaminhadoParaAnalise` after-commit), e o `DistribuicaoService` (HU-080 caixa do setor/distribuir/lote, HU-081 assumir) com a `CaixaSetorController`. ÚNICO editor de `routes/gestao.php` na Wave 3.

Purpose: as solicitações chegam à análise (em_analise) vindas da Fase 9; aqui elas entram na caixa do setor com prazo (analysis_due_at) e podem ser atribuídas/assumidas sem sair da caixa (modelo SAPS que cobre férias/ausências — HU-080 RN-004). O dispatch do `EncaminhadoParaAnalise` é o gatilho da pré-análise (10-08, listener auto-descoberto, paralelo a este plano). A auditoria do histórico de atribuição é SÍNCRONA (RN-006), não depende do evento — lição das Fases 8/9.
Output: encaminharAnalise estendido + DistribuicaoService + CaixaSetorController + rotas, cobertos por feature tests (encaminhamento dispara evento+SLA; distribuir single/lote; assumir; vínculo de setor; 403).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md
@.planning/phases/10-analise-tecnica-sedur/10-02-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-03-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-05-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-04-SUMMARY.md

# PADRÕES A ESPELHAR (transação+auditoria síncrona+evento after-commit; forceFill de colunas fora do fillable; controller server-driven) — ler antes de codar:
@app/Services/Expresso/FluxoExpressoService.php
@app/Support/Audit/AuditService.php
@app/Http/Controllers/Gestao/ResultadoExpressoController.php
@app/Models/ViabilityRequest.php
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-080-Distribuir-processo.md
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-081-Assumir-analise.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HU-079 — encaminharAnalise materializa o SLA + dispara EncaminhadoParaAnalise (after-commit)</name>
  <read_first>app/Services/Expresso/FluxoExpressoService.php, app/Services/Analise/AnalysisSlaService.php, app/Events/EncaminhadoParaAnalise.php</read_first>
  <files>app/Services/Expresso/FluxoExpressoService.php, tests/Feature/Analise/EncaminhamentoAnaliseTest.php</files>
  <action>
    TDD estrito (RED primeiro). Estender SOMENTE o `encaminharAnalise` do FluxoExpressoService (NÃO mudar a regra de roteamento — toggle/semi-expresso/pendente seguem → em_analise; a Fase 9 fica verde):
    - injetar `AnalysisSlaService` no construtor (adicionar à assinatura; atualizar instâncias/bindings se necessário).
    - DENTRO da transação do encaminharAnalise, após a transição protocolada→em_analise, gravar via forceFill (colunas fora do fillable): `analysis_stage = AnalysisStage::Distribuicao`, `analysis_stage_started_at = now()`, `analysis_due_at = $this->sla->dueAtFor(AnalysisStage::Distribuicao)`. (sector_id/assigned_user_id ficam nulos — a distribuição é a Task 2.)
    - APÓS o commit: `EncaminhadoParaAnalise::dispatch($request)` (gatilho da pré-análise 10-08). A auditoria síncrona 'expresso'/'decisao' result 'analise' já existente permanece.

    `EncaminhamentoAnaliseTest` (SQLite + fake SpatialRepository): protocolada sem zona → decide() → encaminharAnalise:
    - status em_analise; analysis_due_at preenchido (≈ now + 2 dias, etapa distribuição); analysis_stage = distribuicao.
    - Event::fake([EncaminhadoParaAnalise::class]) → assertDispatched(EncaminhadoParaAnalise) com a request; e a auditoria síncrona 'expresso'/'decisao' (result 'analise') continua gravada (assert independe do evento).
    - CONFIRMAR (regressão): caminho deferida/indeferida NÃO dispara EncaminhadoParaAnalise.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="EncaminhamentoAnaliseTest|FluxoExpressoElegibilidadeTest"</verify>
  <acceptance_criteria>
    - grep "EncaminhadoParaAnalise::dispatch" e "analysis_due_at" em app/Services/Expresso/FluxoExpressoService.php.
    - EncaminhamentoAnaliseTest verde (SLA materializado + evento + auditoria síncrona); a suíte do fluxo expresso (Fase 9) segue verde.
  </acceptance_criteria>
  <done>encaminharAnalise materializa o SLA (etapa distribuição) e dispara EncaminhadoParaAnalise after-commit, sem mudar o roteamento da Fase 9.</done>
</task>

<task type="auto">
  <name>Task 2: HU-080/081 — DistribuicaoService (distribuir single+lote, assumir) com SLA + auditoria por processo</name>
  <read_first>app/Support/Audit/AuditService.php, app/Models/Sector.php, app/Models/ViabilityRequest.php, app/Services/Analise/AnalysisSlaService.php</read_first>
  <files>app/Services/Analise/DistribuicaoService.php, tests/Feature/Analise/DistribuicaoServiceTest.php</files>
  <action>
    TDD estrito. `DistribuicaoService` (injeta AnalysisSlaService, AuditService):
    - `distribuir(ViabilityRequest $request, User $analista, ?User $ator = null): void` — valida que $analista pertence ao setor do processo (sectors do analista contém request.sector_id; senão lança/retorna erro de domínio); numa transação grava (forceFill) assigned_user_id=$analista->id, assigned_at=now(), analysis_stage=Analise, analysis_stage_started_at=now(), analysis_due_at=sla->dueAtFor(Analise); SEM mudar sector_id (não sai da caixa — RN-004) nem o status (segue em_analise). Audita SÍNCRONO ('analise'/'distribuir', properties quem/quando/setor — RN-006).
    - `distribuirLote(iterable $requests, User $analista, ?User $ator = null): array` — itera chamando distribuir, auditoria POR processo (RN-007), devolve resumo {ok, falhas}.
    - `assumir(ViabilityRequest $request, User $analista): void` — o próprio analista (vinculado ao setor) pega o processo da caixa para si (mesmo efeito de distribuir, ator=analista); audita ('analise'/'assumir').

    `DistribuicaoServiceTest` (SQLite):
    - distribuir atribui analista vinculado, recalcula analysis_due_at (etapa análise ≈ +10d), NÃO altera sector_id nem status; auditoria 'analise'/'distribuir' gravada com setor.
    - analista NÃO vinculado ao setor → erro de domínio (não atribui).
    - distribuirLote 3 processos → 3 auditorias; resumo ok=3.
    - assumir pelo analista do setor → assigned_user_id = analista; auditado.
  </action>
  <verify>php artisan test --compact --filter=DistribuicaoServiceTest</verify>
  <acceptance_criteria>
    - grep "assigned_user_id" e "AnalysisSlaService" em app/Services/Analise/DistribuicaoService.php; grep "->log('analise'" (auditoria síncrona).
    - DistribuicaoServiceTest verde (distribuir/lote/assumir; vínculo de setor; SLA recalculado; sem sair da caixa).
  </acceptance_criteria>
  <done>DistribuicaoService real: caixa do setor, distribuir (single+lote) e assumir com SLA recalculado e auditoria por processo (RN-006/007).</done>
</task>

<task type="auto">
  <name>Task 3: CaixaSetorController (listar caixa + distribuir/lote + assumir) + rotas</name>
  <read_first>app/Http/Controllers/Gestao/ResultadoExpressoController.php, app/Services/Analise/DistribuicaoService.php, routes/gestao.php</read_first>
  <files>app/Http/Controllers/Gestao/CaixaSetorController.php, app/Http/Requests/Gestao/DistribuirProcessoRequest.php, routes/gestao.php, tests/Feature/Analise/CaixaSetorTest.php</files>
  <action>
    TDD estrito. Rotas em `routes/gestao.php` (ÚNICO editor de rotas da Wave 3), prefixo `caixa-setor`:
    - GET `caixa-setor` → index (lista a caixa: processos em_analise do(s) setor(es) do usuário, atribuídos/não atribuídos) — gated `permission:analisar-processos` (analista vê a caixa do seu setor).
    - POST `caixa-setor/distribuir` → distribuir (single+lote: aceita array de request_ids + analista) — gated `permission:distribuir-processos` (gestor).
    - POST `caixa-setor/{viabilityRequest}/assumir` → assumir — gated `permission:analisar-processos`.

    `CaixaSetorController`: index server-driven (espelha ResultadoExpressoController; filtra por setor do usuário; ordena por analysis_due_at; Inertia props — página em 10-16). distribuir/assumir delegam ao DistribuicaoService; `DistribuirProcessoRequest` valida request_ids (array de ids existentes em_analise do setor) + analista (vinculado ao setor). 403 auditado sem permissão.

    `CaixaSetorTest` (SQLite):
    - analista vê SÓ a caixa do(s) seu(s) setor(es); sem analisar-processos → 403 auditado.
    - distribuir sem distribuir-processos → 403; com → atribui (via service); lote distribui N.
    - assumir pega o processo para o analista (assigned_user_id).
    - assertInertia component + props (lista ordenada por prazo).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=CaixaSetorTest</verify>
  <acceptance_criteria>
    - grep "caixa-setor" em routes/gestao.php sob analisar-processos/distribuir-processos.
    - CaixaSetorTest verde (escopo do setor, 403, distribuir/lote/assumir, Inertia props).
  </acceptance_criteria>
  <done>CaixaSetorController: caixa do setor (escopada), distribuir (single+lote, gestor) e assumir (analista), gated e auditado.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-079 — encaminhar dispara pré-análise + SLA | EncaminhamentoAnaliseTest (evento + analysis_due_at) |
| HU-080 RN-004 — atribui sem sair da caixa | DistribuicaoServiceTest (sector_id intacto) |
| HU-080 RN-007 — distribuição em lote | DistribuicaoServiceTest (lote, auditoria por item) |
| HU-080 RN-006 — histórico de atribuição auditado | DistribuicaoServiceTest (auditoria síncrona com setor) |
| HU-081 — assumir da caixa | DistribuicaoServiceTest + CaixaSetorTest (assumir) |
| HU-080 CA-04 — segurança de acesso | CaixaSetorTest (403 distribuir/analisar) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="EncaminhamentoAnaliseTest|DistribuicaoServiceTest|CaixaSetorTest"` verde.
- A suíte do fluxo expresso (Fase 9) CONTINUA verde (encaminharAnalise só ESTENDIDO; roteamento intacto).
</verification>

<success_criteria>
- HU-079 encaminhamento materializa o SLA e dispara EncaminhadoParaAnalise (gatilho da pré-análise) sem mudar o roteamento da Fase 9.
- HU-080/081 caixa do setor: distribuir (single+lote) e assumir sem sair da caixa, com SLA recalculado e auditoria por processo.
- ÚNICO editor de routes/gestao.php na Wave 3; paralelo file-disjunto com 10-08. ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-07-SUMMARY.md`: o ponto de dispatch do EncaminhadoParaAnalise e a materialização do SLA no encaminharAnalise, a assinatura do DistribuicaoService (distribuir/distribuirLote/assumir), as rotas caixa-setor.* e os gates. Insumo direto de 10-08 (reage ao evento), 10-14 (fila/consulta lê assigned/sector/due_at) e 10-16 (tela da caixa/distribuição).
</output>

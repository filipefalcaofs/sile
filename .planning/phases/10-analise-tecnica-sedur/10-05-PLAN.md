---
phase: 10-analise-tecnica-sedur
plan: 05
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - app/Services/Analise/AnalysisSlaService.php
  - app/Services/Analise/SlaStatus.php
  - tests/Feature/Analise/AnalysisSlaServiceTest.php
autonomous: true
requirements: [HU-144]

must_haves:
  truths:
    - "AnalysisSlaService::dueAtFor(stage, from) materializa o prazo-limite absoluto da etapa REUSANDO o BusinessDeadlineCalculator (Fase 9) e os parâmetros analise.sla.<etapa>_dias — HU-137 (feriados/dias úteis) troca só no calculator, sem tocar este serviço (mesmo seam)"
    - "AnalysisSlaService::statusFor(dueAt, now) devolve o semáforo on-the-fly: verde (dentro do prazo, abaixo do limiar), amarelo (>= analise.sla.semaforo.amarelo_percentual do prazo) e vermelho (estourado) + tempo restante — calculado, nunca persistido como cor"
    - "O prazo é contado em DIAS a partir do parâmetro (default distribuicao=2, analise=10); o limiar amarelo é percentual parametrizável (default 80%) — tudo via Settings::get com default inline (não depende do seeder 10-01)"
    - "Serviço PURO (sem rota, sem persistir além de expor o due_at a quem grava): o materializador é chamado pelo encaminhamento/distribuição (10-07) para gravar analysis_due_at; a fila (HU-144) e a UI (10-16) consomem statusFor"
  artifacts:
    - path: "app/Services/Analise/AnalysisSlaService.php"
      provides: "Cálculo do prazo-limite por etapa (reusa BusinessDeadlineCalculator) e do semáforo on-the-fly"
      contains: "BusinessDeadlineCalculator"
    - path: "app/Services/Analise/SlaStatus.php"
      provides: "DTO/enum do semáforo (verde/amarelo/vermelho + restante) consumido pela fila/UI"
      contains: "amarelo"
  key_links:
    - from: "app/Services/Analise/AnalysisSlaService.php"
      to: "app/Services/Expresso/BusinessDeadlineCalculator.php"
      via: "dueAt(from, dias) — HU-137 (feriados) troca SÓ no calculator (seam pronto)"
      pattern: "BusinessDeadlineCalculator"
    - from: "app/Services/Analise/AnalysisSlaService.php"
      to: "App\\Support\\Settings"
      via: "lê analise.sla.<etapa>_dias e analise.sla.semaforo.amarelo_percentual com default inline"
      pattern: "analise.sla"
---

<objective>
Fundação do SLA da fila de análise (Wave 2): `AnalysisSlaService` que materializa o prazo-limite absoluto por etapa (reusando o `BusinessDeadlineCalculator` da Fase 9) e calcula o semáforo (verde/amarelo/vermelho) + tempo restante on-the-fly, a partir dos parâmetros administráveis.

Purpose: HU-144 transforma o prazo num elemento operacional do dia a dia (não um relatório a posteriori — corrige a distorção "19 dias reportados vs 42h medidos" do legado). O `analysis_due_at` (coluna indexada de 10-02) é a base da ORDENAÇÃO da fila; gravá-lo de forma centralizada (este serviço calcula; 10-07 grava na transição) e calcular o semáforo on-the-fly evita cor persistida e mantém o cálculo parametrizável. Reusar o `BusinessDeadlineCalculator` mantém o seam de HU-137 (feriados) pronto — a regra de dias úteis entra lá sem tocar a fila.
Output: `AnalysisSlaService` + `SlaStatus` (DTO/enum do semáforo), cobertos por feature tests (prazo por etapa, limiar amarelo, vermelho estourado, parametrização).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md
@.planning/phases/10-analise-tecnica-sedur/10-01-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-02-SUMMARY.md

# PADRÕES A ESPELHAR (seam de prazo; Settings com default inline; DTO readonly) — ler antes de codar:
@app/Services/Expresso/BusinessDeadlineCalculator.php
@app/Support/Settings.php
@app/Enums/AnalysisStage.php
@app/Services/Expresso/DecisionResult.php
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-144-Fila-de-trabalho-do-analista-com-SLA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SlaStatus (DTO/enum do semáforo) + AnalysisSlaService::dueAtFor (prazo por etapa, reusa BusinessDeadlineCalculator)</name>
  <read_first>app/Services/Expresso/BusinessDeadlineCalculator.php, app/Enums/AnalysisStage.php, app/Support/Settings.php</read_first>
  <files>app/Services/Analise/SlaStatus.php, app/Services/Analise/AnalysisSlaService.php, tests/Feature/Analise/AnalysisSlaServiceTest.php</files>
  <action>
    TDD estrito (RED primeiro).

    `SlaStatus` (enum string ou readonly DTO — à discrição; preferir enum `Verde='verde'|Amarelo='amarelo'|Vermelho='vermelho'` com label() pt-BR para o badge; o "tempo restante" sai como valor separado no statusFor).

    `AnalysisSlaService` (construtor injeta `BusinessDeadlineCalculator`):
    - `diasDaEtapa(AnalysisStage $stage): int` — lê `analise.sla.{$stage->value}_dias` via `Settings::get(..., config('sile.analise.sla...'))` com DEFAULT INLINE (distribuicao=2, analise=10); etapa sem parâmetro → fallback razoável documentado.
    - `dueAtFor(AnalysisStage $stage, ?DateTimeInterface $from = null): Carbon` — `$from ??= now()`; `return $this->calculator->dueAt($from, $this->diasDaEtapa($stage) * 24);` (HOJE em horas-calendário via o calculator; HU-137 troca a contagem SÓ lá). Documentar a conversão dias→horas e que o seam de dias úteis fica no calculator.

    `AnalysisSlaServiceTest` (SQLite, RefreshDatabase):
    - dueAtFor(Distribuicao, from) == from + 2 dias (default); dueAtFor(Analise) == from + 10 dias.
    - parametrizado: gravar Parameter analise.sla.analise_dias='5' (limpar cache do Settings) → due == from + 5 dias.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=AnalysisSlaServiceTest</verify>
  <acceptance_criteria>
    - grep "BusinessDeadlineCalculator" e "analise.sla" em app/Services/Analise/AnalysisSlaService.php.
    - AnalysisSlaServiceTest verde para dueAtFor (default + parametrizado).
  </acceptance_criteria>
  <done>dueAtFor por etapa reusando o BusinessDeadlineCalculator (seam HU-137) e os parâmetros (default inline).</done>
</task>

<task type="auto">
  <name>Task 2: AnalysisSlaService::statusFor (semáforo on-the-fly + tempo restante)</name>
  <read_first>app/Services/Analise/AnalysisSlaService.php</read_first>
  <files>app/Services/Analise/AnalysisSlaService.php, tests/Feature/Analise/AnalysisSlaServiceTest.php</files>
  <action>
    TDD estrito. Acrescentar ao `AnalysisSlaService`:
    - `statusFor(DateTimeInterface $dueAt, DateTimeInterface $startedAt, ?DateTimeInterface $now = null): array` (ou objeto): calcula o semáforo —
      - VERMELHO se `now > dueAt` (estourado, isOverdue do calculator);
      - AMARELO se a fração decorrida `(now - startedAt) / (dueAt - startedAt)` >= `analise.sla.semaforo.amarelo_percentual/100` (default 80);
      - VERDE caso contrário.
      - devolve também `restante` (humano-legível, ex.: diffForHumans) e o `SlaStatus`.
    - `now` injetável para teste determinístico.

    `AnalysisSlaServiceTest`: 
    - prazo de 10 dias, decorrido 0% → verde; decorrido 85% (após startedAt) → amarelo; now > dueAt → vermelho.
    - limiar parametrizável: amarelo_percentual='50' → vira amarelo aos 50%.
  </action>
  <verify>php artisan test --compact --filter=AnalysisSlaServiceTest</verify>
  <acceptance_criteria>
    - grep "amarelo_percentual" e "isOverdue" (ou comparação now>dueAt) em app/Services/Analise/AnalysisSlaService.php.
    - AnalysisSlaServiceTest verde (verde/amarelo/vermelho + limiar parametrizável).
  </acceptance_criteria>
  <done>Semáforo on-the-fly (verde/amarelo/vermelho + restante) parametrizável; nada de cor persistida.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-144 CA-01 — fila ordenada por prazo (due_at materializado) | AnalysisSlaServiceTest (dueAtFor por etapa) |
| HU-144 RN-002 — limiar do semáforo parametrizável | AnalysisSlaServiceTest (amarelo_percentual) |
| HU-144 CA-02 / HU-137 — seam de dias úteis | AnalysisSlaServiceTest (reusa BusinessDeadlineCalculator; troca futura só lá) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=AnalysisSlaServiceTest` verde.
- Suíte completa CONTINUA verde (serviço novo, sem rota; não toca a Fase 9).
</verification>

<success_criteria>
- AnalysisSlaService materializa o prazo por etapa (reusa BusinessDeadlineCalculator — seam HU-137) e calcula o semáforo on-the-fly, tudo parametrizável (default inline).
- Serviço puro (sem rota); o due_at é gravado por quem transiciona (10-07) e o semáforo consumido pela fila (10-14/10-16).
- ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-05-SUMMARY.md`: assinaturas de dueAtFor/statusFor, o SlaStatus (cores + label), os parâmetros lidos e os defaults inline, e a nota do seam HU-137. Insumo direto de 10-07 (grava analysis_due_at na transição), 10-14 (fila ordenada + contadores) e 10-16 (badge do semáforo).
</output>

---
phase: 10-analise-tecnica-sedur
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Solicitacao/ViabilityRequestStateMachine.php
  - app/Events/EncaminhadoParaAnalise.php
  - tests/Feature/Solicitacao/ViabilityRequestStateMachineTest.php
  - tests/Feature/Analise/EncaminhadoParaAnaliseEventTest.php
autonomous: true
requirements: [HU-079, HU-083, HU-084, HU-086, HU-087, HU-089]

must_haves:
  truths:
    - "A ViabilityRequestStateMachine GANHA as transições da análise humana SEM tocar as existentes: em_analise → {em_pendencia, deferida, indeferida}; em_pendencia → {em_analise}. deferida/indeferida seguem FINAIS (sem saída = encerramento HU-089)"
    - "Anti-regressão travada: deferida→em_analise e indeferida→em_analise INVÁLIDAS (decisão é final); em_pendencia→deferida/indeferida INVÁLIDAS (precisa voltar para em_analise antes de decidir); os testes das Fases 8/9 (protocolada/rascunho/aguardando_bap) seguem verdes"
    - "Existe o evento de domínio EncaminhadoParaAnalise (carrega a ViabilityRequest) — disparado after-commit pelo encaminhamento (wiring em 10-07) e consumido pelo listener AUTO-DESCOBERTO da pré-análise (10-08); criado aqui para que dispatcher e listener fiquem em planos paralelos"
    - "Cada transição nova grava timeline (viability_request_transitions) + auditoria (RN-002) reusando o transition() existente — sem nova mecânica"
  artifacts:
    - path: "app/Services/Solicitacao/ViabilityRequestStateMachine.php"
      provides: "Mapa de transições estendido com a análise humana (em_analise/em_pendencia)"
      contains: "'em_analise' =>"
    - path: "app/Events/EncaminhadoParaAnalise.php"
      provides: "Evento de domínio do encaminhamento à análise (seam dispatcher↔listener auto-descoberto)"
      contains: "class EncaminhadoParaAnalise"
  key_links:
    - from: "app/Services/Solicitacao/ViabilityRequestStateMachine.php"
      to: "tests/Feature/Solicitacao/ViabilityRequestStateMachineTest.php"
      via: "só ADICIONA entradas no mapa; cancelada→deferida, protocolada→rascunho, rascunho→em_analise seguem inválidas (Fases 8/9 verdes)"
      pattern: "'em_pendencia'"
    - from: "app/Events/EncaminhadoParaAnalise.php"
      to: "app/Models/ViabilityRequest.php"
      via: "Dispatchable carregando a request (espelha ResultadoEmitido/SolicitacaoProtocolada)"
      pattern: "ViabilityRequest"
---

<objective>
Fundação da máquina de estados e do evento (Track C da Wave 1): ESTENDER a `ViabilityRequestStateMachine` com as transições da análise humana (`em_analise → {em_pendencia, deferida, indeferida}`; `em_pendencia → {em_analise}`) sem tocar as transições das Fases 8/9, e criar o evento de domínio `EncaminhadoParaAnalise`.

Purpose: a decisão humana (10-10), o ciclo de pendência (10-11) e o encerramento (HU-089) dependem dessas transições; criá-las isoladas (TDD) e antes dos consumidores mantém a anti-regressão travada (decisão é FINAL). O evento `EncaminhadoParaAnalise` é o seam que permite o dispatcher (10-07, no `FluxoExpressoService::encaminharAnalise`) e o listener AUTO-DESCOBERTO da pré-análise (10-08) ficarem em planos PARALELOS — a lição das Fases 8/9 (auto-descoberta, NUNCA Event::listen; travar por CONTAGEM no listener) começa aqui, com o evento estável.
Output: mapa estendido na StateMachine + evento `EncaminhadoParaAnalise`, cobertos por feature tests (transições novas + anti-regressão; construção do evento).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md

# PADRÕES A ESPELHAR (mapa de transições só ADICIONA; evento de domínio Dispatchable) — ler antes de codar:
@app/Services/Solicitacao/ViabilityRequestStateMachine.php
@app/Enums/ViabilityRequestStatus.php
@app/Events/ResultadoEmitido.php
@tests/Feature/Solicitacao/ViabilityRequestStateMachineTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: transições da análise humana na StateMachine (TDD + anti-regressão)</name>
  <read_first>app/Services/Solicitacao/ViabilityRequestStateMachine.php, app/Enums/ViabilityRequestStatus.php, tests/Feature/Solicitacao/ViabilityRequestStateMachineTest.php</read_first>
  <files>app/Services/Solicitacao/ViabilityRequestStateMachine.php, tests/Feature/Solicitacao/ViabilityRequestStateMachineTest.php</files>
  <action>
    TDD estrito (RED→GREEN). ACRESCENTAR testes ao `ViabilityRequestStateMachineTest` SEM apagar os existentes:
    - em_analise→em_pendencia, em_analise→deferida, em_analise→indeferida: válidas e auditadas (transition() grava timeline + Activity 'solicitacoes'/'transicao').
    - em_pendencia→em_analise: válida e auditada.
    - ANTI-REGRESSÃO (inválidas, lançam InvalidStatusTransitionException): deferida→em_analise, indeferida→em_analise, em_pendencia→deferida, em_pendencia→indeferida, em_analise→protocolada.
    - CONFIRMAR que os testes das Fases 8/9 (protocolada→{cancelada,em_analise,deferida,indeferida,aguardando_bap}; aguardando_bap→...; protocolada→rascunho inválida; rascunho→em_analise inválida; cancelada→deferida inválida) seguem verdes.

    No `ViabilityRequestStateMachine::TRANSITIONS`, ACRESCENTAR (sem remover nada):
    - `'em_analise' => ['em_pendencia', 'deferida', 'indeferida']`
    - `'em_pendencia' => ['em_analise']`
    Manter `'rascunho'`, `'protocolada'`, `'aguardando_bap'` intactos. Não criar transação própria (o caller controla — padrão atual).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=ViabilityRequestStateMachineTest</verify>
  <acceptance_criteria>
    - grep "'em_analise' =>" e "'em_pendencia' =>" em app/Services/Solicitacao/ViabilityRequestStateMachine.php.
    - ViabilityRequestStateMachineTest verde — novas transições válidas + anti-regressão (deferida→em_analise inválida) + os testes antigos intactos.
  </acceptance_criteria>
  <done>StateMachine ESTENDIDA com a análise humana (em_analise/em_pendencia); decisão final; anti-regressão e Fases 8/9 verdes.</done>
</task>

<task type="auto">
  <name>Task 2: evento de domínio EncaminhadoParaAnalise</name>
  <read_first>app/Events/ResultadoEmitido.php</read_first>
  <files>app/Events/EncaminhadoParaAnalise.php, tests/Feature/Analise/EncaminhadoParaAnaliseEventTest.php</files>
  <action>
    TDD. Criar `EncaminhadoParaAnalise` (espelhar `ResultadoEmitido`/`SolicitacaoProtocolada`: `use Dispatchable, SerializesModels;` construtor com `public ViabilityRequest $request`). É o evento disparado quando uma solicitação entra em `em_analise` (o dispatch fica no `FluxoExpressoService::encaminharAnalise` em 10-07; o listener auto-descoberto `PreAnalisarProcesso` o consome em 10-08). NÃO registrar listener via Event::listen — auto-descoberta (lição Fases 8/9).

    `EncaminhadoParaAnaliseEventTest`: Event::fake; disparar `EncaminhadoParaAnalise::dispatch($request)` e assertDispatched carregando a request correta (prova o contrato do payload, base do teste de CONTAGEM de listeners em 10-08).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=EncaminhadoParaAnaliseEventTest</verify>
  <acceptance_criteria>
    - grep "class EncaminhadoParaAnalise" + "public ViabilityRequest" em app/Events/EncaminhadoParaAnalise.php.
    - EncaminhadoParaAnaliseEventTest verde (dispatch carrega a request).
  </acceptance_criteria>
  <done>Evento EncaminhadoParaAnalise (Dispatchable) criado — seam estável para dispatcher (10-07) e listener auto-descoberto (10-08).</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-086/087 — decidir a partir de em_analise | ViabilityRequestStateMachineTest (em_analise→deferida/indeferida) |
| HU-083/084 — ciclo de pendência | ViabilityRequestStateMachineTest (em_analise→em_pendencia→em_analise) |
| HU-089 — encerramento (estado final) | ViabilityRequestStateMachineTest (deferida/indeferida sem saída) |
| Anti-regressão — decisão final | ViabilityRequestStateMachineTest (deferida→em_analise inválida) |
| HU-079/140 — seam do encaminhamento | EncaminhadoParaAnaliseEventTest (payload da request) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="ViabilityRequestStateMachineTest|EncaminhadoParaAnaliseEventTest"` verde.
- Suíte completa CONTINUA verde — transições só ADICIONADAS; Fases 8/9 intactas.
</verification>

<success_criteria>
- StateMachine ESTENDIDA com a análise humana (em_analise→{em_pendencia,deferida,indeferida}; em_pendencia→em_analise), decisão final, anti-regressão travada.
- Evento EncaminhadoParaAnalise criado como seam para dispatcher (10-07) ‖ listener auto-descoberto (10-08).
- ZERO dependência nova; nenhuma regressão nas Fases 8/9.
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-03-SUMMARY.md`: o mapa FINAL da StateMachine (entradas novas), as transições inválidas travadas (anti-regressão), e a assinatura do evento EncaminhadoParaAnalise. Insumo direto de 10-07 (dispara o evento no encaminharAnalise), 10-08 (listener auto-descoberto), 10-10 (em_analise→deferida/indeferida), 10-11 (em_analise→em_pendencia→em_analise).
</output>

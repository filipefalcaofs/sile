---
phase: 10-analise-tecnica-sedur
plan: 11
type: execute
wave: 5
depends_on: ["10-02", "10-03"]
files_modified:
  - app/Services/Analise/PendenciaService.php
  - app/Events/PendenciaSolicitada.php
  - app/Notifications/PendenciaSolicitadaNotification.php
  - app/Http/Controllers/Portal/PendenciaRespostaController.php
  - app/Http/Requests/Portal/ResponderPendenciaRequest.php
  - routes/portal.php
  - resources/js/pages/portal/solicitacoes/pendencias.tsx
  - tests/Feature/Analise/PendenciaServiceTest.php
  - tests/Feature/Analise/PendenciaRespostaPortalTest.php
autonomous: true
requirements: [HU-083, HU-084]

must_haves:
  truths:
    - "HU-083 (PARCIAL — ciclo interno): PendenciaService::abrir(request, analista, descricao) cria analysis_pendencies (status aberta, due_at por analise.pendencia.prazo_resposta_dias) e transiciona em_analise→em_pendencia; dispara o evento PendenciaSolicitada (gancho EP11) e envia e-mail SIMPLES ao requerente (in-app/multicanal pleno → EP11)"
    - "HU-084: o requerente responde a pendência pelo portal (Minhas solicitações) → PendenciaService::responder grava response/responded_at (status respondida) e transiciona em_pendencia→em_analise (reabre a análise)"
    - "Anti-fachada: o convite via Simplifica/Regin está BLOQUEADO → EP11/Fase 13 — aqui é portal SILE + e-mail simples REAIS; o evento PendenciaSolicitada é o gancho honesto para os canais plenos, NUNCA um 'enviado' simulado"
    - "PendenciaService é serviço (a abertura pelo analista — endpoint gestao — é wired em 10-15); este plano entrega o ciclo: serviço + evento + e-mail + a RESPOSTA pelo portal (routes/portal.php + página) — ÚNICO editor de routes/portal.php na Wave 5, disjunto de 10-10/10-12"
    - "Toda transição/ação audita (RN-002); só o dono da solicitação (ou representante) responde a própria pendência (policy/escopo)"
  artifacts:
    - path: "app/Services/Analise/PendenciaService.php"
      provides: "Ciclo de pendência interno: abrir (→em_pendencia) e responder (→em_analise)"
      contains: "em_pendencia"
    - path: "app/Events/PendenciaSolicitada.php"
      provides: "Evento gancho para a comunicação plena do EP11 (multicanal)"
      contains: "class PendenciaSolicitada"
    - path: "app/Http/Controllers/Portal/PendenciaRespostaController.php"
      provides: "Resposta do requerente pelo portal (reabre a análise)"
      contains: "responder"
  key_links:
    - from: "app/Services/Analise/PendenciaService.php"
      to: "app/Services/Solicitacao/ViabilityRequestStateMachine.php"
      via: "abrir: em_analise→em_pendencia; responder: em_pendencia→em_analise (transições de 10-03)"
      pattern: "EmPendencia"
    - from: "app/Services/Analise/PendenciaService.php"
      to: "app/Events/PendenciaSolicitada.php"
      via: "PendenciaSolicitada::dispatch após o commit (gancho EP11; e-mail simples agora)"
      pattern: "PendenciaSolicitada::dispatch"
---

<objective>
Ciclo de pendência interno (Wave 5, PARCIAL HU-083/084): `PendenciaService` (abrir → em_pendencia; responder → em_analise), o evento gancho `PendenciaSolicitada`, o e-mail simples ao requerente e a RESPOSTA pelo portal (Minhas solicitações). Owns `routes/portal.php` (disjunto do gestao).

Purpose: o analista precisa pedir complementação ao requerente e reabrir a análise quando ela chega. O escopo AGORA é o ciclo interno real (estado em_pendencia + portal SILE + e-mail simples); o convite via Simplifica/Regin e os canais plenos (WhatsApp/in-app/templates) ficam BLOQUEADOS → EP11/Fase 13 — registrados, nunca simulados. O evento `PendenciaSolicitada` é o gancho honesto para esses canais. A abertura pelo analista (endpoint gestao) é wired em 10-15; aqui entrego o serviço + evento + e-mail + a resposta do cidadão.
Output: PendenciaService + PendenciaSolicitada + notification + PendenciaRespostaController (portal) + página de resposta + rotas portal, cobertos por feature tests (abrir/responder; transições; e-mail; escopo do dono).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md
@.planning/phases/10-analise-tecnica-sedur/10-02-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-03-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-07-SUMMARY.md

# PADRÕES A ESPELHAR (notification sem anexo + EmailLog; evento de domínio; portal route + página; policy/escopo do dono) — ler antes de codar:
@app/Notifications/ResultadoExpressoNotification.php
@app/Events/ResultadoEmitido.php
@routes/portal.php
@app/Services/Solicitacao/ViabilityRequestStateMachine.php
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-083-Solicitar-pendência.md
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-084-Receber-complementacao.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PendenciaService (abrir/responder) + PendenciaSolicitada + e-mail simples</name>
  <read_first>app/Services/Solicitacao/ViabilityRequestStateMachine.php, app/Notifications/ResultadoExpressoNotification.php, app/Support/Audit/AuditService.php</read_first>
  <files>app/Services/Analise/PendenciaService.php, app/Events/PendenciaSolicitada.php, app/Notifications/PendenciaSolicitadaNotification.php, tests/Feature/Analise/PendenciaServiceTest.php</files>
  <action>
    TDD estrito (RED primeiro). `PendenciaSolicitada` (Dispatchable, carrega ViabilityRequest + AnalysisPendency) — gancho EP11.

    `PendenciaService` (injeta ViabilityRequestStateMachine, AuditService):
    - `abrir(ViabilityRequest $request, User $analista, string $descricao): AnalysisPendency` — exige status em_analise; transação: cria analysis_pendencies (requested_by=$analista, description, status Aberta, due_at = now()+analise.pendencia.prazo_resposta_dias via Settings default inline 15) → transiciona em_analise→em_pendencia (StateMachine) → audita SÍNCRONO ('analise'/'pendencia-aberta'). APÓS o commit: PendenciaSolicitada::dispatch + envia PendenciaSolicitadaNotification (e-mail simples, SEM anexo) ao requerente (respeitar toggle de notificação se existir; degradação controlada).
    - `responder(AnalysisPendency $pendency, string $resposta): void` — exige status aberta + request em_pendencia; transação: grava response/responded_at + status Respondida → transiciona em_pendencia→em_analise (reabre) → audita ('analise'/'pendencia-respondida').
    - (expiração por prazo é gancho do scheduler EP11/HU-147 — fora do escopo; o due_at já fica gravado.)

    `PendenciaSolicitadaNotification` (espelhar ResultadoExpressoNotification — via mail, EmailLog, sem anexo): assunto/corpo simples informando a pendência e o link do portal; texto honesto (canal pleno EP11).

    `PendenciaServiceTest` (SQLite, Notification::fake):
    - abrir cria pendência (status aberta, due_at +15d), transiciona em_analise→em_pendencia, dispara PendenciaSolicitada (Event::fake) e envia notification (Notification::assertSentTo o requerente).
    - abrir fora de em_analise → erro de domínio.
    - responder grava response + status respondida, transiciona em_pendencia→em_analise; auditoria das duas pontas.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=PendenciaServiceTest</verify>
  <acceptance_criteria>
    - grep "EmPendencia" e "PendenciaSolicitada::dispatch" em app/Services/Analise/PendenciaService.php.
    - grep "class PendenciaSolicitada" no evento.
    - PendenciaServiceTest verde (abrir/responder; transições; evento + e-mail simples; escopo de estado).
  </acceptance_criteria>
  <done>Ciclo de pendência interno real (abrir→em_pendencia / responder→em_analise) com evento gancho + e-mail simples; convite Simplifica/Regin bloqueado → EP11.</done>
</task>

<task type="auto">
  <name>Task 2: resposta da pendência pelo portal (Minhas solicitações) — controller + rota + página</name>
  <read_first>routes/portal.php, app/Services/Analise/PendenciaService.php</read_first>
  <files>app/Http/Controllers/Portal/PendenciaRespostaController.php, app/Http/Requests/Portal/ResponderPendenciaRequest.php, routes/portal.php, resources/js/pages/portal/solicitacoes/pendencias.tsx, tests/Feature/Analise/PendenciaRespostaPortalTest.php</files>
  <action>
    TDD estrito. Rotas em `routes/portal.php` (ÚNICO editor de portal na Wave 5), sob auth do portal (web), escopadas ao dono/representante:
    - GET `portal/solicitacoes/{viabilityRequest}/pendencias` → show (pendências abertas da própria solicitação).
    - POST `portal/solicitacoes/{viabilityRequest}/pendencias/{pendency}/responder` → responder.

    `PendenciaRespostaController`: policy/escopo — só o requester (ou representante, reusando o mecanismo da Fase 1) acessa/responde a própria pendência (senão 403 auditado); delega a `PendenciaService::responder`; `ResponderPendenciaRequest` valida response (required string). Página `pendencias.tsx` (Inertia/React, padrão do portal — light): lista a pendência aberta + textarea de resposta + submit (useForm). Mobile-first/acessível.

    `PendenciaRespostaPortalTest` (SQLite):
    - dono responde → status respondida + request volta a em_analise; outro usuário → 403 auditado.
    - responder pendência inexistente/já respondida → erro tratado.
    - assertInertia component + props (a pendência aberta).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=PendenciaRespostaPortalTest && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - grep "pendencias" em routes/portal.php; grep "responder" em app/Http/Controllers/Portal/PendenciaRespostaController.php.
    - PendenciaRespostaPortalTest verde (dono responde reabre análise; 403 para terceiros) e tsc verde.
  </acceptance_criteria>
  <done>Resposta da pendência pelo portal (escopada ao dono) reabrindo a análise; página do portal + rota.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-083 — abrir pendência (→em_pendencia) + notificar | PendenciaServiceTest (abrir + Notification) |
| HU-084 — resposta reabre a análise (→em_analise) | PendenciaServiceTest + PendenciaRespostaPortalTest |
| Anti-fachada — convite Simplifica/Regin bloqueado (gancho) | PendenciaServiceTest (PendenciaSolicitada como gancho; e-mail simples real) |
| Escopo/segurança — só o dono responde | PendenciaRespostaPortalTest (403 para terceiros) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="PendenciaServiceTest|PendenciaRespostaPortalTest"` verde; `npx tsc --noEmit` verde.
- Suíte completa CONTINUA verde; ÚNICO editor de routes/portal.php na Wave 5 (disjunto de 10-10/10-12).
</verification>

<success_criteria>
- Ciclo de pendência interno REAL (abrir→em_pendencia / responder→em_analise) com evento gancho PendenciaSolicitada + e-mail simples; resposta pelo portal escopada ao dono.
- Convite Simplifica/Regin + multicanal BLOQUEADOS → EP11/Fase 13 (registrado, não simulado).
- ZERO dependência nova; a abertura pelo analista (endpoint gestao) é wired em 10-15.
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-11-SUMMARY.md`: as assinaturas de PendenciaService (abrir/responder), o evento PendenciaSolicitada (gancho EP11), a notification simples, as rotas do portal e o escopo do dono. Insumo direto de 10-15 (endpoint gestao de abrir pendência) e 10-16/10-17 (badge de pendência no detalhe/ficha; aviso ao requerente).
</output>

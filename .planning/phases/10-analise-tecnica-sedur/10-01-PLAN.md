---
phase: 10-analise-tecnica-sedur
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/seeders/ParameterSeeder.php
  - database/seeders/RolesAndPermissionsSeeder.php
  - config/sile.php
  - tests/Feature/Seeders/ParameterSeederTest.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
  - tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
autonomous: true
requirements: [HU-014, HU-082, HU-132, HU-136, HU-138, HU-142, HU-144, HU-083]

must_haves:
  truths:
    - "O catálogo HU-014 ganha 11 parâmetros do grupo novo 'analise' (toggle features.analise_tecnica fica no grupo 'features' existente): SLA por etapa, semáforo, prazo de pendência, janela/itens de precedentes e os 4 do TVL PDF — administráveis com tipo, default, validação e histórico; total do catálogo 56→67"
    - "5 permissões aditivas novas (analisar-processos, distribuir-processos, emitir-tvl, encaminhar-malha-fina, manter-setores) entram via givePermissionTo (NUNCA sync — preserva ajustes do admin) e são atribuídas aos papéis (analista/gestor/administrador); total 19→24"
    - "config/sile.php espelha os parâmetros de negócio analise.* / features.analise_tecnica (fallback do Settings::get sem banco) e guarda as CONSTANTES TÉCNICAS fora do catálogo (autosave debounce, cache de precedentes) com comentário do porquê"
    - "Reusa risco.mapa_encaminhamento (Fase 6) e os parâmetros do expresso — NÃO recria parâmetro de encaminhamento; este plano é o ÚNICO dono da mudança de catálogo/permissões da Fase 10"
    - "ParameterSeederTest, DatabaseSeederTest e RolesAndPermissionsSeederTest seguem verdes com as contagens atualizadas (67 parâmetros / 24 permissões) e o grupo 'analise' inserido na lista ordenada"
  artifacts:
    - path: "database/seeders/ParameterSeeder.php"
      provides: "11 novos parâmetros administráveis do grupo analise (HU-014)"
      contains: "analise.precedentes.janela_meses"
    - path: "database/seeders/RolesAndPermissionsSeeder.php"
      provides: "5 permissões aditivas da análise técnica + atribuição por papel"
      contains: "analisar-processos"
    - path: "config/sile.php"
      provides: "Fallback dos parâmetros analise.* + constantes técnicas (autosave/cache)"
      contains: "'analise' =>"
  key_links:
    - from: "database/seeders/ParameterSeeder.php"
      to: "config/sile.php"
      via: "cada parâmetro do catálogo tem o espelho de fallback em sile.analise.* / sile.features.analise_tecnica (Settings::get lê config quando o banco está indisponível)"
      pattern: "analise\\."
    - from: "tests/Feature/Seeders/ParameterSeederTest.php"
      to: "database/seeders/ParameterSeeder.php"
      via: "asserção de contagem 67 + grupo 'analise' na lista ordenada + asserts por chave dos novos parâmetros"
      pattern: "assertSame\\(67"
    - from: "tests/Feature/Authorization/RolesAndPermissionsSeederTest.php"
      to: "database/seeders/RolesAndPermissionsSeeder.php"
      via: "asserção de contagem 24 + asserts das 5 permissões novas e dos papéis"
      pattern: "analisar-processos"
---

<objective>
Fundação de parametrização e segurança da Fase 10 (Track A da Wave 1, ÚNICO dono do catálogo): registrar no catálogo HU-014 os 11 parâmetros administráveis da análise técnica (grupo `analise`), as 5 permissões aditivas, e espelhar tudo em `config/sile.php` (fallback + constantes técnicas). Nada de valor de negócio hardcoded — SLA por etapa, limiar do semáforo, prazo de pendência, janela/itens de precedentes, disco/assinatura/TTL do TVL e o toggle da feature nascem administráveis, com efeito sem deploy.

Purpose: a Fase 10 inteira lê parâmetros via `Settings::get(..., config('sile....'))` e gates via `permission:`. Garantir o catálogo + fallback + permissões ANTES dos consumidores (SLA, precedentes, TVL, fila, decisão, setores) evita acoplamento ao seeder e mantém os serviços testáveis com default inline. Centralizar a mudança de catálogo/permissões em UM plano impede que planos paralelos colidam nos testes de contagem (precedente das Fases 8/9).
Output: 11 parâmetros novos no `ParameterSeeder` (catálogo 56→67, grupo novo `analise`), 5 permissões no `RolesAndPermissionsSeeder` (19→24), bloco `analise` + toggle em `config/sile.php`, e os três testes de seeder atualizados e verdes.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md

# PADRÕES A ESPELHAR (catálogo HU-014; permissões aditivas; fallback config; testes de contagem) — ler antes de codar:
@database/seeders/ParameterSeeder.php
@database/seeders/RolesAndPermissionsSeeder.php
@config/sile.php
@app/Support/Settings.php
@tests/Feature/Seeders/ParameterSeederTest.php
@tests/Feature/Seeders/DatabaseSeederTest.php
@tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: 11 parâmetros novos (grupo analise) no catálogo HU-014 + asserts no ParameterSeederTest + fallback config</name>
  <read_first>database/seeders/ParameterSeeder.php, tests/Feature/Seeders/ParameterSeederTest.php, config/sile.php</read_first>
  <files>database/seeders/ParameterSeeder.php, config/sile.php, tests/Feature/Seeders/ParameterSeederTest.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <action>
    TDD: PRIMEIRO atualizar `ParameterSeederTest` (RED), depois adicionar os parâmetros ao `catalog()`.

    No `ParameterSeederTest`:
    - trocar a contagem do catálogo e da idempotência de `assertSame(56, ...)` para `assertSame(67, ...)`.
    - atualizar a lista ordenada de grupos inserindo `'analise'` no início: `['analise', 'expresso', 'features', 'geo', 'integracoes', 'louos', 'retencao', 'risco', 'seguranca', 'solicitacao', 'ui']` (conferir a lista real existente e só ACRESCENTAR 'analise').
    - acrescentar `test_seeder_registra_parametros_da_analise_tecnica`: asserir por chave group/type/default_value/validation_rules dos 11 novos (e `value` null).

    No `ParameterSeeder::catalog()`, acrescentar (chaves EXATAS; group conforme indicado; reusar risco.mapa_encaminhamento, NÃO recriar):
    1. `features.analise_tecnica` — group `features`, type `boolean`, default `'1'`, rules `['required','boolean']`, desc "Habilita o módulo de análise técnica (fila, ficha, decisão humana, TVL PDF); desligado, degrada de forma comunicada".
    2. `analise.sla.distribuicao_dias` — group `analise`, type `integer`, default `'2'`, rules `['required','integer','min:1','max:60']`, desc "Prazo (dias) para distribuir/assumir um processo na caixa do setor antes do vencimento (etapa distribuição)".
    3. `analise.sla.analise_dias` — group `analise`, type `integer`, default `'10'`, rules `['required','integer','min:1','max:180']`, desc "Prazo (dias) para concluir a análise técnica de um processo assumido (etapa análise)".
    4. `analise.sla.semaforo.amarelo_percentual` — group `analise`, type `integer`, default `'80'`, rules `['required','integer','min:1','max:99']`, desc "Percentual do prazo a partir do qual o semáforo do SLA fica amarelo (HU-144 RN-002)".
    5. `analise.pendencia.prazo_resposta_dias` — group `analise`, type `integer`, default `'15'`, rules `['required','integer','min:1','max:180']`, desc "Prazo (dias) para o requerente responder a uma pendência antes de expirar (HU-083/084)".
    6. `analise.precedentes.janela_meses` — group `analise`, type `integer`, default `'12'`, rules `['required','integer','min:1','max:120']`, desc "Janela temporal (meses) das estatísticas de precedentes do CNAE na zona (HU-142 RN-003)".
    7. `analise.precedentes.max_itens` — group `analise`, type `integer`, default `'10'`, rules `['required','integer','min:1','max:50']`, desc "Quantidade máxima de processos precedentes do imóvel exibidos na ficha (HU-142 RN-003)".
    8. `analise.tvl.disk` — group `analise`, type `string`, default `'local'`, rules `['required','string','max:50']`, desc "Disco de storage onde o TVL PDF é gravado — NUNCA público; download por URL assinada (HU-132)".
    9. `analise.tvl.assinatura.modo` — group `analise`, type `string`, default `'imagem'`, rules `['required','in:imagem,nenhuma']`, desc "Modo de assinatura do TVL PDF: imagem do diretor (legado) ou nenhuma; assinatura digital gov.br/ICP é gancho → SEDUR (HU-132 RN-005)".
    10. `analise.tvl.assinatura.imagem_path` — group `analise`, type `string`, default `''`, rules `['nullable','string','max:255']`, desc "Caminho da imagem de assinatura usada no TVL PDF quando o modo é 'imagem'".
    11. `analise.tvl.download.ttl_minutos` — group `analise`, type `integer`, default `'5'`, rules `['required','integer','min:1','max:1440']`, desc "Validade (minutos) da URL temporária assinada de download do TVL PDF no backoffice".

    Em `config/sile.php`: acrescentar `'analise_tecnica' => true` no bloco `features` e criar o bloco `'analise'` espelhando os 10 parâmetros de negócio E guardando as CONSTANTES TÉCNICAS (fora do catálogo — comentário do porquê): `autosave` (debounce_ms da ficha, NÃO administrável) e `precedentes.cache_ttl_segundos`. Manter o comentário-padrão do arquivo.

    No `DatabaseSeederTest`: atualizar qualquer asserção de contagem de parâmetros de 56 para 67 (manter as demais asserções das fases anteriores intactas). NÃO alterar a contagem de permissões aqui (é a Task 2).

    Manter o padrão do seeder (upsert por key SÓ de metadados; `value` nunca no update — preserva o que o admin gravou).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="ParameterSeederTest|DatabaseSeederTest"</verify>
  <acceptance_criteria>
    - grep "analise.precedentes.janela_meses" e "analise.tvl.disk" em database/seeders/ParameterSeeder.php.
    - grep "assertSame(67" em tests/Feature/Seeders/ParameterSeederTest.php (catálogo + idempotência).
    - grep "'analise'" na lista de grupos do ParameterSeederTest e "'analise' =>" em config/sile.php.
    - php artisan config:show sile.analise.precedentes.janela_meses retorna 12.
    - ParameterSeederTest e DatabaseSeederTest verdes (catálogo 67, grupo analise).
  </acceptance_criteria>
  <done>11 parâmetros novos (grupo analise + toggle features), catálogo 56→67, fallback em config/sile.php (negócio + constantes técnicas), testes verdes.</done>
</task>

<task type="auto">
  <name>Task 2: 5 permissões aditivas + atribuição por papel + RolesAndPermissionsSeederTest (19→24)</name>
  <read_first>database/seeders/RolesAndPermissionsSeeder.php, tests/Feature/Authorization/RolesAndPermissionsSeederTest.php, tests/Feature/Seeders/DatabaseSeederTest.php</read_first>
  <files>database/seeders/RolesAndPermissionsSeeder.php, tests/Feature/Authorization/RolesAndPermissionsSeederTest.php</files>
  <action>
    TDD: PRIMEIRO atualizar `RolesAndPermissionsSeederTest` (RED — contagem 19→24 + asserts das 5 novas e dos papéis), depois ajustar o seeder.

    No `RolesAndPermissionsSeeder`:
    - acrescentar ao array `$permissions` (mantendo as 19 existentes): `'analisar-processos'`, `'distribuir-processos'`, `'emitir-tvl'`, `'encaminhar-malha-fina'`, `'manter-setores'`.
    - atribuição ADITIVA (givePermissionTo, NUNCA sync):
      - `analista`: + `'analisar-processos'`, `'encaminhar-malha-fina'`, `'emitir-tvl'` (analista emite TVL interno e provoca malha fina; mantém consultar-solicitacoes já existente).
      - `gestor`: + `'analisar-processos'`, `'distribuir-processos'`, `'encaminhar-malha-fina'`, `'emitir-tvl'`, `'manter-setores'`.
      - `administrador`: + as 5 (administra setores e tudo).
    - manter `app(PermissionRegistrar::class)->forgetCachedPermissions()` no início e no fim.

    No `RolesAndPermissionsSeederTest` (em tests/Feature/Authorization/): a asserção de contagem total de permissões está em `test_seeder_e_idempotente` (`assertSame(19, Permission::query()->count())`) — atualizar 19→24; asserir que as 5 novas existem (guard web) e a atribuição por papel (ex.: analista TEM analisar-processos e NÃO TEM distribuir-processos; gestor TEM distribuir-processos e manter-setores). NÃO tocar contagem de permissões no `DatabaseSeederTest` — ele NÃO assere permissões (só roles=4 e parâmetros, estes já em 67 pela Task 1).

    Decisão a registrar no SUMMARY: a consulta de processos (HU-082) reusa `consultar-solicitacoes` (não cria permissão de consulta nova — a validar com a SEDUR), como no resultado expresso (09-11).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="RolesAndPermissionsSeederTest|DatabaseSeederTest"</verify>
  <acceptance_criteria>
    - grep "analisar-processos" e "manter-setores" em database/seeders/RolesAndPermissionsSeeder.php (array + atribuições).
    - grep "givePermissionTo" (sem sync) preservado.
    - RolesAndPermissionsSeederTest verde (24 permissões; atribuição por papel) e DatabaseSeederTest verde (67 parâmetros; sem contagem de permissões nele).
  </acceptance_criteria>
  <done>5 permissões aditivas (19→24) atribuídas aos papéis sem sync; testes de seeder verdes.</done>
</task>

</tasks>

## Mapa CA → teste (HU-014)
| CA / RN | Teste |
|---|---|
| HU-014 — parâmetro com tipo/default/validação/histórico | ParameterSeederTest::test_seeder_registra_parametros_da_analise_tecnica |
| HU-014 — catálogo completo e idempotente (67) | ParameterSeederTest::test_seeder_cria_catalogo_completo / test_seeder_e_idempotente |
| HU-013/RN-003 — permissões granulares por papel | RolesAndPermissionsSeederTest (5 novas + atribuição) |
| HU-014 — fallback sem banco (config) | config:show sile.analise.* + DatabaseSeederTest |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="ParameterSeederTest|DatabaseSeederTest|RolesAndPermissionsSeederTest"` verde (67 parâmetros, grupo analise, 24 permissões).
- `php artisan config:show sile.analise` mostra o bloco (negócio + constantes técnicas).
- Suíte completa CONTINUA verde (só catálogo/permissões/config; nenhum consumidor alterado).
</verification>

<success_criteria>
- 11 parâmetros administráveis novos (catálogo 56→67, grupo analise) com fallback em config/sile.php.
- 5 permissões aditivas (19→24) atribuídas por papel sem sync; constantes técnicas só no config (parametrização do que faz sentido).
- ÚNICO plano dono do catálogo da Fase 10; reusa risco.mapa_encaminhamento e os parâmetros do expresso. ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-01-SUMMARY.md`: as 11 chaves novas (group/type/default/rules), a estrutura do bloco `analise` no config (negócio + constantes técnicas com valores), as 5 permissões e suas atribuições por papel, a nova contagem (67 / 24) e a decisão de reusar consultar-solicitacoes na consulta. Insumo direto de 10-05 (SLA), 10-06 (precedentes), 10-04 (manter-setores), 10-07 (distribuir/analisar), 10-09/10-10 (analisar), 10-11 (pendência), 10-12 (encaminhar-malha-fina), 10-13 (TVL/emitir-tvl).
</output>

---
phase: 10-analise-tecnica-sedur
plan: 12
type: execute
wave: 5
depends_on: ["10-02"]
files_modified:
  - app/Services/Analise/MalhaFinaService.php
  - tests/Feature/Analise/MalhaFinaServiceTest.php
autonomous: true
requirements: [HU-136]

must_haves:
  truths:
    - "HU-136: MalhaFinaService::encaminhar(request, ator, motivo) cria fine_mesh_referrals (motivo OBRIGATÓRIO — RN-002) e liga a flag in_fine_mesh — ORTOGONAL ao status: NÃO muda o status do processo (flag + tabela, não estado)"
    - "RN-001: funciona em QUALQUER status, inclusive deferida/indeferida (corrige o bug legado que recusava deferidos) — provado em teste sobre um processo já decidido"
    - "RN-004: encaminhamento em LOTE (lista de processos, motivo único) com auditoria POR processo; RN-003: malha fina é distinta de semi-expresso (gatilho) e da caixa de entrada (chegada automática)"
    - "Repetível: o mesmo processo pode ser encaminhado mais de uma vez (cada um é uma linha em fine_mesh_referrals); resolver(referral) marca resolved_at sem mexer no status"
    - "Serviço PURO (sem rota): o endpoint encaminhar (incl. lote da consulta) é wired em 10-15 (Wave 7); aqui a lógica é testada no nível de serviço; toda ação audita (RN-002)"
  artifacts:
    - path: "app/Services/Analise/MalhaFinaService.php"
      provides: "Encaminhar à malha fina (flag + tabela, ortogonal ao status, qualquer status, lote) + resolver"
      contains: "in_fine_mesh"
  key_links:
    - from: "app/Services/Analise/MalhaFinaService.php"
      to: "app/Models/FineMeshReferral.php"
      via: "create(reason obrigatório) + ViabilityRequest.in_fine_mesh=true (flag ortogonal — NÃO transiciona status)"
      pattern: "FineMeshReferral"
---

<objective>
Malha fina (Wave 5, serviço puro): `MalhaFinaService` — encaminhamento humano provocado de QUALQUER processo (inclusive já decidido) para revisão, com motivo obrigatório, em lote, ORTOGONAL ao status (flag `in_fine_mesh` + tabela `fine_mesh_referrals`). Endpoint HTTP em 10-15.

Purpose: HU-136 é provocação humana (distinta do semi-expresso automático e da caixa de entrada — RN-003). O legado tinha um bug que recusava encaminhar deferidos; aqui a malha fina atinge QUALQUER status (RN-001) porque é flag+tabela, não transição de estado — não muda o desfecho, só sinaliza revisão. Repetível e em lote (RN-004), auditada por processo (RN-002).
Output: `MalhaFinaService` (encaminhar single+lote, resolver), coberto por feature tests (qualquer status incl. deferida; motivo obrigatório; lote; flag ortogonal; repetível).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md
@.planning/phases/10-analise-tecnica-sedur/10-02-SUMMARY.md

# PADRÕES A ESPELHAR (serviço transacional + auditoria síncrona; forceFill de coluna fora do fillable) — ler antes de codar:
@app/Services/Analise/DistribuicaoService.php
@app/Support/Audit/AuditService.php
@app/Models/FineMeshReferral.php
@app/Models/ViabilityRequest.php
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-136-Encaminhar-para-malha-fina.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MalhaFinaService (encaminhar single + lote + resolver) ortogonal ao status</name>
  <read_first>app/Models/FineMeshReferral.php, app/Models/ViabilityRequest.php, app/Support/Audit/AuditService.php</read_first>
  <files>app/Services/Analise/MalhaFinaService.php, tests/Feature/Analise/MalhaFinaServiceTest.php</files>
  <action>
    TDD estrito (RED primeiro). `MalhaFinaService` (injeta AuditService):
    - `encaminhar(ViabilityRequest $request, User $ator, string $motivo): FineMeshReferral` — motivo OBRIGATÓRIO (string vazia → erro de domínio, RN-002); transação: cria fine_mesh_referrals (referred_by=$ator, reason=$motivo, resolved_at null) + forceFill in_fine_mesh=true (flag ortogonal — NÃO chama a StateMachine, NÃO muda o status); audita SÍNCRONO ('analise'/'malha-fina-encaminhar', properties motivo/status atual). Funciona em QUALQUER status (não valida status — RN-001).
    - `encaminharLote(iterable $requests, User $ator, string $motivo): array` — itera, auditoria POR processo (RN-004), resumo {ok, falhas}.
    - `resolver(FineMeshReferral $referral, User $ator): void` — marca resolved_at=now(); audita. (A flag in_fine_mesh pode permanecer enquanto houver referral aberto; ao resolver o último, opcionalmente baixa a flag — decisão à discrição, documentar.)

    `MalhaFinaServiceTest` (SQLite):
    - encaminhar em_analise → fine_mesh_referrals criado + in_fine_mesh=true; status INALTERADO.
    - **encaminhar um processo DEFERIDO** (status deferida) → funciona (RN-001, corrige bug legado); status segue deferida.
    - motivo vazio → erro de domínio (não cria).
    - repetível: encaminhar 2x o mesmo processo → 2 linhas em fine_mesh_referrals.
    - lote 3 processos → 3 referrals + 3 auditorias.
    - resolver marca resolved_at sem mexer no status.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=MalhaFinaServiceTest</verify>
  <acceptance_criteria>
    - grep "in_fine_mesh" e ausência de chamada à StateMachine em app/Services/Analise/MalhaFinaService.php (ortogonal ao status).
    - grep "->log('analise'" (auditoria síncrona por processo).
    - MalhaFinaServiceTest verde (qualquer status incl. deferida; motivo obrigatório; lote; repetível; resolver).
  </acceptance_criteria>
  <done>MalhaFinaService real: encaminha qualquer processo (incl. deferido) por flag+tabela ortogonal ao status, em lote, auditado; resolver.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-136 RN-001 — qualquer status, inclusive deferido | MalhaFinaServiceTest (encaminhar deferido) |
| HU-136 RN-002 — motivo obrigatório + auditoria | MalhaFinaServiceTest (motivo vazio → erro; auditoria) |
| HU-136 RN-004 — lote com auditoria por processo | MalhaFinaServiceTest (lote 3) |
| HU-136 RN-003 — ortogonal (flag, não estado) | MalhaFinaServiceTest (status inalterado; repetível) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=MalhaFinaServiceTest` verde.
- Suíte completa CONTINUA verde (serviço novo, sem rota; não toca a StateMachine — malha fina é ortogonal).
</verification>

<success_criteria>
- Malha fina REAL: encaminha qualquer processo (incl. deferido — corrige bug legado) por flag+tabela ortogonal ao status, em lote, repetível e auditada.
- Serviço puro (endpoint em 10-15); paralelo file-disjunto com 10-10/10-11. ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-12-SUMMARY.md`: assinaturas de encaminhar/encaminharLote/resolver, a regra ortogonal (flag in_fine_mesh + tabela, sem tocar status), RN-001 (qualquer status) e a auditoria por processo. Insumo direto de 10-14 (consulta filtra in_fine_mesh + lote da consulta), 10-15 (endpoint encaminhar) e 10-16 (ação na consulta/detalhe).
</output>

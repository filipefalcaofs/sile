---
phase: 10-analise-tecnica-sedur
plan: 10
type: execute
wave: 5
depends_on: ["10-02", "10-03", "10-09"]
files_modified:
  - app/Services/Analise/AnaliseTecnicaDecisionService.php
  - app/Services/Analise/AnaliseDecisionResult.php
  - tests/Feature/Analise/AnaliseTecnicaDecisionTest.php
  - tests/Feature/Analise/AnaliseTecnicaAuditoriaTest.php
autonomous: true
requirements: [HU-085, HU-086, HU-087, HU-088, HU-089]

must_haves:
  truths:
    - "AnaliseTecnicaDecisionService::decide(record, analista) constrói a decisão a partir da FICHA FINALIZADA (não do resolver puro): RN-004 — todas as CNAEs deferidas → DEFERIDA; alguma indeferida → INDEFERIDA; grava na MESMA viability_decisions (Fase 9) com flow='analise_tecnica' e decided_by_user_id=analista (≠ null)"
    - "Pode DEFERIR sem zona oficial (o humano decide o caso pendente) com fundamentação própria da ficha — é o caso que a lei manda o humano decidir; NÃO recusa por veredito pendente (diferente do FluxoExpressoService)"
    - "Transação: cria ViabilityDecision (per_cnae/conditions/fundamentacao/rules_versions da ficha; TVL só no deferimento via TvlNumberGenerator) → transiciona em_analise→deferida|indeferida (StateMachine, = encerramento HU-089) → AUDITA SÍNCRONO (HU-078) → APÓS o commit dispara ResultadoEmitido (os 3 listeners da Fase 9 reusam; Regin/SEFAZ seguem BLOQUEADOS honestos → Fase 13)"
    - "Exige ficha FINALIZADA (rascunho não decide — CA-03 bloqueio); idempotência: 1 decisão por solicitação (unique viability_request_id da Fase 9 é a defesa final)"
    - "Serviço PURO (sem rota): o endpoint deferir/indeferir/encerrar é wired em 10-15 (Wave 7); aqui a lógica é testada no nível de serviço"
  artifacts:
    - path: "app/Services/Analise/AnaliseTecnicaDecisionService.php"
      provides: "Decisão humana → ViabilityDecision (flow analise_tecnica) + transição + auditoria síncrona + ResultadoEmitido"
      contains: "flow"
    - path: "app/Services/Analise/AnaliseDecisionResult.php"
      provides: "Retorno da decisão (status final + decisão + outcome) para o controller/comando"
      contains: "AnaliseDecisionResult"
  key_links:
    - from: "app/Services/Analise/AnaliseTecnicaDecisionService.php"
      to: "app/Models/ViabilityDecision.php"
      via: "create(flow='analise_tecnica', decided_by_user_id=analista) — REUSO da tabela da Fase 9 (fonte do TVL/explicabilidade)"
      pattern: "'analise_tecnica'"
    - from: "app/Services/Analise/AnaliseTecnicaDecisionService.php"
      to: "app/Events/ResultadoEmitido.php"
      via: "ResultadoEmitido::dispatch APÓS o commit (reusa os 3 listeners da Fase 9; Regin/SEFAZ bloqueados)"
      pattern: "ResultadoEmitido::dispatch"
---

<objective>
Decisão técnica humana (Wave 5, serviço puro): `AnaliseTecnicaDecisionService::decide()` que conclui o processo a partir da FICHA FINALIZADA — defere/indefere (RN-004), grava na MESMA `viability_decisions` da Fase 9 (flow `analise_tecnica`, decided_by analista), transiciona (= encerramento HU-089), audita SÍNCRONO e dispara `ResultadoEmitido` após o commit. Endpoint HTTP em 10-15.

Purpose: é o fechamento da decisão HUMANA — a contraparte do FluxoExpressoService para os casos que a lei manda o humano decidir. Reusar `ViabilityDecision` (mesma tabela/fonte do TVL/explicabilidade) é a decisão central do arquiteto: `ResultadoExpressoController`/`ViabilityDecisionResource` servem os dois flows; `DecisionOutcome`/`TvlNumberGenerator`/`ResultadoEmitido` reusados. Diferença crítica: o humano PODE deferir sem zona (o caso pendente é exatamente o que exige humano) — a decisão nasce da ficha, não do resolver puro. Regin/SEFAZ disparam (reuso dos listeners) mas seguem BLOQUEADOS honestos → Fase 13.
Output: `AnaliseTecnicaDecisionService` + `AnaliseDecisionResult`, cobertos por feature tests (defere/indefere; deferir sem zona; bloqueio de rascunho; ResultadoEmitido; auditoria síncrona).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md
@.planning/phases/10-analise-tecnica-sedur/10-02-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-03-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-09-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-05-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-02-SUMMARY.md

# PADRÕES A ESPELHAR (transação + ViabilityDecision + TVL + auditoria síncrona + ResultadoEmitido after-commit) — ler antes de codar:
@app/Services/Expresso/FluxoExpressoService.php
@app/Models/ViabilityDecision.php
@app/Enums/DecisionOutcome.php
@app/Services/Expresso/TvlNumberGenerator.php
@app/Events/ResultadoEmitido.php
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-086-Deferir-solicitacao.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AnaliseTecnicaDecisionService::decide (defere/indefere a partir da ficha) + ViabilityDecision + TVL + transição</name>
  <read_first>app/Services/Expresso/FluxoExpressoService.php, app/Models/ViabilityDecision.php, app/Services/Expresso/TvlNumberGenerator.php, app/Services/Analise/AnalysisRecordService.php</read_first>
  <files>app/Services/Analise/AnaliseTecnicaDecisionService.php, app/Services/Analise/AnaliseDecisionResult.php, tests/Feature/Analise/AnaliseTecnicaDecisionTest.php</files>
  <action>
    TDD estrito (RED primeiro). `AnaliseDecisionResult` (readonly): outcome (DecisionOutcome), decision (ViabilityDecision), emitted (bool).

    `AnaliseTecnicaDecisionService` (injeta ViabilityRequestStateMachine, TvlNumberGenerator, AuditService):
    - `decide(AnalysisRecord $record, User $analista): AnaliseDecisionResult` — exige record FINALIZADA (senão lança DomainException — CA-03 bloqueio; rascunho não decide) e request em em_analise.
    - outcome (RN-004): lê o status escolhido por CNAE do per_cnae da ficha — TODAS deferidas → DEFERIDA; QUALQUER indeferida → INDEFERIDA.
    - transação (espelhar FluxoExpressoService::emitir): cria `ViabilityDecision` (flow 'analise_tecnica', outcome, consolidated_result derivado da ficha, per_cnae/conditions/fundamentacao/rules_versions DA FICHA — pode deferir sem zona com fundamentação própria; tvl_product_number só no deferimento via TvlNumberGenerator; decided_by_user_id=$analista->id; decided_at now) → transiciona em_analise→deferida|indeferida (= encerramento HU-089) → AUDITA SÍNCRONO ('analise'/'decisao', result outcome, rulesVersion da ficha) — tudo antes do commit.
    - APÓS o commit: `ResultadoEmitido::dispatch($request, $decision)`.
    - robustez idempotente: capturar QueryException de unique(viability_request_id) → no-op (decisão já existe; emitted=false) — mesma defesa da Fase 9.

    `AnaliseTecnicaDecisionTest` (SQLite + fake SpatialRepository):
    - ficha finalizada com TODAS CNAEs deferidas → DEFERIDA: 1 ViabilityDecision (flow 'analise_tecnica', decided_by=analista, tvl_product_number não-nulo), status deferida; Event::fake assertDispatched(ResultadoEmitido).
    - uma CNAE indeferida → INDEFERIDA: decisão sem TVL, status indeferida.
    - **deferir SEM zona** (consolidado pendente no engine_snapshot, mas o analista escolheu deferida na ficha) → DEFERE com fundamentação da ficha (anti-fachada inversa: o humano DECIDE o caso pendente — diferente do expresso que recusa).
    - ficha em RASCUNHO → DomainException (CA-03 não decide).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=AnaliseTecnicaDecisionTest</verify>
  <acceptance_criteria>
    - grep "'analise_tecnica'" e "ResultadoEmitido::dispatch" em app/Services/Analise/AnaliseTecnicaDecisionService.php.
    - grep "Finalizada" (exige ficha finalizada) no service.
    - AnaliseTecnicaDecisionTest verde (defere/indefere; deferir sem zona; bloqueio de rascunho; evento).
  </acceptance_criteria>
  <done>Decisão humana real reusando ViabilityDecision (flow analise_tecnica), TVL no deferimento, transição/encerramento e ResultadoEmitido; pode deferir o caso pendente.</done>
</task>

<task type="auto">
  <name>Task 2: auditoria SÍNCRONA da decisão humana (HU-078) independente do evento</name>
  <read_first>app/Support/Audit/AuditService.php, app/Services/Analise/AnaliseTecnicaDecisionService.php</read_first>
  <files>tests/Feature/Analise/AnaliseTecnicaAuditoriaTest.php</files>
  <action>
    TDD estrito. `AnaliseTecnicaAuditoriaTest` (SQLite):
    - Event::fake([ResultadoEmitido::class]) (isola o evento) → após decide() deferido: existe Activity log_name 'analise' event 'decisao' result 'deferida' com rules_version preenchida e properties por_cnae + decided_by — PROVANDO que a auditoria é SÍNCRONA e NÃO depende do evento (HU-078, lição Fases 8/9).
    - a transição (solicitacoes/transicao) em_analise→deferida também auditada.
    - decided_by_user_id = analista (não null) na decisão (distingue do flow expresso, decided_by null = sistema).
  </action>
  <verify>php artisan test --compact --filter=AnaliseTecnicaAuditoriaTest</verify>
  <acceptance_criteria>
    - AnaliseTecnicaAuditoriaTest verde (auditoria síncrona 'analise'/'decisao' com rules_version, independente do evento; decided_by = analista).
  </acceptance_criteria>
  <done>Auditoria autoritativa SÍNCRONA da decisão humana (HU-078), independente do ResultadoEmitido; decided_by = analista.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-086 RN-004 — defere se todas CNAEs deferidas | AnaliseTecnicaDecisionTest (deferida + TVL) |
| HU-087 — indefere se alguma indeferida | AnaliseTecnicaDecisionTest (indeferida, sem TVL) |
| HU-085/088 — fundamentação/condicionantes da ficha na decisão | AnaliseTecnicaDecisionTest (per_cnae/conditions/fundamentacao persistidos) |
| Caso pendente — humano defere sem zona | AnaliseTecnicaDecisionTest (defere sem zona) |
| HU-086 CA-03 — bloqueio (ficha rascunho) | AnaliseTecnicaDecisionTest (DomainException) |
| HU-078 — auditoria síncrona | AnaliseTecnicaAuditoriaTest (independe do evento) |
| HU-089 — encerramento (transição final) | AnaliseTecnicaDecisionTest (status deferida/indeferida) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="AnaliseTecnicaDecisionTest|AnaliseTecnicaAuditoriaTest"` verde.
- Suíte completa CONTINUA verde (serviço novo; reusa ViabilityDecision/ResultadoEmitido sem reescrever a Fase 9).
</verification>

<success_criteria>
- Decisão humana real → ViabilityDecision (flow analise_tecnica, decided_by analista) a partir da ficha finalizada; defere/indefere (RN-004); pode deferir o caso pendente.
- Auditoria SÍNCRONA (HU-078) + ResultadoEmitido after-commit (reusa os 3 listeners; Regin/SEFAZ bloqueados → Fase 13).
- Serviço puro (endpoint em 10-15); ZERO dependência nova; nenhuma reescrita da Fase 9.
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-10-SUMMARY.md`: a assinatura de decide()/AnaliseDecisionResult, a regra RN-004 (todas deferidas→defere), o flow 'analise_tecnica' + decided_by analista, o ponto de TVL/transição/auditoria síncrona/ResultadoEmitido, e a diferença em relação ao expresso (defere o caso pendente). Insumo direto de 10-13 (TVL do deferimento humano), 10-15 (endpoint deferir/indeferir/encerrar) e 10-18 (golden/smoke da decisão humana).
</output>

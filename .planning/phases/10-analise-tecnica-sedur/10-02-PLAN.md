---
phase: 10-analise-tecnica-sedur
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/*_create_sectors_table.php
  - database/migrations/*_create_sector_user_table.php
  - database/migrations/*_add_analysis_columns_to_viability_requests_table.php
  - database/migrations/*_create_analysis_records_table.php
  - database/migrations/*_create_analysis_divergences_table.php
  - database/migrations/*_create_analysis_pendencies_table.php
  - database/migrations/*_create_fine_mesh_referrals_table.php
  - database/migrations/*_create_standard_texts_table.php
  - database/migrations/*_create_tvl_documents_table.php
  - app/Enums/AnalysisRecordStatus.php
  - app/Enums/AnalysisCategory.php
  - app/Enums/AnalysisStage.php
  - app/Enums/AnalysisPendencyStatus.php
  - app/Models/Sector.php
  - app/Models/AnalysisRecord.php
  - app/Models/AnalysisDivergence.php
  - app/Models/AnalysisPendency.php
  - app/Models/FineMeshReferral.php
  - app/Models/StandardText.php
  - app/Models/TvlDocument.php
  - app/Models/ViabilityRequest.php
  - app/Models/User.php
  - database/factories/SectorFactory.php
  - database/factories/AnalysisRecordFactory.php
  - database/factories/AnalysisDivergenceFactory.php
  - database/factories/AnalysisPendencyFactory.php
  - database/factories/FineMeshReferralFactory.php
  - database/factories/StandardTextFactory.php
  - database/factories/TvlDocumentFactory.php
  - tests/Feature/Analise/AnaliseSchemaTest.php
  - tests/Feature/Analise/AnaliseModelsTest.php
autonomous: true
requirements: [HU-138, HU-135, HU-140, HU-083, HU-136, HU-085, HU-132, HU-144, HU-082]

must_haves:
  truths:
    - "Existem as tabelas: sectors (name/active) + pivot sector_user (analista N:N); analysis_records (ficha versionada: viability_request_id, revision, status, analyst_user_id, engine_snapshot/engine_rules_versions jsonb, engine_available bool, per_cnae/conditions/parking jsonb, parecer text, finalized_at); analysis_divergences (record_id, cnae, field, suggested_value, final_value, justification); analysis_pendencies (requested_by, description, status, due_at, responded_at, response); fine_mesh_referrals (referred_by, reason, created_at, resolved_at); standard_texts (category, content, active, version); tvl_documents (viability_decision_id, disk, path, verification_code, generated_by, generated_at) — todas jsonb portável SQLite/pgsql, sem geometry própria"
    - "viability_requests GANHA colunas FORA do fillable (como protocoled_at/bap_*): sector_id, assigned_user_id, assigned_at, analysis_category, in_fine_mesh (bool, índice), analysis_stage, analysis_stage_started_at, analysis_due_at (índice — base do SLA) — sem tocar colunas existentes; índices em sector_id/assigned_user_id/analysis_due_at/analysis_category/in_fine_mesh"
    - "Enums dedicados: AnalysisRecordStatus (rascunho/finalizada), AnalysisCategory (expresso/semi_expresso), AnalysisStage (distribuicao/analise), AnalysisPendencyStatus (aberta/respondida/expirada) — cada um com label() pt-BR"
    - "Models com casts() (jsonb→array, datetimes, enums, booleans) e relations: ViabilityRequest GANHA sector()/assignedTo()/analysisRecords()/currentAnalysisRecord()/pendencies()/fineMeshReferrals(); AnalysisRecord pertence à request+analyst e tem divergences(); revisão é append-only (finalizada não recebe update de negócio)"
    - "Factories úteis com states: AnalysisRecordFactory rascunho()/finalizada()/semMotor(engine_available=false); SectorFactory ativo()/inativo(); AnalysisPendencyFactory aberta()/respondida()/expirada(); StandardTextFactory por categoria — todas associando a uma ViabilityRequest::factory()->protocoled() quando aplicável"
  artifacts:
    - path: "database/migrations"
      provides: "8 tabelas novas + colunas de análise em viability_requests (portáveis jsonb, índices)"
      contains: "analysis_records"
    - path: "app/Models/AnalysisRecord.php"
      provides: "Ficha de análise versionada (revisão imutável após finalizar) com casts jsonb e divergências"
      contains: "class AnalysisRecord"
    - path: "app/Models/Sector.php"
      provides: "Setor (caixa de análise) com analistas N:N e flag active"
      contains: "belongsToMany"
  key_links:
    - from: "app/Models/ViabilityRequest.php"
      to: "app/Models/AnalysisRecord.php"
      via: "hasMany analysisRecords + helper currentAnalysisRecord (maior revisão) — a ficha pertence ao processo"
      pattern: "analysisRecords"
    - from: "app/Models/Sector.php"
      to: "app/Models/User.php"
      via: "belongsToMany analysts via sector_user (vínculo N:N administrável — HU-138 RN-005)"
      pattern: "sector_user"
    - from: "app/Models/TvlDocument.php"
      to: "app/Models/ViabilityDecision.php"
      via: "belongsTo viabilityDecision — cada emissão/reimpressão é uma linha auditada (HU-132)"
      pattern: "viability_decision_id"
---

<objective>
Fundação de dados e domínio da Fase 10 (Track B da Wave 1): as 8 tabelas novas (setores+pivot, ficha versionada, divergências, pendências, malha fina, textos-padrão, documentos TVL), as colunas de análise em `viability_requests` (atribuição/SLA/categoria/malha fina), os 4 enums e os models+factories. Tudo portável (jsonb, sem geometry própria — os precedentes usam a geometry já existente da Fase 8), rodando em SQLite.

Purpose: a ficha é o coração da análise humana (HU-135/140) e a fonte do parecer; a decisão final (10-10) grava na `viability_decisions` da Fase 9 (reuso), mas a ficha, as divergências, as pendências, a malha fina e o TVL precisam de schema próprio. A modelagem segue o legado SAPS (per_cnae espelha a ficha) e as regras (revisão imutável RN-003, malha fina ORTOGONAL ao status via flag+tabela). As colunas de análise ficam FORA do fillable (como protocoled_at/bap_*): escritas por serviços dedicados (distribuição, SLA), nunca pelo cidadão.
Output: 8 tabelas + colunas, 4 enums, 7 models novos + ViabilityRequest/User estendidos, 7 factories — cobertos por feature tests (schema + models/relations, SQLite).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md
@.planning/phases/09-fluxo-expresso/09-02-SUMMARY.md

# PADRÕES A ESPELHAR (migrations driver-portáveis jsonb; model imutável; enum label; factory states) — ler antes de codar:
@app/Models/ViabilityRequest.php
@app/Models/ViabilityDecision.php
@app/Enums/ViabilityRequestStatus.php
@app/Enums/DecisionOutcome.php
@database/factories/ViabilityRequestFactory.php
@database/factories/ViabilityDecisionFactory.php
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-135-Preencher-ficha-de-análise.md
@docs/SILE_HUs_Completas_MD/EP02-Administracao/HU-138-Manter-setores.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: migrations das 8 tabelas + colunas de análise em viability_requests + AnaliseSchemaTest</name>
  <read_first>app/Models/ViabilityRequest.php, database/migrations (a migration create_viability_requests_table para os tipos jsonb portáveis e o padrão de add columns das colunas BAP)</read_first>
  <files>database/migrations/*_create_sectors_table.php, database/migrations/*_create_sector_user_table.php, database/migrations/*_add_analysis_columns_to_viability_requests_table.php, database/migrations/*_create_analysis_records_table.php, database/migrations/*_create_analysis_divergences_table.php, database/migrations/*_create_analysis_pendencies_table.php, database/migrations/*_create_fine_mesh_referrals_table.php, database/migrations/*_create_standard_texts_table.php, database/migrations/*_create_tvl_documents_table.php, tests/Feature/Analise/AnaliseSchemaTest.php</files>
  <action>
    TDD: escrever `AnaliseSchemaTest` (Schema::hasTable/hasColumns) ANTES, confirmar RED, depois `php artisan make:migration --no-interaction` (uma por tabela; usar `json` portável como nas tabelas da Fase 8 — NÃO há geometry própria aqui).

    1. `sectors`: id; name (string, unique); active (boolean default true); timestamps.
    2. `sector_user` (pivot N:N): id; sector_id (FK sectors cascadeOnDelete); user_id (FK users cascadeOnDelete); unique(sector_id,user_id); timestamps.
    3. `add_analysis_columns_to_viability_requests` (ADITIVA — não toca colunas existentes): `sector_id` (FK sectors nullOnDelete, nullable, índice), `assigned_user_id` (FK users nullOnDelete, nullable, índice), `assigned_at` (timestamp nullable), `analysis_category` (string nullable, índice), `in_fine_mesh` (boolean default false, índice), `analysis_stage` (string nullable), `analysis_stage_started_at` (timestamp nullable), `analysis_due_at` (timestamp nullable, índice — base do SLA/fila).
    4. `analysis_records`: id; viability_request_id (FK cascadeOnDelete, índice); revision (unsignedInteger); status (string default 'rascunho'); analyst_user_id (FK users nullOnDelete, nullable); engine_snapshot (json nullable); engine_rules_versions (json nullable); engine_available (boolean default true); per_cnae (json nullable); conditions (json nullable); parking (json nullable); parecer (text nullable); finalized_at (timestamp nullable); timestamps. unique(viability_request_id, revision).
    5. `analysis_divergences`: id; analysis_record_id (FK cascadeOnDelete, índice); cnae (string); field (string); suggested_value (text nullable); final_value (text nullable); justification (text nullable); timestamps.
    6. `analysis_pendencies`: id; viability_request_id (FK cascadeOnDelete, índice); requested_by_user_id (FK users nullOnDelete, nullable); description (text); status (string default 'aberta', índice); due_at (timestamp nullable); responded_at (timestamp nullable); response (text nullable); timestamps.
    7. `fine_mesh_referrals`: id; viability_request_id (FK cascadeOnDelete, índice); referred_by_user_id (FK users nullOnDelete, nullable); reason (text); resolved_at (timestamp nullable); timestamps (created_at = encaminhamento).
    8. `standard_texts`: id; category (string, índice); content (text); active (boolean default true); version (unsignedInteger default 1); timestamps.
    9. `tvl_documents`: id; viability_decision_id (FK viability_decisions cascadeOnDelete, índice); disk (string); path (string); verification_code (string, unique); generated_by_user_id (FK users nullOnDelete, nullable); generated_at (timestamp); timestamps.

    `AnaliseSchemaTest`: Schema::hasTable das 8 + hasColumns dos campos críticos de cada uma; hasColumns das 8 colunas novas em viability_requests; asserir o unique(viability_request_id, revision) de analysis_records (via tentativa de duplicar OU prova no AnaliseModelsTest da Task 3). `php artisan migrate:fresh` roda limpo em SQLite.
  </action>
  <verify>php artisan test --compact --filter=AnaliseSchemaTest</verify>
  <acceptance_criteria>
    - php artisan migrate:fresh roda limpo em SQLite (json portável, sem geometry).
    - grep "analysis_due_at" na migration de colunas (índice) e "viability_request_id" + "revision" (unique) na de analysis_records.
    - AnaliseSchemaTest verde (8 tabelas + 8 colunas).
  </acceptance_criteria>
  <done>8 tabelas + 8 colunas de análise (portáveis jsonb, índices), unicidade da revisão; schema test verde em SQLite.</done>
</task>

<task type="auto">
  <name>Task 2: enums (4) + models (7 novos) + ViabilityRequest/User estendidos</name>
  <read_first>app/Enums/ViabilityRequestStatus.php, app/Enums/DecisionOutcome.php, app/Models/ViabilityRequest.php, app/Models/ViabilityDecision.php, app/Models/User.php</read_first>
  <files>app/Enums/AnalysisRecordStatus.php, app/Enums/AnalysisCategory.php, app/Enums/AnalysisStage.php, app/Enums/AnalysisPendencyStatus.php, app/Models/Sector.php, app/Models/AnalysisRecord.php, app/Models/AnalysisDivergence.php, app/Models/AnalysisPendency.php, app/Models/FineMeshReferral.php, app/Models/StandardText.php, app/Models/TvlDocument.php, app/Models/ViabilityRequest.php, app/Models/User.php</files>
  <action>
    TDD onde houver lógica (helpers/relations cobertos na Task 3).

    Enums (string + label() pt-BR; espelhar ViabilityRequestStatus/DecisionOutcome):
    - `AnalysisRecordStatus`: Rascunho='rascunho', Finalizada='finalizada'. Helper `isFinalizada(): bool`.
    - `AnalysisCategory`: Expresso='expresso', SemiExpresso='semi_expresso'. (A categoria de consulta combina este enum + flag in_fine_mesh + is_virtual_office — derivada na HU-082, não persistida aqui.)
    - `AnalysisStage`: Distribuicao='distribuicao', Analise='analise'. (Etapa atual do SLA — extensível; alinha com os parâmetros analise.sla.<etapa>_dias.)
    - `AnalysisPendencyStatus`: Aberta='aberta', Respondida='respondida', Expirada='expirada'.

    Models (casts() no método; usar #[Fillable] como os models irmãos; PHPDoc de relations):
    - `Sector`: fillable name/active; cast active→bool; `analysts(): BelongsToMany(User, 'sector_user')` withTimestamps; `requests(): HasMany(ViabilityRequest)`.
    - `AnalysisRecord` (revisão IMUTÁVEL após finalizar): fillable dos campos de rascunho (viability_request_id, revision, status, analyst_user_id, engine_snapshot, engine_rules_versions, engine_available, per_cnae, conditions, parking, parecer, finalized_at); casts status→AnalysisRecordStatus, engine_snapshot/engine_rules_versions/per_cnae/conditions/parking→array, engine_available→bool, finalized_at→datetime; relations viabilityRequest() belongsTo, analyst() belongsTo(User) nullable, divergences() hasMany; helper isFinalizada().
    - `AnalysisDivergence`: fillable analysis_record_id/cnae/field/suggested_value/final_value/justification; belongsTo analysisRecord.
    - `AnalysisPendency`: fillable viability_request_id/requested_by_user_id/description/status/due_at/responded_at/response; casts status→AnalysisPendencyStatus, due_at/responded_at→datetime; relations viabilityRequest(), requestedBy() belongsTo(User).
    - `FineMeshReferral`: fillable viability_request_id/referred_by_user_id/reason/resolved_at; cast resolved_at→datetime; relations viabilityRequest(), referredBy() belongsTo(User); helper isResolved().
    - `StandardText`: fillable category/content/active/version; cast active→bool, version→int.
    - `TvlDocument`: fillable viability_decision_id/disk/path/verification_code/generated_by_user_id/generated_at; cast generated_at→datetime; relations viabilityDecision() belongsTo, generatedBy() belongsTo(User).
    - `ViabilityRequest`: ADICIONAR casts (assigned_at/analysis_stage_started_at/analysis_due_at→datetime; analysis_category→AnalysisCategory; analysis_stage→AnalysisStage; in_fine_mesh→bool) e relations: sector() belongsTo, assignedTo() belongsTo(User,'assigned_user_id'), analysisRecords() hasMany, currentAnalysisRecord() hasOne(latestOfMany por revision), pendencies() hasMany(AnalysisPendency), fineMeshReferrals() hasMany. NÃO incluir as colunas de análise no #[Fillable] (escritas via serviços por forceFill/preenchimento controlado — como protocol_number/status).
    - `User`: ADICIONAR relation sectors() belongsToMany(Sector,'sector_user') withTimestamps (vínculo do analista; HU-138 RN-005).
  </action>
  <verify>vendor/bin/pint --dirty --format agent</verify>
  <acceptance_criteria>
    - grep "enum AnalysisRecordStatus" + "Finalizada" (com label) nos enums.
    - grep "currentAnalysisRecord" e "in_fine_mesh" (cast) em app/Models/ViabilityRequest.php; "sector_user" em app/Models/User.php e app/Models/Sector.php.
    - pint limpo; classes carregam (`php artisan about` ou autoload sem erro).
  </acceptance_criteria>
  <done>4 enums + 7 models novos + ViabilityRequest/User estendidos (casts/relations), colunas de análise fora do fillable.</done>
</task>

<task type="auto">
  <name>Task 3: factories (7) + AnaliseModelsTest (relations, casts, revisão imutável, 1:revisão)</name>
  <read_first>database/factories/ViabilityRequestFactory.php, database/factories/ViabilityDecisionFactory.php</read_first>
  <files>database/factories/SectorFactory.php, database/factories/AnalysisRecordFactory.php, database/factories/AnalysisDivergenceFactory.php, database/factories/AnalysisPendencyFactory.php, database/factories/FineMeshReferralFactory.php, database/factories/StandardTextFactory.php, database/factories/TvlDocumentFactory.php, tests/Feature/Analise/AnaliseModelsTest.php</files>
  <action>
    TDD estrito (RED→GREEN). Factories com states (espelhar ViabilityDecisionFactory):
    - `SectorFactory`: definition name fake + active true; states inativo().
    - `AnalysisRecordFactory`: definition rascunho (status Rascunho, revision 1, engine_available true, per_cnae/conditions/parking arrays plausíveis, parecer null, finalized_at null), associando a ViabilityRequest::factory()->protocoled() (ou estado em_analise quando o factory existir — usar protocoled() + forceFill do status em testes); states finalizada() (status Finalizada, parecer preenchido, finalized_at now), semMotor() (engine_available false, engine_snapshot null — FA-01).
    - `AnalysisDivergenceFactory`: cnae/field/suggested_value/final_value/justification plausíveis, associando a AnalysisRecord::factory().
    - `AnalysisPendencyFactory`: aberta por default (status Aberta, due_at +15d), states respondida()/expirada().
    - `FineMeshReferralFactory`: reason obrigatório, resolved_at null; state resolvido().
    - `StandardTextFactory`: category/content/active/version; state inativo().
    - `TvlDocumentFactory`: associando a ViabilityDecision::factory() (deferida), disk 'local', path/verification_code fake, generated_at now.

    `AnaliseModelsTest` (SQLite, RefreshDatabase):
    - Sector::factory()->hasAttached(User::factory(),[], 'analysts') → relation analysts retorna o usuário; ViabilityRequest com sector_id resolve sector()/assignedTo().
    - AnalysisRecord rascunho cria com casts array; divergences() relaciona; **unicidade da revisão**: duas records com mesma (viability_request_id, revision) lançam QueryException.
    - currentAnalysisRecord retorna a MAIOR revisão (criar rev 1 e rev 2 → current == rev 2).
    - AnalysisRecord state finalizada() tem isFinalizada()==true e finalized_at preenchido.
    - AnalysisPendency states (aberta/respondida/expirada) com cast de status; TvlDocument pertence à decisão (deferida) com verification_code unique (duplicar lança QueryException).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=AnaliseModelsTest</verify>
  <acceptance_criteria>
    - grep "finalizada" e "semMotor" em database/factories/AnalysisRecordFactory.php.
    - AnaliseModelsTest verde (relations, casts, unicidade revisão, currentAnalysisRecord, verification_code unique).
  </acceptance_criteria>
  <done>7 factories com states úteis + AnaliseModelsTest verde (relations/casts/unicidades/revisão).</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-138 — setor + analistas N:N | AnaliseModelsTest (Sector::analysts, sector_user) |
| HU-135 RN-003 — revisão versionada imutável | AnaliseModelsTest (unicidade revisão + currentAnalysisRecord + isFinalizada) |
| HU-140 FA-01 — degradação (motor indisponível) | AnalysisRecordFactory::semMotor (engine_available=false) |
| HU-132 — TVL document auditado | AnaliseModelsTest (TvlDocument 1:N decisão, verification_code unique) |
| HU-144 — base do SLA (analysis_due_at indexado) | AnaliseSchemaTest (coluna + índice) |
| HU-083 — pendência com status | AnaliseModelsTest (AnalysisPendency states/cast) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- Suíte SQLite migra limpo (json portável; sem geometry). NUNCA `migrate:fresh --env=testing`.
- `php artisan test --compact --filter="AnaliseSchemaTest|AnaliseModelsTest"` verde.
- Suíte completa CONTINUA verde — a Fase 8/9 não regrediu (colunas só ADICIONADAS em viability_requests; nenhuma coluna alterada).
</verification>

<success_criteria>
- 8 tabelas + 8 colunas de análise (portáveis jsonb, índices); revisão da ficha imutável (unique) e malha fina ORTOGONAL (flag + tabela).
- 4 enums + 7 models + ViabilityRequest/User estendidos + 7 factories; colunas de análise fora do fillable.
- ZERO dependência nova; sem geometry própria (precedentes reusam a geometry da Fase 8).
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-02-SUMMARY.md`: nomes EXATOS de tabelas/colunas/índices, dos 4 enums (casos + label), dos 7 models (casts/relations, incl. ViabilityRequest::currentAnalysisRecord/sector/assignedTo e User::sectors), das factories e states. Insumo direto de 10-04 (Sector CRUD), 10-05 (analysis_due_at/stage), 10-06 (engine_snapshot da zona), 10-07 (sector_id/assigned_user_id), 10-08 (cria AnalysisRecord rev 1), 10-09 (ficha/divergências), 10-10 (decisão lê a ficha), 10-11 (pendências), 10-12 (malha fina), 10-13 (tvl_documents), 10-14 (consulta filtra as colunas).
</output>

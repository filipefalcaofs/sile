---
phase: 10-analise-tecnica-sedur
plan: 16
type: execute
wave: 7
depends_on: ["10-14"]
files_modified:
  - resources/js/pages/gestao/processos/fila.tsx
  - resources/js/pages/gestao/processos/index.tsx
  - resources/js/pages/gestao/processos/show.tsx
  - tests/Feature/Analise/ProcessoUiSmokeTest.php
autonomous: true
requirements: [HU-082, HU-144, HU-136]

must_haves:
  truths:
    - "HU-144: página de fila (meus processos / caixa do setor) — DataTable ordenada por prazo com badge de semáforo (verde/amarelo/vermelho) + KPI cards de contadores (aguardando/em análise/em pendência/vencendo hoje), abas, mobile-first/dark; clique abre o processo"
    - "HU-082: página de consulta (filtros completos do SAPS + analista + categoria, paginação server-side, botão exportar CSV) e página de detalhe com abas (tramitação, dados, polígono, anexos, timeline visual com tempo por etapa, DAM/vistoria quando houver) + mini-mapa Leaflet permanente + os 3 identificadores (processo/BAP/TVL)"
    - "HU-136: ação de malha fina em lote a partir da seleção na consulta (POST processos/malha-fina — rota de 10-15); a busca global Cmd+K (HU-082 RN-009) é o componente montado no layout por 10-17 (sole layout editor) e consome o mesmo endpoint leve (10-14)"
    - "Usa o design system do console (TailAdmin: AppShell/PageBreadcrumb, DataTable, Badge, KPI cards, Pagination, EmptyState) + o mapa Leaflet reusável (Fase 4); ZERO dependência nova; tsc/build verdes"
    - "Páginas file-disjuntas de 10-15 (controllers/rotas) e de 10-17 (ficha/admin/nav/command-search) — consomem nomes de rota (route()) das Waves 6/7; verificação por tsc/build + um smoke Inertia leve"
  artifacts:
    - path: "resources/js/pages/gestao/processos/fila.tsx"
      provides: "Fila do analista (meus/setor) com semáforo de SLA + contadores (HU-144)"
      contains: "semaforo"
    - path: "resources/js/pages/gestao/processos/show.tsx"
      provides: "Detalhe do processo: abas + timeline + mini-mapa Leaflet + 3 identificadores (HU-082)"
      contains: "timeline"
  key_links:
    - from: "resources/js/pages/gestao/processos/index.tsx"
      to: "app/Http/Controllers/Gestao/ProcessoController.php"
      via: "GET gestao.processos.index (server-driven; filtros/paginação/CSV) renderizado pela página"
      pattern: "processos.index"
    - from: "resources/js/pages/gestao/processos/index.tsx"
      to: "app/Http/Controllers/Gestao/MalhaFinaController.php"
      via: "seleção múltipla → POST gestao.processos.malha-fina (lote, com motivo — rota de 10-15)"
      pattern: "malha-fina"
---

<objective>
UI do console — fila, consulta e detalhe (Wave 7, páginas): as telas React/Inertia da fila com SLA (HU-144), da consulta de processos (HU-082) e do detalhe (timeline + mini-mapa). Consome os endpoints de 10-14/10-15; o componente global de busca Cmd+K é montado no layout por 10-17 (sole layout editor), consumindo o mesmo endpoint.

Purpose: entregar a operação navegável do analista — a fila prioriza o trabalho por prazo (corrige a distorção do legado) e a consulta supera o SAPS ("filtros insuficientes"/"lento") com filtros completos e lote de malha fina. Reusa o design system do console (TailAdmin, DataTable/KPI/Badge) e o mapa Leaflet da Fase 4. Páginas file-disjuntas das rotas (10-15) e da ficha/admin/layout (10-17) — co-agendadas na Wave 7, verificadas por tsc/build + smoke Inertia leve.
Output: 3 páginas de UI (fila, consulta, detalhe) + 1 smoke Inertia, tsc/build verdes.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md
@.planning/phases/10-analise-tecnica-sedur/10-14-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-15-SUMMARY.md

# PADRÕES A ESPELHAR (páginas do console TailAdmin: DataTable/KPI/Badge/Pagination/EmptyState; mapa Leaflet reusável) — ler antes de codar (ativar a skill inertia-react-development):
@resources/js/pages/gestao/resultados-expresso/index.tsx
@resources/js/pages/gestao/resultados-expresso/show.tsx
@resources/js/components/app/app-sidebar.tsx
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-082-Consultar-processo.md
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-144-Fila-de-trabalho-do-analista-com-SLA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: página da fila (HU-144) + consulta (HU-082) com filtros/paginação/CSV/lote malha fina</name>
  <read_first>resources/js/pages/gestao/resultados-expresso/index.tsx, resources/js/components/app/app-sidebar.tsx</read_first>
  <files>resources/js/pages/gestao/processos/fila.tsx, resources/js/pages/gestao/processos/index.tsx</files>
  <action>
    Ativar a skill `inertia-react-development`. Seguir o padrão TailAdmin do console (componentes ui/Table, ui/Badge, KPI cards, ui/pagination, ui/empty-state). ZERO dependência nova.

    `fila.tsx` (HU-144): KPI cards de contadores (aguardando análise/em análise/em pendência/vencendo hoje); abas "Meus processos" / "Caixa do setor"; DataTable ordenada por prazo com colunas (protocolo, empresa, etapa, tempo na etapa, prazo) e Badge de semáforo (verde/amarelo/vermelho conforme props de 10-14); clique → processos.show. EmptyState. Mobile-first + dark.
    `index.tsx` (HU-082): barra de filtros (grupo de status/status, número de processo/produto/BAP, serviço, setor, zona, datas, inscrição, nome, CNPJ, CEP, logradouro, bairro, ANALISTA, CATEGORIA) que faz GET preservando estado; DataTable paginada server-side ("Mostrando X–Y de Z"); seleção múltipla → ação "Encaminhar à malha fina em lote" (POST gestao.processos.malha-fina, com motivo — rota de 10-15); botão "Exportar CSV" (GET ?formato=csv); EmptyState distinguindo busca vazia de lista vazia; linha → processos.show.

    Não criar teste de JS aqui; garantir tsc/build (Task 3).
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - As páginas usam os componentes do console (Table/Badge/KPI/pagination/empty-state) — ZERO dependência nova.
    - grep "semaforo" (ou cor do SLA) em fila.tsx e "malha-fina" (ação em lote) em index.tsx.
    - `npx tsc --noEmit` verde.
  </acceptance_criteria>
  <done>Fila com SLA (semáforo + contadores + abas) e consulta filtrável (paginação + CSV + lote de malha fina) no padrão do console.</done>
</task>

<task type="auto">
  <name>Task 2: detalhe do processo (abas + timeline + mini-mapa)</name>
  <read_first>resources/js/pages/gestao/resultados-expresso/show.tsx, resources/js/components/app/app-sidebar.tsx</read_first>
  <files>resources/js/pages/gestao/processos/show.tsx</files>
  <action>
    Ativar a skill `inertia-react-development`.
    `show.tsx` (HU-082 RN-006/007/010): cabeçalho com os 3 identificadores (número do processo, BAP, produto TVL); abas — Tramitação (transições), Informações do processo, Polígono, Anexos, Timeline VISUAL (etapas com duração/responsável/tipo — props de transitions), DAM/vistoria quando houver; mini-mapa Leaflet permanente (reusar o componente de mapa da Fase 4) com o polígono + zona/via sobrepostas; links para a ficha (processos/{id}/ficha — 10-17) e ações conforme permissão. Somente leitura aqui (ações ficam na ficha/consulta).

    Nota de coordenação (Wave 7): a busca global Cmd+K (HU-082 RN-009) é um componente de layout — fica em 10-17 (ÚNICO editor de gestao-layout.tsx), que cria e monta o command-search consumindo gestao.processos.busca. Esta página NÃO edita o layout.

    Garantir tsc/build (Task 3).
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - grep "timeline" e (mapa/Leaflet) em show.tsx.
    - `npx tsc --noEmit` verde; esta página NÃO edita gestao-layout.tsx (sem colisão com 10-17).
  </acceptance_criteria>
  <done>Detalhe do processo (abas + timeline visual + mini-mapa), somente leitura; Cmd+K fica em 10-17.</done>
</task>

<task type="auto">
  <name>Task 3: smoke Inertia leve das telas + build</name>
  <read_first>tests/Feature/Analise/ProcessoConsultaTest.php, tests/Feature/Analise/ProcessoFilaTest.php</read_first>
  <files>tests/Feature/Analise/ProcessoUiSmokeTest.php</files>
  <action>
    TDD leve. `ProcessoUiSmokeTest` (SQLite): com permissão, GET gestao.processos.index / .fila / .show renderizam o componente Inertia correto com as props essenciais (assertInertia component + chaves) — garante que as páginas casam com o backend (10-14). (As asserções de comportamento já estão em 10-14; aqui é o smoke do contrato página↔props.)
    Rodar `npm run build` ao final para garantir o bundle.
  </action>
  <verify>php artisan test --compact --filter=ProcessoUiSmokeTest && npx tsc --noEmit && npm run build</verify>
  <acceptance_criteria>
    - ProcessoUiSmokeTest verde (componentes/props de index/fila/show).
    - `npx tsc --noEmit` e `npm run build` verdes.
  </acceptance_criteria>
  <done>Smoke Inertia confirma o contrato página↔props; build/typecheck verdes.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste / evidência |
|---|---|
| HU-144 — fila por prazo + semáforo + contadores | ProcessoUiSmokeTest (fila props) + smoke navegável (10-18) |
| HU-082 RN-006/010 — detalhe abas/timeline/mini-mapa | ProcessoUiSmokeTest (show props) + tsc/build |
| HU-082 RN-011 — exportar CSV do filtrado | index.tsx (botão CSV → ?formato=csv) |
| HU-136 — malha fina em lote da consulta | index.tsx (seleção + POST malha-fina) |

<verification>
- `php artisan test --compact --filter=ProcessoUiSmokeTest` verde.
- `npx tsc --noEmit` e `npm run build` verdes.
- Páginas file-disjuntas de 10-15 e 10-17; ZERO dependência nova.
</verification>

<success_criteria>
- Fila com SLA (HU-144), consulta filtrável (HU-082) e detalhe (timeline + mini-mapa + 3 identificadores) navegáveis no padrão do console; lote de malha fina e CSV na consulta (busca global Cmd+K entregue por 10-17).
- Reusa o design system e o mapa Leaflet; tsc/build verdes; ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-16-SUMMARY.md`: as páginas criadas (fila/index/show), os componentes reusados, o que cada uma consome (incl. o lote de malha fina) e a nota de que o Cmd+K fica em 10-17. Insumo do smoke navegável (10-18).
</output>

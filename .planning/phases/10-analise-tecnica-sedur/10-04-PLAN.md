---
phase: 10-analise-tecnica-sedur
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - app/Http/Controllers/Gestao/SectorController.php
  - app/Http/Controllers/Gestao/StandardTextController.php
  - app/Http/Requests/Gestao/SectorRequest.php
  - app/Http/Requests/Gestao/StandardTextRequest.php
  - app/Http/Resources/SectorResource.php
  - app/Http/Resources/StandardTextResource.php
  - routes/gestao.php
  - tests/Feature/Analise/SectorCrudTest.php
  - tests/Feature/Analise/StandardTextCrudTest.php
autonomous: true
requirements: [HU-138, HU-085]

must_haves:
  truths:
    - "HU-138: o admin mantém setores (criar/editar nome+situação) e o vínculo analista↔setor N:N (anexar/desanexar) por interface, gated por manter-setores; um analista pode pertencer a mais de um setor (RN-005)"
    - "HU-138 RN-004: setor com processos em aberto NÃO é excluído — só inativado (toggleActivation); não há rota destroy destrutiva; inativar preserva o histórico e o vínculo"
    - "HU-085: biblioteca de textos-padrão administrável (CRUD por categoria, ativa/inativa, versão) — gated por manter-parametros (decisão registrada: reuso, sem 6ª permissão; a leitura da lista ativa pelo parecer fica sob analisar-processos em 10-09); editar o conteúdo INCREMENTA a versão (RN-005, rastreabilidade)"
    - "Toda ação audita (RN-002) via AuditService/HasAuditoria; acesso sem permissão → 403 auditado (CA-04)"
    - "Backend somente (controllers+routes+requests+resources) — as telas de console ficam em 10-17 (Wave 7); este é o ÚNICO plano que edita routes/gestao.php na Wave 2"
  artifacts:
    - path: "app/Http/Controllers/Gestao/SectorController.php"
      provides: "CRUD de setores + vínculo de analistas (sem destroy destrutivo)"
      contains: "manter-setores"
    - path: "app/Http/Controllers/Gestao/StandardTextController.php"
      provides: "CRUD versionado da biblioteca de textos-padrão (HU-085)"
      contains: "StandardText"
  key_links:
    - from: "routes/gestao.php"
      to: "app/Http/Controllers/Gestao/SectorController.php"
      via: "Route::middleware('permission:manter-setores')->prefix('setores')"
      pattern: "manter-setores"
    - from: "app/Http/Controllers/Gestao/SectorController.php"
      to: "app/Models/Sector.php"
      via: "syncAnalysts via sector_user (HU-138 RN-005) + toggleActivation (RN-004)"
      pattern: "analysts"
---

<objective>
Cadastros de apoio da Fase 10 (Wave 2, backend): o CRUD de **setores** (HU-138 — caixa de análise + vínculo de analistas N:N) e a **biblioteca de textos-padrão** (HU-085 — fundamentações versionadas para o parecer). Backend completo (controller/request/resource/rotas) gated por permissão; as telas de console vêm em 10-17.

Purpose: HU-138 é PRÉ-REQUISITO da distribuição (10-07) e da fila (HU-144) — o processo cai na caixa do setor e é atribuído sem sair dela (cobre férias/ausências). A biblioteca de textos-padrão dá consistência de redação ao parecer (HU-085 RN-004/005). Os dois são CRUDs administráveis simples; reúno-os num plano para que a Wave 2 tenha um ÚNICO editor de `routes/gestao.php` (evita conflito com os planos paralelos 10-05/10-06, que são serviços sem rota).
Output: SectorController (CRUD + analistas + inativação protegida) + StandardTextController (CRUD versionado) + requests/resources + rotas, cobertos por feature tests (403, CRUD, vínculo, inativação, versionamento).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md
@.planning/phases/10-analise-tecnica-sedur/10-01-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-02-SUMMARY.md

# PADRÕES A ESPELHAR (CRUD console + permissão + request + resource + rota) — ler antes de codar:
@app/Http/Controllers/Gestao/ViabilityServiceTypeController.php
@routes/gestao.php
@app/Models/Sector.php
@app/Models/StandardText.php
@docs/SILE_HUs_Completas_MD/EP02-Administracao/HU-138-Manter-setores.md
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-085-Emitir-parecer.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HU-138 SectorController (CRUD + analistas N:N + inativação protegida) + rotas</name>
  <read_first>app/Http/Controllers/Gestao/ViabilityServiceTypeController.php, app/Models/Sector.php, routes/gestao.php</read_first>
  <files>app/Http/Controllers/Gestao/SectorController.php, app/Http/Requests/Gestao/SectorRequest.php, app/Http/Resources/SectorResource.php, routes/gestao.php, tests/Feature/Analise/SectorCrudTest.php</files>
  <action>
    TDD estrito (RED primeiro). Espelhar o ViabilityServiceTypeController (server-driven, Form Request, Resource).

    Rotas em `routes/gestao.php`, sob `permission:manter-setores`, prefixo `setores`:
    - GET `setores` → index; POST `setores` → store; PUT `setores/{sector}` → update; PUT `setores/{sector}/ativacao` → toggleActivation; PUT `setores/{sector}/analistas` → syncAnalysts.
    - SEM destroy (HU-138 RN-004: setor não é excluído — só inativado; preserva histórico).

    `SectorController`:
    - index: lista setores (com count de processos e analistas), Inertia::render('gestao/setores/index', ...) — a página vem em 10-17; aqui o controller já entrega props.
    - store/update: nome único, active; `SectorRequest` valida (name required unique ignorando o próprio no update; active boolean).
    - toggleActivation: inverte active; ao INATIVAR um setor com processos em aberto (requests com status em_analise/em_pendencia e sector_id deste), permitir a inativação mas registrar/retornar aviso (RN-004 — não exclui; a redistribuição é operação separada de distribuição em 10-07). NÃO bloquear inativação; bloquear seria exclusão — aqui a regra é "não exclui".
    - syncAnalysts: recebe lista de user_ids (analistas) e faz `$sector->analysts()->sync($ids)` (vínculo N:N administrável; um analista em vários setores — RN-005).
    - Toda ação audita ('setores'/'criar'|'atualizar'|'ativacao'|'vincular-analistas') via AuditService.

    `SectorCrudTest` (SQLite):
    - sem manter-setores → 403 auditado; com a permissão → 200.
    - criar setor (nome único; duplicado → 422); editar nome/situação.
    - syncAnalysts vincula/desvincula (assert pivot sector_user); um analista em 2 setores.
    - toggleActivation inativa (active=false) e o setor inativo NÃO some (continua consultável) — RN-004.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=SectorCrudTest</verify>
  <acceptance_criteria>
    - grep "manter-setores" e "setores" em routes/gestao.php; grep "analysts()->sync" em app/Http/Controllers/Gestao/SectorController.php.
    - SectorCrudTest verde (403, CRUD, vínculo N:N, inativação não-destrutiva).
  </acceptance_criteria>
  <done>HU-138 backend: setores administráveis com analistas N:N e inativação protegida; gated por manter-setores, auditado.</done>
</task>

<task type="auto">
  <name>Task 2: HU-085 StandardTextController (CRUD versionado da biblioteca de textos-padrão) + rotas</name>
  <read_first>app/Http/Controllers/Gestao/ViabilityServiceTypeController.php, app/Models/StandardText.php</read_first>
  <files>app/Http/Controllers/Gestao/StandardTextController.php, app/Http/Requests/Gestao/StandardTextRequest.php, app/Http/Resources/StandardTextResource.php, routes/gestao.php, tests/Feature/Analise/StandardTextCrudTest.php</files>
  <action>
    TDD estrito. Espelhar o padrão CRUD.

    Rotas em `routes/gestao.php`, sob `permission:manter-parametros` (decisão: biblioteca administrável reusa a permissão de admin de configuração — NÃO cria 6ª permissão; a leitura da lista ATIVA para inserção no parecer fica sob analisar-processos em 10-09; pendência SEDUR: coordenação pode exigir permissão própria), prefixo `textos-padrao`:
    - GET `textos-padrao` → index; POST → store; PUT `{standardText}` → update; PUT `{standardText}/ativacao` → toggleActivation.

    `StandardTextController`:
    - index: lista por categoria (filtro), com active e version; Inertia props (página em 10-17).
    - store: category/content/active; version inicia em 1.
    - update: ao alterar `content`, INCREMENTAR `version` (RN-005 — rastreabilidade da fundamentação); editar só metadados (category/active) NÃO incrementa.
    - toggleActivation: inverte active (preserva histórico — desativar não exclui).
    - `StandardTextRequest`: category required string; content required string; active boolean.
    - Audita ('textos-padrao'/'criar'|'atualizar'|'ativacao').

    `StandardTextCrudTest` (SQLite):
    - sem manter-parametros → 403; com → 200.
    - criar (version=1); editar content → version=2; editar só active → version inalterada.
    - toggleActivation desativa sem excluir; filtro por categoria.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=StandardTextCrudTest</verify>
  <acceptance_criteria>
    - grep "textos-padrao" em routes/gestao.php sob permission:manter-parametros.
    - grep "version" (incremento ao mudar content) em app/Http/Controllers/Gestao/StandardTextController.php.
    - StandardTextCrudTest verde (403, CRUD, versionamento por mudança de conteúdo, ativação).
  </acceptance_criteria>
  <done>HU-085 backend: biblioteca de textos-padrão versionada e administrável; gated por manter-parametros, auditada.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-138 CA-01 — setor disponível como caixa | SectorCrudTest (criar/editar) |
| HU-138 RN-005 — analista↔setor N:N administrável | SectorCrudTest (syncAnalysts; analista em 2 setores) |
| HU-138 RN-004/CA-03 — não exclui com pendência (inativa) | SectorCrudTest (toggleActivation; sem destroy) |
| HU-138 CA-04 / HU-085 — segurança de acesso | SectorCrudTest/StandardTextCrudTest (403 auditado) |
| HU-085 RN-005 — textos-padrão versionados | StandardTextCrudTest (version++ ao mudar content) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="SectorCrudTest|StandardTextCrudTest"` verde.
- Suíte completa CONTINUA verde; permissões permanecem 24 (reuso de manter-setores/manter-parametros; nenhuma permissão nova além das de 10-01).
</verification>

<success_criteria>
- HU-138 setores administráveis (analistas N:N, inativação protegida) e HU-085 textos-padrão versionados — backend completo, gated e auditado.
- ÚNICO editor de routes/gestao.php na Wave 2 (paralelos 10-05/10-06 são serviços sem rota); telas de console em 10-17.
- ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-04-SUMMARY.md`: rotas/nomes (setores.*, textos-padrao.*), assinaturas dos controllers (syncAnalysts/toggleActivation/version++), os Resources e a decisão de permissão (manter-setores; textos-padrão sob manter-parametros — pendência SEDUR). Insumo de 10-07 (caixa do setor usa o vínculo), 10-09 (parecer lê textos-padrão ativos) e 10-17 (telas de console + nav).
</output>

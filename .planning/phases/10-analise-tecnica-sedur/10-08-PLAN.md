---
phase: 10-analise-tecnica-sedur
plan: 08
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified:
  - app/Services/Analise/PreAnaliseService.php
  - app/Listeners/PreAnalisarProcesso.php
  - tests/Feature/Analise/PreAnaliseServiceTest.php
  - tests/Feature/Analise/PreAnalisarProcessoListenerTest.php
autonomous: true
requirements: [HU-140]

must_haves:
  truths:
    - "HU-140: ao encaminhar à análise, o listener AUTO-DESCOBERTO PreAnalisarProcesso (reage a EncaminhadoParaAnalise) roda o SolicitacaoViabilityResolver FRESCO e cria analysis_records revisão 1 (rascunho) pré-preenchida — engine_snapshot, engine_rules_versions, per_cnae (status sugerido por CNAE) — sem recomputo posterior (RN-004)"
    - "Listener AUTO-DESCOBERTO (NÃO Event::listen) — lição Fases 8/9; o teste TRAVA por CONTAGEM exatamente os listeners de EncaminhadoParaAnalise (só PreAnalisarProcesso nesta fase)"
    - "FA-01 degradação honesta: motor indisponível/sem dado confiável → revisão 1 criada com engine_available=false e ficha 'vazia' (modo manual), NUNCA falha silenciosa — o evento/erro é registrado"
    - "Idempotência: reprocessar o mesmo encaminhamento NÃO cria duas revisões 1 (se já existe rev 1, no-op) — recalcular é ação explícita (nova revisão) em 10-09, não automática"
    - "A pré-análise registra auditoria (RN-005: dados de entrada, versão das regras, resultados sugeridos)"
  artifacts:
    - path: "app/Services/Analise/PreAnaliseService.php"
      provides: "Cria a revisão 1 pré-preenchida reusando SolicitacaoViabilityResolver (degradação FA-01)"
      contains: "SolicitacaoViabilityResolver"
    - path: "app/Listeners/PreAnalisarProcesso.php"
      provides: "Listener auto-descoberto de EncaminhadoParaAnalise → PreAnaliseService"
      contains: "EncaminhadoParaAnalise"
  key_links:
    - from: "app/Listeners/PreAnalisarProcesso.php"
      to: "app/Events/EncaminhadoParaAnalise.php"
      via: "handle(EncaminhadoParaAnalise $event) — AUTO-DESCOBERTO por type-hint (sem Event::listen)"
      pattern: "handle(EncaminhadoParaAnalise"
    - from: "app/Services/Analise/PreAnaliseService.php"
      to: "app/Services/Solicitacao/SolicitacaoViabilityResolver.php"
      via: "resolve($request) FRESCO → engine_snapshot/per_cnae da revisão 1 (mesmo motor, sem lógica paralela)"
      pattern: "resolver->resolve"
---

<objective>
Pré-análise pelo motor (Wave 3, paralelo a 10-07): o listener AUTO-DESCOBERTO `PreAnalisarProcesso` (reage a `EncaminhadoParaAnalise`) e o `PreAnaliseService` que reusa o `SolicitacaoViabilityResolver` para criar a `analysis_records` revisão 1 (rascunho) pré-preenchida. Serviço/listener — sem rota.

Purpose: é a MAIOR alavanca de produtividade do projeto (HU-140) — transforma ~30 min de preenchimento manual em ~5 min de revisão. O motor roda SEMPRE no encaminhamento e entrega a ficha pré-preenchida (enquadramento/status sugerido por CNAE), com a versão das regras registrada (RN-004 — reabrir não reexecuta; recalcular é explícito em 10-09). Anti-fachada: o motor é o REAL (resolver da Fase 9), não um stub; FA-01 degrada honesto (engine_available=false, modo manual) sem falha silenciosa. O listener é auto-descoberto e TRAVADO por contagem (lição Fases 8/9).
Output: `PreAnaliseService` + `PreAnalisarProcesso` (auto-descoberto), cobertos por feature tests (revisão 1 pré-preenchida; degradação FA-01; idempotência; contagem de listeners).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md
@.planning/phases/10-analise-tecnica-sedur/10-02-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-03-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-06-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-04-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-06-SUMMARY.md

# PADRÕES A ESPELHAR (listener AUTO-DESCOBERTO + contagem; reuso do resolver; degradação honesta) — ler antes de codar:
@app/Services/Solicitacao/SolicitacaoViabilityResolver.php
@app/Listeners/AvaliarFluxoExpresso.php
@app/Events/EncaminhadoParaAnalise.php
@app/Models/AnalysisRecord.php
@app/Support/Audit/AuditService.php
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-140-Pre-analisar-processo-pelo-motor.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PreAnaliseService — cria a revisão 1 pré-preenchida (reusa resolver) + FA-01 + idempotência</name>
  <read_first>app/Services/Solicitacao/SolicitacaoViabilityResolver.php, app/Models/AnalysisRecord.php, app/Support/Audit/AuditService.php</read_first>
  <files>app/Services/Analise/PreAnaliseService.php, tests/Feature/Analise/PreAnaliseServiceTest.php</files>
  <action>
    TDD estrito (RED primeiro). `PreAnaliseService` (injeta SolicitacaoViabilityResolver, AuditService):
    - `preAnalisar(ViabilityRequest $request): ?AnalysisRecord` — idempotente: se já existe revisão 1, no-op (retorna a existente). Senão:
      - tenta `$resolved = $this->resolver->resolve($request);` num try/catch.
      - SUCESSO: cria AnalysisRecord rev 1 (status rascunho) com engine_available=true, engine_snapshot = `$resolved->toSnapshot()` INTEGRAL (a zona NÃO é top-level — fica aninhada em engine_snapshot['por_cnae'][i]['consulta']['territorio']['zona'] = {status,nome,...}, conforme o 10-06-SUMMARY que o PrecedentService lê), engine_rules_versions (resolved->rules_versions), per_cnae (status sugerido por CNAE: deferida/indeferida/análise mapeado da tendência), conditions/parking pré-marcados quando o motor souber. Audita ('analise'/'pre-analise', rulesVersion, properties de entrada/sugestão — RN-005).
      - FA-01 (exceção do motor OU consolidado pendente sem dado confiável): cria AnalysisRecord rev 1 com engine_available=false e ficha vazia/modo manual + aviso; audita o evento (result 'degradado') — NUNCA falha silenciosa, NUNCA inventa sugestão.
    - mapear tendência→status sugerido com a MESMA semântica do resolver (sem lógica de decisão paralela — RN-001 da HU-140: sugestão, não decisão).

    `PreAnaliseServiceTest` (SQLite + fake SpatialRepository):
    - request em_analise com CNAEs → cria rev 1 rascunho com engine_available=true, per_cnae preenchido, engine_rules_versions presentes; auditoria 'analise'/'pre-analise' gravada.
    - idempotência: chamar 2x → 1 única revisão 1.
    - FA-01: resolver lançando/zona indisponível → rev 1 com engine_available=false (modo manual), auditado; sem sugestão inventada.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=PreAnaliseServiceTest</verify>
  <acceptance_criteria>
    - grep "resolver->resolve" e "engine_available" em app/Services/Analise/PreAnaliseService.php.
    - PreAnaliseServiceTest verde (rev 1 pré-preenchida; idempotência; FA-01 engine_available=false sem fachada).
  </acceptance_criteria>
  <done>PreAnaliseService cria a revisão 1 pré-preenchida pelo motor real (reuso do resolver), idempotente, com FA-01 honesto e auditoria.</done>
</task>

<task type="auto">
  <name>Task 2: PreAnalisarProcesso (listener AUTO-DESCOBERTO) + teste de CONTAGEM de listeners</name>
  <read_first>app/Listeners/AvaliarFluxoExpresso.php, app/Events/EncaminhadoParaAnalise.php</read_first>
  <files>app/Listeners/PreAnalisarProcesso.php, tests/Feature/Analise/PreAnalisarProcessoListenerTest.php</files>
  <action>
    TDD estrito. `PreAnalisarProcesso` (auto-descoberto por type-hint — espelhar AvaliarFluxoExpresso; NÃO registrar em EventServiceProvider/Event::listen):
    - `handle(EncaminhadoParaAnalise $event): void` → `$this->preAnalise->preAnalisar($event->request);` (injeta PreAnaliseService). Pode implementar ShouldQueue se o padrão da casa for assíncrono (verificar AvaliarFluxoExpresso); manter consistente.

    `PreAnalisarProcessoListenerTest` (SQLite):
    - **CONTAGEM (lição Fases 8/9)**: `Event::fake` não serve aqui; usar `Illuminate\Support\Facades\Event::getListeners(EncaminhadoParaAnalise::class)` (ou disparar o evento REAL e asserir efeito único) para travar que EXATAMENTE 1 listener (PreAnalisarProcesso) reage a EncaminhadoParaAnalise — falha se alguém adicionar/duplicar.
    - integração: disparar EncaminhadoParaAnalise(request) REAL → existe analysis_records rev 1 (o listener auto-descoberto rodou). Disparar 2x → 1 revisão (idempotência via service).
  </action>
  <verify>php artisan test --compact --filter=PreAnalisarProcessoListenerTest</verify>
  <acceptance_criteria>
    - grep "handle(EncaminhadoParaAnalise" em app/Listeners/PreAnalisarProcesso.php; NÃO há Event::listen para EncaminhadoParaAnalise no projeto.
    - PreAnalisarProcessoListenerTest verde (contagem = 1 listener; integração cria rev 1; idempotente).
  </acceptance_criteria>
  <done>Listener auto-descoberto PreAnalisarProcesso, travado por contagem; integração cria a revisão 1 ao encaminhar.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-140 CA-01 — ficha pré-preenchida | PreAnaliseServiceTest (rev 1 com per_cnae/sugestão) |
| HU-140 CA-03 / FA-01 — degradação controlada | PreAnaliseServiceTest (engine_available=false, modo manual) |
| HU-140 RN-004 — não reexecuta ao reabrir (idempotência) | PreAnaliseServiceTest (2x → 1 revisão) |
| HU-140 RN-005 — auditoria da pré-análise | PreAnaliseServiceTest (Activity 'analise'/'pre-analise') |
| Lição Fases 8/9 — listener auto-descoberto travado | PreAnalisarProcessoListenerTest (contagem = 1) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="PreAnaliseServiceTest|PreAnalisarProcessoListenerTest"` verde.
- Suíte completa CONTINUA verde (serviço/listener novos; sem rota; file-disjunto de 10-07).
</verification>

<success_criteria>
- HU-140: ficha rev 1 pré-preenchida pelo motor REAL (reuso do resolver), idempotente, com FA-01 honesto (engine_available=false) e auditoria.
- Listener AUTO-DESCOBERTO travado por contagem (1) — lição Fases 8/9; paralelo file-disjunto com 10-07 (que dispara o evento).
- ZERO dependência nova; nenhuma lógica de decisão paralela (sugestão, não decisão — RN-001).
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-08-SUMMARY.md`: a assinatura do PreAnaliseService (preAnalisar, mapeamento tendência→status sugerido, FA-01), o listener auto-descoberto e a contagem travada, e o shape da revisão 1 (engine_snapshot/engine_rules_versions/per_cnae). Insumo direto de 10-09 (a ficha abre a partir da revisão 1; recalcular cria nova revisão) e 10-17 (a ficha SAPS exibe a sugestão + destaque de pendência).
</output>

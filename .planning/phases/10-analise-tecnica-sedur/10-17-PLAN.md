---
phase: 10-analise-tecnica-sedur
plan: 17
type: execute
wave: 7
depends_on: ["10-04", "10-09", "10-14"]
files_modified:
  - resources/js/pages/gestao/processos/ficha.tsx
  - resources/js/pages/gestao/setores/index.tsx
  - resources/js/pages/gestao/textos-padrao/index.tsx
  - resources/js/components/app/command-search.tsx
  - resources/js/layouts/gestao-layout.tsx
  - tests/Feature/Analise/FichaUiSmokeTest.php
autonomous: true
requirements: [HU-135, HU-140, HU-142, HU-085, HU-086, HU-087, HU-088, HU-132, HU-138]

must_haves:
  truths:
    - "HU-135: a ficha SAPS é navegável — por CNAE (status escolhido × SUGERIDO pelo motor com destaque dos gatilhos/pendências — HU-140), condicionantes (checkboxes + texto livre com inserção de textos-padrão — HU-085 RN-009), seção de vagas (requerente × exigido → veredito), parecer (textarea + biblioteca de textos-padrão), autosave (PATCH debounce de config), finalizar, nova revisão e diff entre revisões (HU-135 RN-007)"
    - "HU-142: painel de PRECEDENTES (processos do imóvel + estatística do CNAE na zona, com 'sem precedentes' explícito) + mini-mapa permanente ao lado do enquadramento; HU-140: campos sem dado confiável destacados como pendência (engine_available=false → modo manual com aviso)"
    - "Ações na ficha conforme permissão: finalizar/decidir (deferir/indeferir/encerrar — gate analisar-processos), abrir pendência, encaminhar malha fina, emitir/baixar TVL (gate emitir-tvl) — POST nas rotas de 10-15; o TVL abre por URL temporária assinada"
    - "Telas admin de setores (HU-138) e textos-padrão (HU-085) no console (CRUD consumindo 10-04) + LINKS de navegação (gestao-layout) para Fila/Processos/Setores/Textos-padrão filtrados por permissão + a busca global Cmd+K (HU-082 RN-009, command-search consumindo gestao.processos.busca de 10-14) montada no layout — ÚNICO editor do gestao-layout na Wave 7"
    - "Padrão TailAdmin do console + mapa Leaflet reusável; ZERO dependência nova; tsc/build verdes; file-disjunto de 10-16 (páginas distintas) e de 10-15 (só consome rotas)"
  artifacts:
    - path: "resources/js/pages/gestao/processos/ficha.tsx"
      provides: "Ficha de análise SAPS: por CNAE/condicionantes/vagas/parecer/autosave/finalizar/diff + precedentes + mini-mapa + ações"
      contains: "autosave"
    - path: "resources/js/layouts/gestao-layout.tsx"
      provides: "Grupos de navegação do console (Análise técnica) filtrados por permissão"
      contains: "Análise"
  key_links:
    - from: "resources/js/pages/gestao/processos/ficha.tsx"
      to: "app/Http/Controllers/Gestao/AnalysisRecordController.php"
      via: "GET/PATCH/finalizar/nova-revisao/diff/precedentes (rotas de 10-09) + ações de decisão/pendência/malha-fina/TVL (rotas de 10-15)"
      pattern: "ficha"
    - from: "resources/js/layouts/gestao-layout.tsx"
      to: "resources/js/components/app/app-sidebar.tsx"
      via: "grupos de nav (Análise técnica: Fila, Processos, Setores, Textos-padrão) filtrados por permissão"
      pattern: "SidebarGroup"
---

<objective>
UI do console — ficha SAPS, admin e navegação (Wave 7, páginas): a ficha de análise (HU-135/140/142/085 + ações de decisão/pendência/malha-fina/TVL), as telas admin de setores (HU-138) e textos-padrão (HU-085), e os links de navegação do console. ÚNICO editor do gestao-layout (nav) na Wave 7.

Purpose: a ficha é a tela onde a análise humana acontece — pré-preenchida pelo motor (HU-140), com precedentes (HU-142), mini-mapa e diff, e as ações que concluem o processo. As telas admin tornam setores e textos-padrão administráveis pela interface. A navegação dá descoberta às telas (hoje os resultados expressos só são acessíveis por URL — follow-up do 09-11). Reusa o design system do console e o mapa Leaflet. File-disjunto de 10-16 (páginas diferentes) e de 10-15 (só consome rotas).
Output: ficha.tsx + setores/index.tsx + textos-padrao/index.tsx + command-search.tsx + grupos de nav no gestao-layout + smoke Inertia, tsc/build verdes.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md
@.planning/phases/10-analise-tecnica-sedur/10-04-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-09-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-15-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-06-SUMMARY.md

# PADRÕES A ESPELHAR (página de console + formulário useForm + nav por permissão; mapa Leaflet) — ler antes de codar (ativar inertia-react-development):
@resources/js/pages/gestao/risco/index.tsx
@resources/js/pages/gestao/resultados-expresso/show.tsx
@resources/js/layouts/gestao-layout.tsx
@resources/js/components/app/app-sidebar.tsx
@docs/legado-saps/02.jpg
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-135-Preencher-ficha-de-análise.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ficha de análise SAPS (por CNAE/condicionantes/vagas/parecer/autosave/finalizar/diff + precedentes + mini-mapa + ações)</name>
  <read_first>resources/js/pages/gestao/resultados-expresso/show.tsx, docs/legado-saps/02.jpg, .planning/phases/10-analise-tecnica-sedur/10-09-SUMMARY.md</read_first>
  <files>resources/js/pages/gestao/processos/ficha.tsx</files>
  <action>
    Ativar a skill `inertia-react-development`. Espelhar a ficha SAPS (prints 02–08) no padrão do console. ZERO dependência nova.
    `ficha.tsx`:
    - Por CNAE: status escolhido (deferida/indeferida/análise) ao lado do SUGERIDO pelo motor (HU-140), com descrição LOUOS/grupo de uso/TLL e DESTAQUE dos gatilhos e dos campos sem dado confiável (pendência de análise — engine_available=false → banner de modo manual).
    - Condicionantes: checkboxes (pré-marcadas pelo motor) + adicionais em texto livre, com inserção da biblioteca de textos-padrão (HU-085 RN-009).
    - Vagas: requerente × exigido → veredito conforme/não conforme.
    - Parecer: textarea + picker de textos-padrão ativos.
    - Autosave: PATCH no rascunho com debounce (constante de config analise.autosave.debounce_ms exposta como prop/inline) — indicar "salvo"/"salvando".
    - Ações (conforme permissão, via rotas 10-09/10-15): Finalizar ficha; Nova revisão; Ver diff (de/para revisões — HU-135 RN-007); Decidir (deferir/indeferir/encerrar — POST processos/{id}/decidir, só com ficha finalizada); Abrir pendência; Encaminhar malha fina; Emitir/baixar TVL (gate emitir-tvl; download por URL assinada).
    - Painel PRECEDENTES (HU-142): processos do imóvel (resultado/data/serviço/analista + link) + estatística do CNAE na zona ("últimos N meses: X deferimentos, Y indeferimentos") + "sem precedentes" explícito; consome GET processos/{id}/precedentes.
    - Mini-mapa Leaflet permanente (reusar o componente da Fase 4) com polígono + zona/via.
    Mobile-first + dark. Estados de loading/empty.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - grep "autosave" e "precedentes" e (mapa/Leaflet) em resources/js/pages/gestao/processos/ficha.tsx.
    - As ações usam route() das rotas de 10-09/10-15; ZERO dependência nova.
    - `npx tsc --noEmit` verde.
  </acceptance_criteria>
  <done>Ficha SAPS navegável (sugestão do motor + condicionantes + vagas + parecer + autosave + finalizar/diff + ações + precedentes + mini-mapa).</done>
</task>

<task type="auto">
  <name>Task 2: telas admin (setores HU-138 + textos-padrão HU-085) no console</name>
  <read_first>resources/js/pages/gestao/risco/index.tsx, .planning/phases/10-analise-tecnica-sedur/10-04-SUMMARY.md</read_first>
  <files>resources/js/pages/gestao/setores/index.tsx, resources/js/pages/gestao/textos-padrao/index.tsx</files>
  <action>
    Ativar `inertia-react-development`. Padrão CRUD do console (modais criar/editar, ConfirmDialog, Pagination, EmptyState — Fases 2.x).
    `setores/index.tsx` (HU-138): lista de setores (nome/situação/contadores) + criar/editar (modal) + toggle ativação + gerenciar analistas (multiselect → PUT setores/{id}/analistas). Consome as rotas de 10-04.
    `textos-padrao/index.tsx` (HU-085): lista por categoria + criar/editar (modal, com aviso de que editar conteúdo incrementa versão) + toggle ativação. Consome as rotas de 10-04.
    Mobile-first + dark.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - As páginas usam o padrão CRUD do console (modais/ConfirmDialog/Pagination/EmptyState).
    - grep "analistas" em setores/index.tsx; grep "versão"/"version" (aviso) em textos-padrao/index.tsx.
    - `npx tsc --noEmit` verde.
  </acceptance_criteria>
  <done>Telas admin de setores (com vínculo de analistas) e textos-padrão (versionado) no console.</done>
</task>

<task type="auto">
  <name>Task 3: navegação do console (Análise técnica) por permissão + busca global Cmd+K + smoke Inertia</name>
  <read_first>resources/js/layouts/gestao-layout.tsx, resources/js/components/app/app-sidebar.tsx, .planning/phases/10-analise-tecnica-sedur/10-14-SUMMARY.md</read_first>
  <files>resources/js/components/app/command-search.tsx, resources/js/layouts/gestao-layout.tsx, tests/Feature/Analise/FichaUiSmokeTest.php</files>
  <action>
    Ativar `inertia-react-development`. No `gestao-layout.tsx` (ÚNICO editor da nav/layout na Wave 7), ACRESCENTAR um grupo "Análise técnica" (e itens) filtrados por permissão (padrão da casa — itens com visible derivado de can):
    - "Fila" (gestao.processos.fila — analisar-processos), "Processos" (gestao.processos.index — consultar-solicitacoes), "Setores" (gestao.setores.index — manter-setores), "Textos-padrão" (gestao.textos-padrao.index — manter-parametros). Incluir também o link "Resultados do fluxo expresso" se ainda não estiver na nav (follow-up do 09-11) — à discrição.

    Criar `command-search.tsx` (HU-082 RN-009): componente global de busca disparado por Cmd/Ctrl+K (modal), consome gestao.processos.busca (10-14, debounce) e navega ao processo selecionado; MONTÁ-LO no gestao-layout (este plano é o único editor do layout). Degradação: visível só para quem tem consultar-solicitacoes.

    `FichaUiSmokeTest` (SQLite): com analisar-processos, GET gestao.processos.ficha (de um processo em_analise com ficha) renderiza o componente 'gestao/processos/ficha' com props (revisão/per_cnae/precedentes/textos-padrão). Smoke do contrato página↔props. `npm run build` ao final.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=FichaUiSmokeTest && npx tsc --noEmit && npm run build</verify>
  <acceptance_criteria>
    - grep "Análise" (grupo de nav) em resources/js/layouts/gestao-layout.tsx; itens filtrados por permissão; command-search montado no layout.
    - grep "processos.busca" em resources/js/components/app/command-search.tsx.
    - FichaUiSmokeTest verde (componente ficha + props); `npx tsc --noEmit` e `npm run build` verdes.
  </acceptance_criteria>
  <done>Navegação do console (Análise técnica) por permissão + busca global Cmd+K montada no layout + smoke da ficha; build/typecheck verdes.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste / evidência |
|---|---|
| HU-135 — ficha por CNAE/condicionantes/vagas/parecer/autosave | FichaUiSmokeTest (props) + smoke navegável (10-18) |
| HU-140 — sugestão do motor + destaque de pendência/modo manual | ficha.tsx (sugerido×escolhido; engine_available) |
| HU-142 — painel de precedentes + "sem precedentes" | ficha.tsx (consome /precedentes) |
| HU-135 RN-007 — diff entre revisões | ficha.tsx (ver diff) |
| HU-086/087/088/089/132 — ações na ficha (decidir/TVL) | ficha.tsx (rotas 10-15) |
| HU-138/HU-085 — telas admin | setores/index.tsx, textos-padrao/index.tsx |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=FichaUiSmokeTest` verde.
- `npx tsc --noEmit` e `npm run build` verdes.
- ÚNICO editor do gestao-layout (nav) na Wave 7; file-disjunto de 10-16 e 10-15.
</verification>

<success_criteria>
- Ficha SAPS navegável (sugestão do motor + condicionantes + vagas + parecer + autosave + finalizar/diff + ações + precedentes + mini-mapa) e telas admin (setores/textos-padrão).
- Navegação do console (Análise técnica) filtrada por permissão; reusa design system + mapa Leaflet; tsc/build verdes; ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-17-SUMMARY.md`: as páginas criadas (ficha/setores/textos-padrão), o command-search (Cmd+K) montado no layout, os grupos de nav adicionados (por permissão) e as rotas que a ficha aciona (decidir/pendência/malha-fina/TVL). Insumo direto do smoke navegável (10-18).
</output>

---
phase: 10-analise-tecnica-sedur
plan: 13
type: execute
wave: 6
depends_on: ["10-01", "10-02", "10-09"]
files_modified:
  - composer.json
  - composer.lock
  - config/sile.php
  - app/Services/Analise/TvlPdfService.php
  - resources/views/tvl/documento.blade.php
  - tests/Feature/Analise/TvlPdfServiceTest.php
autonomous: true
requirements: [HU-132]

must_haves:
  truths:
    - "barryvdh/laravel-dompdf é instalado via composer require (dependência PRÉ-APROVADA no PROJECT.md — lib do SIGVISA); é a ÚNICA dependência nova da fase e fica isolada neste plano"
    - "TvlPdfService::generate(ViabilityDecision, ator): TvlDocument — SÓ outcome=deferida (FA-01: indeferida/em_analise → bloqueio com mensagem) e perfil emitir-tvl (gate no controller 10-15); monta o PDF com número de produto (TVL), CNAEs deferidas, condicionantes (da ficha) e fundamentação legal (CA-01)"
    - "Storage: grava no disco parametrizado analise.tvl.disk NUNCA público; cada emissão/reimpressão cria uma linha em tvl_documents (verification_code único, generated_by/at) auditada (RN-002/CA-03); o download por URL temporária assinada (TTL analise.tvl.download.ttl_minutos) é wired em 10-15"
    - "Assinatura parametrizável (analise.tvl.assinatura.modo/imagem_path): imagem do diretor (legado) ou nenhuma; gov.br/ICP é gancho → SEDUR (RN-005). O TVL NÃO vai ao cidadão (CA-02 — relatório interno)"
    - "Serviço/template (sem rota): testado com Storage::fake (assert %PDF gravado no disco não-público + linha tvl_documents). dompdf renderiza Blade real, não HTML simulado"
  artifacts:
    - path: "app/Services/Analise/TvlPdfService.php"
      provides: "Geração do TVL PDF (dompdf) a partir da ViabilityDecision deferida + condicionantes da ficha"
      contains: "TvlDocument"
    - path: "resources/views/tvl/documento.blade.php"
      provides: "Template do TVL PDF (número de produto, CNAEs deferidas, condicionantes, fundamentação, assinatura parametrizável)"
      contains: "tvl_product_number"
  key_links:
    - from: "app/Services/Analise/TvlPdfService.php"
      to: "app/Models/ViabilityDecision.php"
      via: "fonte = ViabilityDecision deferida (mesma fonte do expresso e da análise humana) + condicionantes da ficha"
      pattern: "ViabilityDecision"
    - from: "app/Services/Analise/TvlPdfService.php"
      to: "app/Models/TvlDocument.php"
      via: "cada emissão grava tvl_documents (disk param NÃO público, verification_code único, auditada)"
      pattern: "TvlDocument::create"
---

<objective>
TVL PDF no backoffice (Wave 6, serviço + única dependência nova): instalar `barryvdh/laravel-dompdf` (pré-aprovada) e o `TvlPdfService::generate(ViabilityDecision): TvlDocument` — só deferida, no disco parametrizado não-público, com tvl_documents auditado e assinatura parametrizável. Endpoints emitir/download em 10-15.

Purpose: HU-132 — o TVL deixou de ir ao cidadão (canal oficial é Regin/SEFAZ); vira relatório administrativo interno emitível sob demanda após deferimento (expresso OU humano). A fonte é a MESMA `ViabilityDecision` (Fase 9 / 10-10) — fonte única do PDF e da explicabilidade. Anti-fachada: dompdf renderiza um Blade real (assert %PDF), grava no disco NUNCA público e cada emissão é auditada em tvl_documents; só deferida (FA-01). Instalação da dep isolada aqui (pré-aprovada no PROJECT.md, lib do SIGVISA), sem aprovação adicional.
Output: dep instalada + TvlPdfService + template Blade + config, cobertos por feature tests (Storage::fake, %PDF, só deferida, disco não-público, auditoria).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-analise-tecnica-sedur/10-CONTEXT.md
@docs/superpowers/specs/2026-06-14-analise-tecnica-design.md
@.planning/phases/10-analise-tecnica-sedur/10-01-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-02-SUMMARY.md
@.planning/phases/10-analise-tecnica-sedur/10-09-SUMMARY.md
@.planning/phases/09-fluxo-expresso/09-02-SUMMARY.md

# PADRÕES A ESPELHAR (Storage disk parametrizado + sha/código; Settings com default inline; ViabilityDecision) — ler antes de codar:
@app/Models/ViabilityDecision.php
@app/Models/ViabilityRequest.php
@app/Support/Settings.php
@docs/SILE_HUs_Completas_MD/EP10-Análise-Tecnica-SEDUR/HU-132-Emitir-TVL-em-PDF-no-backoffice.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: instalar barryvdh/laravel-dompdf (pré-aprovada) + config</name>
  <read_first>composer.json, config/sile.php</read_first>
  <files>composer.json, composer.lock, config/sile.php</files>
  <action>
    Instalar a dependência PRÉ-APROVADA (PROJECT.md lista dompdf como lib aplicável do SIGVISA; CONTEXT autoriza na wave do TVL — sem aprovação adicional):
    - `composer require barryvdh/laravel-dompdf` (versão compatível com Laravel 13/PHP 8.5; deixar o composer resolver — NÃO inventar versão).
    - confirmar o auto-discovery do provider/facade (Barryvdh\DomPDF) — `php artisan package:discover` sem erro.
    - garantir que `config/sile.php` já tem o bloco analise.tvl.* (de 10-01); se faltar a constante técnica de paginação/papel do PDF, adicioná-la em config (ex.: analise.tvl.paper = 'a4', orientation = 'portrait' — constante técnica, fora do catálogo).
    NÃO mudar outras dependências.
  </action>
  <verify>composer show barryvdh/laravel-dompdf && php artisan package:discover</verify>
  <acceptance_criteria>
    - grep "barryvdh/laravel-dompdf" em composer.json (require) e presente em composer.lock.
    - php artisan package:discover roda sem erro (provider descoberto).
  </acceptance_criteria>
  <done>dompdf instalado (pré-aprovado) e descoberto — única dependência nova da fase, isolada.</done>
</task>

<task type="auto">
  <name>Task 2: TvlPdfService + template Blade + tvl_documents auditado (Storage::fake, %PDF)</name>
  <read_first>app/Models/ViabilityDecision.php, app/Models/TvlDocument.php, app/Support/Settings.php</read_first>
  <files>app/Services/Analise/TvlPdfService.php, resources/views/tvl/documento.blade.php, tests/Feature/Analise/TvlPdfServiceTest.php</files>
  <action>
    TDD estrito (RED primeiro). `TvlPdfService` (usa Barryvdh\DomPDF\Facade\Pdf):
    - `generate(ViabilityDecision $decision, ?User $ator = null): TvlDocument` — exige outcome=Deferida (senão DomainException — FA-01); monta os dados (tvl_product_number, solicitação/empresa/imóvel, CNAEs DEFERIDAS, condicionantes da ficha finalizada via currentAnalysisRecord/conditions, fundamentação legal da decisão), renderiza `Pdf::loadView('tvl.documento', $dados)` com paper/orientation de config; grava no disco `Settings::get('analise.tvl.disk', config('sile.analise.tvl.disk','local'))` (NUNCA 'public') num path determinístico; cria `TvlDocument` (disk, path, verification_code único, generated_by=$ator, generated_at) e AUDITA ('analise'/'tvl-emitido', subject decisão) — cada emissão/reimpressão = nova linha (RN-002/CA-03).
    - assinatura: ler analise.tvl.assinatura.modo; 'imagem' → embute a imagem de analise.tvl.assinatura.imagem_path (se existir); 'nenhuma' → sem assinatura; gov.br/ICP = gancho documentado (→ SEDUR).
    - `documento.blade.php`: layout simples do TVL (cabeçalho SEDUR, número de produto, atividades deferidas, condicionantes, fundamentação, código de verificação/QR opcional, assinatura) — pt-BR, conteúdo só da decisão real.

    `TvlPdfServiceTest` (SQLite, Storage::fake do disco de TVL):
    - decisão DEFERIDA → generate grava um arquivo cujo conteúdo começa com '%PDF' no disco fake (Storage::assertExists) + cria TvlDocument (verification_code único, generated_by) + auditoria 'analise'/'tvl-emitido'.
    - decisão INDEFERIDA → DomainException (FA-01, não gera).
    - reimpressão (generate 2x) → 2 linhas tvl_documents (cada emissão auditada).
    - o disco usado NÃO é 'public' (assert config/uso).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=TvlPdfServiceTest</verify>
  <acceptance_criteria>
    - grep "loadView('tvl.documento'" e "TvlDocument::create" em app/Services/Analise/TvlPdfService.php; grep "Deferida" (bloqueio FA-01).
    - grep "tvl_product_number" em resources/views/tvl/documento.blade.php.
    - TvlPdfServiceTest verde (%PDF gravado no disco não-público; só deferida; tvl_documents + auditoria; reimpressão).
  </acceptance_criteria>
  <done>TVL PDF real (dompdf) a partir da ViabilityDecision deferida, disco não-público, tvl_documents auditado, assinatura parametrizável; só deferida.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-132 CA-01 — PDF com número/atividades/condicionantes/fundamentação | TvlPdfServiceTest (%PDF + dados) |
| HU-132 FA-01 / RN-001 — só deferida + perfil | TvlPdfServiceTest (indeferida → DomainException) + gate em 10-15 |
| HU-132 CA-03 / RN-002 — emissão/reimpressão auditada | TvlPdfServiceTest (tvl_documents + Activity; reimpressão = 2 linhas) |
| HU-132 — disco não público | TvlPdfServiceTest (disk ≠ public) |
| HU-132 RN-005 — assinatura parametrizável | TvlPdfServiceTest/config (modo imagem/nenhuma) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=TvlPdfServiceTest` verde.
- `composer show barryvdh/laravel-dompdf` confirma a instalação; suíte completa CONTINUA verde.
</verification>

<success_criteria>
- TVL PDF real (dompdf, Blade renderizado, %PDF) a partir da ViabilityDecision deferida; disco parametrizado NÃO público; tvl_documents auditado; assinatura parametrizável; só deferida (FA-01).
- ÚNICA dependência nova da fase (pré-aprovada), isolada neste plano; NÃO vai ao cidadão (CA-02).
- Serviço/template sem rota (emitir/download em 10-15); paralelo file-disjunto com 10-14.
</success_criteria>

<output>
Criar `.planning/phases/10-analise-tecnica-sedur/10-13-SUMMARY.md`: a instalação do dompdf, a assinatura de TvlPdfService::generate, o template e os dados do PDF, o disco/assinatura parametrizados e a auditoria tvl_documents. Insumo direto de 10-15 (endpoints emitir/download por URL assinada, gate emitir-tvl) e 10-18 (smoke do TVL).
</output>

---
phase: 06-classificacao-de-risco
plan: 08
type: execute
wave: 7
depends_on: ["06-06"]
files_modified:
  - resources/js/pages/gestao/risco/index.tsx
  - resources/js/pages/gestao/risco/condicionantes.tsx
  - resources/js/layouts/gestao-layout.tsx
  - resources/js/components/icons/index.ts
autonomous: false
requirements: [HU-019, HU-020, HU-052, HU-053]

must_haves:
  truths:
    - "Analista/gestor vê a tabela de risco vigente no console (listagem com busca, filtro por nível e dimensão municipal/sanitária separadas)"
    - "Administrador atualiza a classificação publicando nova versão pela interface (4-olhos comunicado)"
    - "Administrador mantém condicionantes-pergunta pela interface (criar/editar/remover) com efeito de reclassificação visível"
    - "Os itens de navegação de risco só aparecem para quem tem a permissão correspondente"
  artifacts:
    - path: "resources/js/pages/gestao/risco/index.tsx"
      provides: "Consulta da tabela de risco vigente (DataTable padrão) + ação de publicar nova versão"
      contains: "gestao/risco/index"
    - path: "resources/js/pages/gestao/risco/condicionantes.tsx"
      provides: "CRUD de condicionantes-pergunta (Modal + ConfirmDialog)"
      contains: "condicionante"
    - path: "resources/js/layouts/gestao-layout.tsx"
      provides: "Itens de navegação 'Classificação de risco' e 'Condicionantes' por permissão"
      contains: "consultar-risco"
  key_links:
    - from: "resources/js/pages/gestao/risco/index.tsx"
      to: "gestao.risco.index / gestao.risco.publicar"
      via: "DataTable server-driven + Form de publicação"
      pattern: "risco"
    - from: "resources/js/layouts/gestao-layout.tsx"
      to: "auth.permissions"
      via: "visible por consultar-risco/manter-risco"
      pattern: "auth.permissions.includes"
---

<objective>
A **UI do console** para a classificação de risco: consulta da tabela vigente (HU-052), publicação versionada da atualização (HU-053/HU-020) e manutenção das condicionantes-pergunta (HU-019). Reusa a biblioteca de componentes da gestão (DataTable, PageHeader, Card, Modal, ConfirmDialog, Pagination, TableToolbar) e a identidade "Console SEDUR" (sidebar escura), padrões travados nas Fases 2.2/2.4.

Purpose: tornar a tabela de risco navegável de ponta a ponta no browser (critério 2 transversal do ROADMAP) e fechar as HUs de manutenção/consulta com tela real — não fachada.
Output: páginas `gestao/risco/index.tsx` e `gestao/risco/condicionantes.tsx`, itens de navegação por permissão na gestao-layout, com checkpoint humano de validação visual (claro/escuro/mobile).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-classificacao-de-risco/06-06-SUMMARY.md
@.planning/STATE.md

# PADRÃO A ESPELHAR (listagem/CRUD do console — ler antes de codar):
@resources/js/pages/gestao/cnaes/index.tsx
@resources/js/layouts/gestao-layout.tsx
@resources/js/components/app/app-sidebar.tsx

# Ativar a skill inertia-react-development (Inertia v3 + React 19) antes de implementar:
# .cursor/skills/inertia-react-development/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Navegação de risco na gestao-layout (por permissão)</name>
  <read_first>resources/js/layouts/gestao-layout.tsx, resources/js/components/icons/index.ts</read_first>
  <files>resources/js/layouts/gestao-layout.tsx, resources/js/components/icons/index.ts</files>
  <action>
    No grupo "Cadastros" da sidebar (gestao-layout.tsx), adicionar dois itens espelhando o item de CNAEs:
    - 'Classificação de risco' → '/gestao/risco', icon (reusar um ícone existente de components/icons — ex.: TableIcon ou um ícone de escudo/alerta se já houver; NÃO adicionar dependência), visible: auth.permissions.includes('consultar-risco').
    - 'Condicionantes' → '/gestao/risco/condicionantes', icon existente, visible: auth.permissions.includes('manter-risco').
    Se for necessário um ícone novo, adicioná-lo como SVG inline em components/icons/index.ts (padrão do projeto — ícones SVG inline, sem libs). Caso contrário, reusar.
  </action>
  <verify>npm run build 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - grep "consultar-risco" resources/js/layouts/gestao-layout.tsx
    - grep "/gestao/risco" resources/js/layouts/gestao-layout.tsx (2 itens)
    - npm run build sem erro (typecheck/bundle verdes).
  </acceptance_criteria>
  <done>Itens 'Classificação de risco' e 'Condicionantes' na sidebar do console, visíveis por permissão; build verde.</done>
</task>

<task type="auto">
  <name>Task 2: Página de consulta da tabela de risco (gestao/risco/index.tsx)</name>
  <read_first>resources/js/pages/gestao/cnaes/index.tsx, .planning/phases/06-classificacao-de-risco/06-06-SUMMARY.md</read_first>
  <files>resources/js/pages/gestao/risco/index.tsx</files>
  <action>
    Espelhar a página de CNAEs (PageHeader → Card(CardHeader com ação) → CardContent(TableToolbar → chips de filtros → DataTable → Pagination)). Consumir as props de RiscoController@index (ver 06-06-SUMMARY): classificacoes (paginadas), versaoMunicipal, versaoSanitaria, resumoNiveis, filtros, perPageOptions.
    - Colunas: CNAE (formatted_code), denominação, risco municipal (Badge por nível: baixo_a/baixo_b verde, alto vermelho), risco sanitário (Badge), versão vigente.
    - Toolbar: busca por código/denominação + filtro por nível (chips). Mostrar a versão vigente municipal/sanitária e um KPI de resumo (767/328/236) usando KpiCard/Badge.
    - Ação "Publicar nova versão" (visível só com manter-risco): abre Modal com Form (useForm/<Form> do Inertia v3) que envia PUT gestao.risco.publicar; comunicar o 4-olhos (aviso de que o publicador deve ser distinto do autor) e exibir flash.error quando o backend bloquear (degradação comunicada).
    - Dimensões municipal × sanitária claramente SEPARADAS na tela (colunas/badges distintos — RN-009).
    Usar Inertia v3 (<Link>, useForm/<Form>). Dark mode + responsivo (mobile-first), padrões Tailwind do projeto.
  </action>
  <verify>npm run build 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - Arquivo resources/js/pages/gestao/risco/index.tsx existe e renderiza component 'gestao/risco/index'.
    - grep -i "municipal" e "sanit" no arquivo (duas dimensões separadas na tela).
    - npm run build verde.
  </acceptance_criteria>
  <done>Tela de consulta da tabela de risco vigente com municipal × sanitário separados, busca/filtro e ação de publicar nova versão (4-olhos comunicado); build verde.</done>
</task>

<task type="auto">
  <name>Task 3: Página de manutenção de condicionantes (gestao/risco/condicionantes.tsx)</name>
  <read_first>resources/js/pages/gestao/cnaes/index.tsx, .planning/phases/06-classificacao-de-risco/06-06-SUMMARY.md</read_first>
  <files>resources/js/pages/gestao/risco/condicionantes.tsx</files>
  <action>
    CRUD espelhando o padrão de CNAEs (datatable em card; criar/editar em Modal; remover via ConfirmDialog danger). Consumir props de RiscoCondicionanteController@index.
    - Listagem: pergunta, CNAE (quando houver), efeito de reclassificação (ex.: "Sim → Alto Risco"), tipo de resposta.
    - Modal de criar/editar: campos pergunta, cnae_code (opcional), tipo_resposta (select booleano_sim_nao), regra_reclassificacao (resposta_gatilho + reclassifica_para select baixo/medio/alto + fundamento), texto_parecer. Submit POST/PUT (useForm/<Form>).
    - Remoção via ConfirmDialog (ui/confirm-dialog) com aviso de impacto.
    - Exibir como a resposta reclassifica o risco (preview do efeito) — torna o mecanismo "DI" tangível para o admin.
    Inertia v3, dark mode, responsivo.
  </action>
  <verify>npm run build 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - Arquivo resources/js/pages/gestao/risco/condicionantes.tsx existe (component 'gestao/risco/condicionantes').
    - grep -i "reclassif" no arquivo (efeito de reclassificação na tela).
    - npm run build verde.
  </acceptance_criteria>
  <done>Tela de manutenção de condicionantes-pergunta com criar/editar/remover e preview do efeito de reclassificação; build verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Telas do console para classificação de risco (consulta da tabela vigente + publicação versionada) e manutenção de condicionantes-pergunta, com navegação por permissão.</what-built>
  <how-to-verify>
    1. Rodar o ambiente: `composer run dev` (ou `npm run dev`).
    2. Logar no console: `/gestao/login` com admin@sile.dev / password (seed dev). Aceitar o termo LGPD se solicitado.
    3. Abrir "Classificação de risco" na sidebar (/gestao/risco): confirmar a tabela vigente com risco municipal e sanitário SEPARADOS, busca por CNAE, filtro por nível e o resumo 767/328/236.
    4. Testar "Publicar nova versão": confirmar o aviso de 4-olhos e a mensagem de bloqueio quando publicador = autor (degradação comunicada).
    5. Abrir "Condicionantes" (/gestao/risco/condicionantes): criar uma condicionante-pergunta com efeito "Sim → Alto Risco", editar e remover (ConfirmDialog).
    6. Validar dark mode (toggle) e viewport mobile (375px) — toolbar empilhada, scroll-x na tabela.
    7. Conferir que um usuário SEM consultar-risco não vê os itens na sidebar e recebe 403 ao acessar /gestao/risco direto.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os ajustes visuais/UX necessários.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` verde (typecheck + bundle).
- `php artisan test --compact --filter=Risco` continua verde (as páginas existindo, os testes Inertia de 06-06 que checam ->component() passam a validar o arquivo em disco — confirmar).
- Checkpoint humano aprovado (validação visual claro/escuro/mobile).
</verification>

<success_criteria>
- Tabela de risco vigente consultável no console com municipal × sanitário separados (HU-052).
- Atualização versionada publicável pela UI com 4-olhos comunicado (HU-053/HU-020).
- Condicionantes-pergunta mantidas pela UI com efeito de reclassificação visível (HU-019).
- Navegação por permissão; dark mode e mobile validados; build verde.
</success_criteria>

<output>
Criar `.planning/phases/06-classificacao-de-risco/06-08-SUMMARY.md`: páginas criadas, componentes reusados, decisões de UX (badges por nível, preview do efeito), resultado do checkpoint humano e screenshots/anotações da validação visual.
</output>

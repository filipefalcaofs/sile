---
phase: 06-classificacao-de-risco
plan: 09
type: execute
wave: 8
depends_on: ["06-07", "06-08"]
files_modified:
  - app/Console/Commands/RiscoClassificarCommand.php
  - tests/Feature/Risco/RiscoClassificarCommandTest.php
autonomous: false
requirements: [HU-047, HU-048, HU-049, HU-050, HU-051, HU-052, HU-053, HU-019, HU-020]

must_haves:
  truths:
    - "Existe um comando que classifica um CNAE real sobre o seed oficial e imprime o resultado fundamentado (evidência de ponta a ponta)"
    - "A suíte completa roda verde após toda a fase (nenhuma regressão nos testes pré-existentes)"
    - "Os 6 critérios de pronto do ROADMAP (Fase 6) estão verificados com evidência fresca"
  artifacts:
    - path: "app/Console/Commands/RiscoClassificarCommand.php"
      provides: "Comando risco:classificar {cnae} para evidência real de classificação"
      contains: "risco:classificar"
  key_links:
    - from: "app/Console/Commands/RiscoClassificarCommand.php"
      to: "App\\Services\\Risco\\RiscoClassificationService"
      via: "classify(RiscoInput) sobre o seed real"
      pattern: "RiscoClassificationService"
---

<objective>
**Fechamento da Fase 6**: verificação integral fresca da fase, comprovação dos 6 critérios de pronto do ROADMAP com evidência, e um comando que classifica um CNAE REAL sobre o seed oficial (evidência de ponta a ponta — o motor processa dado real, não fachada). Espelha o fechamento da Fase 4 (04-08: verificação + evidência real).

Purpose: garantir a regra "nenhuma afirmação de sucesso sem evidência fresca" e entregar uma prova navegável/executável da classificação real, fechando a fase para o gsd-verifier.
Output: comando `risco:classificar {cnae}` auditado, teste do comando, e o relatório de verificação integral (suíte completa + golden cases + evidência real) no SUMMARY, com checkpoint humano.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-classificacao-de-risco/06-CONTEXT.md
@.planning/ROADMAP.md
@.planning/phases/06-classificacao-de-risco/06-05-SUMMARY.md
@.planning/phases/06-classificacao-de-risco/06-07-SUMMARY.md

# PADRÃO A ESPELHAR (comando auditado + relatório pt-BR):
@app/Console/Commands/ImportRedesimCommand.php
@app/Services/Risco/RiscoClassificationService.php
@app/Services/Risco/RiscoResult.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comando risco:classificar {cnae} (evidência real de ponta a ponta)</name>
  <read_first>app/Console/Commands/ImportRedesimCommand.php, app/Services/Risco/RiscoClassificationService.php, app/Services/Risco/RiscoInput.php</read_first>
  <files>app/Console/Commands/RiscoClassificarCommand.php, tests/Feature/Risco/RiscoClassificarCommandTest.php</files>
  <action>
    TDD. Criar `App\Console\Commands\RiscoClassificarCommand` (auto-descoberto), assinatura `risco:classificar {cnae : CNAE em dígitos ou formatado} {--resposta=* : pares chave=valor das respostas de condicionante} {--gatilho=* : codigos de gatilho ativos no contexto}`:
    - normaliza o CNAE para dígitos, monta RiscoInput, chama `app(RiscoClassificationService::class)->classify($input)`.
    - imprime relatório pt-BR: CNAE + denominação, risco municipal (nível + condicionantes gerais), risco sanitário (nível original → final, reclassificado?), encaminhamento (fluxo + dimensão decisiva + motivo + gatilhos), fundamentação legal e versões de regras aplicadas.
    - exit 0 com classificação; exit 1 se o CNAE for inválido (formato). CNAE sem regra vigente → exit 0 com resultado "segue para análise" (não é erro — é decisão real).

    Teste RiscoClassificarCommandTest (Feature, seed real RiscoMunicipalSeeder+RiscoSanitarioSeeder+RiskTriggerSeeder):
    - test_classifica_cnae_baixo_a_real_e_indica_expresso: artisan('risco:classificar', ['cnae' => '0111301'])->assertExitCode(0) + expectsOutputToContain 'expresso' / 'Baixo Risco A' / 'Decreto'.
    - test_classifica_com_gatilho_zeis_indica_analise: --gatilho=zeis_especial → saída contém 'análise'.
    - test_cnae_invalido_sai_com_erro: 'abc' → exit 1.
  </action>
  <verify>php artisan test --compact --filter=RiscoClassificarCommandTest</verify>
  <acceptance_criteria>
    - grep "risco:classificar" app/Console/Commands/RiscoClassificarCommand.php
    - test_classifica_cnae_baixo_a_real_e_indica_expresso verde (evidência sobre o seed REAL).
    - php artisan risco:classificar 0111301 (após seed dev) imprime classificação fundamentada com encaminhamento expresso.
  </acceptance_criteria>
  <done>Comando classifica um CNAE real sobre o seed oficial e imprime resultado fundamentado com encaminhamento e versões; testes verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Verificação integral fresca da fase</name>
  <read_first>.planning/ROADMAP.md, .planning/phases/06-classificacao-de-risco/06-CONTEXT.md</read_first>
  <files>tests/Feature/Risco/RiscoClassificarCommandTest.php</files>
  <action>
    Rodar e LER o output completo (evidência fresca — não "deve passar"):
    - `vendor/bin/pint --dirty --format agent` → sem pendências.
    - `php artisan test --compact` → suíte COMPLETA verde (todos os testes pré-existentes + os novos da Fase 6). Registrar o total de testes (deve ser > 448 + novos) e confirmar ZERO falhas.
    - `php artisan test --compact --filter=Risco` → todos os grupos de risco verdes.
    - `php artisan migrate:fresh --seed --env=testing` (ou equivalente) → o seed completo roda sem erro e a distribuição 767/328/236 é carregada (confirmar via tinker/contagem ou pelo RiscoSeedDistributionTest).

    Mapear cada critério de pronto do ROADMAP (Fase 6) ao teste/evidência que o comprova (tabela no SUMMARY):
    1. Dados versionados + seed 767/328/236 → RuleVersion + RiscoSeedDistributionTest.
    2. Classifica municipal × sanitário separados → RiscoClassificationServiceTest::test_classifica_..._dimensoes_separadas.
    3. Condicionante-pergunta reclassifica → golden sanitario-reclassifica-1031 + service test.
    4. Baixo/médio→expresso; alto/gatilhos→análise; ZEIS → service tests + golden cases.
    5. Tabela vigente consultável e atualizável com auditoria → RiscoConsultaTest + UI 06-08.
    6. Golden cases na suíte de regressão → RiscoGoldenCaseTest.

    Se QUALQUER item falhar, registrar como gap (não marcar a fase como pronta) — acionar correção antes do checkpoint.
  </action>
  <verify>php artisan test --compact 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - `php artisan test --compact` termina com 0 failures/0 errors (output lido e colado no SUMMARY).
    - `vendor/bin/pint --dirty --format agent` sem pendências.
    - Tabela critério→evidência preenchida no SUMMARY, todos os 6 cobertos.
  </acceptance_criteria>
  <done>Suíte completa verde com evidência fresca; os 6 critérios do ROADMAP mapeados a testes que passam.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fase 6 completa: regras versionadas (rule_versions/4-olhos), classificação municipal (Decreto 767/328/236) e sanitária (VISA) separadas, condicionante-pergunta reclassificadora, encaminhamento parametrizável + gatilhos/ZEIS, mantenedores+consulta no console, golden cases de regressão e comando de classificação real.</what-built>
  <how-to-verify>
    1. `composer run dev`.
    2. Evidência de motor real (terminal): `php artisan risco:classificar 0111301` → deve classificar Baixo Risco A municipal, encaminhamento EXPRESSO, citando o Decreto 32.636/2020 e a versão de regras. Repetir com `--gatilho=zeis_especial` → encaminhamento ANÁLISE.
    3. Console: `/gestao/login` (admin@sile.dev/password) → "Classificação de risco": tabela vigente com municipal × sanitário separados e resumo 767/328/236; publicar nova versão (4-olhos); "Condicionantes": criar/editar/remover.
    4. Confirmar a suíte: `php artisan test --compact` verde (sem regressão).
    5. Conferir que pendências SEDUR (mapa médio↔baixo_b, lista completa de gatilhos, dimensão TVL) estão PARAMETRIZADAS (tela de parâmetros risco.* e tabela de gatilhos), não bloqueando — e que NADA é simulado (CNAE sem regra → análise, nunca nível inventado).
  </how-to-verify>
  <resume-signal>Digite "aprovado" para fechar a Fase 6 ou descreva os ajustes necessários.</resume-signal>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact` — suíte completa verde (evidência fresca lida).
- `php artisan risco:classificar 0111301` imprime classificação real fundamentada (evidência de ponta a ponta sobre o CSV oficial).
- Checkpoint humano aprovado.
</verification>

<success_criteria>
- Comando classifica CNAE real sobre o seed oficial com fundamentação e encaminhamento (evidência anti-fachada).
- Suíte completa verde; nenhum teste pré-existente quebrado.
- Os 6 critérios de pronto do ROADMAP (Fase 6) comprovados com evidência fresca.
- Pendências SEDUR registradas como parametrizáveis (não bloqueiam, não simuladas).
</success_criteria>

<output>
Criar `.planning/phases/06-classificacao-de-risco/06-09-SUMMARY.md` com: a tabela critério→evidência (6 critérios), o total de testes da suíte completa (output colado), a saída do comando `risco:classificar` para 2 CNAEs reais (um expresso, um análise), o resultado do checkpoint humano, e as pendências SEDUR que seguem parametrizadas. Atualizar STATE.md/ROADMAP (status da Fase 6) é responsabilidade do orquestrador de fase ao concluir.
</output>

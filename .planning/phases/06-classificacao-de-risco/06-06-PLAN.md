---
phase: 06-classificacao-de-risco
plan: 06
type: execute
wave: 6
depends_on: ["06-05"]
files_modified:
  - database/seeders/RolesAndPermissionsSeeder.php
  - app/Http/Controllers/Gestao/RiscoController.php
  - app/Http/Controllers/Gestao/RiscoCondicionanteController.php
  - app/Http/Requests/Gestao/PublishRiscoVersionRequest.php
  - app/Http/Requests/Gestao/StoreRiscoCondicionanteRequest.php
  - app/Http/Requests/Gestao/UpdateRiscoCondicionanteRequest.php
  - app/Services/Risco/RiscoMaintenanceService.php
  - routes/gestao.php
  - tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
  - tests/Feature/Risco/RiscoConsultaTest.php
  - tests/Feature/Risco/RiscoCondicionanteMaintenanceTest.php
autonomous: true
requirements: [HU-019, HU-020, HU-052, HU-053]

must_haves:
  truths:
    - "Analista/gestor consulta a tabela de risco vigente (municipal e sanitária) por CNAE, com busca e filtro por nível"
    - "Administrador mantém condicionantes (pergunta + efeito de reclassificação) com auditoria"
    - "Atualizar a classificação publica uma NOVA versão (4-olhos), preservando a anterior — nunca edição destrutiva"
    - "Sem a permissão de risco a ação é bloqueada e auditada (CA-04)"
  artifacts:
    - path: "app/Http/Controllers/Gestao/RiscoController.php"
      provides: "Consulta da tabela vigente + publicação de nova versão"
      contains: "class RiscoController"
    - path: "app/Http/Controllers/Gestao/RiscoCondicionanteController.php"
      provides: "CRUD de condicionantes-pergunta (HU-019)"
      contains: "class RiscoCondicionanteController"
    - path: "app/Services/Risco/RiscoMaintenanceService.php"
      provides: "Publicação versionada (usa RuleVersionService) e escrita auditada das condicionantes"
      contains: "RuleVersionService"
  key_links:
    - from: "routes/gestao.php"
      to: "App\\Http\\Controllers\\Gestao\\RiscoController"
      via: "rotas sob permission:consultar-risco / manter-risco"
      pattern: "permission:consultar-risco"
    - from: "app/Services/Risco/RiscoMaintenanceService.php"
      to: "App\\Services\\Rules\\RuleVersionService"
      via: "publish 4-olhos ao atualizar a classificação"
      pattern: "publish\\("
---

<objective>
Os **mantenedores e a consulta** da tabela de risco no console SEDUR (backend): consultar a tabela de risco vigente (HU-052), atualizar de forma versionada e auditada via publicação 4-olhos (HU-053/HU-020) e manter as condicionantes-pergunta (HU-019). Reusa o padrão de CRUD da gestão (CnaeController) e o `RuleVersionService` (06-01).

Purpose: ROADMAP Fase 6, critério 5 (tabela vigente consultável e atualizável com auditoria) e HU-019/020. Anti-fachada: atualizar NÃO sobrescreve — publica nova versão (a anterior fica como histórico), espelhando a disciplina de versionamento já provada.
Output: permissões `consultar-risco`/`manter-risco`, `RiscoController` (index consulta + publish), `RiscoCondicionanteController` (CRUD), `RiscoMaintenanceService`, FormRequests, rotas, com feature tests cobrindo consulta, atualização versionada e bloqueio por permissão.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-classificacao-de-risco/06-02-SUMMARY.md
@.planning/phases/06-classificacao-de-risco/06-03-SUMMARY.md
@.planning/phases/06-classificacao-de-risco/06-05-SUMMARY.md

# PADRÃO A ESPELHAR (CRUD console + permissão + auditoria):
@app/Http/Controllers/Gestao/CnaeController.php
@routes/gestao.php
@database/seeders/RolesAndPermissionsSeeder.php
@tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
@app/Services/Rules/RuleVersionService.php
@app/Http/Requests/Gestao/StoreCnaeRequest.php
@app/Support/Settings.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Permissões consultar-risco/manter-risco + rotas da gestão</name>
  <read_first>database/seeders/RolesAndPermissionsSeeder.php, tests/Feature/Authorization/RolesAndPermissionsSeederTest.php, routes/gestao.php</read_first>
  <files>database/seeders/RolesAndPermissionsSeeder.php, tests/Feature/Authorization/RolesAndPermissionsSeederTest.php, routes/gestao.php</files>
  <action>
    TDD. Adicionar duas permissões ao RolesAndPermissionsSeeder (aditivo, givePermissionTo — nunca sync): `consultar-risco` e `manter-risco`. Atribuições espelhando CNAEs:
    - analista, gestor: `consultar-risco`.
    - administrador: `consultar-risco` + `manter-risco`.

    Atualizar RolesAndPermissionsSeederTest: a permissão sobe de 10 para 12 — corrigir `assertSame(10, Permission::query()->count())` → 12; adicionar asserções de que consultar-risco/manter-risco existem e que administrador tem manter-risco enquanto analista/gestor não.

    Adicionar as rotas em routes/gestao.php (dentro do grupo auth:gestao + lgpd.accepted, espelhando o bloco de CNAEs/território), prefixo `risco`:
    - `permission:consultar-risco`: GET `risco` → RiscoController@index (name gestao.risco.index); GET `risco/condicionantes` → RiscoCondicionanteController@index (name gestao.risco.condicionantes.index).
    - `permission:manter-risco`: PUT `risco/publicar` → RiscoController@publish (name gestao.risco.publicar); POST `risco/condicionantes` → store; PUT `risco/condicionantes/{condicionante}` → update; DELETE `risco/condicionantes/{condicionante}` → destroy.

    (As classes Controller/FormRequest entram nas Tasks 2/3 — criar rotas referenciando-as; o teste de rota completo roda após as classes existirem.)
  </action>
  <verify>php artisan test --compact --filter=RolesAndPermissionsSeederTest && php artisan route:list --path=gestao/risco</verify>
  <acceptance_criteria>
    - grep "consultar-risco" e "manter-risco" em database/seeders/RolesAndPermissionsSeeder.php
    - grep "12" tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
    - grep "permission:consultar-risco" e "permission:manter-risco" em routes/gestao.php
    - RolesAndPermissionsSeederTest verde (12 permissões).
  </acceptance_criteria>
  <done>Permissões de risco seedadas e atribuídas; rotas da gestão registradas sob as permissões corretas; teste de permissões verde (12).</done>
</task>

<task type="auto">
  <name>Task 2: RiscoController (consulta vigente HU-052) + RiscoMaintenanceService (publish versionado HU-053)</name>
  <read_first>app/Http/Controllers/Gestao/CnaeController.php, app/Services/Rules/RuleVersionService.php, app/Models/RuleVersion.php, app/Models/RiskClassification.php</read_first>
  <files>app/Http/Controllers/Gestao/RiscoController.php, app/Services/Risco/RiscoMaintenanceService.php, app/Http/Requests/Gestao/PublishRiscoVersionRequest.php, tests/Feature/Risco/RiscoConsultaTest.php</files>
  <action>
    TDD. `RiscoController@index` espelhando CnaeController@index (server-driven, paginação parametrizada — reusar um parâmetro de page size; se não houver, usar `ui.cnaes.per_page` como fallback técnico OU criar `ui.risco.per_page` apenas se necessário — preferir reuso; NÃO inflar catálogo sem necessidade):
    - Consulta a versão VIGENTE municipal (RuleVersion::vigente(RuleDomain::RiscoMunicipal)) e lista RiskClassification dessa versão com join opcional ao CNAE (formatted_code, description) — busca por código/denominação (whereLike caseSensitive:false, padrão [02-04]) e filtro por nível (risco_municipal in baixo_a/baixo_b/alto). Expor também a versão vigente sanitária e contagem por nível.
    - Render Inertia 'gestao/risco/index' com: classificacoes (paginadas), versaoMunicipal (version, valid_from), versaoSanitaria, filtros, perPageOptions, resumoNiveis ({baixo_a, baixo_b, alto}). Auditar a consulta relevante (log 'risco', event 'consulta-tabela').

    `RiscoMaintenanceService`:
    - `publishNewVersion(RuleDomain $domain, string $version, array $rows, int $authorId, int $publisherId): RuleVersion` — usa RuleVersionService.openDraft + (copia/insere as classificações no rascunho) + publish (4-olhos). Esta é a operação de HU-053/HU-020 "atualizar versionado": nasce rascunho, publica fechando a vigente anterior. (Para esta fase, o conteúdo do rascunho pode ser herdado da versão vigente + as alterações enviadas — implementar o caminho real de cópia+alteração, sem fachada.)

    `RiscoController@publish` (PublishRiscoVersionRequest): recebe a nova versão/alterações, chama o service. Como 4-olhos exige publicador ≠ autor, o endpoint usa o usuário autenticado como publisher e exige `created_by`/origem do rascunho distinta — se for o mesmo usuário e domínio sensível, retornar erro pt-BR comunicado (back()->with('error', ...)) — degradação controlada, nunca silenciosa.

    `PublishRiscoVersionRequest`: authorize via permissão (true; gate no middleware), rules para version (string, única no domínio) e payload das alterações.

    Testes RiscoConsultaTest:
    - test_analista_consulta_tabela_de_risco_vigente: seed risco; actingAs analista guard gestao; GET /gestao/risco assertOk + Inertia component 'gestao/risco/index' has classificacoes + where resumoNiveis.baixo_a 767 (após seed real) OU usar factories controladas para contagem determinística.
    - test_busca_e_filtro_por_nivel_funcionam.
    - test_consulta_e_auditada (log 'risco' event 'consulta-tabela').
    - test_sem_permissao_consultar_risco_recebe_403_auditado (espelhar TerritoryPageTest: usuário com acessar-gestao sem consultar-risco → 403 + activity_log bloqueado).
    - test_atualizar_publica_nova_versao_preservando_a_anterior (PUT /gestao/risco/publicar com manter-risco → nova RuleVersion vigente + anterior Substituida; 4-olhos respeitado).
  </action>
  <verify>php artisan test --compact --filter=RiscoConsultaTest</verify>
  <acceptance_criteria>
    - grep "RuleVersion::vigente" app/Http/Controllers/Gestao/RiscoController.php
    - grep "publishNewVersion" app/Services/Risco/RiscoMaintenanceService.php e grep "publish(" (usa RuleVersionService)
    - test_atualizar_publica_nova_versao_preservando_a_anterior verde (versionado, não destrutivo).
    - test_sem_permissao_consultar_risco_recebe_403_auditado verde.
  </acceptance_criteria>
  <done>Consulta da tabela vigente (HU-052) com busca/filtro/auditoria e atualização versionada 4-olhos (HU-053/020); bloqueio por permissão auditado; testes verdes.</done>
</task>

<task type="auto">
  <name>Task 3: RiscoCondicionanteController CRUD (HU-019) + auditoria</name>
  <read_first>app/Http/Controllers/Gestao/CnaeController.php, app/Models/RiskCondicionante.php, app/Http/Requests/Gestao/StoreCnaeRequest.php, app/Http/Requests/Gestao/UpdateCnaeRequest.php</read_first>
  <files>app/Http/Controllers/Gestao/RiscoCondicionanteController.php, app/Http/Requests/Gestao/StoreRiscoCondicionanteRequest.php, app/Http/Requests/Gestao/UpdateRiscoCondicionanteRequest.php, tests/Feature/Risco/RiscoCondicionanteMaintenanceTest.php</files>
  <action>
    TDD. `RiscoCondicionanteController` CRUD espelhando CnaeController (index Inertia 'gestao/risco/condicionantes', store/update/destroy com FormRequest, flash status pt-BR). Edita RiskCondicionante da versão sanitária vigente (escopo por rule_version_id vigente). Campos (HU-019 RN-004): pergunta (obrigatória), tipo_resposta (in booleano_sim_nao), cnae_code (opcional), regra_reclassificacao (resposta_gatilho bool + reclassifica_para in baixo/medio/alto + fundamento texto), texto_parecer. Auditoria automática via HasAuditoria (CA-02) — RiskCondicionante já usa HasAuditoria.

    FormRequests com validação dinâmica (regra_reclassificacao.reclassifica_para in:baixo,medio,alto; pergunta required string).

    Testes RiscoCondicionanteMaintenanceTest:
    - test_administrador_cria_condicionante_pergunta (POST com manter-risco → RiskCondicionante criada na versão vigente; activity_log do model registra a criação).
    - test_administrador_edita_e_remove_condicionante.
    - test_validacao_rejeita_reclassifica_para_invalido (422 / erro).
    - test_sem_permissao_manter_risco_bloqueia (usuário consultar-risco tenta POST → 403).
  </action>
  <verify>php artisan test --compact --filter=RiscoCondicionanteMaintenanceTest</verify>
  <acceptance_criteria>
    - grep "class RiscoCondicionanteController" e métodos index/store/update/destroy
    - grep "reclassifica_para" nos FormRequests (validação in:baixo,medio,alto)
    - test_administrador_cria_condicionante_pergunta e test_sem_permissao_manter_risco_bloqueia verdes.
  </acceptance_criteria>
  <done>CRUD de condicionantes-pergunta (HU-019) com validação e auditoria; bloqueio por permissão; testes verdes.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=Risco` + `--filter=RolesAndPermissionsSeederTest` verdes.
- `php artisan test --compact` — suíte completa verde.
- `php artisan route:list --path=gestao/risco` lista as rotas de consulta e manutenção sob as permissões corretas.
</verification>

<success_criteria>
- Tabela de risco vigente consultável (busca/filtro/auditoria) — HU-052.
- Atualização da classificação é versionada (nova versão, anterior preservada) com 4-olhos — HU-053/HU-020, sem fachada.
- Condicionantes mantidas como pergunta com efeito de reclassificação, auditadas — HU-019.
- Bloqueio por permissão auditado (CA-04); suíte completa verde.
</success_criteria>

<output>
Criar `.planning/phases/06-classificacao-de-risco/06-06-SUMMARY.md`: nomes de rota/controller, contrato das props Inertia de 'gestao/risco/index' e 'gestao/risco/condicionantes' (para a UI 06-08), assinatura do RiscoMaintenanceService e a decisão sobre 4-olhos na publicação pela UI.
</output>

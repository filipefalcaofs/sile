---
phase: 06-classificacao-de-risco
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - app/Enums/Fluxo.php
  - app/Enums/TipoGatilho.php
  - database/migrations/XXXX_XX_XX_XXXXXX_create_risk_triggers_table.php
  - app/Models/RiskTrigger.php
  - database/factories/RiskTriggerFactory.php
  - database/seeders/RiskTriggerSeeder.php
  - database/seeders/ParameterSeeder.php
  - config/sile.php
  - app/Services/Risco/RiscoInput.php
  - app/Services/Risco/RiscoResult.php
  - database/seeders/DatabaseSeeder.php
  - tests/Unit/Risco/RiscoDtoTest.php
  - tests/Feature/Seeders/ParameterSeederTest.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
  - tests/Feature/Risco/RiskTriggerSeederTest.php
autonomous: true
requirements: [HU-049, HU-050, HU-051]

must_haves:
  truths:
    - "O mapa risk_level→fluxo é parâmetro administrável (default baixo_a/baixo_b=expresso, alto=analise) e troca sem deploy"
    - "A dimensão decisiva do encaminhamento é parametrizável (default municipal)"
    - "Os gatilhos CNAE (semi-expresso) são tabela parametrizada, com os 3 conhecidos seedados e ativáveis"
    - "RiscoInput e RiscoResult são DTOs readonly com contrato snake_case (toArray) e versoes()"
  artifacts:
    - path: "app/Enums/Fluxo.php"
      provides: "Destino do encaminhamento (expresso, analise)"
      contains: "case Expresso"
    - path: "app/Models/RiskTrigger.php"
      provides: "Gatilho parametrizado (semi-expresso) com motivo auditável"
      contains: "class RiskTrigger"
    - path: "app/Services/Risco/RiscoResult.php"
      provides: "DTO readonly do resultado (municipal+sanitário separados, encaminhamento, fundamentação, versões)"
      contains: "final readonly class RiscoResult"
    - path: "database/seeders/ParameterSeeder.php"
      provides: "Parâmetros risco.mapa_encaminhamento e risco.dimensao_tvl"
      contains: "risco.mapa_encaminhamento"
  key_links:
    - from: "database/seeders/ParameterSeeder.php"
      to: "config/sile.php"
      via: "fallback sile.risco.* espelha o catálogo (Settings::get)"
      pattern: "'risco'"
    - from: "tests/Feature/Seeders/ParameterSeederTest.php"
      to: "tests/Feature/Seeders/DatabaseSeederTest.php"
      via: "contagem do catálogo atualizada nos DOIS testes"
      pattern: "Parameter::query\\(\\)->count\\(\\)"
---

<objective>
Tornar o **encaminhamento parametrizável** e preparar o contrato do motor (06-05): o mapa configurável `risk_level → fluxo` (HU-014, sem hardcode, sem inventar "médio" no decreto), a dimensão decisiva parametrizável (`risco.dimensao_tvl`, default municipal), a tabela de **gatilhos CNAE** (semi-expresso, motivo auditado) e os DTOs readonly `RiscoInput`/`RiscoResult` espelhando `TerritoryResult`.

Purpose: HU-049 (encaminhamento parametrizável + gatilhos), HU-050 (alto → análise via mapa) e HU-051 (exceções/gatilho ZEIS). Toda a regra de roteamento nasce como parâmetro/dado — o motor (06-05) só aplica.
Output: enums `Fluxo`/`TipoGatilho`, tabela `risk_triggers` + model + seeder (3 gatilhos conhecidos), 2 parâmetros novos no catálogo (com fallback em config/sile.php), DTOs `RiscoInput`/`RiscoResult`, e a contagem do catálogo atualizada nos DOIS testes (ParameterSeederTest + DatabaseSeederTest).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-classificacao-de-risco/06-CONTEXT.md
@.planning/phases/06-classificacao-de-risco/06-02-SUMMARY.md
@.planning/phases/06-classificacao-de-risco/06-03-SUMMARY.md

# PADRÃO A ESPELHAR:
@app/Services/Geo/TerritoryResult.php
@database/seeders/ParameterSeeder.php
@config/sile.php
@app/Support/Settings.php
@app/Models/Parameter.php
@tests/Feature/Seeders/ParameterSeederTest.php
@tests/Feature/Seeders/DatabaseSeederTest.php
@app/Enums/RiscoMunicipal.php
@app/Enums/RiscoSanitario.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enums Fluxo/TipoGatilho + tabela risk_triggers + model + seeder dos 3 gatilhos conhecidos</name>
  <read_first>app/Models/RuleVersion.php, app/Models/RiskClassification.php, database/seeders/RolesAndPermissionsSeeder.php</read_first>
  <files>app/Enums/Fluxo.php, app/Enums/TipoGatilho.php, database/migrations/XXXX_XX_XX_XXXXXX_create_risk_triggers_table.php, app/Models/RiskTrigger.php, database/factories/RiskTriggerFactory.php, database/seeders/RiskTriggerSeeder.php, database/seeders/DatabaseSeeder.php, tests/Feature/Risco/RiskTriggerSeederTest.php</files>
  <action>
    TDD. Enum `App\Enums\Fluxo` (string): `Expresso='expresso'`, `Analise='analise'`; label() pt-BR ('Fluxo expresso', 'Análise técnica'). (NÃO incluir 'semi_expresso' como fluxo — semi-expresso é a CATEGORIA do gatilho que derruba para 'analise'; o destino é sempre expresso|analise.)

    Enum `App\Enums\TipoGatilho` (string) — os 3 conhecidos (CONTEXT): `EnquadramentoAusente='enquadramento_ausente'`, `ZeisEspecial='zeis_especial'`, `DadosDoProcesso='dados_do_processo'`; label() pt-BR.

    Migration `create_risk_triggers_table` (TABULAR, parametrizada — sem hardcode no código):
    - id, `string('codigo')` (cast TipoGatilho), `string('titulo')`, `text('motivo')` (motivo auditado quando acionado), `boolean('ativo')->default(true)`, `string('categoria')->default('semi_expresso')`, timestamps, unique('codigo').

    Model `App\Models\RiskTrigger` (HasAuditoria, HasFactory, Fillable, casts codigo→TipoGatilho + ativo→bool; scope `ativos()`). Factory.

    Seeder `RiskTriggerSeeder` (upsert por codigo, ADITIVO em ativo — não sobrescreve ativo administrado, padrão ParameterSeeder/RolesAndPermissionsSeeder): semeia os 3 gatilhos com titulo/motivo pt-BR. Registrar no DatabaseSeeder.

    Teste RiskTriggerSeederTest: test_seeder_cria_os_tres_gatilhos_conhecidos (count===3, codigos esperados, todos ativo=true por default), test_seeder_e_idempotente, test_scope_ativos_filtra (desativar um → ativos()===2).
  </action>
  <verify>php artisan migrate:fresh --env=testing 2>&1 | tail -5 && php artisan test --compact --filter=RiskTriggerSeederTest</verify>
  <acceptance_criteria>
    - grep "case ZeisEspecial = 'zeis_especial'" app/Enums/TipoGatilho.php
    - grep "unique('codigo')" na migration risk_triggers
    - grep "RiskTriggerSeeder::class" database/seeders/DatabaseSeeder.php
    - test_seeder_cria_os_tres_gatilhos_conhecidos verde (3 gatilhos).
  </acceptance_criteria>
  <done>Gatilhos como tabela parametrizada (3 conhecidos seedados, ativáveis), enums Fluxo/TipoGatilho, migra em SQLite; testes verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Parâmetros risco.mapa_encaminhamento + risco.dimensao_tvl (catálogo + config + DOIS testes)</name>
  <read_first>database/seeders/ParameterSeeder.php, config/sile.php, app/Models/Parameter.php, tests/Feature/Seeders/ParameterSeederTest.php, tests/Feature/Seeders/DatabaseSeederTest.php</read_first>
  <files>database/seeders/ParameterSeeder.php, config/sile.php, tests/Feature/Seeders/ParameterSeederTest.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <action>
    TDD. Adicionar 2 parâmetros ao catálogo do `ParameterSeeder` (29 → 31). Usar grupo novo `risco`:
    - `risco.mapa_encaminhamento`: group 'risco', type 'json', default_value `'{"baixo_a":"expresso","baixo_b":"expresso","alto":"analise"}'` (JSON string), validation_rules `['required','json']`, description 'Mapa de encaminhamento por nível de risco da dimensão decisiva (expresso/analise) — Decreto 32.636/2020 não tem nível médio'.
    - `risco.dimensao_tvl`: group 'risco', type 'string', default_value 'municipal', validation_rules `['required','in:municipal,sanitario']`, description 'Dimensão de risco que prevalece no encaminhamento/TVL (default municipal; confirmar com a SEDUR)'.

    Adicionar o bloco fallback em `config/sile.php` (espelha o catálogo — Settings::get lê config no fallback):
    ```php
    'risco' => [
        'mapa_encaminhamento' => ['baixo_a' => 'expresso', 'baixo_b' => 'expresso', 'alto' => 'analise'],
        'dimensao_tvl' => 'municipal',
    ],
    ```
    (Decisão: o fallback de config é o ARRAY já decodificado; typedValue() do parâmetro json também devolve array — o consumidor sempre recebe array.)

    Atualizar `tests/Feature/Seeders/ParameterSeederTest.php`:
    - test_seeder_cria_catalogo_completo: 29 → 31, e incluir 'risco' na lista ordenada de grupos (passa a ['features','geo','integracoes','retencao','risco','seguranca','ui']).
    - test_seeder_e_idempotente: 29 → 31.
    - ADICIONAR test_seeder_registra_parametros_de_encaminhamento_de_risco: asserir mapa_encaminhamento (group risco, type json, default_value contém 'baixo_a','expresso','alto','analise'; validation ['required','json']) e dimensao_tvl (default 'municipal', validation ['required','in:municipal,sanitario']).

    Atualizar `tests/Feature/Seeders/DatabaseSeederTest.php`: trocar TODAS as asserções `assertSame(29, Parameter::query()->count())` por `assertSame(31, ...)` (test_seed_completo E test_seed_e_idempotente).
  </action>
  <verify>php artisan test --compact --filter=ParameterSeederTest && php artisan test --compact --filter=DatabaseSeederTest</verify>
  <acceptance_criteria>
    - grep "risco.mapa_encaminhamento" database/seeders/ParameterSeeder.php
    - grep "'risco'" config/sile.php (bloco com mapa_encaminhamento + dimensao_tvl)
    - grep "31" tests/Feature/Seeders/ParameterSeederTest.php E grep "31" tests/Feature/Seeders/DatabaseSeederTest.php (contagem nos DOIS testes)
    - ParameterSeederTest e DatabaseSeederTest verdes.
  </acceptance_criteria>
  <done>Dois parâmetros de encaminhamento no catálogo (31) com fallback em config/sile.php; contagem atualizada nos DOIS testes; sem "médio" inventado.</done>
</task>

<task type="auto">
  <name>Task 3: DTOs RiscoInput e RiscoResult (readonly, contrato snake_case)</name>
  <read_first>app/Services/Geo/TerritoryResult.php, app/Enums/RiscoMunicipal.php, app/Enums/RiscoSanitario.php, app/Enums/Fluxo.php</read_first>
  <files>app/Services/Risco/RiscoInput.php, app/Services/Risco/RiscoResult.php, tests/Unit/Risco/RiscoDtoTest.php</files>
  <action>
    TDD. Criar os DTOs readonly espelhando `TerritoryResult` (imutável, toArray snake_case, métodos de resumo).

    `App\Services\Risco\RiscoInput` (final readonly):
    - propriedades: `string $cnaeCode`, `array $respostasCondicionantes` (mapa pergunta/condicionante_id → resposta booleana), `array $gatilhosContexto` (lista de TipoGatilho->value ativos no contexto — ex.: zeis_especial vindo do território da Fase 4; enquadramento_ausente; dados_do_processo), `?CarbonInterface $data` (reprodução por época, default null = vigente).
    - construtor com promoção; método estático `paraCnae(string $cnae): self` (atalho sem respostas/gatilhos).

    `App\Services\Risco\RiscoResult` (final readonly) — contrato consumido pelo EP07+:
    - propriedades (todas arrays de shape estável, como TerritoryResult): `array $municipal` (status, nivel, nivel_label, condicionantes, versao_regras), `array $sanitario` (status, nivel_original, nivel_final, reclassificado(bool), condicionantes_perguntas[], versao_regras), `array $encaminhamento` (fluxo, dimensao_decisiva, motivo, gatilhos_acionados[]), `array $fundamentacao` (referências legais), `array $versoes`.
    - `status` por dimensão ∈ {classificado, nao_classificado} (nao_classificado quando o CNAE não está na tabela vigente — FA-02, segue para análise; NUNCA inventar nível).
    - métodos: `versoes(): array` (por dimensão), `toArray(): array` (snake_case, espelhar TerritoryResult::toArray), `encaminhadoParaAnalise(): bool` (fluxo === 'analise').

    Testes RiscoDtoTest:
    - test_risco_input_para_cnae_inicializa_sem_respostas_nem_gatilhos.
    - test_risco_result_to_array_em_snake_case (chaves municipal/sanitario/encaminhamento/fundamentacao/versoes; sub-chaves snake_case).
    - test_versoes_devolve_versao_por_dimensao.
    - test_encaminhado_para_analise_reflete_o_fluxo.
  </action>
  <verify>php artisan test --compact --filter=RiscoDtoTest</verify>
  <acceptance_criteria>
    - grep "final readonly class RiscoInput" e "final readonly class RiscoResult"
    - grep "public function toArray" e "public function versoes" e "encaminhadoParaAnalise" em RiscoResult.php
    - RiscoDtoTest verde (4 testes).
  </acceptance_criteria>
  <done>DTOs readonly RiscoInput/RiscoResult com contrato snake_case e métodos de resumo, espelhando TerritoryResult; testes verdes.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=Risco` + `--filter=ParameterSeederTest` + `--filter=DatabaseSeederTest` verdes.
- `php artisan test --compact` — suíte completa verde (contagem 31 propagada; a migration risk_triggers tabular não derruba nada).
</verification>

<success_criteria>
- Encaminhamento parametrizável: mapa risk_level→fluxo (default baixo_a/baixo_b=expresso, alto=analise) e dimensao_tvl (default municipal) — troca sem deploy, sem hardcode.
- Gatilhos como tabela parametrizada (3 conhecidos), ativáveis, com motivo auditável.
- DTOs RiscoInput/RiscoResult readonly com contrato estável para o motor (06-05) e o EP07.
- Contagem do catálogo (31) atualizada nos DOIS testes; suíte verde.
</success_criteria>

<output>
Criar `.planning/phases/06-classificacao-de-risco/06-04-SUMMARY.md`: chaves dos parâmetros, shape do mapa_encaminhamento, contrato EXATO de RiscoInput/RiscoResult (propriedades e toArray), e os codigos dos gatilhos. Contrato direto do motor (06-05) e da UI (06-08).
</output>

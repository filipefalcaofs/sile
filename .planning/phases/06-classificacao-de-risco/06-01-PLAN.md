---
phase: 06-classificacao-de-risco
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Enums/RuleDomain.php
  - app/Enums/RuleVersionStatus.php
  - database/migrations/XXXX_XX_XX_XXXXXX_create_rule_versions_table.php
  - app/Models/RuleVersion.php
  - database/factories/RuleVersionFactory.php
  - app/Services/Rules/RuleVersionService.php
  - app/Exceptions/FourEyesViolationException.php
  - tests/Unit/Rules/RuleVersionTest.php
  - tests/Unit/Rules/RuleVersionServiceTest.php
autonomous: true
requirements: [HU-019, HU-020, HU-053]

must_haves:
  truths:
    - "Uma versão de regra pode nascer como rascunho e ser publicada, virando vigente"
    - "Publicar uma nova versão fecha a vigente anterior do mesmo domínio sem apagá-la (histórico preservado)"
    - "A publicação por quatro olhos rejeita publicador igual ao autor do rascunho em domínios sensíveis"
    - "A versão vigente, a versão de uma data e uma versão específica de um domínio são consultáveis por scope"
    - "Toda abertura/publicação de versão é auditada com domínio, versão e versão de regras (RN-002)"
  artifacts:
    - path: "app/Enums/RuleDomain.php"
      provides: "Domínios de regra versionada (risco_municipal, risco_sanitario, condicionante)"
      contains: "enum RuleDomain"
    - path: "app/Enums/RuleVersionStatus.php"
      provides: "Status de vigência (rascunho, vigente, substituida)"
      contains: "case Rascunho"
    - path: "app/Models/RuleVersion.php"
      provides: "Cabeçalho genérico de versão com scopes vigente/naData/versao"
      contains: "scopeVigente"
    - path: "app/Services/Rules/RuleVersionService.php"
      provides: "openDraft/publish com fechamento da vigente anterior, 4-olhos e auditoria"
      contains: "public function publish"
  key_links:
    - from: "app/Services/Rules/RuleVersionService.php"
      to: "App\\Support\\Audit\\AuditService"
      via: "log('regras', 'publicacao-versao', rulesVersion)"
      pattern: "audit->log\\("
    - from: "app/Services/Rules/RuleVersionService.php"
      to: "App\\Models\\RuleVersion"
      via: "DB::transaction fecha vigente anterior e promove rascunho"
      pattern: "RuleVersionStatus::Substituida"
---

<objective>
Construir a infraestrutura genérica de **regras como dados versionados** que sustenta toda a Fase 6 (e que a Fase 5 herda): o cabeçalho de versão `rule_versions` espelhando o padrão de `geo_layers`/`GeoLayerService` (vigência, fechamento sem apagar, auditoria) + o status `rascunho` (habilita o sandbox HU-143 da Fase 5) e `published_by` (publicação por quatro olhos).

Purpose: separar a versão (cabeçalho: domínio, vigência, autoria, publicação) das tabelas tipadas por domínio (risk_classifications, risk_sanitary_classifications, risk_condicionantes — planos 06-02/06-03). Sem essa fundação, nenhum dado de risco pode ser versionado nem auditado (RN-002/RN-005 de HU-019/HU-020/HU-053).
Output: enums `RuleDomain`/`RuleVersionStatus`, migration `rule_versions`, model `RuleVersion` (scopes), `RuleVersionService` (openDraft/publish/4-olhos/auditoria), factory e testes unit verdes em SQLite.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-classificacao-de-risco/06-CONTEXT.md

# PADRÃO A ESPELHAR (versionamento da Fase 4) — ler antes de codar:
@app/Models/GeoLayer.php
@app/Services/Geo/GeoLayerService.php
@app/Enums/GeoLayerStatus.php
@app/Concerns/HasAuditoria.php
@app/Support/Audit/AuditService.php
@database/factories/GeoLayerFactory.php
@tests/Unit/Geo/GeoLayerTest.php
@tests/Unit/Geo/GeoLayerServiceTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enums RuleDomain e RuleVersionStatus</name>
  <read_first>app/Enums/GeoLayerStatus.php, app/Enums/GeoLayerType.php</read_first>
  <files>app/Enums/RuleDomain.php, app/Enums/RuleVersionStatus.php, tests/Unit/Rules/RuleVersionTest.php</files>
  <action>
    TDD (RED→GREEN→REFACTOR). Criar dois enums string espelhando o padrão de `GeoLayerStatus` (case + label()).

    `App\Enums\RuleDomain` (string):
    - `RiscoMunicipal = 'risco_municipal'`
    - `RiscoSanitario = 'risco_sanitario'`
    - `Condicionante = 'condicionante'`
    - label() em pt-BR: 'Risco municipal (Decreto 32.636/2020)', 'Risco sanitário (VISA)', 'Condicionante'.
    - Método `isSensitive(): bool` retornando true para RiscoMunicipal e RiscoSanitario (domínios com publicação 4-olhos; condicionante false por ora) — classificação do domínio, alimenta o RuleVersionService. (A Fase 5 acrescentará domínios LOUOS sem tocar a 6.)

    `App\Enums\RuleVersionStatus` (string), espelhando GeoLayerStatus + estado rascunho:
    - `Rascunho = 'rascunho'`, `Vigente = 'vigente'`, `Substituida = 'substituida'`.
    - label() pt-BR: 'Rascunho', 'Vigente', 'Substituída'.

    Comece pelo teste `tests/Unit/Rules/RuleVersionTest.php` (método test_enums_expoem_label_e_sensibilidade) asserindo values, labels e isSensitive antes de criar os enums.
  </action>
  <verify>php artisan test --compact --filter=RuleVersionTest</verify>
  <acceptance_criteria>
    - grep "case RiscoMunicipal = 'risco_municipal'" app/Enums/RuleDomain.php
    - grep "case Rascunho = 'rascunho'" app/Enums/RuleVersionStatus.php
    - grep "isSensitive" app/Enums/RuleDomain.php
    - Teste test_enums_expoem_label_e_sensibilidade passa (verde) e falhava antes (RED registrado).
  </acceptance_criteria>
  <done>Enums RuleDomain e RuleVersionStatus existem com labels pt-BR e isSensitive; teste unit verde.</done>
</task>

<task type="auto">
  <name>Task 2: Migration rule_versions + model RuleVersion + factory + scopes</name>
  <read_first>database/migrations/2026_06_13_214038_create_geo_layers_table.php, app/Models/GeoLayer.php, database/factories/GeoLayerFactory.php</read_first>
  <files>database/migrations/XXXX_XX_XX_XXXXXX_create_rule_versions_table.php, app/Models/RuleVersion.php, database/factories/RuleVersionFactory.php, tests/Unit/Rules/RuleVersionTest.php</files>
  <action>
    TDD. Criar a migration com `php artisan make:migration create_rule_versions_table --no-interaction` (timestamp automático — substituir o placeholder XXXX no files_modified).

    Schema `rule_versions` (TABULAR — sem PostGIS, roda em SQLite; NÃO pode quebrar os 448 testes atuais):
    - `id`
    - `string('domain')` (cast RuleDomain)
    - `string('version')`
    - `string('status')->default('rascunho')` (cast RuleVersionStatus)
    - `date('valid_from')->nullable()`
    - `date('valid_to')->nullable()`
    - `string('source')->nullable()`
    - `string('rules_version')->nullable()`
    - `timestamp('published_at')->nullable()`
    - `foreignId('created_by')->nullable()->constrained('users')->nullOnDelete()` (autor do rascunho)
    - `foreignId('published_by')->nullable()->constrained('users')->nullOnDelete()` (publicador — 4 olhos)
    - `timestamps()`
    - `unique(['domain', 'version'])`
    - `index(['domain', 'valid_to'])`

    Model `App\Models\RuleVersion` espelhando `GeoLayer`:
    - `use HasAuditoria; use HasFactory;`
    - `#[Fillable([...])]` com domain, version, status, valid_from, valid_to, source, rules_version, published_at, created_by, published_by.
    - casts(): domain→RuleDomain, status→RuleVersionStatus, valid_from/valid_to→'date', published_at→'datetime'.
    - scopeVigente(Builder, RuleDomain|string $domain): where domain + whereNull('valid_to') + where status vigente.
    - scopeNaData(Builder, RuleDomain|string $domain, CarbonInterface $date): valid_from <= date AND (valid_to null OR valid_to > date) — copiar a lógica EXATA de GeoLayer::scopeNaData.
    - scopeVersao(Builder, RuleDomain|string $domain, string $version): where domain + where version.
    - belongsTo created_by/published_by → User (author(), publisher()).

    Factory `RuleVersionFactory`: definition vigente (domain RiscoMunicipal, version 'decreto-32636-2020', status Vigente, valid_from subYear, valid_to null). States `rascunho()` e `substituida()`.

    Testes em RuleVersionTest: test_scope_vigente_retorna_versao_sem_valid_to, test_scope_na_data_retorna_versao_da_epoca, test_scope_versao_filtra_por_domínio_e_versao (espelhar GeoLayerTest).
  </action>
  <verify>php artisan migrate:fresh --env=testing 2>&1 | tail -5 && php artisan test --compact --filter=RuleVersionTest</verify>
  <acceptance_criteria>
    - grep "unique(\['domain', 'version'\])" na migration
    - grep "published_by" na migration
    - grep "scopeVigente" app/Models/RuleVersion.php e grep "scopeNaData" e grep "scopeVersao"
    - migrate:fresh roda sem erro em SQLite (driver da suíte)
    - Testes de scope passam.
  </acceptance_criteria>
  <done>rule_versions migra em SQLite; RuleVersion com scopes vigente/naData/versao e auditoria; factory pronta; testes verdes.</done>
</task>

<task type="auto">
  <name>Task 3: RuleVersionService (openDraft/publish/4-olhos/auditoria)</name>
  <read_first>app/Services/Geo/GeoLayerService.php, app/Support/Audit/AuditService.php</read_first>
  <files>app/Services/Rules/RuleVersionService.php, app/Exceptions/FourEyesViolationException.php, tests/Unit/Rules/RuleVersionServiceTest.php</files>
  <action>
    TDD. Criar `App\Services\Rules\RuleVersionService` espelhando `GeoLayerService::openVersion` (DB::transaction, idempotência por domain+version, fechamento da vigente anterior sem apagar):

    - construtor: `public function __construct(private AuditService $audit) {}`
    - `openDraft(RuleDomain $domain, string $version, string $source, ?int $createdBy = null): RuleVersion`
      → idempotente por (domain, version): se existir, devolve sem duplicar.
      → cria RuleVersion status Rascunho, valid_from null, valid_to null, source, rules_version=version, created_by.
      → NÃO mexe na vigente (rascunho coexiste com a vigente — base do sandbox HU-143).
    - `publish(RuleVersion $draft, ?int $publishedBy = null, ?CarbonInterface $validFrom = null): RuleVersion`
      → dentro de DB::transaction.
      → 4 OLHOS: se `$draft->domain->isSensitive()` e `$publishedBy !== null` e `$publishedBy === $draft->created_by` → lançar `FourEyesViolationException` ("A publicação por quatro olhos exige um publicador diferente do autor do rascunho.").
      → fechar a vigente anterior do MESMO domínio: update where domain + whereNull('valid_to') set status Substituida + valid_to=validFrom (default today()).
      → promover o rascunho: status Vigente, valid_from=validFrom, published_at=now(), published_by.
      → auditar via AuditService->log(logName:'regras', event:'publicacao-versao', description:"Publicação da versão {$version} do domínio {$domain->label()}", properties:[dominio,versao,substituiu(versão anterior)], subject:$draft, result:'sucesso', rulesVersion:$draft->version).
      → retornar o rascunho promovido (refresh).

    `App\Exceptions\FourEyesViolationException extends \RuntimeException`.

    Testes (espelhar GeoLayerServiceTest, SQLite):
    - test_open_draft_cria_rascunho_sem_mexer_na_vigente
    - test_open_draft_e_idempotente_por_dominio_e_versao
    - test_publish_promove_rascunho_e_fecha_vigente_anterior_sem_apagar (asserir 2 linhas, anterior Substituida com valid_to, nova Vigente)
    - test_publish_audita_com_versao_de_regras (Activity log_name 'regras', event 'publicacao-versao', rules_version)
    - test_publish_quatro_olhos_rejeita_publicador_igual_ao_autor (expectException FourEyesViolationException)
    - test_publish_permite_publicador_distinto_em_dominio_sensivel
  </action>
  <verify>php artisan test --compact --filter=RuleVersionServiceTest</verify>
  <acceptance_criteria>
    - grep "public function openDraft" e "public function publish" em app/Services/Rules/RuleVersionService.php
    - grep "FourEyesViolationException" no service
    - grep "RuleVersionStatus::Substituida" no service (fecha anterior)
    - grep "logName: 'regras'" no service
    - 6 testes do RuleVersionServiceTest verdes; o teste de 4-olhos falhava antes da regra (RED).
  </acceptance_criteria>
  <done>RuleVersionService abre rascunho, publica fechando a vigente sem apagar, aplica 4-olhos em domínio sensível e audita; testes verdes.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=Rules` (toda a pasta tests/Unit/Rules) verde.
- `php artisan test --compact` — suíte completa CONTINUA verde (a migration tabular não pode derrubar os 448 testes existentes; rodar para confirmar antes de fechar o plano).
</verification>

<success_criteria>
- rule_versions é o cabeçalho genérico de versão (domain/version/status/vigência/autoria/publicação) com unique(domain,version) e roda em SQLite.
- RuleVersion expõe scopes vigente/naData/versao (mesma disciplina do GeoLayer).
- RuleVersionService.openDraft cria rascunho; publish fecha a vigente anterior sem apagar, aplica 4-olhos em domínio sensível e audita com rules_version.
- Nenhuma dependência nova; nenhuma fachada (a publicação é operação real auditada).
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/06-classificacao-de-risco/06-01-SUMMARY.md` registrando: nomes finais de tabela/colunas, contrato do RuleVersionService (assinaturas openDraft/publish), decisão sobre 4-olhos (isSensitive por domínio) e qualquer desvio. Esse SUMMARY é insumo direto dos planos 06-02/06-03 (FK rule_version_id) e 06-06 (publicação pelos mantenedores).
</output>

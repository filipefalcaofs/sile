---
phase: 06-classificacao-de-risco
plan: 07
type: execute
wave: 6
depends_on: ["06-05"]
files_modified:
  - tests/Fixtures/golden/risco/municipal-baixo-a-expresso.json
  - tests/Fixtures/golden/risco/municipal-alto-analise.json
  - tests/Fixtures/golden/risco/sanitario-reclassifica-1031.json
  - tests/Fixtures/golden/risco/gatilho-zeis-analise.json
  - tests/Fixtures/golden/risco/cnae-sem-regra-analise.json
  - tests/Feature/Risco/RiscoGoldenCaseTest.php
  - tests/Feature/Risco/RiscoSeedDistributionTest.php
autonomous: true
requirements: [HU-047, HU-048, HU-049, HU-050, HU-051]

must_haves:
  truths:
    - "Existe uma suíte de golden cases entrada→esperado que roda o motor real sobre o seed oficial e protege contra regressão de domínio"
    - "Cada golden case é um fixture JSON declarativo, executado via #[DataProvider]"
    - "A reclassificação por condicionante (1031-7/00) está coberta como golden case"
    - "A distribuição oficial do seed (767/328/236) é verificada como regressão"
  artifacts:
    - path: "tests/Feature/Risco/RiscoGoldenCaseTest.php"
      provides: "Harness #[DataProvider] que carrega os fixtures e compara com o RiscoResult real"
      contains: "#[DataProvider"
    - path: "tests/Fixtures/golden/risco/sanitario-reclassifica-1031.json"
      provides: "Golden case real da reclassificação por condicionante-pergunta"
      contains: "1031"
  key_links:
    - from: "tests/Feature/Risco/RiscoGoldenCaseTest.php"
      to: "App\\Services\\Risco\\RiscoClassificationService"
      via: "classify(RiscoInput) comparado ao esperado do fixture"
      pattern: "classify\\("
    - from: "tests/Feature/Risco/RiscoGoldenCaseTest.php"
      to: "tests/Fixtures/golden/risco"
      via: "DataProvider lê os .json"
      pattern: "glob\\(|Fixtures/golden/risco"
---

<objective>
A **suíte de golden cases** de classificação de risco — critério 6 do ROADMAP (Fase 6) e a proteção de regressão de domínio que a Fase 5 herda. Casos entrada→esperado declarados como fixtures JSON, executados pelo motor REAL (06-05) sobre o seed OFICIAL, via `#[DataProvider]` (PHPUnit). Tabular → roda em SQLite (suíte rápida, sem PostGIS).

Purpose: cristalizar o comportamento esperado do motor em casos âncora auditáveis pela SEDUR, cobrindo as quatro situações decisivas (baixo→expresso, alto→análise, reclassificação por condicionante, gatilho/ZEIS→análise, CNAE sem regra→análise) + a distribuição oficial do seed.
Output: fixtures em `tests/Fixtures/golden/risco/*.json`, `RiscoGoldenCaseTest` (#[DataProvider]) e `RiscoSeedDistributionTest` (767/328/236), todos verdes contra o seed real.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-classificacao-de-risco/06-02-SUMMARY.md
@.planning/phases/06-classificacao-de-risco/06-03-SUMMARY.md
@.planning/phases/06-classificacao-de-risco/06-05-SUMMARY.md

# Contrato do motor e DTOs (consumidos pelos golden cases):
@app/Services/Risco/RiscoClassificationService.php
@app/Services/Risco/RiscoInput.php
@app/Services/Risco/RiscoResult.php
@database/seeders/RiscoMunicipalSeeder.php
@database/seeders/RiscoSanitarioSeeder.php
@database/seeders/RiskTriggerSeeder.php
# Referência de import por CSV (não há #[DataProvider] no repo ainda — este plano estabelece o padrão):
@app/Services/CnaeImportService.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixtures JSON dos golden cases (entrada → esperado)</name>
  <read_first>app/Services/Risco/RiscoResult.php, app/Services/Risco/RiscoInput.php, .planning/phases/06-classificacao-de-risco/06-05-SUMMARY.md</read_first>
  <files>tests/Fixtures/golden/risco/municipal-baixo-a-expresso.json, tests/Fixtures/golden/risco/municipal-alto-analise.json, tests/Fixtures/golden/risco/sanitario-reclassifica-1031.json, tests/Fixtures/golden/risco/gatilho-zeis-analise.json, tests/Fixtures/golden/risco/cnae-sem-regra-analise.json</files>
  <action>
    Criar 5 fixtures JSON declarativos. Cada arquivo tem o shape:
    ```json
    {
      "nome": "Baixo Risco A municipal segue para o expresso",
      "input": {
        "cnae_code": "0111301",
        "respostas_condicionantes": {},
        "gatilhos_contexto": [],
        "data": null
      },
      "esperado": {
        "municipal_nivel": "baixo_a",
        "sanitario_reclassificado": false,
        "fluxo": "expresso",
        "dimensao_decisiva": "municipal",
        "gatilhos_acionados": []
      }
    }
    ```
    Usar CNAEs REAIS do seed oficial (escolher do CSV do Decreto/VISA — confirmar os códigos antes de fixar):
    - municipal-baixo-a-expresso.json: CNAE baixo_a real (ex.: 0111301) → fluxo expresso.
    - municipal-alto-analise.json: CNAE alto real (escolher um 'ALTO' do CSV do Decreto) → fluxo analise.
    - sanitario-reclassifica-1031.json: cnae '1031700', respostas_condicionantes {"<id ou chave da pergunta>": true} → sanitario_reclassificado true, nivel_final alto (golden case da VISA do CONTEXT).
    - gatilho-zeis-analise.json: CNAE baixo_a real + gatilhos_contexto ["zeis_especial"] → fluxo analise, gatilhos_acionados contém zeis_especial (HU-051).
    - cnae-sem-regra-analise.json: cnae '9999999' (inexistente no seed) → municipal_status nao_classificado, fluxo analise.

    Os valores ESPERADOS devem refletir o comportamento real do motor (06-05). Onde a chave de resposta da condicionante depender do id gerado no seed, usar uma estratégia estável (ex.: o teste resolve a condicionante do CNAE pelo cnae_code e injeta a resposta) — documentar no fixture o critério.
  </action>
  <verify>ls tests/Fixtures/golden/risco/*.json | wc -l</verify>
  <acceptance_criteria>
    - 5 arquivos .json em tests/Fixtures/golden/risco/
    - grep "1031" tests/Fixtures/golden/risco/sanitario-reclassifica-1031.json
    - grep "zeis_especial" tests/Fixtures/golden/risco/gatilho-zeis-analise.json
    - Cada fixture tem chaves nome/input/esperado válidas (json válido — `php -r "json_decode(file_get_contents(\$argv[1]),true,512,JSON_THROW_ON_ERROR);" <arquivo>` não lança).
  </acceptance_criteria>
  <done>5 golden cases declarativos cobrindo expresso/análise/reclassificação/gatilho-ZEIS/CNAE-sem-regra com CNAEs reais do seed.</done>
</task>

<task type="auto">
  <name>Task 2: RiscoGoldenCaseTest com #[DataProvider] sobre o seed real</name>
  <read_first>app/Services/Risco/RiscoClassificationService.php, tests/Feature/Risco/RiscoClassificationServiceTest.php</read_first>
  <files>tests/Feature/Risco/RiscoGoldenCaseTest.php</files>
  <action>
    TDD. Criar `RiscoGoldenCaseTest` (Feature, RefreshDatabase) que:
    - no setUp roda os seeders reais: RiscoMunicipalSeeder, RiscoSanitarioSeeder, RiskTriggerSeeder (o seed oficial — golden cases batem contra o dado real).
    - método estático `goldenCases(): array` (data provider) que faz `glob(base_path('tests/Fixtures/golden/risco/*.json'))`, decodifica cada um e devolve `[nome => [dadosDoCaso]]`.
    - `#[DataProvider('goldenCases')]` no método `test_golden_case(array $caso)`: monta RiscoInput a partir de `input` (resolvendo a chave da condicionante pelo cnae_code quando necessário), chama `app(RiscoClassificationService::class)->classify($input)`, e asserta cada chave de `esperado` contra o RiscoResult (municipal.nivel, sanitario.reclassificado/nivel_final, encaminhamento.fluxo, dimensao_decisiva, gatilhos_acionados).
    - Usar atributo PHP 8 `use PHPUnit\Framework\Attributes\DataProvider;` (PHPUnit 12 do projeto).

    Garantir que o teste FALHA de forma clara se um esperado divergir (mensagem com o nome do caso) — é o critério de regressão de domínio.
  </action>
  <verify>php artisan test --compact --filter=RiscoGoldenCaseTest</verify>
  <acceptance_criteria>
    - grep "#[DataProvider('goldenCases')]" tests/Feature/Risco/RiscoGoldenCaseTest.php
    - grep "glob(" tests/Feature/Risco/RiscoGoldenCaseTest.php (lê os fixtures)
    - O test_golden_case roda 5 vezes (um por fixture) e todas verdes contra o seed real.
  </acceptance_criteria>
  <done>Harness de golden cases por #[DataProvider] roda o motor real sobre o seed oficial; 5 casos verdes; regressão de domínio protegida.</done>
</task>

<task type="auto">
  <name>Task 3: RiscoSeedDistributionTest (regressão da distribuição oficial)</name>
  <read_first>database/seeders/RiscoMunicipalSeeder.php, app/Models/RiskClassification.php, app/Enums/RiscoMunicipal.php</read_first>
  <files>tests/Feature/Risco/RiscoSeedDistributionTest.php</files>
  <action>
    TDD. `RiscoSeedDistributionTest` (Feature, RefreshDatabase) seedando RiscoMunicipalSeeder e asserindo a distribuição OFICIAL como regressão de domínio (independente do DatabaseSeederTest):
    - test_distribuicao_oficial_do_decreto: baixo_a===767, baixo_b===328, alto===236, total===1331 (RiskClassification por nível na versão vigente).
    - test_cobertura_e_unica_por_cnae_na_versao_vigente: nenhum cnae_code duplicado na versão vigente (groupBy count==1).
    Estes números são o critério de aceite de domínio — se o CSV oficial mudar (nova redação do decreto), este teste falha e força reconferência.
  </action>
  <verify>php artisan test --compact --filter=RiscoSeedDistributionTest</verify>
  <acceptance_criteria>
    - grep "767" e "328" e "236" e "1331" em tests/Feature/Risco/RiscoSeedDistributionTest.php
    - Ambos os testes verdes contra o seed real.
  </acceptance_criteria>
  <done>Distribuição oficial 767/328/236 e unicidade por CNAE travadas como regressão; testes verdes.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências (se algum arquivo PHP foi tocado).
- `php artisan test --compact --filter=RiscoGoldenCase` + `--filter=RiscoSeedDistribution` verdes.
- `php artisan test --compact` — suíte completa verde.
- Evidência de regressão de domínio: os golden cases rodam contra o SEED REAL (não fixture sintético do dado), e a distribuição 767/328/236 é assertada.
</verification>

<success_criteria>
- Suíte de golden cases entrada→esperado por #[DataProvider] sobre o seed oficial (critério 6 do ROADMAP).
- Reclassificação por condicionante (1031-7/00) coberta como golden case.
- Distribuição oficial do Decreto (767/328/236) travada como regressão.
- Padrão de golden case estabelecido para a Fase 5 herdar.
</success_criteria>

<output>
Criar `.planning/phases/06-classificacao-de-risco/06-07-SUMMARY.md`: shape final do fixture JSON, como o harness resolve a chave da condicionante, e a lista de golden cases. Padrão reutilizável pelo motor LOUOS (Fase 5).
</output>

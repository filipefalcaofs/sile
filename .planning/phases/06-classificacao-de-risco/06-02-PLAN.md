---
phase: 06-classificacao-de-risco
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/Enums/RiscoMunicipal.php
  - database/migrations/XXXX_XX_XX_XXXXXX_create_risk_classifications_table.php
  - app/Models/RiskClassification.php
  - database/factories/RiskClassificationFactory.php
  - app/Services/Risco/RiscoMunicipalImportService.php
  - database/data/risco/decreto-32636-2020-risco-municipal-unificado-cnae.csv
  - database/seeders/RiscoMunicipalSeeder.php
  - database/seeders/DatabaseSeeder.php
  - tests/Unit/Risco/RiscoMunicipalImportServiceTest.php
  - tests/Feature/Risco/RiscoMunicipalSeederTest.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
autonomous: true
requirements: [HU-020, HU-047]

must_haves:
  truths:
    - "Toda subclasse CNAE do Decreto tem uma classificação de risco municipal (baixo_a, baixo_b ou alto) versionada"
    - "O seed oficial do Decreto carrega exatamente 767 baixo_a + 328 baixo_b + 236 alto = 1.331 classificações"
    - "As condicionantes gerais por CNAE vêm parseadas do CSV (separadas por '|'), sem perda"
    - "A carga abre/publica uma versão do domínio risco_municipal e é auditada com a versão de regras"
  artifacts:
    - path: "app/Enums/RiscoMunicipal.php"
      provides: "Níveis do Decreto (baixo_a, baixo_b, alto) — NÃO existe 'médio'"
      contains: "case BaixoA"
    - path: "app/Models/RiskClassification.php"
      provides: "Classificação municipal por CNAE ligada a rule_version_id"
      contains: "class RiskClassification"
    - path: "app/Services/Risco/RiscoMunicipalImportService.php"
      provides: "Import real do CSV do Decreto com relatório de contagem"
      contains: "767"
    - path: "database/data/risco/decreto-32636-2020-risco-municipal-unificado-cnae.csv"
      provides: "CSV oficial commitado (fonte do seed)"
      min_lines: 1332
  key_links:
    - from: "database/seeders/RiscoMunicipalSeeder.php"
      to: "App\\Services\\Rules\\RuleVersionService"
      via: "openDraft + publish do domínio risco_municipal"
      pattern: "RuleVersionService"
    - from: "database/seeders/RiscoMunicipalSeeder.php"
      to: "App\\Services\\Risco\\RiscoMunicipalImportService"
      via: "import do CSV oficial"
      pattern: "RiscoMunicipalImportService"
    - from: "database/seeders/DatabaseSeeder.php"
      to: "RiscoMunicipalSeeder"
      via: "registro no pipeline de seed dev"
      pattern: "RiscoMunicipalSeeder::class"
---

<objective>
Modelar e carregar a **dimensão municipal** de risco a partir do dado oficial real: o anexo do Decreto nº 32.636/2020 (CSV com 1.331 subclasses CNAE — **767 BAIXO A + 328 BAIXO B + 236 ALTO**). A classificação fica ligada a uma versão de regra (`rule_versions`, domínio `risco_municipal`) — dado versionado, nunca código.

Purpose: entregar a base consultável e versionada de HU-020 (manter classificação de risco) e o insumo municipal de HU-047 (classificar por risco). O motor (06-05) e os mantenedores (06-06) consomem esta tabela.
Output: enum `RiscoMunicipal`, tabela `risk_classifications`, model + factory, `RiscoMunicipalImportService` (parse do CSV oficial com relatório), `RiscoMunicipalSeeder` (versão + import + auditoria) integrado ao DatabaseSeeder, com a contagem 767/328/236 assertada por teste.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-classificacao-de-risco/06-CONTEXT.md
@.planning/phases/06-classificacao-de-risco/06-01-SUMMARY.md

# PADRÃO A ESPELHAR (seed de CSV oficial + versionamento):
@app/Services/CnaeImportService.php
@database/seeders/CnaeSeeder.php
@app/Models/Cnae.php
@app/Services/Rules/RuleVersionService.php
@app/Models/RuleVersion.php
@database/seeders/DatabaseSeeder.php
@tests/Feature/Seeders/DatabaseSeederTest.php

# FONTE OFICIAL (header: cnae,descricao,condicionantes,risco_municipal_unificado):
# docs/dados-oficiais/decreto-32636-2020-risco-municipal-unificado-cnae.csv
# 1.331 linhas de dados (1.332 com header): 767 BAIXO A / 328 BAIXO B / 236 ALTO. Condicionantes separadas por ' | '.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enum RiscoMunicipal + migration risk_classifications + model + factory</name>
  <read_first>app/Models/Cnae.php, app/Models/RuleVersion.php, app/Enums/RuleDomain.php</read_first>
  <files>app/Enums/RiscoMunicipal.php, database/migrations/XXXX_XX_XX_XXXXXX_create_risk_classifications_table.php, app/Models/RiskClassification.php, database/factories/RiskClassificationFactory.php, tests/Unit/Risco/RiscoMunicipalImportServiceTest.php</files>
  <action>
    TDD. Enum `App\Enums\RiscoMunicipal` (string) — o Decreto SÓ tem três níveis, NÃO existe "médio":
    - `BaixoA = 'baixo_a'`, `BaixoB = 'baixo_b'`, `Alto = 'alto'`.
    - label() pt-BR: 'Baixo Risco A', 'Baixo Risco B', 'Alto Risco'.
    - static `fromDecreto(string $raw): self` mapeando os rótulos do CSV: 'BAIXO A'→BaixoA, 'BAIXO B'→BaixoB, 'ALTO'→Alto (trim+uppercase; valor desconhecido lança InvalidArgumentException com a string crua — NUNCA inventar nível).

    Migration `create_risk_classifications_table` (`php artisan make:migration ... --no-interaction`, TABULAR — roda em SQLite):
    - `id`
    - `foreignId('rule_version_id')->constrained('rule_versions')->cascadeOnDelete()`
    - `string('cnae_code', 7)` (dígitos, mesma normalização do Cnae.code)
    - `string('risco_municipal')` (cast RiscoMunicipal)
    - `jsonb('condicionantes')->nullable()` (lista de condicionantes gerais parseadas do CSV; usar `jsonb` como em geo_features — em SQLite vira json, ok)
    - `text('observacao')->nullable()`
    - `timestamps()`
    - `unique(['rule_version_id', 'cnae_code'])`
    - `index('cnae_code')`

    Model `App\Models\RiskClassification`:
    - `use HasAuditoria; use HasFactory;`
    - `#[Fillable(['rule_version_id','cnae_code','risco_municipal','condicionantes','observacao'])]`
    - casts: risco_municipal→RiscoMunicipal, condicionantes→'array'.
    - belongsTo RuleVersion (ruleVersion()).

    Factory `RiskClassificationFactory`: rule_version_id via RuleVersion::factory(), cnae_code numerify('#######'), risco_municipal randomElement dos 3, condicionantes [].

    Teste inicial (RED) em RiscoMunicipalImportServiceTest::test_enum_from_decreto_mapeia_rotulos_oficiais — asserir fromDecreto('BAIXO A')/('BAIXO B')/('ALTO') e exceção em 'MÉDIO'.
  </action>
  <verify>php artisan migrate:fresh --env=testing 2>&1 | tail -5 && php artisan test --compact --filter=RiscoMunicipalImportServiceTest</verify>
  <acceptance_criteria>
    - grep "case BaixoB = 'baixo_b'" app/Enums/RiscoMunicipal.php e ausência de qualquer 'medio' no enum (grep -i medio app/Enums/RiscoMunicipal.php retorna vazio)
    - grep "fromDecreto" app/Enums/RiscoMunicipal.php
    - grep "unique(\['rule_version_id', 'cnae_code'\])" na migration
    - migrate:fresh roda em SQLite sem erro.
    - Teste do enum verde; lançava antes (RED).
  </acceptance_criteria>
  <done>Enum RiscoMunicipal (3 níveis, sem "médio"), risk_classifications migra em SQLite, model+factory prontos; teste do mapeamento verde.</done>
</task>

<task type="auto">
  <name>Task 2: RiscoMunicipalImportService + CSV oficial commitado</name>
  <read_first>app/Services/CnaeImportService.php, docs/dados-oficiais/decreto-32636-2020-risco-municipal-unificado-cnae.csv</read_first>
  <files>database/data/risco/decreto-32636-2020-risco-municipal-unificado-cnae.csv, app/Services/Risco/RiscoMunicipalImportService.php, tests/Unit/Risco/RiscoMunicipalImportServiceTest.php</files>
  <action>
    TDD. Primeiro COPIAR o CSV oficial para dentro do repositório de dados (fonte reprodutível do seed, padrão database/data/):
    `mkdir -p database/data/risco && cp docs/dados-oficiais/decreto-32636-2020-risco-municipal-unificado-cnae.csv database/data/risco/decreto-32636-2020-risco-municipal-unificado-cnae.csv` (1.332 linhas com header).

    Criar `App\Services\Risco\RiscoMunicipalImportService` espelhando `CnaeImportService` (SplFileObject READ_CSV, valida header EXPECTED_HEADER=['cnae','descricao','condicionantes','risco_municipal_unificado'], upsert em chunks por (rule_version_id, cnae_code)):
    - `import(RuleVersion $version, string $csvPath): array` retornando relatório:
      `{lidos, importados, atualizados, rejeitados[], por_nivel: {baixo_a, baixo_b, alto}, total: int}`.
    - Normalizar cnae_code com `preg_replace('/\D/','',$cnae)` (mesmo padrão do CnaeImportService) e validar `/^\d{7}$/`.
    - `risco_municipal` via `RiscoMunicipal::fromDecreto($row['risco_municipal_unificado'])`.
    - `condicionantes`: explodir a coluna por '|', trim de cada item, remover prefixo '- ' e itens vazios → array (jsonb). Linha sem condicionante (string vazia ou '−') → [].
    - upsert por (rule_version_id, cnae_code) — re-import não duplica.
    - NÃO logar 1.331 activities (ruído) — a auditoria é o log explícito no seeder (Task 3).

    Testes (em SQLite, sobre o CSV REAL commitado):
    - test_import_carrega_todas_as_classificacoes_do_decreto: criar RuleVersion vigente do domínio RiscoMunicipal (factory), importar o CSV de database/data/risco/, asserir relatório total=1331 e por_nivel ['baixo_a'=>767,'baixo_b'=>328,'alto'=>236]; asserir RiskClassification::count()===1331.
    - test_import_parseia_condicionantes_gerais: para o CNAE 0111-3/01 (cnae_code '0111301') asserir 3 condicionantes parseadas (escritório / não residencial / área ≤ 1.250m²).
    - test_import_rejeita_nivel_desconhecido: feed de uma linha fixture com 'MÉDIO' → rejeitado, não inserido.
    - test_import_e_idempotente: importar 2x → 1331 (sem duplicar).
  </action>
  <verify>php artisan test --compact --filter=RiscoMunicipalImportServiceTest</verify>
  <acceptance_criteria>
    - test -f database/data/risco/decreto-32636-2020-risco-municipal-unificado-cnae.csv && wc -l < database/data/risco/...csv == 1332
    - grep "EXPECTED_HEADER" e "'risco_municipal_unificado'" em RiscoMunicipalImportService.php
    - Teste test_import_carrega_todas_as_classificacoes_do_decreto verde com asserção literal 767/328/236/1331.
    - Teste de condicionantes (0111301 → 3 itens) verde.
  </acceptance_criteria>
  <done>CSV oficial commitado; import real parseia 1.331 classificações (767/328/236) e condicionantes gerais; idempotente; testes verdes com contagem assertada.</done>
</task>

<task type="auto">
  <name>Task 3: RiscoMunicipalSeeder (versão + import + auditoria) + DatabaseSeeder</name>
  <read_first>database/seeders/CnaeSeeder.php, database/seeders/DatabaseSeeder.php, tests/Feature/Seeders/DatabaseSeederTest.php, app/Services/Rules/RuleVersionService.php</read_first>
  <files>database/seeders/RiscoMunicipalSeeder.php, database/seeders/DatabaseSeeder.php, tests/Feature/Risco/RiscoMunicipalSeederTest.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <action>
    TDD. Criar `Database\Seeders\RiscoMunicipalSeeder` espelhando `CnaeSeeder` (delega ao service + audita o relatório):
    - via `RuleVersionService`: `openDraft(RuleDomain::RiscoMunicipal, 'decreto-32636-2020', source:'Decreto Municipal nº 32.636/2020 (redação Dec. 38.673/2024)')` e `publish(...)` (idempotente — se a versão já existir/estiver vigente, reutiliza; o seeder pode `RuleVersion::vigente(RiscoMunicipal)->first() ?? openDraft+publish`).
    - `RiscoMunicipalImportService->import($version, database_path('data/risco/decreto-32636-2020-risco-municipal-unificado-cnae.csv'))`.
    - `AuditService->log(logName:'risco', event:'importacao-classificacao-municipal', description:'Importação da classificação de risco municipal (Decreto 32.636/2020)', properties:$report, rulesVersion:'decreto-32636-2020')`.

    Registrar `RiscoMunicipalSeeder::class` no `DatabaseSeeder` (após CnaeSeeder — depende dos CNAEs e do RuleVersion).

    Atualizar `tests/Feature/Seeders/DatabaseSeederTest.php` (manter os testes existentes verdes; ADICIONAR asserções, sem remover):
    - test_seed_completo: acrescentar `$this->assertSame(1331, RiskClassification::query()->count());` e asserir Activity log_name 'risco' event 'importacao-classificacao-municipal' existe, e `$this->assertSame(1, RuleVersion::vigente(RuleDomain::RiscoMunicipal)->count());`.
    - test_seed_e_idempotente: acrescentar `$this->assertSame(1331, RiskClassification::query()->count());` após 2x seed.

    Criar `tests/Feature/Risco/RiscoMunicipalSeederTest.php`:
    - test_seeder_publica_versao_vigente_e_carrega_o_decreto: seed só do RiscoMunicipalSeeder (com RolesAndPermissionsSeederPrereqs: precisa de CNAEs? não — risk_classifications.cnae_code é string solta; mas o seeder roda isolado: seedar RuleVersion via service). Asserir RuleVersion vigente do domínio + 1331 classificações + distribuição 767/328/236 (RiskClassification::where('risco_municipal','baixo_a')->count()===767 etc.).
    - test_seeder_e_idempotente: 2x → 1 versão vigente + 1331 classificações.
  </action>
  <verify>php artisan test --compact --filter=RiscoMunicipalSeederTest && php artisan test --compact --filter=DatabaseSeederTest</verify>
  <acceptance_criteria>
    - grep "RiscoMunicipalSeeder::class" database/seeders/DatabaseSeeder.php
    - grep "1331" tests/Feature/Seeders/DatabaseSeederTest.php
    - grep "logName: 'risco'" database/seeders/RiscoMunicipalSeeder.php
    - RiscoMunicipalSeederTest asserta literalmente 767/328/236 por nível.
    - DatabaseSeederTest (todos os métodos, incl. os pré-existentes) verde.
  </acceptance_criteria>
  <done>RiscoMunicipalSeeder publica a versão vigente e carrega 1.331 classificações auditadas; DatabaseSeeder integrado; DatabaseSeederTest e RiscoMunicipalSeederTest verdes com a contagem oficial.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=Risco` + `--filter=DatabaseSeederTest` verdes.
- `php artisan test --compact` — suíte completa verde (o novo seeder e a migration não podem derrubar os 448 testes; rodar para confirmar).
- Evidência da contagem oficial: o teste asserta 767/328/236=1331 sobre o CSV REAL commitado (sem fixture sintético para a contagem).
</verification>

<success_criteria>
- risk_classifications guarda a classificação municipal por CNAE ligada a uma versão de regra (dado versionado).
- O seed carrega 767 baixo_a + 328 baixo_b + 236 alto = 1.331, com condicionantes gerais parseadas e auditoria com rules_version 'decreto-32636-2020'.
- NÃO existe nível "médio" no enum municipal (regra firme do analista-negocio).
- Idempotência preservada; suíte completa verde.
</success_criteria>

<output>
Criar `.planning/phases/06-classificacao-de-risco/06-02-SUMMARY.md`: nomes finais de tabela/colunas, contrato do relatório do import (chaves por_nivel/total), shape do array `condicionantes`, e a rules_version usada ('decreto-32636-2020'). Insumo direto do motor (06-05) e dos mantenedores (06-06).
</output>

---
phase: 05-motor-louos
plan: 05
type: execute
wave: 5
depends_on: ["05-04"]
files_modified:
  - app/Services/Louos/LouosEnquadramentoService.php
  - tests/Feature/Louos/LouosConsolidacaoTest.php
autonomous: true
requirements: [HU-044, HU-045, HU-042, HU-043]

must_haves:
  truths:
    - "O motor consolida as dimensões em um parecer único: permitido / permitido_com_condicoes / nao_permitido / pendente"
    - "Sem zona (Quadro 10 indisponivel) ou sem enquadramento (Quadro 7 nao_encontrado), o consolidado é SEMPRE pendente — nunca permitido/nao_permitido sem o dado (anti-fachada)"
    - "Quadro 10 proibido consolida nao_permitido; permitido_condicionado ou restrições/condicionantes incidentes consolidam permitido_com_condicoes; senão permitido"
    - "Toda execução registra fundamentação legal real (Lei 9.148/2016 + quadros aplicados + base_legal das regras) e a versão das regras"
    - "Vagas exigidas (HU-042) são comparadas com as declaradas via parâmetro versionado; restrições especiais (HU-043) usam a camada ZEIS do território (Fase 4) — ambas alimentam condicionantes, sem bloquear sozinhas"
  artifacts:
    - path: "app/Services/Louos/LouosEnquadramentoService.php"
      provides: "Consolidação final + fundamentação + vagas + restrições ZEIS"
      contains: "consolidar"
      min_lines: 120
  key_links:
    - from: "app/Services/Louos/LouosEnquadramentoService.php"
      to: "App\\Enums\\ResultadoViabilidade"
      via: "consolida o resultado único do parecer"
      pattern: "ResultadoViabilidade::"
    - from: "app/Services/Louos/LouosEnquadramentoService.php"
      to: "App\\Support\\Settings"
      via: "lê louos.vagas.exigencia_por_grupo (sem hardcode)"
      pattern: "Settings::get\\('louos"
    - from: "app/Services/Louos/LouosEnquadramentoService.php"
      to: "App\\Services\\Geo\\TerritoryResult"
      via: "restrições especiais a partir de input.territory.restricoes (ZEIS, Fase 4)"
      pattern: "->restricoes"
---

<objective>
Fechar o motor: **consolidar** as dimensões num parecer único (HU-044), registrar a **fundamentação legal** (HU-045), aplicar **condicionantes de vagas** parametrizadas (HU-042) e **restrições especiais** via ZEIS do território (HU-043). É a regra final que substitui o consolidado provisório do 05-03.

Purpose: HU-044 (resultado consolidado), HU-045 (fundamentação legal + versão), HU-042 (vagas como dado versionado/parametrizado, alimenta a ficha sem bloquear sozinha), HU-043 (restrições especiais usando a camada ambiental ZEIS da Fase 4). Anti-fachada: o consolidado é `pendente` sempre que falta zona/enquadramento — o motor jamais declara permitido/nao_permitido sem o dado. Sem zona real, a degradação comunicada já provada em 05-04 propaga até o parecer.
Output: `consolidar()` reescrito com a lógica final + fundamentação + vagas + restrições, coberto por feature tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-motor-louos/05-CONTEXT.md
@.planning/phases/05-motor-louos/05-03-SUMMARY.md
@.planning/phases/05-motor-louos/05-04-SUMMARY.md

# PADRÃO A ESPELHAR (fundamentação + parametrização + uso do território):
@app/Services/Risco/RiscoClassificationService.php
@app/Services/Louos/LouosEnquadramentoService.php
@app/Services/Louos/EnquadramentoResult.php
@app/Services/Geo/TerritoryResult.php
@app/Enums/ResultadoViabilidade.php
@app/Enums/Quadro10Permissao.php
@app/Support/Settings.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidação do parecer (HU-044) + fundamentação legal (HU-045)</name>
  <read_first>app/Services/Louos/LouosEnquadramentoService.php, app/Services/Louos/EnquadramentoResult.php, app/Enums/ResultadoViabilidade.php, app/Services/Risco/RiscoClassificationService.php</read_first>
  <files>app/Services/Louos/LouosEnquadramentoService.php, tests/Feature/Louos/LouosConsolidacaoTest.php</files>
  <action>
    TDD estrito. Reescrever `consolidar(array $quadro7, array $quadro10, array $quadro11, array $quadro11a, EnquadramentoInput $input): array` com a lógica final (HU-044) e `buildFundamentacao(...)` (HU-045).

    PRECEDÊNCIA do consolidado (anti-fachada — o que não pode ser decidido vira pendente):
    1. Se quadro7 status === STATUS_NAO_ENCONTRADO → ResultadoViabilidade::Pendente, motivo 'Atividade sem enquadramento no Quadro 7 — segue para análise técnica'.
    2. Senão, se quadro10 status ∈ {STATUS_INDISPONIVEL, STATUS_NAO_ENCONTRADO} → Pendente, motivo do quadro10 (ex.: 'Permissão por zona pendente da base oficial (SEDUR)'). NUNCA permitido/nao_permitido sem a permissão.
    3. Senão (quadro10 identificado):
       - permissao === Quadro10Permissao::Proibido → ResultadoViabilidade::NaoPermitido, motivo 'Atividade proibida na zona pelo Quadro 10' (RN-005).
       - permissao === Quadro10Permissao::PermitidoCondicionado OU houver condicionantes (de vagas/restrições, Task 2) OU quadro11/11a identificado com condicoes → ResultadoViabilidade::PermitidoComCondicoes (RN-006).
       - senão → ResultadoViabilidade::Permitido.
    O consolidado retorna {resultado: <valor ResultadoViabilidade>, fundamentacao: [...], condicionantes: [...], motivo: <texto>}.

    `buildFundamentacao(...)` (referências legais REAIS, espelha RiscoClassificationService::buildFundamentacao):
    - Sempre que houver enquadramento: 'Lei nº 9.148/2016 (LOUOS) — Quadro 7' + a versão.
    - Quadro 10 identificado: a base_legal da permissão (se presente) e 'Lei nº 9.148/2016 — Quadro 10'.
    - Quadro 11/11A identificado: a base_legal das condições + 'Quadro 11'/'Quadro 11A'.
    - Quando pendente por degradação: incluir o motivo da pendência (sem citar regra que não foi aplicada — honesto).

    Atualizar enquadrar() para chamar o consolidar() final e incluir a fundamentação na auditoria (properties: resultado_consolidado, fundamentacao, versoes).

    Testes `tests/Feature/Louos/LouosConsolidacaoTest.php` (RefreshDatabase; factories + TerritoryResult montado):
    1. test_proibido_consolida_nao_permitido (HU-044/RN-005) — quadro7 ok + zona com permissao 'proibido' → resultado 'nao_permitido'.
    2. test_permitido_sem_condicoes_consolida_permitido — quadro7 ok + zona 'permitido' + sem condições/restrições → 'permitido'.
    3. test_permitido_condicionado_consolida_com_condicoes (RN-006) — zona 'permitido_condicionado' → 'permitido_com_condicoes'.
    4. test_sem_zona_consolida_pendente (anti-fachada central) — zona indisponivel → 'pendente', motivo de base pendente; fundamentacao NÃO cita Quadro 10.
    5. test_sem_enquadramento_consolida_pendente — quadro7 nao_encontrado → 'pendente'.
    6. test_fundamentacao_cita_louos_e_quadros_aplicados (HU-045) — caso permitido cita 'Lei nº 9.148/2016' e os quadros aplicados, com versões em versoes().
    7. test_resultado_e_auditado_com_fundamentacao_e_versao (CA-02/RN-002).
  </action>
  <verify>php artisan test --compact --filter=LouosConsolidacaoTest</verify>
  <acceptance_criteria>
    - grep "ResultadoViabilidade::NaoPermitido" e "ResultadoViabilidade::Pendente" no service
    - grep "buildFundamentacao" app/Services/Louos/LouosEnquadramentoService.php
    - grep "Lei nº 9.148/2016" app/Services/Louos/LouosEnquadramentoService.php
    - test_sem_zona_consolida_pendente passa e FALHAVA antes (RED) — prova anti-fachada do consolidado.
    - LouosConsolidacaoTest verde (testes 1-7).
  </acceptance_criteria>
  <done>Consolidação produz parecer único com precedência correta; sem zona/enquadramento sempre pendente; fundamentação legal real registrada e auditada; testes verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Condicionantes de vagas (HU-042) + restrições especiais ZEIS (HU-043)</name>
  <read_first>app/Services/Louos/LouosEnquadramentoService.php, app/Support/Settings.php, app/Services/Geo/TerritoryResult.php</read_first>
  <files>app/Services/Louos/LouosEnquadramentoService.php, tests/Feature/Louos/LouosConsolidacaoTest.php</files>
  <action>
    TDD estrito. Acrescentar dois geradores de condicionante que alimentam `condicionantes[]` do consolidado (e empurram o resultado para permitido_com_condicoes quando incidentes) — sem bloquear sozinhos (RN-005 do HU-042: alimenta a ficha HU-135).

    1. VAGAS (HU-042) — `avaliarVagas(array $quadro7, EnquadramentoInput $input): ?array`:
       - exigencia = `Settings::get('louos.vagas.exigencia_por_grupo', [])` (mapa grupo_uso → {vagas, carga_descarga, ...}); SEM hardcode.
       - Se exigencia vazia OU sem entrada para o grupo do quadro7 → retornar condicionante informativa {tipo:'vagas', conforme:null, motivo:'Exigência de vagas não parametrizada para o grupo'} (degradação honesta — não bloqueia, vai à ficha).
       - Se houver exigência: comparar com `$input->vagasDeclaradas`; conforme = declarado >= exigido em cada item. Gerar {tipo:'vagas', conforme:bool, exigido, declarado, motivo:'Imóvel Conforme/Não Conforme quanto a vagas'}. Não conforme NÃO vira nao_permitido — vira condicionante (permitido_com_condicoes), insumo da análise (HU-135).

    2. RESTRIÇÕES ESPECIAIS (HU-043) — `avaliarRestricoes(EnquadramentoInput $input): array`:
       - Ler `$input->territory?->restricoes` (shape {status, itens[], motivo, versao_camada}); itens = restrições incidentes reais da Fase 4 (ex.: ZEIS).
       - Para cada item incidente → condicionante {tipo:'restricao', nome, motivo:'Restrição territorial incidente (ex.: ZEIS) — observar condicionantes especiais'}. Restrição incidente empurra para permitido_com_condicoes (não decide sozinha; o roteamento à análise é do motor de risco, Fase 6).
       - Território null/sem restrições → [].

    3. Integrar em consolidar(): coletar condicionantes de vagas + restrições; se houver condicionante(s) e o resultado seria 'permitido' → rebaixar para 'permitido_com_condicoes'; sempre incluir as condicionantes em consolidado['condicionantes'].

    Acrescentar testes ao LouosConsolidacaoTest:
    8. test_vagas_nao_parametrizadas_geram_condicionante_informativa_sem_bloquear — exigência vazia → condicionante tipo vagas conforme null; resultado permanece permitido (ou permitido_com_condicoes só por causa da condicionante informativa? definir: condicionante informativa de "não parametrizado" NÃO rebaixa; só vagas NÃO CONFORME rebaixa). Asserir resultado 'permitido' e a condicionante presente.
    9. test_vagas_nao_conformes_geram_permitido_com_condicoes (HU-042) — parâmetro louos.vagas.exigencia_por_grupo com exigência > declarado → 'permitido_com_condicoes', condicionante conforme=false.
    10. test_restricao_zeis_incidente_gera_permitido_com_condicoes (HU-043) — TerritoryResult.restricoes com item ZEIS → 'permitido_com_condicoes', condicionante tipo restricao.
    11. test_vagas_parametrizadas_sem_deploy (HU-042/HU-014) — gravar Parameter louos.vagas.exigencia_por_grupo e provar mudança de veredito sem deploy (via Settings/cache).
  </action>
  <verify>php artisan test --compact --filter=LouosConsolidacaoTest</verify>
  <acceptance_criteria>
    - grep "Settings::get('louos.vagas.exigencia_por_grupo'" app/Services/Louos/LouosEnquadramentoService.php
    - grep "->restricoes" app/Services/Louos/LouosEnquadramentoService.php (usa a camada ZEIS da Fase 4)
    - grep "avaliarVagas" e "avaliarRestricoes" no service
    - test_vagas_nao_conformes_geram_permitido_com_condicoes e test_restricao_zeis_incidente_gera_permitido_com_condicoes verdes (falhavam antes — RED).
    - LouosConsolidacaoTest verde (todos os 11 testes).
  </acceptance_criteria>
  <done>Vagas parametrizadas (HU-042) e restrições ZEIS (HU-043) viram condicionantes que rebaixam para permitido_com_condicoes sem bloquear sozinhas; parametrização sem deploy provada; testes verdes.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=Louos` verde (todo o motor: Quadro7 + Territorio + Consolidacao).
- `php artisan test --compact` — suíte completa verde.
- Evidência anti-fachada: o teste 4 (sem zona → pendente) e o 11 (vagas parametrizadas sem deploy) provam degradação honesta e parametrização real.
</verification>

<success_criteria>
- Parecer único consolidado com precedência correta (proibido→nao_permitido; sem dado→pendente; condicionado/condicionantes→permitido_com_condicoes; senão permitido).
- Fundamentação legal real (Lei 9.148/2016 + quadros + base_legal) e versões registradas em toda execução.
- Vagas (HU-042) e restrições ZEIS (HU-043) como condicionantes parametrizadas, sem bloqueio cego.
- Motor completo: nunca declara viabilidade sem o dado da zona (anti-fachada).
</success_criteria>

<output>
Criar `.planning/phases/05-motor-louos/05-05-SUMMARY.md`: regra final de precedência do consolidado, shape definitivo de consolidado {resultado, fundamentacao, condicionantes, motivo}, contrato de vagasDeclaradas (HU-042) e de restrições (HU-043), e como o parecer fica `pendente` sem zona. Insumo do EP07 (consumo do motor), do sandbox (05-08) e dos golden cases (05-09).
</output>

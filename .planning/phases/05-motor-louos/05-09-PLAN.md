---
phase: 05-motor-louos
plan: 09
type: execute
wave: 7
depends_on: ["05-05", "05-08"]
files_modified:
  - tests/Fixtures/golden/louos/quadro7-enquadramento.json
  - tests/Fixtures/golden/louos/requer-zona-pendente.json
  - tests/Fixtures/golden/louos/proibido-na-zona.json
  - tests/Feature/Louos/LouosGoldenCaseTest.php
  - app/Console/Commands/LouosEnquadrarCommand.php
  - tests/Feature/Louos/LouosEnquadrarCommandTest.php
autonomous: false
requirements: [HU-038, HU-039, HU-044, HU-045, HU-046]

must_haves:
  truths:
    - "Existe uma suíte de golden cases (entrada→resultado esperado) executada pelo MOTOR REAL sobre o SEED REAL via #[DataProvider]"
    - "Casos que dependem de zona (requer_zona) esperam resultado 'pendente' enquanto a base não chega — degradação travada como regressão, sem fachada"
    - "O comando louos:enquadrar classifica um CNAE + área pelo motor real e imprime o parecer fundamentado (evidência de ponta a ponta)"
    - "A fase fecha com a suíte completa verde (--exclude-group postgis) e os golden cases verdes"
  artifacts:
    - path: "tests/Feature/Louos/LouosGoldenCaseTest.php"
      provides: "Regressão de domínio do motor LOUOS via #[DataProvider] sobre o seed real"
      contains: "DataProvider"
    - path: "app/Console/Commands/LouosEnquadrarCommand.php"
      provides: "Comando louos:enquadrar — evidência real do motor"
      contains: "louos:enquadrar"
  key_links:
    - from: "tests/Feature/Louos/LouosGoldenCaseTest.php"
      to: "App\\Services\\Louos\\LouosEnquadramentoService"
      via: "executa o motor real sobre os seeders oficiais"
      pattern: "LouosEnquadramentoService"
    - from: "app/Console/Commands/LouosEnquadrarCommand.php"
      to: "App\\Services\\Louos\\LouosEnquadramentoService"
      via: "enquadrar(EnquadramentoInput) e imprime parecer + fundamentação"
      pattern: "enquadrar\\("
---

<objective>
Fechar a fase com a **proteção de regressão de domínio** (golden cases — critério 7) e a **evidência real de ponta a ponta** (comando `louos:enquadrar`), seguidas da verificação integral.

Purpose: critério 7 do ROADMAP (golden cases verdes no CI a cada mudança de regra) e a entrega funcional sem fachada (o comando prova o motor real sobre o seed real). Casos `requer_zona` esperam `pendente` — travam a degradação honesta como regressão. Encerra a Fase 5 com a suíte completa verde.
Output: fixtures golden + LouosGoldenCaseTest (#[DataProvider]), comando louos:enquadrar + teste, e o checkpoint humano de verificação integral.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-motor-louos/05-CONTEXT.md
@.planning/phases/05-motor-louos/05-05-SUMMARY.md
@.planning/phases/05-motor-louos/05-02-SUMMARY.md

# PADRÃO A ESPELHAR (golden #[DataProvider] sobre seed real; comando de evidência):
@tests/Feature/Risco/RiscoGoldenCaseTest.php
@app/Console/Commands/RiscoClassificarCommand.php
@app/Services/Louos/LouosEnquadramentoService.php
@app/Services/Louos/EnquadramentoInput.php
@app/Services/Geo/TerritoryResult.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Golden cases do motor LOUOS (#[DataProvider]) — inclui casos requer_zona=pendente</name>
  <read_first>tests/Feature/Risco/RiscoGoldenCaseTest.php, app/Services/Louos/LouosEnquadramentoService.php, app/Services/Louos/EnquadramentoInput.php, app/Services/Geo/TerritoryResult.php, .planning/phases/05-motor-louos/05-02-SUMMARY.md</read_first>
  <files>tests/Fixtures/golden/louos/quadro7-enquadramento.json, tests/Fixtures/golden/louos/requer-zona-pendente.json, tests/Fixtures/golden/louos/proibido-na-zona.json, tests/Feature/Louos/LouosGoldenCaseTest.php</files>
  <action>
    TDD/regressão. Espelhar RiscoGoldenCaseTest: seed OFICIAL no setUp (LouosQuadro7Seeder + LouosQuadro10Seeder + LouosQuadro11Seeder), provider carrega tests/Fixtures/golden/louos/*.json por __DIR__ (antes do boot), executa o motor REAL e asserta cada chave de `esperado`.

    Fixtures (cada um {nome, input:{area, cnae, zona?, restricoes?, vagas_declaradas?, requer_zona?}, esperado:{quadro7_status, quadro7_grupo?, resultado, ...}}):
    1. `quadro7-enquadramento.json` — CNAE+área cobertos pelo seed real do Quadro 7 (escolher casos REAIS do CSV 05-02): asserir quadro7_status 'identificado', quadro7_grupo esperado, e resultado 'pendente' (sem zona — honesto). É o caso-âncora do enquadramento por área.
    2. `requer-zona-pendente.json` — marcado `requer_zona: true`, SEM zona no input → esperado resultado 'pendente', quadro10_status 'indisponivel'. Trava a degradação como regressão (CASO CHAVE anti-fachada).
    3. `proibido-na-zona.json` — input com zona identificada (montar TerritoryResult com zona cujo (zona×grupo) tem permissao 'proibido' no seed do Quadro 10) → esperado resultado 'nao_permitido'. Prova o caminho decidível quando há zona.

    `tests/Feature/Louos/LouosGoldenCaseTest.php`:
    - goldenCases() provider lê os *.json (glob por __DIR__, como o Risco).
    - test_golden_case(array $caso): monta EnquadramentoInput a partir de input (área, cnaePrincipal; quando há `zona`, construir um TerritoryResult com a zona como identificado e as restricoes/itens; quando `requer_zona`/sem zona, territory null ou zona indisponivel) e executa app(LouosEnquadramentoService)->enquadrar(); assertGolden por chave (quadro7_status/quadro7_grupo/quadro10_status/resultado/...), com mensagem nomeando o golden case e a chave divergente (igual ao RiscoGoldenCaseTest).
    - Documentar no topo: casos requer_zona esperam 'pendente' até a base oficial de zona chegar (decisão do 05-CONTEXT).
  </action>
  <verify>php artisan test --compact --filter=LouosGoldenCaseTest</verify>
  <acceptance_criteria>
    - grep "#\[DataProvider" tests/Feature/Louos/LouosGoldenCaseTest.php (ou use PHPUnit\Framework\Attributes\DataProvider)
    - grep "requer_zona" tests/Fixtures/golden/louos/requer-zona-pendente.json
    - grep "nao_permitido" tests/Fixtures/golden/louos/proibido-na-zona.json
    - LouosGoldenCaseTest verde com os 3+ casos; o caso requer-zona-pendente prova a degradação esperada.
  </acceptance_criteria>
  <done>Golden cases executam o motor real sobre o seed real via DataProvider; casos requer_zona travam 'pendente'; caso proibido prova o caminho decidível; verde.</done>
</task>

<task type="auto">
  <name>Task 2: Comando louos:enquadrar (evidência real do motor)</name>
  <read_first>app/Console/Commands/RiscoClassificarCommand.php, app/Services/Louos/LouosEnquadramentoService.php, app/Services/Louos/EnquadramentoInput.php, app/Services/Geo/TerritoryResult.php</read_first>
  <files>app/Console/Commands/LouosEnquadrarCommand.php, tests/Feature/Louos/LouosEnquadrarCommandTest.php</files>
  <action>
    TDD. Espelhar RiscoClassificarCommand (auto-descoberto). Signature:
    `louos:enquadrar {cnae : subclasse em dígitos ou formatada} {--area= : área ocupada em m²} {--zona= : zona urbanística hipotética (entrada explícita do operador)} {--restricao=* : restrição incidente (ex.: ZEIS)}`.

    handle(LouosEnquadramentoService $service): int
    - Normalizar cnae (7 dígitos); área obrigatória numérica (--area), erro/exit 1 se ausente/inválida.
    - Montar EnquadramentoInput: cnaePrincipal, area; se --zona informado, construir um TerritoryResult com zona identificado (nome=zona) e restricoes a partir de --restricao (itens identificados) — DEIXAR CLARO no help/saída que zona é ENTRADA EXPLÍCITA do operador (não inventada pelo sistema); sem --zona, territory null → o parecer sai 'pendente' (degradação real e honesta).
    - Chamar enquadrar() e imprimir o parecer: enquadramento (Quadro 7 grupo/subgrupo), permissão (Quadro 10 ou "indisponível — zona pendente"), condições (Quadro 11/11A), resultado consolidado (label de ResultadoViabilidade), condicionantes, fundamentação legal e versões. CNAE válido sem regra NÃO é erro (segue pendente, exit 0); formato inválido → exit 1.

    `tests/Feature/Louos/LouosEnquadrarCommandTest.php` (RefreshDatabase; seeda os Quadros):
    - test_enquadra_sem_zona_sai_pendente (artisan('louos:enquadrar', cnae do seed, --area=200) → expectsOutputToContain enquadramento + 'Pendente'; assertExitCode 0).
    - test_enquadra_com_zona_proibida_sai_nao_permitido (--zona com permissao proibido no seed → 'Não permitido').
    - test_cnae_invalido_falha (cnae malformado → exit 1).
  </action>
  <verify>php artisan test --compact --filter=LouosEnquadrarCommandTest && php artisan louos:enquadrar 0111301 --area=200 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - grep "louos:enquadrar" app/Console/Commands/LouosEnquadrarCommand.php
    - grep "enquadrar(" app/Console/Commands/LouosEnquadrarCommand.php (usa o motor real)
    - LouosEnquadrarCommandTest verde (3 testes).
    - A execução real do comando imprime parecer fundamentado (evidência de ponta a ponta).
  </acceptance_criteria>
  <done>Comando louos:enquadrar dá evidência real do motor (parecer fundamentado + versões), com degradação honesta sem zona; testes verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fechamento da Fase 5: golden cases do motor LOUOS (regressão de domínio), comando louos:enquadrar (evidência real) e verificação integral da fase.</what-built>
  <how-to-verify>
    1. Rodar a suíte completa: `php artisan test --compact` (default --exclude-group postgis no phpunit.xml) → confirmar TUDO verde (baseline 519 + os novos da Fase 5).
    2. Rodar `php artisan test --compact --filter=LouosGoldenCaseTest` → confirmar os golden cases verdes (incluindo requer-zona=pendente).
    3. Evidência real do motor: `php artisan louos:enquadrar <CNAE do seed> --area=200` (sem zona → parecer 'Pendente' com enquadramento real e fundamentação) e novamente com `--zona=<zona proibida do seed>` → 'Não permitido'. Ler o parecer impresso.
    4. Confirmar `vendor/bin/pint --dirty --format agent` limpo.
  </how-to-verify>
  <resume-signal>Digite "aprovado" com a evidência (contagem de testes verdes + saída do comando) ou descreva o que falta.</resume-signal>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=Louos` verde (motor + mantenedores + sandbox + golden + comando).
- `php artisan test --compact` — suíte COMPLETA verde (--exclude-group postgis). Evidência fresca antes de fechar a fase.
- `php artisan louos:enquadrar` produz parecer real (evidência de ponta a ponta).
</verification>

<success_criteria>
- Golden cases (#[DataProvider]) verdes sobre o seed real; casos requer_zona travam 'pendente' (anti-fachada, critério 7).
- Comando louos:enquadrar dá evidência real do motor com fundamentação e versões.
- Suíte completa verde; pint limpo.
- Fase 5 entregue de ponta a ponta, sem fachada, com a degradação sem zona explicitamente comunicada e bloqueada.
</success_criteria>

<output>
Criar `.planning/phases/05-motor-louos/05-09-SUMMARY.md`: lista de golden cases e o que cada um trava (com os casos requer_zona), assinatura do comando louos:enquadrar e exemplos de saída (evidência), contagem final da suíte, e o estado de bloqueio que permanece (zona urbanística e atributo viário LOUOS pendentes SEDUR — Quadro 10/11 degradam para pendente). Marco de conclusão da Fase 5.
</output>

---
phase: 05-motor-louos
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - database/data/louos/quadro7-faixas.csv
  - database/data/louos/quadro10-permissoes.csv
  - database/data/louos/quadro11-condicoes-via.csv
  - app/Services/Louos/LouosQuadro7ImportService.php
  - app/Services/Louos/LouosQuadro10ImportService.php
  - app/Services/Louos/LouosQuadro11ImportService.php
  - database/seeders/LouosQuadro7Seeder.php
  - database/seeders/LouosQuadro10Seeder.php
  - database/seeders/LouosQuadro11Seeder.php
  - database/seeders/DatabaseSeeder.php
  - tests/Feature/Louos/LouosQuadro7ImportServiceTest.php
  - tests/Feature/Louos/LouosSeedDistributionTest.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
autonomous: true
requirements: [HU-038, HU-039, HU-040, HU-041, HU-046]

must_haves:
  truths:
    - "O Quadro 7 é carregado como dado REAL derivado da Lei 9.148/2016 (modelo Enquadramento TVL do SAPS): CNAE → faixa de área → grupo/subgrupo, com proveniência documentada"
    - "Quadros 10 e 11/11A são carregados como dados modelados derivados da Lei (versionados), prontos para ligar quando a base de zona/atributo viário chegar"
    - "Cada Quadro publica uma versão vigente própria via RuleVersionService (reuso da Fase 6), com a importação auditada (RN-002)"
    - "O import do Quadro 7 valida faixas NÃO SOBREPOSTAS por CNAE e rejeita linha inválida (relatório), nunca insere silenciosamente"
    - "A distribuição derivada do Quadro 7 é travada por teste de regressão — se o seed mudar, o teste falha (âncora de domínio)"
  artifacts:
    - path: "app/Services/Louos/LouosQuadro7ImportService.php"
      provides: "Import CSV→faixas com validação de não-sobreposição e rejeição auditável"
      contains: "faixas"
      min_lines: 60
    - path: "database/seeders/LouosQuadro7Seeder.php"
      provides: "Publica versão vigente louos_quadro7 e importa o seed real auditado"
      contains: "RuleDomain::LouosQuadro7"
    - path: "database/data/louos/quadro7-faixas.csv"
      provides: "Seed real derivado da Lei 9.148/2016 (Quadro 7) com cabeçalho de proveniência"
      contains: "cnae"
  key_links:
    - from: "database/seeders/LouosQuadro7Seeder.php"
      to: "App\\Services\\Rules\\RuleVersionService"
      via: "openDraft + publish (reuso da Fase 6, não recria)"
      pattern: "RuleVersionService"
    - from: "app/Services/Louos/LouosQuadro7ImportService.php"
      to: "App\\Models\\LouosQuadro7Faixa"
      via: "upsert por (rule_version_id, cnae_code, area_min)"
      pattern: "upsert\\("
    - from: "database/seeders/DatabaseSeeder.php"
      to: "LouosQuadro7Seeder"
      via: "registro no pipeline de seed"
      pattern: "LouosQuadro7Seeder"
---

<objective>
Carregar os Quadros da LOUOS como **dados versionados**: o Quadro 7 REAL (derivado da Lei nº 9.148/2016, modelo "Enquadramento TVL" do SAPS) e os Quadros 10/11/11A MODELADOS (derivados da Lei, prontos para a carga oficial). Cada quadro publica uma versão vigente própria reusando o `RuleVersionService` da Fase 6.

Purpose: dá ao motor (05-03..05-05) e aos mantenedores (05-06) o dado real sobre o qual operar. Escopo honesto: Quadro 7 é entregável (enquadra por CNAE + área); Quadros 10/11/11A são modelados como dado versionado mas o motor degradará sem zona/atributo viário (planos 05-04/05-05). Sem fachada: o seed do Quadro 7 é derivado de fonte legal real com proveniência documentada e substituível pela planilha oficial da SEDUR — "muda a carga, não a lógica".
Output: 3 CSVs em database/data/louos/, 3 import services, 3 seeders publicando versões vigentes auditadas, registro no DatabaseSeeder e teste de distribuição.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-motor-louos/05-CONTEXT.md
@.planning/phases/05-motor-louos/05-01-SUMMARY.md
@docs/superpowers/specs/2026-06-14-motor-louos-design.md
@docs/legado-saps/14-enquadramento-tvl-louos-faixas-area.jpg

# PADRÃO A ESPELHAR (seed de dado oficial como versão) — ler antes de codar:
@app/Services/Risco/RiscoMunicipalImportService.php
@database/seeders/RiscoMunicipalSeeder.php
@database/seeders/DatabaseSeeder.php
@tests/Feature/Risco/RiscoSeedDistributionTest.php
@app/Models/RuleVersion.php
@app/Services/Rules/RuleVersionService.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quadro 7 REAL — CSV derivado da Lei + import com validação de faixas + seeder</name>
  <read_first>app/Services/Risco/RiscoMunicipalImportService.php, database/seeders/RiscoMunicipalSeeder.php, app/Models/LouosQuadro7Faixa.php, docs/legado-saps/14-enquadramento-tvl-louos-faixas-area.jpg</read_first>
  <files>database/data/louos/quadro7-faixas.csv, app/Services/Louos/LouosQuadro7ImportService.php, database/seeders/LouosQuadro7Seeder.php, tests/Feature/Louos/LouosQuadro7ImportServiceTest.php</files>
  <action>
    TDD estrito. O Quadro 7 enquadra CNAE + área → grupo/subgrupo de uso por FAIXAS de área (modelo do SAPS na imagem: ex. 07.01.05 → até 350m² = nR1-01; acima = nR2-01).

    1. `database/data/louos/quadro7-faixas.csv` — seed REAL DERIVADO da Lei nº 9.148/2016 (Quadro 7) + modelo Enquadramento TVL do SAPS. Cabeçalho (linha 1) com colunas exatas: `cnae,grupo,subgrupo,area_min,area_max,observacao`. Acima do cabeçalho NÃO (CSV puro); registrar a PROVENIÊNCIA no phpdoc do import e no SUMMARY (derivado da Lei 9.148/2016; substituível pela planilha oficial SEDUR). Conteúdo: subconjunto REAL e coerente (não linhas aleatórias) cobrindo CNAEs com suas faixas de área e grupos de uso da LOUOS — no mínimo ~20 CNAEs com pelo menos um CNAE de 2 faixas (ex.: comércio: [0,350]→nR1, (350,∞)→nR2) e um CNAE de faixa única "qualquer área" (area_max vazio). Códigos no formato oficial DDDD-D/SS; a normalização para dígitos é do import.

    2. `App\Services\Louos\LouosQuadro7ImportService` (espelha RiscoMunicipalImportService):
       - `import(RuleVersion $version, string $csvPath): array` retornando {lidos, importados, atualizados, rejeitados[], cnaes_distintos, total_faixas}.
       - Valida cabeçalho exato; normaliza cnae para 7 dígitos (rejeita formato inválido); area_min/area_max numéricos (area_max vazio = null = sem limite superior).
       - VALIDAÇÃO DE NÃO-SOBREPOSIÇÃO por CNAE: ordenar faixas do CNAE por area_min e rejeitar (relatório, sem inserir) quando uma faixa começa antes do fim da anterior. Faixa com area_min > area_max também é rejeitada.
       - Upsert por (rule_version_id, cnae_code, area_min) — NÃO toca 'observacao' (anotações dos mantenedores sobrevivem ao re-import). Em lote (array_chunk 500).

    3. `database/seeders/LouosQuadro7Seeder` (espelha RiscoMunicipalSeeder):
       - se não houver vigente de RuleDomain::LouosQuadro7, openDraft('lei-9148-2016-quadro7', 'Lei nº 9.148/2016 — Quadro 7 (enquadramento por área), derivado do modelo TVL/SAPS') + publish (sem 4-olhos no seed — publishedBy null).
       - import(version, database_path('data/louos/quadro7-faixas.csv')); auditar via AuditService->log(logName:'louos', event:'importacao-quadro7', properties:$report, rulesVersion:'lei-9148-2016-quadro7').

    Testes `tests/Feature/Louos/LouosQuadro7ImportServiceTest.php` (RefreshDatabase, SQLite):
    - test_importa_faixas_do_csv_real (seeda via LouosQuadro7Seeder; asserir total_faixas > 0 e LouosQuadro7Faixa::count() === total).
    - test_rejeita_faixas_sobrepostas_do_mesmo_cnae (CSV de fixture inline/temпорário com 2 faixas sobrepostas → rejeitado, não inserido).
    - test_upsert_preserva_observacao_no_reimport (gravar observacao numa faixa, reimportar, observacao persiste).
    - test_importacao_e_idempotente (rodar o seeder 2x → mesma contagem, 1 versão vigente).
  </action>
  <verify>php artisan test --compact --filter=LouosQuadro7ImportServiceTest</verify>
  <acceptance_criteria>
    - grep "cnae,grupo,subgrupo,area_min,area_max,observacao" database/data/louos/quadro7-faixas.csv
    - grep "RuleDomain::LouosQuadro7" database/seeders/LouosQuadro7Seeder.php
    - grep "sobrepost" app/Services/Louos/LouosQuadro7ImportService.php (validação de não-sobreposição)
    - grep "logName: 'louos'" database/seeders/LouosQuadro7Seeder.php
    - LouosQuadro7ImportServiceTest verde (4 testes); o teste de sobreposição falhava antes da validação (RED).
  </acceptance_criteria>
  <done>Quadro 7 carregado como versão vigente real, faixas não-sobrepostas validadas, import auditado e idempotente; testes verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Quadros 10 e 11/11A MODELADOS — CSVs + imports + seeders versionados</name>
  <read_first>app/Models/LouosQuadro10Permissao.php, app/Models/LouosQuadro11CondicaoVia.php, database/seeders/LouosQuadro7Seeder.php, app/Services/Louos/LouosQuadro7ImportService.php</read_first>
  <files>database/data/louos/quadro10-permissoes.csv, database/data/louos/quadro11-condicoes-via.csv, app/Services/Louos/LouosQuadro10ImportService.php, app/Services/Louos/LouosQuadro11ImportService.php, database/seeders/LouosQuadro10Seeder.php, database/seeders/LouosQuadro11Seeder.php</files>
  <action>
    TDD leve (os imports são upserts diretos; testar contagem e enum). Escopo honesto: dados MODELADOS derivados da Lei — versionados como dado; o motor degradará sem zona/atributo viário real (planos 05-04/05).

    1. `database/data/louos/quadro10-permissoes.csv` — cabeçalho `zona,grupo_uso,subgrupo,permissao,condicionante_ref,base_legal`. Conteúdo modelado coerente: para algumas zonas-exemplo da LOUOS (ex.: ZR-1, ZC, ZPAM) e os grupos de uso do Quadro 7, registrar permissao ∈ {permitido, permitido_condicionado, proibido} com base_legal citando a Lei 9.148/2016. Mínimo ~12 linhas cobrindo os 3 valores de permissão.

    2. `database/data/louos/quadro11-condicoes-via.csv` — cabeçalho `quadro,classe_via,grupo_uso,condicoes,base_legal` (coluna `quadro` ∈ {11, 11a} distingue os dois domínios). `condicoes` é texto JSON (ex.: {"recuo_minimo_m":5,"vagas_carga":1}). Mínimo ~8 linhas (algumas de cada quadro).

    3. `App\Services\Louos\LouosQuadro10ImportService::import(RuleVersion, csvPath): array` — upsert por (rule_version_id, zona, grupo_uso, subgrupo) em LouosQuadro10Permissao; valida permissao contra Quadro10Permissao (rejeita desconhecido); relatório {lidos, importados, atualizados, rejeitados[]}.

    4. `App\Services\Louos\LouosQuadro11ImportService::import(RuleVersion $version, string $csvPath, string $quadro): array` — importa SÓ as linhas cujo `quadro` casa com o parâmetro ('11' ou '11a'), faz json_decode de `condicoes`, upsert por (rule_version_id, classe_via, grupo_uso).

    5. `database/seeders/LouosQuadro10Seeder`: publica vigente de RuleDomain::LouosQuadro10 ('lei-9148-2016-quadro10') + import + auditoria (event 'importacao-quadro10').

    6. `database/seeders/LouosQuadro11Seeder`: publica DUAS versões vigentes — RuleDomain::LouosQuadro11 ('lei-9148-2016-quadro11') e RuleDomain::LouosQuadro11a ('lei-9148-2016-quadro11a') — usando o MESMO CSV, filtrando por `quadro` ('11' e '11a' respectivamente); auditoria (events 'importacao-quadro11' e 'importacao-quadro11a').

    Não criar arquivo de teste novo aqui — a Task 3 (DatabaseSeederTest + distribution) cobre a carga. Garantir idempotência (reuso de vigente como nos seeders de risco).
  </action>
  <verify>php artisan db:seed --class=LouosQuadro10Seeder --env=testing 2>&1 | tail -5 && php artisan db:seed --class=LouosQuadro11Seeder --env=testing 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - grep "zona,grupo_uso,subgrupo,permissao,condicionante_ref,base_legal" database/data/louos/quadro10-permissoes.csv
    - grep "RuleDomain::LouosQuadro11a" database/seeders/LouosQuadro11Seeder.php
    - grep "Quadro10Permissao" app/Services/Louos/LouosQuadro10ImportService.php (validação do enum)
    - Os dois comandos de seed rodam sem erro (exit 0) em SQLite.
  </acceptance_criteria>
  <done>Quadros 10 e 11/11A carregados como versões vigentes modeladas e versionadas; imports validam permissão/condições; seeders idempotentes.</done>
</task>

<task type="auto">
  <name>Task 3: Registrar no DatabaseSeeder + travar distribuição e contagens</name>
  <read_first>database/seeders/DatabaseSeeder.php, tests/Feature/Seeders/DatabaseSeederTest.php, tests/Feature/Risco/RiscoSeedDistributionTest.php</read_first>
  <files>database/seeders/DatabaseSeeder.php, tests/Feature/Louos/LouosSeedDistributionTest.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <action>
    1. `database/seeders/DatabaseSeeder.php` — registrar LouosQuadro7Seeder, LouosQuadro10Seeder e LouosQuadro11Seeder no pipeline `$this->call([...])`, APÓS os seeders de risco e ANTES (ou após) DevAdminSeeder/CompanySeeder — manter a ordem coerente (regras antes dos dados de dev). Não remover seeders existentes.

    2. `tests/Feature/Louos/LouosSeedDistributionTest.php` (RefreshDatabase, SQLite; espelha RiscoSeedDistributionTest — seeda só os Quadros):
       - setUp: $this->seed([LouosQuadro7Seeder::class, LouosQuadro10Seeder::class, LouosQuadro11Seeder::class]).
       - test_quadro7_tem_versao_vigente_unica_e_faixas (RuleVersion::vigente(LouosQuadro7)->count()===1; LouosQuadro7Faixa por essa versão === total esperado — usar a contagem REAL gerada pelo CSV e fixá-la como âncora com comentário "derivado da Lei 9.148/2016").
       - test_quadro7_nao_tem_faixas_sobrepostas_por_cnae (para cada CNAE da versão vigente, ordenar por area_min e asserir que cada faixa começa onde a anterior termina ou depois — regressão da não-sobreposição sobre o dado real).
       - test_quadro10_e_quadro11_e_11a_tem_versao_vigente (RuleVersion::vigente de cada domínio === 1; contagens das tabelas > 0).

    3. `tests/Feature/Seeders/DatabaseSeederTest.php` — acrescentar asserções no test_seed_completo... (e idempotente): RuleVersion::vigente(LouosQuadro7/10/11/11a)->count()===1 cada; LouosQuadro7Faixa::count() === <total âncora>; LouosQuadro10Permissao/LouosQuadro11CondicaoVia > 0; Activity log_name 'louos' event 'importacao-quadro7' existe. NÃO alterar a contagem de Parameter (já 33 do 05-01).
  </action>
  <verify>php artisan test --compact --filter="LouosSeedDistributionTest|DatabaseSeederTest"</verify>
  <acceptance_criteria>
    - grep "LouosQuadro7Seeder::class" database/seeders/DatabaseSeeder.php
    - grep "vigente(RuleDomain::LouosQuadro7)" tests/Feature/Louos/LouosSeedDistributionTest.php
    - grep "importacao-quadro7" tests/Feature/Seeders/DatabaseSeederTest.php
    - LouosSeedDistributionTest e DatabaseSeederTest verdes.
  </acceptance_criteria>
  <done>Quadros entram no seed completo; distribuição do Quadro 7 e versões vigentes dos 4 domínios travadas por teste; DatabaseSeederTest verde.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=Louos` verde.
- `php artisan test --compact` — suíte completa verde (o seed cresceu, mas é tabular; confirmar 519+novos verdes).
- Evidência anti-fachada: o seed do Quadro 7 é derivado de fonte legal real com proveniência documentada (não fixture sintético) e a não-sobreposição é validada sobre o dado real.
</verification>

<success_criteria>
- Quadro 7 carregado como versão vigente real (Lei 9.148/2016), faixas validadas, import auditado e idempotente.
- Quadros 10/11/11A carregados como versões vigentes modeladas, prontos para a carga oficial.
- Distribuição do Quadro 7 travada por teste de regressão (âncora de domínio).
- Reuso total do RuleVersionService (HU-046) — nenhuma infra de versão recriada.
</success_criteria>

<output>
Criar `.planning/phases/05-motor-louos/05-02-SUMMARY.md`: total de faixas/linhas por quadro (âncoras), os identificadores de versão publicados (lei-9148-2016-quadro7/10/11/11a), o layout final dos CSVs, a proveniência do Quadro 7 e a decisão de escopo honesto (10/11/11A modelados). Insumo de 05-03/04/05 (motor consome esses dados), 05-06 (mantenedores) e 05-09 (golden cases).
</output>

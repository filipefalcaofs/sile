---
phase: 05-motor-louos
plan: 07
type: execute
wave: 4
depends_on: ["05-06"]
files_modified:
  - resources/js/pages/gestao/louos/index.tsx
  - resources/js/layouts/gestao-layout.tsx
  - tests/Feature/Louos/LouosUiTest.php
autonomous: false
requirements: [HU-015, HU-016, HU-017, HU-018]

must_haves:
  truths:
    - "Existe uma página no console (gestão SEDUR) que lista a versão vigente dos Quadros 7/10/11/11A com a vigência e um resumo dos dados"
    - "O administrador publica uma nova versão de um Quadro pela UI, com a regra de quatro olhos comunicada (autor distinto do publicador)"
    - "A navegação do console expõe 'Quadros LOUOS' apenas para quem tem consultar-louos"
    - "A página usa os componentes padrão do console (PageHeader, Card, DataTable, Modal) e é responsiva/acessível"
  artifacts:
    - path: "resources/js/pages/gestao/louos/index.tsx"
      provides: "Tela de consulta/publicação dos Quadros da LOUOS no console"
      contains: "gestao/louos"
    - path: "resources/js/layouts/gestao-layout.tsx"
      provides: "Item de navegação 'Quadros LOUOS' gateado por permissão"
      contains: "consultar-louos"
  key_links:
    - from: "resources/js/pages/gestao/louos/index.tsx"
      to: "gestao.louos.publicar"
      via: "useForm PUT para publicar nova versão (4-olhos)"
      pattern: "louos/publicar|louos.publicar"
    - from: "resources/js/layouts/gestao-layout.tsx"
      to: "/gestao/louos"
      via: "SidebarItem visível com consultar-louos"
      pattern: "/gestao/louos"
---

<objective>
Entregar a **UI dos mantenedores dos Quadros** no console SEDUR: consulta da versão vigente dos Quadros 7/10/11/11A e publicação de nova versão por quatro olhos, navegável de ponta a ponta. Consome as props/rotas do 05-06.

Purpose: HU-015..018 (mantenedores) — a parte navegável do critério 1 do ROADMAP e do critério transversal 2 (navegável no browser). Reusa os componentes do console (padrão da Fase 2.4/6) — sem dependência nova. Roda em paralelo ao motor (arquivos disjuntos).
Output: página gestao/louos/index.tsx, item de navegação no gestao-layout, teste de componente Inertia. Fecha com checkpoint humano de validação visual.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-motor-louos/05-06-SUMMARY.md
@.planning/phases/05-motor-louos/05-01-SUMMARY.md

# PADRÃO A ESPELHAR (tela de mantenedor versionado no console):
@resources/js/pages/gestao/risco/index.tsx
@resources/js/layouts/gestao-layout.tsx
@resources/js/components/app/app-sidebar.tsx
@resources/js/components/app/page-header.tsx
@resources/js/components/ui/data-table/data-table.tsx
@resources/js/components/ui/modal.tsx
</context>

<skills>
Ativar a skill `inertia-react-development` (Inertia v3 + React 19) e `tailwindcss-development` ao construir a página. Seguir o padrão de listagem do console (PageHeader → Card → DataTable → Pagination) e o variant `console` (sidebar escura).
</skills>

<tasks>

<task type="auto">
  <name>Task 1: Página gestao/louos/index.tsx (consulta dos 4 Quadros + publicação) + navegação</name>
  <read_first>resources/js/pages/gestao/risco/index.tsx, resources/js/layouts/gestao-layout.tsx, resources/js/components/app/page-header.tsx, resources/js/components/ui/data-table/data-table.tsx, resources/js/components/ui/modal.tsx</read_first>
  <files>resources/js/pages/gestao/louos/index.tsx, resources/js/layouts/gestao-layout.tsx</files>
  <action>
    Espelhar gestao/risco/index.tsx (GestaoLayout, PageHeader, Card, DataTable, Pagination, KpiCard, Modal). Props vindas do LouosController@index (ver 05-06-SUMMARY): versão vigente por quadro (version + valid_from), resumo dos dados, listagem paginada do quadro selecionado (param `quadro`), filtros.

    1. `resources/js/pages/gestao/louos/index.tsx`:
       - Cabeçalho com PageHeader ("Quadros da LOUOS", trilha).
       - Cartões/resumo por Quadro (7/10/11/11A) mostrando versão vigente + vigência + resumo (ex.: total de faixas/permissões/classes). Quadros sem dado real (10/11/11A modelados) exibem um aviso honesto "modelado — aplica quando a base oficial chegar" (sem fingir vigência operacional plena).
       - Seletor de Quadro (tabs/Select) que troca o param `quadro` (useServerTable/router visit preservando estado, padrão do projeto).
       - DataTable do quadro selecionado com as colunas próprias (Quadro 7: cnae, grupo, subgrupo, faixa de área; Quadro 10: zona, grupo de uso, permissão (Badge), base legal; Quadro 11/11A: classe de via, condições, base legal) + busca/filtro + Pagination (meta from/to/total).
       - Botão "Publicar nova versão" (visível só com manter-louos via auth.permissions) abrindo Modal com formulário (useForm PUT gestao.louos.publicar): quadro (oculto = selecionado), version, author_id (select de autores), alteracoes. Comunicar a regra de quatro olhos no texto do modal; tratar flash.error (autor=publicador) via Alert (já no GestaoLayout).
       - Estados vazios via EmptyState; responsivo (mobile-first) e acessível (aria-sort na tabela, labels nos inputs).

    2. `resources/js/layouts/gestao-layout.tsx` — acrescentar no grupo 'Cadastros' (ou 'Visão geral') o item:
       `{ name: 'Quadros LOUOS', href: '/gestao/louos', icon: <TableIcon /> (ou ícone adequado já importado), visible: auth.permissions.includes('consultar-louos') }`.
  </action>
  <verify>npm run build 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - grep "gestao/louos/index" resources/js/pages/gestao/louos/index.tsx (componente nomeado/coerente)
    - grep "louos.publicar|louos/publicar" resources/js/pages/gestao/louos/index.tsx
    - grep "consultar-louos" resources/js/layouts/gestao-layout.tsx
    - npm run build conclui sem erro de typecheck/bundle.
  </acceptance_criteria>
  <done>Página de consulta/publicação dos Quadros existe no console com os componentes padrão; navegação gateada por consultar-louos; build verde.</done>
</task>

<task type="auto">
  <name>Task 2: Teste de componente Inertia + typecheck</name>
  <read_first>tests/Feature/Louos/LouosConsultaTest.php, resources/js/pages/gestao/louos/index.tsx</read_first>
  <files>tests/Feature/Louos/LouosUiTest.php</files>
  <action>
    TDD de integração (a página agora existe em disco — pode-se assertar o componente). `tests/Feature/Louos/LouosUiTest.php` (RefreshDatabase; seeda quadros + permissões; actingAs gestao):
    - test_pagina_de_quadros_renderiza_componente_e_props — GET /gestao/louos: assertInertia ->component('gestao/louos/index') e ->has('versoesVigentes'/'classificacoes' conforme o shape do 05-06) com where() em ao menos um campo.
    - test_admin_ve_acao_de_publicar_e_analista_nao — assertInertia where em uma prop de permissão (ex.: abilities.publish) true para admin, false para analista (ou conferir via auth.permissions compartilhado).
    Garantir que estes testes complementam (não duplicam) o LouosConsultaTest do 05-06.
  </action>
  <verify>php artisan test --compact --filter=LouosUiTest && npx tsc --noEmit 2>&1 | tail -10</verify>
  <acceptance_criteria>
    - grep "component('gestao/louos/index')" tests/Feature/Louos/LouosUiTest.php
    - LouosUiTest verde.
    - typecheck (tsc --noEmit) sem erros.
  </acceptance_criteria>
  <done>Componente da página assertado por teste Inertia; typecheck verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Tela de mantenedores dos Quadros da LOUOS no console (consulta vigente dos 4 Quadros + publicação por quatro olhos), com navegação gateada por permissão.</what-built>
  <how-to-verify>
    1. Com a app rodando (`composer run dev`), logar no console em /gestao/login (admin@sile.dev / password) e aceitar o termo se pedido.
    2. Acessar "Quadros LOUOS" na navegação → confirmar a listagem do Quadro 7 com versão vigente, faixas por CNAE, e os resumos dos Quadros 10/11/11A com o aviso honesto de "modelado".
    3. Abrir "Publicar nova versão", tentar publicar com o próprio usuário como autor → confirmar o bloqueio de quatro olhos comunicado (Alert). Publicar com autor distinto → confirmar nova versão vigente.
    4. Verificar responsividade no viewport mobile (375px) e contraste/acessibilidade básica.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os ajustes visuais necessários.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` e `npx tsc --noEmit` sem erros.
- `php artisan test --compact --filter=Louos` verde.
- `php artisan test --compact` — suíte completa verde.
- Validação visual humana (checkpoint) aprovada.
</verification>

<success_criteria>
- Console exibe a versão vigente dos 4 Quadros com vigência e resumo, navegável e responsivo.
- Publicação de nova versão pela UI com quatro olhos comunicado.
- Quadros modelados (10/11/11A) sinalizados honestamente — sem fingir operação plena.
- Navegação gateada por consultar-louos; sem dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/05-motor-louos/05-07-SUMMARY.md`: nome do componente e shape consumido, decisões de UX (aviso de quadro modelado, modal de publicação 4-olhos) e o resultado do checkpoint humano. Insumo do 05-08 (a UI do sandbox segue o mesmo padrão) e 05-09.
</output>

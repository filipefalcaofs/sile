---
phase: 05-motor-louos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Enums/RuleDomain.php
  - app/Enums/ResultadoViabilidade.php
  - app/Enums/Quadro10Permissao.php
  - database/migrations/XXXX_XX_XX_XXXXXX_create_louos_quadro7_faixas_table.php
  - database/migrations/XXXX_XX_XX_XXXXXX_create_louos_quadro10_permissoes_table.php
  - database/migrations/XXXX_XX_XX_XXXXXX_create_louos_quadro11_condicoes_via_table.php
  - app/Models/LouosQuadro7Faixa.php
  - app/Models/LouosQuadro10Permissao.php
  - app/Models/LouosQuadro11CondicaoVia.php
  - database/factories/LouosQuadro7FaixaFactory.php
  - database/factories/LouosQuadro10PermissaoFactory.php
  - database/factories/LouosQuadro11CondicaoViaFactory.php
  - app/Services/Louos/EnquadramentoInput.php
  - app/Services/Louos/EnquadramentoResult.php
  - database/seeders/ParameterSeeder.php
  - config/sile.php
  - tests/Feature/Seeders/ParameterSeederTest.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
  - tests/Unit/Louos/RuleDomainLouosTest.php
  - tests/Unit/Louos/EnquadramentoResultTest.php
  - tests/Feature/Louos/LouosQuadrosSchemaTest.php
autonomous: true
requirements: [HU-046, HU-015, HU-016, HU-017, HU-018]

must_haves:
  truths:
    - "Os quatro Quadros da LOUOS (7, 10, 11, 11A) existem como domínios de regra versionada, reusando rule_versions da Fase 6 (não recria a infra)"
    - "As tabelas tipadas louos_quadro7_faixas / louos_quadro10_permissoes / louos_quadro11_condicoes_via referenciam uma versão (rule_version_id) e rodam em SQLite (tabular, sem PostGIS)"
    - "O contrato do motor existe: EnquadramentoInput (área + CNAEs + território) e EnquadramentoResult (cada quadro como dimensão {status, dados, motivo, versao_regra} + consolidado), espelhando RiscoResult/TerritoryResult"
    - "Os domínios LOUOS são sensíveis (isSensitive=true): publicação exige quatro olhos, sem tocar o comportamento da Fase 6"
    - "O catálogo de parâmetros cobre vagas (HU-042) e amostra do sandbox (HU-143) — nada hardcoded — com a contagem atualizada nos dois testes de seeder"
  artifacts:
    - path: "app/Enums/RuleDomain.php"
      provides: "Domínios LOUOS (louos_quadro7/10/11/11a) somados aos de risco, todos isSensitive"
      contains: "LouosQuadro7"
    - path: "app/Services/Louos/EnquadramentoResult.php"
      provides: "DTO readonly de saída do motor (quadros como dimensões + consolidado + versoes + toArray)"
      contains: "final readonly class EnquadramentoResult"
    - path: "app/Services/Louos/EnquadramentoInput.php"
      provides: "DTO readonly de entrada (area, cnaePrincipal, cnaesSecundarios, territory, data, versoesOverride)"
      contains: "final readonly class EnquadramentoInput"
    - path: "app/Models/LouosQuadro7Faixa.php"
      provides: "Faixa de área do Quadro 7 ligada a rule_version_id"
      contains: "class LouosQuadro7Faixa"
  key_links:
    - from: "app/Models/LouosQuadro7Faixa.php"
      to: "App\\Models\\RuleVersion"
      via: "belongsTo ruleVersion (FK rule_version_id)"
      pattern: "belongsTo\\(RuleVersion::class"
    - from: "database/migrations/XXXX_XX_XX_XXXXXX_create_louos_quadro7_faixas_table.php"
      to: "rule_versions"
      via: "foreignId rule_version_id constrained"
      pattern: "constrained\\('rule_versions'\\)"
    - from: "database/seeders/ParameterSeeder.php"
      to: "tests/Feature/Seeders/ParameterSeederTest.php"
      via: "catálogo cresce 31→33 (vagas + sandbox) e os testes travam a contagem"
      pattern: "louos\\."
---

<objective>
Construir a **fundação do motor LOUOS**: estender a infra de regras versionadas da Fase 6 (`rule_versions`) com os quatro Quadros como domínios, criar as tabelas tipadas por domínio, os enums de resultado e os DTOs de entrada/saída do motor — o contrato sobre o qual os planos 05-02..05-09 trabalham.

Purpose: HU-046 (versionar regras da LOUOS) é a REUTILIZAÇÃO da infra `rule_versions`/`RuleVersionService` (Fase 6) — NÃO recriar. Os mantenedores (HU-015..018) e o motor (HU-038..045) precisam das tabelas tipadas e do contrato DTO antes de qualquer lógica. Sem fachada: tabelas tabulares que rodam em SQLite e não derrubam os 519 testes.
Output: enum `RuleDomain` estendido (louos_quadro7/10/11/11a, todos sensíveis), enums `ResultadoViabilidade` e `Quadro10Permissao`, 3 migrations + 3 models + 3 factories, DTOs `EnquadramentoInput`/`EnquadramentoResult`, 2 parâmetros novos (vagas + sandbox) com os dois testes de seeder atualizados.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-motor-louos/05-CONTEXT.md
@docs/superpowers/specs/2026-06-14-motor-louos-design.md

# PADRÃO A ESPELHAR (Fase 6 — versão como dado; Fase 4 — DTO de motor) — ler antes de codar:
@app/Enums/RuleDomain.php
@app/Models/RuleVersion.php
@app/Models/RiskClassification.php
@database/factories/RuleVersionFactory.php
@database/migrations/2026_06_14_014716_create_risk_classifications_table.php
@app/Services/Risco/RiscoResult.php
@app/Services/Risco/RiscoInput.php
@app/Services/Geo/TerritoryResult.php
@database/seeders/ParameterSeeder.php
@tests/Feature/Seeders/ParameterSeederTest.php
@tests/Feature/Seeders/DatabaseSeederTest.php
</context>

<nota_de_mapeamento>
ATENÇÃO (correção de mapeamento): os arquivos oficiais das HUs definem **HU-015 = Manter Quadro 7**, **HU-016 = Manter Quadro 10**, **HU-017 = Manter Quadro 11**, **HU-018 = Manter Quadro 11A** (o spec/05-CONTEXT havia invertido 015↔016). Use SEMPRE este mapeamento. Mapa motor: HU-038=Quadro 7, HU-039=Quadro 10, HU-040=Quadro 11, HU-041=Quadro 11A.
</nota_de_mapeamento>

<tasks>

<task type="auto">
  <name>Task 1: Estender RuleDomain (louos_*) + enums ResultadoViabilidade e Quadro10Permissao</name>
  <read_first>app/Enums/RuleDomain.php, app/Enums/Fluxo.php, app/Enums/RuleVersionStatus.php</read_first>
  <files>app/Enums/RuleDomain.php, app/Enums/ResultadoViabilidade.php, app/Enums/Quadro10Permissao.php, tests/Unit/Louos/RuleDomainLouosTest.php</files>
  <action>
    TDD estrito (RED → verificar RED → GREEN → verificar GREEN → REFACTOR). NÃO recriar a infra de versão — apenas ESTENDER o enum existente.

    1. `App\Enums\RuleDomain` — acrescentar 4 casos (manter os 3 de risco intactos):
       - `LouosQuadro7 = 'louos_quadro7'`
       - `LouosQuadro10 = 'louos_quadro10'`
       - `LouosQuadro11 = 'louos_quadro11'`
       - `LouosQuadro11a = 'louos_quadro11a'`
       - label() pt-BR: 'Quadro 7 da LOUOS (enquadramento por área)', 'Quadro 10 da LOUOS (permissão por zona)', 'Quadro 11 da LOUOS (condições pela via)', 'Quadro 11A da LOUOS (condições complementares pela via)'.
       - `isSensitive()`: retornar true para os 4 domínios LOUOS (publicação 4-olhos — são regras que decidem viabilidade). Os domínios de risco continuam como estão.

    2. `App\Enums\ResultadoViabilidade` (string) — resultado consolidado do parecer (HU-044):
       - `Permitido = 'permitido'`, `PermitidoComCondicoes = 'permitido_com_condicoes'`, `NaoPermitido = 'nao_permitido'`, `Pendente = 'pendente'`.
       - label() pt-BR: 'Permitido', 'Permitido com condições', 'Não permitido', 'Pendente de análise técnica'.

    3. `App\Enums\Quadro10Permissao` (string) — permissão da atividade na zona (dado do Quadro 10):
       - `Permitido = 'permitido'`, `PermitidoCondicionado = 'permitido_condicionado'`, `Proibido = 'proibido'`.
       - label() pt-BR: 'Permitido', 'Permitido condicionado', 'Proibido'.

    Escrever PRIMEIRO `tests/Unit/Louos/RuleDomainLouosTest.php`:
    - test_dominios_louos_existem_e_sao_sensiveis (asserir os 4 values, labels e isSensitive()===true para os 4).
    - test_dominios_de_risco_permanecem_inalterados (RiscoMunicipal/RiscoSanitario isSensitive true, Condicionante false).
    - test_enums_de_resultado_e_permissao_expoem_label (values + labels de ResultadoViabilidade e Quadro10Permissao).
  </action>
  <verify>php artisan test --compact --filter=RuleDomainLouosTest</verify>
  <acceptance_criteria>
    - grep "LouosQuadro7 = 'louos_quadro7'" app/Enums/RuleDomain.php
    - grep "LouosQuadro11a = 'louos_quadro11a'" app/Enums/RuleDomain.php
    - grep "case Pendente = 'pendente'" app/Enums/ResultadoViabilidade.php
    - grep "case PermitidoCondicionado = 'permitido_condicionado'" app/Enums/Quadro10Permissao.php
    - Os 3 testes do RuleDomainLouosTest passam (verde) e falhavam antes (RED).
  </acceptance_criteria>
  <done>RuleDomain tem os 4 domínios LOUOS sensíveis sem alterar os de risco; enums ResultadoViabilidade e Quadro10Permissao existem; teste unit verde.</done>
</task>

<task type="auto">
  <name>Task 2: Migrations + models + factories das 3 tabelas tipadas (tabular, SQLite)</name>
  <read_first>database/migrations/2026_06_14_014716_create_risk_classifications_table.php, database/migrations/2026_06_14_014718_create_risk_condicionantes_table.php, app/Models/RiskClassification.php, database/factories/RiskClassificationFactory.php</read_first>
  <files>database/migrations/XXXX_XX_XX_XXXXXX_create_louos_quadro7_faixas_table.php, database/migrations/XXXX_XX_XX_XXXXXX_create_louos_quadro10_permissoes_table.php, database/migrations/XXXX_XX_XX_XXXXXX_create_louos_quadro11_condicoes_via_table.php, app/Models/LouosQuadro7Faixa.php, app/Models/LouosQuadro10Permissao.php, app/Models/LouosQuadro11CondicaoVia.php, database/factories/LouosQuadro7FaixaFactory.php, database/factories/LouosQuadro10PermissaoFactory.php, database/factories/LouosQuadro11CondicaoViaFactory.php, tests/Feature/Louos/LouosQuadrosSchemaTest.php</files>
  <action>
    TDD. Criar as migrations com `php artisan make:migration` (substituir o placeholder XXXX no files_modified pelos timestamps gerados). TODAS as tabelas são TABULARES (sem PostGIS) — rodam em SQLite e NÃO podem quebrar os 519 testes. Espelhar `risk_classifications` (FK rule_version_id cascadeOnDelete, jsonb que vira json em SQLite, unique/index).

    Migration `louos_quadro7_faixas` (enquadramento por faixa de área — modelo "Enquadramento TVL" do SAPS: CNAE → faixa de área → grupo/subgrupo de uso):
    - id; foreignId('rule_version_id')->constrained('rule_versions')->cascadeOnDelete()
    - string('cnae_code', 7)  // FK lógica para Cnae.code em dígitos (mesma normalização do import de risco)
    - string('grupo')          // grupo de uso da LOUOS (ex.: nR1, nR2)
    - string('subgrupo')->nullable() // subgrupo/enquadramento (ex.: nR1-01)
    - decimal('area_min', 12, 2)->default(0)
    - decimal('area_max', 12, 2)->nullable() // null = sem limite superior ("qualquer área")
    - text('observacao')->nullable()
    - timestamps(); index(['rule_version_id', 'cnae_code'])

    Migration `louos_quadro10_permissoes` (permissão da atividade na zona):
    - id; foreignId('rule_version_id')->constrained('rule_versions')->cascadeOnDelete()
    - string('zona')
    - string('grupo_uso')       // casa com o grupo do Quadro 7
    - string('subgrupo')->nullable()
    - string('permissao')        // cast Quadro10Permissao (permitido|permitido_condicionado|proibido)
    - string('condicionante_ref')->nullable()
    - string('base_legal')->nullable()
    - text('observacao')->nullable()
    - timestamps(); index(['rule_version_id', 'zona', 'grupo_uso'])

    Migration `louos_quadro11_condicoes_via` (condições de instalação pela via — serve aos domínios louos_quadro11 E louos_quadro11a; o domínio da versão distingue 11 de 11A):
    - id; foreignId('rule_version_id')->constrained('rule_versions')->cascadeOnDelete()
    - string('classe_via')       // classificação viária LOUOS (atributo pendente SEDUR — modelado)
    - string('grupo_uso')->nullable()
    - jsonb('condicoes')->nullable()
    - string('base_legal')->nullable()
    - text('observacao')->nullable()
    - timestamps(); index(['rule_version_id', 'classe_via'])

    Models (espelhar RiskClassification): `#[Fillable([...])]`, `use HasAuditoria; use HasFactory;`, casts() e `belongsTo(RuleVersion::class)` ruleVersion().
    - `App\Models\LouosQuadro7Faixa`: casts area_min/area_max → 'decimal:2'.
    - `App\Models\LouosQuadro10Permissao`: casts permissao → Quadro10Permissao::class.
    - `App\Models\LouosQuadro11CondicaoVia`: casts condicoes → 'array'.

    Factories: definition mínima válida ligada a `RuleVersion::factory()` (ex.: LouosQuadro7FaixaFactory: cnae_code dígitos, grupo 'nR1', subgrupo 'nR1-01', area_min 0, area_max 350).

    Teste `tests/Feature/Louos/LouosQuadrosSchemaTest.php` (RefreshDatabase, SQLite):
    - test_tabelas_louos_migram_em_sqlite (criar uma RuleVersion::factory()->create() e uma linha de cada tabela via factory; asserir persistência e o belongsTo ruleVersion).
    - test_permissao_do_quadro10_e_castada_para_enum (asserir instanceof Quadro10Permissao).
    - test_condicoes_do_quadro11_sao_array (asserir is_array no condicoes).
  </action>
  <verify>php artisan migrate:fresh --env=testing 2>&1 | tail -5 && php artisan test --compact --filter=LouosQuadrosSchemaTest</verify>
  <acceptance_criteria>
    - grep "constrained('rule_versions')" em cada uma das 3 migrations
    - grep "decimal('area_max', 12, 2)->nullable()" na migration do quadro7
    - grep "belongsTo(RuleVersion::class" em cada um dos 3 models
    - migrate:fresh roda sem erro em SQLite
    - LouosQuadrosSchemaTest verde (3 testes).
  </acceptance_criteria>
  <done>3 tabelas tipadas migram em SQLite com FK rule_version_id; models com casts e ruleVersion(); factories prontas; schema test verde.</done>
</task>

<task type="auto">
  <name>Task 3: DTOs EnquadramentoInput/Result + parâmetros (vagas, sandbox) + contagem nos dois seeders</name>
  <read_first>app/Services/Risco/RiscoResult.php, app/Services/Risco/RiscoInput.php, app/Services/Geo/TerritoryResult.php, database/seeders/ParameterSeeder.php, tests/Feature/Seeders/ParameterSeederTest.php, tests/Feature/Seeders/DatabaseSeederTest.php</read_first>
  <files>app/Services/Louos/EnquadramentoInput.php, app/Services/Louos/EnquadramentoResult.php, database/seeders/ParameterSeeder.php, config/sile.php, tests/Feature/Seeders/ParameterSeederTest.php, tests/Feature/Seeders/DatabaseSeederTest.php, tests/Unit/Louos/EnquadramentoResultTest.php</files>
  <action>
    TDD para os DTOs; os parâmetros são travados pelos testes de seeder (atualizar a contagem).

    1. `App\Services\Louos\EnquadramentoInput` (final readonly, espelha RiscoInput) — campos:
       - `float $area`
       - `string $cnaePrincipal`
       - `array $cnaesSecundarios = []` (list<string>)
       - `?\App\Services\Geo\TerritoryResult $territory = null` (zona/via vêm daqui; null = território não fornecido)
       - `array $vagasDeclaradas = []` (vagas/carga/descarga declaradas pelo requerente — HU-042)
       - `?\Carbon\CarbonInterface $data = null` (reprodução por época)
       - `array $versoesOverride = []` (map domínio→version; modo sandbox, resolução por versão específica)
       - phpdoc completo das shapes; helper estático `paraConsulta(float $area, string $cnae, ?TerritoryResult $territory = null): self`.

    2. `App\Services\Louos\EnquadramentoResult` (final readonly, espelha RiscoResult/TerritoryResult) — construtor com arrays:
       - `array $quadro7, array $quadro10, array $quadro11, array $quadro11a, array $consolidado` + `array $versoes` (privado).
       - Constantes de status de dimensão: `STATUS_IDENTIFICADO = 'identificado'`, `STATUS_NAO_ENCONTRADO = 'nao_encontrado'`, `STATUS_INDISPONIVEL = 'indisponivel'`.
       - `versoes(): array<string,?string>` (quadro7/10/11/11a).
       - `resultado(): string` → `$this->consolidado['resultado']` (valor de ResultadoViabilidade).
       - `pendente(): bool` → resultado === ResultadoViabilidade::Pendente->value.
       - `toArray(): array` snake_case com quadro7/quadro10/quadro11/quadro11a/consolidado/versoes.
       Documentar: cada quadro é dimensão {status, ...dados, motivo, versao_regra}; consolidado {resultado, fundamentacao[], condicionantes[], motivo}.

    3. `database/seeders/ParameterSeeder.php` — acrescentar ao catálogo (grupo NOVO `louos`):
       - `louos.vagas.exigencia_por_grupo`: group 'louos', type 'json', default_value '{}', validation_rules ['required','json'], description 'Exigência de vagas (estacionamento, carga/descarga) por grupo de uso da LOUOS — base do veredito de conformidade (HU-042); vazio enquanto a SEDUR não parametriza (motor registra "não parametrizado", não bloqueia)'.
       - `louos.sandbox.amostra_padrao`: group 'louos', type 'integer', default_value '50', validation_rules ['required','integer','min:1','max:1000'], description 'Tamanho padrão da amostra de cenários reprocessados na simulação de impacto de regra (HU-143)'.

    4. `config/sile.php` — acrescentar fallbacks (Settings lê config("sile.{$key}")): seção `louos` com `vagas.exigencia_por_grupo => []` e `sandbox.amostra_padrao => 50` (mesma disciplina das seções risco/geo já existentes).

    5. Atualizar `tests/Feature/Seeders/ParameterSeederTest.php`:
       - trocar a contagem 31 → 33 (em test_seeder_cria_catalogo_completo E test_seeder_e_idempotente).
       - trocar a lista de grupos para incluir 'louos' em ordem alfabética: ['features','geo','integracoes','louos','retencao','risco','seguranca','ui'].
       - adicionar test_seeder_registra_parametros_do_motor_louos asserindo os dois parâmetros (group/type/default/validation; vagas typedValue()===[] e sandbox typedValue()===50).

    6. Atualizar `tests/Feature/Seeders/DatabaseSeederTest.php`: trocar a contagem de Parameter 31 → 33 (em test_seed_completo... e test_seed_e_idempotente).

    7. `tests/Unit/Louos/EnquadramentoResultTest.php`: test_to_array_e_versoes_expoem_o_contrato (montar um EnquadramentoResult com dimensões e asserir toArray() snake_case, versoes() e resultado()/pendente()).
  </action>
  <verify>php artisan test --compact --filter="EnquadramentoResultTest|ParameterSeederTest|DatabaseSeederTest"</verify>
  <acceptance_criteria>
    - grep "final readonly class EnquadramentoInput" app/Services/Louos/EnquadramentoInput.php
    - grep "STATUS_INDISPONIVEL = 'indisponivel'" app/Services/Louos/EnquadramentoResult.php
    - grep "louos.vagas.exigencia_por_grupo" database/seeders/ParameterSeeder.php
    - grep "louos.sandbox.amostra_padrao" database/seeders/ParameterSeeder.php
    - grep "assertSame(33" tests/Feature/Seeders/ParameterSeederTest.php
    - ParameterSeederTest, DatabaseSeederTest e EnquadramentoResultTest verdes.
  </acceptance_criteria>
  <done>DTOs de entrada/saída do motor existem com toArray/versoes/constantes; 2 parâmetros novos no grupo louos; contagem 33 travada nos dois seeders; testes verdes.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="Louos|ParameterSeederTest|DatabaseSeederTest|RuleDomain"` verde.
- `php artisan test --compact` — suíte completa (baseline 519, --exclude-group postgis é o default do phpunit.xml) CONTINUA verde: as migrations tabulares NÃO podem derrubar nada. Rodar antes de fechar.
</verification>

<success_criteria>
- RuleDomain estende (não recria) a infra da Fase 6 com os 4 Quadros como domínios sensíveis.
- 3 tabelas tipadas referenciam rule_version_id e rodam em SQLite.
- EnquadramentoInput/Result fixam o contrato do motor (dimensão por quadro + consolidado) espelhando RiscoResult/TerritoryResult.
- Catálogo de parâmetros cobre vagas e sandbox; contagem 33 batida nos dois testes de seeder.
- Nenhuma dependência nova; nada hardcoded; nenhuma fachada.
</success_criteria>

<output>
Criar `.planning/phases/05-motor-louos/05-01-SUMMARY.md`: nomes finais de tabelas/colunas, assinatura final dos DTOs (EnquadramentoInput campos; EnquadramentoResult shape de cada dimensão e do consolidado, constantes de status), os 2 parâmetros e a nova contagem 33, e a confirmação do mapeamento de HUs (015=Q7, 016=Q10, 017=Q11, 018=Q11A). Insumo direto de 05-02 (seeds), 05-03/04/05 (motor) e 05-06 (mantenedores).
</output>

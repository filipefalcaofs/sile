---
phase: 05-motor-louos
plan: 08
type: execute
wave: 6
depends_on: ["05-05", "05-06", "05-07"]
files_modified:
  - app/Services/Louos/LouosSandboxSimulationService.php
  - app/Http/Controllers/Gestao/LouosSandboxController.php
  - app/Http/Requests/Gestao/SimulateLouosRequest.php
  - routes/gestao.php
  - resources/js/pages/gestao/louos/sandbox.tsx
  - resources/js/layouts/gestao-layout.tsx
  - tests/Feature/Louos/LouosSandboxTest.php
autonomous: false
requirements: [HU-143, HU-046]

must_haves:
  truths:
    - "O gestor simula uma versão RASCUNHO de um Quadro contra cenários reais e vê quantos resultados mudariam, antes de publicar"
    - "A simulação roda em sandbox: NÃO altera a versão vigente, não publica, não dispara integração/notificação (RN-001)"
    - "A simulação reexecuta o MOTOR REAL com a versão rascunho (resolução por versão específica) e lista cada caso divergente (resultado vigente × simulado)"
    - "A publicação a partir da simulação cria nova versão por quatro olhos (RN-002/RN-005) — autor distinto do publicador"
    - "O tamanho da amostra é parametrizável (louos.sandbox.amostra_padrao) e consta no resumo (RN-003)"
  artifacts:
    - path: "app/Services/Louos/LouosSandboxSimulationService.php"
      provides: "Reexecuta o motor com a versão rascunho sobre cenários e computa divergências, sem efeito colateral"
      contains: "simulate"
      min_lines: 60
  key_links:
    - from: "app/Services/Louos/LouosSandboxSimulationService.php"
      to: "App\\Services\\Louos\\LouosEnquadramentoService"
      via: "enquadrar com versoesOverride (versão rascunho) vs vigente — motor real, sem fachada"
      pattern: "versoesOverride|enquadrar\\("
    - from: "app/Services/Louos/LouosSandboxSimulationService.php"
      to: "App\\Models\\LouosQuadro7Faixa"
      via: "cenários derivados dos CNAEs reais da versão vigente do Quadro 7 (fonte auto-contida, sem arquivo novo)"
      pattern: "LouosQuadro7Faixa::"
    - from: "app/Http/Controllers/Gestao/LouosSandboxController.php"
      to: "App\\Services\\Rules\\RuleVersionService"
      via: "publish do rascunho (4-olhos) somente quando o gestor confirma"
      pattern: "publish"
---

<objective>
Entregar o **sandbox de simulação de regra (HU-143)**: o gestor edita uma versão rascunho de um Quadro, simula o impacto contra cenários reais reexecutando o MOTOR REAL com a versão candidata, vê as divergências e só então publica por quatro olhos.

Purpose: HU-143 (simular impacto antes de publicar) — critério 6 do ROADMAP. Torna segura a meta de ampliar o expresso (medir → parametrizar → medir). Reusa o `status rascunho` do rule_versions (Fase 6) e o modo de resolução "por versão específica" do motor (05-03). Escopo honesto: a amostra é DERIVADA dos CNAEs reais da versão vigente do Quadro 7 já seedada (fonte auto-contida, sem arquivo novo) — quando o EP08 trouxer processos reais, a mesma simulação passa a usá-los (muda a fonte da amostra, não a lógica). RN-001 absoluta: zero efeito colateral.
Output: LouosSandboxSimulationService, controller + FormRequest + rota, página sandbox.tsx, teste. Fecha com checkpoint humano.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-motor-louos/05-CONTEXT.md
@.planning/phases/05-motor-louos/05-05-SUMMARY.md
@.planning/phases/05-motor-louos/05-06-SUMMARY.md
@docs/SILE_HUs_Completas_MD/EP02-Administracao/HU-143-Simular-impacto-de-parametrizacao.md

# PADRÃO A ESPELHAR (motor por versão específica; rascunho; publish 4-olhos):
@app/Services/Louos/LouosEnquadramentoService.php
@app/Services/Louos/EnquadramentoInput.php
@app/Services/Rules/RuleVersionService.php
@app/Services/Louos/LouosMaintenanceService.php
@app/Support/Settings.php
@resources/js/pages/gestao/louos/index.tsx
</context>

<skills>
Ativar `inertia-react-development` e `tailwindcss-development` na página do sandbox.
</skills>

<tasks>

<task type="auto">
  <name>Task 1: LouosSandboxSimulationService (reexecuta o motor com a versão rascunho, sem efeito colateral)</name>
  <read_first>app/Services/Louos/LouosEnquadramentoService.php, app/Services/Louos/EnquadramentoInput.php, app/Services/Rules/RuleVersionService.php, app/Support/Settings.php</read_first>
  <files>app/Services/Louos/LouosSandboxSimulationService.php, tests/Feature/Louos/LouosSandboxTest.php</files>
  <action>
    TDD estrito. Construtor recebe LouosEnquadramentoService.

    `simulate(RuleDomain $domain, string $versaoRascunho, ?int $amostra = null): array`:
    - amostra = $amostra ?? (int) Settings::get('louos.sandbox.amostra_padrao', 50) (RN-003 — sem hardcode).
    - FONTE DOS CENÁRIOS (auto-contida, sem arquivo novo — NÃO ler arquivo externo, NÃO reusar os golden do 05-09, que é wave 7/posterior): derivar a amostra dos CNAEs reais da versão vigente do Quadro 7 já seedada. Para cada CNAE distinto escopado por `RuleVersion::vigente(LouosQuadro7)` (via LouosQuadro7Faixa->cnae_code, ordenado por cnae_code para estabilidade), montar um cenário com (a) uma área representativa derivada de uma faixa do próprio CNAE (ex.: ponto médio entre area_min e area_max, ou area_min + 1 quando area_max for nulo) e (b) um TerritoryResult com uma ZONA identificada fixa retirada do seed do Quadro 10 — assim o consolidado pode ir ALÉM de 'pendente' e a simulação exercita divergências reais (sem a zona o consolidado seria sempre 'pendente' e não haveria o que comparar). Limitar a `amostra`.
    - Para cada cenário, montar dois EnquadramentoInput idênticos exceto a versão do domínio simulado:
      - vigente: sem versoesOverride (usa a vigente).
      - candidato: versoesOverride = [$domain->value => $versaoRascunho] (modo "versão específica" do motor — 05-03).
    - Chamar enquadrar() nos dois e comparar consolidado['resultado']. Acumular: total reprocessado, mudariam (count), distribuição (de→para: ex. permitido→pendente), e a lista de casos divergentes {cenario, resultado_vigente, resultado_simulado, motivo_simulado}.
    - RN-001: NÃO publicar, NÃO alterar a vigente, NÃO disparar integração/notificação. A simulação é só leitura + cálculo. NÃO auditar como decisão (é simulação) — opcionalmente registrar um log informativo 'louos'/'simulacao' SEM rulesVersion de decisão.
    - Retornar {dominio, versao_rascunho, amostra_usada, total, mudariam, distribuicao, divergencias[]}.

    Testes `tests/Feature/Louos/LouosSandboxTest.php` (RefreshDatabase; seeda os Quadros; cria um rascunho via RuleVersionService->openDraft + tabela tipada com uma alteração que muda um resultado):
    - test_simulacao_nao_altera_a_versao_vigente (RN-001) — após simulate(), RuleVersion::vigente do domínio é a MESMA (id inalterado), o rascunho continua rascunho, nenhuma linha da vigente mudou.
    - test_simulacao_reexecuta_o_motor_e_conta_divergencias — os cenários vêm dos CNAEs reais da versão vigente do Quadro 7 (fonte explícita) com a zona fixa do Quadro 10; criar um rascunho do LouosQuadro10 (openDraft + copiar as permissões da vigente) que, para a (zona × grupo de uso) usada pelos cenários, troca a permissão de 'permitido' para 'proibido' → simulate(LouosQuadro10, versaoRascunho) retorna mudariam >= 1 e a divergência lista resultado_vigente='permitido' × resultado_simulado='nao_permitido' para o(s) CNAE(s) afetado(s). (Garante divergência determinística sobre a fonte real de cenários.)
    - test_amostra_e_parametrizavel_e_consta_no_resumo (RN-003) — passar amostra=2 → amostra_usada===2; sem passar usa Settings.
    - test_simulacao_nao_dispara_efeito_colateral — nenhuma Activity de decisão 'enquadramento' com rulesVersion de publicação é criada pela simulação; nenhuma notificação enfileirada.
  </action>
  <verify>php artisan test --compact --filter=LouosSandboxTest</verify>
  <acceptance_criteria>
    - grep "public function simulate" app/Services/Louos/LouosSandboxSimulationService.php
    - grep "versoesOverride" app/Services/Louos/LouosSandboxSimulationService.php (usa o motor real por versão)
    - grep "Settings::get('louos.sandbox.amostra_padrao'" app/Services/Louos/LouosSandboxSimulationService.php
    - grep "LouosQuadro7Faixa" app/Services/Louos/LouosSandboxSimulationService.php (cenários derivados da fonte real — sem arquivo novo, sem golden do 05-09)
    - test_simulacao_nao_altera_a_versao_vigente passa e FALHAVA antes (RED) — prova RN-001.
    - LouosSandboxTest verde (4 testes).
  </acceptance_criteria>
  <done>Sandbox reexecuta o motor real com a versão rascunho, conta divergências e respeita RN-001 (zero efeito colateral); amostra parametrizável; testes verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Controller + rota + página de simulação e publicação 4-olhos</name>
  <read_first>app/Http/Controllers/Gestao/LouosController.php, routes/gestao.php, resources/js/pages/gestao/louos/index.tsx, app/Services/Louos/LouosMaintenanceService.php</read_first>
  <files>app/Http/Controllers/Gestao/LouosSandboxController.php, app/Http/Requests/Gestao/SimulateLouosRequest.php, routes/gestao.php, resources/js/pages/gestao/louos/sandbox.tsx, resources/js/layouts/gestao-layout.tsx, tests/Feature/Louos/LouosSandboxTest.php</files>
  <action>
    1. `App\Http\Controllers\Gestao\LouosSandboxController`:
       - `index(Request)` — Inertia::render('gestao/louos/sandbox', [...]) com a lista de rascunhos por domínio (RuleVersion status rascunho) e a amostra padrão.
       - `simulate(SimulateLouosRequest)` — chama LouosSandboxSimulationService->simulate(domain, versaoRascunho, amostra) e devolve o relatório (back/Inertia) — SEM publicar.
       - `publish(SimulateLouosRequest ou request próprio)` — publica o rascunho via RuleVersionService->publish (4-olhos, autor != publicador) APENAS quando o gestor confirma; comunica bloqueio de 4-olhos via flash.error.
    2. `App\Http\Requests\Gestao\SimulateLouosRequest` — rules: quadro in:quadro7,quadro10,quadro11,quadro11a; versao_rascunho string exists; amostra integer nullable min:1.
    3. `routes/gestao.php` — no grupo `permission:manter-louos` (simular/publicar é manutenção), prefix louos, name louos.sandbox.*:
       ```
       Route::get('simulacao', [LouosSandboxController::class, 'index'])->name('sandbox.index');
       Route::post('simulacao', [LouosSandboxController::class, 'simulate'])->name('sandbox.simular');
       Route::put('simulacao/publicar', [LouosSandboxController::class, 'publish'])->name('sandbox.publicar');
       ```
       (Importar o controller.)
    4. `resources/js/pages/gestao/louos/sandbox.tsx` — UI: selecionar Quadro + rascunho + amostra, botão "Simular impacto" (POST), exibir o resumo (total, mudariam, distribuição) e a tabela de divergências (resultado vigente × simulado), e o botão "Publicar versão" (PUT, 4-olhos comunicado). Reusar componentes do console; responsivo/acessível.
    5. `resources/js/layouts/gestao-layout.tsx` — item de navegação "Simulação de regras" (href /gestao/louos/simulacao, visible: auth.permissions.includes('manter-louos')).
    6. Acrescentar a LouosSandboxTest os testes de rota: test_gestor_simula_pela_rota_sem_publicar (POST simulacao → relatório, vigente intacta), test_publicar_exige_quatro_olhos (autor=publicador → flash.error; distinto → nova versão vigente), test_simulacao_sem_permissao_manter_louos_bloqueada (analista → 403).
  </action>
  <verify>php artisan test --compact --filter=LouosSandboxTest && npm run build 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - grep "louos.sandbox" routes/gestao.php (ou name sandbox.*)
    - grep "gestao/louos/sandbox" resources/js/pages/gestao/louos/sandbox.tsx
    - grep "manter-louos" resources/js/layouts/gestao-layout.tsx (item simulação)
    - LouosSandboxTest verde (rota + serviço); o teste de 403 e de 4-olhos falhavam antes (RED).
    - npm run build verde.
  </acceptance_criteria>
  <done>Sandbox navegável: simula impacto sem efeito colateral e publica por 4-olhos; rota/nav gateadas por manter-louos; build verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Sandbox de simulação de regra (HU-143): editar rascunho de Quadro, simular impacto contra cenários reais com o motor real e publicar por quatro olhos.</what-built>
  <how-to-verify>
    1. Com a app rodando, no console acessar "Simulação de regras".
    2. Selecionar um Quadro com rascunho (criar um rascunho com alteração via mantenedor, se necessário) e clicar "Simular impacto" → conferir o resumo (amostra usada, quantos mudariam, distribuição) e a tabela de divergências (vigente × simulado).
    3. Confirmar que a versão vigente NÃO mudou após simular (volte ao mantenedor de Quadros e verifique a vigência).
    4. Publicar com autor = publicador → confirmar bloqueio 4-olhos; com autor distinto → nova versão vigente.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva ajustes.</resume-signal>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent`, `npm run build` e `npx tsc --noEmit` sem erros.
- `php artisan test --compact --filter=Louos` verde.
- `php artisan test --compact` — suíte completa verde.
- Evidência anti-fachada: a simulação reexecuta o MOTOR REAL por versão específica (não imita resultado) e RN-001 (vigente intacta) é testada.
</verification>

<success_criteria>
- Gestor simula impacto de uma versão rascunho contra cenários reais com o motor real, vê divergências e amostra.
- RN-001: zero efeito colateral (vigente intacta, sem publicação/integração/notificação na simulação).
- Publicação a partir da simulação por quatro olhos (HU-046/HU-053).
- Amostra parametrizável (HU-014); sem dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/05-motor-louos/05-08-SUMMARY.md`: contrato de simulate() e do relatório, a fonte dos cenários de amostra (derivada dos CNAEs reais da versão vigente do Quadro 7 + zona fixa do Quadro 10; nota de escopo: processos reais entram no EP08), as rotas do sandbox e a decisão de não auditar a simulação como decisão. Insumo do 05-09 (verificação) e do EP15 (medir → parametrizar → medir).
</output>

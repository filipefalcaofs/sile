---
phase: 05-motor-louos
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - app/Services/Louos/LouosEnquadramentoService.php
  - tests/Feature/Louos/LouosEnquadramentoQuadro7Test.php
autonomous: true
requirements: [HU-038, HU-046]

must_haves:
  truths:
    - "Dado um CNAE principal e uma área, o motor enquadra a atividade pelo Quadro 7 retornando grupo/subgrupo de uso da faixa de área correspondente"
    - "A área é casada à faixa correta (limites inclusivos no início; area_max nulo = sem limite superior)"
    - "CNAE sem faixa na versão vigente devolve quadro7 status nao_encontrado — nunca um grupo inventado (FA-02)"
    - "O motor resolve a versão de regra em 3 modos (vigente / na data / versão específica para sandbox) espelhando RiscoClassificationService/TerritoryService"
    - "Toda execução é auditada (log 'louos'/'enquadramento') com a versão de regras aplicada (RN-002)"
  artifacts:
    - path: "app/Services/Louos/LouosEnquadramentoService.php"
      provides: "Motor LOUOS: enquadramento por área (Quadro 7) + resolução de versão + auditoria"
      contains: "public function enquadrar"
      min_lines: 70
  key_links:
    - from: "app/Services/Louos/LouosEnquadramentoService.php"
      to: "App\\Models\\LouosQuadro7Faixa"
      via: "busca faixa por (rule_version_id, cnae_code) e área entre area_min/area_max"
      pattern: "LouosQuadro7Faixa::"
    - from: "app/Services/Louos/LouosEnquadramentoService.php"
      to: "App\\Models\\RuleVersion"
      via: "resolve versão vigente/naData/versão específica"
      pattern: "RuleVersion::(vigente|naData|versao)"
    - from: "app/Services/Louos/LouosEnquadramentoService.php"
      to: "App\\Support\\Audit\\AuditService"
      via: "log('louos','enquadramento', rulesVersion)"
      pattern: "logName: 'louos'"
---

<objective>
Construir o **núcleo do motor LOUOS** com a primeira dimensão entregável: o **Quadro 7** (enquadramento por área). Recebe `EnquadramentoInput` (área + CNAE), resolve a versão da regra, encontra a faixa de área do CNAE e devolve grupo/subgrupo de uso no `EnquadramentoResult`, auditado.

Purpose: HU-038 (enquadrar pelo Quadro 7 com CNAE + área) e HU-046 (resolução/registro de versão). É a parte ENTREGÁVEL agora (depende só de CNAE carregado + área informada). Estabelece o esqueleto do service (resolução de versão em 3 modos, helpers de dimensão, auditoria) que 05-04 e 05-05 estendem. Sem fachada: CNAE sem faixa = nao_encontrado (nunca grupo inventado); nesta etapa o consolidado fica `pendente` (zona/via ainda não avaliados — honesto).
Output: `LouosEnquadramentoService::enquadrar(EnquadramentoInput): EnquadramentoResult` com o Quadro 7 real, coberto por feature tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-motor-louos/05-CONTEXT.md
@.planning/phases/05-motor-louos/05-01-SUMMARY.md
@.planning/phases/05-motor-louos/05-02-SUMMARY.md

# PADRÃO A ESPELHAR (motor com DTO + versão + auditoria + degradação):
@app/Services/Risco/RiscoClassificationService.php
@app/Services/Geo/TerritoryService.php
@app/Services/Louos/EnquadramentoInput.php
@app/Services/Louos/EnquadramentoResult.php
@app/Models/RuleVersion.php
@app/Models/LouosQuadro7Faixa.php
@app/Support/Audit/AuditService.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: enquadrar() + Quadro 7 (faixa de área → grupo/subgrupo) + resolução de versão (3 modos)</name>
  <read_first>app/Services/Risco/RiscoClassificationService.php, app/Services/Geo/TerritoryService.php, app/Services/Louos/EnquadramentoResult.php, app/Models/LouosQuadro7Faixa.php, app/Models/RuleVersion.php</read_first>
  <files>app/Services/Louos/LouosEnquadramentoService.php, tests/Feature/Louos/LouosEnquadramentoQuadro7Test.php</files>
  <action>
    TDD estrito (RED → verificar RED → GREEN → verificar GREEN → REFACTOR), um comportamento por vez. Construtor: `public function __construct(private AuditService $audit) {}`.

    Método público `enquadrar(EnquadramentoInput $input): EnquadramentoResult`:
    - Normalizar cnaePrincipal para 7 dígitos (preg_replace '/\D/', '' — precedente do risco).
    - Avaliar a dimensão `quadro7` via método privado `enquadrarQuadro7(...)`.
    - Nesta etapa, `quadro10`/`quadro11`/`quadro11a` ainda NÃO são avaliados: chamar helpers que retornam dimensão com status STATUS_INDISPONIVEL e motivo 'Dimensão ainda não avaliada nesta etapa do motor' (placeholder honesto — 05-04 substitui pela lógica real). NÃO inventar permissão.
    - `consolidado`: nesta etapa, montar via método privado `consolidar(...)` que retorna ResultadoViabilidade::Pendente (motivo: 'Enquadramento por área realizado; permissão por zona e condições pela via ainda não avaliadas') — honesto: o motor está incompleto, logo pendente. 05-05 reescreve o consolidar() com a lógica final.
    - Auditar (uma vez) via AuditService->log(logName:'louos', event:'enquadramento', description:"Enquadramento LOUOS do CNAE {$input->cnaePrincipal}", properties:[cnae, area, quadro7_status, resultado_consolidado, versoes], result:'sucesso', rulesVersion: <versão do quadro7>).
    - Retornar EnquadramentoResult com as 4 dimensões + consolidado + versoes.

    Resolução de versão `resolveVersion(RuleDomain $domain, EnquadramentoInput $input): ?RuleVersion` (3 modos, espelha RiscoClassificationService::resolveVersion):
    - se `$input->versoesOverride[$domain->value]` existir → `RuleVersion::versao($domain, $version)->first()` (modo sandbox).
    - senão se `$input->data` → `RuleVersion::naData($domain, $input->data)->first()`.
    - senão → `RuleVersion::vigente($domain)->first()`.

    `enquadrarQuadro7(string $cnae, float $area, EnquadramentoInput $input): array`:
    - version = resolveVersion(RuleDomain::LouosQuadro7, $input); se null → dimensão nao_encontrado com motivo 'Quadro 7 sem versão vigente'.
    - Buscar LouosQuadro7Faixa por (rule_version_id, cnae_code=$cnae) e área dentro da faixa: `area_min <= $area AND (area_max IS NULL OR $area <= area_max)`. Limite inferior inclusivo; area_max nulo = sem teto.
    - Nenhuma faixa → dimensão {status: STATUS_NAO_ENCONTRADO, grupo: null, subgrupo: null, faixa: null, motivo: 'CNAE sem enquadramento parametrizado no Quadro 7 vigente', versao_regra: version->version}.
    - Faixa encontrada → {status: STATUS_IDENTIFICADO, grupo, subgrupo, faixa: {area_min, area_max}, motivo: null, versao_regra: version->version}.

    Helpers de dimensão privados (espelham TerritoryService::dimIdentificado/dimNaoEncontrado/dimIndisponivel) reutilizáveis por 05-04.

    Testes `tests/Feature/Louos/LouosEnquadramentoQuadro7Test.php` (RefreshDatabase; seeda LouosQuadro7Seeder para dado real OU usa factories de RuleVersion+LouosQuadro7Faixa para cenários de faixa controlados — preferir factories para os casos de fronteira):
    1. test_enquadra_cnae_por_area_na_faixa_correta — CNAE com 2 faixas; área na faixa 1 → grupo/subgrupo da faixa 1.
    2. test_area_acima_do_limite_cai_na_faixa_superior — mesma config, área acima do teto da faixa 1 → faixa 2.
    3. test_limite_inferior_e_inclusivo — área == area_min de uma faixa → identifica essa faixa.
    4. test_faixa_sem_teto_aceita_qualquer_area_acima — faixa com area_max null → área muito grande identifica.
    5. test_cnae_sem_faixa_retorna_nao_encontrado (FA-02) — CNAE inexistente → quadro7 status nao_encontrado, grupo null.
    6. test_consolidado_fica_pendente_enquanto_motor_incompleto — resultado()===ResultadoViabilidade::Pendente->value (zona/via não avaliados).
    7. test_enquadramento_e_auditado_com_versao_de_regras (CA-02/RN-002) — Activity log_name 'louos', event 'enquadramento', rules_version preenchida.
  </action>
  <verify>php artisan test --compact --filter=LouosEnquadramentoQuadro7Test</verify>
  <acceptance_criteria>
    - grep "public function enquadrar(EnquadramentoInput" app/Services/Louos/LouosEnquadramentoService.php
    - grep "RuleVersion::versao" app/Services/Louos/LouosEnquadramentoService.php (modo sandbox)
    - grep "logName: 'louos'" app/Services/Louos/LouosEnquadramentoService.php
    - grep "STATUS_NAO_ENCONTRADO" app/Services/Louos/LouosEnquadramentoService.php
    - LouosEnquadramentoQuadro7Test verde (7 testes); o teste 5 (nao_encontrado) e o 3 (limite inclusivo) falhavam antes (RED).
  </acceptance_criteria>
  <done>Motor enquadra por área (Quadro 7) com faixas corretas, resolve versão em 3 modos, degrada para nao_encontrado sem faixa e audita; consolidado pendente honesto; testes verdes.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=LouosEnquadramentoQuadro7Test` verde.
- `php artisan test --compact` — suíte completa verde.
- Evidência anti-fachada: o teste 5 prova que CNAE sem regra não recebe grupo inventado (nao_encontrado) e o teste 6 prova consolidado pendente enquanto as dimensões territoriais não foram avaliadas.
</verification>

<success_criteria>
- enquadrar() devolve EnquadramentoResult com o Quadro 7 real (grupo/subgrupo por faixa de área).
- Resolução de versão em 3 modos (vigente/naData/versão) pronta para o sandbox.
- CNAE sem faixa nunca recebe grupo inventado.
- Toda execução auditada com a versão de regras.
</success_criteria>

<output>
Criar `.planning/phases/05-motor-louos/05-03-SUMMARY.md`: assinatura de enquadrar(), shape final da dimensão quadro7, contrato dos helpers de dimensão e do resolveVersion (3 modos), e a decisão do consolidado pendente provisório. Insumo direto de 05-04 (que substitui os placeholders de quadro10/11/11a) e 05-05 (que reescreve consolidar()).
</output>

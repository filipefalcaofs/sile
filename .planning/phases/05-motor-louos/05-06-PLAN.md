---
phase: 05-motor-louos
plan: 06
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - database/seeders/RolesAndPermissionsSeeder.php
  - tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
  - app/Services/Louos/LouosMaintenanceService.php
  - app/Http/Controllers/Gestao/LouosController.php
  - app/Http/Requests/Gestao/PublishLouosVersionRequest.php
  - routes/gestao.php
  - tests/Feature/Louos/LouosMaintenanceTest.php
  - tests/Feature/Louos/LouosConsultaTest.php
autonomous: true
requirements: [HU-015, HU-016, HU-017, HU-018, HU-046]

must_haves:
  truths:
    - "Analista/gestor/administrador consultam a versão vigente dos Quadros 7/10/11/11A pelo console (consulta auditada)"
    - "Administrador publica uma NOVA versão de um Quadro por quatro olhos (autor distinto do publicador) — a vigente anterior é preservada, nunca editada destrutivamente"
    - "A manutenção reusa o RuleVersionService da Fase 6 (HU-046) — não recria infra de versão"
    - "Permissões consultar-louos e manter-louos existem e gateiam a consulta vs a manutenção (espelha risco)"
    - "Publicação por autor igual ao publicador em domínio sensível é bloqueada e comunicada (flash.error), nunca silenciosa"
  artifacts:
    - path: "app/Services/Louos/LouosMaintenanceService.php"
      provides: "Publica nova versão de um Quadro copiando o dado da vigente + alterações, 4-olhos"
      contains: "publishNewVersion"
    - path: "app/Http/Controllers/Gestao/LouosController.php"
      provides: "Consulta vigente dos 4 quadros + endpoint de publicação versionada"
      contains: "class LouosController"
  key_links:
    - from: "app/Services/Louos/LouosMaintenanceService.php"
      to: "App\\Services\\Rules\\RuleVersionService"
      via: "openDraft + publish (4-olhos), reuso da Fase 6"
      pattern: "RuleVersionService"
    - from: "routes/gestao.php"
      to: "App\\Http\\Controllers\\Gestao\\LouosController"
      via: "rotas gestao.louos.* atrás de permission:consultar-louos / manter-louos"
      pattern: "permission:manter-louos"
---

<objective>
Entregar os **mantenedores dos Quadros da LOUOS** no console SEDUR (backend): consulta da versão vigente de cada Quadro e publicação de NOVA versão por quatro olhos, reusando o `RuleVersionService` da Fase 6. Espelha o padrão dos mantenedores de risco (06-06).

Purpose: HU-015 (Manter Quadro 7), HU-016 (Manter Quadro 10), HU-017 (Manter Quadro 11), HU-018 (Manter Quadro 11A) — mapeamento autoritativo dos arquivos de HU. HU-046 (versionar): atualizar publica nova versão, nunca edição destrutiva. Critério 1 do ROADMAP: "Admin mantém os Quadros 7/10/11/11A como dados versionados, com vigência e histórico auditado". Roda em paralelo ao motor (arquivos disjuntos).
Output: permissões consultar-louos/manter-louos, LouosMaintenanceService, LouosController (consulta + publish), FormRequest, rotas gestao.louos.* e feature tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-motor-louos/05-CONTEXT.md
@.planning/phases/05-motor-louos/05-01-SUMMARY.md
@.planning/phases/05-motor-louos/05-02-SUMMARY.md

# PADRÃO A ESPELHAR (mantenedor versionado 4-olhos da Fase 6):
@app/Services/Risco/RiscoMaintenanceService.php
@app/Http/Controllers/Gestao/RiscoController.php
@app/Http/Requests/Gestao/PublishRiscoVersionRequest.php
@app/Services/Rules/RuleVersionService.php
@database/seeders/RolesAndPermissionsSeeder.php
@tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
@routes/gestao.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Permissões consultar-louos/manter-louos + LouosMaintenanceService (4-olhos)</name>
  <read_first>database/seeders/RolesAndPermissionsSeeder.php, tests/Feature/Authorization/RolesAndPermissionsSeederTest.php, app/Services/Risco/RiscoMaintenanceService.php, app/Services/Rules/RuleVersionService.php</read_first>
  <files>database/seeders/RolesAndPermissionsSeeder.php, tests/Feature/Authorization/RolesAndPermissionsSeederTest.php, app/Services/Louos/LouosMaintenanceService.php, tests/Feature/Louos/LouosMaintenanceTest.php</files>
  <action>
    TDD estrito.

    1. `RolesAndPermissionsSeeder` — acrescentar (aditivo, givePermissionTo) as permissões `consultar-louos` e `manter-louos` ao array $permissions:
       - analista/gestor/administrador recebem `consultar-louos` (espelha consultar-risco).
       - administrador recebe `manter-louos`.
       - Total de permissões passa de 12 → 14.

    2. `tests/Feature/Authorization/RolesAndPermissionsSeederTest` — atualizar test_seeder_e_idempotente (Permission count 12 → 14) e acrescentar test_papeis_recebem_permissoes_de_louos (consultar-louos para admin/analista/gestor; manter-louos só admin; cidadão sem nenhuma).

    3. `App\Services\Louos\LouosMaintenanceService` (espelha RiscoMaintenanceService):
       - `publishNewVersion(RuleDomain $domain, string $version, array $alteracoes, int $authorId, int $publisherId): RuleVersion` em DB::transaction.
       - openDraft($domain, $version, source da vigente ?? 'Atualização manual do '.$domain->label().' (mantenedores SEDUR)', $authorId).
       - copiar as linhas da tabela tipada do domínio (LouosQuadro7Faixa para LouosQuadro7; LouosQuadro10Permissao para LouosQuadro10; LouosQuadro11CondicaoVia para LouosQuadro11/11a) da versão vigente para o rascunho, aplicando $alteracoes (chave natural por tabela: quadro7 = cnae_code+area_min; quadro10 = zona+grupo_uso+subgrupo; quadro11 = classe_via+grupo_uso). Insert em lote sem model events (auditoria é o evento único do publish).
       - publish($draft, $publisherId) — o RuleVersionService aplica o 4-olhos (domínio LOUOS é isSensitive).
       - Método auxiliar privado `copyRows(RuleDomain, ?RuleVersion $current, RuleVersion $draft, array $alteracoes)` com um switch por domínio (mantém o service coeso).

    Testes `tests/Feature/Louos/LouosMaintenanceTest.php` (RefreshDatabase; seeda LouosQuadro7Seeder etc. para ter vigente):
    - test_publica_nova_versao_do_quadro7_preservando_a_anterior (vigente vira substituida com valid_to; nova vigente; faixas copiadas + alteração aplicada).
    - test_publicacao_quatro_olhos_rejeita_autor_igual_ao_publicador (FourEyesViolationException via RuleVersionService).
    - test_publica_nova_versao_do_quadro10_com_alteracao_de_permissao.
  </action>
  <verify>php artisan test --compact --filter="LouosMaintenanceTest|RolesAndPermissionsSeederTest"</verify>
  <acceptance_criteria>
    - grep "consultar-louos" e "manter-louos" database/seeders/RolesAndPermissionsSeeder.php
    - grep "assertSame(14" tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
    - grep "publishNewVersion" app/Services/Louos/LouosMaintenanceService.php
    - grep "RuleVersionService" app/Services/Louos/LouosMaintenanceService.php (reuso, não recria)
    - LouosMaintenanceTest e RolesAndPermissionsSeederTest verdes; o teste de 4-olhos falhava antes (RED).
  </acceptance_criteria>
  <done>Permissões de LOUOS seedadas (14 total); LouosMaintenanceService publica nova versão por quatro olhos preservando a anterior, reusando a Fase 6; testes verdes.</done>
</task>

<task type="auto">
  <name>Task 2: LouosController (consulta vigente + publish) + FormRequest + rotas</name>
  <read_first>app/Http/Controllers/Gestao/RiscoController.php, app/Http/Requests/Gestao/PublishRiscoVersionRequest.php, routes/gestao.php, app/Services/Louos/LouosMaintenanceService.php</read_first>
  <files>app/Http/Controllers/Gestao/LouosController.php, app/Http/Requests/Gestao/PublishLouosVersionRequest.php, routes/gestao.php, tests/Feature/Louos/LouosConsultaTest.php</files>
  <action>
    TDD estrito (feature tests de rota). Espelhar RiscoController (server-driven, auditoria, Inertia::render).

    1. `App\Http\Controllers\Gestao\LouosController`:
       - `index(Request $request): Response` — montar, para cada domínio LOUOS, a versão vigente (RuleVersion::vigente) e um resumo dos dados (ex.: total de faixas do Quadro 7 por grupo; contagem de permissões do Quadro 10; classes de via do 11/11A) + uma listagem paginada do Quadro selecionado (param `quadro`, default 'quadro7') com busca/filtro (PER_PAGE_OPTIONS técnica, default Settings ui.cnaes.per_page como no RiscoController). Auditar (log 'louos', event 'consulta-quadros', rulesVersion da versão consultada). Inertia::render('gestao/louos/index', [...]) — props consumidas pelo 05-07.
       - `publish(PublishLouosVersionRequest $request): RedirectResponse` — resolver o domínio pelo campo validado `quadro`, exigir author_id != publisher (id do usuário) com flash.error pt-BR quando igual (degradação comunicada), chamar LouosMaintenanceService->publishNewVersion(...), back()->with('status', ...).

    2. `App\Http\Requests\Gestao\PublishLouosVersionRequest` — authorize via permission manter-louos (ou true se o gate é a rota); rules: quadro in:quadro7,quadro10,quadro11,quadro11a; version string; author_id exists users; alteracoes array (estrutura por quadro validada minimamente).

    3. `routes/gestao.php` — acrescentar, no grupo `auth:gestao` + `lgpd.accepted` (espelhando o bloco de risco):
       ```
       Route::middleware('permission:consultar-louos')->prefix('louos')->name('louos.')->group(function () {
           Route::get('/', [LouosController::class, 'index'])->name('index');
       });
       Route::middleware('permission:manter-louos')->prefix('louos')->name('louos.')->group(function () {
           Route::put('publicar', [LouosController::class, 'publish'])->name('publicar');
       });
       ```
       Importar o LouosController no topo do arquivo.

    Testes `tests/Feature/Louos/LouosConsultaTest.php` (RefreshDatabase; seeda quadros + RolesAndPermissionsSeeder; actingAs($user,'gestao')):
    - test_analista_consulta_quadros_vigentes (GET /gestao/louos 200; auditoria 'louos'/'consulta-quadros').
    - test_consulta_sem_permissao_e_bloqueada (usuário sem consultar-louos → 403, evento auditado).
    - test_admin_publica_nova_versao_do_quadro (PUT /gestao/louos/publicar com author_id != publisher → redirect + status; nova versão vigente).
    - test_publicacao_com_autor_igual_ao_publicador_e_bloqueada (flash.error, sem nova versão).
    - test_publicar_sem_permissao_manter_louos_e_bloqueado (analista → 403).
    NÃO criar a página React aqui (é o 05-07); os testes usam assertRedirect/assertForbidden e assert de auditoria/estado, sem ->component() (a página só existe no 05-07).
  </action>
  <verify>php artisan test --compact --filter=LouosConsultaTest && php artisan route:list --path=gestao/louos</verify>
  <acceptance_criteria>
    - grep "class LouosController" app/Http/Controllers/Gestao/LouosController.php
    - grep "permission:manter-louos" routes/gestao.php e grep "permission:consultar-louos" routes/gestao.php
    - grep "louos.publicar" no route:list (rota PUT existe)
    - grep "in:quadro7,quadro10,quadro11,quadro11a" app/Http/Requests/Gestao/PublishLouosVersionRequest.php
    - LouosConsultaTest verde (5 testes); o teste de 403 sem permissão e o de autor=publicador falhavam antes (RED).
  </acceptance_criteria>
  <done>Console consulta os 4 Quadros vigentes (auditado) e publica nova versão por quatro olhos atrás de manter-louos; consulta gateada por consultar-louos; testes verdes.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="Louos|RolesAndPermissionsSeederTest"` verde.
- `php artisan test --compact` — suíte completa verde.
- Evidência anti-fachada: a publicação é operação real versionada (a vigente anterior vira substituida com valid_to) e o 4-olhos é bloqueio real testado.
</verification>

<success_criteria>
- Permissões consultar-louos/manter-louos seedadas e gateando consulta vs manutenção (14 permissões totais).
- Consulta da versão vigente dos 4 Quadros auditada no console.
- Publicação de nova versão por quatro olhos preservando a anterior, reusando o RuleVersionService (HU-046) — sem recriar infra.
- Nenhuma edição destrutiva da vigente; bloqueio de autor=publicador comunicado.
</success_criteria>

<output>
Criar `.planning/phases/05-motor-louos/05-06-SUMMARY.md`: nomes finais das rotas (gestao.louos.index/publicar), contrato das props enviadas ao Inertia (insumo do 05-07), assinatura de LouosMaintenanceService::publishNewVersion e a chave natural de alteração por quadro, e as permissões adicionadas. Insumo direto de 05-07 (UI), 05-08 (sandbox publica via mesma infra) e 05-09 (verificação).
</output>

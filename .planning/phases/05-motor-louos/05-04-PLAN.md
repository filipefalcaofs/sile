---
phase: 05-motor-louos
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - app/Services/Louos/LouosEnquadramentoService.php
  - tests/Feature/Louos/LouosEnquadramentoTerritorioTest.php
autonomous: true
requirements: [HU-039, HU-040, HU-041]

must_haves:
  truths:
    - "Quando a zona vem indisponível do território (Fase 4), o Quadro 10 retorna status indisponivel e NUNCA consulta a tabela de permissões nem inventa permissão (anti-fachada)"
    - "Quando há zona identificada, o Quadro 10 busca a permissão por (zona, grupo de uso do Quadro 7) na versão vigente e retorna permitido/permitido_condicionado/proibido"
    - "Quadros 11 e 11A usam a classe viária do território; sem o atributo viário LOUOS (pendente SEDUR) degradam para indisponivel/nao_encontrado, sem inventar condição"
    - "O Quadro 10 só é avaliado se o Quadro 7 identificou um grupo de uso (sem enquadramento não há o que permitir)"
    - "Cada dimensão registra a versão de regra consultada (RN-002/RN-004)"
  artifacts:
    - path: "app/Services/Louos/LouosEnquadramentoService.php"
      provides: "Dimensões territoriais do motor (Quadro 10 com degradação + Quadros 11/11A parciais)"
      contains: "enquadrarQuadro10"
  key_links:
    - from: "app/Services/Louos/LouosEnquadramentoService.php"
      to: "App\\Services\\Geo\\TerritoryResult"
      via: "lê input.territory.zona/via (status e propriedades) — não consulta o território, recebe-o"
      pattern: "->territory"
    - from: "app/Services/Louos/LouosEnquadramentoService.php"
      to: "App\\Models\\LouosQuadro10Permissao"
      via: "busca permissão por (rule_version_id, zona, grupo_uso) SOMENTE quando a zona está disponível"
      pattern: "LouosQuadro10Permissao::"
---

<objective>
Adicionar ao motor as **dimensões territoriais**: o Quadro 10 (permissão por zona) com a **degradação sem zona** como peça central anti-fachada, e os Quadros 11/11A (condições pela via) parciais. O motor recebe o território (TerritoryResult da Fase 4) — não o consulta — e, quando a base é pendente, degrada de forma comunicada.

Purpose: HU-039 (Quadro 10 — permissão na zona, RN-005 proibitivo impede deferimento, RN-006 condicionado gera condicionante), HU-040/HU-041 (Quadros 11/11A — condições pela via). Escopo honesto (decisão do 05-CONTEXT + bloqueio da Fase 4): a zona urbanística e o atributo viário LOUOS estão pendentes SEDUR; o motor NUNCA inventa permissão/condição sem o dado — retorna indisponivel e deixa o consolidado pendente (05-05). Quando a SEDUR entregar a base, a MESMA lógica passa a decidir — muda a carga, não a lógica.
Output: métodos `enquadrarQuadro10`, `enquadrarQuadro11`, `enquadrarQuadro11a` substituindo os placeholders do 05-03, com a degradação testada explicitamente.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-motor-louos/05-CONTEXT.md
@.planning/phases/05-motor-louos/05-03-SUMMARY.md
@.planning/phases/05-motor-louos/05-02-SUMMARY.md

# PADRÃO A ESPELHAR (degradação sem fonte; dimensão recebida, não consultada):
@app/Services/Geo/TerritoryService.php
@app/Services/Geo/TerritoryResult.php
@app/Services/Louos/LouosEnquadramentoService.php
@app/Models/LouosQuadro10Permissao.php
@app/Models/LouosQuadro11CondicaoVia.php
@app/Enums/Quadro10Permissao.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quadro 10 (permissão por zona) com degradação sem zona</name>
  <read_first>app/Services/Geo/TerritoryResult.php, app/Services/Louos/LouosEnquadramentoService.php, app/Models/LouosQuadro10Permissao.php, app/Enums/Quadro10Permissao.php</read_first>
  <files>app/Services/Louos/LouosEnquadramentoService.php, tests/Feature/Louos/LouosEnquadramentoTerritorioTest.php</files>
  <action>
    TDD estrito. Substituir o placeholder de `quadro10` (05-03) por `enquadrarQuadro10(array $quadro7, EnquadramentoInput $input): array`.

    Regras (espelha a degradação do TerritoryService::isBlocked, mas o motor RECEBE o território, não o consulta):
    - Ler a zona de `$input->territory?->zona` (shape do TerritoryResult: {status, nome, propriedades, motivo, versao_camada}).
    - DEGRADAÇÃO (anti-fachada): se `$input->territory` é null OU `zona['status'] !== 'identificado'` (ex.: 'indisponivel' = base de zoneamento pendente SEDUR) → retornar dimensão {status: STATUS_INDISPONIVEL, permissao: null, condicionante_ref: null, base_legal: null, motivo: <motivo da zona ou 'Permissão por zona pendente da base oficial (SEDUR)'>, versao_regra: null}. NÃO consultar LouosQuadro10Permissao, NÃO inventar permissão.
    - Pré-condição de enquadramento: se `quadro7['status'] !== STATUS_IDENTIFICADO` (sem grupo de uso) → {status: STATUS_INDISPONIVEL, permissao: null, motivo: 'Sem enquadramento (Quadro 7) não há permissão a verificar', versao_regra: null}.
    - Com zona identificada E grupo de uso: version = resolveVersion(RuleDomain::LouosQuadro10, $input); derivar o nome da zona das propriedades do território (chave usual ex.: 'ZONA'/'zona' — usar o `nome` do TerritoryResult quando presente). Buscar LouosQuadro10Permissao por (rule_version_id, zona, grupo_uso = quadro7['grupo']) (considerar subgrupo quando houver).
      - Encontrada → {status: STATUS_IDENTIFICADO, permissao: <valor Quadro10Permissao>, condicionante_ref, base_legal, motivo: null, versao_regra}.
      - Não encontrada → {status: STATUS_NAO_ENCONTRADO, permissao: null, motivo: 'Combinação zona × grupo de uso sem regra no Quadro 10 vigente', versao_regra}.
    - Atualizar a chamada em enquadrar() para usar enquadrarQuadro10(quadro7, input) no lugar do placeholder.

    Testes em `tests/Feature/Louos/LouosEnquadramentoTerritorioTest.php` (RefreshDatabase; usar factories de RuleVersion+LouosQuadro10Permissao + montar TerritoryResult diretamente para controlar o status da zona):
    1. test_zona_indisponivel_degrada_quadro10_sem_consultar_tabela (CENTRAL anti-fachada) — TerritoryResult com zona status 'indisponivel'; quadro10 status indisponivel, permissao null, motivo presente. Garantir que NÃO há permissão inventada.
    2. test_territorio_nulo_degrada_quadro10 — input.territory null → quadro10 indisponivel.
    3. test_zona_identificada_retorna_permissao_permitido — zona 'ZR-1' + grupo do quadro7 com permissao 'permitido' no seed/factory → quadro10 identificado, permissao 'permitido'.
    4. test_zona_identificada_retorna_proibido — combinação com permissao 'proibido' → quadro10 'proibido' (RN-005 — o consolidado em 05-05 vira nao_permitido).
    5. test_zona_identificada_sem_regra_retorna_nao_encontrado — zona+grupo sem linha no Quadro 10 → nao_encontrado.
    6. test_sem_enquadramento_quadro7_nao_avalia_quadro10 — quadro7 nao_encontrado → quadro10 indisponivel com motivo de pré-condição.
  </action>
  <verify>php artisan test --compact --filter=LouosEnquadramentoTerritorioTest</verify>
  <acceptance_criteria>
    - grep "enquadrarQuadro10" app/Services/Louos/LouosEnquadramentoService.php
    - grep "STATUS_INDISPONIVEL" no método do quadro10 (degradação)
    - grep "LouosQuadro10Permissao::" app/Services/Louos/LouosEnquadramentoService.php
    - O teste test_zona_indisponivel_degrada_quadro10_sem_consultar_tabela passa e FALHAVA antes (RED) — prova anti-fachada.
    - LouosEnquadramentoTerritorioTest verde (Task 1: testes 1-6).
  </acceptance_criteria>
  <done>Quadro 10 avalia permissão por zona quando há zona; degrada para indisponivel sem zona/sem enquadramento, nunca inventa permissão; testes verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Quadros 11 e 11A (condições pela via) parciais com degradação</name>
  <read_first>app/Services/Louos/LouosEnquadramentoService.php, app/Services/Geo/TerritoryResult.php, app/Models/LouosQuadro11CondicaoVia.php</read_first>
  <files>app/Services/Louos/LouosEnquadramentoService.php, tests/Feature/Louos/LouosEnquadramentoTerritorioTest.php</files>
  <action>
    TDD estrito. Substituir os placeholders de `quadro11`/`quadro11a` por um método compartilhado `enquadrarCondicoesVia(RuleDomain $domain, array $quadro7, EnquadramentoInput $input): array` (chamado para LouosQuadro11 e LouosQuadro11a).

    Regras (escopo honesto — geometria viária existe na Fase 4, mas o ATRIBUTO de classificação viária LOUOS pende SEDUR):
    - Ler a via de `$input->territory?->via` (shape {status, nome, propriedades, distancia_m, motivo, versao_camada}).
    - DEGRADAÇÃO: se território null OU via status !== 'identificado' → {status: STATUS_INDISPONIVEL, classe_via: null, condicoes: [], motivo: <motivo da via ou 'Condições pela via dependem da classificação viária LOUOS (pendente SEDUR)'>, versao_regra: null}.
    - Extrair a classe viária das propriedades da via SOMENTE pela chave do atributo LOUOS (ex.: 'CLASSE_VIA_LOUOS'); se ausente (caso atual — só geometria, sem o atributo) → {status: STATUS_INDISPONIVEL, classe_via: null, condicoes: [], motivo: 'Via identificada, porém sem o atributo de classificação viária LOUOS (pendente SEDUR)', versao_regra: null}. NUNCA inventar a classe.
    - Com classe presente: version = resolveVersion($domain, $input); buscar LouosQuadro11CondicaoVia por (rule_version_id, classe_via, grupo_uso opcional) → identificado {classe_via, condicoes, base_legal} ou nao_encontrado.
    - enquadrar() chama enquadrarCondicoesVia(LouosQuadro11, ...) para quadro11 e (LouosQuadro11a, ...) para quadro11a.

    Acrescentar testes ao LouosEnquadramentoTerritorioTest:
    7. test_via_sem_atributo_louos_degrada_quadro11 (anti-fachada) — TerritoryResult com via identificada SEM a chave de classe LOUOS → quadro11 indisponivel, condicoes vazio, motivo de atributo pendente.
    8. test_via_indisponivel_degrada_quadro11_e_11a — via status 'indisponivel' → quadro11 e quadro11a indisponivel.
    9. test_classe_via_presente_retorna_condicoes_quadro11 — propriedades com CLASSE_VIA_LOUOS e linha no Quadro 11 → identificado com condicoes (array). Repetir para quadro11a com a versão do domínio 11a.
  </action>
  <verify>php artisan test --compact --filter=LouosEnquadramentoTerritorioTest</verify>
  <acceptance_criteria>
    - grep "enquadrarCondicoesVia" app/Services/Louos/LouosEnquadramentoService.php
    - grep "CLASSE_VIA_LOUOS" app/Services/Louos/LouosEnquadramentoService.php (só usa o atributo real)
    - O teste test_via_sem_atributo_louos_degrada_quadro11 passa e FALHAVA antes (RED) — anti-fachada do atributo pendente.
    - LouosEnquadramentoTerritorioTest verde (todos os 9 testes).
  </acceptance_criteria>
  <done>Quadros 11/11A aplicam condições quando a classe viária LOUOS existe e degradam sem o atributo (pendente SEDUR), sem inventar condição; testes verdes.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=LouosEnquadramento` (Quadro7 + Territorio) verde.
- `php artisan test --compact` — suíte completa verde.
- Evidência anti-fachada (decisão do 05-CONTEXT): zona indisponível → Quadro 10 indisponivel (nunca permitido/proibido inventado); via sem atributo LOUOS → Quadro 11/11A indisponivel. Os dois caminhos de degradação têm teste nomeado.
</verification>

<success_criteria>
- Quadro 10 decide permissão por (zona × grupo de uso) quando há zona; degrada para indisponivel sem zona, sem enquadramento ou sem regra.
- Quadros 11/11A aplicam condições pela via quando o atributo viário LOUOS existe; degradam de forma comunicada enquanto pende.
- O motor NUNCA inventa permissão/condição sem o dado; o consolidado segue pendente (05-05 fecha a regra).
- Versões de regra registradas por dimensão.
</success_criteria>

<output>
Criar `.planning/phases/05-motor-louos/05-04-SUMMARY.md`: shape final das dimensões quadro10/quadro11/quadro11a, a chave de propriedade usada para zona e para a classe viária LOUOS (contrato do wiring com o território no EP07), e a regra de degradação. Insumo direto de 05-05 (consolidação) e 05-09 (golden cases com casos requer_zona).
</output>

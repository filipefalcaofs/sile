---
phase: 04-georreferenciamento-e-territorio
plan: "04-04"
type: execute
wave: 2
depends_on: ["04-01"]
requirements: [HU-036, HU-034, HU-035, HU-032]
files_modified:
  - app/Services/Geo/GeoLayerService.php
  - app/Services/Geo/GeoJsonLayerImporter.php
  - app/Console/Commands/ImportGeoLayerCommand.php
  - database/seeders/GeoLayerSeeder.php
  - database/seeders/DatabaseSeeder.php
  - database/data/geo/bairros.geojson
  - database/data/geo/restricoes-ambientais.geojson
  - database/data/geo/vias.geojson
  - tests/Fixtures/geo/bairros-amostra.geojson
  - tests/Unit/Geo/GeoLayerServiceTest.php
  - tests/Feature/Geo/ImportGeoLayerTest.php
  - tests/Feature/Geo/GeoLayerSeederTest.php
autonomous: true

must_haves:
  truths:
    - "Um comando importa GeoJSON oficial para geo_features, idempotente por (type, version), com diff auditado (origem, responsável, data, features +/-/~)"
    - "A carga de uma nova versão NÃO apaga a anterior; a anterior fica com valid_to preenchido (histórico/reprodução)"
    - "O seed de desenvolvimento carrega dados OFICIAIS REAIS do GeoSalvador a partir de um snapshot COMMITADO em database/data/geo/ (bairro real é requisito rígido; via e restrição reais) — nunca polígono inventado, nunca dependente de rede em teste/CI"
    - "Zona urbanística e lote são seedadas como camadas pendente_fonte (feature_count 0), comunicando o bloqueio pendente SEDUR"
  artifacts:
    - path: "app/Console/Commands/ImportGeoLayerCommand.php"
      provides: "comando geo:importar {type} {arquivo} --version --source"
      contains: "geo:importar"
    - path: "app/Services/Geo/GeoJsonLayerImporter.php"
      provides: "insert via ST_GeomFromGeoJSON por feature; idempotente por (type,version)"
      contains: "ST_GeomFromGeoJSON"
    - path: "app/Services/Geo/GeoLayerService.php"
      provides: "abertura de versão (fecha a anterior) + diff auditado"
      contains: "valid_to"
    - path: "database/seeders/GeoLayerSeeder.php"
      provides: "carga real driver-aware + camadas pendentes zona/lote"
      contains: "pendente_fonte"
  key_links:
    - from: "app/Services/Geo/GeoJsonLayerImporter.php"
      to: "geo_features.geometry"
      via: "ST_MakeValid(ST_SetSRID(ST_GeomFromGeoJSON(...),4326))"
      pattern: "ST_GeomFromGeoJSON"
    - from: "app/Services/Geo/GeoLayerService.php"
      to: "AuditService"
      via: "log territorio/carga-camada com diff e rules_version"
      pattern: "carga-camada"
    - from: "database/seeders/GeoLayerSeeder.php"
      to: "DB::getDriverName()"
      via: "guard pgsql (SQLite não tem PostGIS)"
      pattern: "getDriverName"
---

<objective>
Implementar a HU-036 (camadas como dados versionados com vigência + diff auditado, RN-004/RN-005): o serviço de versionamento (`GeoLayerService`), o importador de GeoJSON (`GeoJsonLayerImporter` + comando `geo:importar`) e a **carga de dados oficiais REAIS** do GeoSalvador no seed de desenvolvimento — bairro, via e restrições ambientais com dado público real; zona e lote seedadas como **pendente_fonte** (bloqueio comunicado, sem polígono inventado).

Purpose: As camadas são o insumo da identificação (04-05) e do motor da Fase 5. RN-004 exige histórico por versão (nova carga não apaga a anterior; reprodução usa a versão da época); RN-005 exige diff auditado (origem/responsável/data/features +/−/~). A regra nº1 (sem fachada) determina: onde há fonte pública real, carrega de verdade; onde não há (zona/lote), a camada fica explicitamente pendente.
Output: GeoLayerService (vigência/diff), GeoJsonLayerImporter + comando idempotente auditado, dados reais versionados em database/data/geo/, GeoLayerSeeder driver-aware registrado no DatabaseSeeder.
</objective>

<context>
@.planning/phases/04-georreferenciamento-e-territorio/04-CONTEXT.md
@.planning/phases/04-georreferenciamento-e-territorio/04-RESEARCH.md
@.planning/phases/04-georreferenciamento-e-territorio/04-01-SUMMARY.md
@app/Models/GeoLayer.php
@app/Models/GeoFeature.php
@app/Enums/GeoLayerType.php
@app/Enums/GeoLayerStatus.php
@app/Services/CnaeImportService.php
@app/Console/Commands/ImportCnaeCommand.php
@app/Support/Audit/AuditService.php
@database/seeders/DatabaseSeeder.php
@tests/PostgisTestCase.php

Veredito de dados (RESEARCH — verificado por chamada real ao ArcGIS GeoSalvador em 2026-06-13, exporta `f=geojson&outSR=4326`):
- ENTREGÁVEIS: bairro (`.../Bairros/MapServer/0/query?where=1=1&outFields=*&f=geojson&outSR=4326`), via/logradouro (`.../Logradouros/MapServer/0`), restrições ambientais (`.../PDDU_2016/MapServer` — Savam/ZEIS/Mata Atlântica).
- BLOQUEADAS (sem fonte pública): zona urbanística LOUOS (pasta ArcGIS vazia, só PDF) e lote cadastral (SEFAZ restrito).
- Pitfalls: 6 (ST_GeomFromGeoJSON só aceita UMA geometria — iterar features[]); 8 (ST_MakeValid na carga); 5 (origem já em 4326 com outSR=4326 dispensa ST_Transform); 11 (maxRecordCount 2000 — paginar quando necessário; bairros ~160 cabem numa página).
</context>

<tasks>

<task type="tdd">
  <name>Task 1: GeoLayerService — abertura de versão (fecha a anterior) + cálculo de diff — TDD</name>
  <requirements>HU-036 (RN-004, RN-005)</requirements>
  <read_first>
    - app/Models/GeoLayer.php (scopes vigente/naData; fillable)
    - app/Support/Audit/AuditService.php (assinatura log())
    - tests/PostgisTestCase.php (base @group postgis)
  </read_first>
  <action>
    Criar `app/Services/Geo/GeoLayerService.php`:
    - `openVersion(GeoLayerType $type, string $version, string $source, ?CarbonInterface $validFrom = null): GeoLayer` — em DB::transaction: fecha a versão vigente anterior do mesmo type (`status = substituida`, `valid_to = $validFrom ?? today()`), cria a nova `GeoLayer` (status `vigente`, `valid_from`, `valid_to = null`, `source`, `rules_version = $version`, `feature_count = 0`). Idempotente por (type, version): se já existir (type, version), retorna a existente sem duplicar (a unique constraint do 04-01 protege).
    - `finalizeCount(GeoLayer $layer): void` — atualiza `feature_count` = `$layer->features()->count()`.
    - `auditLoad(GeoLayer $layer, array $diff): void` — `AuditService::log('territorio','carga-camada', "Carga da camada {$type} versão {$version}", properties: [origem, responsável (auth), type, version, diff], subject: $layer, result: 'sucesso', rulesVersion: $version)`. O diff é `['adicionadas'=>int,'alteradas'=>int,'removidas'=>int]`.
    - `computeDiff(GeoLayer $previous = null, GeoLayer $current): array` — adicionadas = features da current; removidas = features da previous (quando há substituição); **alteradas = 0 nesta fase**. DECISÃO DOCUMENTADA (RN-005): sem chave estável por feature, a carga de nova versão (e o re-import) é contabilizada como **remove + add**, NÃO como alteração in-place. RN-005 exige o resumo de features adicionadas/removidas — que é exatamente o que o diff entrega; "alteradas" fica reservado para quando houver identificador estável de feature (evolução futura). Anotar isso no PHPDoc do método e no SUMMARY. (A lógica de contagem é pura/consultável; testável.)

    RED:
    - Unit `tests/Unit/Geo/GeoLayerServiceTest.php` (SQLite — sem geometria, só linhas de geo_layers): `openVersion` cria a vigente; uma 2ª `openVersion` do mesmo type fecha a 1ª (`valid_to` preenchido, status substituida) e cria a nova vigente; idempotência por (type,version) não duplica. `computeDiff` conta corretamente. (Não usa PostGIS — apenas linhas de geo_layers, que existem em SQLite.)
    Rodar (RED). GREEN. REFACTOR.
  </action>
  <verify>php artisan test --compact tests/Unit/Geo/GeoLayerServiceTest.php</verify>
  <acceptance_criteria>
    - `rg "function openVersion|function computeDiff|function auditLoad" app/Services/Geo/GeoLayerService.php` casa os três.
    - `rg "status.*substituida|valid_to" app/Services/Geo/GeoLayerService.php` casa (fecha a anterior).
    - `rg "carga-camada" app/Services/Geo/GeoLayerService.php` casa.
    - `php artisan test --compact tests/Unit/Geo/GeoLayerServiceTest.php` verde.
  </acceptance_criteria>
  <done>Versionamento com vigência implementado: nova versão fecha a anterior sem apagá-la; diff calculado; carga auditável. Provado em SQLite (lógica de linhas, sem geometria).</done>
</task>

<task type="tdd">
  <name>Task 2: GeoJsonLayerImporter + comando geo:importar (idempotente, auditado) — TDD @group postgis</name>
  <requirements>HU-036, HU-034, HU-035, HU-032</requirements>
  <read_first>
    - app/Console/Commands/ImportCnaeCommand.php (padrão de comando + relatório pt-BR + exit codes)
    - app/Services/Geo/GeoLayerService.php (Task 1)
    - .planning/phases/04-georreferenciamento-e-territorio/04-RESEARCH.md (Code Examples → Import de GeoJSON)
    - tests/PostgisTestCase.php
  </read_first>
  <action>
    Criar `app/Services/Geo/GeoJsonLayerImporter.php`:
    - `import(GeoLayerType $type, string $version, string $source, array $featureCollection): array` — valida que é `FeatureCollection` com `features[]`; via `GeoLayerService::openVersion`; itera `features[]` (Pitfall 6 — UMA geometria por insert) e insere em chunks por transação:
    ```php
    DB::table('geo_features')->insert([
        'geo_layer_id' => $layer->id,
        'geometry' => DB::raw('ST_MakeValid(ST_SetSRID(ST_GeomFromGeoJSON('
            . DB::connection()->getPdo()->quote(json_encode($feature['geometry'])) . '), 4326))'),
        'properties' => json_encode($feature['properties'] ?? []),
        'created_at' => now(), 'updated_at' => now(),
    ]);
    ```
    (origem já em 4326 → sem ST_Transform; se a fonte vier em 4674/31984, documentar uso de ST_Transform(...,4326)). Features com geometria nula/ inválida são registradas no relatório (nunca descartadas em silêncio — Pitfall 8). Ao final: `finalizeCount` + `auditLoad` com o diff. Retorna relatório `['lidas','inseridas','invalidas'=>[],'version','feature_count']`.

    Criar `app/Console/Commands/ImportGeoLayerCommand.php` (signature `geo:importar {type} {arquivo : caminho do GeoJSON} {--version= : versão da camada} {--source= : origem/URL}`): valida type contra GeoLayerType; valida arquivo existe e é JSON válido (exit FAILURE com mensagem pt-BR senão); chama o importer; imprime relatório pt-BR (Lidas/Inseridas/Inválidas). Auto-descoberto (sem registro manual).

    RED → `tests/Feature/Geo/ImportGeoLayerTest.php` (`#[Group('postgis')]`, estende PostgisTestCase): usa o fixture `tests/Fixtures/geo/bairros-amostra.geojson` (criar nesta task: 2–3 bairros REAIS de Salvador extraídos do GeoSalvador em 4326 — polígonos pequenos e válidos). Casos: (a) importa → cria GeoLayer vigente + N geo_features com geometria (assert via `SELECT COUNT(*)` e `ST_IsValid`); (b) re-importar a MESMA (type,version) é idempotente (não duplica features — limpa/recria as features da versão); (c) importar uma NOVA versão fecha a anterior (valid_to preenchido) e mantém as features antigas (assert 2 layers, a antiga substituida); (d) activity 'territorio'/'carga-camada' com properties.diff e rules_version = versão. Rodar `--group postgis` (RED). GREEN. REFACTOR + pint.

    Nota de idempotência: ao reimportar (type,version) existente, o importer deve apagar as features daquela versão e reinserir (idempotência real), OU detectar e pular. Escolher apagar+reinserir (carga reproduzível) e documentar.
  </action>
  <verify>php artisan test --compact --group postgis tests/Feature/Geo/ImportGeoLayerTest.php</verify>
  <acceptance_criteria>
    - `rg "ST_MakeValid\(ST_SetSRID\(ST_GeomFromGeoJSON" app/Services/Geo/GeoJsonLayerImporter.php` casa.
    - `rg "geo:importar" app/Console/Commands/ImportGeoLayerCommand.php` casa; `php artisan list | rg "geo:importar"` lista o comando.
    - `test -f tests/Fixtures/geo/bairros-amostra.geojson` verdadeiro e contém `"type": "FeatureCollection"`.
    - `php artisan test --compact --group postgis tests/Feature/Geo/ImportGeoLayerTest.php` verde (idempotência + versionamento + diff auditado).
  </acceptance_criteria>
  <done>Comando geo:importar carrega GeoJSON oficial em geo_features, é idempotente por (type,version), preserva versões anteriores e audita o diff — provado contra PostGIS real com dado real de amostra.</done>
</task>

<task type="auto">
  <name>Task 3: Carga real GeoSalvador (database/data/geo/) + GeoLayerSeeder driver-aware + camadas pendentes</name>
  <requirements>HU-034, HU-035, HU-032, HU-031, HU-033, HU-036</requirements>
  <read_first>
    - database/data/cnaes-subclasses-2-3.csv (precedente de dado oficial versionado em database/data/)
    - database/seeders/DatabaseSeeder.php (ordem de seeders)
    - app/Services/Geo/GeoJsonLayerImporter.php (Task 2)
    - app/Enums/GeoLayerType.php (isBlockedSource)
    - tests/Feature/Seeders/DatabaseSeederTest.php (roda o seed completo em SQLite — o GeoLayerSeeder NÃO pode quebrar)
  </read_first>
  <action>
    Baixar dados OFICIAIS REAIS do GeoSalvador (em 4326) e versioná-los em `database/data/geo/`:
    - `bairros.geojson` — completo (`.../Bairros/MapServer/0/query?where=1=1&outFields=*&f=geojson&outSR=4326`, ~160 features).
    - `restricoes-ambientais.geojson` — extrato real de uma camada de restrição do PDDU_2016 (ex.: ZEIS_Emendas_region ou Savam_*), em 4326.
    - `vias.geojson` — extrato REAL e LIMITADO de logradouros (ex.: as vias de poucos bairros), em 4326 (o dataset completo é grande demais para versionar; o extrato é dado oficial real, não inventado — "muda a carga, nunca a lógica").
    Comando de referência por camada (validar a URL antes — endpoints podem mudar de estrutura): `curl -sG '<MapServer/0>/query' --data-urlencode 'where=1=1' --data-urlencode 'outFields=*' --data-urlencode 'f=geojson' --data-urlencode 'outSR=4326' -o database/data/geo/<arquivo>.geojson`. Usar `resultOffset`/`resultRecordCount` se a camada exceder 2000 (Pitfall 11).

    REPRODUTIBILIDADE (sem dependência de rede no CI): o GeoJSON é buscado UMA vez e **versionado/commitado** em `database/data/geo/` (snapshot oficial — precedente do CSV de CNAEs em `database/data/`). A carga (seed, testes @group postgis, CI) lê o arquivo COMMITADO; nunca busca da rede em tempo de teste.

    REGRA SEM FACHADA, por camada:
    - **bairro (HU-034) é REQUISITO RÍGIDO**: `database/data/geo/bairros.geojson` real DEVE existir e ser commitado. Se o endpoint estiver indisponível na execução e não houver snapshot, é **BLOQUEIO EXPLÍCITO da fase** (bairro real não provado) — registrar no SUMMARY (e o orquestrador no STATE/ROADMAP); NUNCA marcar skip nem inventar polígono. A fase NÃO fecha sem bairro real.
    - **via/restrição**: se o endpoint estiver indisponível, seedar a camada como `pendente_fonte` e registrar no SUMMARY (degradação comunicada aceitável — secundárias nesta fase).
    - **zona/lote**: sempre `pendente_fonte` (sem fonte pública confirmada).

    Criar `database/seeders/GeoLayerSeeder.php` (guard de driver — SQLite não executa PostGIS; é seed de DEV que roda no Postgres real):
    ```php
    public function run(): void
    {
        if (DB::getDriverName() !== 'pgsql') {
            // Em SQLite (testes) PostGIS não existe; a carga real é exercida pelos testes @group postgis.
            $this->seedPendingBlockedLayers(); // ainda cria as linhas pendente_fonte (sem geometria) — comunica o bloqueio
            return;
        }
        // bairro/via/restricao: importa de database/data/geo/*.geojson via GeoJsonLayerImporter (quando o arquivo existir)
        // zona/lote: seedPendingBlockedLayers()
    }
    ```
    `seedPendingBlockedLayers()`: cria/garante (firstOrCreate por type+version, idempotente) uma GeoLayer para `GeoLayerType::Zona` e `GeoLayerType::Lote` com `status = pendente_fonte`, `feature_count = 0`, `source = 'pendente-sedur'`, version `'pendente-sedur'`. Isto comunica o bloqueio na UI (04-07) sem inventar geometria. As linhas pendentes NÃO têm geo_features → podem ser criadas em qualquer driver (não dependem de PostGIS).

    Registrar `GeoLayerSeeder::class` em `DatabaseSeeder::run()` (após CnaeSeeder, antes/depois de CompanySeeder — sem dependência entre eles).

    Testes:
    - RED → `tests/Feature/Geo/GeoLayerSeederTest.php` com DOIS cenários:
      (a) SQLite (TestCase normal): seedar GeoLayerSeeder cria as linhas pendente_fonte de zona e lote (feature_count 0) e NÃO quebra (sem PostGIS); confirma que `GeoLayer::where('type','zona')->where('status','pendente_fonte')->exists()`.
      (b) `#[Group('postgis')]` (estende PostgisTestCase): após seed, bairro tem features reais (`GeoFeature::whereHas('layer', fn ($q) => $q->where('type', 'bairro'))->count() > 0`) carregadas do snapshot COMMITADO `database/data/geo/bairros.geojson`. Se o snapshot de bairro NÃO existir, o teste **FALHA** (bloqueio da fase — bairro real não provado), NUNCA markTestSkipped. (via/restrição podem estar pendente_fonte se o snapshot não existir; bairro não.)
    - Confirmar que `DatabaseSeederTest` (SQLite, seed completo) continua verde com o GeoLayerSeeder registrado (as linhas pendentes não afetam as contagens de Parameter/Cnae/Company; ajustar o teste só se necessário para tolerar as novas linhas de geo_layers).
    Rodar. GREEN. REFACTOR + pint.
  </action>
  <verify>php artisan test --compact tests/Feature/Geo/GeoLayerSeederTest.php tests/Feature/Seeders/DatabaseSeederTest.php && php artisan test --compact --group postgis tests/Feature/Geo/GeoLayerSeederTest.php</verify>
  <acceptance_criteria>
    - `test -f database/data/geo/bairros.geojson` verdadeiro e o arquivo contém `"FeatureCollection"` (dado real do GeoSalvador).
    - `rg "pendente_fonte|seedPendingBlockedLayers" database/seeders/GeoLayerSeeder.php` casa.
    - `rg "DB::getDriverName\(\) !== 'pgsql'" database/seeders/GeoLayerSeeder.php` casa (guard).
    - `rg "GeoLayerSeeder::class" database/seeders/DatabaseSeeder.php` casa.
    - `php artisan test --compact tests/Feature/Seeders/DatabaseSeederTest.php` verde (sem regressão do seed completo em SQLite).
    - `database/data/geo/bairros.geojson` está COMMITADO (snapshot oficial) — a carga não depende de rede em teste/CI.
    - Cenário @group postgis: bairro com features reais (do snapshot commitado); zona/lote pendentes. Bairro é requisito rígido — snapshot ausente FAZ O TESTE FALHAR (bloqueio da fase), nunca skip silencioso.
  </acceptance_criteria>
  <done>Dados oficiais reais do GeoSalvador versionados e carregáveis; o seed de dev carrega bairro/via/restrição reais no Postgres e marca zona/lote como pendente_fonte; SQLite (testes) não quebra. Bloqueio comunicado, sem fachada.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --exclude-group postgis tests/Unit/Geo tests/Feature/Geo tests/Feature/Seeders` verde (suíte SQLite intacta).
- `php artisan test --compact --group postgis tests/Feature/Geo/ImportGeoLayerTest.php tests/Feature/Geo/GeoLayerSeederTest.php` verde (carga real contra PostGIS).
- `php artisan geo:importar bairro database/data/geo/bairros.geojson --version=geosalvador-2024 --source=GeoSalvador` (em dev Postgres) reporta features inseridas.
</verification>

<success_criteria>
- HU-036 RN-004/RN-005: versionamento com vigência + diff auditado provados.
- Carga real de bairro/via/restrição (dado oficial GeoSalvador) no seed de dev.
- Zona e lote seedadas como pendente_fonte (bloqueio comunicado, sem polígono inventado).
- Comando geo:importar idempotente e auditado.
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/04-georreferenciamento-e-territorio/04-04-SUMMARY.md`: URLs exatas usadas e quais camadas foram efetivamente carregadas (e quais ficaram pendente_fonte), assinatura do GeoJsonLayerImporter/GeoLayerService (consumidos por 04-05), nome do comando, e a observação de bloqueio de zona/lote para o STATE/ROADMAP (registrado pelo orquestrador no fechamento).
</output>

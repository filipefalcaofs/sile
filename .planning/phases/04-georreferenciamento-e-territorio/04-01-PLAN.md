---
phase: 04-georreferenciamento-e-territorio
plan: "04-01"
type: execute
wave: 1
depends_on: []
requirements: [HU-036, HU-031, HU-033]
files_modified:
  - database/migrations/XXXX_create_geo_layers_table.php
  - database/migrations/XXXX_create_geo_features_table.php
  - app/Models/GeoLayer.php
  - app/Models/GeoFeature.php
  - app/Enums/GeoLayerType.php
  - app/Enums/GeoLayerStatus.php
  - database/factories/GeoLayerFactory.php
  - database/factories/GeoFeatureFactory.php
  - config/database.php
  - phpunit.xml
  - .env.example
  - .env
  - app/Console/Commands/PrepareGeoTestDatabaseCommand.php
  - tests/PostgisTestCase.php
  - tests/Unit/Geo/GeoLayerTest.php
  - tests/Feature/Geo/GeoSchemaTest.php
  - tests/Feature/Geo/PostgisSmokeTest.php
autonomous: true

must_haves:
  truths:
    - "As tabelas geo_layers e geo_features existem e migram em SQLite (suíte atual) sem quebrar os 385 testes"
    - "No PostgreSQL a coluna geometry(Geometry,4326) e o índice GiST existem; ST_Contains e ST_Area funcionam sobre um polígono inserido"
    - "O grupo @group postgis EXECUTA de verdade com o Postgres de pé: skip silencioso é proibido — servidor alcançável + erro vira FALHA (nunca passa sem rodar SQL espacial)"
    - "Existe um preflight executável (geo:preparar-banco-de-testes) que cria o banco sile_testing + extensão postgis no Postgres de dev (porta 5433)"
    - "Camada é dado versionado com vigência: GeoLayer resolve a versão vigente e a versão de uma data passada"
    - "Camadas sem fonte pública (zona, lote) são representáveis como status pendente_fonte, sem features"
  artifacts:
    - path: "database/migrations/XXXX_create_geo_features_table.php"
      provides: "coluna geometry(Geometry,4326) + properties jsonb; índice GiST só em pgsql"
      contains: "DB::getDriverName() === 'pgsql'"
    - path: "app/Models/GeoLayer.php"
      provides: "modelo versionado com scopes vigente()/naData()"
      exports: ["scopeVigente", "scopeNaData"]
    - path: "app/Enums/GeoLayerType.php"
      provides: "enum bairro|zona|via|lote|restricao"
      contains: "case Bairro"
    - path: "app/Console/Commands/PrepareGeoTestDatabaseCommand.php"
      provides: "preflight idempotente: CREATE DATABASE sile_testing + CREATE EXTENSION postgis"
      contains: "geo:preparar-banco-de-testes"
    - path: "tests/PostgisTestCase.php"
      provides: "base @group postgis com guard de honestidade (fail-not-skip quando o servidor está de pé)"
      contains: "markTestSkipped"
  key_links:
    - from: "tests/PostgisTestCase.php"
      to: "config('database.default')"
      via: "createApplication define pgsql_testing"
      pattern: "database.default.*pgsql_testing"
    - from: "tests/PostgisTestCase.php"
      to: "servidor Postgres de teste"
      via: "fsockopen — servidor de pé exige execução (fail), não skip"
      pattern: "fsockopen|POSTGIS_TESTS_REQUIRED"
    - from: "database/migrations/XXXX_create_geo_features_table.php"
      to: "índice GiST"
      via: "DB::statement guardado por driver"
      pattern: "USING GIST"
---

<objective>
Estabelecer a fundação espacial do EP04: o esquema de **camadas geográficas versionadas** (`geo_layers` + `geo_features`) com coluna PostGIS `geometry(Geometry,4326)` e índice GiST, os modelos/enums correspondentes e a **infraestrutura de teste PostGIS** (conexão dedicada + base `PostgisTestCase`) — tudo **driver-aware** para não quebrar os 385 testes que rodam em SQLite `:memory:`.

Purpose: É o alicerce de HU-036 (RN-004 dados versionados com vigência), HU-031 e HU-033 (modelagem honesta das camadas bloqueadas zona/lote como `pendente_fonte`, sem polígono inventado) e de toda a identificação espacial das Fases seguintes. Sem este esquema e sem a estratégia de teste PostGIS resolvida, nenhuma consulta `ST_*` pode ser provada "sem fachada".
Output: 2 migrations driver-aware, modelos `GeoLayer`/`GeoFeature`, enums `GeoLayerType`/`GeoLayerStatus`, factories, conexão `pgsql_testing`, `PostgisTestCase` e um smoke `@group postgis` que prova o stack PostGIS de ponta a ponta.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/04-georreferenciamento-e-territorio/04-CONTEXT.md
@.planning/phases/04-georreferenciamento-e-territorio/04-RESEARCH.md
@database/migrations/2026_06_12_035745_create_companies_table.php
@app/Models/Company.php
@app/Enums/CompanySource.php
@config/database.php
@phpunit.xml

Decisões travadas relevantes (CONTEXT + RESEARCH):
- SRID 4326 fixo; coluna `geometry(Geometry,4326)` GENÉRICA (aceita Polygon, MultiPolygon e LineString — bairro/zona/lote são polígonos, via é LineString).
- Índice GiST e qualquer DDL PostGIS SÓ em `pgsql` (Pitfall 1): em SQLite a coluna `geometry` compila como tipo genérico e as tabelas são criadas, mas o índice GiST quebraria a suíte.
- Carga de nova versão NÃO apaga a anterior; consulta operacional usa a vigente (`valid_to IS NULL`), reprodução usa a versão da data da decisão.
- Sem dependência nova no backend (PostGIS já habilitado em `docker/postgres/init/01-extensions.sql`).
- Camada bloqueada (zona/lote, sem fonte pública) é modelada como `status = pendente_fonte` com `feature_count = 0` — NUNCA polígono inventado.

Ambiente de banco (fatos do `.env`/`docker-compose.yml`):
- DEV usa **pgsql** real: host `127.0.0.1`, **porta 5433** (container `sile-pgsql`, imagem `sile/pgsql:16-postgis-pgvector`), banco `sile`, user `sile`, senha `secret`. PostGIS já habilitado no banco `sile` pelo init do container.
- A suíte PHPUnit usa **SQLite `:memory:`** (phpunit.xml) — isso vale SÓ para o PHPUnit. Comandos Artisan (`migrate`, `migrate:fresh`) leem o `.env` e batem no **pgsql `sile` (dev)** — NUNCA rodar `migrate:fresh` "de teste" via Artisan (apagaria o banco de dev). A migração em `:memory:` é provada pelo PHPUnit (RefreshDatabase), não pelo Artisan.
- O banco de teste espacial `sile_testing` **NÃO existe** por padrão (o container só cria `sile`) — este plano adiciona o preflight que o cria.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration geo_layers + enums + modelo GeoLayer (camada versionada) — TDD</name>
  <requirements>HU-036 (RN-004), HU-031, HU-033</requirements>
  <read_first>
    - app/Enums/CompanySource.php (padrão de enum string com label())
    - app/Models/Company.php (padrão de model: #[Fillable], casts(), HasFactory)
    - database/migrations/2026_06_12_035745_create_companies_table.php (padrão de migration)
  </read_first>
  <action>
    Criar `app/Enums/GeoLayerType.php` (enum string: `Bairro='bairro'`, `Zona='zona'`, `Via='via'`, `Lote='lote'`, `Restricao='restricao'`) com método `label(): string` em pt-BR. Adicionar método `isBlockedSource(): bool` retornando true para `Zona` e `Lote` (camadas sem fonte pública confirmada na pesquisa — usado pelo seeder e pela UI; é classificação de dado, não decisão hardcoded de negócio).

    Criar `app/Enums/GeoLayerStatus.php` (enum string: `Vigente='vigente'`, `Substituida='substituida'`, `PendenteFonte='pendente_fonte'`) com `label()`.

    Criar migration `create_geo_layers_table`:
    ```php
    Schema::create('geo_layers', function (Blueprint $table) {
        $table->id();
        $table->string('type');            // GeoLayerType
        $table->string('version');         // ex.: 'geosalvador-2024-06'
        $table->string('status')->default('vigente'); // GeoLayerStatus
        $table->date('valid_from')->nullable();
        $table->date('valid_to')->nullable();          // NULL = vigente
        $table->string('source')->nullable();          // origem (URL/órgão); 'pendente-sedur' nas bloqueadas
        $table->string('rules_version')->nullable();    // versão auditável da carga
        $table->unsignedInteger('feature_count')->default(0);
        $table->timestamps();
        $table->unique(['type', 'version']);            // carga idempotente por (type, version)
        $table->index(['type', 'valid_to']);            // consulta da vigente
    });
    ```

    Criar `app/Models/GeoLayer.php` com `HasAuditoria`, `HasFactory`, `#[Fillable([...])]` (type, version, status, valid_from, valid_to, source, rules_version, feature_count), `casts()` (`type` => GeoLayerType::class, `status` => GeoLayerStatus::class, `valid_from`/`valid_to` => 'date'), relação `features(): HasMany`, e scopes:
    - `scopeVigente(Builder $q, GeoLayerType|string $type)`: `where('type', ...)->whereNull('valid_to')`.
    - `scopeNaData(Builder $q, GeoLayerType|string $type, CarbonInterface $date)`: `where('type', ...)->where('valid_from','<=',$date)->where(fn($q)=>$q->whereNull('valid_to')->orWhere('valid_to','>',$date))`.

    Criar `database/factories/GeoLayerFactory.php` (states: `vigente()`, `pendenteFonte()` com feature_count 0 + status pendente_fonte + source 'pendente-sedur').

    RED → escrever `tests/Unit/Geo/GeoLayerTest.php` ANTES: enum label/isBlockedSource; casts; `GeoLayer::vigente('bairro')` retorna só a de `valid_to` nulo; `GeoLayer::naData('bairro', '2020-01-01')` retorna a versão vigente naquela data e não a posterior. Rodar e confirmar que falha pelo motivo certo (classe/migration inexistente). GREEN → implementar. REFACTOR.
  </action>
  <verify>php artisan test --compact tests/Unit/Geo/GeoLayerTest.php</verify>
  <acceptance_criteria>
    - `rg "case Bairro = 'bairro'" app/Enums/GeoLayerType.php` casa.
    - `rg "pendente_fonte" app/Enums/GeoLayerStatus.php` casa.
    - `rg "unique\(\['type', 'version'\]\)" database/migrations/*_create_geo_layers_table.php` casa.
    - `rg "scopeVigente|scopeNaData" app/Models/GeoLayer.php` casa ambos.
    - `php artisan test --compact tests/Unit/Geo/GeoLayerTest.php` verde.
  </acceptance_criteria>
  <done>Esquema e modelo de camada versionada existem; a seleção da versão vigente e da versão por data é provada por teste em SQLite (lógica de vigência é pura, sem PostGIS).</done>
</task>

<task type="auto">
  <name>Task 2: Migration geo_features (geometry 4326 + GiST driver-aware) + modelo GeoFeature — TDD</name>
  <requirements>HU-036</requirements>
  <read_first>
    - app/Models/GeoLayer.php (criado na Task 1 — relação features())
    - .planning/phases/04-georreferenciamento-e-territorio/04-RESEARCH.md (seção "Migration — colunas geometry + índice GiST driver-aware")
    - phpunit.xml (DB_CONNECTION sqlite :memory:)
  </read_first>
  <action>
    Criar migration `create_geo_features_table`:
    ```php
    public function up(): void
    {
        Schema::create('geo_features', function (Blueprint $table) {
            $table->id();
            $table->foreignId('geo_layer_id')->constrained()->cascadeOnDelete();
            // Coluna GENÉRICA: aceita Polygon, MultiPolygon e LineString; SRID 4326 fixo.
            $table->geometry('geometry', subtype: 'geometry', srid: 4326);
            $table->jsonb('properties')->nullable();
            $table->timestamps();
        });

        // Índice espacial SÓ no PostgreSQL — mantém a suíte SQLite verde (Pitfall 1).
        if (DB::getDriverName() === 'pgsql') {
            DB::statement('CREATE INDEX geo_features_geometry_gist ON geo_features USING GIST (geometry)');
        }
    }
    ```
    `down()`: `Schema::dropIfExists('geo_features')`.

    Criar `app/Models/GeoFeature.php` (HasFactory; `#[Fillable(['geo_layer_id','properties'])]` — geometry é gravada via DB::raw/ST_*, NUNCA mass-assign; `casts()` `properties` => 'array'; relação `layer(): BelongsTo`). NÃO castar `geometry` (é tipo PostGIS bruto; leitura usa `ST_AsGeoJSON` quando necessário).

    Criar `database/factories/GeoFeatureFactory.php` (gera apenas geo_layer_id + properties; a geometria é responsabilidade dos testes @group postgis via insert SQL).

    RED → `tests/Feature/Geo/GeoSchemaTest.php` (roda na suíte SQLite default, com RefreshDatabase — é o RefreshDatabase do PHPUnit que prova a migração em `:memory:`): asserta que `Schema::hasTable('geo_features')` e `Schema::hasColumns('geo_features', ['geo_layer_id','geometry','properties'])`; cria um GeoLayer + GeoFeature (sem geometria) e confirma a relação `layer()`/`features()`. Confirma que migrar NÃO lança em SQLite (o índice GiST não é criado). Rodar (RED por tabela/coluna inexistente). GREEN → implementar. REFACTOR.

    NÃO usar `php artisan migrate:fresh` para "provar a migração SQLite": Artisan lê o `.env` (pgsql `sile`, porta 5433) e apagaria o banco de DEV. A prova da migração em `:memory:` é o próprio GeoSchemaTest (RefreshDatabase).
  </action>
  <verify>php artisan test --compact tests/Feature/Geo/GeoSchemaTest.php</verify>
  <acceptance_criteria>
    - `rg "subtype: 'geometry', srid: 4326" database/migrations/*_create_geo_features_table.php` casa.
    - `rg "DB::getDriverName\(\) === 'pgsql'" database/migrations/*_create_geo_features_table.php` casa e o `CREATE INDEX ... USING GIST` está dentro do guard.
    - `php artisan test --compact tests/Feature/Geo/GeoSchemaTest.php` verde (a migração em `:memory:` é provada aqui, sem tocar o banco de dev).
  </acceptance_criteria>
  <done>Tabela geo_features com geometry(4326) existe; migra em SQLite sem GiST (suíte intacta) e o índice GiST é criado apenas no PostgreSQL.</done>
</task>

<task type="auto">
  <name>Task 3: Conexão pgsql_testing + DB_TEST_* no .env + preflight executável (cria sile_testing)</name>
  <requirements>HU-036</requirements>
  <read_first>
    - config/database.php (conexão pgsql existente — host 127.0.0.1, porta via env, espelhar)
    - .env (DB_CONNECTION=pgsql, DB_HOST=127.0.0.1, DB_PORT=5433, DB_DATABASE=sile, DB_USERNAME=sile, DB_PASSWORD=secret)
    - .env.example (template versionado)
    - app/Console/Commands/ImportCnaeCommand.php (padrão de comando Artisan)
    - phpunit.xml (env de teste)
  </read_first>
  <action>
    Em `config/database.php`, adicionar a conexão `pgsql_testing` (espelha `pgsql`, lê env `DB_TEST_*` com defaults para o Postgres de DEV — porta 5433):
    ```php
    'pgsql_testing' => [
        'driver' => 'pgsql',
        'host' => env('DB_TEST_HOST', env('DB_HOST', '127.0.0.1')),
        'port' => env('DB_TEST_PORT', env('DB_PORT', '5433')),
        'database' => env('DB_TEST_DATABASE', 'sile_testing'),
        'username' => env('DB_TEST_USERNAME', env('DB_USERNAME', 'sile')),
        'password' => env('DB_TEST_PASSWORD', env('DB_PASSWORD', 'secret')),
        'charset' => 'utf8',
        'prefix' => '',
        'prefix_indexes' => true,
        'search_path' => 'public',
        'sslmode' => env('DB_SSLMODE', 'prefer'),
    ],
    ```

    Adicionar ao `.env` (local, NÃO versionado) E ao `.env.example` (versionado) o bloco DB_TEST_* + a flag de enforcement:
    ```dotenv
    # Banco de testes espaciais (PostGIS). O grupo `--group postgis` roda aqui,
    # reutilizando o container de dev (porta 5433). O banco sile_testing é criado
    # pelo preflight: `php artisan geo:preparar-banco-de-testes`.
    DB_TEST_HOST=127.0.0.1
    DB_TEST_PORT=5433
    DB_TEST_DATABASE=sile_testing
    DB_TEST_USERNAME=sile
    DB_TEST_PASSWORD=secret
    # Com o servidor de pé o grupo postgis NUNCA é pulado em silêncio; o CI define
    # POSTGIS_TESTS_REQUIRED=true para transformar qualquer skip em FALHA.
    POSTGIS_TESTS_REQUIRED=false
    ```

    Em `phpunit.xml`, adicionar SOMENTE um comentário de que o grupo `postgis` roda sob `pgsql_testing` (lendo DB_TEST_*); NÃO trocar o `DB_CONNECTION` global (segue SQLite `:memory:`).

    Criar `app/Console/Commands/PrepareGeoTestDatabaseCommand.php` — preflight idempotente e executável (o dev tem o container postgis na 5433; resolve W2: o banco sile_testing não existe por padrão):
    ```php
    protected $signature = 'geo:preparar-banco-de-testes';
    protected $description = 'Cria o banco de testes espaciais (sile_testing) + extensão postgis — idempotente';

    public function handle(): int
    {
        $testDb = (string) config('database.connections.pgsql_testing.database');
        // SELECT pode ser preparado; CREATE DATABASE não roda em transação nem como
        // prepared statement → unprepared. Conecta no 'pgsql' (banco sile) para criar.
        $exists = DB::connection('pgsql')->selectOne('SELECT 1 FROM pg_database WHERE datname = ?', [$testDb]);
        if ($exists === null) {
            DB::connection('pgsql')->unprepared('CREATE DATABASE "'.$testDb.'"');
            $this->info("Banco {$testDb} criado.");
        } else {
            $this->line("Banco {$testDb} já existe.");
        }
        DB::connection('pgsql_testing')->statement('CREATE EXTENSION IF NOT EXISTS postgis');
        $this->info("Extensão postgis garantida em {$testDb}.");

        return self::SUCCESS;
    }
    ```
    (Auto-descoberto; idempotente — rodar várias vezes não duplica nem apaga. NÃO toca o banco `sile`.)
  </action>
  <verify>php artisan geo:preparar-banco-de-testes && php artisan tinker --execute 'dd(DB::connection("pgsql_testing")->selectOne("SELECT postgis_version()"));'</verify>
  <acceptance_criteria>
    - `rg "'pgsql_testing'" config/database.php` casa e usa `DB_TEST_*` com porta default `5433`.
    - `rg "DB_TEST_DATABASE=sile_testing" .env.example` casa (e foi adicionado também ao `.env` local).
    - `rg "geo:preparar-banco-de-testes" app/Console/Commands/PrepareGeoTestDatabaseCommand.php` casa; `php artisan list | rg "geo:preparar-banco-de-testes"` lista o comando.
    - `php artisan geo:preparar-banco-de-testes` conclui com sucesso e `SELECT postgis_version()` em `pgsql_testing` retorna a versão (banco de teste pronto, com PostGIS).
  </acceptance_criteria>
  <done>O banco de testes espaciais sile_testing existe com PostGIS via um preflight executável e idempotente; a conexão pgsql_testing aponta para o container de dev (5433). Sem isto o grupo postgis não teria onde rodar.</done>
</task>

<task type="auto">
  <name>Task 4: PostgisTestCase com guard de honestidade (fail-not-skip) + smoke @group postgis (ST_Contains + ST_Area) — TDD</name>
  <requirements>HU-036</requirements>
  <read_first>
    - tests/TestCase.php (base do projeto)
    - app/Console/Commands/PrepareGeoTestDatabaseCommand.php (Task 3 — preflight)
    - config/database.php (conexão pgsql_testing — Task 3)
    - .planning/phases/04-georreferenciamento-e-territorio/04-RESEARCH.md (seção "Estratégia de testes PostGIS")
  </read_first>
  <action>
    Criar `tests/PostgisTestCase.php` (base dos testes espaciais) com **guard de honestidade** — resolve W2/W3a: skip silencioso é PROIBIDO quando o servidor está de pé:
    ```php
    abstract class PostgisTestCase extends TestCase
    {
        use RefreshDatabase;

        protected function createApplication()
        {
            $app = parent::createApplication();
            // Aponta a conexão default para o Postgres+PostGIS ANTES do migrate (setUpTraits).
            $app['config']->set('database.default', 'pgsql_testing');
            return $app;
        }

        // RefreshDatabase chama este hook ANTES de migrar.
        protected function beforeRefreshingDatabase(): void
        {
            $conn = config('database.connections.pgsql_testing');
            $required = filter_var(env('POSTGIS_TESTS_REQUIRED', false), FILTER_VALIDATE_BOOL);

            // 1) Servidor Postgres alcançável? Distingue "máquina sem Postgres"
            //    de "servidor de pé mas mal configurado".
            $socket = @fsockopen($conn['host'], (int) $conn['port'], $errno, $errstr, 2);
            if ($socket === false) {
                $msg = "Servidor Postgres de teste inacessível em {$conn['host']}:{$conn['port']} ({$errstr}).";
                if ($required) {
                    $this->fail("POSTGIS_TESTS_REQUIRED=true mas {$msg} O grupo postgis NÃO pode ser pulado neste ambiente.");
                }
                $this->markTestSkipped("{$msg} Suba o container de dev (porta 5433) para executar os testes espaciais.");
            }
            fclose($socket);

            // 2) Servidor de pé → execução OBRIGATÓRIA. Qualquer falha aqui é FALHA, nunca skip.
            try {
                DB::connection('pgsql_testing')->statement('CREATE EXTENSION IF NOT EXISTS postgis');
            } catch (\Throwable $e) {
                $this->fail(
                    "Servidor Postgres de pé mas o banco de teste espacial não está pronto. "
                    ."Rode 'php artisan geo:preparar-banco-de-testes'. Erro: ".$e->getMessage()
                );
            }
        }
    }
    ```
    Anotar a classe/filhos com `#[Group('postgis')]` (PHPUnit 12). PHPDoc deixando explícito: "Sem fachada — markTestSkipped SÓ quando NÃO há servidor Postgres (máquina sem o container). Com o servidor alcançável, qualquer problema é FALHA, não skip. CI define POSTGIS_TESTS_REQUIRED=true."

    RED → `tests/Feature/Geo/PostgisSmokeTest.php` (`#[Group('postgis')]`, estende `PostgisTestCase`): cria um GeoLayer (bairro, vigente) + insere UM GeoFeature com geometria via SQL real:
    ```php
    DB::table('geo_features')->insert([
        'geo_layer_id' => $layer->id,
        'geometry' => DB::raw("ST_SetSRID(ST_GeomFromText('POLYGON((-38.6 -13.0, -38.4 -13.0, -38.4 -12.9, -38.6 -12.9, -38.6 -13.0))'), 4326)"),
        'properties' => json_encode(['NOME_BAIRRO' => 'TESTE']),
        'created_at' => now(), 'updated_at' => now(),
    ]);
    ```
    Asserta SQL espacial REAL (ponto contido, ponto fora, e área > 0 — W2 pede um ST_Area real):
    ```php
    $dentro = DB::selectOne("SELECT COUNT(*) c FROM geo_features WHERE ST_Contains(geometry, ST_SetSRID(ST_MakePoint(-38.5, -12.95), 4326))");
    $this->assertSame(1, (int) $dentro->c);
    $fora = DB::selectOne("SELECT COUNT(*) c FROM geo_features WHERE ST_Contains(geometry, ST_SetSRID(ST_MakePoint(0, 0), 4326))");
    $this->assertSame(0, (int) $fora->c);
    $area = DB::selectOne('SELECT ST_Area(geometry) AS a FROM geo_features LIMIT 1');
    $this->assertGreaterThan(0, (float) $area->a);
    ```
    Confirma também que o índice GiST existe: `SELECT indexname FROM pg_indexes WHERE tablename='geo_features' AND indexdef ILIKE '%gist%'`.
    Rodar `php artisan geo:preparar-banco-de-testes` e depois `php artisan test --group postgis` (RED até a conexão/migrations PostGIS estarem corretas). GREEN. Com o container de dev de pé, este teste DEVE reportar PASSOU (executou o SQL) — se aparecer "skipped" com o servidor de pé, o guard está errado.
  </action>
  <verify>php artisan geo:preparar-banco-de-testes && php artisan test --compact --group postgis tests/Feature/Geo/PostgisSmokeTest.php</verify>
  <acceptance_criteria>
    - `rg "fsockopen|POSTGIS_TESTS_REQUIRED" tests/PostgisTestCase.php` casa (guard de honestidade) e há `->fail(` no caminho "servidor de pé + erro".
    - `rg "ST_Contains\(geometry, ST_SetSRID\(ST_MakePoint" tests/Feature/Geo/PostgisSmokeTest.php` casa e há um `ST_Area(` real asseverado.
    - PROVA DE EXECUÇÃO (não skip): com o container de dev de pé + preflight rodado, `php artisan test --compact --group postgis tests/Feature/Geo/PostgisSmokeTest.php` reporta o teste como **PASSOU** (1+ assertions executadas), NÃO "skipped". Registrar a linha de resultado no SUMMARY.
    - PROVA DO GUARD: derrubar/renomear o banco `sile_testing` com o servidor de pé faz o teste **FALHAR** com a mensagem do preflight (nunca skip silencioso). [verificação pontual, restaurar com o preflight depois]
    - A suíte SQLite continua intacta: `php artisan test --compact --exclude-group postgis` verde.
  </acceptance_criteria>
  <done>O grupo postgis EXECUTA de fato (ST_Contains/ST_Area reais passam) e o guard impede passe-em-silêncio: com o servidor de pé, qualquer problema vira FALHA, não skip. Sem quebrar a suíte SQLite.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --exclude-group postgis` — suíte existente (385) verde + os novos testes SQLite (GeoLayerTest, GeoSchemaTest). NÃO usar `migrate:fresh` via Artisan (bate no banco de dev `sile`).
- `php artisan geo:preparar-banco-de-testes` (uma vez) e então `php artisan test --compact --group postgis` — smoke PostGIS PASSOU (executou ST_Contains/ST_Area), nunca "skipped" com o container de dev de pé.
</verification>

<success_criteria>
- geo_layers e geo_features migram em SQLite e PostgreSQL; índice GiST só no Postgres.
- GeoLayer expõe vigência (vigente/naData) provada por teste.
- Camadas bloqueadas modeláveis como status pendente_fonte (sem features).
- Preflight `geo:preparar-banco-de-testes` cria sile_testing + postgis; pgsql_testing aponta para a 5433.
- PostgisTestCase com guard de honestidade: smoke ST_Contains/ST_Area EXECUTA (não skip) com o servidor de pé — sem fachada de teste.
- Zero regressão nos 385 testes SQLite.
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/04-georreferenciamento-e-territorio/04-01-SUMMARY.md` registrando: nomes exatos das migrations/colunas, decisões (coluna genérica, GiST por driver), o comando de preflight (`geo:preparar-banco-de-testes`) e as vars `DB_TEST_*`/`POSTGIS_TESTS_REQUIRED`, o guard de honestidade do PostgisTestCase (fail-not-skip), a LINHA DE RESULTADO provando que o smoke postgis PASSOU (não skip), e como rodar o grupo postgis localmente e no CI.
</output>

---
phase: 04-georreferenciamento-e-territorio
plan: "04-07"
type: execute
wave: 5
depends_on: ["04-06"]
requirements: [HU-030, HU-036, HU-037]
files_modified:
  - package.json
  - package-lock.json
  - resources/js/components/geo/map-imovel.tsx
  - resources/js/components/geo/mapa-section.tsx
  - resources/js/pages/gestao/territorio/index.tsx
  - resources/js/components/app/app-sidebar.tsx
  - resources/js/components/icons/index.tsx
  - resources/js/layouts/gestao-layout.tsx
  - tests/Feature/Geo/TerritoryPageTest.php
autonomous: true

must_haves:
  truths:
    - "A página de consulta territorial (gestão) renderiza um mapa Leaflet com tiles OSM, atribuição e altura explícita, montado só no cliente (SSR-safe)"
    - "O usuário geocodifica um endereço, vê o marcador no mapa, identifica bairro/via/restrições reais e enxerga zona/lote como 'Indisponível — base pendente SEDUR' (sem valor falso)"
    - "As camadas carregadas podem ser sobrepostas no mapa (overlay GeoJSON) e o marcador é arrastável para ajustar a localização (HU-037), disparando a validação de sobreposição"
    - "O componente do mapa é reutilizável (resources/js/components/geo/) para as Fases 7 e 8"
  artifacts:
    - path: "resources/js/components/geo/map-imovel.tsx"
      provides: "componente Leaflet reutilizável (marcador arrastável + overlay GeoJSON + OSM)"
      contains: "react-leaflet"
    - path: "resources/js/components/geo/mapa-section.tsx"
      provides: "wrapper de montagem client-side (SSR-safe)"
      contains: "useEffect"
    - path: "resources/js/pages/gestao/territorio/index.tsx"
      provides: "página de consulta territorial (gestão console)"
      contains: "Consulta territorial"
  key_links:
    - from: "resources/js/components/geo/mapa-section.tsx"
      to: "map-imovel (Leaflet)"
      via: "lazy import + guarda mounted (Pitfall 9 SSR)"
      pattern: "lazy\\(|mounted"
    - from: "resources/js/pages/gestao/territorio/index.tsx"
      to: "/gestao/territorio/geocodificar e /identificar e /validar-localizacao"
      via: "useHttp/router"
      pattern: "territorio/(geocodificar|identificar|validar-localizacao)"
---

<objective>
Entregar o mapa interativo (HU-030), a consulta de camadas (HU-036 UI) e a confirmação/ajuste da localização com alerta de sobreposição (HU-037 UI), numa página de "Consulta territorial" na gestão (console SEDUR). O componente de mapa Leaflet é construído **reutilizável** (`resources/js/components/geo/`) para as Fases 7 e 8.

Purpose: Fecha SC#1 (endereço geocodificado + imóvel no mapa), SC#3 (camadas consultáveis) e a parte de UI do SC#4 (validar/ajustar). A página consome os endpoints reais do 04-03/04-06 e comunica honestamente as camadas bloqueadas (zona/lote pendente SEDUR) — sem fachada.
Output: deps leaflet/react-leaflet, componente MapImovel + wrapper client-side, página gestao/territorio/index, item de navegação e ícone, e o teste de componente da página.
</objective>

<context>
@.planning/phases/04-georreferenciamento-e-territorio/04-CONTEXT.md
@.planning/phases/04-georreferenciamento-e-territorio/04-RESEARCH.md
@.planning/phases/04-georreferenciamento-e-territorio/04-03-SUMMARY.md
@.planning/phases/04-georreferenciamento-e-territorio/04-06-SUMMARY.md
@resources/js/pages/portal/empresas/cadastrar.tsx
@resources/js/layouts/gestao-layout.tsx
@resources/js/components/app/app-sidebar.tsx
@resources/js/components/icons/index.tsx
@package.json

Skills: ativar `inertia-react-development` e `tailwindcss-development` (UI Inertia v3 + Tailwind v4).
Pitfalls Leaflet (RESEARCH): 9 (SSR Inertia v3 — montar só no cliente via lazy + guarda mounted), 10 (ícone do marcador some no build Vite — importar marker-icon/2x/shadow + L.Icon.Default.mergeOptions; importar leaflet/dist/leaflet.css; MapContainer com altura explícita), 3 (Leaflet usa [lat,lng]; o backend usa [lng,lat]). react-leaflet@^5 exige React 19 (casa com o projeto). Leaflet v2 (alpha) NÃO usar.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependências Leaflet + componente MapImovel reutilizável + wrapper client-side</name>
  <requirements>HU-030, HU-036, HU-037</requirements>
  <read_first>
    - package.json (deps atuais — React 19; sem libs de mapa)
    - .planning/phases/04-georreferenciamento-e-territorio/04-RESEARCH.md (Mapa react-leaflet v5 — Code Examples)
    - resources/js/components/app/page-header.tsx (padrão de componentes)
  </read_first>
  <action>
    Instalar as dependências aprovadas no CONTEXT (versões da pesquisa):
    ```bash
    npm install leaflet@^1.9.0 react-leaflet@^5.0.0
    npm install -D @types/leaflet@^1.9
    ```
    (Confirmar que package.json passou a listar leaflet, react-leaflet em dependencies e @types/leaflet em devDependencies; commitar package-lock.json.)

    Criar `resources/js/components/geo/map-imovel.tsx` (componente reutilizável):
    - `import 'leaflet/dist/leaflet.css';` + import dos assets de ícone (`marker-icon.png`, `marker-icon-2x.png`, `marker-shadow.png`) e `L.Icon.Default.mergeOptions({ iconUrl, iconRetinaUrl, shadowUrl })` (Pitfall 10).
    - Props: `{ lat, lng, camadas?: {id,type,geojson}[], onMove?: (latlng:{lat,lng})=>void, draggable?: boolean }`.
    - `MapContainer` com `style={{ height: 420 }}` (altura explícita), `TileLayer` OSM com `attribution='&copy; OpenStreetMap (ODbL)'`, `Marker draggable` com `eventHandlers={{ dragend: e => onMove?.(e.target.getLatLng()) }}` (HU-037 ajuste), `{camadas?.map(c => <GeoJSON key={c.id} data={c.geojson} />)}` (overlay HU-036), `Popup`. ATENÇÃO Pitfall 3: aqui as posições são [lat, lng] (padrão Leaflet); ao chamar o backend, converter para lng/lat.

    Criar `resources/js/components/geo/mapa-section.tsx` (wrapper SSR-safe — Pitfall 9):
    ```tsx
    const MapImovel = lazy(() => import('@/components/geo/map-imovel').then(m => ({ default: m.MapImovel })));
    // guarda mounted via useEffect; fallback skeleton h-[420px] animate-pulse rounded-2xl bg-gray-100 dark:bg-white/[0.03]
    ```

    Adicionar um ícone `MapPinIcon` em `resources/js/components/icons/index.tsx` (SVG inline no padrão dos demais — ex.: pin de localização) para o item de navegação.
  </action>
  <verify>npm run typecheck && npm run build 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - `rg '"leaflet"|"react-leaflet"' package.json` casa (dependencies) e `rg '"@types/leaflet"' package.json` casa (devDependencies).
    - `rg "leaflet/dist/leaflet.css" resources/js/components/geo/map-imovel.tsx` casa.
    - `rg "L.Icon.Default.mergeOptions" resources/js/components/geo/map-imovel.tsx` casa (Pitfall 10).
    - `rg "lazy\(|mounted" resources/js/components/geo/mapa-section.tsx` casa (Pitfall 9).
    - `rg "MapPinIcon" resources/js/components/icons/index.tsx` casa.
    - `npm run typecheck` e `npm run build` sem erros.
  </acceptance_criteria>
  <done>Dependências de mapa instaladas; componente Leaflet reutilizável (marcador arrastável + overlay + OSM, com ícone/CSS/altura corretos) e wrapper client-side SSR-safe compilam.</done>
</task>

<task type="auto">
  <name>Task 2: Página de consulta territorial + navegação + teste de componente</name>
  <requirements>HU-030, HU-036, HU-037</requirements>
  <read_first>
    - resources/js/pages/portal/empresas/cadastrar.tsx (useHttp + useForm + Card/Alert/Button/Input + layout)
    - resources/js/layouts/gestao-layout.tsx (grupos da sidebar — onde adicionar "Território")
    - .planning/phases/04-georreferenciamento-e-territorio/04-06-SUMMARY.md (shape das props e dos JSONs)
    - tests/Feature/Geo/TerritoryPageTest.php (criado em 04-06 — adicionar a asserção de componente)
  </read_first>
  <action>
    Criar `resources/js/pages/gestao/territorio/index.tsx` (layout `GestaoLayout`, console SEDUR):
    - PageHeader "Consulta territorial" (breadcrumb Painel → Consulta territorial).
    - Card "Localizar imóvel": Input de endereço + botão "Localizar" → `useHttp` POST `/gestao/territorio/geocodificar`. Em sucesso, posiciona o marcador (lat/lng) no `MapImovel` via `mapa-section`. Erro/indisponível/toggle off → `Alert` (variant error/warning) com a mensagem do backend e dica "Posicione o ponto manualmente no mapa" (degradação comunicada). Respeitar `geocodingEnabled` (prop) — desabilitar o botão e exibir hint quando false.
    - Ao mover o marcador (onMove) ou após geocodificar → POST `/gestao/territorio/identificar` {lat,lng}; renderizar painel "Identificação" com bairro/via/restrições reais; para zona e lote, exibir `Alert`/badge "Indisponível — base pendente SEDUR" usando o status `indisponivel`/`motivo` do TerritoryResult (NUNCA exibir valor inventado).
    - Card "Camadas" (HU-036): lista as `camadas` (prop) com type_label, status_label e feature_count; camadas `pendente_fonte` (zona/lote) aparecem com badge "Pendente SEDUR"; toggle de overlay para as camadas com features (passa o geojson ao MapImovel). (O carregamento do GeoJSON das camadas pode ser via prop inicial limitada ou endpoint — manter simples: overlay das camadas já entregues pelo backend; se vazio, só o marcador.)
    - Seção "Validar localização" (HU-037): com o marcador posicionado/ajustado, botão "Validar localização" → POST `/gestao/territorio/validar-localizacao` {polygon} (montar um polígono mínimo a partir do ponto/ajuste — ou um quadrado pequeno ao redor do ponto como placeholder de polígono informado nesta fase); exibir o resultado: `validado` (sucesso), `alerta_sobreposicao` (Alert warning com o percentual e o limiar `sobreposicaoMinima`), `indisponivel` (Alert info "Base de lotes pendente SEDUR"). Mostrar o limiar parametrizado.
    - Exibir a atribuição OSM/ODbL visível (obrigação de licença).
    - `Pagina.layout = (page) => <GestaoLayout>{page}</GestaoLayout>`.

    Em `resources/js/layouts/gestao-layout.tsx`, adicionar ao grupo "Visão geral" (ou um grupo "Território") o item:
    ```tsx
    { name: 'Consulta territorial', href: '/gestao/territorio', icon: <MapPinIcon />, visible: auth.permissions.includes('consultar-territorio') }
    ```
    (importar MapPinIcon).

    Em `tests/Feature/Geo/TerritoryPageTest.php`, atualizar o teste de GET /gestao/territorio para incluir `->component('gestao/territorio/index')` no assertInertia (agora que a página existe) — fecha a pendência do precedente [03-04].
  </action>
  <verify>npm run typecheck && npm run build 2>&1 | tail -15 && php artisan test --compact tests/Feature/Geo/TerritoryPageTest.php</verify>
  <acceptance_criteria>
    - `test -f resources/js/pages/gestao/territorio/index.tsx` verdadeiro.
    - `rg "territorio/(geocodificar|identificar|validar-localizacao)" resources/js/pages/gestao/territorio/index.tsx` casa as três chamadas.
    - `rg "pendente SEDUR|Indisponível" resources/js/pages/gestao/territorio/index.tsx` casa (zona/lote comunicados, sem valor falso).
    - `rg "Consulta territorial" resources/js/layouts/gestao-layout.tsx` casa (nav item) com visible por consultar-territorio.
    - `rg "->component\('gestao/territorio/index'\)" tests/Feature/Geo/TerritoryPageTest.php` casa.
    - `npm run typecheck`, `npm run build` e `php artisan test --compact tests/Feature/Geo/TerritoryPageTest.php` verdes.
  </acceptance_criteria>
  <done>Página de consulta territorial navegável: geocodifica, mostra o mapa Leaflet com marcador arrastável e overlay de camadas, identifica bairro/via/restrições reais, comunica zona/lote como pendente SEDUR e valida a localização com o limiar parametrizado — tudo sobre os endpoints reais.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` e `npm run build` sem erros (Vite).
- `php artisan test --compact tests/Feature/Geo/TerritoryPageTest.php` verde (com ->component()).
- Validação visual (claro/escuro + mobile 375px) e o smoke navegável ficam para o fechamento (04-08).
</verification>

<success_criteria>
- HU-030 (mapa interativo), HU-036 (camadas consultáveis + bloqueio comunicado) e HU-037 UI (ajuste + alerta de sobreposição) entregues.
- Componente de mapa reutilizável para Fases 7/8; SSR-safe; ícone/CSS/altura corretos.
- Página protegida por consultar-territorio; degradação comunicada (geocoding off, lote pendente).
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/04-georreferenciamento-e-territorio/04-07-SUMMARY.md`: caminho do componente reutilizável e suas props (contrato para Fases 7/8), versões instaladas de leaflet/react-leaflet, e como a página comunica as camadas pendentes. Registrar pendência de validação visual/screenshots para o fechamento.
</output>

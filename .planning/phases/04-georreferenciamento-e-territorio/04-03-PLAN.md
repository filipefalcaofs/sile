---
phase: 04-georreferenciamento-e-territorio
plan: "04-03"
type: tdd
wave: 2
depends_on: ["04-02"]
requirements: [HU-029]
files_modified:
  - app/Services/Geo/Geocoder.php
  - app/Services/Geo/NominatimGeocoder.php
  - app/Services/Geo/GeocodeResult.php
  - app/Services/Geo/GeocoderException.php
  - app/Services/Geo/AddressNotFoundException.php
  - app/Providers/AppServiceProvider.php
  - app/Providers/FortifyServiceProvider.php
  - app/Http/Controllers/Gestao/GeocodeController.php
  - app/Http/Requests/Gestao/GeocodeRequest.php
  - routes/gestao.php
  - bootstrap/app.php
  - tests/Unit/Geo/NominatimGeocoderTest.php
  - tests/Feature/Geo/GeocodeEndpointTest.php
autonomous: true

must_haves:
  truths:
    - "Um endereço de Salvador é convertido em lat/lng + endereço normalizado + confiança através do contrato Geocoder"
    - "O provider Nominatim envia User-Agent identificável, respeita timeout/retry/backoff parametrizados e cacheia apenas sucesso"
    - "O endpoint de geocodificação é auditado em toda saída (sucesso/não encontrado/indisponível/bloqueado) e exige a permissão consultar-territorio"
    - "Com features.geocoding desligado nenhum request HTTP sai (degradação comunicada 422), nunca falha silenciosa"
  artifacts:
    - path: "app/Services/Geo/Geocoder.php"
      provides: "contrato geocode(string): GeocodeResult"
      contains: "interface Geocoder"
    - path: "app/Services/Geo/NominatimGeocoder.php"
      provides: "provider real Nominatim com cache/throttle/retry"
      contains: "User-Agent"
    - path: "app/Http/Controllers/Gestao/GeocodeController.php"
      provides: "endpoint auditado toggle-aware"
      contains: "Settings::enabled('geocoding')"
  key_links:
    - from: "app/Providers/AppServiceProvider.php"
      to: "NominatimGeocoder"
      via: "bind(Geocoder::class)"
      pattern: "bind\\(Geocoder::class"
    - from: "app/Providers/FortifyServiceProvider.php"
      to: "RateLimiter geocoding"
      via: "Settings::get('seguranca.throttle.geocoding.por_minuto')"
      pattern: "RateLimiter::for\\('geocoding'"
    - from: "routes/gestao.php"
      to: "GeocodeController"
      via: "rota gestao/territorio/geocodificar com throttle e permissão"
      pattern: "territorio/geocodificar"
---

<objective>
Implementar a HU-029 (geocodificar endereço) no padrão contrato+provider+cache da Fase 3: interface `Geocoder` + `NominatimGeocoder` real (Nominatim/OSM), com `base_url`/toggle parametrizados, throttle e retry/backoff herdados da Fase 3.1, e um endpoint da gestão auditado em todas as saídas.

Purpose: É a porta de entrada do território — endereço → coordenada — insumo do mapa (04-07) e da identificação (04-05). Atrás de contrato para a Fase 13 trocar o binding (self-host/base SEDUR) sem tocar call sites. Reuso quase 1:1 do `BrasilApiCnpjLookup`.
Output: contrato + DTO + provider + exceções, binding, RateLimiter nomeado `geocoding`, `GeocodeController` (POST /gestao/territorio/geocodificar) auditado e protegido por `consultar-territorio`.
</objective>

<context>
@.planning/phases/04-georreferenciamento-e-territorio/04-CONTEXT.md
@.planning/phases/04-georreferenciamento-e-territorio/04-RESEARCH.md
@.planning/phases/04-georreferenciamento-e-territorio/04-02-SUMMARY.md
@app/Services/Cnpj/CnpjLookup.php
@app/Services/Cnpj/BrasilApiCnpjLookup.php
@app/Services/Cnpj/CnpjData.php
@app/Http/Controllers/Portal/CnpjLookupController.php
@app/Providers/FortifyServiceProvider.php
@app/Providers/AppServiceProvider.php
@routes/gestao.php
@bootstrap/app.php

Política Nominatim (RESEARCH, confiança ALTA): ≤1 req/s; User-Agent (ou Referer) OBRIGATÓRIO identificando a app (User-Agents de libs HTTP são bloqueados → vira HTML, tratar como falha); cache obrigatório de sucesso; endpoint `GET {base_url}/search?q=...&format=jsonv2&addressdetails=1&countrycodes=br&limit=1` retornando `lat`, `lon`, `display_name`, `importance` (0–1, proxy de confiança), `address{}`. Atribuição OSM/ODbL no mapa.
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Contrato Geocoder + GeocodeResult + NominatimGeocoder (cache/throttle/retry) — TDD</name>
  <requirements>HU-029</requirements>
  <read_first>
    - app/Services/Cnpj/BrasilApiCnpjLookup.php (cache só sucesso; retry/timeout/backoff via Settings; ConnectionException→Exceção própria)
    - app/Services/Cnpj/CnpjData.php (DTO readonly com named constructor + toArray)
    - app/Providers/AppServiceProvider.php (padrão de bind do contrato)
    - .planning/phases/04-georreferenciamento-e-territorio/04-RESEARCH.md (Code Examples → Geocoder)
  </read_first>
  <action>
    Criar `app/Services/Geo/Geocoder.php` (interface): `public function geocode(string $address): GeocodeResult;` com PHPDoc das exceções (`AddressNotFoundException` quando nada é encontrado; `GeocoderException` em indisponibilidade/erro).

    Criar `app/Services/Geo/GeocodeResult.php` (DTO `final readonly`): propriedades `float $latitude`, `float $longitude`, `string $displayName`, `?float $confidence` (de `importance`), `array $address`. Named constructor `fromNominatim(array $payload): self` (mapeia `lat`/`lon` para float, `display_name`, `importance`, `address`). Método `toArray(): array` em snake_case (`latitude`, `longitude`, `display_name`, `confidence`, `address`). ATENÇÃO Pitfall 3 (ordem lng/lat): GeocodeResult guarda latitude e longitude nomeados; documentar que GeoJSON/ST_MakePoint usam [lng, lat] e o consumidor deve respeitar.

    Criar `app/Services/Geo/GeocoderException.php` e `app/Services/Geo/AddressNotFoundException.php` (espelham CnpjLookupException/CnpjNotFoundException).

    Criar `app/Services/Geo/NominatimGeocoder.php implements Geocoder`: lê `integrations.geocoding.base_url` via `Settings::get(..., config('sile.integrations.geocoding.base_url'))`; `Cache::remember("sile.geocoding.".sha1($address), (int) config('sile.integrations.geocoding.cache_ttl', 86400), fn() => ...)`; dentro do closure: `Http::withHeaders(['User-Agent' => config('sile.integrations.geocoding.user_agent')])->timeout(Settings::get('integrations.geocoding.timeout', config(...)))->retry(Settings::get('integrations.geocoding.retries', ...), Settings::get('integrations.geocoding.backoff_ms', ...), throw: false)->acceptJson()->get("{$baseUrl}/search", ['q'=>$address,'format'=>'jsonv2','addressdetails'=>1,'countrycodes'=>'br','limit'=>1])`. Tratamento: `ConnectionException` → `GeocoderException`; `$response->failed()` → `GeocoderException`; corpo vazio / não-array (HTML de bloqueio) → `AddressNotFoundException`; senão `GeocodeResult::fromNominatim($json[0])`. A exceção dentro do closure impede o cache (falha nunca cacheada — precedente [03-02]).

    Em `AppServiceProvider::register()`, adicionar `$this->app->bind(Geocoder::class, NominatimGeocoder::class);` (comentário: Fase 13 troca por self-host/base SEDUR sem tocar call sites).

    RED → `tests/Unit/Geo/NominatimGeocoderTest.php` com `Http::preventStrayRequests()` e `Http::fake`: (a) sucesso retorna GeocodeResult com latitude≈-12.97/longitude≈-38.5 e confidence; (b) corpo `[]` → AddressNotFoundException; (c) `Http::sequence` pushFailedConnection com retries=0/1 → GeocoderException; (d) header `User-Agent` presente (`Http::assertSent(fn($r)=>$r->hasHeader('User-Agent'))`); (e) falha NÃO é cacheada (segunda chamada após falha refaz o request). Rodar (RED). GREEN → implementar. REFACTOR.
  </action>
  <verify>php artisan test --compact tests/Unit/Geo/NominatimGeocoderTest.php</verify>
  <acceptance_criteria>
    - `rg "interface Geocoder" app/Services/Geo/Geocoder.php` casa.
    - `rg "format.*jsonv2|countrycodes.*br" app/Services/Geo/NominatimGeocoder.php` casa.
    - `rg "bind\(Geocoder::class, NominatimGeocoder::class\)" app/Providers/AppServiceProvider.php` casa.
    - `rg "hasHeader\('User-Agent'\)|User-Agent" tests/Unit/Geo/NominatimGeocoderTest.php` casa.
    - `php artisan test --compact tests/Unit/Geo/NominatimGeocoderTest.php` verde.
  </acceptance_criteria>
  <done>O contrato Geocoder está implementado pelo NominatimGeocoder com cache de sucesso, retry/timeout/backoff parametrizados e User-Agent obrigatório; provado por testes com Http::fake.</done>
</task>

<task type="tdd">
  <name>Task 2: RateLimiter geocoding + GeocodeController auditado + rota protegida — TDD</name>
  <requirements>HU-029</requirements>
  <read_first>
    - app/Http/Controllers/Portal/CnpjLookupController.php (toggle-aware; auditoria em todas as saídas; shouldRenderJsonWhen)
    - app/Providers/FortifyServiceProvider.php (RateLimiter::for('cnpj-lookup') — espelhar)
    - routes/gestao.php (grupo auth:gestao + permission; onde criar o grupo 'territorio')
    - bootstrap/app.php (shouldRenderJsonWhen — adicionar os endpoints de território)
    - app/Support/Audit/AuditService.php (assinatura log())
  </read_first>
  <action>
    Em `FortifyServiceProvider::boot()`, adicionar (espelhando 'cnpj-lookup'):
    ```php
    RateLimiter::for('geocoding', function (Request $request) {
        return Limit::perMinute(
            (int) Settings::get('seguranca.throttle.geocoding.por_minuto', 60),
        )->by($request->user()?->id ?: $request->ip());
    });
    ```

    Criar `app/Http/Requests/Gestao/GeocodeRequest.php` (authorize: `$this->user('gestao')?->can('consultar-territorio')`; rules: `address` => required|string|min:3|max:255).

    Criar `app/Http/Controllers/Gestao/GeocodeController.php` (`__invoke(GeocodeRequest): JsonResponse`), injetando `Geocoder` + `AuditService`. Fluxo idêntico ao CnpjLookupController, log_name `'territorio'`, event `'geocodificacao'`:
    - se `! Settings::enabled('geocoding')` → audita result `'bloqueado'` (motivo `'toggle-desativado'`) e retorna 422 `{message: 'A geocodificação está desativada. Informe a localização manualmente no mapa.'}` (degradação comunicada — sem request HTTP).
    - sucesso → audita `'sucesso'` (properties: endereço, provider host) e retorna `$result->toArray()`.
    - `AddressNotFoundException` → audita `'falha'` (motivo `'nao-encontrado'`), 404 `{message: 'Endereço não localizado. Ajuste o texto ou posicione o ponto no mapa.'}`.
    - `GeocoderException|ConnectionException` → audita `'falha'` (motivo `'indisponibilidade'`), 503 `{message: 'Serviço de geocodificação indisponível. Posicione o ponto manualmente no mapa.'}`.

    Em `routes/gestao.php`, criar o grupo de território DENTRO do grupo `auth:gestao` + `lgpd.accepted`, sob `permission:consultar-territorio`, prefixo `territorio`, name `gestao.territorio.`:
    ```php
    Route::middleware('permission:consultar-territorio')->prefix('territorio')->name('territorio.')->group(function () {
        Route::post('geocodificar', GeocodeController::class)->middleware('throttle:geocoding')->name('geocodificar');
    });
    ```

    Em `bootstrap/app.php`, estender `shouldRenderJsonWhen` para incluir `$request->is('gestao/territorio/*') && $request->expectsJson()` (mesmo padrão do portal/empresas/consultar-cnpj).

    RED → `tests/Feature/Geo/GeocodeEndpointTest.php` (RefreshDatabase; `Http::preventStrayRequests()`; seed ParameterSeeder + RolesAndPermissionsSeeder): (a) usuário sem permissão (`actingAs($user,'gestao')`) recebe 403 e gera activity bloqueada de segurança (CA-04); (b) analista com permissão + `Http::fake` de sucesso → 200 com latitude/longitude + activity 'territorio'/'geocodificacao' result 'sucesso' (CA-01/CA-02); (c) toggle `features.geocoding` desligado → 422 + activity 'bloqueado' e `Http::assertNothingSent()` (CA-03); (d) Nominatim indisponível (fake failed) → 503 + activity 'falha'. Header `Accept: application/json`. Rodar (RED). GREEN. REFACTOR + `vendor/bin/pint --dirty`.
  </action>
  <verify>php artisan test --compact tests/Feature/Geo/GeocodeEndpointTest.php && php artisan route:list --path=territorio</verify>
  <acceptance_criteria>
    - `rg "RateLimiter::for\('geocoding'" app/Providers/FortifyServiceProvider.php` casa.
    - `rg "Settings::enabled\('geocoding'\)" app/Http/Controllers/Gestao/GeocodeController.php` casa.
    - `rg "territorio/geocodificar|gestao/territorio" routes/gestao.php bootstrap/app.php` casa.
    - `php artisan route:list --path=territorio` lista `gestao.territorio.geocodificar` com throttle:geocoding.
    - `php artisan test --compact tests/Feature/Geo/GeocodeEndpointTest.php` verde (CA-01..CA-04).
  </acceptance_criteria>
  <done>Endpoint de geocodificação operacional, auditado em todas as saídas, protegido por permissão e throttle, com toggle desligando antes de qualquer request — sem fachada.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact tests/Unit/Geo/NominatimGeocoderTest.php tests/Feature/Geo/GeocodeEndpointTest.php` verde.
- A chamada REAL ao Nominatim (evidência sem fachada) fica para o fechamento (04-08); aqui tudo é provado com Http::fake.
</verification>

<success_criteria>
- HU-029 CA-01..CA-04 cobertos por testes.
- Geocoder atrás de contrato com binding trocável; cache de sucesso; User-Agent obrigatório; throttle/retry parametrizados.
- Endpoint da gestão auditado, protegido e com degradação comunicada quando o toggle está desligado.
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/04-georreferenciamento-e-territorio/04-03-SUMMARY.md`: contrato/DTO (chaves de toArray), rota/nome do endpoint, log_name/event auditados e o contrato JSON consumido pelo mapa (04-07). Registrar que a chamada real ao Nominatim é evidência do fechamento.
</output>

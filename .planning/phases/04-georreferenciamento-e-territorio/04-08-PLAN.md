---
phase: 04-georreferenciamento-e-territorio
plan: "04-08"
type: execute
wave: 6
depends_on: ["04-03", "04-04", "04-05", "04-06", "04-07"]
requirements: [HU-029, HU-030, HU-031, HU-032, HU-033, HU-034, HU-035, HU-036, HU-037]
files_modified:
  - .github/workflows/tests.yml
  - database/seeders/DevGeoSeeder.php
  - docs/deploy/postgis-testing.md
  - tests/Feature/Geo/PhaseFourSmokeTest.php
autonomous: true

must_haves:
  truths:
    - "A suíte SQLite inteira (385+ novos) e o grupo @group postgis passam verdes; pint/typecheck/build limpos; migrate:fresh --seed funciona no Postgres"
    - "Existe um workflow de CI real (.github/workflows/tests.yml) com service container postgis/postgis:16-3.5 que roda --group postgis com POSTGIS_TESTS_REQUIRED=true (skip vira FALHA) — enforcement real, não aspiracional"
    - "Há evidência registrada de UMA chamada REAL ao Nominatim em dev (endereço de Salvador → coordenada)"
    - "Há evidência de consulta espacial REAL (golden point → bairro esperado) sobre o snapshot oficial commitado do GeoSalvador (sem rede); bairro ausente é BLOQUEIO da fase, não skip"
    - "Zona urbanística e lote estão visivelmente comunicados como bloqueados (pendente SEDUR) na UI e documentados — nunca inventados"
  artifacts:
    - path: ".github/workflows/tests.yml"
      provides: "CI com Postgres+PostGIS rodando a suíte SQLite e o grupo postgis (enforcement W3b)"
      contains: "postgis/postgis"
    - path: "tests/Feature/Geo/PhaseFourSmokeTest.php"
      provides: "smoke @group postgis end-to-end (geocode→identify→validar) sobre dado real commitado"
      contains: "golden"
    - path: "docs/deploy/postgis-testing.md"
      provides: "como rodar o grupo postgis local (preflight) e no CI"
      contains: "POSTGIS_TESTS_REQUIRED"
  key_links:
    - from: ".github/workflows/tests.yml"
      to: "grupo postgis"
      via: "service container postgis/postgis + POSTGIS_TESTS_REQUIRED=true"
      pattern: "POSTGIS_TESTS_REQUIRED|--group postgis"
    - from: "tests/Feature/Geo/PhaseFourSmokeTest.php"
      to: "snapshot commitado database/data/geo/bairros.geojson + TerritoryService"
      via: "golden point → bairro real"
      pattern: "golden"
---

<objective>
Fechar a Fase 4 com **verificação integral e evidência fresca, sem fachada**: rodar a suíte completa (SQLite + `@group postgis`), pint/typecheck/build e `migrate:fresh --seed`; registrar a evidência de integração real (uma chamada real ao Nominatim e uma consulta espacial real sobre os dados oficiais do GeoSalvador); confirmar que as camadas bloqueadas (zona/lote) estão comunicadas na UI e documentadas; e documentar como rodar o grupo PostGIS no CI.

Purpose: É o critério de pronto do projeto (regra Superpowers "nenhuma afirmação de sucesso sem evidência fresca" + regra "integração real validada com evidência registrada"). Garante que o EP04 entrega lógica real de ponta a ponta e que o escopo honesto (bairro/via/restrição reais; zona/lote bloqueadas) está visível.
Output: smoke @group postgis end-to-end, documento de CI/PostGIS, e o relatório de evidências no SUMMARY.
</objective>

<context>
@.planning/phases/04-georreferenciamento-e-territorio/04-CONTEXT.md
@.planning/phases/04-georreferenciamento-e-territorio/04-RESEARCH.md
@.planning/phases/04-georreferenciamento-e-territorio/04-01-SUMMARY.md
@.planning/phases/04-georreferenciamento-e-territorio/04-04-SUMMARY.md
@.planning/phases/04-georreferenciamento-e-territorio/04-05-SUMMARY.md
@.planning/phases/04-georreferenciamento-e-territorio/04-06-SUMMARY.md
@.planning/phases/04-georreferenciamento-e-territorio/04-07-SUMMARY.md
@docs/deploy/producao-assincrona.md

Critério de pronto transversal (ROADMAP): CAs cobertos por PHPUnit; navegável de ponta a ponta com lógica real; sem hardcode; integrações validadas contra real quando houver; pint limpo; telas responsivas/acessíveis; observabilidade mínima. Evidência sem fachada (regra nº1): a chamada real ao Nominatim e a consulta espacial sobre dado oficial real são obrigatórias no fechamento; camadas sem fonte pública ficam bloqueadas e comunicadas.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke @group postgis (snapshot commitado) + CI real (postgis) + doc — enforcement W3b</name>
  <requirements>HU-029, HU-030, HU-031, HU-032, HU-033, HU-034, HU-035, HU-036, HU-037</requirements>
  <read_first>
    - tests/PostgisTestCase.php (guard de honestidade — 04-01)
    - app/Console/Commands/PrepareGeoTestDatabaseCommand.php (preflight — 04-01)
    - .planning/phases/04-georreferenciamento-e-territorio/04-04-SUMMARY.md (camadas reais carregadas + snapshot bairros.geojson commitado + versões)
    - .planning/phases/04-georreferenciamento-e-territorio/04-05-SUMMARY.md (TerritoryService)
    - .planning/phases/04-georreferenciamento-e-territorio/04-RESEARCH.md (Estratégia de testes PostGIS — exemplo de service container)
    - composer.json / .env.example (versão PHP, extensões, env)
  </read_first>
  <action>
    Criar `tests/Feature/Geo/PhaseFourSmokeTest.php` (`#[Group('postgis')]`, estende PostgisTestCase) — golden cases de domínio sobre dado REAL do **snapshot COMMITADO** (sem rede):
    - Carregar a camada de bairro real via GeoJsonLayerImporter/GeoLayerSeeder a partir de `database/data/geo/bairros.geojson` (commitado no 04-04). Se o snapshot de bairro NÃO existir, o teste **FALHA** (bloqueio da fase — bairro real não provado), NUNCA markTestSkipped. O guard do PostgisTestCase (04-01) já garante que, com o servidor de pé, não há skip silencioso.
    - Golden point conhecido de Salvador (ex.: Pelourinho ≈ lat -12.9718, lng -38.5073 — conferir o bairro esperado contra o dado real carregado) → `TerritoryService::identify` retorna o bairro esperado, `zona`/`lote` como `indisponivel` (pendente SEDUR), e a versão da camada de bairro registrada.
    - Confirmar auditoria 'territorio'/'identificacao' criada.
    Anotar o conjunto de golden points como proteção de regressão de domínio (Validation Architecture do RESEARCH).

    Criar `.github/workflows/tests.yml` — CI REAL (enforcement W3b; o repo ainda não tem CI). Roda a suíte SQLite e o grupo postgis com `POSTGIS_TESTS_REQUIRED=true` (skip do grupo vira FALHA — fecha W2/W3):
    ```yaml
    name: tests
    on: [push, pull_request]
    jobs:
      tests:
        runs-on: ubuntu-latest
        services:
          postgres:
            image: postgis/postgis:16-3.5
            env: { POSTGRES_DB: sile_testing, POSTGRES_USER: sile, POSTGRES_PASSWORD: secret }
            ports: ['5432:5432']
            options: >-
              --health-cmd "pg_isready -U sile" --health-interval 10s --health-timeout 5s --health-retries 5
        env:
          DB_TEST_HOST: 127.0.0.1
          DB_TEST_PORT: 5432            # serviço do CI expõe 5432 (dev usa 5433)
          DB_TEST_DATABASE: sile_testing
          DB_TEST_USERNAME: sile
          DB_TEST_PASSWORD: secret
          POSTGIS_TESTS_REQUIRED: 'true'  # skip do grupo postgis vira FALHA
        steps:
          - uses: actions/checkout@v4
          - uses: shivammathur/setup-php@v2
            with:
              php-version: '8.4'   # alinhar à versão do projeto (AGENTS: 8.5; composer require: ^8.3)
              extensions: pdo_pgsql, pgsql, redis, mbstring
          - run: composer install --no-interaction --prefer-dist
          - uses: actions/setup-node@v4
            with: { node-version: '20' }
          - run: npm ci && npm run build
          - run: cp .env.example .env && php artisan key:generate
          - run: php artisan test --compact --exclude-group postgis   # suíte SQLite
          - run: php artisan test --compact --group postgis           # espacial (a imagem postgis já cria a extensão em sile_testing)
    ```
    (Na imagem `postgis/postgis`, a extensão postgis é criada automaticamente no `POSTGRES_DB`; o preflight `geo:preparar-banco-de-testes` é para o LOCAL, onde o container só tem `sile`.)

    Criar `docs/deploy/postgis-testing.md`: conexão `pgsql_testing` (porta 5433 local / 5432 CI), o preflight `php artisan geo:preparar-banco-de-testes` (local), como rodar `--group postgis` e `--exclude-group postgis`, e a regra de enforcement: com o servidor de pé o grupo NUNCA pula em silêncio (guard fail-not-skip); o CI define `POSTGIS_TESTS_REQUIRED=true`.
  </action>
  <verify>php artisan geo:preparar-banco-de-testes && php artisan test --compact --group postgis</verify>
  <acceptance_criteria>
    - `rg "golden|Pelourinho|identify\(" tests/Feature/Geo/PhaseFourSmokeTest.php` casa; o smoke carrega do snapshot commitado e FALHA (não skip) se bairro ausente.
    - `test -f .github/workflows/tests.yml` e `rg "postgis/postgis|POSTGIS_TESTS_REQUIRED|--group postgis" .github/workflows/tests.yml` casa (CI real, enforcement).
    - `test -f docs/deploy/postgis-testing.md` e `rg "POSTGIS_TESTS_REQUIRED|geo:preparar-banco-de-testes" docs/deploy/postgis-testing.md` casa.
    - `php artisan test --compact --group postgis` verde e o smoke reporta PASSOU (executou identify sobre bairro real) — NÃO "skipped".
  </acceptance_criteria>
  <done>O grupo postgis tem enforcement REAL (CI com service container postgis + POSTGIS_TESTS_REQUIRED) e um smoke que prova a identificação real (golden point → bairro real do snapshot commitado, zona/lote pendentes), sem skip silencioso e sem dependência de rede.</done>
</task>

<task type="auto">
  <name>Task 2: Verificação integral + evidência de integração real (Nominatim + consulta espacial)</name>
  <requirements>HU-029, HU-030, HU-031, HU-032, HU-033, HU-034, HU-035, HU-036, HU-037</requirements>
  <read_first>
    - composer.json (scripts test) e package.json (scripts typecheck/build)
    - .planning/phases/04-georreferenciamento-e-territorio/04-03-SUMMARY.md (endpoint de geocodificação)
    - .env (conexão Postgres de dev; NÃO commitar)
  </read_first>
  <action>
    Verificação integral (rodar FRESCO e ler o output inteiro antes de afirmar):
    - `php artisan test --compact --exclude-group postgis` → suíte SQLite inteira verde (385 anteriores + novos).
    - `php artisan test --compact --group postgis` → grupo espacial verde (Postgres+PostGIS).
    - `vendor/bin/pint --dirty --format agent` → sem pendências.
    - `npm run typecheck` e `npm run build` → sem erros.
    - `php artisan migrate:fresh --seed` no Postgres de dev → conclui; o GeoLayerSeeder carrega bairro/via/restrição reais e marca zona/lote pendente_fonte.

    Evidência de integração REAL (regra nº1 — registrar como prova, sem fachada):
    - Nominatim: executar UMA chamada real em dev a partir do endpoint (com `features.geocoding=1`), p.ex. via `php artisan tinker --execute 'dd(app(App\Services\Geo\Geocoder::class)->geocode("Praça Municipal, Salvador, BA"));'` OU um POST autenticado a `/gestao/territorio/geocodificar` — capturar a coordenada retornada (≈ lat -12.97 / lng -38.5) e registrar como evidência no SUMMARY (endereço, coordenada, data/hora). Restaurar o estado (sem deixar toggles alterados).
    - Consulta espacial real: com `migrate:fresh --seed` no Postgres, executar uma consulta de identificação sobre o bairro REAL carregado (golden point) e registrar o bairro retornado + a versão da camada.
    - Bloqueios visíveis: confirmar (browser ou screenshot/automação) que a página /gestao/territorio comunica zona/lote como "Indisponível — base pendente SEDUR" e lista as camadas carregadas; registrar evidência.

    Criar `database/seeders/DevGeoSeeder.php` SOMENTE se necessário para reproduzir o smoke navegável em dev (ex.: garantir um usuário analista com a permissão consultar-territorio para o smoke) — caso o DevAdminSeeder já cubra, NÃO criar (preferir reusar; remover o arquivo do files_modified no SUMMARY se não for usado). NÃO duplicar lógica de carga (que vive no GeoLayerSeeder).

    Registrar todas as evidências (comandos + outputs resumidos) no SUMMARY do fechamento.
  </action>
  <verify>php artisan test --compact --exclude-group postgis && php artisan test --compact --group postgis && vendor/bin/pint --dirty --format agent && npm run typecheck && npm run build</verify>
  <acceptance_criteria>
    - Suíte SQLite verde (sem regressão) e grupo postgis verde (executado, não skip) — outputs lidos e registrados.
    - `vendor/bin/pint --dirty --format agent` sem pendências; `npm run typecheck` e `npm run build` sem erros.
    - `php artisan migrate:fresh --seed` (Postgres) concluído com bairro/via/restrição carregados e zona/lote pendente_fonte.
    - BAIRRO REAL é critério de fechamento: a consulta espacial sobre o bairro carregado retorna o bairro esperado. Se o bairro real NÃO carregou (endpoint indisponível e sem snapshot commitado), a fase é **BLOQUEADA** (registrar no SUMMARY para o orquestrador anotar STATE/ROADMAP) — NÃO fechar com skip/fachada.
    - Evidência registrada no SUMMARY: 1 chamada real ao Nominatim (endereço→coordenada), 1 consulta espacial real (golden point→bairro+versão), e os bloqueios zona/lote visíveis na UI.
  </acceptance_criteria>
  <done>Fase 4 verificada de ponta a ponta com evidência fresca: lógica real (geocodificação real + consulta espacial real sobre dado oficial), suíte completa verde, e camadas bloqueadas comunicadas — sem nenhuma fachada.</done>
</task>

</tasks>

<verification>
- `php artisan test --compact --exclude-group postgis` (SQLite, sem regressão) verde.
- `php artisan geo:preparar-banco-de-testes` (uma vez) e `php artisan test --compact --group postgis` (PostGIS) verde e EXECUTADO (não skip).
- `.github/workflows/tests.yml` existe e roda o grupo postgis com `POSTGIS_TESTS_REQUIRED=true` (enforcement).
- `vendor/bin/pint --dirty --format agent`, `npm run typecheck`, `npm run build` limpos.
- `php artisan migrate:fresh --seed` (Postgres) ok; bairro real carregado (senão fase BLOQUEADA, não fechada).
- Evidência real (Nominatim + consulta espacial sobre bairro real + bloqueios visíveis) registrada.
</verification>

<success_criteria>
- Todos os CAs das HUs HU-029..HU-037 cobertos por testes passando (incl. o tratamento honesto das HUs bloqueadas HU-031/HU-033 e HU-037 RN-005).
- Os 5 critérios de pronto do ROADMAP da Fase 4 satisfeitos com evidência.
- Escopo honesto confirmado: bairro/via/restrição reais; zona/lote bloqueadas e comunicadas (sem fachada).
- Enforcement real do grupo postgis: CI (.github/workflows/tests.yml) roda `--group postgis` com `POSTGIS_TESTS_REQUIRED=true` (skip vira FALHA) — o SQL espacial não pode deixar de rodar.
- Componente de mapa pronto para reuso nas Fases 7/8.
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/04-georreferenciamento-e-territorio/04-08-SUMMARY.md` com: resultados de cada comando de verificação (contagens de teste, incluindo a LINHA provando que o grupo postgis EXECUTOU — não skip), a confirmação do CI (.github/workflows/tests.yml com postgis + POSTGIS_TESTS_REQUIRED — o item "CI da suíte postgis" deixa de ser pendência), as evidências de integração real (chamada Nominatim + consulta espacial sobre bairro real + screenshots/observação dos bloqueios), e a lista explícita de camadas ENTREGUES vs BLOQUEADAS (zona LOUOS e lote cadastral + HU-037 RN-005 pendentes SEDUR) para o orquestrador atualizar STATE.md/ROADMAP.md. Se o bairro real não tiver carregado, registrar a fase como BLOQUEADA (não fechada).
</output>

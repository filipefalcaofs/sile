---
phase: 04-georreferenciamento-e-territorio
plan: "04-02"
type: execute
wave: 1
depends_on: []
requirements: [HU-014, HU-029, HU-037, HU-036]
files_modified:
  - database/seeders/ParameterSeeder.php
  - config/sile.php
  - database/seeders/RolesAndPermissionsSeeder.php
  - tests/Feature/Seeders/ParameterSeederTest.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
  - tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
autonomous: true

must_haves:
  truths:
    - "O catálogo HU-014 ganha 4 parâmetros novos do EP04 (toggle de geocodificação, base_url, throttle por minuto, limiar de sobreposição) com tipo, default e validação"
    - "Settings::get cai no fallback de config/sile.php sem banco (build/CI/Unit) para as 4 chaves novas e para as constantes técnicas de geocodificação"
    - "Existe a permissão consultar-territorio concedida a analista, gestor e administrador (CA-04 das HUs do EP04)"
    - "A contagem do catálogo passa de 25 para 29 em ParameterSeederTest e DatabaseSeederTest"
  artifacts:
    - path: "database/seeders/ParameterSeeder.php"
      provides: "4 chaves novas no catalog()"
      contains: "geo.validacao.sobreposicao_minima"
    - path: "config/sile.php"
      provides: "fallbacks geo/features/seguranca + integrations.geocoding.* (constantes técnicas)"
      contains: "'geocoding'"
    - path: "database/seeders/RolesAndPermissionsSeeder.php"
      provides: "permissão consultar-territorio"
      contains: "consultar-territorio"
  key_links:
    - from: "config/sile.php"
      to: "Settings::get('integrations.geocoding.base_url')"
      via: "fallback de config"
      pattern: "integrations.*geocoding.*base_url"
    - from: "database/seeders/RolesAndPermissionsSeeder.php"
      to: "analista/gestor/administrador"
      via: "givePermissionTo aditivo"
      pattern: "consultar-territorio"
---

<objective>
Registrar no catálogo de parâmetros administráveis (HU-014) os 4 valores de negócio novos do EP04 e seus fallbacks em `config/sile.php` (incluindo as constantes técnicas de geocodificação), e criar a permissão `consultar-territorio` que protege as telas/endpoints territoriais (CA-04). É a base parametrizável e de autorização consumida pela geocodificação (04-03), pela validação de localização (04-06) e pelas telas (04-07).

Purpose: Parametrização máxima (regra do projeto): toggle de feature, URL de integração trocável sem deploy, limite de throttle e limiar de negócio (sobreposição) administráveis com validação e histórico. A permissão fecha o CA-04 (segurança de acesso) das HUs do EP04.
Output: 4 parâmetros novos (catálogo 25 → 29), fallbacks em config/sile.php, permissão `consultar-territorio` seedada, e os 3 testes de seeder atualizados.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/04-georreferenciamento-e-territorio/04-CONTEXT.md
@.planning/phases/04-georreferenciamento-e-territorio/04-RESEARCH.md
@database/seeders/ParameterSeeder.php
@config/sile.php
@database/seeders/RolesAndPermissionsSeeder.php
@tests/Feature/Seeders/ParameterSeederTest.php
@tests/Feature/Seeders/DatabaseSeederTest.php
@tests/Feature/Authorization/RolesAndPermissionsSeederTest.php

Decisões travadas (RESEARCH, seção "Parâmetros novos"):
- Constantes técnicas (timeout/retries/backoff/cache_ttl/user_agent) ficam SÓ em config/sile.php, NUNCA no registry (precedente [02-02]/[03-01]).
- ParameterSeeder faz upsert só de metadados (value administrado é preservado).
- Chaves pt-BR (geo.*, seguranca.*) espelham o nome do parâmetro — Settings::get lê config("sile.{chave}").
</context>

<tasks>

<task type="auto">
  <name>Task 1: 4 parâmetros novos do EP04 + fallbacks em config/sile.php — TDD</name>
  <requirements>HU-014, HU-029, HU-037</requirements>
  <read_first>
    - database/seeders/ParameterSeeder.php (catalog() — onde adicionar)
    - config/sile.php (estrutura de fallbacks; bloco integrations.cnpj_lookup como espelho)
    - tests/Feature/Seeders/ParameterSeederTest.php (asserções de contagem 25 e por chave)
    - tests/Feature/Seeders/DatabaseSeederTest.php (assertSame(25, Parameter::count()))
  </read_first>
  <action>
    Em `ParameterSeeder::catalog()`, adicionar EXATAMENTE estas 4 chaves (seguindo o formato existente):
    ```php
    'features.geocoding' => [
        'group' => 'features', 'type' => 'boolean', 'default_value' => '1',
        'validation_rules' => ['required', 'boolean'],
        'description' => 'Habilita a geocodificação de endereços (Nominatim/OSM)',
    ],
    'integrations.geocoding.base_url' => [
        'group' => 'integracoes', 'type' => 'string',
        'default_value' => 'https://nominatim.openstreetmap.org',
        'validation_rules' => ['required', 'url'],
        'requires_connection_test' => true,
        'description' => 'URL base do serviço de geocodificação (Nominatim; trocável por self-host sem deploy)',
    ],
    'seguranca.throttle.geocoding.por_minuto' => [
        'group' => 'seguranca', 'type' => 'integer', 'default_value' => '60',
        'validation_rules' => ['required', 'integer', 'min:1', 'max:300'],
        'description' => 'Limite de geocodificações por minuto por usuário (Nominatim recomenda ~1 req/s)',
    ],
    'geo.validacao.sobreposicao_minima' => [
        'group' => 'geo', 'type' => 'integer', 'default_value' => '50',
        'validation_rules' => ['required', 'integer', 'min:1', 'max:100'],
        'description' => 'Percentual mínimo de sobreposição entre o polígono informado e o lote oficial antes de alertar (HU-037)',
    ],
    ```

    Em `config/sile.php`, adicionar os fallbacks (chaves pt-BR espelhando os params + bloco de constantes técnicas de geocodificação):
    ```php
    'features' => [ /* ...existentes... */ 'geocoding' => true ],
    'geo' => ['validacao' => ['sobreposicao_minima' => 50]],
    'seguranca' => ['throttle' => [ /* cnpj_lookup existente */ 'geocoding' => ['por_minuto' => 60] ]],
    'integrations' => [
        // ...cnpj_lookup, govbr existentes...
        'geocoding' => [
            'base_url' => 'https://nominatim.openstreetmap.org',
            'user_agent' => 'SILE-SEDUR-Salvador/1.0 (contato@sedur.salvador.ba.gov.br)',
            'timeout' => 8, 'retries' => 2, 'backoff_ms' => 1000, 'cache_ttl' => 86400,
        ],
    ],
    ```
    Atenção: NÃO duplicar os blocos `features`/`seguranca`/`integrations` — adicionar as chaves DENTRO dos arrays já existentes.

    RED → atualizar `ParameterSeederTest`: trocar `assertSame(25, ...)` por `assertSame(29, ...)` (2 ocorrências: `test_seeder_cria_catalogo_completo` e `test_seeder_e_idempotente`); incluir 'geo' na lista de grupos distintos esperada (`['features', 'geo', 'integracoes', 'retencao', 'seguranca', 'ui']`); adicionar um método `test_seeder_registra_parametros_do_georreferenciamento()` asseverando as 4 chaves (group/type/default_value/validation_rules; base_url com requires_connection_test true). Em `DatabaseSeederTest`, trocar as 2 ocorrências de `assertSame(25, Parameter::query()->count())` por `29`. Rodar (RED: contagem 29 vs 25 atual). GREEN → adicionar as chaves. REFACTOR.
  </action>
  <verify>php artisan test --compact tests/Feature/Seeders/ParameterSeederTest.php tests/Feature/Seeders/DatabaseSeederTest.php</verify>
  <acceptance_criteria>
    - `rg "features.geocoding|integrations.geocoding.base_url|seguranca.throttle.geocoding.por_minuto|geo.validacao.sobreposicao_minima" database/seeders/ParameterSeeder.php` casa as 4.
    - `rg "'geocoding'" config/sile.php` casa (em features, seguranca.throttle e integrations).
    - `rg "assertSame\(29," tests/Feature/Seeders/ParameterSeederTest.php tests/Feature/Seeders/DatabaseSeederTest.php` casa.
    - `php artisan test --compact tests/Feature/Seeders/ParameterSeederTest.php tests/Feature/Seeders/DatabaseSeederTest.php` verde.
  </acceptance_criteria>
  <done>Catálogo HU-014 com 29 parâmetros; as 4 chaves do EP04 têm fallback em config e são preservadas em re-seed; Settings::get funciona sem banco.</done>
</task>

<task type="auto">
  <name>Task 2: Permissão consultar-territorio (CA-04) — TDD</name>
  <requirements>HU-036, HU-029, HU-037</requirements>
  <read_first>
    - database/seeders/RolesAndPermissionsSeeder.php (lista de permissões + atribuição aditiva por papel)
    - tests/Feature/Authorization/RolesAndPermissionsSeederTest.php (padrão de asserção de permissões por papel)
  </read_first>
  <action>
    Em `RolesAndPermissionsSeeder::run()`, adicionar `'consultar-territorio'` ao array `$permissions`. Conceder (givePermissionTo aditivo, NUNCA sync) a `analista`, `gestor` e `administrador` — alinhado a HU-036 (analista consulta camadas) e às demais HUs do EP04. NÃO conceder a `cidadao` (o uso do cidadão entra nas Fases 7/8 com as próprias permissões).

    RED → em `RolesAndPermissionsSeederTest`, adicionar `test_papeis_recebem_permissao_consultar_territorio()`: após seed, `administrador`, `analista` e `gestor` têm `consultar-territorio`; `cidadao` NÃO tem. Se houver teste que conta o total de permissões, incrementar o número esperado. Rodar (RED). GREEN → adicionar. REFACTOR.
  </action>
  <verify>php artisan test --compact tests/Feature/Authorization/RolesAndPermissionsSeederTest.php</verify>
  <acceptance_criteria>
    - `rg "consultar-territorio" database/seeders/RolesAndPermissionsSeeder.php` casa (na lista e nas atribuições de analista/gestor/administrador).
    - `rg "consultar_territorio|consultar-territorio" tests/Feature/Authorization/RolesAndPermissionsSeederTest.php` casa.
    - `php artisan test --compact tests/Feature/Authorization/RolesAndPermissionsSeederTest.php` verde.
  </acceptance_criteria>
  <done>Permissão consultar-territorio existe e é concedida a analista/gestor/administrador; cidadão não a recebe; provado por teste.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact tests/Feature/Seeders/ tests/Feature/Authorization/RolesAndPermissionsSeederTest.php` verde.
- `php artisan config:show sile.integrations.geocoding.base_url` retorna o default.
</verification>

<success_criteria>
- 4 parâmetros novos no catálogo (25 → 29) com tipo/default/validação e fallback em config.
- Permissão consultar-territorio seedada e concedida aos papéis de gestão.
- Testes de seeder (parâmetros, database, permissões) verdes.
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/04-georreferenciamento-e-territorio/04-02-SUMMARY.md` com: as 4 chaves novas (e seus defaults/validações), a contagem do catálogo (29), e a permissão consultar-territorio + papéis. Estes são o contrato para 04-03 (toggle/base_url/throttle), 04-06 (sobreposicao_minima) e 04-03/04-06/04-07 (permissão).
</output>

---
phase: 01-identidade-acesso-e-auditoria-transversal
plan: 06
type: execute
wave: 6
depends_on: ["01-05"]
files_modified:
  - app/Providers/FortifyServiceProvider.php
  - app/Actions/Fortify/UpdateUserPassword.php
  - routes/web.php
  - routes/settings.php
  - app/Http/Controllers/Settings/PasswordController.php
  - resources/js/pages/auth/login.tsx
  - resources/js/pages/auth/forgot-password.tsx
  - resources/js/pages/auth/reset-password.tsx
  - resources/js/layouts/settings-layout.tsx
  - resources/js/pages/settings/password.tsx
  - tests/Feature/Auth/AuthenticationTest.php
  - tests/Feature/Auth/PasswordResetTest.php
  - tests/Feature/Settings/PasswordUpdateTest.php
autonomous: true
requirements: ["HU-002", "HU-003", "HU-004"]

must_haves:
  truths:
    - "Usuário autentica e é redirecionado ao ambiente do seu perfil — portal ou gestão (HU-002 CA-01)"
    - "Login, falha de login, bloqueio temporário e logout ficam registrados no histórico de acessos (HU-002 CA-02/CA-03)"
    - "Cidadão autenticado que tenta a gestão recebe 403 auditado (HU-002 CA-04)"
    - "Usuário recupera a senha por link de e-mail com token e expira/invalida corretamente (HU-003)"
    - "Usuário autenticado altera a própria senha exigindo a senha atual e a política vigente (HU-004)"
    - "Tentativas de login são limitadas pelo parâmetro administrável, nunca hardcoded"
  artifacts:
    - path: "app/Providers/FortifyServiceProvider.php"
      provides: "Views Inertia de login/recuperação, rate limiter parametrizado e redirect por perfil"
      contains: "LoginResponse"
    - path: "resources/js/pages/auth/login.tsx"
      provides: "Tela de login pt-BR"
    - path: "routes/settings.php"
      provides: "Grupo de configurações da conta (auth+verified+lgpd)"
    - path: "resources/js/pages/settings/password.tsx"
      provides: "Tela de alteração de senha"
  key_links:
    - from: "app/Providers/FortifyServiceProvider.php"
      to: "config/sile.php"
      via: "RateLimiter::for('login') lendo security.login.max_attempts"
      pattern: "max_attempts"
    - from: "app/Providers/FortifyServiceProvider.php"
      to: "routes/gestao.php"
      via: "LoginResponse redireciona can('acessar-gestao') para gestao.dashboard"
      pattern: "acessar-gestao"
    - from: "app/Actions/Fortify/UpdateUserPassword.php"
      to: "app/Support/Audit/AuditService.php"
      via: "auditoria explícita da troca de senha"
      pattern: "senha-alterada"
---

<objective>
Entregar HU-002 (Autenticar usuário), HU-003 (Recuperar senha) e HU-004 (Alterar senha): login único com redirect por perfil para os dois ambientes, throttling parametrizado com bloqueio auditado, recuperação por link de e-mail e alteração de senha autenticada — os 4 CAs de cada HU cobertos por testes.

Propósito: completa o ciclo de autenticação sobre o Fortify; o histórico de acessos (listeners do 01-02) passa a ser alimentado por requests reais.
Output: jornada completa de autenticação navegável e testada.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-CONTEXT.md
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md
@docs/SILE_HUs_Completas_MD/EP01-Identidade-Acesso-e-Seguranca/HU-002-Autenticar-usuario.md
@docs/SILE_HUs_Completas_MD/EP01-Identidade-Acesso-e-Seguranca/HU-003-Recuperar-senha.md
@docs/SILE_HUs_Completas_MD/EP01-Identidade-Acesso-e-Seguranca/HU-004-Alterar-senha.md
</context>

<important_notes>
- **Decisão travada (RESEARCH, Padrão 1):** UM guard `web`, UMA tela de login; o redirect pós-login decide o ambiente via binding de `Laravel\Fortify\Contracts\LoginResponse` (`can('acessar-gestao')` → gestão; senão portal). Dois guards foram rejeitados.
- Rate limit NUNCA hardcoded: `Settings::get('security.login.max_attempts')` (config sile do plano 01-01).
- Usuários de teste para login real: `User::factory()->withAcceptedLgpdTerm()->...` quando o teste navegar além do redirect; o redirect do login em si (302) não passa pelo middleware lgpd.
- O POST `/login` em teste dispara os listeners reais do plano 01-02 — assertar efeitos em `access_logs` (não usar `Event::fake()`).
- PUT `/user/password` do Fortify usa error bag `updatePassword` — usar `assertSessionHasErrorsIn('updatePassword', ...)`.
- **Wave 6 paralela (01-06 ∥ 01-07, mesmo worktree):** NÃO rodar `php artisan test --compact` (suíte completa) durante este plano — um RED transitório do 01-07 quebraria a verificação. Todas as verificações deste plano são ESCOPADAS aos grupos próprios (`tests/Feature/Auth`, `tests/Feature/Settings`); a suíte completa roda no fechamento da wave 6 (sampling do executor, conforme 01-VALIDATION.md) e novamente no 01-08/01-09.
- TDD estrito. Pint após PHP. Typecheck/build após TSX.
</important_notes>

<tasks>

<task type="auto">
  <name>Task 1: HU-002 — Login com redirect por perfil, throttle parametrizado e auditoria (TDD)</name>
  <files>app/Providers/FortifyServiceProvider.php, resources/js/pages/auth/login.tsx, tests/Feature/Auth/AuthenticationTest.php</files>
  <read_first>
    - app/Providers/FortifyServiceProvider.php (estado pós 01-04 — views de register/verify já registradas; método register() atual; rate limiter 'login' publicado)
    - .planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md (Padrão 1 — código do rate limiter e do LoginResponse)
    - app/Listeners/RecordSuccessfulLogin.php (efeitos esperados nos asserts)
    - tests/Feature/Audit/AccessLogRecordingTest.php (asserts de access_logs já provados no 01-02 — não duplicar, focar no redirect por perfil e CA-04)
    - resources/js/pages/auth/register.tsx (convenções de form estabelecidas no 01-04)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Auth/AuthenticationTest.php` com `RefreshDatabase` + seed de roles no setUp:
    - `test_pagina_de_login_renderiza`: GET `/login` → 200, component `auth/login`, prop `canResetPassword` presente.
    - `test_cidadao_autentica_e_vai_para_o_portal` (CA-01): cidadão; POST `/login` `['email' => ..., 'password' => 'password']` → `assertAuthenticated()`; redirect contém `/portal`.
    - `test_administrador_autentica_e_vai_para_a_gestao` (CA-01): admin → redirect contém `/gestao`.
    - `test_login_gera_registro_no_historico_de_acessos` (CA-02): após POST → `assertDatabaseHas('access_logs', ['user_id' => $user->id, 'event' => 'login', 'ip_address' => '127.0.0.1'])`.
    - `test_credencial_invalida_nao_autentica_e_registra_falha` (CA-03): senha errada → `assertGuest()`, `assertSessionHasErrors('email')`, `assertDatabaseHas('access_logs', ['email' => $user->email, 'event' => 'falha'])`.
    - `test_bloqueio_temporario_apos_tentativas_excedidas` (CA-03/RN parametrizada): ler `$max = (int) \App\Support\Settings::get('security.login.max_attempts');` e errar a senha `$max` vezes; a tentativa seguinte → `assertTooManyRequests()` (seguir starter kit) e `assertDatabaseHas('access_logs', ['event' => 'bloqueio'])`.
    - `test_logout_encerra_sessao_e_registra` (CA-02): cidadão autenticado POST `/logout` → `assertGuest()`; access_logs `event = 'logout'`.
    - `test_cidadao_logado_nao_acessa_gestao_e_bloqueio_e_auditado` (CA-04): login real de cidadão com termo aceito; GET `/gestao` → 403; `assertDatabaseHas('activity_log', ['event' => 'acesso-negado', 'result' => 'bloqueado'])`.
    CONFIRMAR RED (view de login inexistente; redirect default errado).

    **GREEN**:
    1. `FortifyServiceProvider::boot()` — acrescentar:
       ```php
       Fortify::loginView(fn (Request $request) => Inertia::render('auth/login', [
           'canResetPassword' => Features::enabled(Features::resetPasswords()),
           'status' => $request->session()->get('status'),
       ]));

       RateLimiter::for('login', function (Request $request) {
           $key = Str::transliterate(Str::lower((string) $request->input(Fortify::username())).'|'.$request->ip());

           return Limit::perMinute((int) Settings::get('security.login.max_attempts'))->by($key);
       });
       ```
       (Se o provider publicado já definir um rate limiter 'login', SUBSTITUIR pelo parametrizado acima — nunca dois registros.)
    2. `FortifyServiceProvider::register()` — redirect por perfil:
       ```php
       use Laravel\Fortify\Contracts\LoginResponse;

       $this->app->instance(LoginResponse::class, new class implements LoginResponse
       {
           public function toResponse($request)
           {
               $user = $request->user();

               $target = $user->can('acessar-gestao')
                   ? route('gestao.dashboard')
                   : route('portal.dashboard');

               return redirect()->intended($target);
           }
       });
       ```
    3. `resources/js/pages/auth/login.tsx`: auth-layout (criado no 01-03); `<Form action="/login" method="post">` com "E-mail", "Senha", checkbox "Manter conectado" (name="remember"), botão "Entrar"; link "Esqueci minha senha" para `/forgot-password` (renderizado se `canResetPassword`); link "Criar conta" para `/register`; exibe flash `status`.
    4. Conferir que a landing `resources/js/pages/home.tsx` (criada no 01-03) já contém os links para `/login` e `/register` — nenhuma alteração necessária nela.
    CONFIRMAR GREEN.

    **REFACTOR** — pint; typecheck.
  </action>
  <verify>php artisan test --compact tests/Feature/Auth/AuthenticationTest.php</verify>
  <acceptance_criteria>
    - `php artisan test --compact tests/Feature/Auth/AuthenticationTest.php` termina com `"result":"passed"` (8 testes)
    - `app/Providers/FortifyServiceProvider.php` contém `loginView`, `RateLimiter::for('login'` e `Settings::get('security.login.max_attempts')` (sem número 5 hardcoded)
    - `app/Providers/FortifyServiceProvider.php` contém `LoginResponse` e `route('gestao.dashboard')`
    - `resources/js/pages/auth/login.tsx` contém `action="/login"` e o texto `Entrar`
    - `resources/js/pages/home.tsx` contém `href="/login"`
  </acceptance_criteria>
  <done>HU-002 com os 4 CAs cobertos: login único, dois destinos por perfil, throttle parametrizado, tudo auditado em access_logs/activity_log.</done>
</task>

<task type="auto">
  <name>Task 2: HU-003 — Recuperação de senha por e-mail (TDD)</name>
  <files>app/Providers/FortifyServiceProvider.php, resources/js/pages/auth/forgot-password.tsx, resources/js/pages/auth/reset-password.tsx, tests/Feature/Auth/PasswordResetTest.php</files>
  <read_first>
    - app/Providers/FortifyServiceProvider.php (estado pós Task 1)
    - app/Actions/Fortify/ResetUserPassword.php (action publicada — confirma uso de passwordRules/Password::defaults)
    - app/Listeners/RecordPasswordResetActivity.php (efeito auditável esperado — event senha-redefinida)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Auth/PasswordResetTest.php` com `RefreshDatabase` + seed de roles:
    - `test_pagina_esqueci_senha_renderiza`: GET `/forgot-password` → 200, component `auth/forgot-password`.
    - `test_link_de_recuperacao_e_enviado` (CA-01): `Notification::fake();` POST `/forgot-password` `['email' => $user->email]` → `Notification::assertSentTo($user, ResetPassword::class)`.
    - `test_pagina_de_redefinicao_renderiza_com_token`: capturar `$notification->token` no callback do assertSentTo; GET `/reset-password/{token}?email=...` → 200, component `auth/reset-password`, props `email` e `token`.
    - `test_senha_e_redefinida_com_token_valido` (CA-01): POST `/reset-password` `['token' => $token, 'email' => $user->email, 'password' => 'NovaSenhaForte123', 'password_confirmation' => ...]` → redirect `/login` com status; `Hash::check('NovaSenhaForte123', $user->fresh()->password)` true.
    - `test_redefinicao_gera_auditoria` (CA-02): após redefinir → `assertDatabaseHas('activity_log', ['event' => 'senha-redefinida'])`.
    - `test_token_invalido_nao_redefine` (CA-03): token aleatório → `assertSessionHasErrors('email')`; senha antiga continua válida.
    - `test_email_desconhecido_nao_envia` (CA-03): `Notification::fake();` POST com e-mail inexistente → erro de sessão e `Notification::assertNothingSent()`.
    - `test_nova_senha_fraca_e_bloqueada` (CA-03/política): password `'abc'` com token válido → erro (Password::defaults aplicada pela action publicada).
    - `test_autenticado_nao_acessa_recuperacao` (CA-04): `actingAs($user)` GET `/forgot-password` → `assertRedirect(route('home'))` (rotas de recuperação são guest-only — o `RedirectIfAuthenticated` do framework redireciona autenticados para a rota nomeada `home`, a landing `/` criada no 01-03; `fortify.home` NÃO é usado pelo middleware `guest`). Mesma leitura do CA-04 aplicada à HU-001 (rota pública guest-only); registrar a interpretação no SUMMARY.
    CONFIRMAR RED.

    **GREEN**:
    1. `FortifyServiceProvider::boot()` — acrescentar:
       ```php
       Fortify::requestPasswordResetLinkView(fn (Request $request) => Inertia::render('auth/forgot-password', [
           'status' => $request->session()->get('status'),
       ]));

       Fortify::resetPasswordView(fn (Request $request) => Inertia::render('auth/reset-password', [
           'email' => $request->email,
           'token' => $request->route('token'),
       ]));
       ```
    2. `resources/js/pages/auth/forgot-password.tsx`: auth-layout; texto "Informe seu e-mail e enviaremos um link para redefinir sua senha."; `<Form action="/forgot-password" method="post">` com campo "E-mail" e botão "Enviar link de recuperação"; exibe flash `status`; link "Voltar ao login".
    3. `resources/js/pages/auth/reset-password.tsx`: auth-layout; `<Form action="/reset-password" method="post">` com hidden token, "E-mail" (readOnly com valor da prop), "Nova senha", "Confirmar nova senha", botão "Redefinir senha".
    CONFIRMAR GREEN.

    **REFACTOR** — pint; typecheck.
  </action>
  <verify>php artisan test --compact tests/Feature/Auth/PasswordResetTest.php</verify>
  <acceptance_criteria>
    - `php artisan test --compact tests/Feature/Auth/PasswordResetTest.php` termina com `"result":"passed"` (9 testes)
    - `tests/Feature/Auth/PasswordResetTest.php` contém `test_autenticado_nao_acessa_recuperacao`
    - `app/Providers/FortifyServiceProvider.php` contém `requestPasswordResetLinkView` e `resetPasswordView`
    - `resources/js/pages/auth/reset-password.tsx` contém `action="/reset-password"`
  </acceptance_criteria>
  <done>HU-003 com os 4 CAs cobertos: fluxo completo de recuperação com token (CA-01), redefinição auditada (CA-02), token/e-mail/senha inválidos bloqueados (CA-03) e rota guest-only para autenticados (CA-04).</done>
</task>

<task type="auto">
  <name>Task 3: HU-004 — Alteração de senha autenticada (TDD)</name>
  <files>app/Actions/Fortify/UpdateUserPassword.php, routes/web.php, routes/settings.php, app/Http/Controllers/Settings/PasswordController.php, resources/js/layouts/settings-layout.tsx, resources/js/pages/settings/password.tsx, tests/Feature/Settings/PasswordUpdateTest.php</files>
  <read_first>
    - app/Actions/Fortify/UpdateUserPassword.php (action publicada — estrutura update e error bag)
    - app/Support/Audit/AuditService.php (assinatura log(logName, event, description, ...) do 01-02)
    - routes/web.php (requires existentes — acrescentar settings)
    - resources/js/layouts/portal-layout.tsx (referência de layout)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Settings/PasswordUpdateTest.php` com `RefreshDatabase` + seed de roles; usuário base `User::factory()->cidadao()->withAcceptedLgpdTerm()->create()`:
    - `test_pagina_de_alteracao_de_senha_renderiza`: actingAs GET `/settings/password` → 200, component `settings/password`.
    - `test_senha_e_alterada_com_dados_validos` (CA-01): PUT `/user/password` `['current_password' => 'password', 'password' => 'NovaSenhaForte123', 'password_confirmation' => ...]` → sem erros; `Hash::check('NovaSenhaForte123', $user->fresh()->password)` true.
    - `test_alteracao_gera_auditoria` (CA-02): após PUT → `assertDatabaseHas('activity_log', ['log_name' => 'seguranca', 'event' => 'senha-alterada', 'result' => 'sucesso', 'causer_id' => $user->id])`.
    - `test_senha_atual_incorreta_bloqueia` (CA-03): current_password errada → `assertSessionHasErrorsIn('updatePassword', ['current_password'])`; senha inalterada.
    - `test_nova_senha_fora_da_politica_bloqueia` (CA-03): password `'abc'` → erro em password no bag updatePassword.
    - `test_visitante_nao_altera_senha` (CA-04): sem auth, GET `/settings/password` → redirect `/login`; PUT `/user/password` → redirect `/login`.
    CONFIRMAR RED (rota /settings/password inexistente; auditoria ausente).

    **GREEN**:
    1. `routes/settings.php` (novo):
       ```php
       <?php

       use App\Http\Controllers\Settings\PasswordController;
       use Illuminate\Support\Facades\Route;

       Route::middleware(['auth', 'verified', 'lgpd.accepted'])
           ->prefix('settings')
           ->name('settings.')
           ->group(function () {
               Route::get('password', [PasswordController::class, 'show'])->name('password.show');
           });
       ```
    2. `routes/web.php`: acrescentar `require __DIR__.'/settings.php';`.
    3. `php artisan make:controller Settings/PasswordController --no-interaction` — `show()`: `Inertia::render('settings/password', ['passwordRules' => Password::defaults()->toPasswordRulesString()])` (mesma alternativa do 01-04 se o método não existir).
    4. `app/Actions/Fortify/UpdateUserPassword.php` — após o `forceFill(...)->save()` existente, acrescentar:
       ```php
       app(\App\Support\Audit\AuditService::class)->log(
           'seguranca',
           'senha-alterada',
           'Senha alterada pelo próprio usuário',
       );
       ```
    5. Frontend:
       - `resources/js/layouts/settings-layout.tsx`: header "Configurações da conta" com link "Voltar ao painel" apontando para `/gestao` se `auth.permissions` inclui `acessar-gestao`, senão `/portal`; nav lateral com itens "Perfil" (`/settings/profile` — criado no plano 01-08) e "Senha" (`/settings/password`).
       - `resources/js/pages/settings/password.tsx`: settings-layout; `<Form action="/user/password" method="put" errorBag="updatePassword">` (conferir prop de error bag do componente Form v3; alternativa `useForm` se necessário) com "Senha atual", "Nova senha" (+ dica passwordRules), "Confirmar nova senha", botão "Alterar senha"; mensagem de sucesso via flash/estado `recentlySuccessful`.
    CONFIRMAR GREEN.

    **REFACTOR** — pint; `npm run typecheck && npm run build`; rodar os grupos do plano: `php artisan test --compact tests/Feature/Auth tests/Feature/Settings` (NÃO a suíte completa — wave 6 paralela; ver important_notes).
  </action>
  <verify>php artisan test --compact tests/Feature/Settings/PasswordUpdateTest.php && php artisan test --compact tests/Feature/Auth tests/Feature/Settings</verify>
  <acceptance_criteria>
    - `routes/settings.php` contém `'lgpd.accepted'` e `password.show`
    - `routes/web.php` contém `require __DIR__.'/settings.php'`
    - `app/Actions/Fortify/UpdateUserPassword.php` contém `senha-alterada`
    - `php artisan test --compact tests/Feature/Settings/PasswordUpdateTest.php` termina com `"result":"passed"` (6 testes)
    - `php artisan test --compact tests/Feature/Auth tests/Feature/Settings` termina com `"result":"passed"` (grupos do plano — suíte completa fica para o fechamento da wave 6)
  </acceptance_criteria>
  <done>HU-004 com os 4 CAs cobertos: troca de senha exigindo a atual, política parametrizada, auditada sem expor hash.</done>
</task>

</tasks>

<verification>
1. `php artisan test --compact tests/Feature/Auth tests/Feature/Settings` — HU-002/003/004 verdes (escopo do plano).
2. Suíte completa NÃO roda dentro deste plano (wave 6 paralela com 01-07) — ela roda no fechamento da wave 6 pelo executor (sampling do 01-VALIDATION.md).
3. `npm run typecheck && npm run build`.
4. `vendor/bin/pint --dirty --format agent`.
</verification>

<success_criteria>
- HU-002: login por perfil, access_logs de login/falha/bloqueio/logout, 403 auditado — testes verdes.
- HU-003: fluxo completo de recuperação com auditoria — testes verdes.
- HU-004: alteração com senha atual + política + auditoria — testes verdes.
- Nenhum limite/expiração hardcoded (Settings/config sile).
</success_criteria>

<output>
Após concluir, criar `.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-06-SUMMARY.md` registrando o comportamento real do throttle (429 vs redirect) e o mecanismo de error bag usado no Form do Inertia v3.
</output>

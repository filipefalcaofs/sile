---
phase: 01-identidade-acesso-e-auditoria-transversal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - composer.lock
  - config/fortify.php
  - config/sile.php
  - config/permission.php
  - config/activitylog.php
  - app/Providers/AppServiceProvider.php
  - app/Providers/FortifyServiceProvider.php
  - app/Actions/Fortify/CreateNewUser.php
  - app/Actions/Fortify/ResetUserPassword.php
  - app/Actions/Fortify/UpdateUserPassword.php
  - app/Support/Settings.php
  - app/Http/Middleware/SecurityHeaders.php
  - bootstrap/app.php
  - bootstrap/providers.php
  - tests/TestCase.php
  - tests/Unit/Support/SettingsTest.php
  - tests/Feature/Security/SecurityHeadersTest.php
  - .env
  - .env.example
  - lang/pt_BR/*
  - database/migrations/* (publicadas por fortify/spatie)
autonomous: true
requirements: []

must_haves:
  truths:
    - "Suite de testes roda verde com Fortify, spatie/permission e spatie/activitylog instalados"
    - "Parâmetros de segurança (política de senha, tentativas de login, expiração de reset) são lidos de config/sile.php — nenhum valor hardcoded em código de produção"
    - "Mensagens nativas de validação/auth saem em pt-BR"
    - "Toda resposta web carrega os cabeçalhos de segurança (X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy)"
  artifacts:
    - path: "config/sile.php"
      provides: "Parâmetros de negócio da fase com defaults (preparação HU-014)"
      contains: "max_attempts"
    - path: "app/Support/Settings.php"
      provides: "Wrapper de leitura de parâmetros (config-backed na Fase 1)"
      exports: ["get"]
    - path: "app/Http/Middleware/SecurityHeaders.php"
      provides: "Cabeçalhos de segurança em todas as respostas web (referência SIGVISA)"
      contains: "X-Frame-Options"
    - path: "lang/pt_BR/validation.php"
      provides: "Traduções pt-BR do framework"
    - path: "tests/TestCase.php"
      provides: "Base de testes com withoutVite()"
      contains: "withoutVite"
  key_links:
    - from: "app/Providers/AppServiceProvider.php"
      to: "config/sile.php"
      via: "Password::defaults() lendo config('sile.security.password.*')"
      pattern: "Password::defaults"
    - from: "app/Support/Settings.php"
      to: "config/sile.php"
      via: "Settings::get() delega para config('sile...')"
      pattern: "config\\('sile"
    - from: "bootstrap/app.php"
      to: "app/Http/Middleware/SecurityHeaders.php"
      via: "append no grupo web"
      pattern: "SecurityHeaders::class"
---

<objective>
Preparar a fundação da Fase 1: instalar as dependências travadas pela pesquisa (Fortify ^1.37, spatie/laravel-permission ^8, spatie/laravel-activitylog ^5, laravel-lang/common ^6.8), configurar locale pt-BR, preparar a base de testes, criar a camada de parametrização (`config/sile.php` + `Settings`) que elimina valores de negócio hardcoded desde o primeiro dia e aplicar os cabeçalhos de segurança em todas as respostas web.

Purpose: todas as demais entregas da fase dependem destes pacotes e da parametrização; o smoke do esqueleto verde antes de qualquer mudança é o ponto de partida do TDD.
Output: dependências instaladas e publicadas, locale pt-BR ativo, `config/sile.php` + `App\Support\Settings` testados, SecurityHeaders ativo, suite verde.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-CONTEXT.md
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md

Convenções obrigatórias: usar `php artisan make:` para criar arquivos; `vendor/bin/pint --dirty --format agent` após alterar PHP; usar `search-docs` (Laravel Boost MCP) antes de implementar quando o MCP estiver ativo; código em inglês, testes nomeados em pt-BR (snake_case sem acentos).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — smoke do esqueleto, instalação e publicação das dependências, locale pt-BR</name>
  <files>composer.json, composer.lock, config/fortify.php, config/permission.php, config/activitylog.php, app/Providers/FortifyServiceProvider.php, app/Actions/Fortify/*, bootstrap/providers.php, database/migrations/* (publicadas), tests/TestCase.php, .env, .env.example, lang/pt_BR/*</files>
  <read_first>
    - composer.json (estado atual das dependências)
    - tests/TestCase.php (base atual, vazia)
    - bootstrap/providers.php (apenas AppServiceProvider hoje)
    - phpunit.xml (sqlite :memory:, MAIL_MAILER=array — não alterar)
    - .planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md (seção "Standard Stack" — comandos de instalação)
  </read_first>
  <action>
    Esta task é de setup (sem código de produção autoral) — o "RED" aqui é o smoke test do esqueleto e o "GREEN" é a suite continuar verde após instalar tudo.

    1. Smoke inicial: rodar `php artisan test --compact` e confirmar verde (2 ExampleTest). Se falhar, parar e investigar antes de instalar qualquer coisa.
    2. Instalar dependências de produção:
       `composer require laravel/fortify:^1.37 spatie/laravel-permission:^8.0 spatie/laravel-activitylog:^5.0 --no-interaction`
    3. Instalar dependência de dev:
       `composer require laravel-lang/common:^6.8 --dev --no-interaction`
    4. Instalar/publicar Fortify: `php artisan fortify:install --no-interaction`. Isso publica `app/Actions/Fortify/*`, `app/Providers/FortifyServiceProvider.php`, `config/fortify.php` e registra o provider em `bootstrap/providers.php`. Confirmar o registro com grep. Migrations publicadas de 2FA/passkeys são inofensivas com as features desligadas — NÃO excluí-las (não brigar com o pacote).
    5. Publicar spatie:
       `php artisan vendor:publish --provider="Spatie\Permission\PermissionServiceProvider" --no-interaction`
       `php artisan vendor:publish --provider="Spatie\Activitylog\ActivitylogServiceProvider" --tag="activitylog-migrations" --no-interaction`
       `php artisan vendor:publish --provider="Spatie\Activitylog\ActivitylogServiceProvider" --tag="activitylog-config" --no-interaction`
       NÃO editar a migration do activity_log nesta task (o Plano 01-02 a estende com as colunas SILE antes de qualquer migrate em dev). NÃO rodar `php artisan migrate` contra o Postgres de dev neste plano — os testes rodam suas próprias migrations no sqlite :memory:.
    6. Editar `config/fortify.php`:
       - array `features` deve conter EXATAMENTE:
         `Features::registration(), Features::resetPasswords(), Features::emailVerification(), Features::updatePasswords()`
         — 2FA e passkeys FORA (não constam nas HUs).
       - chave `'home' => '/portal'` (destino pós-cadastro/verificação — a rota `portal.dashboard` responde em `/portal` e nasce no plano 01-03; os testes do 01-04 assertam redirect para `/portal`; o redirect pós-login por perfil é definido no plano 01-06 via LoginResponse).
    7. Traduções pt-BR: `php artisan lang:add pt_BR` e commitar o diretório `lang/`. Editar `.env` E `.env.example`: `APP_LOCALE=pt_BR`, `APP_FALLBACK_LOCALE=pt_BR`, `APP_FAKER_LOCALE=pt_BR`.
    8. Editar `tests/TestCase.php` para evitar ViteException (Pitfall 1 do RESEARCH):
       ```php
       protected function setUp(): void
       {
           parent::setUp();
           $this->withoutVite();
       }
       ```
    9. Rodar `php artisan route:list --path=login` e `php artisan route:list --path=register` e REGISTRAR no SUMMARY os nomes reais das rotas do Fortify (confiança BAIXA na pesquisa — os testes dos planos seguintes usam URIs literais `/login`, `/register` etc., mas os nomes devem ficar documentados).
    10. Rodar a suite completa de novo: `php artisan test --compact` — deve continuar verde.
  </action>
  <verify>
    `php artisan test --compact` (verde); `php artisan route:list --path=login` (lista GET e POST /login); `vendor/bin/pint --dirty --format agent`
  </verify>
  <acceptance_criteria>
    - `composer.json` contém `"laravel/fortify"`, `"spatie/laravel-permission"`, `"spatie/laravel-activitylog"` em require e `"laravel-lang/common"` em require-dev
    - `bootstrap/providers.php` contém `FortifyServiceProvider`
    - `config/fortify.php` contém `Features::registration()` e `'home' => '/portal'`, e NÃO contém `Features::twoFactorAuthentication()` descomentado
    - `tests/TestCase.php` contém `$this->withoutVite();`
    - `.env.example` contém `APP_LOCALE=pt_BR`
    - Arquivo `lang/pt_BR/validation.php` existe
    - `php artisan test --compact` sai com código 0
  </acceptance_criteria>
  <done>Esqueleto continua verde com todas as dependências da fase instaladas, Fortify com as 4 features das HUs habilitadas e home em /portal, locale pt-BR ativo e base de testes imune ao Vite.</done>
</task>

<task type="auto">
  <name>Task 2: TDD — config/sile.php, wrapper Settings e política de senha parametrizada</name>
  <files>config/sile.php, app/Support/Settings.php, app/Providers/AppServiceProvider.php, tests/Unit/Support/SettingsTest.php</files>
  <read_first>
    - app/Providers/AppServiceProvider.php (estado atual — boot vazio)
    - .planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md (Padrão 7 — parametrização sem UI)
    - config/auth.php (chave passwords.users.expire que será espelhada)
  </read_first>
  <action>
    TDD Red-Green-Refactor:

    RED — criar `tests/Unit/Support/SettingsTest.php` (via `php artisan make:test --phpunit --unit Support/SettingsTest`) com:
    - `test_le_parametro_de_config_sile`: `Settings::get('security.login.max_attempts')` retorna `5`.
    - `test_retorna_default_quando_parametro_ausente`: `Settings::get('inexistente.chave', 'padrao')` retorna `'padrao'`.
    - `test_politica_de_senha_reflete_parametros`: `Validator::make(['password' => 'fraca'], ['password' => Password::defaults()])` falha; `Validator::make(['password' => 'SenhaForte1'], ['password' => Password::defaults()])` passa (>= 8 chars, maiúscula+minúscula, número — símbolos desligados por default).
    Rodar `php artisan test --compact --filter=SettingsTest` e CONFIRMAR que falha (classes inexistentes).

    GREEN — implementar:
    1. `config/sile.php` (novo) com exatamente:
       ```php
       return [
           'security' => [
               'password' => [
                   'min_length' => 8,
                   'require_mixed_case' => true,
                   'require_numbers' => true,
                   'require_symbols' => false,
               ],
               'login' => ['max_attempts' => 5],
               'password_reset_expire' => 60,
           ],
       ];
       ```
    2. `app/Support/Settings.php` (via `php artisan make:class Support/Settings`):
       ```php
       class Settings
       {
           public static function get(string $key, mixed $default = null): mixed
           {
               return config("sile.{$key}", $default);
           }
       }
       ```
       PHPDoc explicando: na Fase 2 (HU-014) o backend passa a ser banco sem tocar nos call sites.
    3. `AppServiceProvider::boot()`:
       ```php
       Password::defaults(function () {
           $config = config('sile.security.password');
           $rule = Password::min($config['min_length']);
           if ($config['require_mixed_case']) { $rule->mixedCase(); }
           if ($config['require_numbers']) { $rule->numbers(); }
           if ($config['require_symbols']) { $rule->symbols(); }
           return $rule;
       });
       config(['auth.passwords.users.expire' => (int) Settings::get('security.password_reset_expire', 60)]);
       ```
       (import `Illuminate\Validation\Rules\Password` — atenção para não confundir com `Illuminate\Support\Facades\Password`).
    Rodar o filtro de novo e confirmar verde.

    REFACTOR — revisar imports e rodar pint.
  </action>
  <verify>
    `php artisan test --compact --filter=SettingsTest` (verde, 3 testes); `php artisan test --compact` (suite toda verde); `vendor/bin/pint --dirty --format agent`
  </verify>
  <acceptance_criteria>
    - `config/sile.php` contém `'max_attempts' => 5` e `'password_reset_expire' => 60`
    - `app/Support/Settings.php` contém `public static function get(string $key, mixed $default = null): mixed`
    - `app/Providers/AppServiceProvider.php` contém `Password::defaults(` e `auth.passwords.users.expire`
    - `php artisan test --compact --filter=SettingsTest` sai com código 0 e roda 3 testes
  </acceptance_criteria>
  <done>Parâmetros de segurança centralizados em config/sile.php com wrapper Settings testado; política de senha do Fortify (cadastro/reset/alteração) derivada dos parâmetros — pronto para virar banco na HU-014 sem tocar call sites.</done>
</task>

<task type="auto">
  <name>Task 3: TDD — middleware SecurityHeaders no grupo web</name>
  <files>app/Http/Middleware/SecurityHeaders.php, bootstrap/app.php, tests/Feature/Security/SecurityHeadersTest.php</files>
  <read_first>
    - bootstrap/app.php (withMiddleware atual — append do HandleInertiaRequests no grupo web)
    - app/Http/Middleware/HandleInertiaRequests.php (referência de middleware existente no projeto)
    - .planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md (Padrão 8 — cabeçalhos exatos, referência SIGVISA)
  </read_first>
  <action>
    TDD Red-Green-Refactor:

    RED — criar `tests/Feature/Security/SecurityHeadersTest.php` (`php artisan make:test --phpunit Security/SecurityHeadersTest`) com:
    - `test_respostas_web_incluem_cabecalhos_de_seguranca`: GET `/` → `assertOk()` e:
      ```php
      $response->assertHeader('X-Frame-Options', 'DENY');
      $response->assertHeader('X-Content-Type-Options', 'nosniff');
      $response->assertHeader('Referrer-Policy', 'strict-origin-when-cross-origin');
      $response->assertHeader('Permissions-Policy', 'camera=(), geolocation=(self), microphone=()');
      ```
    Rodar `php artisan test --compact --filter=SecurityHeadersTest` e CONFIRMAR RED (cabeçalhos ausentes).

    GREEN —
    1. `php artisan make:middleware SecurityHeaders --no-interaction` com handle exato:
       ```php
       public function handle(Request $request, Closure $next): Response
       {
           $response = $next($request);

           $response->headers->set('X-Frame-Options', 'DENY');
           $response->headers->set('X-Content-Type-Options', 'nosniff');
           $response->headers->set('Referrer-Policy', 'strict-origin-when-cross-origin');
           $response->headers->set('Permissions-Policy', 'camera=(), geolocation=(self), microphone=()');

           return $response;
       }
       ```
       (`geolocation=(self)` em vez de `geolocation=()` — a Fase 4 usa mapa interativo que pode pedir a localização do usuário no próprio domínio.)
    2. `bootstrap/app.php` — acrescentar ao append do grupo web existente:
       ```php
       $middleware->web(append: [
           HandleInertiaRequests::class,
           \App\Http\Middleware\SecurityHeaders::class,
       ]);
       ```
    Rodar o filtro → verde.

    REFACTOR — pint; suite completa.
  </action>
  <verify>
    `php artisan test --compact --filter=SecurityHeadersTest` (verde); `php artisan test --compact` (suite toda); `vendor/bin/pint --dirty --format agent`
  </verify>
  <acceptance_criteria>
    - `app/Http/Middleware/SecurityHeaders.php` contém `X-Frame-Options` e `Permissions-Policy`
    - `bootstrap/app.php` contém `SecurityHeaders::class` dentro do append do grupo web
    - `php artisan test --compact --filter=SecurityHeadersTest` sai com código 0
    - `php artisan test --compact` (suite completa) sai com código 0
  </acceptance_criteria>
  <done>Toda resposta web (portal, gestão, auth) carrega os cabeçalhos de segurança — base transversal para todas as fases, provada por teste.</done>
</task>

</tasks>

<verification>
- `php artisan test --compact` verde (ExampleTests + SettingsTest + SecurityHeadersTest).
- `php artisan route:list` funciona e lista rotas do Fortify (login, register, forgot-password, reset-password, email/verify, user/password).
- `vendor/bin/pint --dirty --format agent` sem pendências.
</verification>

<success_criteria>
- Dependências da fase instaladas nas versões travadas pela pesquisa, com vendor publish feito.
- Locale pt-BR ativo com traduções commitadas.
- `config/sile.php` + `Settings` operacionais e testados; política de senha parametrizada.
- SecurityHeaders aplicado ao grupo web e coberto por teste.
- Nenhuma migration de dev executada ainda com schema incompleto do activity_log (extensão fica no Plano 01-02).
</success_criteria>

<output>
Após completar, criar `.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-01-SUMMARY.md` incluindo os nomes reais das rotas do Fortify levantados via `route:list`.
</output>

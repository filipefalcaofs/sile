---
phase: 01-identidade-acesso-e-auditoria-transversal
plan: 08
type: execute
wave: 7
depends_on: ["01-06", "01-07"]
files_modified:
  - app/Http/Requests/ProfileUpdateRequest.php
  - app/Http/Controllers/Settings/ProfileController.php
  - routes/settings.php
  - resources/js/pages/settings/profile.tsx
  - app/Http/Controllers/Portal/AccessHistoryController.php
  - app/Http/Controllers/Gestao/AccessHistoryController.php
  - config/sile.php
  - routes/portal.php
  - routes/gestao.php
  - resources/js/pages/portal/acessos.tsx
  - resources/js/pages/gestao/acessos.tsx
  - resources/js/layouts/portal-layout.tsx
  - tests/Feature/Settings/ProfileTest.php
  - tests/Feature/AccessHistory/AccessHistoryTest.php
autonomous: true
requirements: ["HU-007", "HU-010"]

must_haves:
  truths:
    - "Usuário consulta e atualiza os próprios dados pessoais; CPF é somente leitura (HU-007 CA-01)"
    - "Alterar o e-mail re-exige verificação (HU-007 RN)"
    - "Atualização do perfil fica auditada com as mudanças de atributos, sem campos sensíveis (HU-007 CA-02)"
    - "Usuário vê os próprios acessos — logins, saídas, falhas — com data/hora e IP (HU-010 CA-01)"
    - "Administrador consulta acessos de qualquer conta e essa consulta fica auditada (HU-010 CA-01/CA-02)"
    - "Quem não tem a permissão não consulta acessos de terceiros e a tentativa é auditada (HU-010 CA-04)"
  artifacts:
    - path: "app/Http/Controllers/Settings/ProfileController.php"
      provides: "Consulta/atualização do próprio perfil"
    - path: "resources/js/pages/portal/acessos.tsx"
      provides: "Histórico de acessos do próprio usuário (paginado)"
    - path: "app/Http/Controllers/Gestao/AccessHistoryController.php"
      provides: "Consulta administrativa de acessos por usuário"
  key_links:
    - from: "routes/gestao.php"
      to: "permissão consultar-acessos-de-qualquer-conta"
      via: "middleware permission na rota de acessos"
      pattern: "consultar-acessos-de-qualquer-conta"
    - from: "app/Http/Controllers/Gestao/AccessHistoryController.php"
      to: "app/Support/Audit/AuditService.php"
      via: "auditoria da consulta a dados de terceiro"
      pattern: "consulta-acessos"
    - from: "app/Http/Controllers/Settings/ProfileController.php"
      to: "verificação de e-mail"
      via: "email_verified_at = null ao trocar e-mail"
      pattern: "email_verified_at"
---

<objective>
Entregar HU-007 (Gerenciar perfil do usuário) e HU-010 (Consultar histórico de acessos): tela de perfil com atualização auditada e re-verificação de e-mail, e consulta do histórico de acessos — o próprio usuário no portal, qualquer conta na gestão (com permissão e consulta auditada).

Propósito: fecha o ciclo de autogestão da conta e dá visibilidade aos registros de acesso que os listeners do plano 01-02 vêm gravando.
Output: perfil e histórico navegáveis, com os 4 CAs de cada HU cobertos.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-CONTEXT.md
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md
@docs/SILE_HUs_Completas_MD/EP01-Identidade-Acesso-e-Seguranca/HU-007-Gerenciar-perfil-do-usuario.md
@docs/SILE_HUs_Completas_MD/EP01-Identidade-Acesso-e-Seguranca/HU-010-Consultar-historico-de-acessos.md
</context>

<important_notes>
- **CPF imutável pós-cadastro** (identidade do titular): exibido somente leitura no perfil; a atualização aceita apenas name, email e phone.
- Padrão starter kit (RESEARCH): ao alterar o e-mail, `email_verified_at = null` + reenvio da notificação de verificação.
- A consulta administrativa usa a permissão `consultar-acessos-de-qualquer-conta` (seedada no 01-03); analista tem `acessar-gestao` mas NÃO essa permissão — é o ator perfeito para o CA-04.
- Consulta de acessos de TERCEIRO é "consulta relevante" (CA-02 da HU-010): auditar explicitamente via AuditService.
- Testes navegando no portal/gestão usam os states `withAcceptedLgpdTerm()` + papel.
- **Limitação consciente de navegação:** a tela `gestao/acessos` é alcançável apenas por URL direta (`/gestao/acessos/{user}`) nesta fase — a listagem/busca de usuários da gestão é a HU-012 (Fase 2), que fechará a navegação. Registrar esta pendência no 01-08-SUMMARY.md (campo de concerns) para o STATE.md.
- **Page size parametrizado:** o tamanho da página do histórico NÃO é hardcoded — `Settings::get('ui.access_history.per_page', 15)` com a chave criada em `config/sile.php` (consistente com a regra de parametrização do projeto e com o lockout do 01-06).
- TDD estrito. Pint após PHP. Typecheck/build após TSX.
</important_notes>

<tasks>

<task type="auto">
  <name>Task 1: HU-007 — Gestão do próprio perfil (TDD)</name>
  <files>app/Http/Requests/ProfileUpdateRequest.php, app/Http/Controllers/Settings/ProfileController.php, routes/settings.php, resources/js/pages/settings/profile.tsx, tests/Feature/Settings/ProfileTest.php</files>
  <read_first>
    - routes/settings.php (grupo criado no 01-06 — onde as rotas entram)
    - resources/js/layouts/settings-layout.tsx (nav já aponta para /settings/profile)
    - app/Concerns/HasAuditoria.php (attribute_changes esperado no CA-02)
    - app/Models/User.php (#[Fillable] atual com cpf/phone)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Settings/ProfileTest.php` com `RefreshDatabase` + seed de roles; usuário base `User::factory()->cidadao()->withAcceptedLgpdTerm()->create()`:
    - `test_pagina_de_perfil_renderiza_com_dados`: GET `/settings/profile` → 200, component `settings/profile`, prop `user` com name/email/cpf/phone.
    - `test_atualiza_nome_e_telefone` (CA-01): PATCH `/settings/profile` `['name' => 'Nome Novo', 'email' => $user->email, 'phone' => '(71) 98888-7777']` → redirect; banco atualizado.
    - `test_atualizacao_gera_auditoria_sem_campos_sensiveis` (CA-02): após PATCH, activity `event = 'updated'` subject User; `attribute_changes` contém `name` e NÃO contém `password`.
    - `test_alterar_email_reexige_verificacao` (CA-01/RN): `Notification::fake();` PATCH com email novo → `email_verified_at` null no banco; `Notification::assertSentTo($user->fresh(), VerifyEmail::class)`.
    - `test_cpf_nao_e_alteravel` (CA-03): PATCH incluindo `'cpf' => '52998224725'` (diferente do atual) → CPF no banco INALTERADO (campo ignorado pela validação/fill).
    - `test_email_duplicado_bloqueia` (CA-03): PATCH com e-mail de outro usuário → `assertSessionHasErrors('email')`.
    - `test_visitante_nao_acessa_perfil` (CA-04): sem auth GET `/settings/profile` → redirect `/login`.
    CONFIRMAR RED.

    **GREEN**:
    1. `php artisan make:request ProfileUpdateRequest --no-interaction`:
       ```php
       public function rules(): array
       {
           return [
               'name' => ['required', 'string', 'max:255'],
               'email' => ['required', 'string', 'email', 'max:255',
                   Rule::unique('users')->ignore($this->user()->id)],
               'phone' => ['nullable', 'string', 'max:20'],
           ];
       }

       public function attributes(): array
       {
           return ['name' => 'nome', 'email' => 'e-mail', 'phone' => 'telefone'];
       }
       ```
    2. `php artisan make:controller Settings/ProfileController --no-interaction`:
       - `edit(Request $request)`: `Inertia::render('settings/profile', ['user' => $request->user()->only('id', 'name', 'email', 'cpf', 'phone'), 'mustVerifyEmail' => $request->user() instanceof MustVerifyEmail, 'status' => $request->session()->get('status')])`.
       - `update(ProfileUpdateRequest $request)`: `$user->fill($request->validated());` se `$user->isDirty('email')` → `$user->email_verified_at = null;` `$user->save();` se `$user->wasChanged('email')` → `$user->sendEmailVerificationNotification();` redirect back com `status` "Perfil atualizado com sucesso.".
    3. `routes/settings.php` — acrescentar no grupo:
       ```php
       Route::get('profile', [ProfileController::class, 'edit'])->name('profile.edit');
       Route::patch('profile', [ProfileController::class, 'update'])->name('profile.update');
       ```
    4. `resources/js/pages/settings/profile.tsx`: settings-layout; título "Meu perfil"; `<Form action="/settings/profile" method="patch">` com "Nome completo", "E-mail" (com aviso "Ao alterar o e-mail, você precisará confirmá-lo novamente."), "Telefone"; campo "CPF" disabled/readOnly com valor formatado (000.000.000-00) e legenda "O CPF não pode ser alterado."; botão "Salvar alterações"; mensagem de sucesso via flash status.
    CONFIRMAR GREEN.

    **REFACTOR** — pint; typecheck.
  </action>
  <verify>php artisan test --compact tests/Feature/Settings/ProfileTest.php</verify>
  <acceptance_criteria>
    - `php artisan test --compact tests/Feature/Settings/ProfileTest.php` termina com `"result":"passed"` (7 testes)
    - `app/Http/Controllers/Settings/ProfileController.php` contém `email_verified_at = null` e `sendEmailVerificationNotification`
    - `app/Http/Requests/ProfileUpdateRequest.php` NÃO contém regra para `cpf`
    - `routes/settings.php` contém `profile.edit` e `profile.update`
    - `resources/js/pages/settings/profile.tsx` contém `O CPF não pode ser alterado`
  </acceptance_criteria>
  <done>HU-007 com os 4 CAs cobertos: perfil consultável e atualizável, e-mail re-verificado, CPF imutável, mudanças auditadas.</done>
</task>

<task type="auto">
  <name>Task 2: HU-010 — Histórico de acessos no portal e consulta administrativa (TDD)</name>
  <files>app/Http/Controllers/Portal/AccessHistoryController.php, app/Http/Controllers/Gestao/AccessHistoryController.php, config/sile.php, routes/portal.php, routes/gestao.php, resources/js/pages/portal/acessos.tsx, resources/js/pages/gestao/acessos.tsx, resources/js/layouts/portal-layout.tsx, tests/Feature/AccessHistory/AccessHistoryTest.php</files>
  <read_first>
    - app/Models/AccessLog.php e database/factories/AccessLogFactory.php (model/factory do 01-02)
    - routes/portal.php (subgrupo protegido pós 01-07) e routes/gestao.php (grupo pós 01-05)
    - app/Support/Audit/AuditService.php (método log para a consulta administrativa — assinatura logName, event, description, properties)
    - resources/js/pages/portal/procuracoes/index.tsx (convenção de tabela estabelecida)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/AccessHistory/AccessHistoryTest.php` com `RefreshDatabase` + seed de roles:
    - `test_usuario_ve_somente_os_proprios_acessos` (CA-01): criar 3 AccessLog do usuário (`AccessLog::factory()->count(3)->create(['user_id' => $user->id, 'email' => $user->email])`) e 2 de OUTRO usuário (user_id e e-mail diferentes); GET `/portal/acessos` → 200, component `portal/acessos`, prop `logs.data` com exatamente 3 itens.
    - `test_bloqueio_sem_user_id_aparece_pelo_email` (CA-01): criar registro `['user_id' => null, 'email' => $user->email, 'event' => 'bloqueio']` (forma como o RecordLockout grava) → GET `/portal/acessos` → o registro aparece na listagem (titular do e-mail vê os próprios bloqueios e tentativas pré-login).
    - `test_acessos_exibem_evento_data_e_ip` (CA-01): registro com `event = 'falha'`, `ip_address = '10.0.0.1'` aparece com esses valores nas props.
    - `test_historico_e_paginado`: criar 20 registros → `logs.data` tem 15 itens (default do parâmetro `ui.access_history.per_page`) e `logs.links`/meta presentes.
    - `test_admin_consulta_acessos_de_qualquer_conta` (CA-01 admin): admin (`administrador()->withAcceptedLgpdTerm()`) GET `/gestao/acessos/{user}` → 200, component `gestao/acessos`, props `targetUser.name` e `logs.data` com os registros do alvo.
    - `test_consulta_administrativa_e_auditada` (CA-02): após o GET admin → `assertDatabaseHas('activity_log', ['log_name' => 'acessos', 'event' => 'consulta-acessos', 'result' => 'sucesso', 'causer_id' => $admin->id])`.
    - `test_analista_sem_permissao_nao_consulta_terceiros` (CA-04): analista (`analista()->withAcceptedLgpdTerm()`) GET `/gestao/acessos/{user}` → 403; activity `event = 'acesso-negado'`, `result = 'bloqueado'`.
    - `test_usuario_inexistente_retorna_404` (CA-03): admin GET `/gestao/acessos/999999` → 404.
    - `test_visitante_nao_acessa_historico` (CA-04): GET `/portal/acessos` sem auth → redirect `/login`.
    CONFIRMAR RED.

    **GREEN**:
    1. `config/sile.php` — acrescentar a seção `ui` (preservando a seção `security` do 01-01):
       ```php
       'ui' => [
           'access_history' => ['per_page' => 15],
       ],
       ```
    2. `php artisan make:controller Portal/AccessHistoryController --invokable --no-interaction` (import `App\Support\Settings`):
       ```php
       public function __invoke(Request $request): Response
       {
           $user = $request->user();

           return Inertia::render('portal/acessos', [
               'logs' => AccessLog::query()
                   ->where(fn ($query) => $query
                       ->where('user_id', $user->id)
                       ->orWhere('email', $user->email))
                   ->latest('created_at')
                   ->paginate((int) Settings::get('ui.access_history.per_page', 15))
                   ->through(fn (AccessLog $log) => [
                       'id' => $log->id,
                       'event' => $log->event,
                       'ip_address' => $log->ip_address,
                       'channel' => $log->channel,
                       'created_at' => $log->created_at->toIso8601String(),
                   ]),
           ]);
       }
       ```
    3. `php artisan make:controller Gestao/AccessHistoryController --invokable --no-interaction`: `__invoke(Request $request, User $user)` — auditar a consulta:
       ```php
       app(AuditService::class)->log(
           'acessos',
           'consulta-acessos',
           'Consulta administrativa do histórico de acessos de outro usuário',
           ['target_user_id' => $user->id],
       );
       ```
       e render `gestao/acessos` com `targetUser => $user->only('id', 'name', 'email')` e `logs` (mesma paginação parametrizada e transform do portal, com o MESMO filtro combinado: `where(user_id = $user->id OR email = $user->email)` — inclui bloqueios/falhas pré-login gravados sem user_id).
    4. Rotas:
       - `routes/portal.php` (dentro do subgrupo protegido): `Route::get('acessos', AccessHistoryController::class)->name('acessos.index');`
       - `routes/gestao.php` (dentro do grupo): `Route::get('acessos/{user}', AccessHistoryController::class)->middleware('permission:consultar-acessos-de-qualquer-conta')->name('acessos.show');`
       (Atenção aos imports com alias — dois controllers de mesmo nome em namespaces distintos.)
    5. Frontend:
       - `resources/js/pages/portal/acessos.tsx`: portal-layout; título "Meus acessos"; tabela com colunas "Data/hora" (formatada `pt-BR` via `new Date(...).toLocaleString('pt-BR')`), "Evento" (badge colorida: login → "Login" verde, logout → "Saída" cinza, falha → "Tentativa falha" vermelha, bloqueio → "Bloqueio temporário" âmbar), "IP", "Canal"; paginação com `<Link>` nos links da prop; estado vazio "Nenhum acesso registrado ainda.".
       - `resources/js/pages/gestao/acessos.tsx`: gestao-layout; título "Acessos de {targetUser.name}"; mesma tabela.
       - `portal-layout.tsx`: adicionar link de navegação "Meus acessos" → `/portal/acessos` e "Procurações" → `/portal/procuracoes` (navegação real entre as telas do portal).
    CONFIRMAR GREEN.

    **REFACTOR** — pint; `npm run typecheck && npm run build`; suíte completa.
  </action>
  <verify>php artisan test --compact tests/Feature/AccessHistory/AccessHistoryTest.php && php artisan test --compact</verify>
  <acceptance_criteria>
    - `php artisan test --compact tests/Feature/AccessHistory/AccessHistoryTest.php` termina com `"result":"passed"` (9 testes)
    - `routes/gestao.php` contém `permission:consultar-acessos-de-qualquer-conta`
    - `app/Http/Controllers/Portal/AccessHistoryController.php` contém `orWhere('email'` e `Settings::get('ui.access_history.per_page'`
    - `config/sile.php` contém `'access_history'` e continua contendo `'max_attempts'`
    - `app/Http/Controllers/Gestao/AccessHistoryController.php` contém `consulta-acessos`
    - `resources/js/pages/portal/acessos.tsx` contém `Tentativa falha`
    - `resources/js/layouts/portal-layout.tsx` contém `/portal/acessos`
    - `php artisan test --compact` (suíte completa) termina com `"result":"passed"`
  </acceptance_criteria>
  <done>HU-010 com os 4 CAs cobertos: usuário vê os próprios acessos com data/IP; admin consulta qualquer conta com auditoria; sem permissão = 403 auditado.</done>
</task>

</tasks>

<verification>
1. `php artisan test --compact tests/Feature/Settings/ProfileTest.php tests/Feature/AccessHistory/AccessHistoryTest.php` — HU-007/010 verdes.
2. `php artisan test --compact` — suíte completa verde.
3. `npm run typecheck && npm run build`.
4. `vendor/bin/pint --dirty --format agent`.
</verification>

<success_criteria>
- HU-007: perfil consultável/atualizável, CPF imutável, e-mail re-verificado, auditoria com attribute_changes — testes verdes.
- HU-010: próprios acessos paginados com evento/data/IP; consulta admin permissionada e auditada; CA-04 com analista — testes verdes.
- Navegação do portal liga dashboard, procurações e acessos (sem telas órfãs).
</success_criteria>

<output>
Após concluir, criar `.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-08-SUMMARY.md` registrando em concerns a limitação de navegação da tela `gestao/acessos` (alcançável apenas por URL direta até a HU-012/Fase 2 entregar a listagem de usuários da gestão) — para propagação ao STATE.md.
</output>

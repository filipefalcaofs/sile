---
phase: 01-identidade-acesso-e-auditoria-transversal
plan: 09
type: execute
wave: 8
depends_on: ["01-08"]
files_modified:
  - database/seeders/DevAdminSeeder.php
  - database/seeders/DatabaseSeeder.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
autonomous: false
requirements: []

must_haves:
  truths:
    - "Um migrate:fresh --seed deixa o ambiente de desenvolvimento pronto: papéis, termo LGPD v1 e admin de desenvolvimento"
    - "A suíte completa, o pint, o typecheck e o build passam — evidência fresca antes do smoke"
    - "A fase é navegável de ponta a ponta no browser executando lógica real (critério de pronto transversal)"
  artifacts:
    - path: "database/seeders/DevAdminSeeder.php"
      provides: "Usuário administrador de desenvolvimento (admin@sile.dev)"
    - path: "database/seeders/DatabaseSeeder.php"
      provides: "Orquestração: roles → termo LGPD → admin dev"
  key_links:
    - from: "database/seeders/DatabaseSeeder.php"
      to: "database/seeders/DevAdminSeeder.php"
      via: "call no run()"
      pattern: "DevAdminSeeder::class"
---

<objective>
Fechar a Fase 1: consolidar os seeds de desenvolvimento (papéis + termo LGPD + admin dev), rodar a verificação integral (suíte, pint, typecheck, build) e validar o fluxo completo no browser com o roteiro de smoke E2E do RESEARCH — checkpoint humano antes de declarar a fase pronta.

Propósito: "NENHUMA AFIRMAÇÃO DE SUCESSO SEM EVIDÊNCIA FRESCA" — a fase só é concluída com a suíte verde E a navegação real verificada por humano.
Output: ambiente seedado, verificação integral verde, aprovação humana do smoke E2E.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-VALIDATION.md
</context>

<important_notes>
- O `DatabaseSeeder` usa `WithoutModelEvents` (esqueleto) — seeds não geram activities, o que é correto para carga de desenvolvimento.
- Credencial do admin dev (`admin@sile.dev` / `password`) é EXCLUSIVA de desenvolvimento — documentar no seeder via PHPDoc. Nunca usar em produção.
- O ambiente local usa Postgres (porta 5433, `.env`). Se o banco não estiver acessível para o `migrate:fresh --seed`, registrar no SUMMARY e orientar no checkpoint a subir o serviço antes do smoke.
- TDD: o seeder é coberto por teste de feature antes da implementação.
</important_notes>

<tasks>

<task type="auto">
  <name>Task 1: Seeds de desenvolvimento consolidados + verificação integral (TDD)</name>
  <files>database/seeders/DevAdminSeeder.php, database/seeders/DatabaseSeeder.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <read_first>
    - database/seeders/DatabaseSeeder.php (calls atuais: RolesAndPermissionsSeeder + LegalTermSeeder)
    - database/seeders/RolesAndPermissionsSeeder.php e database/seeders/LegalTermSeeder.php (seeders existentes)
    - database/factories/UserFactory.php (gerador de CPF — o seeder usa CPF fixo válido)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Seeders/DatabaseSeederTest.php` com `RefreshDatabase`:
    - `test_seed_completo_prepara_ambiente_de_desenvolvimento`: `$this->seed();` (DatabaseSeeder) → existem os 4 papéis; `LegalTerm::current('lgpd')` não-nulo; existe user `admin@sile.dev` com `hasRole('administrador')` true e `email_verified_at` não-nulo.
    - `test_seed_e_idempotente`: `$this->seed(); $this->seed();` → continua 1 usuário admin@sile.dev e 4 papéis.
    - `test_admin_dev_acessa_gestao_apos_aceitar_termo`: após seed, logar de verdade — POST `/login` `['email' => 'admin@sile.dev', 'password' => 'password']` → redirect para `/gestao`; (sem aceite do termo ainda) GET `/gestao` → redirect ao termo LGPD — comprova o fluxo de primeiro acesso do admin.
    CONFIRMAR RED.

    **GREEN**:
    1. `php artisan make:seeder DevAdminSeeder --no-interaction`:
       ```php
       /**
        * Usuário administrador de DESENVOLVIMENTO (admin@sile.dev / password).
        * Nunca executar em produção com estas credenciais.
        */
       public function run(): void
       {
           $admin = User::firstOrCreate(
               ['email' => 'admin@sile.dev'],
               [
                   'name' => 'Administrador SILE',
                   'cpf' => '11144477735',
                   'phone' => null,
                   'password' => 'password',
                   'email_verified_at' => now(),
               ],
           );

           $admin->assignRole('administrador');
       }
       ```
       (O cast `hashed` do User cuida do hash; `assignRole` é idempotente. O CPF 111.444.777-35 é válido pelo algoritmo e DIFERENTE do CPF de exemplo do smoke — 529.982.247-25 — para não colidir com o cadastro do passo 1 do roteiro.)
    2. `DatabaseSeeder::run()` — ordem final:
       ```php
       $this->call([
           RolesAndPermissionsSeeder::class,
           LegalTermSeeder::class,
           DevAdminSeeder::class,
       ]);
       ```
    CONFIRMAR GREEN.

    **Verificação integral (evidência fresca, na ordem):**
    ```bash
    php artisan test --compact
    vendor/bin/pint --dirty --format agent
    npm run typecheck
    npm run build
    php artisan migrate:fresh --seed   # contra o Postgres local; se indisponível, registrar no SUMMARY
    ```
    Ler o output COMPLETO de cada comando antes de afirmar sucesso.

    **REFACTOR** — pint.
  </action>
  <verify>php artisan test --compact && vendor/bin/pint --dirty --format agent && npm run typecheck && npm run build</verify>
  <acceptance_criteria>
    - `php artisan test --compact` termina com `"result":"passed"` (suíte completa da fase)
    - `php artisan test --compact --filter=DatabaseSeederTest` termina com `"result":"passed"` (3 testes)
    - `database/seeders/DatabaseSeeder.php` contém `DevAdminSeeder::class`
    - `database/seeders/DevAdminSeeder.php` contém `admin@sile.dev` e `firstOrCreate`
    - `npm run typecheck` exit 0; `npm run build` exit 0
    - `vendor/bin/pint --dirty --format agent` sem arquivos corrigidos pendentes
  </acceptance_criteria>
  <done>Ambiente de desenvolvimento reproduzível com um comando; verificação integral verde com evidência fresca.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke E2E navegável no browser (roteiro completo da fase)</name>
  <what-built>
    Fase 1 completa: cadastro com CPF (HU-001), login com redirect por perfil (HU-002), recuperação de senha (HU-003), alteração de senha (HU-004), confirmação de e-mail (HU-005), termo LGPD versionado (HU-006), perfil (HU-007), procuração com representação "em nome de" (HU-008/009), histórico de acessos (HU-010) e auditoria transversal RN-002 — tudo com lógica real, sem fachada.
  </what-built>
  <how-to-verify>
    Preparação: Postgres local ativo (porta 5433, conforme .env) → `php artisan migrate:fresh --seed` → `composer run dev` (serve + queue + logs + vite). E-mails saem em `storage/logs/laravel.log` (MAIL_MAILER=log).

    1. **Cadastro (HU-001/005)**: abrir `/register` → criar conta com CPF válido (ex.: 529.982.247-25) e senha forte → deve cair na tela "Confirme seu e-mail"; copiar o link de verificação do `storage/logs/laravel.log` → abrir → e-mail confirmado.
    2. **Termo LGPD (HU-006)**: após a confirmação, navegar para `/portal` → deve exigir o termo → aceitar → dashboard do portal abre.
    3. **Lockout (HU-002)**: sair; em `/login`, errar a senha 5 vezes → 6ª tentativa deve mostrar bloqueio temporário com mensagem pt-BR.
    4. **Recuperação (HU-003)**: `/forgot-password` → informar o e-mail → copiar link do log → redefinir senha → logar com a nova senha.
    5. **Perfil (HU-007)**: `/settings/profile` → alterar nome (ok); alterar e-mail → volta a "não verificado" (re-exige confirmação); conferir CPF somente leitura. `/settings/password` → trocar senha informando a atual.
    6. **Procuração (HU-008/009)**: criar um segundo usuário (novo cadastro — CPF válido diferente, ex.: 123.456.789-09); como usuário A, `/portal/procuracoes` → vincular B pelo e-mail; logar como B → "Atuar em nome de" → banner "Atuando em nome de A" aparece; como A, revogar; como B, navegar → representação encerrada com aviso (efeito imediato).
    7. **Acessos (HU-010)**: `/portal/acessos` → lista com logins, falhas e bloqueio dos passos 3/4, com data/hora e IP.
    8. **Segregação (HU-002 CA-04)**: como cidadão, acessar `/gestao` → 403; logar com `admin@sile.dev` / `password` → aceitar termo → `/gestao` abre; `/gestao/acessos/{id do usuário A}` → histórico de A visível.
    9. **Trilha (RN-002)**: `php artisan tinker --execute 'App\Models\Activity::latest()->take(5)->get(["description","event","result","channel","ip_address","acting_for_user_id"])'` → registros enriquecidos com canal, IP e resultado; ações do passo 6 com acting_for_user_id preenchido.
  </how-to-verify>
  <resume-signal>Digite "aprovado" para concluir a fase, ou descreva os problemas encontrados (vira gap closure via /gsd-plan-phase 1 --gaps).</resume-signal>
</task>

</tasks>

<verification>
1. Task 1: suíte completa + pint + typecheck + build verdes com output lido.
2. Task 2: aprovação humana do roteiro de 9 passos.
3. Critério de pronto transversal do ROADMAP: CAs cobertos por testes passando; funcionalidade navegável real; nenhum valor de negócio hardcoded; pint sem pendências.
</verification>

<success_criteria>
- `migrate:fresh --seed` deixa ambiente pronto (papéis, termo, admin dev).
- Verificação integral verde (testes, pint, typecheck, build).
- Smoke E2E aprovado pelo usuário — fase navegável de ponta a ponta com lógica real.
</success_criteria>

<output>
Após concluir, criar `.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-09-SUMMARY.md` registrando o resultado da verificação integral e do smoke (aprovado/gaps).
</output>

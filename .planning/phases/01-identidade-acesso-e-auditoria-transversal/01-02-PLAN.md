---
phase: 01-identidade-acesso-e-auditoria-transversal
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - database/migrations/*_create_activity_log_table.php (publicada — editar antes de migrar)
  - database/migrations/*_create_access_logs_table.php
  - config/activitylog.php
  - app/Models/Activity.php
  - app/Models/AccessLog.php
  - app/Support/Audit/RecordActivityAction.php
  - app/Support/Audit/AuditService.php
  - app/Concerns/HasAuditoria.php
  - app/Models/User.php
  - app/Listeners/RecordSuccessfulLogin.php
  - app/Listeners/RecordLogout.php
  - app/Listeners/RecordFailedLogin.php
  - app/Listeners/RecordLockout.php
  - app/Listeners/RecordRegistrationActivity.php
  - app/Listeners/RecordEmailVerifiedActivity.php
  - app/Listeners/RecordPasswordResetActivity.php
  - database/factories/AccessLogFactory.php
  - bootstrap/app.php
  - tests/Feature/Audit/AuditInfrastructureTest.php
  - tests/Feature/Audit/AccessLogRecordingTest.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "Toda activity gravada (por model event ou chamada manual) é enriquecida automaticamente com ip_address, user_agent, channel, result e acting_for_user_id num ponto único"
    - "Serviços conseguem registrar explicitamente ação, resultado e versão de regras via AuditService"
    - "Exceções de autorização (403) geram registro de auditoria com result=bloqueado sem nenhum controller precisar lembrar disso"
    - "Models de domínio com HasAuditoria logam apenas atributos alterados e nunca password/remember_token"
    - "Login, logout, falha de login e bloqueio temporário geram registro em access_logs com e-mail, IP e canal (base da HU-010)"
    - "Cadastro, confirmação de e-mail e redefinição de senha geram activity com causer correto (base do CA-02 das HU-001/003/005)"
  artifacts:
    - path: "app/Models/Activity.php"
      provides: "Model Activity estendido com colunas SILE"
      contains: "class Activity extends"
    - path: "app/Support/Audit/RecordActivityAction.php"
      provides: "Action class v5 — enriquecimento central RN-002"
      contains: "extends LogActivityAction"
    - path: "app/Support/Audit/AuditService.php"
      provides: "Registro explícito (ação, resultado, versão de regras)"
      contains: "logBlocked"
    - path: "app/Concerns/HasAuditoria.php"
      provides: "Trait padrão SILE para models de domínio"
      contains: "getActivitylogOptions"
    - path: "app/Models/AccessLog.php"
      provides: "Histórico de acessos dedicado (HU-010)"
    - path: "app/Listeners/RecordSuccessfulLogin.php"
      provides: "Listener de Login → access_logs"
      contains: "AccessLog::create"
    - path: "tests/Feature/Audit/AuditInfrastructureTest.php"
      provides: "Testes da infraestrutura RN-002"
      min_lines: 60
    - path: "tests/Feature/Audit/AccessLogRecordingTest.php"
      provides: "Testes dos listeners de auth (access_logs + activities)"
      min_lines: 60
  key_links:
    - from: "config/activitylog.php"
      to: "app/Support/Audit/RecordActivityAction.php"
      via: "chave actions.log_activity"
      pattern: "RecordActivityAction"
    - from: "config/activitylog.php"
      to: "app/Models/Activity.php"
      via: "chave activity_model"
      pattern: "Activity::class"
    - from: "bootstrap/app.php"
      to: "app/Support/Audit/AuditService.php"
      via: "withExceptions intercepta AuthorizationException/UnauthorizedException e chama logBlocked"
      pattern: "logBlocked"
    - from: "app/Listeners/RecordSuccessfulLogin.php"
      to: "access_logs"
      via: "evento Illuminate\\Auth\\Events\\Login (auto-descoberto)"
      pattern: "Login \\$event"
---

<objective>
Construir a infraestrutura de auditoria transversal (RN-002 de TODAS as 131 HUs) sobre spatie/laravel-activitylog v5: migration estendida com colunas SILE (ip_address, user_agent, channel, acting_for_user_id, result, rules_version), model Activity próprio, action class de enriquecimento central, trait HasAuditoria, AuditService para registro explícito, interceptação de 403 no exception handler (CA-04 transversal) e a tabela dedicada `access_logs` alimentada por listeners dos eventos nativos de auth (base da HU-010 e do CA-02 das HUs de autenticação).

Purpose: é a fundação exigida por todas as fases seguintes — nenhuma HU é concluída sem CA-02 (auditoria), e este mecanismo único evita auditoria manual espalhada por controllers. Os planos 01-04, 01-06 e 01-08 consomem diretamente os listeners, o AccessLog e o AuditService criados aqui.
Output: trilha de auditoria funcional, testada, com enriquecimento automático, registro de bloqueios e histórico de acessos.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-CONTEXT.md
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-01-SUMMARY.md

ATENÇÃO — API v5 do activitylog (breaking changes; NÃO usar exemplos v4 da web):
`tapActivity()` → `beforeActivityLogged(Activity $activity, string $eventName)`; `$activity->changes()` → `$activity->attribute_changes`; `getExtraProperty()` → `getProperty()`; `dontSubmitEmptyLogs()` → `dontLogEmptyChanges()`; trait `HasActivity` = LogsActivity + CausesActivity.
Convenções: `php artisan make:` para arquivos; pint após PHP; testes PHPUnit com métodos test_snake_case em pt-BR.
PROIBIDO `Event::fake()` global nos testes deste plano (Pitfall 4) — os listeners SÃO o objeto do teste; assertar efeitos no banco.
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD — migration estendida, model Activity e action de enriquecimento central</name>
  <files>database/migrations/*_create_activity_log_table.php, config/activitylog.php, app/Models/Activity.php, app/Support/Audit/RecordActivityAction.php, tests/Feature/Audit/AuditInfrastructureTest.php</files>
  <read_first>
    - database/migrations/ (localizar a migration publicada do activitylog — nome contém create_activity_log_table)
    - config/activitylog.php (chaves actions e activity_model publicadas)
    - vendor/spatie/laravel-activitylog/src/Actions/LogActivityAction.php (assinatura real do save() na v5 instalada)
    - .planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md (Padrão 2 e "Migration estendida" em Code Examples — schema exato)
    - app/Models/User.php (será causer nos testes)
  </read_first>
  <action>
    TDD Red-Green-Refactor:

    RED — criar `tests/Feature/Audit/AuditInfrastructureTest.php` (`php artisan make:test --phpunit Audit/AuditInfrastructureTest`) com `RefreshDatabase` e testes:
    - `test_activity_persiste_colunas_sile_de_origem_e_resultado`: registrar rota inline no início do teste (request real é necessária para o enriquecimento — chamada direta em contexto de teste tem `request()` sem IP):
      ```php
      Route::middleware('web')->get('/_test/audit', function () {
          activity('teste')->log('Ação de teste');

          return response()->noContent();
      });

      $this->get('/_test/audit')->assertNoContent();

      $this->assertDatabaseHas('activity_log', [
          'log_name' => 'teste',
          'description' => 'Ação de teste',
          'ip_address' => '127.0.0.1',
          'channel' => 'portal',
          'result' => 'sucesso',
      ]);
      ```
    - `test_activity_aceita_acting_for_user_id_via_context`: igual ao anterior, mas a closure da rota faz `Context::add('acting_for_user_id', $outroUser->id)` antes de `activity('teste')->log(...)` → registro com `acting_for_user_id` = id do outro user.
    - `test_activity_persiste_result_e_rules_version_explicitos` (prova de schema + fillable): criar direto pelo model — `\App\Models\Activity::create(['description' => 'Execução de regra', 'result' => 'falha', 'rules_version' => 'louos-v1'])` → `assertDatabaseHas('activity_log', ['result' => 'falha', 'rules_version' => 'louos-v1'])`.
    Rodar `php artisan test --compact --filter=AuditInfrastructureTest` e CONFIRMAR RED (tabela sem colunas SILE, classes inexistentes).

    GREEN —
    1. EDITAR a migration publicada `*_create_activity_log_table.php` (projeto novo, sem produção — editar antes de migrar é seguro) para o schema exato do RESEARCH:
       ```php
       Schema::create('activity_log', function (Blueprint $table) {
           $table->bigIncrements('id');
           $table->string('log_name')->nullable()->index();
           $table->text('description');
           $table->nullableMorphs('subject', 'subject');
           $table->string('event')->nullable();
           $table->nullableMorphs('causer', 'causer');
           $table->json('properties')->nullable();
           $table->json('attribute_changes')->nullable();
           $table->string('ip_address', 45)->nullable();
           $table->string('user_agent', 500)->nullable();
           $table->string('channel', 20)->nullable();
           $table->foreignId('acting_for_user_id')->nullable()->constrained('users');
           $table->string('result', 20)->nullable()->index();
           $table->string('rules_version', 50)->nullable()->index();
           $table->timestamps();
       });
       ```
       Manter o down() com dropIfExists. Conferir se a publicada da v5 traz migrations extras (ex.: add_event/add_batch antigas não existem na v5 — se houver mais de uma migration publicada, consolidar no create).
    2. `app/Models/Activity.php`: estender `Spatie\Activitylog\Models\Activity`, adicionar ao `$fillable`/casts as colunas novas (`ip_address`, `user_agent`, `channel`, `acting_for_user_id`, `result`, `rules_version`) e relacionamento `actingFor(): BelongsTo` para User. ATENÇÃO: mesclar com o `$fillable` do model pai (não substituir — o pai preenche description/properties/etc.); conferir `vendor/spatie/laravel-activitylog/src/Models/Activity.php`.
    3. `app/Support/Audit/RecordActivityAction.php` estendendo `Spatie\Activitylog\Actions\LogActivityAction`, sobrescrevendo `save()` conforme RESEARCH (Padrão 2):
       - console (fora de testes): `channel ??= 'console'`
       - com request: `ip_address ??= request()->ip()`; `user_agent ??= substr((string) request()->userAgent(), 0, 500)`; `channel ??= request()->routeIs('gestao.*') ? 'gestao' : 'portal'`; `acting_for_user_id ??= Context::get('acting_for_user_id')`
       - sempre: `result ??= 'sucesso'`; chamar `parent::save($activity)`.
       Adaptar a assinatura ao que a v5 instalada declarar (lido no read_first).
    4. `config/activitylog.php`: `'activity_model' => App\Models\Activity::class` e `'actions' => ['log_activity' => App\Support\Audit\RecordActivityAction::class]` (conferir estrutura exata da chave actions no config publicado da v5 instalada).
    Rodar o filtro → verde.

    REFACTOR — pint, revisar imports.
  </action>
  <verify>
    `php artisan test --compact --filter=AuditInfrastructureTest` (verde); `php artisan test --compact` (suite toda); `vendor/bin/pint --dirty --format agent`
  </verify>
  <acceptance_criteria>
    - Migration `*_create_activity_log_table.php` contém `acting_for_user_id`, `rules_version`, `attribute_changes`
    - `app/Models/Activity.php` contém `extends \Spatie\Activitylog\Models\Activity` (ou import equivalente) e `public function actingFor(`
    - `config/activitylog.php` contém `App\Models\Activity::class` e `RecordActivityAction::class`
    - `php artisan test --compact --filter=AuditInfrastructureTest` sai com código 0
  </acceptance_criteria>
  <done>Qualquer activity gravada no sistema carrega origem (ip/user_agent/channel), resultado e "em nome de" automaticamente — RN-002 estrutural pronto.</done>
</task>

<task type="auto">
  <name>Task 2: TDD — HasAuditoria, CausesActivity no User, AuditService e auditoria de 403 no exception handler</name>
  <files>app/Concerns/HasAuditoria.php, app/Models/User.php, app/Support/Audit/AuditService.php, bootstrap/app.php, tests/Feature/Audit/AuditInfrastructureTest.php</files>
  <read_first>
    - app/Models/User.php (atributos #[Fillable]/#[Hidden] atuais)
    - bootstrap/app.php (withExceptions atual — só shouldRenderJsonWhen; withMiddleware com SecurityHeaders do 01-01)
    - app/Support/Audit/RecordActivityAction.php (criado na Task 1)
    - vendor/spatie/laravel-activitylog/src/Traits/CausesActivity.php (confirmar API da trait v5)
    - vendor/spatie/laravel-permission/src/Exceptions/UnauthorizedException.php (classe interceptada no handler)
  </read_first>
  <action>
    TDD Red-Green-Refactor (ampliando AuditInfrastructureTest):

    RED — adicionar testes:
    - `test_model_com_has_auditoria_loga_somente_atributos_alterados`: atualizar `name` de um User → existe activity com `event=updated`, `attribute_changes` contendo apenas `name` (não `email`), e `properties`/`attribute_changes` NUNCA contendo `password` nem `remember_token` (atualizar password junto e assertar ausência).
    - `test_audit_service_registra_evento_explicito`: autenticado (`actingAs`), `app(AuditService::class)->log('seguranca', 'senha-alterada', 'Senha alterada pelo próprio usuário')` → `assertDatabaseHas('activity_log', ['log_name' => 'seguranca', 'event' => 'senha-alterada', 'result' => 'sucesso', 'causer_id' => $user->id])`.
    - `test_audit_service_registra_bloqueio_com_resultado_bloqueado`: `app(AuditService::class)->logBlocked('seguranca', 'Tentativa de acesso sem permissão', ['rota' => '/gestao'])` autenticado → `assertDatabaseHas('activity_log', ['log_name' => 'seguranca', 'event' => 'acesso-negado', 'result' => 'bloqueado'])` com causer correto.
    - `test_excecao_de_autorizacao_gera_auditoria_de_bloqueio`: registrar no teste uma rota web autenticada que lança `Illuminate\Auth\Access\AuthorizationException` (ex.: `Route::middleware(['web', 'auth'])->get('/_test/403', fn () => throw new AuthorizationException)`), fazer GET autenticado → `assertForbidden()` E `assertDatabaseHas('activity_log', ['event' => 'acesso-negado', 'result' => 'bloqueado'])`.
    Rodar filtro → RED.

    GREEN —
    1. `app/Concerns/HasAuditoria.php` (trait):
       ```php
       trait HasAuditoria
       {
           use LogsActivity;

           public function getActivitylogOptions(): LogOptions
           {
               return LogOptions::defaults()
                   ->logFillable()
                   ->logOnlyDirty()
                   ->dontLogEmptyChanges()
                   ->logExcept(['password', 'remember_token']);
           }
       }
       ```
    2. `app/Models/User.php`: adicionar `use HasAuditoria;` e `use \Spatie\Activitylog\Traits\CausesActivity;` (NÃO usar `HasActivity` — colidiria com a LogsActivity já embutida na HasAuditoria).
    3. `app/Support/Audit/AuditService.php` (via make:class) com EXATAMENTE estas assinaturas (os planos 01-06/01-07/01-08 chamam nesta ordem de parâmetros — logName, event, description, properties):
       ```php
       public function log(
           string $logName,
           string $event,
           string $description,
           array $properties = [],
           ?Model $subject = null,
           string $result = 'sucesso',
           ?string $rulesVersion = null,
       ): Activity

       public function logBlocked(string $logName, string $description, array $properties = []): Activity
       // delega para log() com event='acesso-negado', result='bloqueado'
       ```
       Implementação de `log()`: montar o builder
       ```php
       $logger = activity($logName)->withProperties($properties)->event($event);

       if (auth()->check()) {
           $logger->causedBy(auth()->user());
       }

       if ($subject !== null) {
           $logger->performedOn($subject);
       }

       /** @var Activity $activity */
       $activity = $logger->log($description);

       $activity->forceFill([
           'result' => $result,
           'rules_version' => $rulesVersion,
       ])->save();

       return $activity;
       ```
       (forceFill pós-log é deliberado: independe da API interna do builder v5; o enriquecimento de ip/channel já aconteceu na action da Task 1 e o `??=` preserva o que vier preenchido.)
    4. `bootstrap/app.php` — dentro de `withExceptions`, usar callbacks de RENDER que retornam null (caem no render padrão e mantêm o 403). NÃO usar `report()`: `AuthorizationException` está na lista interna de dontReport do framework e o callback nunca dispararia:
       ```php
       $exceptions->render(function (\Illuminate\Auth\Access\AuthorizationException $e, \Illuminate\Http\Request $request) {
           app(\App\Support\Audit\AuditService::class)->logBlocked('seguranca', 'Tentativa de acesso sem permissão', [
               'rota' => $request->path(),
               'metodo' => $request->method(),
           ]);

           return null;
       });

       $exceptions->render(function (\Spatie\Permission\Exceptions\UnauthorizedException $e, \Illuminate\Http\Request $request) {
           app(\App\Support\Audit\AuditService::class)->logBlocked('seguranca', 'Tentativa de acesso sem permissão', [
               'rota' => $request->path(),
               'metodo' => $request->method(),
           ]);

           return null;
       });
       ```
       (Dois callbacks separados de propósito: UnauthorizedException do spatie estende HttpException, não AuthorizationException.)
    Rodar filtro → verde.

    REFACTOR — pint; conferir que `php artisan test --compact` segue verde.
  </action>
  <verify>
    `php artisan test --compact --filter=AuditInfrastructureTest` (verde, >= 7 testes); `php artisan test --compact`; `vendor/bin/pint --dirty --format agent`
  </verify>
  <acceptance_criteria>
    - `app/Concerns/HasAuditoria.php` contém `logExcept(['password', 'remember_token'])`
    - `app/Models/User.php` contém `HasAuditoria` e `CausesActivity`
    - `app/Support/Audit/AuditService.php` contém `public function log(` com parâmetros na ordem `string $logName, string $event, string $description` e contém `public function logBlocked(`
    - `bootstrap/app.php` contém `AuthorizationException` e `UnauthorizedException` em callbacks `$exceptions->render(`
    - `php artisan test --compact --filter=AuditInfrastructureTest` sai com código 0
  </acceptance_criteria>
  <done>CA-02 (auditoria) e CA-04 (bloqueio auditado) têm mecanismo transversal único: models auditam via trait, serviços via AuditService, e qualquer 403 de autorização é registrado sem código por controller.</done>
</task>

<task type="auto">
  <name>Task 3: TDD — tabela access_logs e listeners dos eventos de autenticação</name>
  <files>database/migrations/*_create_access_logs_table.php, app/Models/AccessLog.php, database/factories/AccessLogFactory.php, app/Listeners/RecordSuccessfulLogin.php, app/Listeners/RecordLogout.php, app/Listeners/RecordFailedLogin.php, app/Listeners/RecordLockout.php, app/Listeners/RecordRegistrationActivity.php, app/Listeners/RecordEmailVerifiedActivity.php, app/Listeners/RecordPasswordResetActivity.php, tests/Feature/Audit/AccessLogRecordingTest.php</files>
  <read_first>
    - app/Support/Audit/AuditService.php (criado na Task 2 — NÃO usar nos listeners de Registered/Verified/PasswordReset; ver justificativa no action)
    - app/Providers/FortifyServiceProvider.php (publicado no 01-01 — rate limiter 'login' default de 5/min, necessário para o teste de lockout)
    - app/Actions/Fortify/CreateNewUser.php (payload aceito pelo POST /register neste momento — name/email/password; cpf só chega no 01-04)
    - .planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md (Padrão 3 — schema access_logs e tabela de eventos→listeners)
  </read_first>
  <action>
    Decisão travada (RESEARCH, Padrão 3): `access_logs` é tabela DEDICADA, não evento filtrado na activity_log — login falho não tem causer e a consulta da HU-010 é hot-path. Listeners são auto-descobertos em `app/Listeners` (Laravel 13 — nenhum registro manual).

    TDD Red-Green-Refactor:

    RED — criar `tests/Feature/Audit/AccessLogRecordingTest.php` (`php artisan make:test --phpunit Audit/AccessLogRecordingTest`) com `RefreshDatabase`. Usar requests HTTP reais (rotas do Fortify já registradas desde o 01-01) — nada de `Event::fake()`:
    - `test_login_bem_sucedido_registra_acesso`: user via factory; POST `/login` `['email' => $user->email, 'password' => 'password']` → `assertDatabaseHas('access_logs', ['user_id' => $user->id, 'email' => $user->email, 'event' => 'login', 'ip_address' => '127.0.0.1', 'channel' => 'portal'])`.
    - `test_falha_de_login_registra_tentativa_com_usuario_conhecido`: POST `/login` com senha errada → `assertGuest()` e `assertDatabaseHas('access_logs', ['user_id' => $user->id, 'email' => $user->email, 'event' => 'falha'])`.
    - `test_falha_de_login_registra_tentativa_com_email_desconhecido`: POST `/login` `['email' => 'nao-existe@example.com', 'password' => 'x']` → `assertDatabaseHas('access_logs', ['user_id' => null, 'email' => 'nao-existe@example.com', 'event' => 'falha'])`.
    - `test_logout_registra_saida`: `actingAs($user)` POST `/logout` → `assertDatabaseHas('access_logs', ['user_id' => $user->id, 'event' => 'logout'])`.
    - `test_bloqueio_temporario_registra_evento`: errar a senha 5 vezes (limite default do provider publicado) e tentar a 6ª → `assertDatabaseHas('access_logs', ['email' => $user->email, 'event' => 'bloqueio'])`.
    - `test_cadastro_gera_activity_de_cadastro`: POST `/register` `['name' => 'Maria da Silva', 'email' => 'maria@example.com', 'password' => 'SenhaForte1', 'password_confirmation' => 'SenhaForte1']` (payload da action publicada — cpf só entra no 01-04) → `assertDatabaseHas('activity_log', ['log_name' => 'cadastro', 'event' => 'cadastro', 'result' => 'sucesso', 'causer_id' => User::firstWhere('email', 'maria@example.com')->id])`.
    - `test_confirmacao_de_email_gera_activity`: `event(new \Illuminate\Auth\Events\Verified($user));` → `assertDatabaseHas('activity_log', ['log_name' => 'seguranca', 'event' => 'email-confirmado', 'causer_id' => $user->id])` (dispatch direto: o fluxo HTTP de verificação só existe no 01-04, mas o listener nasce aqui).
    - `test_redefinicao_de_senha_gera_activity`: `event(new \Illuminate\Auth\Events\PasswordReset($user));` → `assertDatabaseHas('activity_log', ['log_name' => 'seguranca', 'event' => 'senha-redefinida', 'causer_id' => $user->id])`.
    Rodar `php artisan test --compact --filter=AccessLogRecordingTest` e CONFIRMAR RED (tabela e listeners inexistentes).

    GREEN —
    1. Migration `php artisan make:migration create_access_logs_table --no-interaction` (schema exato do RESEARCH):
       ```php
       Schema::create('access_logs', function (Blueprint $table) {
           $table->id();
           $table->foreignId('user_id')->nullable()->constrained('users')->nullOnDelete();
           $table->string('email');
           $table->string('event', 20);              // login|logout|falha|bloqueio
           $table->string('ip_address', 45)->nullable();
           $table->string('user_agent', 500)->nullable();
           $table->string('channel', 20)->nullable(); // portal|gestao
           $table->timestamp('created_at');
           $table->index(['user_id', 'created_at']);
           $table->index('email');
       });
       ```
       (Sem updated_at — registro imutável; `nullOnDelete` preserva o log se a conta for excluída.)
    2. `app/Models/AccessLog.php` (`php artisan make:model AccessLog --no-interaction` — a factory é criada manualmente na etapa 3):
       ```php
       #[Fillable(['user_id', 'email', 'event', 'ip_address', 'user_agent', 'channel'])]
       class AccessLog extends Model
       {
           use HasFactory;

           const UPDATED_AT = null;

           protected function casts(): array
           {
               return ['created_at' => 'datetime'];
           }

           public function user(): BelongsTo
           {
               return $this->belongsTo(User::class);
           }
       }
       ```
       (Seguir o estilo de atributo `#[Fillable]` do esqueleto; NÃO aplicar HasAuditoria — access_logs JÁ É dado de auditoria, evitar recursão/ruído. Conferir a sintaxe da constante UPDATED_AT no model User/vendor — usar a forma aceita pelo Laravel 13.)
    3. `database/factories/AccessLogFactory.php` (consumida pelos testes da HU-010 no plano 01-08):
       ```php
       public function definition(): array
       {
           return [
               'user_id' => User::factory(),
               'email' => fake()->safeEmail(),
               'event' => 'login',
               'ip_address' => fake()->ipv4(),
               'user_agent' => fake()->userAgent(),
               'channel' => 'portal',
               'created_at' => now(),
           ];
       }
       ```
    4. Listeners de access_logs (`php artisan make:listener RecordSuccessfulLogin --event=Login --no-interaction` etc. — ajustar imports para `Illuminate\Auth\Events\*`). Conteúdo exato dos handles:
       - `RecordSuccessfulLogin` (evento `Login`):
         ```php
         AccessLog::create([
             'user_id' => $event->user->getAuthIdentifier(),
             'email' => $event->user->email,
             'event' => 'login',
             'ip_address' => request()->ip(),
             'user_agent' => substr((string) request()->userAgent(), 0, 500),
             'channel' => request()->routeIs('gestao.*') ? 'gestao' : 'portal',
         ]);
         ```
       - `RecordLogout` (evento `Logout`): igual com `'event' => 'logout'`; guardar `$event->user?->getAuthIdentifier()` e `$event->user?->email ?? ''` (logout de sessão expirada pode vir sem user).
       - `RecordFailedLogin` (evento `Failed`): `'user_id' => $event->user?->getAuthIdentifier()`, `'email' => (string) ($event->credentials['email'] ?? '')`, `'event' => 'falha'`, mesmos campos de origem.
       - `RecordLockout` (evento `Lockout`): `'user_id' => null`, `'email' => (string) $event->request->input('email', '')`, `'event' => 'bloqueio'`, ip/user_agent do `$event->request`.
    5. Listeners de activity (NÃO usar AuditService aqui: no evento `Registered` o usuário ainda não está autenticado — `auth()->user()` é null; o causer deve ser explícito):
       - `RecordRegistrationActivity` (evento `Registered`):
         ```php
         activity('cadastro')
             ->causedBy($event->user)
             ->performedOn($event->user)
             ->event('cadastro')
             ->log('Usuário cadastrado');
         ```
       - `RecordEmailVerifiedActivity` (evento `Verified`): `activity('seguranca')->causedBy($event->user)->performedOn($event->user)->event('email-confirmado')->log('E-mail confirmado pelo usuário');`
       - `RecordPasswordResetActivity` (evento `PasswordReset`): `activity('seguranca')->causedBy($event->user)->performedOn($event->user)->event('senha-redefinida')->log('Senha redefinida via link de recuperação');`
       (O enriquecimento de ip/channel/result vem da RecordActivityAction da Task 1 — nada a repetir aqui.)
    Rodar o filtro → verde.

    REFACTOR — pint; suíte completa (AuditInfrastructureTest + AccessLogRecordingTest + planos anteriores).
  </action>
  <verify>
    `php artisan test --compact --filter=AccessLogRecordingTest` (verde, 8 testes); `php artisan test --compact` (suite toda); `vendor/bin/pint --dirty --format agent`
  </verify>
  <acceptance_criteria>
    - Migration `*_create_access_logs_table.php` contém `index(['user_id', 'created_at'])` e NÃO contém `updated_at`
    - `app/Models/AccessLog.php` contém `UPDATED_AT`
    - Existem os 7 listeners em `app/Listeners/`: RecordSuccessfulLogin, RecordLogout, RecordFailedLogin, RecordLockout, RecordRegistrationActivity, RecordEmailVerifiedActivity, RecordPasswordResetActivity
    - `app/Listeners/RecordRegistrationActivity.php` contém `causedBy($event->user)`
    - `database/factories/AccessLogFactory.php` existe e contém `'event' => 'login'`
    - `php artisan test --compact --filter=AccessLogRecordingTest` sai com código 0 (8 testes)
    - `php artisan test --compact` (suite completa) sai com código 0
  </acceptance_criteria>
  <done>Eventos nativos de auth alimentam access_logs (login/logout/falha/bloqueio) e a trilha de activities (cadastro/e-mail confirmado/senha redefinida) com causer correto — base pronta para HU-001/002/003/005 (CA-02) e HU-010.</done>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=Audit` verde (AuditInfrastructureTest + AccessLogRecordingTest).
- `php artisan test --compact` suite completa verde.
- Grep nas colunas SILE: `rg "rules_version" database/migrations app/Models/Activity.php` retorna ocorrências.
- `rg "AccessLog::create" app/Listeners/` retorna os 4 listeners de acesso.
- `vendor/bin/pint --dirty --format agent` sem pendências.
</verification>

<success_criteria>
- Infraestrutura RN-002 completa: enriquecimento central, registro explícito, trait de model, 403 auditado.
- Campos exigidos pela RN-002 todos mapeados: usuário (causer), data/hora (created_at), origem (ip/user_agent/channel), ação (description/event/log_name), dados de entrada (properties), resultado (result), erros (properties.error), versão de regras (rules_version), "em nome de" (acting_for_user_id).
- access_logs + 7 listeners operacionais, provados com requests HTTP reais.
- Nenhum exemplo v4 (tapActivity/changes()) no código.
</success_criteria>

<output>
Após completar, criar `.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-02-SUMMARY.md` documentando a API final do AuditService (assinaturas) e confirmando que o render() de 403 mantém o status padrão.
</output>

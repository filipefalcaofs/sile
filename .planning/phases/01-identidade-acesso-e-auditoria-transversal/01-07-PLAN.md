---
phase: 01-identidade-acesso-e-auditoria-transversal
plan: 07
type: execute
wave: 6
depends_on: ["01-05"]
files_modified:
  - database/migrations/*_create_procurations_table.php
  - app/Models/Procuration.php
  - database/factories/ProcurationFactory.php
  - app/Policies/ProcurationPolicy.php
  - app/Http/Requests/StoreProcurationRequest.php
  - app/Http/Controllers/Portal/ProcurationController.php
  - app/Http/Controllers/Portal/RepresentationController.php
  - app/Http/Middleware/ResolveRepresentation.php
  - app/Support/Representation/CurrentRepresentation.php
  - app/Providers/AppServiceProvider.php
  - app/Http/Middleware/HandleInertiaRequests.php
  - routes/portal.php
  - resources/js/layouts/portal-layout.tsx
  - resources/js/pages/portal/procuracoes/index.tsx
  - resources/js/types/index.d.ts
  - tests/Feature/Procuration/LinkAttorneyTest.php
  - tests/Feature/Procuration/RevokeAttorneyTest.php
autonomous: true
requirements: ["HU-008", "HU-009"]

must_haves:
  truths:
    - "Representante legal vincula procurador por e-mail de conta existente, com vigência (HU-008 CA-01)"
    - "Vínculo e revogação ficam auditados; ações em representação carregam 'em nome de' na auditoria (HU-008/009 CA-02)"
    - "E-mail inexistente, auto-procuração e duplicidade ativa são bloqueados com mensagens pt-BR (HU-008 CA-03)"
    - "Revogação tem efeito imediato: na request seguinte o procurador perde a representação (HU-009 CA-01)"
    - "Terceiro não revoga procuração alheia e a tentativa é auditada (HU-009 CA-04)"
    - "Procurador em representação vê banner 'Atuando em nome de' no portal"
  artifacts:
    - path: "app/Models/Procuration.php"
      provides: "Procuração user→user com vigência e revogação"
      contains: "public function isActive(): bool"
    - path: "app/Http/Middleware/ResolveRepresentation.php"
      provides: "Revalidação da procuração a cada request + Context 'em nome de'"
    - path: "app/Support/Representation/CurrentRepresentation.php"
      provides: "Estado da representação na request para controllers e Inertia"
    - path: "resources/js/pages/portal/procuracoes/index.tsx"
      provides: "Tela de gestão de procurações (outorgadas e recebidas)"
  key_links:
    - from: "app/Http/Middleware/ResolveRepresentation.php"
      to: "app/Support/Audit/RecordActivityAction.php"
      via: "Context::add('acting_for_user_id') consumido pelo enriquecimento"
      pattern: "acting_for_user_id"
    - from: "routes/portal.php"
      to: "app/Http/Middleware/ResolveRepresentation.php"
      via: "middleware no subgrupo protegido do portal"
      pattern: "ResolveRepresentation"
    - from: "app/Http/Middleware/HandleInertiaRequests.php"
      to: "app/Support/Representation/CurrentRepresentation.php"
      via: "share actingFor"
      pattern: "actingFor"
---

<objective>
Entregar HU-008 (Vincular procurador) e HU-009 (Revogar procuração): procuração user→user com vigência, atuação "em nome de" resolvida a cada request (efeito imediato da revogação), rastreabilidade total na auditoria via `acting_for_user_id` e UI de gestão das procurações no portal.

Propósito: base de representação legal exigida pelos fluxos de solicitação (Fases 8+); o campo "em nome de" da auditoria (RN-002) passa a ser alimentado de verdade.
Output: ciclo completo vincular → atuar em nome de → revogar com efeito imediato, tudo auditado.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-CONTEXT.md
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md
@docs/SILE_HUs_Completas_MD/EP01-Identidade-Acesso-e-Seguranca/HU-008-Vincular-procurador.md
@docs/SILE_HUs_Completas_MD/EP01-Identidade-Acesso-e-Seguranca/HU-009-Revogar-procuracao.md
</context>

<important_notes>
- **Decisões travadas (RESEARCH, Padrão 5 + Open Question 2):** procurador localizado por E-MAIL de conta já existente (sem convite por e-mail nesta fase); procuração permanece user→user (escopo por empresa só na Fase 3, por migration aditiva); unicidade "uma ativa por par" validada na APLICAÇÃO (índice único parcial rejeitado por portabilidade SQLite/Postgres — Pitfall 8).
- **Pitfall 7:** validar a procuração SOMENTE no switch deixa o procurador navegando após a revogação. `ResolveRepresentation` revalida em TODA request do portal.
- "Procurador" NÃO é papel do spatie — é cidadão com procuração ativa (anti-padrão documentado).
- Testes usam `User::factory()->cidadao()->withAcceptedLgpdTerm()->create()` (state do 01-05) para navegar no portal.
- **Wave 6 paralela (01-06 ∥ 01-07, mesmo worktree):** NÃO rodar `php artisan test --compact` (suíte completa) durante este plano — um RED transitório do 01-06 quebraria a verificação. Todas as verificações deste plano são ESCOPADAS a `--filter=Procuration`; a suíte completa roda no fechamento da wave 6 (sampling do executor, conforme 01-VALIDATION.md) e novamente no 01-08/01-09.
- TDD estrito. Pint após PHP. Typecheck/build após TSX.
</important_notes>

<tasks>

<task type="auto">
  <name>Task 1: HU-008 — Modelo de procuração e vínculo de procurador (TDD)</name>
  <files>database/migrations/*_create_procurations_table.php, app/Models/Procuration.php, database/factories/ProcurationFactory.php, app/Http/Requests/StoreProcurationRequest.php, app/Http/Controllers/Portal/ProcurationController.php, routes/portal.php, resources/js/pages/portal/procuracoes/index.tsx, tests/Feature/Procuration/LinkAttorneyTest.php</files>
  <read_first>
    - routes/portal.php (estrutura pós 01-05 — subgrupo lgpd.accepted onde as rotas entram)
    - app/Concerns/HasAuditoria.php (trait usada pelo model)
    - database/factories/UserFactory.php (states cidadao/withAcceptedLgpdTerm)
    - .planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md (Padrão 5 — schema procurations)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Procuration/LinkAttorneyTest.php` com `RefreshDatabase` + seed de roles; helper privado `portalUser()` retornando `User::factory()->cidadao()->withAcceptedLgpdTerm()->create()`:
    - `test_pagina_de_procuracoes_renderiza`: GET `/portal/procuracoes` → 200, component `portal/procuracoes/index`, props `granted` (outorgadas) e `received` (recebidas) presentes.
    - `test_vinculo_de_procurador_criado_com_sucesso` (CA-01): outorgante POST `/portal/procuracoes` `['attorney_email' => $attorney->email, 'expires_at' => null]` → redirect com flash; `assertDatabaseHas('procurations', ['grantor_user_id' => $grantor->id, 'attorney_user_id' => $attorney->id, 'revoked_at' => null])`.
    - `test_vinculo_com_vigencia_definida` (CA-01): POST com `expires_at` futuro → gravado.
    - `test_vinculo_gera_auditoria` (CA-02): após POST → activity com `subject_type` Procuration, `event = 'created'`, `result = 'sucesso'` (HasAuditoria + enriquecimento).
    - `test_email_inexistente_bloqueia_com_orientacao` (CA-03): attorney_email sem conta → `assertSessionHasErrors('attorney_email')` e a mensagem contém `'cadastr'` (orientação de cadastro prévio); nenhum registro criado.
    - `test_auto_procuracao_bloqueada` (CA-03): attorney_email = e-mail do próprio outorgante → erro.
    - `test_duplicidade_ativa_bloqueada` (CA-03): par já com procuração ativa → erro em attorney_email; count continua 1.
    - `test_procuracao_revogada_permite_novo_vinculo` (CA-03 contraprova): par com procuração REVOGADA → novo POST cria (count 2).
    - `test_visitante_nao_acessa_procuracoes` (CA-04): GET sem auth → redirect `/login`.
    CONFIRMAR RED.

    **GREEN**:
    1. `php artisan make:model Procuration -mf --no-interaction`; migration:
       ```php
       Schema::create('procurations', function (Blueprint $table) {
           $table->id();
           $table->foreignId('grantor_user_id')->constrained('users')->cascadeOnDelete();
           $table->foreignId('attorney_user_id')->constrained('users')->cascadeOnDelete();
           $table->timestamp('starts_at');
           $table->timestamp('expires_at')->nullable();
           $table->timestamp('revoked_at')->nullable();
           $table->foreignId('revoked_by_user_id')->nullable()->constrained('users');
           $table->timestamps();
           $table->index(['grantor_user_id', 'attorney_user_id']);
       });
       ```
    2. `app/Models/Procuration.php`: `use HasFactory, HasAuditoria;`; `#[Fillable(['grantor_user_id', 'attorney_user_id', 'starts_at', 'expires_at', 'revoked_at', 'revoked_by_user_id'])]`; casts datetime para starts_at/expires_at/revoked_at; relações `grantor()`, `attorney()`, `revokedBy()` (BelongsTo User com FKs explícitas); scope e método:
       ```php
       public function scopeActive(Builder $query): Builder
       {
           return $query->whereNull('revoked_at')
               ->where('starts_at', '<=', now())
               ->where(fn (Builder $q) => $q->whereNull('expires_at')->orWhere('expires_at', '>', now()));
       }

       public function isActive(): bool
       {
           return $this->revoked_at === null
               && $this->starts_at <= now()
               && ($this->expires_at === null || $this->expires_at > now());
       }
       ```
    3. `ProcurationFactory`: definition `['grantor_user_id' => User::factory(), 'attorney_user_id' => User::factory(), 'starts_at' => now(), 'expires_at' => null]`; states `revoked()` (`revoked_at => now()`) e `expired()` (`starts_at => now()->subDays(10), 'expires_at' => now()->subDay()`).
    4. `php artisan make:request StoreProcurationRequest --no-interaction`:
       - rules: `'attorney_email' => ['required', 'email', 'exists:users,email']`, `'expires_at' => ['nullable', 'date', 'after:now']`.
       - messages: `'attorney_email.exists' => 'Não encontramos uma conta com este e-mail. Oriente o procurador a se cadastrar no SILE primeiro.'`; attributes: `'attorney_email' => 'e-mail do procurador', 'expires_at' => 'validade'`.
       - `after()` (validador adicional): se sem erros — bloquear auto-procuração (`'Você não pode outorgar procuração para si mesmo.'`) e duplicidade ativa via `Procuration::query()->active()->where(grantor = $this->user()->id, attorney = $attorney->id)->exists()` (`'Já existe uma procuração ativa para este procurador.'`).
    5. `php artisan make:controller Portal/ProcurationController --no-interaction`:
       - `index()`: render `portal/procuracoes/index` com `granted` (`Procuration::with('attorney')->where('grantor_user_id', $userId)->latest()->get()` mapeado para id, attorney.name, attorney.email, starts_at, expires_at, revoked_at, is_active) e `received` (espelho com grantor) — datas formatadas ISO, formatação de exibição no React.
       - `store(StoreProcurationRequest $request)`: localizar attorney por e-mail; `Procuration::create([... 'starts_at' => now(), 'expires_at' => $request->validated('expires_at')])`; redirect back com `status` "Procurador vinculado com sucesso.".
    6. `routes/portal.php` — dentro do subgrupo `lgpd.accepted`:
       ```php
       Route::get('procuracoes', [ProcurationController::class, 'index'])->name('procuracoes.index');
       Route::post('procuracoes', [ProcurationController::class, 'store'])->name('procuracoes.store');
       ```
    7. `resources/js/pages/portal/procuracoes/index.tsx`: portal-layout; título "Minhas procurações"; card "Vincular procurador" com `<Form action="/portal/procuracoes" method="post">` (campos "E-mail do procurador" e "Validade (opcional)" type=date) e botão "Vincular"; tabela "Procurações outorgadas" (Procurador, E-mail, Início, Validade, Situação [Ativa/Revogada/Expirada], Ações); tabela "Procurações recebidas" (Outorgante, Situação, Ações — botões da Task 2); estados vazios pt-BR ("Você ainda não outorgou procurações.").
    CONFIRMAR GREEN.

    **REFACTOR** — pint; typecheck.
  </action>
  <verify>php artisan test --compact tests/Feature/Procuration/LinkAttorneyTest.php</verify>
  <acceptance_criteria>
    - `php artisan test --compact tests/Feature/Procuration/LinkAttorneyTest.php` termina com `"result":"passed"` (9 testes)
    - `app/Models/Procuration.php` contém `public function isActive(): bool` e `scopeActive`
    - `app/Http/Requests/StoreProcurationRequest.php` contém `exists:users,email` e a mensagem com `cadastr`
    - `routes/portal.php` contém `procuracoes.store`
    - Migration NÃO contém `DB::statement` (portabilidade SQLite/Postgres)
  </acceptance_criteria>
  <done>HU-008 CA-01/02/03 cobertos (CA-02 "em nome de" completa na Task 2): vínculo real por e-mail com vigência, validações pt-BR e auditoria.</done>
</task>

<task type="auto">
  <name>Task 2: HU-009 — Revogação com efeito imediato + representação "em nome de" (TDD)</name>
  <files>app/Policies/ProcurationPolicy.php, app/Http/Controllers/Portal/ProcurationController.php, app/Http/Controllers/Portal/RepresentationController.php, app/Http/Middleware/ResolveRepresentation.php, app/Support/Representation/CurrentRepresentation.php, app/Providers/AppServiceProvider.php, app/Http/Middleware/HandleInertiaRequests.php, routes/portal.php, resources/js/layouts/portal-layout.tsx, resources/js/pages/portal/procuracoes/index.tsx, resources/js/types/index.d.ts, tests/Feature/Procuration/RevokeAttorneyTest.php</files>
  <read_first>
    - app/Support/Audit/RecordActivityAction.php (consome Context 'acting_for_user_id' — o elo que este middleware alimenta)
    - app/Support/Audit/AuditService.php (assinatura log(logName, event, description, properties) do 01-02)
    - app/Http/Middleware/HandleInertiaRequests.php (share atual — acrescentar actingFor)
    - app/Http/Middleware/EnsureLgpdTermAccepted.php (referência de middleware do projeto)
    - resources/js/layouts/portal-layout.tsx (onde entra o banner)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Procuration/RevokeAttorneyTest.php` com `RefreshDatabase` + seed de roles (mesmo helper `portalUser()`):
    - `test_outorgante_revoga_procuracao` (CA-01): DELETE `/portal/procuracoes/{procuration}` pelo grantor → redirect; `revoked_at` não-nulo e `revoked_by_user_id = grantor->id` no banco.
    - `test_revogacao_gera_auditoria` (CA-02): activity `event = 'updated'` com subject Procuration (HasAuditoria registra o dirty de revoked_at).
    - `test_procurador_ativa_representacao` (HU-008 CA-01/CA-02): attorney com procuração ativa POST `/portal/representacao` `['procuration_id' => $p->id]` → redirect portal.dashboard; sessão tem `acting_procuration_id`; `assertDatabaseHas('activity_log', ['event' => 'representacao-iniciada', 'acting_for_user_id' => $grantor->id])`.
    - `test_acoes_durante_representacao_carregam_em_nome_de` (CA-02 transversal): com representação ativa na sessão, registrar rota inline com TODOS os middlewares do subgrupo portal (`['web', 'auth', 'verified', 'lgpd.accepted', \App\Http\Middleware\ResolveRepresentation::class]`) que executa `activity('teste')->log('Ação em representação')`; GET → activity com `acting_for_user_id = $grantor->id`.
    - `test_representacao_compartilha_acting_for_com_inertia`: com representação ativa, GET `/portal` → `assertInertia(->where('actingFor.name', $grantor->name))`.
    - `test_revogacao_tem_efeito_imediato` (CA-01 HU-009/Pitfall 7): attorney com representação ativa na sessão; grantor revoga; attorney GET `/portal` → 200 SEM `actingFor` (null), sessão sem `acting_procuration_id` e flash de aviso presente.
    - `test_procuracao_expirada_nao_ativa_representacao` (CA-03): factory `expired()` → POST representacao → `assertSessionHasErrors('procuration_id')`; sessão vazia.
    - `test_terceiro_nao_revoga_procuracao_alheia` (CA-04): user C DELETE procuração de A→B → 403; `assertDatabaseHas('activity_log', ['event' => 'acesso-negado', 'result' => 'bloqueado'])`; procuração continua ativa.
    - `test_attorney_encerra_a_propria_representacao`: DELETE `/portal/representacao` → sessão limpa; redirect.
    CONFIRMAR RED.

    **GREEN**:
    1. `php artisan make:policy ProcurationPolicy --model=Procuration --no-interaction`: método `delete(User $user, Procuration $procuration): bool` → `$user->id === $procuration->grantor_user_id;` (policies são auto-descobertas).
    2. `ProcurationController::destroy(Procuration $procuration)`: `Gate::authorize('delete', $procuration);` → `$procuration->update(['revoked_at' => now(), 'revoked_by_user_id' => $request->user()->id]);` → redirect back com `status` "Procuração revogada.".
    3. `app/Support/Representation/CurrentRepresentation.php`:
       ```php
       <?php

       namespace App\Support\Representation;

       use App\Models\Procuration;
       use App\Models\User;

       class CurrentRepresentation
       {
           private ?Procuration $procuration = null;

           public function set(Procuration $procuration): void
           {
               $this->procuration = $procuration;
           }

           public function procuration(): ?Procuration
           {
               return $this->procuration;
           }

           public function grantor(): ?User
           {
               return $this->procuration?->grantor;
           }
       }
       ```
       Registrar em `AppServiceProvider::register()`: `$this->app->scoped(CurrentRepresentation::class);`.
    4. `php artisan make:middleware ResolveRepresentation --no-interaction`:
       ```php
       public function handle(Request $request, Closure $next): Response
       {
           $procurationId = $request->session()->get('acting_procuration_id');

           if ($procurationId !== null) {
               $procuration = Procuration::query()->with('grantor')->find($procurationId);

               if ($procuration === null
                   || $procuration->attorney_user_id !== $request->user()->id
                   || ! $procuration->isActive()) {
                   $request->session()->forget('acting_procuration_id');
                   $request->session()->flash('status', 'A representação foi encerrada porque a procuração não está mais ativa.');
               } else {
                   Context::add('acting_for_user_id', $procuration->grantor_user_id);
                   app(CurrentRepresentation::class)->set($procuration);
               }
           }

           return $next($request);
       }
       ```
    5. `routes/portal.php`: subgrupo protegido passa a `Route::middleware(['lgpd.accepted', \App\Http\Middleware\ResolveRepresentation::class])->group(...)`; acrescentar dentro dele:
       ```php
       Route::delete('procuracoes/{procuration}', [ProcurationController::class, 'destroy'])->name('procuracoes.destroy');
       Route::post('representacao', [RepresentationController::class, 'store'])->name('representacao.store');
       Route::delete('representacao', [RepresentationController::class, 'destroy'])->name('representacao.destroy');
       ```
    6. `php artisan make:controller Portal/RepresentationController --no-interaction`:
       - `store(Request $request)`: `$request->validate(['procuration_id' => ['required', 'integer']]);` localizar `Procuration::query()->active()->where('id', $id)->where('attorney_user_id', $request->user()->id)->first()`; se null → `throw ValidationException::withMessages(['procuration_id' => 'Esta procuração não está ativa.']);` senão: `session(['acting_procuration_id' => $procuration->id]); Context::add('acting_for_user_id', $procuration->grantor_user_id);` e `app(AuditService::class)->log('procuracao', 'representacao-iniciada', 'Início de atuação em nome do outorgante', ['procuration_id' => $procuration->id]);` → redirect `route('portal.dashboard')` com status "Você está atuando em nome de {nome}.".
       - `destroy(Request $request)`: forget da sessão + `AuditService::log('procuracao', 'representacao-encerrada', 'Fim da atuação em nome do outorgante')` → redirect back com status "Representação encerrada.".
    7. `HandleInertiaRequests::share` — acrescentar:
       ```php
       'actingFor' => fn () => app(\App\Support\Representation\CurrentRepresentation::class)->grantor()?->only('id', 'name'),
       ```
    8. Frontend:
       - `portal-layout.tsx`: se `actingFor` presente, banner destacado (bg-amber-100 dark:bg-amber-900, full-width) "Atuando em nome de {actingFor.name}" com botão "Encerrar representação" (`<Link href="/portal/representacao" method="delete" as="button">`).
       - `portal/procuracoes/index.tsx`: na tabela de outorgadas, botão "Revogar" (`<Link href={`/portal/procuracoes/${id}`} method="delete" as="button">`) apenas para ativas; nas recebidas ativas, botão "Atuar em nome de" (`<Link href="/portal/representacao" method="post" data={{ procuration_id: id }} as="button">`).
       - Tipar `actingFor` em `resources/js/types/index.d.ts`.
    CONFIRMAR GREEN.

    **REFACTOR** — pint; `npm run typecheck && npm run build`; rodar o grupo do plano: `php artisan test --compact --filter=Procuration` (NÃO a suíte completa — wave 6 paralela; ver important_notes).
  </action>
  <verify>php artisan test --compact --filter=Procuration</verify>
  <acceptance_criteria>
    - `php artisan test --compact tests/Feature/Procuration/RevokeAttorneyTest.php` termina com `"result":"passed"` (9 testes)
    - `app/Http/Middleware/ResolveRepresentation.php` contém `Context::add('acting_for_user_id'` e `isActive()`
    - `app/Policies/ProcurationPolicy.php` contém `grantor_user_id`
    - `app/Http/Middleware/HandleInertiaRequests.php` contém `'actingFor'`
    - `routes/portal.php` contém `ResolveRepresentation::class` e `representacao.store`
    - `resources/js/layouts/portal-layout.tsx` contém `Atuando em nome de`
    - `php artisan test --compact --filter=Procuration` termina com `"result":"passed"` (escopo do plano — suíte completa fica para o fechamento da wave 6)
  </acceptance_criteria>
  <done>HU-008 e HU-009 com os 4 CAs cobertos: representação revalidada a cada request, revogação com efeito imediato, trilha "em nome de" alimentada de ponta a ponta.</done>
</task>

</tasks>

<verification>
1. `php artisan test --compact --filter=Procuration` — HU-008/009 verdes (escopo do plano).
2. Suíte completa NÃO roda dentro deste plano (wave 6 paralela com 01-06) — ela roda no fechamento da wave 6 pelo executor (sampling do 01-VALIDATION.md).
3. `npm run typecheck && npm run build`.
4. `vendor/bin/pint --dirty --format agent`.
</verification>

<success_criteria>
- HU-008: vínculo por e-mail com vigência, validações pt-BR, auditoria com "em nome de" — testes verdes.
- HU-009: revogação restrita ao outorgante, efeito imediato na request seguinte, bloqueio de terceiro auditado — testes verdes.
- `acting_for_user_id` populado em activities geradas durante representação (RN-002 completo).
</success_criteria>

<output>
Após concluir, criar `.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-07-SUMMARY.md` registrando o desenho final da representação (sessão + Context + scoped service).
</output>

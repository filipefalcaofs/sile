---
phase: 01-identidade-acesso-e-auditoria-transversal
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - database/migrations/*_create_legal_terms_table.php
  - database/migrations/*_create_legal_term_acceptances_table.php
  - app/Models/LegalTerm.php
  - app/Models/LegalTermAcceptance.php
  - app/Models/User.php
  - database/factories/LegalTermFactory.php
  - database/factories/UserFactory.php
  - database/seeders/LegalTermSeeder.php
  - database/seeders/DatabaseSeeder.php
  - app/Http/Middleware/EnsureLgpdTermAccepted.php
  - app/Http/Controllers/Portal/LgpdTermController.php
  - bootstrap/app.php
  - routes/portal.php
  - routes/gestao.php
  - resources/js/pages/portal/termo-lgpd.tsx
  - tests/Feature/Lgpd/TermAcceptanceTest.php
  - tests/Feature/Routing/EnvironmentAccessTest.php
autonomous: true
requirements: ["HU-006"]

must_haves:
  truths:
    - "Usuário autenticado e verificado sem aceite do termo vigente é levado à página do termo antes de usar o sistema (HU-006 CA-01)"
    - "O aceite registra quem, quando, qual versão e IP, e gera auditoria (HU-006 CA-02)"
    - "Aceite sem marcar a concordância é bloqueado (HU-006 CA-03)"
    - "Nova versão publicada do termo re-exige aceite de todos (HU-006 CA-01)"
    - "O texto do termo vem do banco (seed versionado), nunca de código (preparação HU-014)"
  artifacts:
    - path: "app/Models/LegalTerm.php"
      provides: "Termo versionado com vigência por publicação"
      contains: "public static function current"
    - path: "app/Http/Middleware/EnsureLgpdTermAccepted.php"
      provides: "Gate de aceite nas rotas autenticadas"
    - path: "database/seeders/LegalTermSeeder.php"
      provides: "Versão 1 do termo LGPD (texto provisório administrável)"
    - path: "resources/js/pages/portal/termo-lgpd.tsx"
      provides: "Página de leitura e aceite do termo"
  key_links:
    - from: "routes/portal.php"
      to: "app/Http/Middleware/EnsureLgpdTermAccepted.php"
      via: "alias lgpd.accepted no subgrupo protegido"
      pattern: "lgpd.accepted"
    - from: "app/Http/Middleware/EnsureLgpdTermAccepted.php"
      to: "app/Models/LegalTerm.php"
      via: "LegalTerm::current('lgpd')"
      pattern: "LegalTerm::current"
    - from: "app/Http/Controllers/Portal/LgpdTermController.php"
      to: "legal_term_acceptances"
      via: "criação do aceite com versão e IP"
      pattern: "LegalTermAcceptance"
---

<objective>
Entregar HU-006 (Aceitar termo LGPD): termos legais versionados no banco, middleware que exige o aceite da versão vigente no primeiro acesso (e a cada nova versão publicada), página de leitura/aceite e registro auditado de consentimento (quem, quando, versão, IP).

Propósito: consentimento LGPD é pré-requisito legal do uso do sistema; o versionamento prepara a administração do texto pela HU-014 (Fase 2) e o monitoramento pela Fase 12.
Output: fluxo de consentimento funcionando de ponta a ponta, com helper de teste para todos os planos seguintes.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-CONTEXT.md
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md
@docs/SILE_HUs_Completas_MD/EP01-Identidade-Acesso-e-Seguranca/HU-006-Aceitar-termo-LGPD.md
</context>

<important_notes>
- **Ordem de middlewares (decisão do RESEARCH, Padrão 6):** portal: `auth` → `verified` → `lgpd.accepted`; gestão: `auth` → `verified` → `permission:acessar-gestao` → `lgpd.accepted` (bloqueio de permissão prevalece sobre termo — CA-04 de acesso continua respondendo 403).
- As rotas do PRÓPRIO termo (show/accept) ficam DENTRO do grupo portal mas FORA do subgrupo `lgpd.accepted` (senão loop de redirect). Logout é rota do Fortify, fora do grupo — não é afetado.
- **Os testes existentes de portal/gestão NÃO quebram com este plano:** eles não seedam o LegalTermSeeder e, sem termo publicado, `LegalTerm::current()` retorna null e o gate desarma. A atualização do `EnvironmentAccessTest` na Task 2 é DEFENSIVA (não corretiva): blinda esses testes contra setups futuros que seedem o `DatabaseSeeder` completo (que passa a incluir o termo a partir deste plano).
- Sem termo publicado no banco, o middleware deixa passar (`LegalTerm::current()` null) — por isso os testes dos planos 01-02/01-03 que não seedam termo continuam verdes.
- Texto do termo NUNCA em código de produção: vive no seed (dado), marcado para substituição pelo texto oficial da SEDUR (Open Question 3 do RESEARCH).
- TDD estrito. Pint após PHP. Typecheck/build após TSX.
</important_notes>

<tasks>

<task type="auto">
  <name>Task 1: Domínio de termos versionados — models, migrations, factories e seed (TDD)</name>
  <files>database/migrations/*_create_legal_terms_table.php, database/migrations/*_create_legal_term_acceptances_table.php, app/Models/LegalTerm.php, app/Models/LegalTermAcceptance.php, app/Models/User.php, database/factories/LegalTermFactory.php, database/seeders/LegalTermSeeder.php, database/seeders/DatabaseSeeder.php, tests/Feature/Lgpd/TermAcceptanceTest.php</files>
  <read_first>
    - app/Models/User.php (estado pós 01-04 — traits e implements atuais)
    - app/Concerns/HasAuditoria.php (trait que LegalTermAcceptance usará)
    - database/seeders/DatabaseSeeder.php (calls atuais)
    - .planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md (Padrão 6 — schema das duas tabelas)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Lgpd/TermAcceptanceTest.php` (`php artisan make:test --phpunit Lgpd/TermAcceptanceTest`) com `RefreshDatabase`, primeiros testes (de domínio):
    - `test_current_retorna_versao_publicada_mais_recente`: criar via factory termo lgpd v1 publicado, v2 publicado e v3 rascunho (published_at null) → `LegalTerm::current('lgpd')->version === 2`.
    - `test_current_retorna_null_sem_termo_publicado`: sem registros → null.
    - `test_usuario_sabe_se_aceitou_o_termo`: user + termo v1; `hasAcceptedTerm($term)` false; criar `LegalTermAcceptance` → true.
    - `test_seeder_publica_versao_1_do_termo`: `$this->seed(LegalTermSeeder::class)` → `LegalTerm::current('lgpd')` não-nulo, `version === 1`, `content` não vazio.
    CONFIRMAR RED.

    **GREEN**:
    1. Migrations (`php artisan make:model LegalTerm -mf --no-interaction` e `php artisan make:model LegalTermAcceptance -m --no-interaction`):
       ```php
       Schema::create('legal_terms', function (Blueprint $table) {
           $table->id();
           $table->string('type', 30);              // ex.: 'lgpd'
           $table->unsignedInteger('version');
           $table->string('title');
           $table->text('content');
           $table->timestamp('published_at')->nullable(); // null = rascunho
           $table->timestamps();
           $table->unique(['type', 'version']);
       });

       Schema::create('legal_term_acceptances', function (Blueprint $table) {
           $table->id();
           $table->foreignId('user_id')->constrained()->cascadeOnDelete();
           $table->foreignId('legal_term_id')->constrained()->cascadeOnDelete();
           $table->string('ip_address', 45)->nullable();
           $table->string('user_agent', 500)->nullable();
           $table->timestamp('accepted_at');
           $table->timestamps();
           $table->unique(['user_id', 'legal_term_id']);
       });
       ```
    2. `app/Models/LegalTerm.php`: `use HasFactory, HasAuditoria;` + `#[Fillable(['type', 'version', 'title', 'content', 'published_at'])]`; cast `published_at => 'datetime'`; e:
       ```php
       public static function current(string $type): ?self
       {
           return static::query()
               ->where('type', $type)
               ->whereNotNull('published_at')
               ->where('published_at', '<=', now())
               ->orderByDesc('version')
               ->first();
       }
       ```
    3. `app/Models/LegalTermAcceptance.php`: `use HasAuditoria;` + `#[Fillable(['user_id', 'legal_term_id', 'ip_address', 'user_agent', 'accepted_at'])]`; cast `accepted_at => 'datetime'`; relações `user()` e `term(): BelongsTo (legal_term_id)`.
    4. `app/Models/User.php`:
       ```php
       public function termAcceptances(): HasMany
       {
           return $this->hasMany(LegalTermAcceptance::class);
       }

       public function hasAcceptedTerm(LegalTerm $term): bool
       {
           return $this->termAcceptances()->where('legal_term_id', $term->id)->exists();
       }
       ```
    5. `database/factories/LegalTermFactory.php`: definition `['type' => 'lgpd', 'version' => 1, 'title' => 'Termo de Consentimento LGPD', 'content' => fake()->paragraphs(3, true), 'published_at' => null]` + state `published()` (`published_at => now()`).
    6. `php artisan make:seeder LegalTermSeeder --no-interaction`: `LegalTerm::firstOrCreate(['type' => 'lgpd', 'version' => 1], [...])` publicado com `title` "Termo de Consentimento para Tratamento de Dados Pessoais" e `content` provisório em pt-BR (3+ parágrafos cobrindo: finalidade do tratamento pelo SILE/SEDUR para licenciamento; dados coletados — nome, CPF, e-mail, telefone, endereço do empreendimento, registros de acesso; direitos do titular conforme Lei nº 13.709/2018; canal de contato do encarregado). Incluir comentário PHPDoc: texto provisório a substituir pelo oficial da SEDUR via nova versão publicada (HU-014).
    7. `DatabaseSeeder`: acrescentar `LegalTermSeeder::class` ao call.
    CONFIRMAR GREEN.

    **REFACTOR** — pint.
  </action>
  <verify>php artisan test --compact --filter=TermAcceptanceTest</verify>
  <acceptance_criteria>
    - `app/Models/LegalTerm.php` contém `public static function current(string $type): ?self`
    - `app/Models/User.php` contém `public function hasAcceptedTerm(LegalTerm $term): bool`
    - Migration de acceptances contém `unique(['user_id', 'legal_term_id'])`
    - `database/seeders/LegalTermSeeder.php` contém `Lei nº 13.709/2018`
    - `php artisan test --compact --filter=TermAcceptanceTest` termina com `"result":"passed"` (4 testes)
  </acceptance_criteria>
  <done>Termos versionados com vigência por publicação, aceite modelado com unicidade por versão, seed v1 pronto.</done>
</task>

<task type="auto">
  <name>Task 2: Middleware lgpd.accepted, página de aceite e integração nos dois ambientes (TDD)</name>
  <files>app/Http/Middleware/EnsureLgpdTermAccepted.php, app/Http/Controllers/Portal/LgpdTermController.php, bootstrap/app.php, routes/portal.php, routes/gestao.php, resources/js/pages/portal/termo-lgpd.tsx, database/factories/UserFactory.php, tests/Feature/Lgpd/TermAcceptanceTest.php, tests/Feature/Routing/EnvironmentAccessTest.php</files>
  <read_first>
    - routes/portal.php e routes/gestao.php (estrutura de grupos pós 01-03)
    - bootstrap/app.php (aliases existentes role/permission; preservar SecurityHeaders e callbacks de 403)
    - tests/Feature/Routing/EnvironmentAccessTest.php (testes que serão atualizados — GETs 200 em portal/gestão)
    - database/factories/UserFactory.php (states existentes)
  </read_first>
  <action>
    **RED** — acrescentar ao `TermAcceptanceTest` (seed de roles + LegalTermSeeder no setUp destes testes):
    - `test_usuario_sem_aceite_e_redirecionado_ao_termo`: cidadão verificado GET `/portal` → `assertRedirect(route('portal.termo-lgpd.show'))`.
    - `test_pagina_do_termo_renderiza_conteudo_vigente`: cidadão GET `/portal/termo-lgpd` → 200, component `portal/termo-lgpd`, props `term.title` e `term.content` presentes, `term.version === 1`.
    - `test_aceite_grava_versao_ip_e_audita` (CA-01/CA-02): POST `/portal/termo-lgpd` com `['accepted' => true]` → redirect `/portal`; `assertDatabaseHas('legal_term_acceptances', ['user_id' => $user->id, 'ip_address' => '127.0.0.1'])` com `legal_term_id` do termo v1; `assertDatabaseHas('activity_log', ['subject_type' => (new LegalTermAcceptance)->getMorphClass(), 'event' => 'created'])` (HasAuditoria); após o aceite, GET `/portal` → 200.
    - `test_aceite_sem_concordancia_e_bloqueado` (CA-03): POST com `['accepted' => false]` → `assertSessionHasErrors('accepted')`; nenhum acceptance criado.
    - `test_nova_versao_publicada_reexige_aceite`: usuário aceitou v1; publicar v2 via factory → GET `/portal` → redirect ao termo.
    - `test_visitante_nao_acessa_o_termo` (CA-04): GET `/portal/termo-lgpd` sem auth → redirect `/login`.
    - `test_gestao_tambem_exige_termo`: admin verificado sem aceite GET `/gestao` → redirect ao termo; cidadão sem aceite GET `/gestao` → 403 (permissão avaliada ANTES do termo).
    CONFIRMAR RED.

    **GREEN**:
    1. `php artisan make:middleware EnsureLgpdTermAccepted --no-interaction`:
       ```php
       public function handle(Request $request, Closure $next): Response
       {
           $term = LegalTerm::current('lgpd');

           if ($term !== null && ! $request->user()->hasAcceptedTerm($term)) {
               return redirect()->route('portal.termo-lgpd.show');
           }

           return $next($request);
       }
       ```
    2. `bootstrap/app.php`: acrescentar alias `'lgpd.accepted' => \App\Http\Middleware\EnsureLgpdTermAccepted::class,`.
    3. `routes/portal.php` — reestruturar (rotas do termo fora do subgrupo):
       ```php
       Route::middleware(['auth', 'verified'])
           ->prefix('portal')
           ->name('portal.')
           ->group(function () {
               Route::get('termo-lgpd', [LgpdTermController::class, 'show'])->name('termo-lgpd.show');
               Route::post('termo-lgpd', [LgpdTermController::class, 'accept'])->name('termo-lgpd.accept');

               Route::middleware('lgpd.accepted')->group(function () {
                   Route::get('/', DashboardController::class)->name('dashboard');
               });
           });
       ```
    4. `routes/gestao.php`: middleware do grupo passa a `['auth', 'verified', 'permission:acessar-gestao', 'lgpd.accepted']`.
    5. `php artisan make:controller Portal/LgpdTermController --no-interaction`:
       - `show()`: `$term = LegalTerm::current('lgpd');` se null → redirect intended portal.dashboard (sem termo publicado, nada a aceitar); se o usuário já aceitou → redirect portal.dashboard; senão `Inertia::render('portal/termo-lgpd', ['term' => $term->only('id', 'version', 'title', 'content')])`.
       - `accept(Request $request)`: `$request->validate(['accepted' => ['accepted']], [], ['accepted' => 'aceite'])`; criar acceptance com `user_id`, `legal_term_id` do vigente, `ip_address => $request->ip()`, `user_agent => substr((string) $request->userAgent(), 0, 500)`, `accepted_at => now()`; `redirect()->intended(route('portal.dashboard'))->with('status', 'Termo aceito com sucesso.')`.
    6. `resources/js/pages/portal/termo-lgpd.tsx`: layout próprio simples (sem portal-layout para não induzir navegação antes do aceite): título do termo, versão, conteúdo em `<div className="whitespace-pre-line ...">`, `<Form action="/portal/termo-lgpd" method="post">` com checkbox name="accepted" rotulado "Li e concordo com o tratamento dos meus dados pessoais conforme descrito" e botão "Aceitar e continuar"; erro de validação exibido; botão "Sair" via logout.
    7. `database/factories/UserFactory.php` — state para os DEMAIS planos:
       ```php
       public function withAcceptedLgpdTerm(): static
       {
           return $this->afterCreating(function (User $user) {
               $term = LegalTerm::current('lgpd')
                   ?? LegalTerm::factory()->published()->create(['type' => 'lgpd', 'version' => 1]);

               LegalTermAcceptance::create([
                   'user_id' => $user->id,
                   'legal_term_id' => $term->id,
                   'ip_address' => '127.0.0.1',
                   'accepted_at' => now(),
               ]);
           });
       }
       ```
    8. ATUALIZAR `tests/Feature/Routing/EnvironmentAccessTest.php` (atualização DEFENSIVA — sem termo seedado esses testes já passam, ver important_notes): os testes que esperam 200 em `/portal` e `/gestao` passam a usar `User::factory()->cidadao()->withAcceptedLgpdTerm()->create()` (e equivalentes admin/analista), tornando-os imunes a setups futuros que seedem o termo. O teste de CA-04 (403 na gestão) NÃO precisa do termo (permissão vem antes) — manter sem aceite para provar a ordem.
    CONFIRMAR GREEN (TermAcceptanceTest + EnvironmentAccessTest + suíte completa).

    **REFACTOR** — pint; `npm run typecheck && npm run build`.
  </action>
  <verify>php artisan test --compact tests/Feature/Lgpd/TermAcceptanceTest.php && php artisan test --compact</verify>
  <acceptance_criteria>
    - `bootstrap/app.php` contém `'lgpd.accepted'`
    - `routes/portal.php` contém `termo-lgpd.show` e o subgrupo `Route::middleware('lgpd.accepted')`
    - `routes/gestao.php` contém `'permission:acessar-gestao', 'lgpd.accepted'` nessa ordem
    - `database/factories/UserFactory.php` contém `public function withAcceptedLgpdTerm(): static`
    - `php artisan test --compact tests/Feature/Lgpd/TermAcceptanceTest.php` termina com `"result":"passed"` (11 testes)
    - `php artisan test --compact` (suíte completa, incluindo EnvironmentAccessTest atualizado) termina com `"result":"passed"`
  </acceptance_criteria>
  <done>HU-006 com os 4 CAs cobertos: consentimento exigido, registrado com versão/IP, auditado, re-exigido em nova versão; helper de teste disponível para os planos 01-06 a 01-09.</done>
</task>

</tasks>

<verification>
1. `php artisan test --compact tests/Feature/Lgpd/TermAcceptanceTest.php` — HU-006 verde.
2. `php artisan test --compact` — suíte completa verde (EnvironmentAccessTest atualizado, sem regressão).
3. `npm run typecheck && npm run build`.
4. `vendor/bin/pint --dirty --format agent`.
</verification>

<success_criteria>
- HU-006: CA-01 (aceite no primeiro acesso e em nova versão), CA-02 (quem/quando/versão/IP + activity), CA-03 (sem concordância bloqueia), CA-04 (anônimo bloqueado) — todos com teste verde.
- Texto do termo é dado versionado (seed), não código.
- State `withAcceptedLgpdTerm()` disponível e usado nos testes atualizados.
</success_criteria>

<output>
Após concluir, criar `.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-05-SUMMARY.md` registrando os testes atualizados e a estrutura final dos grupos de rota.
</output>

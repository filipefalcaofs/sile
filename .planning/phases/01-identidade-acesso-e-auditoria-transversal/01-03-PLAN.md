---
phase: 01-identidade-acesso-e-auditoria-transversal
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - app/Models/User.php
  - bootstrap/app.php
  - database/seeders/RolesAndPermissionsSeeder.php
  - database/seeders/DatabaseSeeder.php
  - database/factories/UserFactory.php
  - routes/web.php
  - routes/portal.php
  - routes/gestao.php
  - app/Http/Controllers/Portal/DashboardController.php
  - app/Http/Controllers/Gestao/DashboardController.php
  - app/Http/Middleware/HandleInertiaRequests.php
  - resources/js/pages/home.tsx
  - resources/js/pages/Welcome.tsx (removido)
  - resources/js/layouts/auth-layout.tsx
  - resources/js/layouts/portal-layout.tsx
  - resources/js/layouts/gestao-layout.tsx
  - resources/js/pages/portal/dashboard.tsx
  - resources/js/pages/gestao/dashboard.tsx
  - resources/js/types/index.d.ts
  - tests/Feature/Routing/EnvironmentAccessTest.php
  - tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "Existem os papéis cidadao, analista, gestor e administrador com permissões granulares seedadas"
    - "Cidadão autenticado acessa o portal; quem não tem a permissão acessar-gestao recebe 403 na gestão e a tentativa fica auditada"
    - "Administrador autenticado acessa a gestão"
    - "Visitante vê a landing pública do SILE com acesso a login e cadastro"
    - "As páginas Inertia recebem auth.user, auth.roles, auth.permissions e flash compartilhados"
  artifacts:
    - path: "database/seeders/RolesAndPermissionsSeeder.php"
      provides: "Seed dos 4 papéis e permissões da Fase 1"
      contains: "forgetCachedPermissions"
    - path: "routes/portal.php"
      provides: "Ambiente SILE Cidadão (prefix portal, name portal.)"
    - path: "routes/gestao.php"
      provides: "Ambiente SILE Gestão (prefix gestao, name gestao., permission:acessar-gestao)"
    - path: "resources/js/pages/home.tsx"
      provides: "Landing pública do SILE"
    - path: "resources/js/layouts/auth-layout.tsx"
      provides: "Layout das telas de autenticação (usado pelos planos 01-04/01-05/01-06)"
    - path: "resources/js/pages/portal/dashboard.tsx"
      provides: "Dashboard mínimo do portal"
    - path: "resources/js/pages/gestao/dashboard.tsx"
      provides: "Dashboard mínimo da gestão"
  key_links:
    - from: "routes/gestao.php"
      to: "spatie/laravel-permission"
      via: "middleware permission:acessar-gestao"
      pattern: "permission:acessar-gestao"
    - from: "app/Http/Middleware/HandleInertiaRequests.php"
      to: "spatie roles/permissions"
      via: "share auth.roles/auth.permissions"
      pattern: "getAllPermissions"
    - from: "routes/web.php"
      to: "routes/portal.php"
      via: "require"
      pattern: "require __DIR__.'/portal.php'"
---

<objective>
Estabelecer o modelo de perfis e permissões (spatie/laravel-permission v8) e os dois ambientes segregados — SILE Cidadão (portal) e SILE Gestão (retaguarda) — com landing pública, dashboards mínimos navegáveis, props compartilhadas com o React e o CA-04 (acesso negado auditado) provado de ponta a ponta.

Propósito: HU-002 (acesso por perfil), HU-008/009 (permissões de procuração) e HU-010 (consulta admin) dependem desta base; a detecção de `channel` da auditoria depende dos nomes de rota `portal.*`/`gestao.*`.
Output: papéis/permissões seedados, rotas segregadas funcionando, landing e dashboards renderizando.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-CONTEXT.md
@.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md
</context>

<important_notes>
- **Decisão travada (RESEARCH, Padrão 4):** papéis são `cidadao`, `analista`, `gestor`, `administrador`. Contador e procurador NÃO são papéis — são cidadãos cujo poder vem de dados (procuração, vínculo empresarial). Um único guard `web`.
- **Pitfall 2:** após criar roles/permissions, chamar `app(\Spatie\Permission\PermissionRegistrar::class)->forgetCachedPermissions()`.
- O grupo portal NESTE plano usa `['auth', 'verified']` — o middleware `lgpd.accepted` será adicionado no plano 01-05 (que criará o middleware e atualizará estes testes).
- O middleware `verified` exige `User implements MustVerifyEmail`, que só chega no plano 01-04. Neste plano, os testes usam usuários `User::factory()->create()` (já verificados por padrão na factory) — o middleware `verified` sem MustVerifyEmail deixa passar, o que é aceitável aqui; o bloqueio de não-verificados é CA do plano 01-04.
- O `bootstrap/app.php` já tem SecurityHeaders no grupo web (01-01) e os callbacks de auditoria de 403 no withExceptions (01-02) — PRESERVAR ambos ao adicionar os aliases.
- Páginas Inertia: nomes kebab/minúsculos exatos (`portal/dashboard`, `home`), case-sensitive no CI (Pitfall 10).
- TDD estrito. Pint após PHP. `npm run typecheck` após criar arquivos TSX.
</important_notes>

<tasks>

<task type="auto">
  <name>Task 1: Papéis, permissões e seeds (TDD)</name>
  <files>app/Models/User.php, database/seeders/RolesAndPermissionsSeeder.php, database/seeders/DatabaseSeeder.php, database/factories/UserFactory.php, tests/Feature/Authorization/RolesAndPermissionsSeederTest.php</files>
  <read_first>
    - app/Models/User.php (estado pós plano 01-02 — traits HasAuditoria/CausesActivity presentes)
    - database/factories/UserFactory.php (estado atual)
    - database/seeders/DatabaseSeeder.php (cria Test User do esqueleto — será substituído)
    - vendor/spatie/laravel-permission/src/PermissionRegistrar.php (método forgetCachedPermissions)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Authorization/RolesAndPermissionsSeederTest.php` (`php artisan make:test --phpunit Authorization/RolesAndPermissionsSeederTest`) com `RefreshDatabase`:
    - `test_seeder_cria_papeis_e_permissoes_da_fase`: rodar `$this->seed(\Database\Seeders\RolesAndPermissionsSeeder::class);` e assertar que existem os roles `cidadao`, `analista`, `gestor`, `administrador` e as permissions `acessar-gestao`, `consultar-acessos-de-qualquer-conta`, `gerenciar-procuracoes-proprias` (via `Role::findByName(...)` / `Permission::findByName(...)` sem exceção, guard `web`).
    - `test_atribuicoes_de_permissao_por_papel`: após seed — `administrador` tem `acessar-gestao` E `consultar-acessos-de-qualquer-conta`; `analista` e `gestor` têm `acessar-gestao`; `cidadao` tem `gerenciar-procuracoes-proprias` e NÃO tem `acessar-gestao` (`$role->hasPermissionTo(...)`).
    - `test_estados_da_factory_atribuem_papel`: após seed, `User::factory()->administrador()->create()->hasRole('administrador')` é true; idem `analista()`, `gestor()`, `cidadao()`.
    - `test_seeder_e_idempotente`: rodar o seeder duas vezes não duplica (count de roles continua 4).
    Rodar `php artisan test --compact --filter=RolesAndPermissionsSeederTest` e CONFIRMAR falha.

    **GREEN**:
    1. `app/Models/User.php`: adicionar `use Spatie\Permission\Traits\HasRoles;` no corpo da classe.
    2. `php artisan make:seeder RolesAndPermissionsSeeder --no-interaction`:
       ```php
       public function run(): void
       {
           app(\Spatie\Permission\PermissionRegistrar::class)->forgetCachedPermissions();

           $permissions = [
               'acessar-gestao',
               'consultar-acessos-de-qualquer-conta',
               'gerenciar-procuracoes-proprias',
           ];

           foreach ($permissions as $permission) {
               Permission::firstOrCreate(['name' => $permission, 'guard_name' => 'web']);
           }

           Role::firstOrCreate(['name' => 'cidadao', 'guard_name' => 'web'])
               ->syncPermissions(['gerenciar-procuracoes-proprias']);
           Role::firstOrCreate(['name' => 'analista', 'guard_name' => 'web'])
               ->syncPermissions(['acessar-gestao']);
           Role::firstOrCreate(['name' => 'gestor', 'guard_name' => 'web'])
               ->syncPermissions(['acessar-gestao']);
           Role::firstOrCreate(['name' => 'administrador', 'guard_name' => 'web'])
               ->syncPermissions(['acessar-gestao', 'consultar-acessos-de-qualquer-conta']);

           app(\Spatie\Permission\PermissionRegistrar::class)->forgetCachedPermissions();
       }
       ```
    3. `DatabaseSeeder`: substituir a criação do Test User por `$this->call(RolesAndPermissionsSeeder::class);` (o usuário admin de desenvolvimento entra no plano 01-09).
    4. `UserFactory`: adicionar states que atribuem papel (o papel deve existir — testes seedam antes):
       ```php
       public function cidadao(): static
       {
           return $this->afterCreating(fn (User $user) => $user->assignRole('cidadao'));
       }
       // idem analista(), gestor(), administrador()
       ```
    Rodar o filtro e CONFIRMAR verde.

    **REFACTOR** — pint.
  </action>
  <verify>php artisan test --compact --filter=RolesAndPermissionsSeederTest</verify>
  <acceptance_criteria>
    - `app/Models/User.php` contém `use HasRoles` (trait no corpo da classe)
    - `database/seeders/RolesAndPermissionsSeeder.php` contém `forgetCachedPermissions` e as 3 strings de permissão exatas
    - `database/factories/UserFactory.php` contém `public function administrador(): static`
    - `php artisan test --compact --filter=RolesAndPermissionsSeederTest` termina com `"result":"passed"` (4 testes)
    - `database/seeders/DatabaseSeeder.php` NÃO contém mais `test@example.com`
  </acceptance_criteria>
  <done>Papéis e permissões da Fase 1 seedados e testados; factory com states por papel para todos os planos seguintes.</done>
</task>

<task type="auto">
  <name>Task 2: Rotas segregadas portal × gestão, landing, layouts, dashboards e props compartilhadas (TDD)</name>
  <files>bootstrap/app.php, routes/web.php, routes/portal.php, routes/gestao.php, app/Http/Controllers/Portal/DashboardController.php, app/Http/Controllers/Gestao/DashboardController.php, app/Http/Middleware/HandleInertiaRequests.php, resources/js/pages/home.tsx, resources/js/layouts/auth-layout.tsx, resources/js/layouts/portal-layout.tsx, resources/js/layouts/gestao-layout.tsx, resources/js/pages/portal/dashboard.tsx, resources/js/pages/gestao/dashboard.tsx, resources/js/types/index.d.ts, tests/Feature/Routing/EnvironmentAccessTest.php</files>
  <read_first>
    - bootstrap/app.php (estado pós 01-02 — preservar SecurityHeaders no grupo web e os callbacks de auditoria no withExceptions)
    - app/Http/Middleware/HandleInertiaRequests.php (método share atual)
    - resources/js/app.tsx (auto-resolução de páginas — não alterar)
    - resources/js/pages/Welcome.tsx (página do esqueleto que será substituída pela landing home.tsx)
    - .planning/phases/01-identidade-acesso-e-auditoria-transversal/01-RESEARCH.md (Padrões 4 e 8)
  </read_first>
  <action>
    **RED** — criar `tests/Feature/Routing/EnvironmentAccessTest.php` com `RefreshDatabase`; no `setUp`, `$this->seed(\Database\Seeders\RolesAndPermissionsSeeder::class);`:
    - `test_landing_publica_renderiza`: GET `/` → 200 e `assertInertia(->component('home'))`.
    - `test_visitante_e_redirecionado_ao_login_no_portal`: GET `/portal` sem auth → `assertRedirect('/login')`.
    - `test_cidadao_acessa_dashboard_do_portal`: `actingAs(User::factory()->cidadao()->create())` GET `/portal` → 200 e `assertInertia(fn ($page) => $page->component('portal/dashboard'))`.
    - `test_props_de_autenticacao_sao_compartilhadas`: no mesmo GET, `assertInertia(->has('auth.user.id')->has('auth.roles')->has('auth.permissions'))` e `auth.roles` contém `cidadao` (usar `->where('auth.roles.0', 'cidadao')` ou equivalente).
    - `test_cidadao_nao_acessa_gestao_e_tentativa_e_auditada` (CA-04): cidadão GET `/gestao` → `assertForbidden()` e `assertDatabaseHas('activity_log', ['log_name' => 'seguranca', 'event' => 'acesso-negado', 'result' => 'bloqueado'])`.
    - `test_administrador_acessa_dashboard_da_gestao`: admin GET `/gestao` → 200, component `gestao/dashboard`.
    - `test_analista_acessa_gestao`: analista GET `/gestao` → 200.
    Rodar `php artisan test --compact --filter=EnvironmentAccessTest` e CONFIRMAR falha (rotas inexistentes).

    **GREEN**:
    1. `bootstrap/app.php` — aliases (dentro do withMiddleware existente, preservando o append do grupo web):
       ```php
       $middleware->alias([
           'role' => \Spatie\Permission\Middleware\RoleMiddleware::class,
           'permission' => \Spatie\Permission\Middleware\PermissionMiddleware::class,
       ]);
       ```
       (Confirmar namespace `Middleware` singular no vendor — spatie/permission v8.)
    2. `routes/portal.php`:
       ```php
       <?php

       use App\Http\Controllers\Portal\DashboardController;
       use Illuminate\Support\Facades\Route;

       Route::middleware(['auth', 'verified'])
           ->prefix('portal')
           ->name('portal.')
           ->group(function () {
               Route::get('/', DashboardController::class)->name('dashboard');
           });
       ```
    3. `routes/gestao.php`:
       ```php
       <?php

       use App\Http\Controllers\Gestao\DashboardController;
       use Illuminate\Support\Facades\Route;

       Route::middleware(['auth', 'verified', 'permission:acessar-gestao'])
           ->prefix('gestao')
           ->name('gestao.')
           ->group(function () {
               Route::get('/', DashboardController::class)->name('dashboard');
           });
       ```
    4. `routes/web.php` — trocar a rota `/` para a landing do SILE e registrar os ambientes:
       ```php
       Route::get('/', fn () => Inertia::render('home'))->name('home');

       require __DIR__.'/portal.php';
       require __DIR__.'/gestao.php';
       ```
       Remover `resources/js/pages/Welcome.tsx` (substituída pela `home.tsx`).
    5. Controllers invocáveis (`php artisan make:controller Portal/DashboardController --invokable --no-interaction` e `Gestao/DashboardController`): retornam `Inertia::render('portal/dashboard')` e `Inertia::render('gestao/dashboard')`.
    6. `HandleInertiaRequests::share`:
       ```php
       return [
           ...parent::share($request),
           'auth' => [
               'user' => $request->user()?->only('id', 'name', 'email'),
               'roles' => $request->user()?->getRoleNames() ?? [],
               'permissions' => $request->user()?->getAllPermissions()->pluck('name') ?? [],
           ],
           'flash' => [
               'status' => $request->session()->get('status'),
           ],
       ];
       ```
    7. Frontend (pt-BR, Tailwind 4, dark mode, mobile-first):
       - `resources/js/pages/home.tsx`: landing pública — título "SILE — Sistema de Licenciamento Eletrônico", subtítulo "Viabilidade locacional de atividades econômicas em Salvador", botões `<Link href="/login">Entrar</Link>` e `<Link href="/register">Criar conta</Link>` (estilizados como botões primário/secundário).
       - `resources/js/layouts/auth-layout.tsx`: card centralizado (max-w-md, rounded-xl, shadow) com cabeçalho "SILE" + subtítulo recebido por prop e `children` para os formulários — será usado pelas telas de auth dos planos 01-04/01-05/01-06.
       - `resources/js/layouts/portal-layout.tsx`: header com título "SILE — Portal do Cidadão", nome do usuário (`usePage().props.auth.user`), botão "Sair" (`<Link href="/logout" method="post" as="button">`), área de flash status, `<main>` com children.
       - `resources/js/layouts/gestao-layout.tsx`: idem com título "SILE — Gestão SEDUR".
       - `resources/js/pages/portal/dashboard.tsx`: usa portal-layout; título "Meu painel"; texto de boas-vindas "Bem-vindo(a) ao SILE".
       - `resources/js/pages/gestao/dashboard.tsx`: usa gestao-layout; título "Painel de gestão".
       - Tipar as props compartilhadas em `resources/js/types/index.d.ts` (criar): interface `SharedProps` com `auth: { user: { id: number; name: string; email: string } | null; roles: string[]; permissions: string[] }; flash: { status?: string }`.
    Rodar o filtro e CONFIRMAR verde. Rodar `npm run typecheck && npm run build`.

    **REFACTOR** — pint; suíte completa.
  </action>
  <verify>php artisan test --compact --filter=EnvironmentAccessTest && php artisan test --compact && npm run typecheck && npm run build</verify>
  <acceptance_criteria>
    - `routes/gestao.php` contém `permission:acessar-gestao`
    - `routes/web.php` contém `Inertia::render('home')`, `require __DIR__.'/portal.php'` e `require __DIR__.'/gestao.php'`
    - `bootstrap/app.php` contém `PermissionMiddleware::class` E continua contendo `SecurityHeaders::class`
    - `app/Http/Middleware/HandleInertiaRequests.php` contém `getAllPermissions`
    - Existem `resources/js/pages/home.tsx`, `resources/js/layouts/auth-layout.tsx`, `resources/js/pages/portal/dashboard.tsx` e `resources/js/pages/gestao/dashboard.tsx`; `resources/js/pages/Welcome.tsx` NÃO existe mais
    - `php artisan test --compact --filter=EnvironmentAccessTest` termina com `"result":"passed"` (7 testes)
    - `npm run typecheck` exit 0 e `npm run build` exit 0
  </acceptance_criteria>
  <done>Landing pública e dois ambientes navegáveis com segregação por permissão, 403 auditado provado por teste, props compartilhadas tipadas no React.</done>
</task>

</tasks>

<verification>
1. `php artisan test --compact` — suíte completa verde.
2. `php artisan route:list --name=portal` e `--name=gestao` — rotas nomeadas com prefixos corretos (alimentam o `channel` da auditoria).
3. `npm run typecheck && npm run build` — frontend compila.
4. `vendor/bin/pint --dirty --format agent` — sem pendências.
</verification>

<success_criteria>
- 4 papéis e 3 permissões seedados, idempotentes, cobertos por teste.
- Landing pública renderizando; portal e gestão segregados; cidadão bloqueado na gestão com auditoria (CA-04 ponta a ponta).
- Dashboards Inertia renderizando com layouts pt-BR.
- Suíte completa verde; typecheck e build verdes; pint limpo.
</success_criteria>

<output>
Após concluir, criar `.planning/phases/01-identidade-acesso-e-auditoria-transversal/01-03-SUMMARY.md` registrando decisões e o estado das rotas (`route:list`).
</output>

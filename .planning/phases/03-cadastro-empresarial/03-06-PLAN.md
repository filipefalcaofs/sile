---
phase: 03-cadastro-empresarial
plan: 06
type: execute
wave: 5
depends_on: ["03-04", "03-05"]
files_modified:
  - app/Services/CompanyCnaeService.php
  - app/Http/Controllers/Portal/CompanyCnaeController.php
  - app/Http/Controllers/Portal/CnaeSearchController.php
  - app/Http/Requests/Portal/UpdatePrimaryCnaeRequest.php
  - app/Http/Requests/Portal/UpdateSecondaryCnaesRequest.php
  - routes/portal.php
  - tests/Feature/Companies/PrimaryCnaeTest.php
  - tests/Feature/Companies/SecondaryCnaesTest.php
autonomous: true
requirements: [HU-025, HU-026]

must_haves:
  truths:
    - "Empresa tem EXATAMENTE um CNAE principal; trocar o principal demove o antigo na mesma transação (demote → promote)"
    - "Secundários são sincronizados como conjunto exato, sem duplicar entre si nem conter o principal; remoção total é permitida"
    - "Somente CNAEs ATIVOS da tabela oficial são aceitos na seleção manual (CA-03)"
    - "Troca de principal e alterações de secundários são auditadas com antes/depois (insumo do motor de regras — Fases 5/6)"
    - "GET /portal/cnaes busca server-side só ativos com id/formatted_code/description"
  artifacts:
    - path: "app/Services/CompanyCnaeService.php"
      provides: "setPrimary/syncSecondaries transacionais com auditoria explícita — ÚNICO ponto de escrita do pivot"
    - path: "app/Http/Controllers/Portal/CompanyCnaeController.php"
      provides: "updatePrimary/updateSecondaries autorizados por manageCnaes"
    - path: "app/Http/Controllers/Portal/CnaeSearchController.php"
      provides: "Busca JSON da tabela oficial (padrão [02-04] de dígitos)"
  key_links:
    - from: "app/Http/Controllers/Portal/CompanyCnaeController.php"
      to: "app/Services/CompanyCnaeService.php"
      via: "controller delega TODA escrita ao service (anti-pattern: sync direto)"
      pattern: "CompanyCnaeService"
    - from: "app/Services/CompanyCnaeService.php"
      to: "activity_log"
      via: "AuditService->log('empresas', 'cnae-principal'/'cnaes-secundarios', ...)"
      pattern: "cnae-principal"
    - from: "app/Http/Requests/Portal/UpdatePrimaryCnaeRequest.php"
      to: "cnaes"
      via: "Rule::exists('cnaes', 'id')->where('active', true)"
      pattern: "where\\('active', true\\)"
---

<objective>
HU-025 (CNAE principal) e HU-026 (CNAEs secundários): CompanyCnaeService transacional como único ponto de escrita do pivot (invariante "exatamente um principal" na aplicação, precedente [01-07]), endpoints autorizados e busca server-side da tabela oficial para os selects da tela 03-08.

Output: service + 2 controllers + 2 FormRequests + rotas + 2 suítes.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-cadastro-empresarial/03-CONTEXT.md
@.planning/phases/03-cadastro-empresarial/03-RESEARCH.md
@.planning/phases/03-cadastro-empresarial/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CompanyCnaeService — setPrimary/syncSecondaries transacionais com auditoria antes/depois</name>
  <files>app/Services/CompanyCnaeService.php, tests/Feature/Companies/PrimaryCnaeTest.php, tests/Feature/Companies/SecondaryCnaesTest.php</files>
  <read_first>
    - .planning/phases/03-cadastro-empresarial/03-RESEARCH.md — "Padrão 3 — CNAEs com auditoria explícita" (código prescrito do setPrimary) e Pitfall 7 (demote→promote; sync calculado preservando o principal)
    - app/Support/Audit/AuditService.php (assinatura)
    - tests/Feature/Roles/ManageRolesTest.php (padrão [02-06] de auditoria antes/depois de conjunto)
  </read_first>
  <action>
    TDD estrito — testes do SERVICE primeiro (endpoints na Task 2 ampliam os mesmos arquivos).

    1. RED — criar PrimaryCnaeTest e SecondaryCnaesTest (RefreshDatabase, seed RolesAndPermissionsSeeder). Cenários do service (instanciar via app(CompanyCnaeService::class)):
       PrimaryCnaeTest:
       - test_define_cnae_principal_unico (CA-01): company sem CNAEs; setPrimary(A) → pivot tem 1 linha is_primary true.
       - test_troca_de_principal_demove_o_antigo (CA-01/Pitfall 7): setPrimary(A) depois setPrimary(B) → A com is_primary false (linha PRESERVADA como secundário? NÃO — conferir RESEARCH: demote deixa o antigo como secundário via update do pivot); contagem de is_primary=true === 1; B é o principal.
       - test_troca_de_principal_e_auditada_com_anterior_e_novo (CA-02): activity 'cnae-principal' com properties cnae_anterior (code de A) e cnae_novo (code de B).
       SecondaryCnaesTest:
       - test_sincroniza_conjunto_exato_de_secundarios (CA-01): principal A; syncSecondaries([B, C]) depois syncSecondaries([C, D]) → secundários finais C e D; principal A INTACTO (sync calculado preserva a linha do principal).
       - test_remocao_total_de_secundarios_e_permitida: syncSecondaries([]) → zero secundários, principal intacto.
       - test_sincronizacao_e_auditada_com_antes_e_depois (CA-02): activity 'cnaes-secundarios' com properties antes [] e depois [codes] (padrão permissoes_antes/depois [02-06]).
       Confirmar RED.

    2. GREEN — `php artisan make:class Services/CompanyCnaeService --no-interaction`, construtor `public function __construct(private AuditService $audit) {}`:
       - `setPrimary(Company $company, Cnae $cnae): void` — implementar o código prescrito no RESEARCH: DB::transaction; capturar $previous = $company->primaryCnae()->first(); DEMOTE (`$company->cnaes()->wherePivot('is_primary', true)->newPivotQuery()->update(['is_primary' => false])`); PROMOTE (`$company->cnaes()->syncWithoutDetaching([$cnae->id => ['is_primary' => true]])`); audit log('empresas', 'cnae-principal', 'CNAE principal definido', ['empresa_id' => $company->id, 'cnae_anterior' => $previous?->code, 'cnae_novo' => $cnae->code], $company).
       - `syncSecondaries(Company $company, array $cnaeIds): void` — DB::transaction; antes = codes dos secundários atuais; montar payload de sync EXATO preservando o principal: `$payload = []; if ($primary = $company->primaryCnae()->first()) { $payload[$primary->id] = ['is_primary' => true]; } foreach ($cnaeIds as $id) { $payload[$id] = ['is_primary' => false]; } $company->cnaes()->sync($payload);` (intenção do usuário = conjunto marcado, padrão syncPermissions [02-06]); depois = codes novos (buscar pelos ids); audit log('empresas', 'cnaes-secundarios', 'CNAEs secundários atualizados', ['empresa_id' => ..., 'antes' => $antes, 'depois' => $depois], $company).
       Rodar `php artisan test --compact --filter='PrimaryCnaeTest|SecondaryCnaesTest'` até verde.

    3. REFACTOR — pint.
  </action>
  <verify>php artisan test --compact --filter='PrimaryCnaeTest|SecondaryCnaesTest'</verify>
  <acceptance_criteria>
    - Filtro composto verde (6 testes de service).
    - `grep "DB::transaction" app/Services/CompanyCnaeService.php` aparece nos DOIS métodos.
    - `grep "cnae_anterior" app/Services/CompanyCnaeService.php` presente.
    - Ordem demote→promote no setPrimary (update is_primary false ANTES do syncWithoutDetaching).
  </acceptance_criteria>
  <done>Invariantes do pivot garantidas em transação com trilha antes/depois para o motor de regras.</done>
</task>

<task type="auto">
  <name>Task 2: Endpoints de CNAE autorizados + busca server-side /portal/cnaes</name>
  <files>app/Http/Controllers/Portal/CompanyCnaeController.php, app/Http/Controllers/Portal/CnaeSearchController.php, app/Http/Requests/Portal/UpdatePrimaryCnaeRequest.php, app/Http/Requests/Portal/UpdateSecondaryCnaesRequest.php, routes/portal.php, tests/Feature/Companies/PrimaryCnaeTest.php, tests/Feature/Companies/SecondaryCnaesTest.php</files>
  <read_first>
    - app/Http/Controllers/Gestao/CnaeController.php — index (padrão de busca: branch de dígitos SÓ quando o termo contém dígitos [02-04]; whereLike caseSensitive false)
    - app/Policies/CompanyPolicy.php (manageCnaes do 03-04)
    - .planning/phases/03-cadastro-empresarial/03-RESEARCH.md — seção "Rotas" (paths/names exatos e nota de ordem)
  </read_first>
  <action>
    TDD estrito.

    1. RED — ampliar as duas suítes com cenários HTTP (helpers do 03-04/03-05):
       PrimaryCnaeTest:
       - test_endpoint_define_principal (CA-01): vinculado ativo; put "/portal/empresas/{id}/cnae-principal" ['cnae_id' => $cnae->id] → redirect back + status; pivot correto.
       - test_cnae_inativo_e_rejeitado (CA-03): cnae active false → assertSessionHasErrors('cnae_id') com mensagem 'O CNAE informado não está ativo na tabela oficial.'; pivot inalterado.
       - test_cnae_inexistente_e_rejeitado (CA-03): cnae_id 999999 → errors cnae_id.
       - test_nao_vinculado_recebe_403 (CA-04): outro user → assertForbidden + activity 'acesso-negado'.
       SecondaryCnaesTest:
       - test_endpoint_sincroniza_secundarios (CA-01): put "/portal/empresas/{id}/cnaes-secundarios" ['cnaes' => [$b->id, $c->id]] → redirect + status; conjunto exato no banco.
       - test_secundario_duplicado_e_rejeitado (CA-03): ['cnaes' => [$b->id, $b->id]] → errors (distinct).
       - test_secundario_igual_ao_principal_e_rejeitado (CA-03): principal A; ['cnaes' => [$a->id]] → errors 'cnaes' com mensagem 'O CNAE principal não pode ser incluído entre os secundários.'.
       - test_nao_vinculado_recebe_403 (CA-04).
       - test_busca_de_cnaes_retorna_somente_ativos: Cnae ativo 'Restaurantes...' (code 5611201) + inativo; get '/portal/cnaes?search=restaurante' → JSON apenas o ativo com chaves exatas id/formatted_code/description.
       - test_busca_de_cnaes_por_codigo: get '/portal/cnaes?search=5611' → encontra por prefixo de dígitos.
       - test_busca_de_cnaes_exige_autenticacao (CA-04): guest → redirect login.
       Confirmar RED.

    2. GREEN:
       - UpdatePrimaryCnaeRequest: authorize() `$this->user()->can('manageCnaes', $this->route('company'))`; rules `['cnae_id' => ['required', 'integer', Rule::exists('cnaes', 'id')->where('active', true)]]`; messages `'cnae_id.exists' => 'O CNAE informado não está ativo na tabela oficial.'`.
       - UpdateSecondaryCnaesRequest: mesmo authorize; rules `['cnaes' => ['present', 'array'], 'cnaes.*' => ['integer', 'distinct', Rule::exists('cnaes', 'id')->where('active', true)]]`; messages para cnaes.*.exists ('Há CNAE inativo ou inexistente na seleção.') e cnaes.*.distinct ('Há CNAE duplicado na seleção.'); withValidator/after(): se o conjunto contém o id do principal atual da empresa → `$validator->errors()->add('cnaes', 'O CNAE principal não pode ser incluído entre os secundários.');`
       - CompanyCnaeController (construtor injeta CompanyCnaeService): `updatePrimary(UpdatePrimaryCnaeRequest $request, Company $company)` → service->setPrimary($company, Cnae::findOrFail($request->validated('cnae_id'))) → back()->with('status', 'CNAE principal definido com sucesso.'); `updateSecondaries(UpdateSecondaryCnaesRequest $request, Company $company)` → service->syncSecondaries($company, $request->validated('cnaes')) → back()->with('status', 'CNAEs secundários atualizados com sucesso.'). PROIBIDO sync direto no controller (anti-pattern do RESEARCH).
       - CnaeSearchController (invokable): termo `$request->string('search')->trim()`; query `Cnae::query()->where('active', true)`; quando termo não vazio aplicar o padrão [02-04]: `$digits = preg_replace('/\D/', '', $term);` branch `where('code', 'like', "{$digits}%")` SÓ se $digits !== '', sempre `orWhereLike('description', "%{$term}%", caseSensitive: false)` dentro do closure; `->orderBy('code')->limit(20)` (constante técnica); retorno `response()->json($cnaes->map(fn ($c) => ['id' => $c->id, 'formatted_code' => $c->formatted_code, 'description' => $c->description]))`.
       - routes/portal.php (depois das rotas de empresas, dentro do mesmo grupo):
         ```php
         Route::put('empresas/{company}/cnae-principal', [CompanyCnaeController::class, 'updatePrimary'])->name('empresas.cnae-principal');
         Route::put('empresas/{company}/cnaes-secundarios', [CompanyCnaeController::class, 'updateSecondaries'])->name('empresas.cnaes-secundarios');
         Route::get('cnaes', CnaeSearchController::class)->name('cnaes.search');
         ```
       Rodar filtro até verde.

    3. REFACTOR — pint; `php artisan route:list --path=portal` para conferir ordem e names.
  </action>
  <verify>php artisan test --compact --filter='PrimaryCnaeTest|SecondaryCnaesTest' && php artisan route:list --name=cnaes.search</verify>
  <acceptance_criteria>
    - Filtro composto verde (17 testes acumulados nos dois arquivos).
    - `grep "sync(" app/Http/Controllers/Portal/CompanyCnaeController.php` NÃO encontra escrita direta de pivot no controller.
    - `grep "O CNAE principal não pode ser incluído entre os secundários." app/Http/Requests/Portal/UpdateSecondaryCnaesRequest.php` presente.
    - `php artisan route:list --name=empresas.cnae-principal` e `--name=cnaes.search` mostram as rotas com names portal.*.
    - Busca retorna no máximo 20 itens, só ativos, shape id/formatted_code/description.
  </acceptance_criteria>
  <done>Endpoints de CNAE com validação de ativos, invariantes no service e busca pronta para o picker da tela 03-08.</done>
</task>

</tasks>

<verification>
- `php artisan test --compact tests/Feature/Companies` verde (ESCOPADO; full-suite só no 03-09).
- `vendor/bin/pint --dirty --format agent` sem pendências.
</verification>

<success_criteria>
- HU-025 CA-01..04 e HU-026 CA-01..04 cobertos por testes pt-BR.
- Escrita do pivot exclusivamente via CompanyCnaeService em transação; auditoria antes/depois pronta para as Fases 5/6.
- Seleção manual restrita a CNAEs ativos; import REDESIM (03-03) segue aceitando inativos com aviso — regras distintas preservadas.
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/03-cadastro-empresarial/03-06-SUMMARY.md` registrando: contratos dos endpoints (paths/payloads), shape do JSON da busca e eventos de auditoria gravados.
</output>

---
phase: 03-cadastro-empresarial
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - app/Http/Controllers/Portal/CompanyController.php
  - app/Http/Controllers/Portal/CompanyLinkController.php
  - app/Http/Requests/Portal/UpdateCompanyRequest.php
  - app/Http/Requests/Portal/EndCompanyLinkRequest.php
  - routes/portal.php
  - tests/Feature/Companies/CompanyUpdateTest.php
  - tests/Feature/Companies/EndCompanyLinkTest.php
autonomous: true
requirements: [HU-024, HU-028]

must_haves:
  truths:
    - "Usuário com vínculo ativo vê o detalhe e atualiza dados complementares; CNPJ é imutável (valor enviado é ignorado — padrão CPF [01-08])"
    - "Atualização gera activity 'updated' com attribute_changes (CA-02)"
    - "Usuário sem vínculo recebe 403 e o evento é auditado (CA-04, render callback global)"
    - "Encerrar vínculo grava ended_at/ended_reason SEM delete físico e é auditado com evento próprio"
    - "Empresa nunca fica sem responsável ativo: último responsável é bloqueado com mensagem pt-BR (CA-03)"
  artifacts:
    - path: "app/Http/Controllers/Portal/CompanyController.php"
      provides: "show com props completas (dados, cnaes, vínculos, abilities) e update autorizado"
    - path: "app/Http/Controllers/Portal/CompanyLinkController.php"
      provides: "destroy = encerramento do PRÓPRIO vínculo (nunca delete)"
    - path: "app/Http/Requests/Portal/EndCompanyLinkRequest.php"
      provides: "after() com proteção do último responsável ativo"
  key_links:
    - from: "app/Http/Controllers/Portal/CompanyController.php"
      to: "app/Policies/CompanyPolicy.php"
      via: "Gate::authorize('view'/'update') — 403 auditado globalmente"
      pattern: "authorize\\('(view|update)'"
    - from: "app/Http/Requests/Portal/EndCompanyLinkRequest.php"
      to: "company_user"
      via: "contagem de responsáveis ativos no after()"
      pattern: "whereNull\\('ended_at'\\)"
    - from: "app/Http/Controllers/Portal/CompanyLinkController.php"
      to: "activity_log"
      via: "AuditService->log('empresas', 'encerramento-vinculo', ...)"
      pattern: "encerramento-vinculo"
---

<objective>
HU-024 (atualizar dados empresariais) e HU-028 (encerrar vínculo): show/update no CompanyController com CNPJ imutável e props completas para a página de detalhe (03-08), e CompanyLinkController::destroy encerrando o próprio vínculo com proteção do último responsável ativo e histórico preservado.

Output: show/update + CompanyLinkController + 2 FormRequests + rotas + 2 suítes.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-cadastro-empresarial/03-CONTEXT.md
@.planning/phases/03-cadastro-empresarial/03-RESEARCH.md
@.planning/phases/03-cadastro-empresarial/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: show + update com CNPJ imutável (HU-024)</name>
  <files>app/Http/Controllers/Portal/CompanyController.php, app/Http/Requests/Portal/UpdateCompanyRequest.php, routes/portal.php, tests/Feature/Companies/CompanyUpdateTest.php</files>
  <read_first>
    - app/Http/Requests/ProfileUpdateRequest.php (precedente CPF imutável por omissão nas regras [01-08])
    - app/Http/Requests/Portal/StoreCompanyRequest.php (rules do 03-04 — update reaproveita o conjunto SEM cnpj)
    - bootstrap/app.php (render callback do 403 — CA-04 sem código novo; log 'seguranca', event 'acesso-negado', result 'bloqueado')
    - .planning/phases/03-cadastro-empresarial/03-04-SUMMARY.md (shape de props e decisão de asserts sem ->component())
    - tests/Feature/Cnae/CnaeCrudTest.php (padrão de assert de attribute_changes em activity de update)
  </read_first>
  <action>
    TDD estrito.

    1. RED — criar tests/Feature/Companies/CompanyUpdateTest.php (setUp RolesAndPermissionsSeeder; helpers portalUser() e companyLinkedTo(User $user, array $linkAttributes = []): Company — factory + CompanyUser). Testes:
       - test_vinculado_ativo_ve_o_detalhe (CA-01): get "/portal/empresas/{id}" → assertOk + assertInertia has('company') com id/legal_name/formatted_cnpj/source.label, has('cnaes'), has('links'), has('abilities') com update true.
       - test_nao_vinculado_nao_ve_detalhe_e_evento_e_auditado (CA-04): outro user → assertForbidden; assertDatabaseHas('activity_log', ['log_name' => 'seguranca', 'event' => 'acesso-negado', 'result' => 'bloqueado']).
       - test_vinculado_ativo_atualiza_dados_complementares (CA-01): put com trade_name/email/street novos → redirect back + sessionHas('status'); assertDatabaseHas('companies', valores novos).
       - test_atualizacao_e_auditada_com_diff (CA-02): activity event 'updated' do subject Company com attribute_changes contendo a chave alterada (espelhar EXATAMENTE o assert de attribute_changes do CnaeCrudTest).
       - test_cnpj_e_imutavel (CA-03): put enviando cnpj diferente → resposta OK; cnpj no banco INALTERADO (valor ignorado por omissão nas regras).
       - test_nao_vinculado_nao_atualiza (CA-04): outro user → 403.
       - test_vinculo_encerrado_nao_atualiza: vínculo com ended_at → put → 403 (policy update exige vínculo ATIVO); show continua acessível (view aceita encerrado).
       Confirmar RED.

    2. GREEN:
       - UpdateCompanyRequest: authorize() true (autorização via policy no controller); MESMAS rules do StoreCompanyRequest EXCETO cnpj (campo simplesmente não entra nas regras — `$request->validated()` nunca o contém; padrão ProfileUpdateRequest); mesma prepareForValidation de phone/zip_code.
       - CompanyController::show(Company $company): `Gate::authorize('view', $company);` carregar `$company->load(['cnaes' => fn ($q) => $q->withPivot('is_primary'), 'links.user']);` usuário efetivo como no index. Props:
         ```php
         'company' => [/* id, legal_name, trade_name, formatted_cnpj, legal_nature_code, legal_nature, size_code, size, street, number, complement, neighborhood, city, state, zip_code, email, phone, source => [value, label], redesim_protocol, redesim_synced_at?->toDateTimeString() */],
         'cnaes' => [
             'primary' => /* ['id', 'formatted_code', 'description'] ?? null (pivot is_primary true) */,
             'secondaries' => /* array dos demais no mesmo shape */,
         ],
         'links' => /* map de $company->links: id, user_name (link->user->name), role_label, started_at/ended_at ->toDateString(), ended_reason, is_current_user (link->user_id === effectiveUser->id) */,
         'abilities' => [
             'update' => $request->user()->can('update', $company),
             'manageCnaes' => $request->user()->can('manageCnaes', $company),
             'endLink' => $request->user()->can('endLink', $company),
         ],
         ```
         Render 'portal/empresas/detalhe' (asserts sem ->component até o 03-08 — decisão registrada no 03-04).
       - CompanyController::update(UpdateCompanyRequest $request, Company $company): `Gate::authorize('update', $company);` `$company->update($request->validated());` → `back()->with('status', 'Dados da empresa atualizados com sucesso.');`
       - routes/portal.php — adicionar APÓS as rotas literais de empresas (comentário de ordem):
         ```php
         Route::get('empresas/{company}', [CompanyController::class, 'show'])->name('empresas.show');
         Route::put('empresas/{company}', [CompanyController::class, 'update'])->name('empresas.update');
         ```
       Rodar filtro até verde.

    3. REFACTOR — pint.
  </action>
  <verify>php artisan test --compact --filter=CompanyUpdateTest</verify>
  <acceptance_criteria>
    - Filtro CompanyUpdateTest verde (7 testes).
    - `grep "cnpj" app/Http/Requests/Portal/UpdateCompanyRequest.php` NÃO encontra cnpj nas rules (imutável por omissão).
    - `grep "authorize('view'" app/Http/Controllers/Portal/CompanyController.php` e `authorize('update'` presentes.
    - `php artisan route:list --name=empresas.show` mostra GET portal/empresas/{company} DEPOIS das literais no arquivo de rotas.
    - Props de show incluem abilities (contrato da tela 03-08).
  </acceptance_criteria>
  <done>Detalhe autorizado com props completas e atualização auditada com CNPJ imutável.</done>
</task>

<task type="auto">
  <name>Task 2: Encerramento de vínculo com proteção do último responsável (HU-028)</name>
  <files>app/Http/Controllers/Portal/CompanyLinkController.php, app/Http/Requests/Portal/EndCompanyLinkRequest.php, routes/portal.php, tests/Feature/Companies/EndCompanyLinkTest.php</files>
  <read_first>
    - .planning/phases/03-cadastro-empresarial/03-RESEARCH.md — "Padrão 2 — Vínculo na criação e encerramento" (regra CA-03 prescrita)
    - app/Http/Requests/Gestao/ToggleUserActivationRequest.php (precedente anti-lockout em FormRequest::after [02-05])
    - app/Support/Audit/AuditService.php (log explícito)
    - app/Enums/CompanyLinkRole.php (Responsavel/Procurador)
  </read_first>
  <action>
    TDD estrito.

    1. RED — criar tests/Feature/Companies/EndCompanyLinkTest.php (mesmos helpers; criar segundo responsável ativo via CompanyUserFactory quando o cenário exigir). Testes:
       - test_usuario_encerra_o_proprio_vinculo_com_motivo (CA-01): empresa com 2 responsáveis ativos; delete "/portal/empresas/{id}/vinculo" com ['ended_reason' => 'Venda da participação'] → redirect route portal.empresas.index + sessionHas('status'); vínculo do user com ended_at NÃO nulo e ended_reason gravado; assertDatabaseCount('company_user', 2) (NUNCA delete físico).
       - test_motivo_e_opcional: delete sem ended_reason → encerra normalmente.
       - test_encerramento_e_auditado (CA-02): activity_log log_name 'empresas', event 'encerramento-vinculo', result 'sucesso'; properties contêm empresa_id e motivo.
       - test_ultimo_responsavel_ativo_e_bloqueado (CA-03): empresa com 1 único responsável ativo → delete → assertSessionHasErrors('vinculo') com mensagem 'A empresa não pode ficar sem responsável ativo.'; ended_at continua null.
       - test_procurador_unico_pode_encerrar_se_ha_responsavel_ativo: empresa com 1 responsavel (outro user) + vínculo procurador do user autenticado → encerra OK (a proteção conta APENAS role responsavel).
       - test_usuario_sem_vinculo_recebe_403 (CA-04): user sem vínculo → assertForbidden + activity 'acesso-negado'.
       - test_vinculo_ja_encerrado_nao_encerra_de_novo: user só com vínculo ended → 403 (policy endLink exige ativo).
       - test_apos_encerrar_empresa_segue_na_listagem_com_situacao_encerrado: encerrar → get /portal/empresas → item presente com link.active false (histórico preservado, HU-028 + HU-027).
       Confirmar RED.

    2. GREEN:
       - EndCompanyLinkRequest: authorize() → `$this->user()->can('endLink', $this->route('company'));` rules: `['ended_reason' => ['nullable', 'string', 'max:255']]`. withValidator/after() (padrão [02-05]):
         ```php
         $company = $this->route('company');
         $effectiveUser = app(CurrentRepresentation::class)->grantor() ?? $this->user();
         $link = $company->activeLinks()->where('user_id', $effectiveUser->id)->first();
         if ($link?->role === CompanyLinkRole::Responsavel
             && ! $company->activeLinks()->where('role', CompanyLinkRole::Responsavel)->whereKeyNot($link->id)->exists()) {
             $validator->errors()->add('vinculo', 'A empresa não pode ficar sem responsável ativo.');
         }
         ```
       - CompanyLinkController::destroy(EndCompanyLinkRequest $request, Company $company): localizar o vínculo ativo do usuário efetivo; `$link->update(['ended_at' => now(), 'ended_reason' => $request->validated('ended_reason')]);` + log explícito `app(AuditService::class)->log('empresas', 'encerramento-vinculo', 'Vínculo com a empresa encerrado pelo usuário', ['empresa_id' => $company->id, 'vinculo_id' => $link->id, 'motivo' => $request->validated('ended_reason')], $company);` → `redirect()->route('portal.empresas.index')->with('status', 'Vínculo com a empresa encerrado.');` (HasAuditoria do CompanyUser ainda gera 'updated' — o evento explícito é o registro de negócio, padrão [02-05]).
       - routes/portal.php: `Route::delete('empresas/{company}/vinculo', [CompanyLinkController::class, 'destroy'])->name('empresas.vinculo.destroy');`
       Rodar filtro até verde.

    3. REFACTOR — pint.
  </action>
  <verify>php artisan test --compact --filter=EndCompanyLinkTest</verify>
  <acceptance_criteria>
    - Filtro EndCompanyLinkTest verde (8 testes).
    - `grep "A empresa não pode ficar sem responsável ativo." app/Http/Requests/Portal/EndCompanyLinkRequest.php` presente.
    - `grep "delete()" app/Http/Controllers/Portal/CompanyLinkController.php` NÃO encontra delete físico do vínculo.
    - `grep "encerramento-vinculo" app/Http/Controllers/Portal/CompanyLinkController.php` presente.
    - `php artisan route:list --name=empresas.vinculo.destroy` mostra DELETE portal/empresas/{company}/vinculo.
  </acceptance_criteria>
  <done>Encerramento auditado com histórico preservado e empresa nunca órfã de responsável.</done>
</task>

</tasks>

<verification>
- `php artisan test --compact tests/Feature/Companies` verde (ESCOPADO; full-suite só no 03-09).
- `vendor/bin/pint --dirty --format agent` sem pendências.
- Ordem das rotas: literais (cadastrar, consultar-cnpj) ANTES de empresas/{company} em routes/portal.php.
</verification>

<success_criteria>
- HU-024 CA-01..04 e HU-028 CA-01..04 cobertos por testes pt-BR.
- CNPJ imutável por omissão; encerramento sem delete físico; proteção do último responsável na aplicação (FormRequest::after, precedente [02-05]).
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/03-cadastro-empresarial/03-05-SUMMARY.md` registrando: shape final das props de show (contrato 03-08), regra do último responsável e nome do evento de auditoria do encerramento.
</output>

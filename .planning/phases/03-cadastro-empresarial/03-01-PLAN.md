---
phase: 03-cadastro-empresarial
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Rules/ValidCnpj.php
  - tests/Unit/Rules/ValidCnpjTest.php
  - database/migrations/*_create_companies_table.php
  - database/migrations/*_create_company_user_table.php
  - database/migrations/*_create_company_cnae_table.php
  - app/Models/Company.php
  - app/Models/CompanyUser.php
  - app/Models/Cnae.php
  - app/Enums/CompanySource.php
  - app/Enums/CompanyLinkRole.php
  - database/factories/CompanyFactory.php
  - database/factories/CompanyUserFactory.php
  - tests/Feature/Companies/CompanyFoundationTest.php
  - config/sile.php
  - database/seeders/ParameterSeeder.php
  - tests/Feature/Seeders/ParameterSeederTest.php
  - app/Http/Controllers/Gestao/CnaeController.php
  - tests/Feature/Cnae/CnaeCrudTest.php
  - app/Http/Middleware/HandleInertiaRequests.php
  - resources/js/layouts/gestao-layout.tsx
  - resources/js/types/index.d.ts
autonomous: true
requirements: [HU-021, HU-023]

must_haves:
  truths:
    - "ValidCnpj aceita 00000000000191 e 12ABC34501DE35 (alfanumérico oficial RFB) e rejeita DV errado, tamanho errado e repetição uniforme"
    - "Tabelas companies/company_user/company_cnae existem com cnpj string(14) unique, restrictOnDelete em company_cnae.cnae_id e unique(company_id, cnae_id)"
    - "Os 3 parâmetros novos estão no catálogo e Settings::enabled('cnpj_lookup') retorna true por padrão"
    - "Excluir CNAE vinculado a empresa é bloqueado com mensagem pt-BR visível na gestão"
  artifacts:
    - path: "app/Rules/ValidCnpj.php"
      provides: "Validação módulo 11 ASCII-48 (numérico + alfanumérico)"
    - path: "app/Models/Company.php"
      provides: "Model com HasAuditoria, relações cnaes/primaryCnae/links/activeLinks e accessor formatted_cnpj"
    - path: "app/Models/CompanyUser.php"
      provides: "Vínculo usuário-empresa com ciclo de vida (started_at/ended_at)"
    - path: "database/seeders/ParameterSeeder.php"
      provides: "Catálogo com features.cnpj_lookup, integrations.cnpj_lookup.base_url e ui.companies.per_page"
  key_links:
    - from: "database/seeders/ParameterSeeder.php"
      to: "config/sile.php"
      via: "mesmos defaults nas chaves novas (fallback do Settings)"
      pattern: "cnpj_lookup"
    - from: "database/migrations/*_create_company_cnae_table.php"
      to: "cnaes.id"
      via: "constrained()->restrictOnDelete()"
      pattern: "restrictOnDelete"
    - from: "app/Http/Controllers/Gestao/CnaeController.php"
      to: "company_cnae"
      via: "verificação companies()->exists() antes do delete"
      pattern: "companies\\(\\)->exists"
---

<objective>
Fundação da Fase 3: Rule ValidCnpj compatível com o CNPJ alfanumérico (produção RFB a partir de julho/2026), schema completo do cadastro empresarial (companies, company_user, company_cnae) com models/enums/factories, parâmetros novos no registry HU-014 e resolução da pendência herdada da Fase 2 (bloqueio de exclusão de CNAE vinculado a empresas).

Propósito: todos os planos seguintes (lookup, import REDESIM, CRUD, CNAEs, telas) dependem deste alicerce.
Output: rule + 3 migrations + 2 models + 2 enums + 2 factories + catálogo de parâmetros atualizado + CnaeController::destroy protegido.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cadastro-empresarial/03-CONTEXT.md
@.planning/phases/03-cadastro-empresarial/03-RESEARCH.md
@.planning/phases/03-cadastro-empresarial/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rule ValidCnpj (módulo 11 ASCII-48, numérico e alfanumérico) com TDD</name>
  <files>app/Rules/ValidCnpj.php, tests/Unit/Rules/ValidCnpjTest.php</files>
  <read_first>
    - app/Rules/ValidCpf.php (padrão de estrutura e mensagem da rule)
    - tests/Unit/Rules/ValidCpfTest.php (padrão de teste unit de rule)
    - .planning/phases/03-cadastro-empresarial/03-RESEARCH.md — seção "Code Examples > ValidCnpj" e Pitfall 1
  </read_first>
  <action>
    TDD estrito (RED → GREEN → REFACTOR):

    1. RED — criar tests/Unit/Rules/ValidCnpjTest.php (use `php artisan make:test --phpunit --unit Rules/ValidCnpjTest --no-interaction`) com helper privado que roda `Validator::make(['cnpj' => $value], ['cnpj' => [new ValidCnpj]])->passes()`. Testes nomeados em pt-BR:
       - test_cnpj_numerico_valido_e_aceito: '00000000000191' e '33000167000101' passam.
       - test_cnpj_alfanumerico_valido_e_aceito: '12ABC34501DE35' passa (exemplo oficial RFB) e com máscara '12.ABC.345/01DE-35' também passa.
       - test_cnpj_com_mascara_e_normalizado: '00.000.000/0001-91' passa.
       - test_cnpj_com_dv_errado_e_rejeitado: '00000000000192' e '12ABC34501DE36' falham.
       - test_cnpj_com_tamanho_errado_e_rejeitado: '0000000000019' (13) e '000000000001911' (15) falham.
       - test_cnpj_com_repeticao_uniforme_e_rejeitado: '11111111111111' falha.
       - test_cnpj_com_letra_nos_digitos_verificadores_e_rejeitado: '12ABC34501DEAA' falha (DVs são SEMPRE numéricos).
       Rodar `php artisan test --compact --filter=ValidCnpjTest` e confirmar que falha porque a classe não existe.

    2. GREEN — criar app/Rules/ValidCnpj.php implementando ValidationRule (mesma assinatura/docblock do ValidCpf). Algoritmo oficial (IN RFB 2.229/2024, módulo 11 sobre ord(char) - 48):
       - Normalizar: `$cnpj = preg_replace('/[^A-Z0-9]/', '', strtoupper((string) $value));`
       - Formato: `preg_match('/^[A-Z\d]{12}\d{2}$/', $cnpj) !== 1` OU `preg_match('/^(.)\1{13}$/', $cnpj)` → `$fail('O campo :attribute não é um CNPJ válido.')` e return.
       - DVs: pesos de 13 posições `$weights = [6, 5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2];`. Para cada `$position` em `[12, 13]`: `$slice = array_slice($weights, 13 - $position);` (12 pesos começando em 5 para o 1º DV; 13 pesos começando em 6 para o 2º); somar `(ord($cnpj[$i]) - 48) * $slice[$i]` para `$i < $position`; `$digit = ($sum % 11) < 2 ? 0 : 11 - ($sum % 11);` e comparar com `(int) $cnpj[$position]`.
       ATENÇÃO: NÃO copiar literalmente o snippet do RESEARCH — o array de 12 pesos com `array_slice($weights, 12 - $position)` produz slice errado para o 2º DV; usar o array de 13 pesos com offset `13 - $position` descrito acima (os vetores de teste do passo 1 provam a forma correta).
       Rodar o filtro e confirmar verde.

    3. REFACTOR — `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>php artisan test --compact --filter=ValidCnpjTest</verify>
  <acceptance_criteria>
    - `php artisan test --compact --filter=ValidCnpjTest` verde com os 7 testes acima.
    - `grep -c "ord(" app/Rules/ValidCnpj.php` >= 1 (algoritmo ASCII-48, não regex numérica).
    - `grep "12ABC34501DE35" tests/Unit/Rules/ValidCnpjTest.php` encontra o vetor alfanumérico oficial.
  </acceptance_criteria>
  <done>Rule aceita os formatos numérico e alfanumérico com DVs corretos e rejeita inválidos com mensagem pt-BR.</done>
</task>

<task type="auto">
  <name>Task 2: Schema companies/company_user/company_cnae + models + enums + factories</name>
  <files>database/migrations/*_create_companies_table.php, database/migrations/*_create_company_user_table.php, database/migrations/*_create_company_cnae_table.php, app/Models/Company.php, app/Models/CompanyUser.php, app/Models/Cnae.php, app/Enums/CompanySource.php, app/Enums/CompanyLinkRole.php, database/factories/CompanyFactory.php, database/factories/CompanyUserFactory.php, tests/Feature/Companies/CompanyFoundationTest.php</files>
  <read_first>
    - .planning/phases/03-cadastro-empresarial/03-RESEARCH.md — seção "Decisão 3 — Schema" (tabelas coluna a coluna)
    - app/Models/Cnae.php (padrão #[Fillable], casts(), accessor formattedCode, HasAuditoria)
    - database/migrations/2026_06_10_023838_create_procurations_table.php (precedente: unicidade condicional NA APLICAÇÃO, índice simples no par)
    - database/migrations/2026_06_10_144536_create_cnaes_table.php (estilo de migration do projeto)
    - database/factories/UserFactory.php (padrão de factory com gerador de documento válido e states afterCreating)
  </read_first>
  <action>
    TDD: escrever tests/Feature/Companies/CompanyFoundationTest.php PRIMEIRO (`php artisan make:test --phpunit Companies/CompanyFoundationTest --no-interaction`), verificar RED, então criar schema/models até verde.

    1. RED — testes (use RefreshDatabase; CNAEs via Cnae::factory):
       - test_factory_cria_empresa_com_cnpj_valido: Company::factory()->create(); validar o cnpj gerado com a ValidCnpj via Validator (passes true); asserts source 'manual', legal_name não vazio.
       - test_cnpj_duplicado_viola_unique_no_banco: criar empresa com cnpj fixo; expectException(QueryException::class); criar segunda com o mesmo cnpj.
       - test_vinculo_e_cnaes_relacionam: company + CompanyUser::factory (role responsavel) + attach de 2 cnaes (um is_primary true) → asserts $company->links()->count() === 1, $company->cnaes()->count() === 2, $company->primaryCnae()->first()->id correto, $company->activeLinks()->count() === 1 e formatted_cnpj no padrão `00.000.000/0001-91` para cnpj '00000000000191'.
       - test_banco_impede_excluir_cnae_vinculado: attach cnae à empresa; expectException(QueryException::class); $cnae->delete() (defesa restrictOnDelete).
       - test_state_from_redesim_marca_origem: Company::factory()->fromRedesim()->create() → source 'redesim', redesim_protocol e redesim_synced_at preenchidos.
       - test_state_ended_encerra_vinculo: CompanyUserFactory ended() → ended_at não nulo.

    2. GREEN — criar na ordem:
       a) Enums (`php artisan make:enum Enums/CompanySource --string --no-interaction` e CompanyLinkRole):
          - CompanySource: `case Manual = 'manual'; case Redesim = 'redesim';` + `public function label(): string` → 'Cadastro manual' / 'REDESIM'.
          - CompanyLinkRole: `case Responsavel = 'responsavel'; case Procurador = 'procurador';` + label() → 'Responsável' / 'Procurador'.
       b) Migrations (`php artisan make:migration create_companies_table --no-interaction` etc.), EXATAMENTE o schema do RESEARCH:
          - companies: id; `$table->string('cnpj', 14)->unique();` (string SEMPRE — alfanumérico em julho/2026); legal_name string; trade_name/legal_nature/size strings nullable; legal_nature_code string(4) nullable; size_code string(2) nullable; street/number/complement/neighborhood/city nullable; state string(2) nullable; zip_code string(8) nullable; email/phone nullable; `$table->string('source')->default('manual');`; redesim_protocol string nullable; redesim_synced_at timestamp nullable; timestamps.
          - company_user: id; foreignId company_id constrained()->cascadeOnDelete(); foreignId user_id constrained()->cascadeOnDelete(); role string; started_at timestamp; ended_at timestamp nullable; ended_reason string nullable; timestamps; `$table->index(['company_id', 'user_id']);` + comentário no estilo da migration de procurations: unicidade "um vínculo ativo por par" fica NA APLICAÇÃO (portabilidade SQLite/Postgres).
          - company_cnae: id; foreignId company_id constrained()->cascadeOnDelete(); `$table->foreignId('cnae_id')->constrained()->restrictOnDelete();` (banco impede excluir CNAE vinculado); boolean is_primary default false (+ comentário: invariante "exatamente um principal" garantida NA APLICAÇÃO pelo CompanyCnaeService — precedente [01-07]); timestamps; `$table->unique(['company_id', 'cnae_id']);`.
       c) Models:
          - Company: `#[Fillable(['cnpj', 'legal_name', 'trade_name', 'legal_nature_code', 'legal_nature', 'size_code', 'size', 'street', 'number', 'complement', 'neighborhood', 'city', 'state', 'zip_code', 'email', 'phone', 'source', 'redesim_protocol', 'redesim_synced_at'])]`; traits HasAuditoria + HasFactory; casts(): `['source' => CompanySource::class, 'redesim_synced_at' => 'datetime']`; relações: `cnaes(): BelongsToMany` → `belongsToMany(Cnae::class)->withPivot('is_primary')->withTimestamps()`; `primaryCnae(): BelongsToMany` → `$this->cnaes()->wherePivot('is_primary', true)`; `links(): HasMany` → hasMany(CompanyUser::class); `activeLinks(): HasMany` → `$this->links()->whereNull('ended_at')`; accessor `formattedCnpj` no padrão do formattedCode do Cnae: `preg_replace('/^(.{2})(.{3})(.{3})(.{4})(.{2})$/', '$1.$2.$3/$4-$5', $this->cnpj)` (posicional — funciona com alfanumérico).
          - CompanyUser: `protected $table = 'company_user';`; `#[Fillable(['company_id', 'user_id', 'role', 'started_at', 'ended_at', 'ended_reason'])]`; HasAuditoria + HasFactory; casts(): `['role' => CompanyLinkRole::class, 'started_at' => 'datetime', 'ended_at' => 'datetime']`; belongsTo company() e user().
          - Cnae: adicionar relação `companies(): BelongsToMany` → `belongsToMany(Company::class)->withPivot('is_primary')->withTimestamps()` (necessária para a Task 3).
       d) Factories:
          - CompanyFactory: cnpj gerado VÁLIDO (12 dígitos aleatórios + 2 DVs calculados pelo mesmo algoritmo de pesos da ValidCnpj — método privado generateCnpj(), padrão generateCpf da UserFactory); legal_name `fake()->company()`; trade_name nullable; legal_nature_code '2062'; legal_nature 'Sociedade Empresária Limitada'; size_code '01'; size 'MICRO EMPRESA'; endereço fake com city 'Salvador', state 'BA', zip_code `fake()->numerify('########')`; email/phone fake; source CompanySource::Manual. States: `fromRedesim()` (source Redesim, redesim_protocol `'BAP'.fake()->numerify('##########')`, redesim_synced_at now()); `withPrimaryCnae(?Cnae $cnae = null)` via afterCreating (attach `[$cnae->id => ['is_primary' => true]]`, criando Cnae::factory se null).
          - CompanyUserFactory: company_id/user_id por factory; role CompanyLinkRole::Responsavel; started_at now(); states `responsavel()`, `procurador()`, `ended()` (ended_at now(), ended_reason 'Encerrado em teste').
       Rodar `php artisan test --compact --filter=CompanyFoundationTest` até verde.

    3. REFACTOR — pint.
  </action>
  <verify>php artisan test --compact --filter=CompanyFoundationTest && php artisan migrate:status</verify>
  <acceptance_criteria>
    - `php artisan test --compact --filter=CompanyFoundationTest` verde (6 testes).
    - `grep "restrictOnDelete" database/migrations/*_create_company_cnae_table.php` encontra a constraint.
    - `grep "unique(\['company_id', 'cnae_id'\])" database/migrations/*_create_company_cnae_table.php` encontra o índice único.
    - `grep "string('cnpj', 14)" database/migrations/*_create_companies_table.php` confirma string(14).
    - `php artisan tinker --execute 'echo App\Models\Company::factory()->make()->cnpj;'` imprime 14 caracteres.
  </acceptance_criteria>
  <done>Schema migrável, models com auditoria e relações, factories gerando CNPJs válidos — base pronta para os planos 03-02 a 03-06.</done>
</task>

<task type="auto">
  <name>Task 3: Parâmetros novos (HU-014) + bloqueio de exclusão de CNAE vinculado + flash.error</name>
  <files>config/sile.php, database/seeders/ParameterSeeder.php, tests/Feature/Seeders/ParameterSeederTest.php, app/Http/Controllers/Gestao/CnaeController.php, tests/Feature/Cnae/CnaeCrudTest.php, app/Http/Middleware/HandleInertiaRequests.php, resources/js/layouts/gestao-layout.tsx, resources/js/types/index.d.ts</files>
  <read_first>
    - database/seeders/ParameterSeeder.php (formato do catálogo; upsert só de metadados)
    - tests/Feature/Seeders/ParameterSeederTest.php (asserções existentes — contagem total deverá ser atualizada)
    - app/Http/Controllers/Gestao/CnaeController.php (destroy atual com comentário da pendência da Fase 2)
    - tests/Feature/Cnae/CnaeCrudTest.php (padrão dos testes de CRUD; teste atual de exclusão auditada)
    - app/Http/Middleware/HandleInertiaRequests.php + resources/js/types/index.d.ts (shape atual de flash em SharedProps)
    - resources/js/layouts/gestao-layout.tsx (exibição atual de flash.status) — ATENÇÃO: arquivo era território da fase 2.4; o STATE.md registra a 2.4 como CONCLUÍDA em 2026-06-11. Confirmar com `git status`/`git log -1 -- resources/js/layouts/gestao-layout.tsx` que não há sessão concorrente editando; fazer apenas a adição mínima descrita.
  </read_first>
  <action>
    TDD: testes primeiro (RED), depois implementação.

    1. RED — atualizar tests/Feature/Seeders/ParameterSeederTest.php: ajustar a contagem total do catálogo de 11 para 14 e adicionar asserts (padrão dos existentes) para:
       - `features.cnpj_lookup`: group 'features', type 'boolean', default_value '1', validation_rules ['required', 'boolean'].
       - `integrations.cnpj_lookup.base_url`: group 'integracoes', type 'string', default_value 'https://brasilapi.com.br/api/cnpj/v1', validation_rules ['required', 'url'], requires_connection_test true (contrato RN-010 — botão real de teste é a Fase 13).
       - `ui.companies.per_page`: group 'ui', type 'integer', default_value '15', validation_rules ['required', 'integer', 'min:5', 'max:100'].
       E em tests/Feature/Cnae/CnaeCrudTest.php adicionar:
       - test_exclusao_de_cnae_vinculado_a_empresa_e_bloqueada: admin autenticado; `$company = Company::factory()->create(); $cnae = Cnae::factory()->create(); $company->cnaes()->attach($cnae->id, ['is_primary' => true]);` delete "/gestao/cnaes/{$cnae->id}" → assertRedirect + `assertSessionHas('error')` com mensagem exata 'CNAE vinculado a empresas não pode ser excluído.' + assertDatabaseHas('cnaes', ['id' => $cnae->id]) + NENHUMA activity 'deleted' para o subject.
       Rodar `php artisan test --compact --filter='ParameterSeederTest|CnaeCrudTest'` e confirmar RED pelos motivos certos.

    2. GREEN:
       a) config/sile.php — adicionar (constantes técnicas timeout/retries/cache_ttl ficam SÓ aqui, NUNCA no registry — precedente [02-02]):
          ```php
          'integrations' => [
              'cnpj_lookup' => [
                  'base_url' => 'https://brasilapi.com.br/api/cnpj/v1',
                  'timeout' => 8,
                  'retries' => 2,
                  'cache_ttl' => 86400,
              ],
          ],
          ```
          e dentro de 'features': `'cnpj_lookup' => true,`; dentro de 'ui': `'companies' => ['per_page' => 15],`.
       b) ParameterSeeder — adicionar as 3 entradas do passo 1 ao catálogo (aditivo; `requires_connection_test => true` apenas na base_url). NÃO tocar nas chaves existentes.
       c) CnaeController::destroy — substituir o comentário da pendência por verificação amigável ANTES do delete:
          ```php
          if ($cnae->companies()->exists()) {
              return back()->with('error', 'CNAE vinculado a empresas não pode ser excluído.');
          }
          ```
       d) Feedback visível (entrega funcional — bloqueio nunca silencioso):
          - HandleInertiaRequests::share — em 'flash', adicionar `'error' => $request->session()->get('error'),`.
          - resources/js/types/index.d.ts — no tipo do flash de SharedProps, adicionar `error?: string | null` (seguir shape existente).
          - gestao-layout.tsx — abaixo do bloco de flash.status, renderizar `{flash.error && (<div className="mb-6"><Alert variant="error" title="Ação bloqueada" message={flash.error} /></div>)}` (Alert já suporta variant error — conferir API no componente).

    3. Verificar GREEN com o filtro composto + `npm run typecheck` + pint.
  </action>
  <verify>php artisan test --compact --filter='ParameterSeederTest|CnaeCrudTest' && npm run typecheck</verify>
  <acceptance_criteria>
    - Filtro 'ParameterSeederTest|CnaeCrudTest' verde, incluindo o teste novo de bloqueio.
    - `php artisan db:seed --class=ParameterSeeder --no-interaction` roda sem erro e `php artisan tinker --execute 'echo App\Models\Parameter::count();'` retorna 14.
    - `grep "requires_connection_test" database/seeders/ParameterSeeder.php` encontra a flag na base_url.
    - `grep "flash.error" resources/js/layouts/gestao-layout.tsx` encontra a exibição.
    - `npm run typecheck` sem erros.
  </acceptance_criteria>
  <done>Parâmetros administráveis registrados com defaults espelhados em config/sile.php, pendência herdada resolvida com bloqueio comunicado e defesa em banco.</done>
</task>

</tasks>

<verification>
- `php artisan test --compact tests/Unit/Rules tests/Feature/Companies tests/Feature/Seeders tests/Feature/Cnae` — grupos da fundação verdes (verificação ESCOPADA; full-suite só no fechamento 03-09).
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `npm run typecheck` sem erros.
</verification>

<success_criteria>
- ValidCnpj cobre numérico + alfanumérico com vetores oficiais RFB testados.
- 3 migrations aplicáveis com constraints exatas do RESEARCH (unique cnpj, restrictOnDelete, unique company+cnae).
- Catálogo de parâmetros com 14 chaves; defaults espelhados em config/sile.php.
- CnaeController::destroy bloqueia exclusão de CNAE vinculado com mensagem pt-BR visível.
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/03-cadastro-empresarial/03-01-SUMMARY.md` (template summary.md) registrando: decisões tomadas, assinatura final da ValidCnpj, nomes exatos das migrations e contagem do catálogo de parâmetros.
</output>

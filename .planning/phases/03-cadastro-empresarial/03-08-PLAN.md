---
phase: 03-cadastro-empresarial
plan: 08
type: execute
wave: 6
depends_on: ["03-05", "03-06"]
files_modified:
  - resources/js/pages/portal/empresas/detalhe.tsx
  - resources/js/pages/portal/empresas/cnae-picker.tsx
autonomous: true
requirements: [HU-024, HU-025, HU-026, HU-028]

must_haves:
  truths:
    - "/portal/empresas/{id} mostra dados da empresa (CNPJ readonly), CNAEs e vínculos em seções claras"
    - "Usuário com vínculo ativo edita dados complementares e salva; erros de validação aparecem nos campos"
    - "Troca de CNAE principal pede confirmação (ConfirmDialog warning) e usa a busca server-side real"
    - "Secundários são editados como conjunto e salvos de uma vez; mensagens de bloqueio (principal entre secundários, inativo) aparecem"
    - "'Encerrar meu vínculo' usa ConfirmDialog danger com motivo opcional; bloqueio do último responsável aparece em alerta"
  artifacts:
    - path: "resources/js/pages/portal/empresas/detalhe.tsx"
      provides: "Página de detalhe com seções Dados / CNAEs / Vínculos"
    - path: "resources/js/pages/portal/empresas/cnae-picker.tsx"
      provides: "Busca incremental de CNAEs ativos (GET /portal/cnaes) local às páginas de empresas"
  key_links:
    - from: "resources/js/pages/portal/empresas/detalhe.tsx"
      to: "/portal/empresas/{id} (update)"
      via: "useForm put"
      pattern: "useForm"
    - from: "resources/js/pages/portal/empresas/cnae-picker.tsx"
      to: "/portal/cnaes"
      via: "busca incremental (useHttp get) com debounce"
      pattern: "/portal/cnaes"
    - from: "resources/js/pages/portal/empresas/detalhe.tsx"
      to: "/portal/empresas/{id}/vinculo"
      via: "router.delete com ended_reason no ConfirmDialog"
      pattern: "vinculo"
---

<objective>
Página de detalhe da empresa (HU-024/025/026/028 na UI): edição de dados com CNPJ imutável visível, gestão de CNAE principal/secundários com busca real e confirmações, e encerramento do próprio vínculo com motivo — tudo nos padrões 2.1/2.2 (+2.4 quando aplicável) e com erros do backend comunicados.

Output: detalhe.tsx + cnae-picker.tsx, typecheck/build verdes.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-cadastro-empresarial/03-CONTEXT.md
@.planning/phases/03-cadastro-empresarial/03-RESEARCH.md
@.planning/phases/03-cadastro-empresarial/03-05-SUMMARY.md
@.planning/phases/03-cadastro-empresarial/03-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detalhe — seção Dados com CNPJ imutável (HU-024)</name>
  <files>resources/js/pages/portal/empresas/detalhe.tsx</files>
  <read_first>
    - COORDENAÇÃO 2.4 (obrigatório ANTES de codar): conferir components/ui/{card.tsx,table.tsx,badge.tsx,confirm-dialog.tsx,alert.tsx} e app/page-header.tsx no disco; usar o que existir; NUNCA editar components/ui/* nem páginas da gestão; se faltar algo, padrões 2.1/2.2 sem bloquear.
    - .planning/phases/03-cadastro-empresarial/03-05-SUMMARY.md (shape EXATO das props de show: company, cnaes{primary, secondaries}, links, abilities)
    - resources/js/pages/portal/empresas/cadastrar.tsx SE JÁ EXISTIR no disco (03-07 roda em paralelo nesta wave — NÃO editar aquele arquivo; apenas alinhar visual). Se não existir, seguir gestao/cnaes/index.tsx como referência de forms.
    - resources/js/components/form/{input.tsx,label.tsx} (error/hint) e ui/{alert.tsx,badge.tsx,button.tsx}
    - .cursor/skills/inertia-react-development/SKILL.md (useForm put, usePage errors)
  </read_first>
  <action>
    Criar resources/js/pages/portal/empresas/detalhe.tsx com PortalLayout:

    1. Tipos locais espelhando as props de show (CompanyDetail, CnaeOption {id, formatted_code, description}, LinkRow, Abilities) — NÃO tocar types/index.d.ts.
    2. PageHeader: título = company.legal_name, trilha [Meu painel, Minhas empresas]; ao lado do título, Badges de origem (source.label — light para manual, info para REDESIM; se redesim_synced_at presente, title com a data) e de situação do vínculo do usuário.
    3. Card 'Dados da empresa':
       - useForm inicializado com os valores atuais (legal_name, trade_name, legal_nature_code, legal_nature, size_code, size, street, number, complement, neighborhood, city, state, zip_code, email, phone).
       - CNPJ exibido em Input `disabled` com value company.formatted_cnpj e hint 'O CNPJ não pode ser alterado.' (imutável — padrão CPF [01-08]); CAMPO NÃO ENTRA no useForm (backend já ignora).
       - Grid responsivo (sm:grid-cols-2) com Inputs error/hint por campo; submit `form.put('/portal/empresas/' + company.id, { preserveScroll: true })`; botão 'Salvar alterações' disabled={processing || !abilities.update}.
       - Se !abilities.update (vínculo encerrado): inputs disabled + Alert variant warning 'Seu vínculo com esta empresa está encerrado — os dados são somente leitura.'
    4. Verificar: typecheck + build + regressão `php artisan test --compact --filter=CompanyUpdateTest`.
  </action>
  <verify>npm run typecheck && npm run build && php artisan test --compact --filter=CompanyUpdateTest</verify>
  <acceptance_criteria>
    - typecheck/build verdes; CompanyUpdateTest verde.
    - `grep "formatted_cnpj" resources/js/pages/portal/empresas/detalhe.tsx` presente em Input disabled.
    - `grep "O CNPJ não pode ser alterado." resources/js/pages/portal/empresas/detalhe.tsx` presente.
    - `grep "abilities" resources/js/pages/portal/empresas/detalhe.tsx` controla edição.
  </acceptance_criteria>
  <done>Seção de dados editável e segura, com imutabilidade do CNPJ comunicada na interface.</done>
</task>

<task type="auto">
  <name>Task 2: Detalhe — CNAEs com picker real + Vínculos com encerramento (HU-025/026/028)</name>
  <files>resources/js/pages/portal/empresas/detalhe.tsx, resources/js/pages/portal/empresas/cnae-picker.tsx</files>
  <read_first>
    - .planning/phases/03-cadastro-empresarial/03-06-SUMMARY.md (contratos: PUT cnae-principal {cnae_id}, PUT cnaes-secundarios {cnaes: number[]}, GET /portal/cnaes?search= → [{id, formatted_code, description}])
    - resources/js/components/ui/confirm-dialog.tsx (API: description aceita ReactNode — o campo de motivo entra aí; variant danger/warning; processing)
    - resources/js/components/ui/data-table/table-toolbar.tsx (padrão de input de busca com ícone — referência visual do picker)
    - resources/js/components/ui/table.tsx (tabela de vínculos)
  </read_first>
  <action>
    1. Criar resources/js/pages/portal/empresas/cnae-picker.tsx — componente LOCAL às páginas de empresas (não vai para components/ui — regra de coordenação 2.4):
       - Props: `{ onSelect: (cnae: CnaeOption) => void; excludeIds?: number[]; placeholder?: string }`.
       - Input de busca com debounce ~350ms; a cada termo (>= 2 chars) GET '/portal/cnaes?search=' via request standalone (useHttp get — conferir API na skill; fallback fetch com header X-Requested-With e credenciais same-origin se a skill indicar limitação); lista de resultados (formatted_code + description) filtrando excludeIds; clique → onSelect + limpa; estados: buscando (Skeleton/spinner), vazio ('Nenhum CNAE ativo encontrado'), erro (Alert inline).
       - SOMENTE dados do endpoint real — nunca lista hardcoded.
    2. Em detalhe.tsx, Card 'Atividades econômicas (CNAEs)' (renderizado com abilities.manageCnaes; senão, somente leitura):
       - Principal: bloco com formatted_code + description atual (ou EmptyState curto 'Nenhum CNAE principal definido'); botão 'Definir/Alterar principal' abre o CnaePicker; selecionar → ConfirmDialog variant warning título 'Alterar CNAE principal' description 'A troca do CNAE principal impacta o enquadramento da atividade nas próximas solicitações. Confirmar a alteração para {code} — {descrição}?' → onConfirm: `router.put('/portal/empresas/{id}/cnae-principal', { cnae_id }, { preserveScroll: true })` com processing.
       - Secundários: lista de chips/linhas (formatted_code + description + botão remover) sobre estado local inicializado com cnaes.secondaries; CnaePicker (excludeIds = principal + já selecionados) adiciona; botão 'Salvar secundários' → `router.put('/portal/empresas/{id}/cnaes-secundarios', { cnaes: ids }, { preserveScroll: true })`; badge de alterações não salvas quando o conjunto difere do inicial.
       - Erros: `const { errors } = usePage().props` — exibir errors.cnae_id e errors.cnaes em Alert variant error dentro do card (mensagens do backend: inativo, duplicado, principal entre secundários).
    3. Card 'Vínculos':
       - Table com colunas Usuário / Papel / Início / Fim / Motivo (dados de links; linha do próprio usuário com destaque is_current_user).
       - Botão 'Encerrar meu vínculo' (variant danger/outline, visível se abilities.endLink) → ConfirmDialog variant danger título 'Encerrar vínculo com a empresa' description = ReactNode com aviso ('Esta ação não exclui o histórico — o vínculo ficará registrado como encerrado.') + `<Input>` controlado para motivo opcional (label 'Motivo (opcional)') → onConfirm: `router.delete('/portal/empresas/{id}/vinculo', { data: { ended_reason }, preserveScroll: true })`.
       - errors.vinculo (último responsável) → Alert variant error no topo do card com a mensagem do backend.
    4. Verificar: typecheck + build + regressão escopada dos grupos da página.
  </action>
  <verify>npm run typecheck && npm run build && php artisan test --compact --filter='PrimaryCnaeTest|SecondaryCnaesTest|EndCompanyLinkTest'</verify>
  <acceptance_criteria>
    - typecheck/build verdes; filtro composto verde.
    - `grep "/portal/cnaes" resources/js/pages/portal/empresas/cnae-picker.tsx` presente (busca real).
    - `grep "ConfirmDialog" resources/js/pages/portal/empresas/detalhe.tsx` aparece para troca de principal (warning) E encerramento (danger).
    - `grep "ended_reason" resources/js/pages/portal/empresas/detalhe.tsx` presente no delete.
    - `grep "errors.vinculo\|errors\['vinculo'\]" resources/js/pages/portal/empresas/detalhe.tsx` presente (bloqueio comunicado).
    - cnae-picker.tsx está em pages/portal/empresas/ (NÃO em components/ui).
  </acceptance_criteria>
  <done>Detalhe completo: CNAEs administráveis com confirmação e busca real, vínculos com encerramento seguro e histórico visível.</done>
</task>

</tasks>

<verification>
- `npm run typecheck && npm run build` verdes.
- `php artisan test --compact --filter='CompanyUpdateTest|PrimaryCnaeTest|SecondaryCnaesTest|EndCompanyLinkTest'` verde (regressão ESCOPADA da wave — full-suite só no 03-09).
- `git status` não mostra mudanças em components/ui/* nem em pages/gestao/* nem em arquivos do 03-07 (index.tsx/cadastrar.tsx/portal-layout.tsx).
</verification>

<success_criteria>
- HU-024/025/026/028 navegáveis de ponta a ponta no detalhe, executando os endpoints reais com confirmações e mensagens pt-BR.
- Nenhum componente duplicado; nenhum arquivo do território 2.4 ou do plano paralelo 03-07 tocado.
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/03-cadastro-empresarial/03-08-SUMMARY.md` registrando: API usada nos requests standalone do picker, decisões de UX (chips de secundários, motivo no ConfirmDialog) e quaisquer fallbacks 2.1/2.2 aplicados.
</output>

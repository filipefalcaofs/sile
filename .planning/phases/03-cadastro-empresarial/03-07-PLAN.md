---
phase: 03-cadastro-empresarial
plan: 07
type: execute
wave: 6
depends_on: ["03-02", "03-04"]
files_modified:
  - resources/js/layouts/portal-layout.tsx
  - resources/js/pages/portal/empresas/index.tsx
  - resources/js/pages/portal/empresas/cadastrar.tsx
autonomous: true
requirements: [HU-021, HU-023, HU-027]

must_haves:
  truths:
    - "Sidebar do portal tem 'Minhas empresas' no grupo Serviços"
    - "/portal/empresas renderiza a listagem no padrão 2.4 (PageHeader → Card → TableToolbar → DataTable → Pagination) com badges de origem e situação do vínculo"
    - "EmptyState distingue lista vazia (CTA 'Cadastrar empresa') de busca sem resultados"
    - "/portal/empresas/cadastrar é página dedicada com botão 'Buscar CNPJ' que consulta o endpoint real e preenche o formulário; toggle OFF desabilita o botão com aviso"
    - "Falha do lookup mostra alerta pt-BR e o preenchimento manual continua possível (FA-03)"
  artifacts:
    - path: "resources/js/pages/portal/empresas/index.tsx"
      provides: "Listagem Minhas empresas server-driven"
    - path: "resources/js/pages/portal/empresas/cadastrar.tsx"
      provides: "Página dedicada de cadastro com lookup vivo"
    - path: "resources/js/layouts/portal-layout.tsx"
      provides: "Item de navegação no grupo Serviços"
  key_links:
    - from: "resources/js/pages/portal/empresas/index.tsx"
      to: "/portal/empresas"
      via: "useServerTable (busca/sort/per_page server-driven)"
      pattern: "useServerTable"
    - from: "resources/js/pages/portal/empresas/cadastrar.tsx"
      to: "/portal/empresas/consultar-cnpj"
      via: "useHttp post (request standalone Inertia v3)"
      pattern: "consultar-cnpj"
    - from: "resources/js/pages/portal/empresas/cadastrar.tsx"
      to: "/portal/empresas (store)"
      via: "useForm post"
      pattern: "useForm"
---

<objective>
Telas do portal para HU-027 (Minhas empresas) e HU-023+HU-021 (cadastro com lookup vivo de CNPJ), seguindo os padrões travados 2.1/2.2 e os componentes da fase 2.4 (DataTable tipada, PageHeader, Card), com a sidebar atualizada.

Output: 2 páginas React + item de sidebar, typecheck e build verdes.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-cadastro-empresarial/03-CONTEXT.md
@.planning/phases/03-cadastro-empresarial/03-RESEARCH.md
@.planning/phases/03-cadastro-empresarial/03-02-SUMMARY.md
@.planning/phases/03-cadastro-empresarial/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar + tela Minhas empresas (padrão de listagem 2.4)</name>
  <files>resources/js/layouts/portal-layout.tsx, resources/js/pages/portal/empresas/index.tsx</files>
  <read_first>
    - COORDENAÇÃO 2.4 (obrigatório ANTES de codar): listar `resources/js/components/ui/data-table/` e `resources/js/components/ui/{card,kpi-card,skeleton,table-action}.tsx` + `resources/js/components/app/page-header.tsx`. O STATE.md registra a fase 2.4 como CONCLUÍDA (componentes no disco) — USAR esses componentes; se algum esperado não existir, usar os componentes 2.1/2.2 (Table, Pagination, EmptyState) SEM bloquear e SEM criar duplicados. NUNCA editar arquivos de components/ui/* nem páginas da gestão.
    - resources/js/pages/gestao/cnaes/index.tsx (LISTAGEM-MODELO do padrão 2.4 — copiar a estrutura PageHeader → Card → CardHeader/CardContent → TableToolbar → chips → DataTable → Pagination/PerPageSelect)
    - resources/js/components/ui/data-table/{data-table.tsx,types.ts,use-server-table.ts,table-toolbar.tsx,per-page-select.tsx} (APIs: ColumnDef<T>, SortState, useServerTable options)
    - resources/js/components/ui/{badge.tsx,empty-state.tsx,pagination.tsx,table-action.tsx,button.tsx} e components/icons/index.tsx (ícones disponíveis)
    - resources/js/layouts/portal-layout.tsx (grupos atuais da sidebar)
    - .planning/phases/03-cadastro-empresarial/03-04-SUMMARY.md (shape EXATO das props do index — contrato do backend)
  </read_first>
  <action>
    1. portal-layout.tsx — no grupo 'Serviços', adicionar como PRIMEIRO item `{ name: 'Minhas empresas', href: '/portal/empresas', icon: <ListIcon /> }` (importar ListIcon de components/icons; manter Procurações em seguida).

    2. Criar resources/js/pages/portal/empresas/index.tsx (espelhar a estrutura do gestao/cnaes/index.tsx, adaptando):
       - Tipos locais (NÃO tocar resources/js/types/index.d.ts): `interface CompanyRow { id: number; legal_name: string; trade_name: string | null; formatted_cnpj: string; source: { value: string; label: string }; primary_cnae: { formatted_code: string; description: string } | null; link: { role: string; role_label: string; active: boolean; started_at: string | null; ended_at: string | null } | null; }` + props da página (companies paginadas com data/meta/links conforme shape ecoado pelo backend, filters, perPageOptions, totalCompanies).
       - `useServerTable({ url: '/portal/empresas', initialSearch: filters.search, initialSort: { column: filters.sort, direction: filters.direction }, initialPerPage: filters.per_page })`.
       - PageHeader: título 'Minhas empresas', trilha ancestral [{ label: 'Meu painel', href: '/portal/painel' }] (conferir a prop exata no page-header.tsx).
       - Card único: CardHeader com título + ação primária `<Button size="sm" href="/portal/empresas/cadastrar">Cadastrar empresa</Button>` (conferir se Button aceita href; senão envolver com <Link>). CardContent: TableToolbar (busca placeholder 'Buscar por razão social ou CNPJ') → DataTable<CompanyRow>.
       - Colunas (ColumnDef<CompanyRow>[]):
         1. 'empresa' (sortable, id 'legal_name'): razão social em font-medium + linha menor com formatted_cnpj e trade_name quando houver.
         2. 'cnae_principal': formatted_code + description truncada (`max-w-[28ch] truncate` com title); '—' quando null.
         3. 'origem': Badge — source.value 'manual' → color light, 'redesim' → color info (conferir palette no badge.tsx); label do backend.
         4. 'vinculo': Badge success 'Ativo' / Badge light 'Encerrado' + role_label ao lado em texto menor.
         5. 'acoes' (align end): TableAction tone brand title 'Ver detalhes' href `/portal/empresas/${row.id}` (ícone EyeIcon).
       - emptyState: se filters.search vazio → EmptyState title 'Nenhuma empresa cadastrada' description 'Cadastre sua primeira empresa para iniciar solicitações de viabilidade.' action botão 'Cadastrar empresa'; senão → title 'Nenhuma empresa encontrada' description 'Ajuste o termo da busca e tente novamente.' (distinção obrigatória 2.2).
       - Rodapé: Pagination compartilhado (meta from/to/total) + PerPageSelect com perPageOptions.
       - Página envolvida em PortalLayout (padrão das páginas do portal — conferir como dashboard.tsx/procuracoes definem layout).

    3. Verificar: `npm run typecheck && npm run build` e regressão escopada `php artisan test --compact --filter=MyCompaniesTest` (props continuam compatíveis).
  </action>
  <verify>npm run typecheck && npm run build && php artisan test --compact --filter=MyCompaniesTest</verify>
  <acceptance_criteria>
    - typecheck e build sem erros; MyCompaniesTest segue verde.
    - `grep "Minhas empresas" resources/js/layouts/portal-layout.tsx` presente no grupo Serviços.
    - `grep "useServerTable" resources/js/pages/portal/empresas/index.tsx` presente (padrão 2.4) OU, se DataTable não existir no disco, `grep "EmptyState"` + Table 2.1 presente com justificativa no SUMMARY.
    - `grep -c "EmptyState" resources/js/pages/portal/empresas/index.tsx` >= 1 com os DOIS textos (lista vazia × busca vazia).
    - Nenhum arquivo de components/ui/* nem páginas de gestao/ modificado (`git status` limpo fora dos files deste plano).
  </acceptance_criteria>
  <done>Minhas empresas navegável no padrão SaaS das listagens, com origem e situação do vínculo visíveis.</done>
</task>

<task type="auto">
  <name>Task 2: Página dedicada de cadastro com lookup vivo de CNPJ</name>
  <files>resources/js/pages/portal/empresas/cadastrar.tsx</files>
  <read_first>
    - .cursor/skills/inertia-react-development/SKILL.md (assinatura EXATA do useHttp e do useForm no Inertia v3 — obrigatório antes de codar)
    - .planning/phases/03-cadastro-empresarial/03-02-SUMMARY.md (shape do JSON do endpoint consultar-cnpj — chaves snake_case)
    - resources/js/components/form/{input.tsx,label.tsx,select.tsx} e ui/{alert.tsx,button.tsx,card.tsx} (APIs de error/hint)
    - resources/js/pages/portal/procuracoes/index.tsx (padrão de form do portal, Pitfall 2 [02-04]: <Form> sobrescreve onSubmit — aqui usar useForm)
    - app/Http/Requests/Portal/StoreCompanyRequest.php (campos/regras — nomes dos inputs idênticos)
  </read_first>
  <action>
    Criar resources/js/pages/portal/empresas/cadastrar.tsx — PÁGINA dedicada (decisão de usabilidade do RESEARCH: ~12 campos + lookup; modal prejudicaria):

    1. Props: `{ cnpjLookupEnabled: boolean }`. PageHeader título 'Cadastrar empresa', trilha [Meu painel, Minhas empresas].
    2. Estado com useForm<CompanyForm> (todos os campos do StoreCompanyRequest: cnpj, legal_name, trade_name, legal_nature_code, legal_nature, size_code, size, street, number, complement, neighborhood, city, state, zip_code, email, phone — strings vazias por default).
    3. Seções em Cards:
       - 'Identificação': CNPJ (Input com value formatado por helper local `formatCnpj(raw)` que insere XX.XXX.XXX/XXXX-XX sobre maiúsculas alfanuméricas e onChange guarda só `[A-Z0-9]` — CNPJ alfanumérico chega em julho/2026, NÃO restringir a dígitos) + botão 'Buscar CNPJ' ao lado; razão social (required), nome fantasia, natureza jurídica (legal_nature_code + legal_nature), porte (size_code + size).
       - 'Endereço': street, number, complement, neighborhood, city, state (2 letras), zip_code (8 dígitos).
       - 'Contato': email, phone.
       Todos os Inputs com error={!!errors.campo} hint={errors.campo} (padrão 2.1).
    4. Lookup (HU-021 na UI):
       - `cnpjLookupEnabled === false` → botão `disabled` com hint 'Consulta automática indisponível — preencha os dados manualmente.' (degradação comunicada, NUNCA falha silenciosa).
       - Habilitado → onClick: limpar lookupError; chamar o endpoint via useHttp (POST '/portal/empresas/consultar-cnpj', { cnpj: form.data.cnpj }) com estado processing no botão ('Buscando...'); sucesso → `setData` apenas dos campos retornados não vazios (legal_name, trade_name, legal_nature_code, legal_nature, size_code, size, street, number, complement, neighborhood, city, state, zip_code, email, phone — chaves snake_case do 03-02; usuário revisa antes de salvar); erro (404/422/503) → setLookupError(message da resposta JSON) exibido em `<Alert variant="error" title="Consulta de CNPJ" message={lookupError} />` acima do form — formulário CONTINUA editável (FA-03: cadastro manual segue possível).
       - NUNCA simular resultado: sem resposta do endpoint, nenhum campo é preenchido.
    5. Submit: form.post('/portal/empresas') (useForm; redirect do backend leva ao index com flash). Botões: 'Cancelar' (Link para /portal/empresas, variant outline) e 'Salvar empresa' (type submit, disabled processing).
    6. Verificar: typecheck + build + regressão escopada CnpjLookupTest|CompanyRegistrationTest (contratos intactos).
  </action>
  <verify>npm run typecheck && npm run build && php artisan test --compact --filter='CnpjLookupTest|CompanyRegistrationTest'</verify>
  <acceptance_criteria>
    - typecheck e build sem erros; filtro de regressão verde.
    - `grep "consultar-cnpj" resources/js/pages/portal/empresas/cadastrar.tsx` presente (endpoint real, não mock).
    - `grep "Consulta automática indisponível" resources/js/pages/portal/empresas/cadastrar.tsx` presente (toggle OFF comunicado).
    - `grep -i "useHttp" resources/js/pages/portal/empresas/cadastrar.tsx` presente (request standalone v3; se a skill indicar API diferente, registrar no SUMMARY o método usado).
    - `grep "A-Z0-9" resources/js/pages/portal/empresas/cadastrar.tsx` presente (input aceita alfanumérico).
  </acceptance_criteria>
  <done>Cadastro navegável com pré-preenchimento real por CNPJ, degradação comunicada e validações visíveis.</done>
</task>

</tasks>

<verification>
- `npm run typecheck && npm run build` verdes.
- `php artisan test --compact --filter='MyCompaniesTest|CnpjLookupTest|CompanyRegistrationTest'` verde (regressão ESCOPADA da wave — full-suite só no 03-09).
- `vendor/bin/pint --dirty --format agent` sem pendências (se algum PHP for tocado — não deve).
- `git status` não mostra mudanças em components/ui/* nem em pages/gestao/*.
</verification>

<success_criteria>
- Fluxo cidadão completo no browser: sidebar → Minhas empresas (EmptyState ou lista com badges) → Cadastrar empresa → Buscar CNPJ (dados reais) → Salvar → volta à lista.
- Padrões 2.1/2.2/2.4 respeitados sem duplicar componentes; dark mode herdado dos componentes do design system.
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/03-cadastro-empresarial/03-07-SUMMARY.md` registrando: componentes 2.4 efetivamente usados (ou fallback 2.1/2.2 aplicado), API exata usada para o request standalone (useHttp) e estrutura final das páginas.
</output>

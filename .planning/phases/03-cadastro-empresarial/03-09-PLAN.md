---
phase: 03-cadastro-empresarial
plan: 09
type: execute
wave: 7
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05", "03-06", "03-07", "03-08"]
files_modified:
  - database/seeders/CompanySeeder.php
  - database/seeders/DatabaseSeeder.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
autonomous: false
requirements: [HU-021, HU-022, HU-023, HU-024, HU-025, HU-026, HU-027, HU-028]

must_haves:
  truths:
    - "php artisan migrate:fresh --seed deixa o ambiente dev navegável com cidadão de teste, empresas exemplo (manual e REDESIM) e CNAEs vinculados"
    - "Suíte COMPLETA verde (255+ testes), pint sem pendências, typecheck e build verdes"
    - "Smoke E2E aprovado pelo usuário com chamada REAL ao provider de CNPJ (BrasilAPI) e import REDESIM real via comando"
  artifacts:
    - path: "database/seeders/CompanySeeder.php"
      provides: "Cidadão dev + empresas exemplo com vínculos e CNAEs (dados públicos oficiais)"
    - path: "database/seeders/DatabaseSeeder.php"
      provides: "CompanySeeder integrado após CnaeSeeder"
  key_links:
    - from: "database/seeders/CompanySeeder.php"
      to: "app/Services/RedesimImportService.php"
      via: "seed da empresa REDESIM via import REAL do redesim-exemplo.json"
      pattern: "RedesimImportService"
    - from: "database/seeders/DatabaseSeeder.php"
      to: "database/seeders/CompanySeeder.php"
      via: "call() na ordem correta (depois de CnaeSeeder)"
      pattern: "CompanySeeder"
---

<objective>
Fechamento da Fase 3: seeds dev integrados (cidadão de teste + empresas exemplo manual/REDESIM com CNAEs), verificação integral fresca (full-suite, pint, typecheck, build, comando real) e smoke E2E navegável com checkpoint humano — incluindo a evidência de integração viva com o provider de CNPJ.

Output: CompanySeeder + DatabaseSeeder atualizado + evidência completa de verificação + aprovação do usuário.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-cadastro-empresarial/03-CONTEXT.md
@.planning/phases/03-cadastro-empresarial/03-RESEARCH.md
@.planning/phases/03-cadastro-empresarial/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seeds dev de empresas + verificação integral fresca</name>
  <files>database/seeders/CompanySeeder.php, database/seeders/DatabaseSeeder.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <read_first>
    - database/seeders/DevAdminSeeder.php (padrão de usuário dev: firstOrCreate, e-mail verificado, CPF fixo válido, aviso "nunca em produção")
    - database/seeders/DatabaseSeeder.php (ordem atual: Roles → LegalTerm → Parameter → Cnae → DevAdmin)
    - database/factories/UserFactory.php — withAcceptedLgpdTerm (lógica de aceite a replicar no seeder via LegalTerm::current('lgpd'))
    - tests/Feature/Seeders/DatabaseSeederTest.php (asserções atuais — ampliar, não substituir)
    - app/Services/RedesimImportService.php e database/data/redesim-exemplo.json (03-03)
  </read_first>
  <action>
    TDD: ampliar DatabaseSeederTest primeiro (RED), depois implementar o seeder.

    1. RED — em DatabaseSeederTest, adicionar asserts após $this->seed():
       - test_seed_cria_cidadao_dev_com_empresas: users tem 'cidadao@sile.dev' com papel cidadao e termo LGPD aceito; companies count >= 3; existe company source 'manual' E source 'redesim'; o cidadão dev tem pelo menos 2 vínculos ativos (company_user ended_at null); pelo menos uma empresa do cidadão tem CNAE principal (company_cnae is_primary true).
       Confirmar RED.

    2. GREEN — criar database/seeders/CompanySeeder.php (`php artisan make:seeder CompanySeeder --no-interaction`):
       - Cidadão dev: `User::firstOrCreate(['email' => 'cidadao@sile.dev'], ['name' => 'Cidadão SILE', 'cpf' => '52998224725', 'phone' => null, 'password' => 'password'])` (CPF válido fixo, distinto do DevAdmin); forceFill email_verified_at quando null; assignRole('cidadao'); aceite LGPD: `LegalTermAcceptance::firstOrCreate(['user_id' => ..., 'legal_term_id' => LegalTerm::current('lgpd')->id], ['ip_address' => '127.0.0.1', 'accepted_at' => now()])` (docblock: credenciais de DESENVOLVIMENTO, nunca produção).
       - Empresa manual: `Company::firstOrCreate(['cnpj' => '47960950000121'], [... 'legal_name' => 'MAGAZINE LUIZA S/A', 'source' => CompanySource::Manual, endereço Salvador/BA fictício plausível ...])` (CNPJ público oficial — dado real); vínculo `CompanyUser::firstOrCreate(['company_id' => ..., 'user_id' => cidadão, 'role' => 'responsavel'], ['started_at' => now()])`; CNAE principal: localizar um Cnae existente da carga oficial (ex.: `Cnae::where('code', '4713004')->first()` — conferir código real presente no CSV; fallback: primeiro Cnae ativo) e `syncWithoutDetaching([$cnae->id => ['is_primary' => true]])`.
       - Empresas REDESIM: executar o import REAL `app(RedesimImportService::class)->import(database_path('data/redesim-exemplo.json'))` (lógica de produção, carga de dev — regra do projeto); em seguida vincular o cidadão dev como responsavel à empresa do CNPJ '00000000000191' via firstOrCreate (conveniência de DEV para a empresa aparecer em "Minhas empresas"; comentário explicando que o import NÃO cria vínculo por design).
       - Idempotente: re-seed não duplica nada (firstOrCreate em tudo).
       - DatabaseSeeder: adicionar `CompanySeeder::class` ao call() DEPOIS de CnaeSeeder e DevAdminSeeder.
       Rodar `php artisan test --compact --filter=DatabaseSeederTest` até verde.

    3. VERIFICAÇÃO INTEGRAL (evidência fresca, nesta ordem — full-suite SÓ aqui, lição das waves paralelas):
       a) `vendor/bin/pint --dirty --format agent` — sem pendências.
       b) `php artisan test --compact` — suíte COMPLETA verde (baseline da Fase 2: 209; registrar o número exato com os novos da fase).
       c) `npm run typecheck && npm run build` — verdes.
       d) Banco dev real: `php artisan migrate:fresh --seed --no-interaction` (usar o banco do .env do projeto) — sem erros.
       e) Comando real: `php artisan redesim:importar database/data/redesim-exemplo.json` — relatório pt-BR; rodar 2ª vez e evidenciar idempotência (atualizados).
       f) `php artisan route:list --path=portal/empresas` e `--path=portal/cnaes` — 9 rotas da fase presentes.
       Registrar TODOS os outputs no SUMMARY (números, não adjetivos).
  </action>
  <verify>php artisan test --compact && npm run typecheck && npm run build && php artisan redesim:importar database/data/redesim-exemplo.json</verify>
  <acceptance_criteria>
    - DatabaseSeederTest verde com o assert novo.
    - Full-suite 100% verde com contagem registrada (>= 255 testes).
    - migrate:fresh --seed sem erros; segundo db:seed não duplica (idempotência).
    - Import real executado 2x com relatório (criados → atualizados).
    - pint/typecheck/build sem pendências.
  </acceptance_criteria>
  <done>Ambiente dev navegável com dados de exemplo reais e verificação integral fresca documentada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke E2E da Fase 3 (aprovação do usuário)</name>
  <read_first>
    - .planning/phases/03-cadastro-empresarial/03-RESEARCH.md — seção "Validação manual no browser" (roteiro original dos 8 passos)
    - .planning/phases/03-cadastro-empresarial/03-0{1..8}-SUMMARY.md (pendências e decisões registradas pelos planos executados)
  </read_first>
  <what-built>
    Cadastro empresarial completo no portal do cidadão: consulta de CNPJ contra a BrasilAPI (integração REAL), cadastro manual com vínculo, listagem "Minhas empresas" (origem manual × REDESIM), detalhe com edição (CNPJ imutável), CNAE principal/secundários com busca da tabela oficial, encerramento de vínculo com proteção do último responsável, import REDESIM por comando e toggle administrável do lookup.
  </what-built>
  <how-to-verify>
    Preparação: `composer run dev` rodando; banco semeado (Task 1). Roteiro de validação manual do 03-RESEARCH.md (seção "Validação manual no browser"), com os passos de integração real:

    1. Registrar cidadão A novo (ou logar cidadao@sile.dev / password); conferir Sidebar > Serviços > "Minhas empresas" (EmptyState no usuário novo; lista semeada no dev).
    2. INTEGRAÇÃO REAL (HU-021): em Cadastrar empresa, informar 00.000.000/0001-91 → "Buscar CNPJ" → campos preenchidos com dados REAIS do Banco do Brasil vindos da BrasilAPI (evidência viva; conferir depois o registro da consulta em activity_log com event consulta-cnpj). Ajustar endereço e salvar → lista mostra badge "Cadastro manual" e vínculo Ativo/Responsável. (Empresa do BB já semeada? Usar outro CNPJ público — ex.: 60.701.190/0001-04 Itaú — para não esbarrar no bloqueio de duplicado; o bloqueio em si é o passo 2b: tentar cadastrar CNPJ já existente e ver a mensagem "Já existe empresa cadastrada com este CNPJ.")
    3. CNAEs (HU-025/026): no detalhe, buscar "restaurante", definir principal 5611-2/01 com ConfirmDialog; adicionar 2 secundários e salvar; trocar o principal → conferir em activity_log os eventos cnae-principal/cnaes-secundarios com antes/depois.
    4. Representação (HU-027): cidadão B outorga procuração ao A (tela de Procurações da Fase 1); A ativa "em nome de B" → Minhas empresas mostra as empresas de B; A cadastra empresa para B (vínculo fica de B); encerrar representação → volta às próprias.
    5. Encerrar vínculo (HU-028): com A único responsável → bloqueio com "A empresa não pode ficar sem responsável ativo."; criar segundo responsável (tinker/factory) → encerramento com motivo funciona; lista mantém a empresa com situação Encerrado.
    6. REDESIM real (HU-022): `php artisan redesim:importar database/data/redesim-exemplo.json` → relatório; rodar de novo → atualizados (idempotente); badge "REDESIM" visível na empresa importada vinculada ao cidadão dev.
    7. Toggle (HU-014/021): na gestão (admin@sile.dev), Parâmetros → desligar features.cnpj_lookup → botão "Buscar CNPJ" desabilitado com aviso (sem deploy); religar → volta a funcionar. Bônus: tentar excluir na gestão um CNAE vinculado a empresa → bloqueio "CNAE vinculado a empresas não pode ser excluído."
    8. Dark mode + responsivo (375px) nas 3 telas novas; capturar screenshots (lista, cadastro com lookup, detalhe) para o SUMMARY.
  </how-to-verify>
  <resume-signal>Digite "aprovado" para concluir a fase, ou descreva os problemas encontrados (vira gap para /gsd-plan-phase 3 --gaps)</resume-signal>
  <acceptance_criteria>
    - Usuário respondeu "aprovado" (ou os problemas relatados foram registrados como gaps para `/gsd-plan-phase 3 --gaps`).
    - Passo 2 executado com chamada REAL ao provider: activity `consulta-cnpj` com `result = 'sucesso'` presente em activity_log (evidência citada no SUMMARY).
    - Passo 5 evidenciou o bloqueio literal "A empresa não pode ficar sem responsável ativo.".
    - Passo 7 evidenciou efeito sem deploy do toggle features.cnpj_lookup (botão desabilitado ⇒ reabilitado) e o bloqueio de exclusão de CNAE vinculado.
    - Screenshots das 3 telas novas (lista, cadastro com lookup, detalhe) capturados para o SUMMARY (claro/escuro/mobile).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Suíte completa, pint, typecheck, build e seeds — todos verdes com evidência fresca (Task 1).
- Smoke E2E dos 8 passos aprovado pelo usuário, incluindo chamada real ao provider e import real (Task 2).
</verification>

<success_criteria>
- Critério de pronto transversal do ROADMAP atendido: CAs das 8 HUs cobertos por testes passando, fluxo navegável de ponta a ponta executando lógica real, nenhum valor de negócio hardcoded, pint limpo.
- Evidência de integração viva (BrasilAPI) e de import REDESIM real registradas no SUMMARY.
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/03-cadastro-empresarial/03-09-SUMMARY.md` registrando: contagem final da suíte, outputs do comando real, resultado do smoke (com screenshots), e pendências para a pauta SEDUR (payload REGIN a validar; associação empresa importada ↔ usuário — Fase 13).
</output>

---
phase: 03-cadastro-empresarial
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - database/data/redesim-exemplo.json
  - tests/Fixtures/redesim/payload-valido.json
  - app/Services/RedesimImportService.php
  - app/Console/Commands/ImportRedesimCommand.php
  - tests/Feature/Companies/RedesimImportTest.php
autonomous: true
requirements: [HU-022]

must_haves:
  truths:
    - "Payload REDESIM válido cria empresa com CNAEs e endereço; reprocessar o mesmo arquivo atualiza sem duplicar (upsert por CNPJ)"
    - "Item inválido (CNPJ inválido, sem razão social, sem protocolo, CNAE principal inexistente) é rejeitado com motivo no relatório, sem inserção parcial"
    - "Import é auditado com relatório e rules_version 'redesim-import-v1' (CA-02)"
    - "php artisan redesim:importar database/data/redesim-exemplo.json roda de verdade, imprime relatório pt-BR e é idempotente"
    - "Não existe rota pública de import — entrada é exclusivamente comando/serviço (transporte real é HU-103, Fase 13)"
  artifacts:
    - path: "database/data/redesim-exemplo.json"
      provides: "Payload de referência REAL para homologação manual (estrutura A VALIDAR com a SEDUR)"
    - path: "app/Services/RedesimImportService.php"
      provides: "Import com validação por item, upsert por CNPJ, source imutável e relatório auditado"
    - path: "app/Console/Commands/ImportRedesimCommand.php"
      provides: "Comando redesim:importar {arquivo} com saída pt-BR e exit codes corretos"
  key_links:
    - from: "app/Console/Commands/ImportRedesimCommand.php"
      to: "app/Services/RedesimImportService.php"
      via: "injeção do service no handle()"
      pattern: "RedesimImportService"
    - from: "app/Services/RedesimImportService.php"
      to: "activity_log"
      via: "AuditService->log('empresas', 'importacao-redesim', ..., rulesVersion: 'redesim-import-v1')"
      pattern: "redesim-import-v1"
    - from: "app/Services/RedesimImportService.php"
      to: "cnaes.code"
      via: "resolução do CNAE principal/secundários por código em dígitos"
      pattern: "where\\('code'"
---

<objective>
HU-022 com lógica REAL atrás de contrato: payload REDESIM de referência versionado (marcado "a validar com a SEDUR"), RedesimImportService com validação por item, upsert por CNPJ, sincronização de CNAEs e relatório auditado, mais o comando `redesim:importar {arquivo}` para execução real em homologação — SEM tela fingindo "receber da REDESIM" e SEM criar vínculo usuário-empresa (associação chega com o transporte na Fase 13/HU-103).

Output: database/data/redesim-exemplo.json + service + comando + RedesimImportTest.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-cadastro-empresarial/03-CONTEXT.md
@.planning/phases/03-cadastro-empresarial/03-RESEARCH.md
@.planning/phases/03-cadastro-empresarial/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0 do plano): payload de referência redesim-exemplo.json + fixture de teste</name>
  <files>database/data/redesim-exemplo.json, tests/Fixtures/redesim/payload-valido.json</files>
  <read_first>
    - .planning/phases/03-cadastro-empresarial/03-RESEARCH.md — seção "Decisão 2 — Estrutura de referência REDESIM" (payload JSON de referência completo)
    - database/data/ (arquivos oficiais existentes — padrão de versionamento de dados)
  </read_first>
  <action>
    Este artefato vem ANTES do teste (Wave 0 Requirements do 03-VALIDATION.md: o arquivo existe antes do RedesimImportTest rodar).

    1. Criar database/data/redesim-exemplo.json: ARRAY com 2 objetos no formato de referência do RESEARCH (chaves exatas: protocolo, data_solicitacao, evento{codigo, descricao}, empresa{cnpj, razao_social, nome_fantasia, natureza_juridica{codigo, descricao}, porte{codigo, descricao}}, endereco{cep, logradouro, numero, complemento, bairro, municipio, uf}, atividades{principal, secundarias[]}, contato{email, telefone}):
       - Item 1: Banco do Brasil — protocolo "BAP2612345678", cnpj "00000000000191", razao_social "BANCO DO BRASIL SA", natureza {"2038", "Sociedade de Economia Mista"}, porte {"05", "DEMAIS"}, endereco Salvador/BA (cep "40020000", logradouro "Rua Chile", numero "10", bairro "Centro", municipio "Salvador", uf "BA"), atividades principal "6422100", secundarias ["6499999"], contato fictício @example.com.
       - Item 2: Petrobras — protocolo "BAP2687654321", cnpj "33000167000101", razao_social "PETROLEO BRASILEIRO S A PETROBRAS", natureza {"2038", "Sociedade de Economia Mista"}, porte {"05", "DEMAIS"}, endereco Salvador/BA fictício plausível, atividades principal "0600001" (extração de petróleo — usar código que EXISTE no CSV oficial; conferir com `grep "0600-0/01" database/data/*.csv` e usar os dígitos correspondentes), secundarias [].
       CNPJs e razões sociais são dados PÚBLICOS oficiais (RFB) — alinhado à regra de seeds com dados oficiais.
    2. Criar tests/Fixtures/redesim/payload-valido.json: array com APENAS o item 1 (Banco do Brasil), mesma estrutura.
    3. Validar sintaxe de ambos com `php -r 'json_decode(file_get_contents("database/data/redesim-exemplo.json"), flags: JSON_THROW_ON_ERROR); json_decode(file_get_contents("tests/Fixtures/redesim/payload-valido.json"), flags: JSON_THROW_ON_ERROR); echo "JSON OK\n";'`.

    NOTA OBRIGATÓRIA: a estrutura é REFERÊNCIA fundamentada (Resolução CGSIM 61/2020 + campos públicos da consulta prévia) e está PENDENTE de validação com a SEDUR (integrador REGIN/JUCEB) — o docblock do service (Task 2) carrega esse aviso; não inventar campos além dos listados.
  </action>
  <verify>php -r 'json_decode(file_get_contents("database/data/redesim-exemplo.json"), flags: JSON_THROW_ON_ERROR); echo "OK\n";'</verify>
  <acceptance_criteria>
    - Os dois arquivos JSON existem e passam no json_decode com JSON_THROW_ON_ERROR.
    - `php -r '$d = json_decode(file_get_contents("database/data/redesim-exemplo.json"), true); echo count($d);'` imprime 2.
    - `grep '"protocolo"' database/data/redesim-exemplo.json` encontra os protocolos BAP*.
    - Códigos de atividades existem no CSV oficial de CNAEs (conferidos por grep em database/data/).
  </acceptance_criteria>
  <done>Payload de referência versionado e fixture pronta — RedesimImportTest pode rodar nas tasks seguintes.</done>
</task>

<task type="auto">
  <name>Task 2: RedesimImportService — validação por item, upsert por CNPJ, CNAEs e relatório auditado</name>
  <files>app/Services/RedesimImportService.php, tests/Feature/Companies/RedesimImportTest.php</files>
  <read_first>
    - app/Services/CnaeImportService.php (padrão de import com relatório do projeto — shape do array de retorno, rejeitados com motivo)
    - app/Support/Audit/AuditService.php (assinatura log com rulesVersion)
    - app/Models/Company.php e app/Enums/CompanySource.php (criados no 03-01)
    - .planning/phases/03-cadastro-empresarial/03-RESEARCH.md — "RedesimImportService (padrão CnaeImportService)" e Pitfall 8
  </read_first>
  <action>
    TDD estrito.

    1. RED — criar tests/Feature/Companies/RedesimImportTest.php (`php artisan make:test --phpunit Companies/RedesimImportTest --no-interaction`, RefreshDatabase). Helper `tempPayload(array $items): string` que escreve json_encode($items) em arquivo temporário (`tempnam(sys_get_temp_dir(), 'redesim')`) e retorna o caminho; helper `validItem(array $overrides = []): array` com a estrutura do payload-valido.json. Seedar Cnae::factory()->create(['code' => '6422100']) e (['code' => '6499999']) nos cenários que precisam. Testes:
       - test_payload_valido_cria_empresa_com_cnaes_e_endereco (CA-01): rodar service no payload-valido.json (fixture) → assertDatabaseHas('companies', ['cnpj' => '00000000000191', 'legal_name' => 'BANCO DO BRASIL SA', 'source' => 'redesim', 'city' => 'Salvador', 'redesim_protocol' => 'BAP2612345678']) + empresa tem primaryCnae code 6422100 e 1 secundário + relatório ['lidos' => 1, 'importados' => 1, 'atualizados' => 0].
       - test_import_e_auditado_com_relatorio_e_versao_de_regras (CA-02): após import, assertDatabaseHas('activity_log', ['log_name' => 'empresas', 'event' => 'importacao-redesim', 'rules_version' => 'redesim-import-v1', 'result' => 'sucesso']).
       - test_item_com_cnpj_invalido_e_rejeitado_sem_insercao_parcial (CA-03): item com cnpj '11111111111111' → rejeitados contém motivo com o protocolo e 'CNPJ inválido'; assertDatabaseCount('companies', 0).
       - test_item_sem_razao_social_e_rejeitado (CA-03): razao_social '' → rejeitado com motivo; count 0.
       - test_item_sem_protocolo_e_rejeitado (CA-03): protocolo ausente → rejeitado; count 0.
       - test_item_com_cnae_principal_inexistente_e_rejeitado (CA-03): atividades.principal '9999999' (não seedado) → rejeitado com motivo citando o código; count 0.
       - test_cnae_inativo_importa_com_aviso: principal existe mas active false → empresa criada E avisos contém o código (dado da Junta é fato consumado; restrição "somente ativos" vale para seleção manual HU-025/026).
       - test_cnae_secundario_inexistente_gera_aviso_e_e_ignorado: principal válido + secundária '8888888' inexistente → empresa criada só com o principal, avisos contém '8888888'.
       - test_reimport_atualiza_sem_duplicar_e_preserva_source (Pitfall 8): criar Company::factory()->create(['cnpj' => '00000000000191', 'source' => 'manual']); importar payload com mesmo CNPJ → relatório atualizados 1; assertDatabaseCount('companies', 1); source CONTINUA 'manual'; redesim_synced_at não nulo; redesim_protocol atualizado.
       - test_import_nao_cria_vinculo_usuario_empresa: após import, assertDatabaseCount('company_user', 0).
       Confirmar RED.

    2. GREEN — `php artisan make:class Services/RedesimImportService --no-interaction`. Docblock OBRIGATÓRIO: estrutura de payload é REFERÊNCIA A VALIDAR COM A SEDUR (integrador REGIN/JUCEB — HU-103/Fase 13 ajusta o parsing sem retrabalho de domínio); campos de solicitação (área, inscrição imobiliária, forma de atuação) são IGNORADOS nesta fase — pertencem à Fase 8. Implementação:
       - `public function import(string $jsonPath): array` — lê o arquivo (RuntimeException pt-BR se ilegível), json_decode com JSON_THROW_ON_ERROR; aceitar objeto único (detectar chave 'protocolo' → wrap em array) ou array de objetos.
       - Por item: extrair protocolo, empresa.*, endereco.*, atividades.*, contato.*. Validar: protocolo obrigatório; razao_social obrigatória; cnpj normalizado (`preg_replace('/[^A-Z0-9]/', '', strtoupper(...))`) válido pela ValidCnpj (usar `Validator::make(['cnpj' => $cnpj], ['cnpj' => [new ValidCnpj]])->fails()`); CNAE principal: normalizar para dígitos e buscar `Cnae::query()->where('code', $digits)->first()` — inexistente → rejeitar. Item rejeitado: mensagem pt-BR `"protocolo {x}: {motivo}"` no array rejeitados; NENHUMA escrita no banco para o item.
       - CNAE principal inativo → importar mesmo assim + aviso `"protocolo {x}: CNAE principal {code} está inativo na tabela oficial"`. Secundárias: resolver cada código; inexistentes → aviso e ignora; inativas → importa com aviso.
       - Persistência por item em `DB::transaction`: `Company::query()->where('cnpj', $cnpj)->first()` — existe → `update()` dos dados cadastrais/endereço/contato + redesim_protocol + redesim_synced_at = now() SEM tocar source (origem de criação imutável — Pitfall 8) e contar em atualizados; não existe → `Company::create([... 'source' => CompanySource::Redesim, 'redesim_protocol' => ..., 'redesim_synced_at' => now()])` e contar em importados. Em seguida sincronizar CNAEs com conjunto exato: `$company->cnaes()->sync($payload)` onde $payload = [principalId => ['is_primary' => true]] + cada secundário => ['is_primary' => false].
       - NUNCA criar company_user (sem usuário do portal no payload — associação é da Fase 13; registrar no docblock).
       - Retorno: `['lidos' => int, 'importados' => int, 'atualizados' => int, 'rejeitados' => array<string>, 'avisos' => array<string>]`.
       - Auditoria ao final: `app(AuditService::class)->log('empresas', 'importacao-redesim', 'Importação de dados REDESIM processada', $relatorio, result: $todosRejeitados ? 'falha' : 'sucesso', rulesVersion: 'redesim-import-v1');` (causer null quando rodado por comando — correto: ação de sistema).
       Rodar filtro até verde.

    3. REFACTOR — pint.
  </action>
  <verify>php artisan test --compact --filter=RedesimImportTest</verify>
  <acceptance_criteria>
    - Filtro RedesimImportTest verde (10 testes da task).
    - `grep "redesim-import-v1" app/Services/RedesimImportService.php` presente.
    - `grep -i "a validar com a sedur\|A VALIDAR" app/Services/RedesimImportService.php` presente no docblock.
    - `grep "company_user\|CompanyUser" app/Services/RedesimImportService.php` NÃO encontra criação de vínculo.
    - Reimport do mesmo arquivo não altera assertDatabaseCount('companies') (provado pelo teste de reimport).
  </acceptance_criteria>
  <done>Lógica real de import processando o formato REDESIM com todas as invariantes (upsert, source imutável, item atômico, relatório auditado).</done>
</task>

<task type="auto">
  <name>Task 3: Comando redesim:importar {arquivo} com relatório pt-BR e exit codes</name>
  <files>app/Console/Commands/ImportRedesimCommand.php, tests/Feature/Companies/RedesimImportTest.php</files>
  <read_first>
    - app/Services/RedesimImportService.php (Task 2)
    - Boost `search-docs` queries ['artisan command arguments output', 'console testing expectsOutput'] (padrão Laravel 13 de comando e teste de console)
  </read_first>
  <action>
    TDD.

    1. RED — adicionar ao RedesimImportTest:
       - test_comando_importa_arquivo_e_imprime_relatorio: seedar CNAEs; `$this->artisan('redesim:importar', ['arquivo' => base_path('tests/Fixtures/redesim/payload-valido.json')])` → expectsOutputToContain('Importados: 1') → assertExitCode(0); empresa no banco.
       - test_comando_com_arquivo_inexistente_falha: artisan com 'storage/nao-existe.json' → expectsOutputToContain('Arquivo não encontrado') → assertExitCode(1).
       - test_comando_com_json_invalido_falha: arquivo temp com conteúdo 'não-json' → expectsOutputToContain('JSON inválido') → assertExitCode(1).
       - test_comando_com_todos_os_itens_rejeitados_retorna_falha: payload só com item de CNPJ inválido → assertExitCode(1) e output contém 'Rejeitados'.
       - test_nao_existe_rota_publica_de_import (CA-04): `$this->assertFalse(Route::has('portal.empresas.importar-redesim'));` + post('/portal/empresas/importar-redesim') retorna 404 — import é exclusivamente comando/serviço.
       Confirmar RED.

    2. GREEN — `php artisan make:command ImportRedesimCommand --command=redesim:importar --no-interaction`:
       - signature `redesim:importar {arquivo : Caminho do arquivo JSON no formato REDESIM}`; description 'Importa dados empresariais no formato REDESIM (HU-022) — transporte real do integrador é a Fase 13'.
       - handle(RedesimImportService $service): int — validar `is_file($path)` → `$this->error("Arquivo não encontrado: {$path}")` e return self::FAILURE; try/catch JsonException ao redor do service → `$this->error('JSON inválido: '.$e->getMessage())` e FAILURE; imprimir relatório pt-BR com $this->info/line: "Lidos: N", "Importados: N", "Atualizados: N", e quando houver: seção "Rejeitados:" com bullets dos motivos e "Avisos:" com bullets; se lidos > 0 e todos rejeitados → FAILURE, senão SUCCESS.
       Rodar filtro até verde.

    3. REFACTOR — pint. Smoke real: `php artisan migrate:fresh --seed --no-interaction && php artisan redesim:importar database/data/redesim-exemplo.json` no banco de DEV (sqlite/pgsql local) — conferir relatório e rodar 2ª vez para evidenciar idempotência (atualizados: 2). Registrar o output no SUMMARY.
  </action>
  <verify>php artisan test --compact --filter=RedesimImportTest</verify>
  <acceptance_criteria>
    - Filtro RedesimImportTest verde (15 testes acumulados no arquivo).
    - `php artisan list | grep redesim` mostra redesim:importar.
    - Execução real contra database/data/redesim-exemplo.json imprime relatório pt-BR com Lidos/Importados/Atualizados e exit 0 (evidência fresca no SUMMARY).
    - Segunda execução real: 'Atualizados: 2' e nenhuma duplicação (idempotência).
  </acceptance_criteria>
  <done>Comando operacional para homologação com payloads reais da SEDUR quando entregues — evidência de execução real registrada.</done>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=RedesimImportTest` verde (verificação ESCOPADA — full-suite só no 03-09).
- `vendor/bin/pint --dirty --format agent` sem pendências.
- Execução real do comando contra database/data/redesim-exemplo.json registrada (relatório + idempotência).
</verification>

<success_criteria>
- HU-022 com lógica real de ponta a ponta atrás de contrato: validação por item, upsert por CNPJ, source imutável, CNAEs sincronizados, relatório auditado com rules_version.
- Nenhuma tela/rota fingindo transporte; comando real para homologação.
- Estrutura de payload explicitamente marcada como "a validar com a SEDUR".
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/03-cadastro-empresarial/03-03-SUMMARY.md` registrando: shape do relatório, decisões de validação (inativo importa com aviso), evidência da execução real do comando e a pendência REGIN/SEDUR.
</output>

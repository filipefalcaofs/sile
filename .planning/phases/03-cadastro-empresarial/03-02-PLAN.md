---
phase: 03-cadastro-empresarial
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/Services/Cnpj/CnpjLookup.php
  - app/Services/Cnpj/CnpjData.php
  - app/Services/Cnpj/BrasilApiCnpjLookup.php
  - app/Services/Cnpj/CnpjLookupException.php
  - app/Services/Cnpj/CnpjNotFoundException.php
  - app/Providers/AppServiceProvider.php
  - app/Http/Requests/Portal/LookupCnpjRequest.php
  - app/Http/Controllers/Portal/CnpjLookupController.php
  - routes/portal.php
  - tests/Fixtures/cnpj/brasilapi-banco-do-brasil.json
  - tests/Feature/Companies/CnpjLookupTest.php
autonomous: true
requirements: [HU-021]

must_haves:
  truths:
    - "POST /portal/empresas/consultar-cnpj com CNPJ válido retorna JSON com os dados reais do provider (CA-01)"
    - "Toda consulta — sucesso, não encontrado, falha e bloqueio por toggle — gera registro em activity_log com result correspondente (CA-02)"
    - "CNPJ inválido e CNPJ inexistente são bloqueados com mensagem pt-BR sem efeito colateral (CA-03)"
    - "Guest não consulta (CA-04) e com features.cnpj_lookup desligado NENHUM request HTTP sai (degradação comunicada)"
    - "Consulta repetida do mesmo CNPJ no TTL usa cache (1 único request); falha nunca é cacheada"
  artifacts:
    - path: "app/Services/Cnpj/CnpjLookup.php"
      provides: "Contrato lookup(string $cnpj): CnpjData — Fase 13 troca o binding sem tocar call sites"
    - path: "app/Services/Cnpj/BrasilApiCnpjLookup.php"
      provides: "Provider real com base_url parametrizada, timeout/retry de config e cache 24h"
    - path: "app/Http/Controllers/Portal/CnpjLookupController.php"
      provides: "Endpoint JSON auditado com toggle"
    - path: "tests/Feature/Companies/CnpjLookupTest.php"
      provides: "CA-01..04 + FA-03 + toggle + cache com Http::fake e preventStrayRequests"
  key_links:
    - from: "app/Providers/AppServiceProvider.php"
      to: "app/Services/Cnpj/BrasilApiCnpjLookup.php"
      via: "bind(CnpjLookup::class, BrasilApiCnpjLookup::class)"
      pattern: "bind\\(CnpjLookup::class"
    - from: "app/Services/Cnpj/BrasilApiCnpjLookup.php"
      to: "parâmetro integrations.cnpj_lookup.base_url"
      via: "Settings::get com fallback config('sile.integrations.cnpj_lookup.base_url')"
      pattern: "integrations\\.cnpj_lookup\\.base_url"
    - from: "app/Http/Controllers/Portal/CnpjLookupController.php"
      to: "activity_log"
      via: "AuditService->log('empresas', 'consulta-cnpj', ...)"
      pattern: "consulta-cnpj"
---

<objective>
HU-021 de ponta a ponta no backend: contrato CnpjLookup + DTO CnpjData + provider BrasilApiCnpjLookup REAL (dados abertos RFB, sem fachada), com base_url permutável por parâmetro (BrasilAPI ↔ minhareceita sem deploy), cache 24h só de sucesso, toggle administrável features.cnpj_lookup e endpoint JSON auditado em todas as saídas.

Propósito: pré-preenchimento do cadastro de empresa (tela no 03-07) com integração viva; o convênio oficial RFB (HU-105, Fase 13) substituirá apenas o binding.
Output: namespace App\Services\Cnpj completo + endpoint POST /portal/empresas/consultar-cnpj + suíte CnpjLookupTest.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-cadastro-empresarial/03-CONTEXT.md
@.planning/phases/03-cadastro-empresarial/03-RESEARCH.md
@.planning/phases/03-cadastro-empresarial/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contrato CnpjLookup + DTO CnpjData + provider BrasilAPI com cache e exceções tipadas</name>
  <files>app/Services/Cnpj/CnpjLookup.php, app/Services/Cnpj/CnpjData.php, app/Services/Cnpj/BrasilApiCnpjLookup.php, app/Services/Cnpj/CnpjLookupException.php, app/Services/Cnpj/CnpjNotFoundException.php, app/Providers/AppServiceProvider.php, tests/Fixtures/cnpj/brasilapi-banco-do-brasil.json, tests/Feature/Companies/CnpjLookupTest.php</files>
  <read_first>
    - .planning/phases/03-cadastro-empresarial/03-RESEARCH.md — seções "Decisão 1", "Code Examples > Provider BrasilAPI" e Pitfalls 4/6 (payload real registrado, arquitetura prescrita)
    - app/Support/Settings.php (assinatura Settings::get/enabled — backend banco+cache+fallback)
    - app/Providers/AppServiceProvider.php (onde registrar o binding)
    - config/sile.php (chaves integrations.cnpj_lookup.* criadas no 03-01)
    - Boost `search-docs` com queries ['http client retry timeout', 'http client fake preventStrayRequests', 'cache remember'] antes de implementar
  </read_first>
  <action>
    TDD — testes do SERVICE primeiro (a parte de endpoint entra na Task 2 no mesmo arquivo de teste).

    1. RED — criar tests/Fixtures/cnpj/brasilapi-banco-do-brasil.json com o payload REAL registrado no RESEARCH (Banco do Brasil: cnpj "00000000000191", razao_social "BANCO DO BRASIL SA", nome_fantasia "DIRECAO GERAL", natureza_juridica "Sociedade de Economia Mista", codigo_natureza_juridica 2038, porte "DEMAIS", codigo_porte 5, cnae_fiscal 6422100, cnae_fiscal_descricao, cnaes_secundarios [{codigo 6499999, descricao "Outras atividades de serviços financeiros não especificadas anteriormente"}], logradouro/numero/bairro/municipio/uf/cep, ddd_telefone_1 "6134939002", descricao_situacao_cadastral "ATIVA", data_inicio_atividade "1966-08-01"). Criar tests/Feature/Companies/CnpjLookupTest.php com `Http::preventStrayRequests()` no setUp e helper `brasilApiFixture(): array` lendo o JSON. Testes do service (resolver via `app(CnpjLookup::class)`):
       - test_lookup_retorna_dto_com_dados_do_provider: `Http::fake(['*/00000000000191' => Http::response($this->brasilApiFixture(), 200)]);` → lookup('00000000000191') retorna CnpjData com legalName 'BANCO DO BRASIL SA', tradeName 'DIRECAO GERAL', legalNatureCode '2038', sizeCode '05', primaryCnaeCode '6422100', secondaryCnaeCodes ['6499999'], city 'BRASILIA', state 'DF', zipCode '70040912', phone '6134939002', registrationStatus 'ATIVA'.
       - test_lookup_usa_url_parametrizada: seedar ParameterSeeder, gravar via model `Parameter::where('key', 'integrations.cnpj_lookup.base_url')->first()->update(['value' => 'https://minhareceita.org'])` (save invalida cache do Settings); `Http::fake(['minhareceita.org/*' => Http::response($this->brasilApiFixture(), 200)])` → lookup OK e `Http::assertSent(fn ($request) => str_contains($request->url(), 'minhareceita.org'))` (resiliência por parâmetro, sem deploy).
       - test_lookup_cacheia_somente_sucesso: fake 200 → duas chamadas lookup do MESMO cnpj → `Http::assertSentCount(1)`.
       - test_falha_de_conexao_nao_e_cacheada: `Http::fake(['*' => Http::failedConnection()])` → expectException(CnpjLookupException::class) na 1ª; refazer `Http::fake(['*' => Http::response($this->brasilApiFixture(), 200)])` → 2ª chamada retorna DTO (nada ficou cacheado).
       - test_cnpj_inexistente_lanca_excecao_tipada: fake `Http::response(['message' => 'CNPJ não encontrado'], 404)` → expectException(CnpjNotFoundException::class).
       Confirmar RED (classes inexistentes).

    2. GREEN — implementar:
       - Interface CnpjLookup: `public function lookup(string $cnpj): CnpjData;` (docblock: lança CnpjNotFoundException em 404 e CnpjLookupException em erro/indisponibilidade).
       - CnpjData: `final readonly class` com propriedades públicas string|array|null: cnpj, legalName, tradeName, legalNatureCode, legalNature, sizeCode, size, primaryCnaeCode, secondaryCnaeCodes (array<string>), street, number, complement, neighborhood, city, state, zipCode, phone, email, registrationStatus. Named constructor `public static function fromBrasilApi(array $payload): self` mapeando: cnpj=(string)cnpj; legalName=razao_social; tradeName=nome_fantasia ?: null; legalNatureCode=(string)codigo_natureza_juridica; legalNature=natureza_juridica; sizeCode=str_pad((string)codigo_porte, 2, '0', STR_PAD_LEFT) quando presente; size=porte; primaryCnaeCode=(string)cnae_fiscal; secondaryCnaeCodes=array de (string)codigo de cnaes_secundarios filtrando códigos vazios/zero; street=logradouro; number=numero; complement=complemento ?: null; neighborhood=bairro; city=municipio; state=uf; zipCode=apenas dígitos de cep; phone=apenas dígitos de ddd_telefone_1 ?: null; email=email ?: null; registrationStatus=descricao_situacao_cadastral. Método `toArray(): array` com chaves snake_case (cnpj, legal_name, trade_name, legal_nature_code, legal_nature, size_code, size, primary_cnae_code, secondary_cnae_codes, street, number, complement, neighborhood, city, state, zip_code, phone, email, registration_status) — contrato do JSON do endpoint e do formulário React.
       - CnpjNotFoundException (recebe string $cnpj) e CnpjLookupException (string $cnpj, ?int $status = null) — ambas extends Exception com mensagens pt-BR.
       - BrasilApiCnpjLookup: implementar EXATAMENTE o padrão do RESEARCH — `Settings::get('integrations.cnpj_lookup.base_url', config('sile.integrations.cnpj_lookup.base_url'))` com rtrim('/'); `Cache::remember("sile.cnpj_lookup.{$cnpj}", (int) config('sile.integrations.cnpj_lookup.cache_ttl', 86400), ...)`; dentro do closure `Http::timeout((int) config('sile.integrations.cnpj_lookup.timeout', 8))->connectTimeout(3)->retry((int) config('sile.integrations.cnpj_lookup.retries', 2), 200, throw: false)->acceptJson()->get("{$baseUrl}/{$cnpj}")`; status 404 → throw CnpjNotFoundException; `$response->failed()` → throw CnpjLookupException($cnpj, $response->status()); senão `CnpjData::fromBrasilApi($response->json())`. Exceção dentro do closure NÃO grava cache (Pitfall 6).
       - AppServiceProvider::register — `$this->app->bind(CnpjLookup::class, BrasilApiCnpjLookup::class);` com comentário: a Fase 13 (HU-105) troca este binding pelo provider conveniado.
       Rodar filtro até verde.

    3. REFACTOR — pint.
  </action>
  <verify>php artisan test --compact --filter=CnpjLookupTest</verify>
  <acceptance_criteria>
    - Filtro CnpjLookupTest verde com os 5 testes do service.
    - `grep "preventStrayRequests" tests/Feature/Companies/CnpjLookupTest.php` presente (zero rede na suíte).
    - `grep "bind(CnpjLookup::class" app/Providers/AppServiceProvider.php` presente.
    - `grep "Cache::remember" app/Services/Cnpj/BrasilApiCnpjLookup.php` presente.
    - `grep -c "snake_case\|legal_name" app/Services/Cnpj/CnpjData.php` >= 1 (toArray com chaves snake_case).
  </acceptance_criteria>
  <done>Contrato + provider real testados hermeticamente, com URL administrável e cache de sucesso.</done>
</task>

<task type="auto">
  <name>Task 2: Endpoint POST /portal/empresas/consultar-cnpj com toggle e auditoria em todas as saídas</name>
  <files>app/Http/Requests/Portal/LookupCnpjRequest.php, app/Http/Controllers/Portal/CnpjLookupController.php, routes/portal.php, tests/Feature/Companies/CnpjLookupTest.php</files>
  <read_first>
    - app/Support/Audit/AuditService.php (assinatura log/logBlocked travada [01-02])
    - routes/portal.php (grupo lgpd.accepted + ResolveRepresentation onde o endpoint entra)
    - tests/Feature/Procuration/LinkAttorneyTest.php (padrão portalUser(): cidadao()->withAcceptedLgpdTerm() + seed RolesAndPermissionsSeeder)
    - app/Rules/ValidCnpj.php (rule do 03-01)
    - app/Models/Parameter.php (update via model invalida cache — booted saved)
  </read_first>
  <action>
    TDD — ampliar CnpjLookupTest com os cenários de endpoint (RED), depois implementar.

    1. RED — novos testes (helper portalUser() idêntico ao LinkAttorneyTest; rotas via postJson):
       - test_consulta_com_sucesso_retorna_dados_para_o_formulario (CA-01): fake 200 fixture; postJson('/portal/empresas/consultar-cnpj', ['cnpj' => '00.000.000/0001-91']) → assertOk + assertJsonPath('legal_name', 'BANCO DO BRASIL SA') + assertJsonPath('cnpj', '00000000000191') + assertJsonStructure com as chaves snake_case do toArray.
       - test_consulta_com_sucesso_e_auditada (CA-02): após o post, assertDatabaseHas('activity_log', ['log_name' => 'empresas', 'event' => 'consulta-cnpj', 'result' => 'sucesso']) e a activity mais recente do event tem properties contendo cnpj consultado e provider (host da base_url).
       - test_cnpj_invalido_e_rejeitado_sem_consultar (CA-03): `Http::fake();` postJson com cnpj '11111111111111' → assertStatus(422) (erro de validação em cnpj) + `Http::assertNothingSent()`.
       - test_cnpj_inexistente_responde_404_com_mensagem (CA-03): fake 404 → assertStatus(404) + assertJsonPath('message', 'CNPJ não encontrado na base da Receita Federal.') + activity 'consulta-cnpj' result 'falha'.
       - test_indisponibilidade_registra_falha_e_orienta_preenchimento_manual (FA-03): `Http::fake(['*' => Http::failedConnection()])` → assertStatus(503) + assertJsonPath('message', 'Serviço de consulta indisponível no momento. Preencha os dados manualmente.') + activity result 'falha'.
       - test_toggle_desligado_bloqueia_sem_request (CA-03/RN-010): seedar ParameterSeeder; `Parameter::where('key', 'features.cnpj_lookup')->first()->update(['value' => '0']);` `Http::fake();` post → assertStatus(422) + assertJsonPath('message', 'A consulta automática de CNPJ está desativada. Preencha os dados manualmente.') + `Http::assertNothingSent()` + activity event 'consulta-cnpj' result 'bloqueado'.
       - test_visitante_nao_consulta (CA-04): postJson sem auth → assertStatus(401).
       Confirmar RED.

    2. GREEN:
       - LookupCnpjRequest: authorize() true (rota já protegida por auth+verified+lgpd); `prepareForValidation()` normaliza cnpj com `preg_replace('/[^A-Z0-9]/', '', strtoupper((string) $this->cnpj))`; rules: `['cnpj' => ['required', 'string', new ValidCnpj]]`.
       - CnpjLookupController (invokable, construtor com `public function __construct(private CnpjLookup $lookup, private AuditService $audit) {}`):
         1. Se `! Settings::enabled('cnpj_lookup')` → audit log('empresas', 'consulta-cnpj', 'Consulta de CNPJ bloqueada: recurso desativado', ['cnpj' => $cnpj, 'motivo' => 'toggle-desativado'], result: 'bloqueado') e `response()->json(['message' => 'A consulta automática de CNPJ está desativada. Preencha os dados manualmente.'], 422)`.
         2. try: `$data = $this->lookup->lookup($request->validated('cnpj'));` → audit log('empresas', 'consulta-cnpj', 'Consulta de CNPJ realizada', ['cnpj' => $cnpj, 'provider' => parse_url((string) Settings::get('integrations.cnpj_lookup.base_url', config('sile.integrations.cnpj_lookup.base_url')), PHP_URL_HOST)], result: 'sucesso') → `response()->json($data->toArray())`.
         3. catch CnpjNotFoundException → audit result 'falha' (properties motivo 'nao-encontrado') → json message 'CNPJ não encontrado na base da Receita Federal.' status 404.
         4. catch CnpjLookupException|Illuminate\Http\Client\ConnectionException → audit result 'falha' (properties motivo 'indisponibilidade') → json message 'Serviço de consulta indisponível no momento. Preencha os dados manualmente.' status 503.
       - routes/portal.php — dentro do grupo `lgpd.accepted + ResolveRepresentation`, adicionar com comentário de ordem (rotas literais de empresas ANTES de empresas/{company} que chega no 03-05):
         `Route::post('empresas/consultar-cnpj', CnpjLookupController::class)->name('empresas.consultar-cnpj');`
       Rodar filtro até verde.

    3. REFACTOR — pint; conferir `php artisan route:list --name=empresas`.
  </action>
  <verify>php artisan test --compact --filter=CnpjLookupTest && php artisan route:list --name=empresas.consultar-cnpj</verify>
  <acceptance_criteria>
    - `php artisan test --compact --filter=CnpjLookupTest` verde com TODOS os cenários (service + endpoint, 12 testes).
    - `php artisan route:list --name=empresas.consultar-cnpj` mostra POST portal/empresas/consultar-cnpj.
    - `grep -c "result: 'falha'\|'falha'" app/Http/Controllers/Portal/CnpjLookupController.php` >= 2 (404 e indisponibilidade auditados).
    - Nenhum teste do arquivo faz request real (preventStrayRequests no setUp + fakes por cenário).
  </acceptance_criteria>
  <done>Endpoint vivo com CA-01..04 + FA-03 cobertos, toggle degradando com aviso e zero request quando desligado.</done>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=CnpjLookupTest` verde (verificação ESCOPADA — full-suite só no 03-09).
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan route:list --path=portal/empresas` lista apenas a rota consultar-cnpj neste ponto.
</verification>

<success_criteria>
- HU-021 backend completo: consulta real atrás de contrato, URL administrável, cache 24h só de sucesso, toggle com degradação comunicada, auditoria em sucesso/falha/bloqueio.
- Zero dependência nova; zero request de rede na suíte.
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/03-cadastro-empresarial/03-02-SUMMARY.md` registrando: shape exato do JSON do endpoint (toArray), exceções tipadas e como trocar o provider por parâmetro (evidência do teste de URL parametrizada).
</output>

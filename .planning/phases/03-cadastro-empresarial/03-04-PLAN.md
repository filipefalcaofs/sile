---
phase: 03-cadastro-empresarial
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - app/Policies/CompanyPolicy.php
  - app/Http/Controllers/Portal/CompanyController.php
  - app/Http/Requests/Portal/StoreCompanyRequest.php
  - app/Models/Company.php
  - routes/portal.php
  - tests/Feature/Companies/CompanyRegistrationTest.php
  - tests/Feature/Companies/MyCompaniesTest.php
autonomous: true
requirements: [HU-023, HU-027]

must_haves:
  truths:
    - "Cadastro cria empresa + vínculo responsável ativo NA MESMA transação, em nome do usuário efetivo (representação cria para o representado)"
    - "CNPJ duplicado é bloqueado com a mensagem 'Já existe empresa cadastrada com este CNPJ.' (CA-03)"
    - "GET /portal/empresas lista SOMENTE as empresas do usuário efetivo (vínculo ativo ou encerrado), com CNAE principal e situação do vínculo, paginação parametrizada e busca case-insensitive"
    - "Procurador em representação ativa vê as empresas do representado; sem representação, não vê"
    - "Não autenticado é redirecionado (CA-04); criação é auditada (CA-02)"
  artifacts:
    - path: "app/Policies/CompanyPolicy.php"
      provides: "view/update/manageCnaes/endLink sobre o usuário efetivo (CurrentRepresentation)"
    - path: "app/Http/Controllers/Portal/CompanyController.php"
      provides: "index/create/store com eager loading anti-N+1 e contagem reutilizável"
    - path: "app/Http/Requests/Portal/StoreCompanyRequest.php"
      provides: "Normalização de CNPJ + ValidCnpj + unique com mensagem pt-BR"
  key_links:
    - from: "app/Policies/CompanyPolicy.php"
      to: "app/Support/Representation/CurrentRepresentation.php"
      via: "effectiveUser(): grantor() ?? $user"
      pattern: "CurrentRepresentation"
    - from: "app/Http/Controllers/Portal/CompanyController.php"
      to: "company_user"
      via: "store transacional cria CompanyUser role responsavel"
      pattern: "DB::transaction"
    - from: "routes/portal.php"
      to: "app/Http/Controllers/Portal/CompanyController.php"
      via: "rotas empresas.index/create/store no grupo lgpd+ResolveRepresentation"
      pattern: "empresas"
---

<objective>
HU-023 (cadastrar empresa) e HU-027 (consultar empresas vinculadas) no backend: CompanyPolicy integrada à representação "em nome de" (Fase 1), store transacional empresa+vínculo, e listagem "Minhas empresas" server-driven (busca, ordenação, paginação parametrizada) — props prontas para a tela do 03-07.

Output: policy + controller (index/create/store) + StoreCompanyRequest + rotas + 2 suítes de teste.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-cadastro-empresarial/03-CONTEXT.md
@.planning/phases/03-cadastro-empresarial/03-RESEARCH.md
@.planning/phases/03-cadastro-empresarial/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CompanyPolicy com representação + store transacional (HU-023)</name>
  <files>app/Policies/CompanyPolicy.php, app/Http/Controllers/Portal/CompanyController.php, app/Http/Requests/Portal/StoreCompanyRequest.php, routes/portal.php, tests/Feature/Companies/CompanyRegistrationTest.php</files>
  <read_first>
    - .planning/phases/03-cadastro-empresarial/03-RESEARCH.md — "Padrão 1 — Autorização com representação" e "Padrão 2 — Vínculo na criação" (código prescrito)
    - app/Support/Representation/CurrentRepresentation.php + app/Http/Middleware/ResolveRepresentation.php (estado scoped, sessão acting_procuration_id)
    - app/Policies/ProcurationPolicy.php (padrão de policy do projeto)
    - tests/Feature/Procuration/LinkAttorneyTest.php (helper portalUser, seed RolesAndPermissionsSeeder)
    - app/Http/Requests/StoreProcurationRequest.php (padrão de FormRequest com mensagens pt-BR)
    - routes/portal.php (estado atual — rota consultar-cnpj do 03-02 já presente)
  </read_first>
  <action>
    TDD estrito.

    1. RED — criar tests/Feature/Companies/CompanyRegistrationTest.php (RefreshDatabase; setUp seeda RolesAndPermissionsSeeder; helper portalUser()). Payload base do post: cnpj '00.000.000/0001-91', legal_name 'Empresa Exemplo Ltda', trade_name, legal_nature_code '2062', legal_nature, size_code '01', size, street/number/neighborhood/city 'Salvador'/state 'BA'/zip_code '40020000', email, phone '(71) 3333-0000'. Testes:
       - test_cadastro_cria_empresa_e_vinculo_responsavel_ativo (CA-01): post /portal/empresas → assertRedirect(route('portal.empresas.index')) + sessionHas('status'); assertDatabaseHas('companies', ['cnpj' => '00000000000191', 'source' => 'manual']); assertDatabaseHas('company_user', ['user_id' => $user->id, 'role' => 'responsavel', 'ended_at' => null]) e started_at não nulo.
       - test_cadastro_e_auditado (CA-02): activity_log com subject_type Company morph, event 'created', result 'sucesso'.
       - test_cnpj_duplicado_e_bloqueado_com_mensagem_clara (CA-03): Company::factory()->create(['cnpj' => '00000000000191']); post → assertSessionHasErrors('cnpj'); mensagem contém 'Já existe empresa cadastrada com este CNPJ.'; assertDatabaseCount('companies', 1) e count('company_user', 0).
       - test_cnpj_invalido_e_rejeitado: cnpj '11111111111111' → errors cnpj; count 0.
       - test_cnpj_alfanumerico_valido_e_aceito: cnpj '12.ABC.345/01DE-35' → criado com cnpj '12ABC34501DE35' (normalizado uppercase).
       - test_telefone_e_cep_sao_normalizados_para_digitos: phone '(71) 3333-0000' → '7133330000'; zip_code '40.020-000' → '40020000'.
       - test_visitante_e_redirecionado (CA-04): post sem auth → assertRedirect('/portal/login'); get /portal/empresas/cadastrar sem auth → redirect login.
       - test_em_representacao_cria_para_o_representado: grantor + attorney (ambos portalUser), Procuration::factory()->create([...]); actingAs($attorney)->withSession(['acting_procuration_id' => $proc->id])->post(...) → company_user do GRANTOR (user_id = grantor->id, não attorney); activity 'created' da Company com acting_for_user_id = grantor->id (enriquecimento central [01-07]).
       Confirmar RED.

    2. GREEN:
       - CompanyPolicy (`php artisan make:policy CompanyPolicy --model=Company --no-interaction`, auto-discovery cobre o par): método privado `effectiveUser(User $user): User` → `app(CurrentRepresentation::class)->grantor() ?? $user;`. Métodos públicos (todos consultando links do usuário EFETIVO):
         `view`: `$company->links()->where('user_id', $this->effectiveUser($user)->id)->exists();` (vínculo ativo OU encerrado — histórico continua visível);
         `update`: idem com `->whereNull('ended_at')` (só vínculo ATIVO);
         `manageCnaes`: igual a update;
         `endLink`: igual a update.
       - StoreCompanyRequest (`php artisan make:request Portal/StoreCompanyRequest --no-interaction`): authorize() true; `prepareForValidation()`: cnpj `preg_replace('/[^A-Z0-9]/', '', strtoupper(...))`, phone e zip_code só dígitos `preg_replace('/\D/', '', ...)` (quando preenchidos). rules():
         cnpj ['required', 'string', new ValidCnpj, Rule::unique('companies', 'cnpj')]; legal_name ['required', 'string', 'max:255']; trade_name/legal_nature/size ['nullable', 'string', 'max:255']; legal_nature_code ['nullable', 'string', 'max:4']; size_code ['nullable', 'string', 'max:2']; street/complement/neighborhood/city ['nullable', 'string', 'max:255']; number ['nullable', 'string', 'max:20']; state ['nullable', 'string', 'size:2']; zip_code ['nullable', 'string', 'size:8']; email ['nullable', 'email', 'max:255']; phone ['nullable', 'string', 'max:11'].
         messages(): `'cnpj.unique' => 'Já existe empresa cadastrada com este CNPJ.'`.
       - CompanyController (`php artisan make:controller Portal/CompanyController --no-interaction`):
         `create()`: `Inertia::render('portal/empresas/cadastrar', ['cnpjLookupEnabled' => Settings::enabled('cnpj_lookup')]);`
         `store(StoreCompanyRequest $request)`: usuário efetivo = `app(CurrentRepresentation::class)->grantor() ?? $request->user();` `DB::transaction(function () use (...) { $company = Company::create([...$request->validated(), 'source' => CompanySource::Manual]); $company->links()->create(['user_id' => $effectiveUser->id, 'role' => CompanyLinkRole::Responsavel, 'started_at' => now()]); });` → `redirect()->route('portal.empresas.index')->with('status', 'Empresa cadastrada com sucesso.');` (auditoria created/HasAuditoria é automática; vínculo também gera created — ambos enriquecidos com acting_for pelo RecordActivityAction).
       - routes/portal.php — adicionar no grupo lgpd+ResolveRepresentation, com comentário "rotas literais ANTES de empresas/{company} (03-05)":
         ```php
         Route::get('empresas', [CompanyController::class, 'index'])->name('empresas.index');
         Route::get('empresas/cadastrar', [CompanyController::class, 'create'])->name('empresas.create');
         Route::post('empresas', [CompanyController::class, 'store'])->name('empresas.store');
         ```
         (index pode retornar resposta mínima `Inertia::render('portal/empresas/index', [...])` provisória — implementação completa na Task 2; para RED/GREEN limpos, implementar a Task 2 logo em seguida no mesmo plano.)
       Rodar filtro até verde.

    3. REFACTOR — pint.

    NOTA para os testes Inertia deste plano: as páginas React só nascem no 03-07 (wave 6) — asserts usam `assertInertia(fn (Assert $page) => $page->has(...)/where(...))` SEM `->component()` (a checagem de componente exige arquivo de página existente; registrar essa decisão no SUMMARY para o 03-07 adicionar a verificação visual).
  </action>
  <verify>php artisan test --compact --filter=CompanyRegistrationTest && php artisan route:list --name=empresas</verify>
  <acceptance_criteria>
    - Filtro CompanyRegistrationTest verde (8 testes).
    - `php artisan route:list --name=empresas` lista empresas.index/create/store/consultar-cnpj.
    - `grep "DB::transaction" app/Http/Controllers/Portal/CompanyController.php` presente.
    - `grep "Já existe empresa cadastrada com este CNPJ." app/Http/Requests/Portal/StoreCompanyRequest.php` presente.
    - `grep "grantor()" app/Policies/CompanyPolicy.php` presente (representação integrada).
  </acceptance_criteria>
  <done>Cadastro transacional com vínculo responsável, CNPJ único/normalizado e representação respeitada — auditado de ponta a ponta.</done>
</task>

<task type="auto">
  <name>Task 2: Listagem "Minhas empresas" server-driven com representação e paginação parametrizada (HU-027)</name>
  <files>app/Http/Controllers/Portal/CompanyController.php, app/Models/Company.php, tests/Feature/Companies/MyCompaniesTest.php</files>
  <read_first>
    - app/Http/Controllers/Gestao/CnaeController.php — método index (padrão server-driven 2.4: whitelists SORTABLE_COLUMNS/PER_PAGE_OPTIONS, whereLike caseSensitive false, filters ecoados, perPageOptions)
    - .planning/phases/03-cadastro-empresarial/03-RESEARCH.md — Pitfall 9 (anti-N+1) e "Padrão 4 — Telas" (colunas esperadas pela UI)
    - app/Support/Settings.php (Settings::get para ui.companies.per_page)
  </read_first>
  <action>
    TDD estrito.

    1. RED — criar tests/Feature/Companies/MyCompaniesTest.php (mesmo setUp/helper). Testes:
       - test_lista_somente_empresas_do_usuario (CA-01): userA com 2 empresas vinculadas (factory withPrimaryCnae + CompanyUser factory), userB com 1; actingAs(userA)->get('/portal/empresas') → assertInertia has('companies.data', 2) e cada item com chaves id, legal_name, formatted_cnpj, source{value,label}, primary_cnae, link{role_label, active}.
       - test_item_traz_cnae_principal_e_situacao_do_vinculo: empresa com primaryCnae → where('companies.data.0.primary_cnae.formatted_code', ...) e where('companies.data.0.link.active', true).
       - test_vinculo_encerrado_aparece_com_situacao_encerrado: CompanyUserFactory ended() → item presente com link.active false (histórico preservado — HU-028 CA conexo).
       - test_busca_filtra_por_razao_social_sem_case (CA-01): empresas 'Padaria Central' e 'Mercado Sul'; get('/portal/empresas?search=padaria') → 1 resultado (whereLike caseSensitive false — fix PostgreSQL [2.4]).
       - test_busca_por_cnpj_com_digitos: search '00.000.000' → encontra a empresa de cnpj 00000000000191 (branch de dígitos vira prefixo de cnpj).
       - test_paginacao_e_parametrizada (CA-03): seedar ParameterSeeder; `Parameter::where('key', 'ui.companies.per_page')->first()->update(['value' => '5']);` criar 6 empresas vinculadas → assertInertia has('companies.data', 5) e where('companies.per_page', 5) (paginator serializado com chaves top-level from/to/total/per_page — shape consumido pelo ui/pagination.tsx).
       - test_visitante_e_redirecionado (CA-04): get sem auth → redirect /portal/login.
       - test_procurador_em_representacao_ve_empresas_do_representado: empresa do grantor; actingAs(attorney)+withSession(acting_procuration_id) → 1 item; SEM a sessão → 0 itens (CA-04/Pitfall 5).
       - test_consulta_propria_nao_exige_log_adicional (CA-02): GET é consulta às PRÓPRIAS empresas — sem activity obrigatória além do padrão; assert apenas resposta 200 com props (documentar no teste o porquê, padrão HU-027 do RESEARCH).
       Confirmar RED.

    2. GREEN:
       - Company model: adicionar `public static function countForUser(User $user): int` → `static::query()->whereHas('links', fn ($q) => $q->where('user_id', $user->id)->whereNull('ended_at'))->count();` (contagem reutilizável — o painel do cidadão consome na evolução; lacuna registrada no STATE).
       - CompanyController::index(Request $request): usuário efetivo (grantor ?? user). Constantes privadas SORTABLE_COLUMNS = ['legal_name'] e PER_PAGE_OPTIONS = [10, 15, 25, 50] (whitelists técnicas, padrão CnaeController). perPage: in_array(per_page) ? request : `(int) Settings::get('ui.companies.per_page', 15)`. Query:
         ```php
         Company::query()
             ->whereHas('links', fn ($q) => $q->where('user_id', $effectiveUser->id))
             ->with([
                 'cnaes' => fn ($q) => $q->wherePivot('is_primary', true),
                 'links' => fn ($q) => $q->where('user_id', $effectiveUser->id)->latest('started_at'),
             ])
             ->when($search !== '', function ($query) use ($search) {
                 $digits = preg_replace('/\D/', '', $search);
                 $query->where(function ($inner) use ($search, $digits) {
                     if ($digits !== '') { $inner->where('cnpj', 'like', "{$digits}%"); }
                     $inner->orWhereLike('legal_name', "%{$search}%", caseSensitive: false)
                           ->orWhereLike('trade_name', "%{$search}%", caseSensitive: false);
                 });
             })
             ->orderBy($sort, $direction)->orderBy('legal_name')
             ->paginate($perPage)->withQueryString()
             ->through(fn (Company $company) => [
                 'id' => $company->id,
                 'legal_name' => $company->legal_name,
                 'trade_name' => $company->trade_name,
                 'formatted_cnpj' => $company->formatted_cnpj,
                 'source' => ['value' => $company->source->value, 'label' => $company->source->label()],
                 'primary_cnae' => ($cnae = $company->cnaes->first()) ? ['formatted_code' => $cnae->formatted_code, 'description' => $cnae->description] : null,
                 'link' => ($link = $company->links->first()) ? ['role' => $link->role->value, 'role_label' => $link->role->label(), 'active' => $link->ended_at === null, 'started_at' => $link->started_at?->toDateString(), 'ended_at' => $link->ended_at?->toDateString()] : null,
             ]);
         ```
         Props: companies, filters (search/sort/direction/per_page ecoados), perPageOptions, totalCompanies (Company::countForUser($effectiveUser)). Render 'portal/empresas/index'.
       Rodar filtro até verde.

    3. REFACTOR — pint; conferir ausência de N+1 (with carregado, nenhuma relação lazy no through).
  </action>
  <verify>php artisan test --compact --filter=MyCompaniesTest</verify>
  <acceptance_criteria>
    - Filtro MyCompaniesTest verde (9 testes).
    - `grep "caseSensitive: false" app/Http/Controllers/Portal/CompanyController.php` presente (padrão 2.4 obrigatório).
    - `grep "countForUser" app/Models/Company.php` presente.
    - `grep "wherePivot('is_primary', true)" app/Http/Controllers/Portal/CompanyController.php` presente (eager constraint anti-N+1).
    - Representação coberta nos DOIS sentidos (com e sem sessão) nos testes.
  </acceptance_criteria>
  <done>Listagem do portal pronta para a DataTable do 03-07, com representação, busca, ordenação e paginação parametrizada.</done>
</task>

</tasks>

<verification>
- `php artisan test --compact tests/Feature/Companies` — grupo da fase verde até aqui (ESCOPADO; full-suite só no 03-09).
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan route:list --path=portal/empresas` coerente (index/cadastrar/store/consultar-cnpj).
</verification>

<success_criteria>
- HU-023 CA-01..04 e HU-027 CA-01..04 cobertos por testes nomeados em pt-BR.
- Vínculo nasce na mesma transação do cadastro; representação cria para o representado e lista as empresas dele.
- Nenhum valor de negócio hardcoded (per_page parametrizado; whitelists são constantes técnicas).
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/03-cadastro-empresarial/03-04-SUMMARY.md` registrando: shape exato das props do index (contrato da tela 03-07), decisão "asserts sem ->component() até a UI existir" e assinaturas da policy.
</output>

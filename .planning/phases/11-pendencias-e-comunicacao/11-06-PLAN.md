---
phase: 11-pendencias-e-comunicacao
plan: 06
type: execute
wave: 3
depends_on: ["11-03", "11-04"]
files_modified:
  - app/Listeners/NotificarResultadoExpresso.php
  - app/Notifications/ResultadoExpressoNotification.php
  - tests/Feature/Expresso/NotificarResultadoExpressoListenerTest.php
  - tests/Feature/Expresso/ResultadoExpressoNotificationTest.php
autonomous: true
requirements: [HU-094, HU-096]

must_haves:
  truths:
    - "A notificação de resultado do fluxo expresso (HU-077, Fase 9) passa a percorrer o NotificationDispatcher — ganha in-app + histórico (communications) além do e-mail, unificando as notificações de processo"
    - "ResultadoExpressoNotification implementa ProcessNotification (communicationType = resultado; via() dinâmico; toDatabase() adicionado) mantendo o toMail() com os assuntos parametrizados (expresso.notificacao.assunto_*)"
    - "O toggle de tipo features.notificacao_resultado_expresso permanece como porta de entrada (off → não notifica + audita 'desativado'); os CANAIS passam a ser resolvidos pelo dispatcher (mapa ∩ toggles de canal)"
    - "ANTI-REGRESSÃO Fase 9: os testes seguem VERDES, migrados para asserir via dispatcher/communications (não mais email_logs); a notificação continua sendo enviada ao requerente EXATAMENTE 1× (auto-descoberta), incl. ExpressoSmokeTest"
    - "O listener permanece AUTO-DESCOBERTO em ResultadoEmitido (type-hint; NUNCA Event::listen); a decisão/auditoria autoritativa da Fase 9 NÃO depende dele e não muda"
  artifacts:
    - path: "app/Listeners/NotificarResultadoExpresso.php"
      provides: "Listener refatorado: degradação por toggle de tipo, depois deliver() pelo dispatcher"
      contains: "NotificationDispatcher"
    - path: "app/Notifications/ResultadoExpressoNotification.php"
      provides: "ProcessNotification (resultado) com via() dinâmico + toDatabase + toMail parametrizado"
      contains: "ProcessNotification"
  key_links:
    - from: "app/Listeners/NotificarResultadoExpresso.php"
      to: "app/Services/Comunicacao/NotificationDispatcher.php"
      via: "após o gate do toggle de tipo, deliver(requester, ResultadoExpressoNotification)"
      pattern: "deliver"
    - from: "app/Notifications/ResultadoExpressoNotification.php"
      to: "app/Notifications/Contracts/ProcessNotification.php"
      via: "implementa o contrato (resultado) — via() lê canais congelados"
      pattern: "communicationType"
---

<objective>
Unifica a notificação de resultado do fluxo expresso (HU-077) no pipeline do EP11 (Wave 3, par paralelo com 11-05 e 11-07): `NotificarResultadoExpresso` passa a usar o `NotificationDispatcher` (ganha in-app + histórico) e `ResultadoExpressoNotification` vira `ProcessNotification` (via dinâmico + toDatabase), mantendo o e-mail com assuntos parametrizados. ANTI-REGRESSÃO: os testes da Fase 9 migram para asserir via dispatcher/communications e seguem verdes.

Purpose: o resultado expresso era a única notificação de processo que ia só por e-mail (EmailLog). Para não ter dupla verdade e para o cidadão ver o desfecho também no in-app e no histórico (HU-096), ela passa pelo mesmo roteador das demais. O toggle de TIPO (`features.notificacao_resultado_expresso`) continua sendo a chave liga/desliga do aviso; os CANAIS passam a sair do `mapa_canais` ∩ toggles de canal. O maior risco é regressão nos testes da Fase 9 — eles migram (cobertura mantida), não somem.
Output: NotificarResultadoExpresso + ResultadoExpressoNotification refatorados; NotificarResultadoExpressoListenerTest + ResultadoExpressoNotificationTest migrados; ExpressoSmokeTest verde.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-pendencias-e-comunicacao/11-CONTEXT.md
@docs/superpowers/specs/2026-06-15-pendencias-comunicacao-design.md
@.planning/phases/11-pendencias-e-comunicacao/11-04-SUMMARY.md

# ALVOS DO REFACTOR + PADRÕES — ler antes de codar:
@app/Listeners/NotificarResultadoExpresso.php
@app/Notifications/ResultadoExpressoNotification.php
@app/Notifications/Contracts/ProcessNotification.php
@tests/Feature/Expresso/NotificarResultadoExpressoListenerTest.php
@tests/Feature/Expresso/ResultadoExpressoNotificationTest.php
@tests/Feature/Expresso/ExpressoSmokeTest.php
@docs/SILE_HUs_Completas_MD/EP11-Pendências-e-Comunicacao/HU-094-Enviar-e-mail.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ResultadoExpressoNotification → ProcessNotification (via dinâmico + toDatabase)</name>
  <read_first>app/Notifications/ResultadoExpressoNotification.php, app/Notifications/Contracts/ProcessNotification.php, tests/Feature/Expresso/ResultadoExpressoNotificationTest.php</read_first>
  <files>app/Notifications/ResultadoExpressoNotification.php, tests/Feature/Expresso/ResultadoExpressoNotificationTest.php</files>
  <action>
    TDD estrito (RED primeiro). `ResultadoExpressoNotification` implementa `ProcessNotification`:
    - `communicationType()` = Resultado; `viabilityRequestId()` = id do processo (adicionar ao construtor se ainda não houver — hoje recebe protocolNumber + outcome; passar também o viabilityRequestId).
    - `via()` DINÂMICO (lê canais congelados pelo dispatcher; fallback ['mail']).
    - adicionar `toDatabase()` (título/resumo do desfecho + link de acompanhamento do portal).
    - manter `toMail()` com os assuntos parametrizados (`expresso.notificacao.assunto_deferida/_indeferida`) e o texto honesto SEM anexo (RN-004). Manter ShouldQueue + failed().

    MIGRAR `ResultadoExpressoNotificationTest`: ajustar a construção da Notification (novo parâmetro viabilityRequestId) e adicionar asserção de `toDatabase()` (estrutura do payload) e de `via()` dinâmico (com canais congelados retorna-os; sem, ['mail']). Manter as asserções de assunto/sem-anexo.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=ResultadoExpressoNotificationTest</verify>
  <acceptance_criteria>
    - grep "ProcessNotification" e "toDatabase" em app/Notifications/ResultadoExpressoNotification.php.
    - ResultadoExpressoNotificationTest verde (via dinâmico, toDatabase, assuntos parametrizados, sem anexo).
  </acceptance_criteria>
  <done>ResultadoExpressoNotification é ProcessNotification multicanal mantendo o e-mail parametrizado e sem anexo.</done>
</task>

<task type="auto">
  <name>Task 2: NotificarResultadoExpresso → dispatcher (migrar listener test + manter smoke verde)</name>
  <read_first>app/Listeners/NotificarResultadoExpresso.php (gate do toggle + EmailLog::create + notify direto), app/Services/Comunicacao/NotificationDispatcher.php (11-04), tests/Feature/Expresso/NotificarResultadoExpressoListenerTest.php, tests/Feature/Expresso/ExpressoSmokeTest.php</read_first>
  <files>app/Listeners/NotificarResultadoExpresso.php, tests/Feature/Expresso/NotificarResultadoExpressoListenerTest.php</files>
  <action>
    TDD estrito. Refatorar `NotificarResultadoExpresso::handle(ResultadoEmitido)` (segue AUTO-DESCOBERTO, type-hint; NUNCA Event::listen):
    - manter o gate do toggle de TIPO `features.notificacao_resultado_expresso`: off → audita 'notificacoes'/'resultado'/'desativado' e retorna (degradação comunicada). 
    - manter a degradação 'sem-destinatario' (sem requerente com e-mail/contato) → audita e retorna.
    - SUBSTITUIR o `EmailLog::create(...)` + `notify(...)` direto por `NotificationDispatcher::deliver($requester, new ResultadoExpressoNotification($protocolo, $outcome, $request->id))`. Os canais (e o communications) passam a ser do dispatcher; remover o uso de EmailLog para este fluxo (imports órfãos limpos).

    MIGRAR `NotificarResultadoExpressoListenerTest`:
    - `test_notifica_o_requerente_uma_unica_vez`: trocar a asserção `email_logs` por `assertDatabaseHas('communications', [type resultado, status na_fila])`; manter `Notification::assertSentToTimes(..., 1)` (contagem — auto-descoberta).
    - `test_toggle_desligado_nao_envia_e_audita_a_degradacao`: permanece (toggle de tipo off → 'desativado' + nada enviado).
    - `test_indeferimento_usa_o_assunto_de_indeferida` e `test_sem_destinatario...`: manter (ajustar só a construção/asserção de canal se necessário).

    ANTI-REGRESSÃO `ExpressoSmokeTest`: ele usa `Notification::fake()` + `assertSentTo($requerente, ResultadoExpressoNotification::class)` e auditoria — como o dispatcher ainda chama `$requester->notify(...)`, deve seguir VERDE sem mudança; rodar para confirmar e ajustar SOMENTE se quebrar.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="NotificarResultadoExpressoListenerTest|ExpressoSmokeTest"</verify>
  <acceptance_criteria>
    - grep "NotificationDispatcher" em app/Listeners/NotificarResultadoExpresso.php; SEM EmailLog::create para este fluxo.
    - NotificarResultadoExpressoListenerTest MIGRADO verde (communications em vez de email_logs; 1×; toggle off → desativado) e ExpressoSmokeTest verde.
  </acceptance_criteria>
  <done>Resultado expresso unificado no dispatcher (in-app + histórico), 1× por auto-descoberta, com toggle de tipo preservado e smoke da Fase 9 verde.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-094/096 — resultado expresso ganha in-app + histórico (dispatcher) | NotificarResultadoExpressoListenerTest (communications) |
| HU-077 RN-005 — assuntos parametrizados, e-mail sem anexo | ResultadoExpressoNotificationTest |
| HU-014 — toggle de tipo off degrada comunicado ('desativado') | NotificarResultadoExpressoListenerTest |
| ANTI-REGRESSÃO Fase 9 — notificação 1×, smoke verde | NotificarResultadoExpressoListenerTest + ExpressoSmokeTest |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="ResultadoExpressoNotificationTest|NotificarResultadoExpressoListenerTest|ExpressoSmokeTest"` verde.
- ANTI-REGRESSÃO: rodar também o grupo do fluxo expresso relacionado a ResultadoEmitido (`php artisan test --compact tests/Feature/Expresso/`) e confirmar verde — o listener segue único e a decisão da Fase 9 não mudou.
- Suíte completa CONTINUA verde.
</verification>

<success_criteria>
- Notificação de resultado unificada no dispatcher (in-app + histórico), mantendo toggle de tipo e e-mail parametrizado sem anexo.
- Testes da Fase 9 migrados (communications) e verdes; notificação 1× (auto-descoberta); ExpressoSmokeTest verde.
- ZERO dupla verdade, ZERO dependência nova; decisão/auditoria da Fase 9 intactas.
</success_criteria>

<output>
Criar `.planning/phases/11-pendencias-e-comunicacao/11-06-SUMMARY.md`: o que mudou no listener/Notification, o novo parâmetro do construtor (viabilityRequestId), o tipo de Communication ('resultado') e quais testes da Fase 9 foram migrados. Insumo direto de 11-08 (histórico/central exibem o resultado) e 11-10 (verificação anti-regressão).
</output>

---
phase: 11-pendencias-e-comunicacao
plan: 08
type: execute
wave: 4
depends_on: ["11-05", "11-06", "11-07"]
files_modified:
  - app/Http/Controllers/NotificationCenterController.php
  - app/Http/Controllers/ComunicacaoHistoricoController.php
  - app/Http/Resources/NotificationResource.php
  - app/Http/Resources/CommunicationResource.php
  - app/Http/Middleware/HandleInertiaRequests.php
  - routes/portal.php
  - routes/gestao.php
  - tests/Feature/Comunicacao/NotificationCenterTest.php
  - tests/Feature/Comunicacao/ComunicacaoHistoricoTest.php
autonomous: true
requirements: [HU-090, HU-096]

must_haves:
  truths:
    - "HU-090 central in-app: o usuário (portal e gestão) lista suas notificações (não-lidas + lidas, paginadas), marca uma como lida e marca todas como lidas — lendo o canal database NATIVO (notifications), com escopo do dono (NUNCA vê notificação de outro)"
    - "O badge do sininho tem dado real: HandleInertiaRequests compartilha a contagem de não-lidas do usuário autenticado (shared prop), avaliada na serialização"
    - "HU-096 histórico unificado: por processo, lista as communications de TODOS os canais (email/in_app/whatsapp) e tipos (pendencia_*/prazo_vencendo/escalonamento_sla/resultado) com status honesto — fonte de verdade única (não EmailLog), ordenado por data"
    - "Escopo do histórico: no portal só o dono/representado do processo (policy view); na gestão, gated por consultar-solicitacoes; a consulta é AUDITADA (RN-002); LGPD respeitada (sem dado sensível indevido)"
    - "É o ÚNICO editor de routes/portal.php, routes/gestao.php e HandleInertiaRequests na Fase 11 (sem permissão nova: central é por dono; histórico reusa consultar-solicitacoes/escopo)"
  artifacts:
    - path: "app/Http/Controllers/NotificationCenterController.php"
      provides: "index/markAsRead/markAllAsRead sobre o canal database do usuário autenticado (portal+gestão)"
      contains: "markAsRead"
    - path: "app/Http/Controllers/ComunicacaoHistoricoController.php"
      provides: "HU-096: histórico unificado de communications por processo (escopo + auditoria)"
      contains: "communications"
    - path: "app/Http/Middleware/HandleInertiaRequests.php"
      provides: "Shared prop da contagem de não-lidas (badge do sininho)"
      contains: "unread"
  key_links:
    - from: "app/Http/Controllers/NotificationCenterController.php"
      to: "App\\Models\\User (notifications/unreadNotifications)"
      via: "lê e marca o canal database nativo do usuário autenticado (escopo do dono)"
      pattern: "unreadNotifications|markAsRead"
    - from: "app/Http/Controllers/ComunicacaoHistoricoController.php"
      to: "app/Models/Communication.php"
      via: "query por viability_request_id (índice) com escopo de permissão/dono + auditoria da consulta"
      pattern: "Communication::"
---

<objective>
Backend + rotas da central in-app e do histórico unificado (Wave 4; ÚNICO route-owner de `portal.php`/`gestao.php` e editor de `HandleInertiaRequests` na fase). `NotificationCenterController` (index/markAsRead/markAllAsRead) sobre o canal `database` nativo, com a contagem de não-lidas como shared prop (badge); e `ComunicacaoHistoricoController` (HU-096) servindo o histórico unificado de `communications` por processo, com escopo de dono/permissão e auditoria.

Purpose: a Fase 11 já produz notificações in-app reais (Wave 3) e o ledger `communications` (Waves 2-3 + scheduler 11-07). Falta a superfície de leitura: a central de notificações (lidas/não-lidas, marcar) e o histórico por processo (HU-096), que é a fonte de verdade única (não junta EmailLog/notifications de forma frágil). Sem permissão nova: a central é do próprio usuário (escopo do dono) e o histórico reusa `consultar-solicitacoes` (gestão) / policy view (portal). Este plano concentra TODAS as rotas e o shared prop da fase — os pares paralelos das ondas anteriores não tocaram rotas.
Output: 2 controllers + 2 Resources + rotas (portal+gestão) + shared prop, cobertos por feature tests (escopo, marcar lida, histórico unificado, auditoria).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-pendencias-e-comunicacao/11-CONTEXT.md
@docs/superpowers/specs/2026-06-15-pendencias-comunicacao-design.md
@.planning/phases/11-pendencias-e-comunicacao/11-01-SUMMARY.md
@.planning/phases/11-pendencias-e-comunicacao/11-05-SUMMARY.md

# PADRÕES A ESPELHAR (controller+resource+rotas+shared prop+auditoria) — ler antes de codar:
@app/Http/Controllers/Gestao/EmailLogController.php
@app/Http/Middleware/HandleInertiaRequests.php
@routes/portal.php
@routes/gestao.php
@app/Support/Audit/AuditService.php
@docs/SILE_HUs_Completas_MD/EP11-Pendências-e-Comunicacao/HU-090-Notificar-pendência.md
@docs/SILE_HUs_Completas_MD/EP11-Pendências-e-Comunicacao/HU-096-Consultar-historico-de-comunicacões.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotificationCenterController (index/markAsRead/markAllAsRead) + shared prop + rotas portal/gestão</name>
  <read_first>app/Http/Middleware/HandleInertiaRequests.php (share), routes/portal.php (grupo auth:web,verified,lgpd.accepted), routes/gestao.php (grupo auth:gestao,acessar-gestao,lgpd.accepted)</read_first>
  <files>app/Http/Controllers/NotificationCenterController.php, app/Http/Resources/NotificationResource.php, app/Http/Middleware/HandleInertiaRequests.php, routes/portal.php, routes/gestao.php, tests/Feature/Comunicacao/NotificationCenterTest.php</files>
  <action>
    TDD estrito (RED primeiro). `NotificationCenterController`:
    - `index`: notificações do `$request->user()` (canal database nativo) — não-lidas + lidas, paginadas (per_page parametrizável, à discrição reusar um ui.* ou constante), via `NotificationResource` (id, type, data, read_at, created_at). Server-driven (Inertia props), página em 11-09.
    - `markAsRead({notification})`: marca UMA como lida (valida que pertence ao usuário — anti-IDOR); `markAllAsRead`: marca todas.
    Registrar rotas nos DOIS ambientes (ÚNICO route-owner da fase):
    - portal.php (grupo auth:web, verified, lgpd.accepted): `GET portal/notificacoes` (index) + `POST portal/notificacoes/{notification}/ler` + `POST portal/notificacoes/ler-todas`, names `portal.notificacoes.*`.
    - gestao.php (grupo auth:gestao, acessar-gestao, lgpd.accepted): `GET gestao/notificacoes` + ações análogas, names `gestao.notificacoes.*`.
    Shared prop em `HandleInertiaRequests::share`: `notificacoes => ['nao_lidas' => fn () => $request->user()?->unreadNotifications()->count() ?? 0]` (closure — avaliada na serialização; só com usuário autenticado).

    `NotificationCenterTest` (SQLite): cria notificações database para o usuário; index lista (paginado, não-lidas/lidas); markAsRead grava read_at; markAllAsRead zera; ESCOPO: usuário A não lê/marca notificação de B (403/404); shared prop reflete a contagem de não-lidas. Cobrir portal e gestão (guards web e gestao).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=NotificationCenterTest</verify>
  <acceptance_criteria>
    - grep "notificacoes" em routes/portal.php e routes/gestao.php; grep "unread" em app/Http/Middleware/HandleInertiaRequests.php.
    - NotificationCenterTest verde (index, markAsRead/markAllAsRead, escopo do dono, shared prop) nos dois guards.
  </acceptance_criteria>
  <done>Central in-app real: usuário lista e marca suas notificações (portal+gestão), badge com contagem real; escopo do dono garantido.</done>
</task>

<task type="auto">
  <name>Task 2: HU-096 ComunicacaoHistoricoController (histórico unificado por processo) + Resource + rotas</name>
  <read_first>app/Models/Communication.php (índice viability_request_id,created_at), app/Support/Audit/AuditService.php, routes/portal.php (solicitacoes/{solicitacao} — policy view), routes/gestao.php (processos — consultar-solicitacoes)</read_first>
  <files>app/Http/Controllers/ComunicacaoHistoricoController.php, app/Http/Resources/CommunicationResource.php, routes/portal.php, routes/gestao.php, tests/Feature/Comunicacao/ComunicacaoHistoricoTest.php</files>
  <action>
    TDD estrito. `ComunicacaoHistoricoController@show(ViabilityRequest)`: lista as `communications` do processo (ordenado por created_at desc, índice composto), de TODOS os canais/tipos, com status honesto, via `CommunicationResource` (channel, type, status, title, summary, recipient, timestamps; error_message só na gestão). É a fonte de verdade ÚNICA (não junta EmailLog/notifications). AUDITA a consulta ('notificacoes'/'historico-consultado', RN-002).
    Escopo:
    - portal: rota `GET portal/solicitacoes/{solicitacao}/comunicacoes` no grupo do dono — gate pela policy `view` (dono/representado); LGPD (sem expor dado sensível indevido; error_message oculto no portal).
    - gestão: rota `GET gestao/processos/{viabilityRequest}/comunicacoes` gated por `permission:consultar-solicitacoes` (já existe — SEM permissão nova).

    `ComunicacaoHistoricoTest` (SQLite): cria communications de tipos/canais variados para um processo; 
    - gestão (consultar-solicitacoes) → lista unificada (ordem por data, todos os canais), auditoria gravada; sem a permissão → 403.
    - portal: dono lista; NÃO-dono → 403; error_message não aparece no payload do portal.
    - integra com o que as Waves 2-3/07 gravaram (ex.: uma pendencia_aberta + um resultado aparecem juntos).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=ComunicacaoHistoricoTest</verify>
  <acceptance_criteria>
    - grep "comunicacoes" em routes/portal.php e routes/gestao.php; grep "Communication::" em app/Http/Controllers/ComunicacaoHistoricoController.php.
    - ComunicacaoHistoricoTest verde (unificado por processo, escopo portal/gestão, auditoria, LGPD).
  </acceptance_criteria>
  <done>HU-096 real: histórico unificado de comunicações por processo, fonte única (communications), escopo de dono/permissão e auditado.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-090 — central in-app (lidas/não-lidas, marcar) com escopo do dono | NotificationCenterTest |
| HU-090 — badge do sininho com contagem real (shared prop) | NotificationCenterTest |
| HU-096 — histórico unificado por processo (todos canais/tipos, fonte única) | ComunicacaoHistoricoTest |
| HU-096 RN — escopo (portal dono / gestão consultar-solicitacoes) + auditoria + LGPD | ComunicacaoHistoricoTest |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="NotificationCenterTest|ComunicacaoHistoricoTest"` verde.
- `php artisan route:list --path=notificacoes` e `--path=comunicacoes` listam as rotas (portal + gestão).
- Suíte completa CONTINUA verde; ÚNICO editor de routes/portal.php, routes/gestao.php e HandleInertiaRequests na Fase 11; permissões seguem 24.
</verification>

<success_criteria>
- Central in-app navegável (backend): listar/marcar notificações nos dois ambientes, badge real; escopo do dono.
- HU-096: histórico unificado por processo (fonte única communications), escopo de dono/permissão e auditado, sem permissão nova.
- ZERO dependência nova; rotas e shared prop concentrados neste plano (sem conflito com pares paralelos).
</success_criteria>

<output>
Criar `.planning/phases/11-pendencias-e-comunicacao/11-08-SUMMARY.md`: as rotas (names) da central e do histórico nos dois ambientes, o shape de NotificationResource/CommunicationResource, a shared prop do badge e os pontos de Inertia props que a UI (11-09) consome. Insumo direto de 11-09 (frontend) e 11-10 (smoke navegável).
</output>

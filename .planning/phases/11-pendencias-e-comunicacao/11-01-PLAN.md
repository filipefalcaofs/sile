---
phase: 11-pendencias-e-comunicacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/XXXX_XX_XX_XXXXXX_create_notifications_table.php
  - database/migrations/XXXX_XX_XX_XXXXXX_create_communications_table.php
  - app/Models/Communication.php
  - app/Enums/CommunicationChannel.php
  - app/Enums/CommunicationType.php
  - app/Enums/CommunicationStatus.php
  - app/Notifications/Contracts/ProcessNotification.php
  - database/factories/CommunicationFactory.php
  - tests/Feature/Comunicacao/DatabaseNotificationChannelTest.php
  - tests/Feature/Comunicacao/CommunicationModelTest.php
autonomous: true
requirements: [HU-090, HU-094, HU-096]

must_haves:
  truths:
    - "O canal database NATIVO de Notifications existe: a tabela notifications está migrada e um User (já Notifiable) recebe uma notificação database, que aparece em unreadNotifications e é marcável como lida (read_at) — base da central in-app (HU-090)"
    - "Existe o ledger imutável communications (HU-096): tabela DEDICADA por processo (viability_request_id nullable+indexado), com channel (email/in_app/whatsapp), type (pendencia_aberta/pendencia_respondida/pendencia_expirada/prazo_vencendo/escalonamento_sla/resultado), status HONESTO (na_fila/enviado/falhou/bloqueado/desativado), recipient_user_id, title/summary, error_message, meta jsonb e os carimbos queued_at/sent_at/failed_at"
    - "O Communication espelha o EmailLog: markAsSent/markAsFailed e os marcadores honestos markAsBlocked/markAsDisabled mudam status + carimbo, sem nunca registrar 'enviado' fictício"
    - "Existe o contrato ProcessNotification (viabilityRequestId, communicationType, channels) que as Notifications de PROCESSO implementam — o dispatcher (11-04) congela os canais e o via() os lê"
    - "EmailLog PERMANECE intacto (e-mails de CONTA: verificação/senha); communications é a fonte de verdade só das comunicações de PROCESSO — sem dupla verdade"
  artifacts:
    - path: "database/migrations/XXXX_XX_XX_XXXXXX_create_communications_table.php"
      provides: "Ledger communications com índices (viability_request_id,type,channel) e (viability_request_id,created_at)"
      contains: "viability_request_id"
    - path: "app/Models/Communication.php"
      provides: "Model do ledger + markAsSent/markAsFailed/markAsBlocked/markAsDisabled (espelha EmailLog)"
      contains: "markAsBlocked"
    - path: "app/Notifications/Contracts/ProcessNotification.php"
      provides: "Contrato das Notifications de processo (viabilityRequestId/communicationType/channels)"
      contains: "communicationType"
  key_links:
    - from: "app/Models/Communication.php"
      to: "communications (migration)"
      via: "casts dos enums channel/type/status + meta jsonb e carimbos datetime"
      pattern: "CommunicationStatus"
    - from: "tests/Feature/Comunicacao/DatabaseNotificationChannelTest.php"
      to: "App\\Models\\User (Notifiable)"
      via: "notify() no canal database grava em notifications; unreadNotifications/markAsRead"
      pattern: "database"
---

<objective>
Fundação dos três mecanismos de comunicação do EP11, SEM tocar regra de negócio existente (Wave 1, par paralelo com 11-02): (1) habilitar o canal `database` nativo de Notifications (`make:notifications-table`) — base da central in-app; (2) criar o ledger DEDICADO e imutável `communications` (model + enums + factory) — fonte de verdade do histórico por processo (HU-096) e do controle de idempotência das rotinas (Wave 5); (3) declarar o contrato `ProcessNotification` que as Notifications de processo implementarão.

Purpose: o ciclo de pendência (Fase 10) já funciona; a Fase 11 generaliza/ativa a comunicação. Antes de qualquer canal/roteador (Wave 2), precisamos da infraestrutura honesta: tabela de in-app real, ledger por processo (não uma view frágil sobre `EmailLog`/`notifications`) e o contrato que destrava o congelamento de canais no disparo. `EmailLog` PERMANECE para e-mails de CONTA (verificação/senha) — `communications` é só de PROCESSO, evitando dupla verdade.
Output: migrations (`notifications` + `communications`), `Communication` (model + enums + factory + marcadores honestos), contrato `ProcessNotification`, cobertos por feature tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-pendencias-e-comunicacao/11-CONTEXT.md
@docs/superpowers/specs/2026-06-15-pendencias-comunicacao-design.md

# PADRÕES A ESPELHAR (model de ledger + marcadores + factory) — ler antes de codar:
@app/Models/EmailLog.php
@app/Models/User.php
@app/Enums/AnalysisPendencyStatus.php
@docs/SILE_HUs_Completas_MD/EP11-Pendências-e-Comunicacao/HU-096-Consultar-historico-de-comunicacões.md
@docs/SILE_HUs_Completas_MD/EP11-Pendências-e-Comunicacao/HU-090-Notificar-pendência.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tabela notifications (canal database nativo) + smoke do in-app</name>
  <read_first>app/Models/User.php (já usa Notifiable), database/migrations (convenção de nomes/datas)</read_first>
  <files>database/migrations/XXXX_XX_XX_XXXXXX_create_notifications_table.php, tests/Feature/Comunicacao/DatabaseNotificationChannelTest.php</files>
  <action>
    TDD estrito (RED primeiro). Gerar a migration da tabela `notifications` com `php artisan make:notifications-table --no-interaction` (estrutura padrão do Laravel: uuid id, type, notifiable morph, data json, read_at). NÃO criar tabela própria — o in-app é o canal `database` NATIVO (decisão do CONTEXT/spec).

    `DatabaseNotificationChannelTest` (SQLite) — prova que o canal funciona de ponta a ponta com uma Notification de teste mínima (anônima/inline com `via()=['database']` e `toDatabase()`):
    - após `$user->notify(...)`, `assertDatabaseHas('notifications', [...])` e `$user->unreadNotifications()->count() === 1`.
    - `$user->unreadNotifications->first()->markAsRead()` zera o `unreadNotifications` e grava `read_at`.

    NÃO alterar o `User` (já é `Notifiable`, confirmado). Sem regra de negócio aqui — só a tabela + o smoke do mecanismo.
  </action>
  <verify>php artisan migrate --no-interaction && vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=DatabaseNotificationChannelTest</verify>
  <acceptance_criteria>
    - Existe a migration de `notifications` e `php artisan migrate` aplica sem erro (SQLite de teste).
    - DatabaseNotificationChannelTest verde: notify no canal database grava, conta como não-lida e markAsRead grava read_at.
  </acceptance_criteria>
  <done>Canal database nativo habilitado (tabela notifications) e provado: base real da central in-app (HU-090).</done>
</task>

<task type="auto">
  <name>Task 2: Ledger communications — migration + enums + model + factory + marcadores honestos</name>
  <read_first>app/Models/EmailLog.php (markAsSent/markAsFailed + casts), app/Enums/AnalysisPendencyStatus.php (estilo de enum)</read_first>
  <files>database/migrations/XXXX_XX_XX_XXXXXX_create_communications_table.php, app/Enums/CommunicationChannel.php, app/Enums/CommunicationType.php, app/Enums/CommunicationStatus.php, app/Models/Communication.php, database/factories/CommunicationFactory.php, tests/Feature/Comunicacao/CommunicationModelTest.php</files>
  <action>
    TDD estrito. Criar via Artisan (`make:migration`, `make:model Communication -f`, `make:enum`). O ledger é DEDICADO e imutável (espelha access_logs/viability_request_transitions) — NÃO é view sobre EmailLog/notifications (CONTEXT: EmailLog não tem viability_request_id; notifications é morph por usuário).

    Migration `communications`:
    - `viability_request_id` (FK nullable, índice), `recipient_user_id` (FK nullable),
    - `channel` (string — email/in_app/whatsapp), `type` (string), `status` (string),
    - `title`, `summary` (nullable), `error_message` (nullable), `meta` (json nullable),
    - `queued_at`/`sent_at`/`failed_at` (nullable timestamps), timestamps padrão.
    - Índices compostos: `(viability_request_id, type, channel)` (idempotência das rotinas da Wave 5) e `(viability_request_id, created_at)` (consulta do histórico HU-096).

    Enums (string-backed): `CommunicationChannel` (Email/InApp/Whatsapp), `CommunicationType` (PendenciaAberta/PendenciaRespondida/PendenciaExpirada/PrazoVencendo/EscalonamentoSla/Resultado — cobre todos os tipos das Waves 3/5 para que NENHUM plano posterior precise editar o enum), `CommunicationStatus` (NaFila/Enviado/Falhou/Bloqueado/Desativado). Valores em snake/lower honestos (na_fila/enviado/falhou/bloqueado/desativado).

    `Communication` (model): `$fillable` dos campos; `casts()` com os 3 enums + `meta` array + carimbos datetime; relações `viabilityRequest()` e `recipient()`. Marcadores espelhando EmailLog, cada um muda status + carimbo (NUNCA inventa "enviado"):
    - `markAsSent()` → status Enviado + sent_at. **GUARDA (defesa em profundidade anti-fachada):** NÃO sobrescrever um status terminal honesto já gravado (Bloqueado/Desativado) — se a linha já está Bloqueado/Desativado, markAsSent é no-op (evita que um NotificationSent tardio transforme um 'bloqueado' real em 'enviado' fictício). Teste essa guarda.
    - `markAsFailed(string $error)` → status Falhou + error_message + failed_at.
    - `markAsBlocked(?string $motivo = null)` → status Bloqueado (canal on mas indisponível — Wave 2/3) + error_message.
    - `markAsDisabled()` → status Desativado (toggle off — degradação honesta).
    `CommunicationFactory` com defaults sensatos (status NaFila, channel Email, type PendenciaAberta) e states por status/canal.

    `CommunicationModelTest` (SQLite):
    - cria via factory e confere casts (enums tipados, meta array).
    - markAsSent/markAsFailed/markAsBlocked/markAsDisabled mudam status + carimbo corretos (assert por status, sem nunca "enviado" indevido).
  </action>
  <verify>php artisan migrate --no-interaction && vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=CommunicationModelTest</verify>
  <acceptance_criteria>
    - grep "viability_request_id" e os dois índices compostos na migration de communications.
    - grep "markAsBlocked" e "markAsDisabled" em app/Models/Communication.php.
    - CommunicationModelTest verde (casts + 4 marcadores honestos).
  </acceptance_criteria>
  <done>Ledger communications real (model+enums+factory) com status honestos e índices de idempotência/consulta — fonte de verdade do histórico de processo.</done>
</task>

<task type="auto">
  <name>Task 3: Contrato ProcessNotification</name>
  <read_first>app/Notifications/PendenciaSolicitadaNotification.php, app/Notifications/ResultadoExpressoNotification.php</read_first>
  <files>app/Notifications/Contracts/ProcessNotification.php</files>
  <action>
    TDD leve (interface — a prova real vem no dispatcher 11-04 e nas migrações 11-05/06). Criar a interface `App\Notifications\Contracts\ProcessNotification` que as Notifications de PROCESSO implementarão:
    - `viabilityRequestId(): ?int` — processo alvo (nullable: nem toda comunicação é de processo).
    - `communicationType(): CommunicationType` — o tipo que vira a linha do ledger.
    - `channels(): array` / mecanismo de canais congelados — os canais resolvidos pelo dispatcher no disparo, lidos pelo `via()` (espelha o congelamento de `VerifyEmailQueued::freezeUrlFor`). Deixar a forma exata (propriedade pública `$channels` + setter, ou método) à discrição, mas DOCUMENTAR que o dispatcher CONGELA e o `via()` LÊ.

    NÃO implementar a interface nas Notifications existentes ainda (isso é 11-05/06, junto da migração dos testes). Apenas o contrato + PHPDoc claro do papel no pipeline.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="CommunicationModelTest|DatabaseNotificationChannelTest"</verify>
  <acceptance_criteria>
    - grep "communicationType" e "viabilityRequestId" em app/Notifications/Contracts/ProcessNotification.php.
    - Suíte do plano continua verde.
  </acceptance_criteria>
  <done>Contrato ProcessNotification publicado: o dispatcher (11-04) e as Notifications de processo (11-05/06) têm a forma comum para congelar canais e alimentar o ledger.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-090 — in-app via canal database nativo (lidas/não lidas) | DatabaseNotificationChannelTest |
| HU-096 — ledger communications por processo (schema + status honestos) | CommunicationModelTest |
| HU-094/095 — status honestos (enviado/falhou/bloqueado/desativado) no ledger | CommunicationModelTest (markAsBlocked/markAsDisabled) |
| Pipeline — contrato ProcessNotification (congelar canais no disparo) | ProcessNotification (consumido em 11-04/05/06) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan migrate --no-interaction` aplica as duas migrations sem erro.
- `php artisan test --compact --filter="DatabaseNotificationChannelTest|CommunicationModelTest"` verde.
- Suíte completa CONTINUA verde (nada de regra de negócio mudou aqui).
</verification>

<success_criteria>
- Canal database nativo habilitado e provado (in-app real, HU-090).
- Ledger communications real (model+enums+factory+marcadores honestos), com índices de idempotência (viability_request_id,type,channel) e consulta (viability_request_id,created_at).
- Contrato ProcessNotification publicado. EmailLog intacto (conta) — ZERO dupla verdade. ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/11-pendencias-e-comunicacao/11-01-SUMMARY.md`: nomes finais das migrations/enums/model, a assinatura dos marcadores do Communication (markAsSent/Failed/Blocked/Disabled), o shape do `meta` e a forma exata do contrato ProcessNotification (como o dispatcher congela canais). Insumo direto de 11-03 (channel marca bloqueado no Communication), 11-04 (dispatcher cria communications + congela canais), 11-05/06 (Notifications implementam ProcessNotification), 11-08 (histórico HU-096 lê communications; central lê notifications).
</output>

---
phase: 11-pendencias-e-comunicacao
plan: 07
type: execute
wave: 3
depends_on: ["11-03", "11-04"]
files_modified:
  - app/Console/Commands/AlertarVencimentosCommand.php
  - app/Console/Commands/EscalonarSlaCommand.php
  - app/Console/Commands/ExpirarPendenciasCommand.php
  - app/Notifications/PrazoVencendoNotification.php
  - app/Notifications/ProcessoEscalonadoNotification.php
  - app/Notifications/PendenciaExpiradaNotification.php
  - routes/console.php
  - tests/Feature/Comunicacao/AlertarVencimentosCommandTest.php
  - tests/Feature/Comunicacao/EscalonarSlaCommandTest.php
  - tests/Feature/Comunicacao/ExpirarPendenciasCommandTest.php
autonomous: true
requirements: [HU-093, HU-147, HU-091]

must_haves:
  truths:
    - "HU-093: notificacoes:alertar-vencimentos (dailyAt 07:00) alerta processos em_analise com analysis_due_at próximo (notificacoes.vencimento.antecedencia_dias) e pendências abertas com due_at próximo — SÓ notifica (sem transição/timeline → sem dupla contagem HU-129)"
    - "HU-147: notificacoes:escalonar-sla (hourly) escala por SLA usando a API pública AnalysisSlaService::statusFor(analysis_due_at, analysis_stage_started_at, now) sobre a FONTE ÚNICA analysis_due_at (mesma origem da fila da Fase 10, 10-14/10-16) — SlaStatus::Amarelo alerta o analista, SlaStatus::Vermelho escala ao(s) gestor(es) (role parametrizável notificacoes.escalonamento.gestor_role); tratamento por notificacoes.escalonamento.tratamento (default só notificar). NÃO usar isOverdue diretamente (é interno do BusinessDeadlineCalculator)"
    - "HU-091 RN-005: pendencias:expirar (dailyAt 06:00) marca AnalysisPendencyStatus::Expirada as pendências com due_at vencido sem resposta e notifica o analista — SEM decisão automática (indeferir por não-resposta é rito SEDUR, NÃO inventar)"
    - "Idempotência (RN-004) SEM schema novo: cada rotina checa communications (existe type=X para o processo/etapa após analysis_stage_started_at?) antes de notificar — 2 execuções NÃO duplicam; comandos withoutOverlapping()->onOneServer() (espelha ExpressoIndeferirSemBapCommand)"
    - "É o ÚNICO editor de routes/console.php na Fase 11; as notificações percorrem o NotificationDispatcher (multicanal + ledger)"
  artifacts:
    - path: "app/Console/Commands/EscalonarSlaCommand.php"
      provides: "HU-147: escala por analysis_due_at (AnalysisSlaService); amarelo→analista, vencido→gestor (role parametrizável); idempotente por communications"
      contains: "AnalysisSlaService"
    - path: "app/Console/Commands/ExpirarPendenciasCommand.php"
      provides: "HU-091 RN-005: Expirada + notifica analista, SEM decisão automática"
      contains: "AnalysisPendencyStatus::Expirada"
    - path: "routes/console.php"
      provides: "Agenda idempotente: alertar-vencimentos (07:00), escalonar-sla (hourly), expirar (06:00) — withoutOverlapping/onOneServer"
      contains: "notificacoes:escalonar-sla"
  key_links:
    - from: "app/Console/Commands/EscalonarSlaCommand.php"
      to: "app/Services/Analise/AnalysisSlaService.php"
      via: "lê o semáforo via statusFor(dueAt, startedAt, now) da FONTE ÚNICA analysis_due_at (sem recalcular prazo à parte)"
      pattern: "statusFor"
    - from: "app/Console/Commands/AlertarVencimentosCommand.php"
      to: "app/Models/Communication.php"
      via: "checa existência de communications (type) para o processo/etapa antes de notificar (idempotência)"
      pattern: "communications"
---

<objective>
Ativa o que ficou DORMENTE: o scheduler de prazos do EP11 (Wave 3, par paralelo com 11-05 e 11-06; ÚNICO editor de `routes/console.php` na fase). Três comandos idempotentes que SÓ notificam (sem mexer no estado, exceto a expiração de pendência que é regra própria): `notificacoes:alertar-vencimentos` (HU-093), `notificacoes:escalonar-sla` (HU-147, fonte única `analysis_due_at` via `AnalysisSlaService`) e `pendencias:expirar` (HU-091 RN-005). Todos percorrem o `NotificationDispatcher` (multicanal + histórico) e checam `communications` para idempotência — sem schema novo.

Purpose: o `analysis_due_at` e o `AnalysisSlaService` já existem (Fase 10) e o scheduler já roda (Fase 3.1) — faltava ATIVAR vencimento/escalonamento/expiração. A fonte de prazo é ÚNICA (`analysis_due_at`) para não brigar com a HU-129 (Fase 15); as rotinas só notificam (sem transição/timeline), exceto a expiração que tem regra própria (Expirada) mas SEM decisão automática (o rito de indeferir por não-resposta é pendência SEDUR — não inventar). A idempotência reusa o índice `(viability_request_id, type, channel)` do ledger.
Output: 3 comandos + 3 notificações de processo + agenda em `routes/console.php`, cobertos por feature tests (incl. idempotência e no-op honesto).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-pendencias-e-comunicacao/11-CONTEXT.md
@docs/superpowers/specs/2026-06-15-pendencias-comunicacao-design.md
@.planning/phases/11-pendencias-e-comunicacao/11-04-SUMMARY.md

# PADRÕES A ESPELHAR (comando idempotente + scheduler + SLA) — ler antes de codar:
@app/Console/Commands/ExpressoIndeferirSemBapCommand.php
@routes/console.php
@app/Services/Analise/AnalysisSlaService.php
@app/Models/Communication.php
@app/Enums/AnalysisPendencyStatus.php
@docs/SILE_HUs_Completas_MD/EP11-Pendências-e-Comunicacao/HU-093-Notificar-vencimentos.md
@docs/SILE_HUs_Completas_MD/EP11-Pendências-e-Comunicacao/HU-147-Escalonar-processos-parados-por-SLA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: notificacoes:alertar-vencimentos (HU-093) + PrazoVencendoNotification + agenda</name>
  <read_first>app/Console/Commands/ExpressoIndeferirSemBapCommand.php, app/Services/Analise/AnalysisSlaService.php (dueAtFor/statusFor), app/Models/AnalysisPendency.php (due_at), routes/console.php</read_first>
  <files>app/Console/Commands/AlertarVencimentosCommand.php, app/Notifications/PrazoVencendoNotification.php, routes/console.php, tests/Feature/Comunicacao/AlertarVencimentosCommandTest.php</files>
  <action>
    TDD estrito (RED primeiro). `AlertarVencimentosCommand` (signature `notificacoes:alertar-vencimentos`): varre
    - processos `em_analise` com `analysis_due_at` dentro da antecedência `notificacoes.vencimento.antecedencia_dias` (Settings→config) → notifica o ANALISTA responsável (assigned_user_id) que o prazo está próximo;
    - pendências `aberta` com `due_at` dentro da antecedência → notifica o REQUERENTE.
    SÓ notifica (NÃO transiciona, NÃO grava timeline). `PrazoVencendoNotification implements ProcessNotification` (communicationType = PrazoVencendo; via dinâmico; toMail/toDatabase/toWhatsApp), enviada via `NotificationDispatcher::deliver`.
    IDEMPOTÊNCIA: antes de notificar, checa em `communications` se já há `type=prazo_vencendo` para aquele processo/pendência dentro da janela atual (ex.: após `analysis_stage_started_at`/ciclo do due_at) — se há, pula. Agenda em `routes/console.php`: `Schedule::command('notificacoes:alertar-vencimentos')->dailyAt('07:00')->withoutOverlapping()->onOneServer()` (com comentário do propósito/idempotência, espelhando as rotinas existentes).

    `AlertarVencimentosCommandTest` (SQLite):
    - processo em_analise vencendo dentro da antecedência → analista recebe PrazoVencendo (communications na_fila); fora da antecedência → nada.
    - pendência aberta vencendo → requerente recebe.
    - IDEMPOTÊNCIA: rodar 2× NÃO duplica communications (assertCount estável).
    - no-op honesto: sem processos na janela → comando sai SUCCESS sem notificar.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=AlertarVencimentosCommandTest</verify>
  <acceptance_criteria>
    - grep "notificacoes:alertar-vencimentos" e "withoutOverlapping" em routes/console.php.
    - AlertarVencimentosCommandTest verde (janela, idempotência 2×, no-op honesto).
  </acceptance_criteria>
  <done>HU-093 ativa: alerta de vencimento (analista/requerente) idempotente, fonte única de prazo, sem dupla contagem.</done>
</task>

<task type="auto">
  <name>Task 2: notificacoes:escalonar-sla (HU-147) + ProcessoEscalonadoNotification + agenda</name>
  <read_first>app/Services/Analise/AnalysisSlaService.php (statusFor/isOverdue, SlaStatus), app/Console/Commands/ExpressoIndeferirSemBapCommand.php, routes/console.php, app/Models/User.php (role gestor)</read_first>
  <files>app/Console/Commands/EscalonarSlaCommand.php, app/Notifications/ProcessoEscalonadoNotification.php, routes/console.php, tests/Feature/Comunicacao/EscalonarSlaCommandTest.php</files>
  <action>
    TDD estrito. `EscalonarSlaCommand` (signature `notificacoes:escalonar-sla`): varre processos `em_analise` e usa o `AnalysisSlaService` (FONTE ÚNICA `analysis_due_at` + `analysis_stage_started_at`) para o semáforo:
    - `Amarelo` (limiar) → alerta o ANALISTA responsável;
    - `Vermelho`/`isOverdue` (vencido) → escala ao(s) GESTOR(es): usuários com a role `notificacoes.escalonamento.gestor_role` (default 'gestor'); 
    - o que fazer por faixa vem de `notificacoes.escalonamento.tratamento` (default só notificar — NUNCA decide/transiciona).
    `ProcessoEscalonadoNotification implements ProcessNotification` (communicationType = EscalonamentoSla; via dinâmico; multicanal) via dispatcher. IDEMPOTÊNCIA por `communications` (já há escalonamento_sla para o processo na etapa atual?). Agenda: `Schedule::command('notificacoes:escalonar-sla')->hourly()->withoutOverlapping()->onOneServer()`.

    Destinatário "gestor do setor" NÃO existe no schema → default role gestor parametrizável (registrar como pendência SEDUR no SUMMARY/STATE, não inventar roteamento ao setor).

    `EscalonarSlaCommandTest` (SQLite, $now injetável no SlaService):
    - amarelo → analista alertado; vencido → gestor(es) escalado(s) (assert por role); prazo coincide com AnalysisSlaService (CA-02 HU-147 — sem fonte paralela).
    - IDEMPOTÊNCIA 2× não duplica; no-op honesto (fila vazia/verde → nada).
    - tratamento default só notifica (status do processo NÃO muda).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=EscalonarSlaCommandTest</verify>
  <acceptance_criteria>
    - grep "notificacoes:escalonar-sla" em routes/console.php; "AnalysisSlaService" e "gestor_role" em app/Console/Commands/EscalonarSlaCommand.php.
    - EscalonarSlaCommandTest verde (amarelo→analista, vencido→gestor, idempotência, sem mudança de status).
  </acceptance_criteria>
  <done>HU-147 ativa: escalonamento por SLA na fonte única (analysis_due_at), gestor parametrizável, idempotente, só notifica.</done>
</task>

<task type="auto">
  <name>Task 3: pendencias:expirar (HU-091 RN-005) + PendenciaExpiradaNotification + agenda</name>
  <read_first>app/Enums/AnalysisPendencyStatus.php (Expirada), app/Models/AnalysisPendency.php (due_at/status), app/Console/Commands/ExpressoIndeferirSemBapCommand.php, routes/console.php</read_first>
  <files>app/Console/Commands/ExpirarPendenciasCommand.php, app/Notifications/PendenciaExpiradaNotification.php, routes/console.php, tests/Feature/Comunicacao/ExpirarPendenciasCommandTest.php</files>
  <action>
    TDD estrito. `ExpirarPendenciasCommand` (signature `pendencias:expirar`): pendências `Aberta` com `due_at` vencido (sem resposta) → marca `AnalysisPendencyStatus::Expirada` (enum já existe) e notifica o ANALISTA responsável (PendenciaExpiradaNotification, communicationType = PendenciaExpirada). 
    CRÍTICO (anti-fachada): SEM decisão automática — NÃO indefere, NÃO transiciona o processo. O rito de não-resposta (indeferir por prazo) é pendência SEDUR (não inventar): hoje expira+notifica+mantém o estado. Auditar a expiração (RN-002). IDEMPOTÊNCIA: a própria mudança Aberta→Expirada impede reprocessar (a 2ª passada não acha mais Aberta vencida); reforçar com checagem de communications se notificar. Agenda: `Schedule::command('pendencias:expirar')->dailyAt('06:00')->withoutOverlapping()->onOneServer()`.

    `ExpirarPendenciasCommandTest` (SQLite):
    - pendência Aberta vencida → vira Expirada + analista notificado + auditoria; o processo NÃO muda de status (anti-fachada: sem indeferimento automático).
    - pendência Aberta dentro do prazo → intacta.
    - IDEMPOTÊNCIA 2× não reprocessa/duplica; no-op honesto quando não há vencidas.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=ExpirarPendenciasCommandTest</verify>
  <acceptance_criteria>
    - grep "pendencias:expirar" em routes/console.php; "AnalysisPendencyStatus::Expirada" em app/Console/Commands/ExpirarPendenciasCommand.php.
    - ExpirarPendenciasCommandTest verde (Expirada + notifica + auditoria; SEM decisão automática; idempotente).
  </acceptance_criteria>
  <done>HU-091 RN-005 ativa: pendência expira (Expirada) e notifica o analista, sem decisão automática; idempotente e auditada.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-093 — alerta de vencimento (analista/requerente) na antecedência | AlertarVencimentosCommandTest |
| HU-147 — escalonamento por SLA (amarelo→analista, vencido→gestor) na fonte única | EscalonarSlaCommandTest |
| HU-147 RN-004 / CA-02 — idempotência + prazo coincide com AnalysisSlaService | EscalonarSlaCommandTest (2×; sem fonte paralela) |
| HU-091 RN-005 — pendência expira + notifica, SEM decisão automática | ExpirarPendenciasCommandTest |
| Anti-dupla-contagem HU-129 — rotinas só notificam (sem transição/timeline) | Os três testes (status do processo inalterado) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="AlertarVencimentosCommandTest|EscalonarSlaCommandTest|ExpirarPendenciasCommandTest"` verde.
- `php artisan schedule:list` lista as três rotinas com withoutOverlapping/onOneServer.
- Suíte completa CONTINUA verde; ÚNICO editor de routes/console.php na Fase 11.
</verification>

<success_criteria>
- HU-093/147/091-RN-005 ativas: vencimento, escalonamento (fonte única analysis_due_at) e expiração de pendência, todos idempotentes e multicanal pelo dispatcher.
- Rotinas SÓ notificam (sem dupla contagem); expiração SEM decisão automática (rito SEDUR registrado, não inventado); gestor parametrizável.
- ZERO dependência nova; agenda idempotente (withoutOverlapping/onOneServer). Pendência SEDUR (gestor do setor) registrada no SUMMARY/STATE.
</success_criteria>

<output>
Criar `.planning/phases/11-pendencias-e-comunicacao/11-07-SUMMARY.md`: signatures dos 3 comandos, a cadência agendada, COMO a idempotência por communications foi feita (chave/janela), os tipos de Communication gerados (prazo_vencendo/escalonamento_sla/pendencia_expirada) e as pendências SEDUR registradas (gestor do setor; rito de não-resposta). Insumo direto de 11-08 (central/histórico exibem essas comunicações) e 11-10 (smoke do scheduler + verificação).
</output>

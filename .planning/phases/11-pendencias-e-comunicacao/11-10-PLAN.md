---
phase: 11-pendencias-e-comunicacao
plan: 10
type: execute
wave: 6
depends_on: ["11-01", "11-02", "11-03", "11-04", "11-05", "11-06", "11-07", "11-08", "11-09"]
files_modified:
  - database/seeders/ComunicacaoDevSeeder.php
  - database/seeders/DatabaseSeeder.php
  - tests/Feature/Seeders/ComunicacaoDevSeederTest.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
autonomous: false
requirements: [HU-090, HU-091, HU-092, HU-093, HU-094, HU-095, HU-096, HU-147]

must_haves:
  truths:
    - "O ambiente de desenvolvimento tem comunicações e notificações REAIS (não linhas fabricadas): o ComunicacaoDevSeeder executa o fluxo REAL (abre uma pendência num processo de exemplo → dispara o listener → cria communications + notificação in-app; responde → notifica o analista), de modo que a central e o histórico têm dados navegáveis"
    - "A suíte INTEGRAL passa fresca via `composer test` (2 processos: SQLite + @group postgis), com a baseline crescida pelos novos testes da fase (era 1132 SQLite + 29 postgis); pint limpo"
    - "Anti-fachada confirmada por evidência: WhatsApp OFF → communications 'desativado' e ON+indisponível → 'bloqueado' (NUNCA 'enviado'); os listeners novos são auto-descobertos (sem Event::listen) e registrados 1× por contagem; nenhum adaptador falso em runtime"
    - "Existe TESTE DE INTEGRAÇÃO pela PIPELINE COMPLETA (dispatcher → notify() real, com WhatsAppChannel E RegistrarEnvioComunicacao ativos, SEM Notification::fake) com features.notificacao_whatsapp=ON + binding UnavailableWhatsAppGateway: a linha communications do canal whatsapp termina 'bloqueado' e JAMAIS é sobrescrita para 'enviado' pelo NotificationSent — fecha o gap do caminho ON+indisponível (pré-Fase 13)"
    - "Smoke navegável de ponta a ponta validado por humano: abrir pendência → in-app + e-mail reais → requerente responde no portal → análise reabre → analista notificado; central de notificações e histórico (HU-096) exibem os eventos"
    - "Veredito do guardião-entrega REGISTRADO (anti-fachada + auditoria RN-002 + parametrização HU-014 + TDD com evidência fresca); REPROVADO bloqueia a conclusão da fase"
  artifacts:
    - path: "database/seeders/ComunicacaoDevSeeder.php"
      provides: "Dados dev via fluxo REAL (pendência aberta/respondida → communications + in-app); idempotente por marcador estável"
      contains: "PendenciaService"
    - path: "tests/Feature/Seeders/ComunicacaoDevSeederTest.php"
      provides: "Prova de que o seed dev gera communications/notifications reais (presença, não fabricação)"
      contains: "communications"
  key_links:
    - from: "database/seeders/ComunicacaoDevSeeder.php"
      to: "app/Services/Analise/PendenciaService.php"
      via: "abre/responde pendência de exemplo pelo caminho REAL (listeners produzem o ledger e o in-app)"
      pattern: "abrir|responder"
    - from: "database/seeders/DatabaseSeeder.php"
      to: "database/seeders/ComunicacaoDevSeeder.php"
      via: "encadeado no seed de desenvolvimento (após os exemplos da análise técnica)"
      pattern: "ComunicacaoDevSeeder"
---

<objective>
Fechamento da Fase 11 (Wave 6, depende de tudo; autonomous=false por conter checkpoint humano): seeds de desenvolvimento que produzem comunicações/notificações REAIS pelo fluxo de verdade, verificação INTEGRAL fresca (`composer test` 2 processos + pint + evidência anti-fachada) e smoke navegável de ponta a ponta com checkpoint humano, fechado pelo veredito do guardião-entrega.

Purpose: a regra de entrega-funcional exige feature operando ponta a ponta com evidência fresca — nada de "deve funcionar". Os seeds usam dados fictícios mas LÓGICA REAL (abrir/responder pendência pelo `PendenciaService`, que dispara os listeners e popula `communications`/in-app), tornando a central e o histórico navegáveis no dev. A verificação roda a suíte inteira (incluindo @group postgis) e confirma os invariantes anti-fachada (WhatsApp bloqueado/desativado nunca "enviado"; listeners auto-descobertos 1×). O guardião-entrega dá o parecer final; REPROVADO trava a fase.
Output: ComunicacaoDevSeeder + encadeamento + teste de presença; relatório de verificação integral; checkpoint humano do smoke; veredito do guardião.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-pendencias-e-comunicacao/11-CONTEXT.md
@docs/superpowers/specs/2026-06-15-pendencias-comunicacao-design.md
@.planning/phases/11-pendencias-e-comunicacao/11-05-SUMMARY.md
@.planning/phases/11-pendencias-e-comunicacao/11-07-SUMMARY.md
@.planning/phases/11-pendencias-e-comunicacao/11-08-SUMMARY.md

# PADRÕES A ESPELHAR (dev seeder idempotente real + verificação) — ler antes de codar:
@database/seeders/DatabaseSeeder.php
@tests/Feature/Seeders/DatabaseSeederTest.php
@.cursor/agents/guardiao-entrega.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ComunicacaoDevSeeder (dados dev via fluxo REAL) + encadeamento + teste de presença</name>
  <read_first>database/seeders/DatabaseSeeder.php (ordem dos seeders dev; exemplos da análise técnica/cidadao@sile.dev), app/Services/Analise/PendenciaService.php, tests/Feature/Seeders/DatabaseSeederTest.php</read_first>
  <files>database/seeders/ComunicacaoDevSeeder.php, database/seeders/DatabaseSeeder.php, tests/Feature/Seeders/ComunicacaoDevSeederTest.php, tests/Feature/Comunicacao/WhatsAppPipelineIntegrationTest.php</files>
  <action>
    TDD estrito. `ComunicacaoDevSeeder`: cria um processo de exemplo DEDICADO (marcador estável próprio — NÃO mutar os exemplos já asseridos pelo DatabaseSeederTest) do cidadao@sile.dev, leva-o a em_analise pelo caminho legítimo (atribuído ao analista@sile.dev) e executa o fluxo REAL (NÃO insere linhas fabricadas):
    - `PendenciaService::abrir(...)` no processo dedicado → o listener NotificarPendencia gera communications (email/in_app) + notificação database para o requerente.
    - responde a pendência via `PendenciaService::responder(...)` → notifica o analista (in-app/communications) e reabre a análise.
    Idempotente por marcador estável (não duplica no re-seed — só cria/abre se o exemplo dedicado ainda não existir). Encadear no `DatabaseSeeder` (após os exemplos da análise técnica), só no perfil de desenvolvimento (mesmo critério dos seeders dev existentes).

    CUIDADO ANTI-REGRESSÃO: o `DatabaseSeederTest` assere contagens/estados dos exemplos do cidadao (ex.: nº de protocoladas, total de ViabilityRequest do cidadao). Como o seeder dedicado ADICIONA um processo (e o leva a em_analise/em_pendencia→em_analise), ATUALIZAR essas asserções no `DatabaseSeederTest` (contagem total e por status do cidadao) para refletir o novo exemplo — mantendo as demais intactas. NÃO reduzir cobertura.

    `ComunicacaoDevSeederTest` (SQLite): após `$this->seed()`, existem communications (type pendencia_aberta/pendencia_respondida) ligadas ao processo dedicado E o requerente dev tem ao menos 1 notificação database (unreadNotifications); o analista tem a notificação da resposta. Idempotência: re-seed NÃO duplica o exemplo nem as comunicações. (Asserções de PRESENÇA/estabilidade, não de contagem global frágil.)

    TESTE DE INTEGRAÇÃO ADICIONAL (anti-fachada WhatsApp, fecha o gap do checker) — `tests/Feature/Comunicacao/WhatsAppPipelineIntegrationTest.php`: SEM Notification::fake (envio real, listeners auto-descobertos ativos), com `features.notificacao_whatsapp` ON e o binding default `UnavailableWhatsAppGateway`, dispara uma ProcessNotification de teste que inclui o canal whatsapp pela pipeline real (NotificationDispatcher → notify()). Asserir: a linha `communications` do canal whatsapp termina `bloqueado` (com auditoria), e NUNCA `enviado` — mesmo após o `NotificationSent` do whatsapp disparar (o RegistrarEnvioComunicacao ignora whatsapp; a guarda do markAsSent protege). Provar também que mail/database na mesma notificação terminam `enviado`.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="ComunicacaoDevSeederTest|DatabaseSeederTest"</verify>
  <acceptance_criteria>
    - grep "PendenciaService" em database/seeders/ComunicacaoDevSeeder.php; "ComunicacaoDevSeeder" em database/seeders/DatabaseSeeder.php.
    - ComunicacaoDevSeederTest verde (communications + in-app reais, idempotente) e DatabaseSeederTest atualizado e verde (78 parâmetros, 24 permissões, exemplos do cidadao reajustados pelo novo processo dedicado).
  </acceptance_criteria>
  <done>Dev navegável com comunicações/notificações REAIS produzidas pelo fluxo de verdade (dados fictícios, lógica real), idempotente.</done>
</task>

<task type="auto">
  <name>Task 2: Verificação INTEGRAL fresca (composer test 2 processos + pint + evidência anti-fachada) + guardião-entrega</name>
  <read_first>.cursor/agents/guardiao-entrega.md, composer.json (script test: --exclude-group postgis depois --group postgis)</read_first>
  <files>(sem código — verificação/relatório no SUMMARY)</files>
  <action>
    Verificação com EVIDÊNCIA FRESCA (regra superpowers):
    1. `vendor/bin/pint --dirty --format agent` — sem pendências.
    2. `composer test` — roda os DOIS processos (SQLite `--exclude-group postgis` e `--group postgis`). Confirmar TUDO verde e que a baseline cresceu com os novos testes da fase (referência anterior: 1132 SQLite + 29 postgis). Ler o output inteiro.
    3. Evidência anti-fachada (grep/inspeção):
       - NENHUM `Event::listen` para os listeners novos (NotificarPendencia/NotificarRespostaPendencia/RegistrarEnvioComunicacao) — auto-descoberta; cada um registrado 1× (os testes de contagem provam).
       - WhatsApp: nos testes/ledger, OFF → `desativado`, ON+indisponível → `bloqueado`; NUNCA `enviado` sem gateway real (binding default Unavailable lança).
       - Nenhum adaptador falso em runtime; o provedor WhatsApp permanece BLOQUEADO (Fase 13) — registrado em STATE/ROADMAP.
    4. Acionar o agent `guardiao-entrega` com a evidência fresca para o veredito final (anti-fachada + auditoria RN-002 + parametrização HU-014 + TDD). Registrar o veredito; REPROVADO bloqueia a conclusão (corrigir e repetir).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && composer test</verify>
  <acceptance_criteria>
    - `composer test` verde nos 2 processos (SQLite + postgis), com a baseline crescida pelos testes novos da fase.
    - Evidência anti-fachada registrada (WhatsApp desativado/bloqueado nunca "enviado"; listeners auto-descobertos 1×; sem adaptador falso).
    - Veredito do guardião-entrega APROVADO registrado no SUMMARY.
  </acceptance_criteria>
  <done>Suíte integral fresca verde + invariantes anti-fachada confirmados + parecer do guardião APROVADO.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Comunicação multicanal do EP11 ponta a ponta: pendência (in-app + e-mail reais), resposta que reabre a análise e notifica o analista, central de notificações (badge/sininho), histórico unificado por processo (HU-096), scheduler de vencimento/escalonamento/expiração e WhatsApp atrás de toggle/contrato (bloqueado honesto).
  </what-built>
  <how-to-verify>
    Com o ambiente dev rodando (`composer run dev`) e o seed aplicado (`php artisan migrate:fresh --seed`):
    1. Logar como analista@sile.dev (gestão) e abrir uma pendência num processo em análise.
    2. Logar como cidadao@sile.dev (portal): conferir o sininho com badge, a notificação in-app da pendência e o e-mail real (Pail/log/Resend de teste); abrir a central de pendências e RESPONDER pelo portal.
    3. Confirmar que a análise REABRIU (em_pendencia→em_analise) e que o analista recebeu a notificação da resposta.
    4. Abrir o histórico de comunicações do processo (HU-096) e ver os eventos unificados (canal/tipo/status honesto), incl. o e-mail e o in-app.
    5. (Opcional) Rodar `php artisan notificacoes:alertar-vencimentos`, `notificacoes:escalonar-sla` e `pendencias:expirar` e conferir que SÓ notificam (sem mudar estado, exceto a pendência expirar) e são idempotentes (2ª execução não duplica).
    6. Confirmar que o WhatsApp está OFF (histórico mostra 'desativado', nunca 'enviado').
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados (cada problema vira gap a corrigir antes de concluir a fase).</resume-signal>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste / Evidência |
|---|---|
| HU-090..096/147 — fluxo ponta a ponta navegável | Checkpoint humano (smoke) + ComunicacaoDevSeederTest |
| Entrega-funcional — lógica real ponta a ponta (sem fachada) | composer test (2 processos) + evidência anti-fachada + guardião |
| HU-095 — WhatsApp nunca finge (desativado/bloqueado) | Evidência anti-fachada (Task 2) + checkpoint (passo 6) |
| Suíte integral fresca verde (SQLite + postgis) | `composer test` (Task 2) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `composer test` verde nos DOIS processos (SQLite + @group postgis), baseline crescida.
- ComunicacaoDevSeederTest + DatabaseSeederTest verdes (dev real, 78 parâmetros/24 permissões).
- Checkpoint humano APROVADO + veredito do guardião-entrega APROVADO.
</verification>

<success_criteria>
- Dev navegável com comunicações/notificações REAIS (fluxo de verdade), idempotente.
- Suíte INTEGRAL fresca verde (2 processos); invariantes anti-fachada confirmados (WhatsApp desativado/bloqueado nunca "enviado"; listeners auto-descobertos 1×).
- Smoke ponta a ponta aprovado por humano; guardião-entrega APROVADO. Bloqueios SEDUR (WhatsApp real, gestor do setor, rito de não-resposta, convite Regin) registrados no STATE/ROADMAP — nunca simulados.
</success_criteria>

<output>
Criar `.planning/phases/11-pendencias-e-comunicacao/11-10-SUMMARY.md`: o resultado do `composer test` (contagens finais SQLite/postgis), o veredito do guardião-entrega, o que o seed dev produz e os bloqueios SEDUR registrados. Fecha a Fase 11.
</output>

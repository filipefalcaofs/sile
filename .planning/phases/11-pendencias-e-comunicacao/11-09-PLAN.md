---
phase: 11-pendencias-e-comunicacao
plan: 09
type: execute
wave: 5
depends_on: ["11-08"]
files_modified:
  - resources/js/components/notificacoes/NotificationBell.tsx
  - resources/js/pages/notificacoes/index.tsx
  - resources/js/pages/portal/pendencias/index.tsx
  - resources/js/components/comunicacoes/HistoricoComunicacoes.tsx
  - tests/Feature/Comunicacao/NotificationCenterUiSmokeTest.php
autonomous: true
requirements: [HU-090, HU-091, HU-096]

must_haves:
  truths:
    - "HU-090: o sininho aparece nos layouts (portal e gestão) exibindo a contagem de não-lidas (shared prop) e abre a central; a página de notificações lista lidas/não-lidas e permite marcar uma e marcar todas via os endpoints de 11-08 (Inertia)"
    - "HU-091: o requerente tem uma central de pendências no portal que lista suas pendências abertas com link para responder. FONTE DE DADOS (pinada, sem rota nova): deriva das solicitações do dono já disponíveis (a listagem 'Minhas solicitações' da Fase 8 filtrada por status em_pendencia) + os endpoints de notificação/histórico do 11-08; e reusa o fluxo PendenciaRespostaController já existente para responder (NÃO recria a resposta). Se faltar QUALQUER endpoint, é exceção que volta ao 11-08 (route-owner) — NÃO criar rota nesta wave frontend-only."
    - "HU-096: o histórico unificado de comunicações de um processo é exibido (canal, tipo, status honesto, data) consumindo o endpoint de 11-08"
    - "A UI é server-driven (Inertia v3 + React 19), com estado de carregamento/vazio honesto; nenhuma tela finge resultado — os dados vêm dos endpoints reais de 11-08"
    - "Frontend-only: NÃO edita rotas nem controllers (11-08 já os entregou); o smoke renderiza as páginas reais (Inertia assert)"
  artifacts:
    - path: "resources/js/components/notificacoes/NotificationBell.tsx"
      provides: "Sininho com badge de não-lidas (shared prop) nos layouts portal/gestão"
      contains: "nao_lidas"
    - path: "resources/js/pages/notificacoes/index.tsx"
      provides: "Central de notificações (lista + marcar lida/todas via Inertia)"
      contains: "ler-todas"
    - path: "resources/js/components/comunicacoes/HistoricoComunicacoes.tsx"
      provides: "Histórico unificado de comunicações por processo (HU-096)"
      contains: "historico"
  key_links:
    - from: "resources/js/components/notificacoes/NotificationBell.tsx"
      to: "HandleInertiaRequests (shared prop notificacoes.nao_lidas)"
      via: "usePage().props lê a contagem de não-lidas para o badge"
      pattern: "notificacoes"
    - from: "resources/js/pages/notificacoes/index.tsx"
      to: "rotas portal.notificacoes.* / gestao.notificacoes.*"
      via: "router.post para marcar lida/todas (Inertia)"
      pattern: "notificacoes"
---

<objective>
Frontend da central in-app e do histórico (Wave 5, depende de 11-08; frontend-only, sem tocar rotas/controllers): o sininho com badge de não-lidas (shared prop) nos layouts portal e gestão, a página da central de notificações (listar + marcar lida/todas), a central de pendências do requerente (lista + link para responder, reusando o fluxo existente) e o componente de histórico unificado de comunicações por processo (HU-096).

Purpose: 11-08 entregou os endpoints, o shared prop e o ledger consultável; agora damos a superfície navegável real (Inertia v3 + React 19) — sem fingir nada: os dados vêm dos endpoints, com estados de carregamento/vazio honestos. A resposta da pendência NÃO é recriada (o `PendenciaRespostaController` do portal já existe desde a Fase 10) — a central de pendências apenas lista e leva ao fluxo real.
Output: NotificationBell + página de notificações + central de pendências + HistoricoComunicacoes, com smoke navegável (Inertia assert).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-pendencias-e-comunicacao/11-CONTEXT.md
@docs/superpowers/specs/2026-06-15-pendencias-comunicacao-design.md
@.planning/phases/11-pendencias-e-comunicacao/11-08-SUMMARY.md

# PADRÕES A ESPELHAR (Inertia React + layouts + UI smoke) — ler antes de codar; ATIVAR a skill inertia-react-development:
@tests/Feature/Analise/FichaUiSmokeTest.php
@tests/Feature/Analise/ProcessoUiSmokeTest.php
@app/Http/Middleware/HandleInertiaRequests.php
@resources/js/pages
@docs/SILE_HUs_Completas_MD/EP11-Pendências-e-Comunicacao/HU-090-Notificar-pendência.md
@docs/SILE_HUs_Completas_MD/EP11-Pendências-e-Comunicacao/HU-096-Consultar-historico-de-comunicacões.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotificationBell (badge) nos layouts + página da central de notificações</name>
  <read_first>resources/js/pages (estrutura de páginas/layout portal e gestão), app/Http/Middleware/HandleInertiaRequests.php (prop notificacoes.nao_lidas), 11-08-SUMMARY (names das rotas), tests/Feature/Analise/ProcessoUiSmokeTest.php (padrão de smoke)</read_first>
  <files>resources/js/components/notificacoes/NotificationBell.tsx, resources/js/pages/notificacoes/index.tsx, tests/Feature/Comunicacao/NotificationCenterUiSmokeTest.php</files>
  <action>
    ATIVAR a skill `inertia-react-development`. TDD do lado servidor: o smoke (RED→GREEN) prova que a página renderiza via Inertia.
    - `NotificationBell.tsx`: lê `usePage().props` a contagem `notificacoes.nao_lidas` (shared prop), mostra o badge e linka para a central (`route('portal.notificacoes.index')`/`gestao.notificacoes.index')` conforme o ambiente). Inserir o sininho nos layouts existentes do portal e da gestão (reusar o componente; ver onde o header/topbar é montado).
    - `pages/notificacoes/index.tsx`: lista as notificações (props server-driven do 11-08), separa não-lidas/lidas, botões "marcar como lida" (router.post na rota `{ambiente}.notificacoes.ler`/{notification}) e "marcar todas" (rota ler-todas). Estado VAZIO honesto ("Nenhuma notificação"). Paginação se aplicável.

    `NotificationCenterUiSmokeTest` (SQLite, Inertia assert — espelha ProcessoUiSmokeTest): autentica um usuário com notificações; GET da central retorna 200 e o componente Inertia `notificacoes/index` com a prop de lista; o shared prop `notificacoes.nao_lidas` está presente. Cobrir portal e gestão.
  </action>
  <verify>npm run build && vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=NotificationCenterUiSmokeTest</verify>
  <acceptance_criteria>
    - Arquivos NotificationBell.tsx e pages/notificacoes/index.tsx existem e referenciam a prop notificacoes/rotas de 11-08.
    - `npm run build` sem erros; NotificationCenterUiSmokeTest verde (componente Inertia + prop) nos dois ambientes.
  </acceptance_criteria>
  <done>Sininho com badge real e central de notificações navegável (listar/marcar) no portal e na gestão.</done>
</task>

<task type="auto">
  <name>Task 2: Central de pendências (portal) + HistoricoComunicacoes (HU-096)</name>
  <read_first>routes/portal.php (solicitacoes/{solicitacao}/pendencias — fluxo de resposta já existente), 11-08-SUMMARY (endpoint de histórico), resources/js/pages/portal (estrutura)</read_first>
  <files>resources/js/pages/portal/pendencias/index.tsx, resources/js/components/comunicacoes/HistoricoComunicacoes.tsx, tests/Feature/Comunicacao/NotificationCenterUiSmokeTest.php</files>
  <action>
    ATIVAR `inertia-react-development`. 
    - `pages/portal/pendencias/index.tsx`: central do requerente — lista as pendências ABERTAS das suas solicitações com o link "Responder" para o fluxo já existente (`portal.solicitacoes.pendencias`/`...responder` — PendenciaRespostaController, Fase 10). NÃO recria a resposta; apenas agrega/lista. Estado vazio honesto. (Se 11-08 não expôs um índice de pendências, consumir via props da própria página servida por um controller leve — à discrição; preferir reusar dados já disponíveis sem nova rota; se precisar de rota, é EXCEÇÃO e deve ir em 11-08, não aqui. Default: derivar das solicitações do usuário já carregáveis.)
    - `components/comunicacoes/HistoricoComunicacoes.tsx`: tabela/linha do tempo do histórico unificado consumindo o endpoint `...comunicacoes` (11-08): canal, tipo, status (com rótulo honesto — enviado/falhou/bloqueado/desativado/na_fila), título/resumo, data. Embutir na página de acompanhamento do processo (portal) e na página do processo (gestão), reusando o componente.

    Ampliar `NotificationCenterUiSmokeTest` (ou um método novo): GET da central de pendências (portal) renderiza o componente Inertia com a lista; e a página de processo que embute o HistoricoComunicacoes renderiza com os dados do endpoint (smoke do componente — ou um teste do payload já coberto por ComunicacaoHistoricoTest em 11-08; aqui foco no render).
  </action>
  <verify>npm run build && vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=NotificationCenterUiSmokeTest</verify>
  <acceptance_criteria>
    - pages/portal/pendencias/index.tsx lista pendências com link ao fluxo de resposta existente; HistoricoComunicacoes.tsx consome o endpoint de histórico.
    - `npm run build` sem erros; smoke verde (render das páginas/componentes reais).
  </acceptance_criteria>
  <done>Central de pendências navegável (lista + link ao fluxo real) e histórico unificado embutido nas telas de processo (HU-096).</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-090 — sininho (badge) + central de notificações navegável | NotificationCenterUiSmokeTest (Inertia render + shared prop) |
| HU-091 — central de pendências do requerente (lista + link ao fluxo real) | NotificationCenterUiSmokeTest (página portal/pendencias) |
| HU-096 — histórico unificado exibido na tela do processo | NotificationCenterUiSmokeTest + ComunicacaoHistoricoTest (11-08, payload) |

<verification>
- `npm run build` sem erros (Vite manifest válido).
- `vendor/bin/pint --dirty --format agent` sem pendências (PHP dos smokes).
- `php artisan test --compact --filter=NotificationCenterUiSmokeTest` verde.
- Frontend-only: NÃO alterou rotas/controllers (11-08 é o route-owner).
</verification>

<success_criteria>
- Sininho com badge real (shared prop) e central de notificações navegável (listar/marcar) nos dois ambientes.
- Central de pendências do requerente (lista + link ao fluxo de resposta REAL já existente) e histórico unificado (HU-096) embutido nas telas de processo.
- UI server-driven honesta (estados vazio/carregando), sem fingir resultado; ZERO rota/controller novo aqui.
</success_criteria>

<output>
Criar `.planning/phases/11-pendencias-e-comunicacao/11-09-SUMMARY.md`: os componentes/páginas criados, onde o sininho foi inserido (layouts), como o histórico foi embutido nas telas de processo e o que o smoke cobre. Insumo direto de 11-10 (smoke navegável de ponta a ponta).
</output>

---
phase: 11-pendencias-e-comunicacao
plan: 04
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - app/Services/Comunicacao/NotificationDispatcher.php
  - app/Listeners/RegistrarEnvioComunicacao.php
  - tests/Feature/Comunicacao/NotificationDispatcherTest.php
  - tests/Feature/Comunicacao/RegistrarEnvioComunicacaoTest.php
autonomous: true
requirements: [HU-090, HU-094, HU-096]

must_haves:
  truths:
    - "NotificationDispatcher::deliver(User, ProcessNotification) resolve os canais SÍNCRONO no disparo: intersecção de notificacoes.mapa_canais[tipo] com os toggles features.notificacao_email/_in_app/_whatsapp (+ gancho de preferência), espelhando VerifyEmailQueued::freezeUrlFor"
    - "Para cada canal HABILITADO cria uma linha communications 'na_fila'; para cada canal do mapa que está com toggle OFF cria 'desativado' + auditoria (degradação comunicada, nunca silenciosa)"
    - "O dispatcher CONGELA os canais resolvidos na Notification (lidos pelo via()) e chama recipient->notify() — o envio em si é enfileirado (ShouldQueue)"
    - "O listener AUTO-DESCOBERTO RegistrarEnvioComunicacao (NotificationSent/NotificationFailed) marca a linha communications como enviado/falhou SOMENTE para os canais mail e database — NUNCA whatsapp (o WhatsAppChannel do 11-03 é dono único da linha whatsapp: bloqueado/enviado; tratar whatsapp aqui sobrescreveria bloqueado→enviado, anti-fachada). Registrado UMA única vez (contagem), espelhando LogNotificationSent + gate por channel"
    - "EmailLog NÃO é tocado para comunicações de processo — a fonte de verdade é communications"
  artifacts:
    - path: "app/Services/Comunicacao/NotificationDispatcher.php"
      provides: "Roteador: resolve canais (mapa_canais ∩ toggles), cria communications (na_fila/desativado), congela canais, notifica"
      contains: "mapa_canais"
    - path: "app/Listeners/RegistrarEnvioComunicacao.php"
      provides: "Marca communications enviado/falhou em NotificationSent/NotificationFailed (auto-descoberto)"
      contains: "NotificationFailed"
  key_links:
    - from: "app/Services/Comunicacao/NotificationDispatcher.php"
      to: "app/Models/Communication.php"
      via: "cria linha na_fila por canal habilitado e desativado por canal off (auditado)"
      pattern: "CommunicationStatus::NaFila"
    - from: "app/Listeners/RegistrarEnvioComunicacao.php"
      to: "Illuminate\\Notifications\\Events\\NotificationSent"
      via: "type-hint do evento no handle (auto-descoberta; NUNCA Event::listen)"
      pattern: "NotificationSent"
---

<objective>
Roteador de notificações de processo do EP11 (Wave 2, par paralelo com 11-03): `NotificationDispatcher::deliver(User, ProcessNotification)` resolve canais por `notificacoes.mapa_canais` ∩ toggles, cria as linhas `communications` (na_fila habilitados; `desativado`+auditoria dos off), congela os canais na Notification e dispara `notify()`; mais o listener AUTO-DESCOBERTO `RegistrarEnvioComunicacao` que fecha o ciclo (na_fila→enviado/falhou) em `NotificationSent`/`NotificationFailed`.

Purpose: este é o coração da comunicação multicanal — o ponto único onde toggles e mapa de canais viram envio real + ledger honesto, sem dupla verdade. Espelha o congelamento de `VerifyEmailQueued::freezeUrlFor` (resolver no disparo, no contexto certo) e o padrão `LogNotificationSent` (marcar o resultado por evento de notificação). Mantém-se desacoplado: as Notifications de processo (11-05/06) só implementam `ProcessNotification`; as rotinas (11-07) e os listeners de domínio chamam o dispatcher.
Output: `NotificationDispatcher` + `RegistrarEnvioComunicacao`, cobertos por feature tests (resolução de canais, na_fila/desativado, enviado/falhou, contagem do listener).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-pendencias-e-comunicacao/11-CONTEXT.md
@docs/superpowers/specs/2026-06-15-pendencias-comunicacao-design.md

# PADRÕES A ESPELHAR (congelar no disparo + listener de NotificationSent + Settings) — ler antes de codar:
@app/Notifications/VerifyEmailQueued.php
@app/Listeners/LogNotificationSent.php
@app/Support/Settings.php
@app/Models/Communication.php
@app/Notifications/Contracts/ProcessNotification.php
@docs/SILE_HUs_Completas_MD/EP11-Pendências-e-Comunicacao/HU-090-Notificar-pendência.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotificationDispatcher (resolve canais + cria communications + congela + notifica)</name>
  <read_first>app/Notifications/VerifyEmailQueued.php (freeze no disparo), app/Support/Settings.php (get com fallback), app/Models/Communication.php, app/Notifications/Contracts/ProcessNotification.php</read_first>
  <files>app/Services/Comunicacao/NotificationDispatcher.php, tests/Feature/Comunicacao/NotificationDispatcherTest.php</files>
  <action>
    TDD estrito (RED primeiro). `NotificationDispatcher::deliver(User $recipient, ProcessNotification $notification): void`:
    - resolve os canais do tipo: `notificacoes.mapa_canais[type]` (Settings→config) ∩ canais com toggle ON (`features.notificacao_email/_in_app/_whatsapp`). Gancho de preferência por usuário documentado mas no-op (default por toggle/mapa — CONTEXT).
    - mapeia nomes de canal de negócio → drivers Laravel: email→'mail', in_app→'database', whatsapp→'whatsapp'.
    - cria `communications` SÍNCRONO: uma linha `na_fila` por canal habilitado (channel/type/viability_request_id/recipient_user_id/title/summary/meta/queued_at=now); para cada canal presente no mapa mas com toggle OFF, cria linha `desativado` + AUDITA ('notificacoes'/type/result 'desativado') — degradação comunicada.
    - CONGELA os canais resolvidos na Notification (via o contrato — propriedade/método lido pelo `via()`); congela TAMBÉM um MAPA explícito {canal→communicationId} na Notification (ex.: propriedade `communicationIds[channel]`), espelhando o `emailLogId` único do LogNotificationSent — o RegistrarEnvioComunicacao (11-04 Task 2) resolve a linha por `$event->channel` nesse mapa, e o WhatsAppChannel (11-03) usa o id do canal `whatsapp`. NÃO usar lookup heurístico por chave.
    - chama `$recipient->notify($notification)` (envio enfileirado pela ShouldQueue da Notification).
    - se NENHUM canal habilitado: não notifica; registra as linhas `desativado` (todas off) — honesto.

    `NotificationDispatcherTest` (SQLite, `Notification::fake()`):
    - mapa {tipo:[email,in_app]} + ambos toggles ON → 2 linhas communications `na_fila` (mail+database) e `Notification::assertSentTo` com via() congelado contendo mail+database.
    - toggle in_app OFF → a linha in_app vira `desativado` + auditoria; só mail `na_fila`.
    - whatsapp no mapa + features.notificacao_whatsapp OFF (default) → linha whatsapp `desativado` + auditoria; canal NÃO entra no via(). (Usa Notification::fake — não resolve o driver whatsapp do 11-03.)
    - todos os canais off → nada enviado; linhas `desativado`; `Notification::assertNothingSent()`.
    Use Parameter::create para ligar/desligar toggles (efeito sem deploy) ou Settings em memória conforme o padrão da casa.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=NotificationDispatcherTest</verify>
  <acceptance_criteria>
    - grep "mapa_canais" e "CommunicationStatus::NaFila" (e desativado) em app/Services/Comunicacao/NotificationDispatcher.php.
    - NotificationDispatcherTest verde: resolução por mapa∩toggles, na_fila dos habilitados, desativado+auditoria dos off, via() congelado, nada enviado quando tudo off.
  </acceptance_criteria>
  <done>Dispatcher resolve canais (mapa∩toggles), cria o ledger honesto (na_fila/desativado) e dispara o envio com canais congelados.</done>
</task>

<task type="auto">
  <name>Task 2: Listener RegistrarEnvioComunicacao (na_fila→enviado/falhou) — auto-descoberto e único</name>
  <read_first>app/Listeners/LogNotificationSent.php (NotificationSent → markAsSent), app/Models/Communication.php</read_first>
  <files>app/Listeners/RegistrarEnvioComunicacao.php, tests/Feature/Comunicacao/RegistrarEnvioComunicacaoTest.php</files>
  <action>
    TDD estrito. `RegistrarEnvioComunicacao` — listener AUTO-DESCOBERTO (type-hint do evento no handle; NUNCA Event::listen — lição Fases 8/9, travar por CONTAGEM). Trata DOIS eventos (dois handles type-hintados ou um listener por evento — à discrição, mas registro único cada):
    - `NotificationSent`: trata SOMENTE os canais `mail` e `database` (NUNCA `whatsapp`), localiza a linha `communications` correspondente pelo MAPA {canal→communicationId} congelado na Notification (ver 11-01/dispatcher; espelha o `emailLogId` único do LogNotificationSent + gate `channel===`) e `markAsSent()` na linha daquele `$event->channel`.
    - **CRÍTICO (anti-fachada):** EXCLUIR o canal `whatsapp` deste listener. O `WhatsAppChannel` (11-03) NÃO relança a exceção, então o Laravel dispara `NotificationSent` com `channel='whatsapp'` mesmo no caminho degradado — se este listener tratasse whatsapp, sobrescreveria `bloqueado`→`enviado` (um "enviado" fictício, proibido). O `WhatsAppChannel` é o DONO ÚNICO da linha whatsapp (só ele distingue `enviado` de `bloqueado`).
    - `NotificationFailed`: `markAsFailed($motivo)` na linha correspondente (mail/database).
    - IGNORA notificações de CONTA (sem Communication associado) — o LogNotificationSent segue cuidando do EmailLog (canal 'mail' de verificação/senha). Não duplicar nem conflitar.

    `RegistrarEnvioComunicacaoTest` (SQLite, SEM fake — envio real para disparar NotificationSent, com `Mail`/transport de teste): dispara uma ProcessNotification de teste no canal database; ao concluir, a linha communications passa de `na_fila` para `enviado`. Simular falha → `falhou` + error_message. CONTAGEM: a auto-descoberta marca UMA vez (sem duplicar). Confirmar que uma notificação de conta (VerifyEmailQueued) NÃO cria/encosta em communications.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=RegistrarEnvioComunicacaoTest</verify>
  <acceptance_criteria>
    - grep "NotificationSent" e "NotificationFailed" em app/Listeners/RegistrarEnvioComunicacao.php; SEM Event::listen para ele em qualquer provider.
    - RegistrarEnvioComunicacaoTest verde: na_fila→enviado/falhou, marca 1×, ignora notificação de conta.
  </acceptance_criteria>
  <done>O ciclo do ledger fecha de verdade: enviado/falhou marcado por evento de notificação, registro único, sem tocar o EmailLog de conta.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-090/094 — canais por tipo (mapa ∩ toggles), envio real | NotificationDispatcherTest |
| HU-014/094 — toggle off degrada comunicado (communications 'desativado' + auditoria) | NotificationDispatcherTest |
| HU-096 — ledger na_fila no disparo → enviado/falhou na conclusão | RegistrarEnvioComunicacaoTest |
| Lição Fases 8/9 — listener único por auto-descoberta (contagem) | RegistrarEnvioComunicacaoTest |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="NotificationDispatcherTest|RegistrarEnvioComunicacaoTest"` verde.
- Suíte completa CONTINUA verde; nenhum Event::listen para o listener (auto-descoberta).
</verification>

<success_criteria>
- Dispatcher resolve canais (mapa∩toggles + gancho de preferência), cria communications (na_fila/desativado+auditoria), congela canais (espelha freezeUrlFor) e dispara notify.
- RegistrarEnvioComunicacao fecha o ciclo (enviado/falhou), registro único, sem tocar EmailLog de conta.
- ZERO dupla verdade, ZERO dependência nova. (Tests do dispatcher usam Notification::fake — independente do driver whatsapp de 11-03.)
</success_criteria>

<output>
Criar `.planning/phases/11-pendencias-e-comunicacao/11-04-SUMMARY.md`: a assinatura do `NotificationDispatcher::deliver`, COMO os canais são congelados e COMO a linha communications é referenciada pela Notification (id congelado vs lookup) para o listener/canal fechar o ciclo, e a regra de resolução (mapa ∩ toggles). Insumo direto de 11-05/06 (listeners de domínio chamam deliver; Notifications implementam ProcessNotification + via dinâmico) e 11-07 (rotinas chamam deliver).
</output>

---
phase: 11-pendencias-e-comunicacao
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - app/Services/Whatsapp/WhatsAppGateway.php
  - app/Services/Whatsapp/WhatsAppMessage.php
  - app/Services/Whatsapp/UnavailableWhatsAppGateway.php
  - app/Services/Whatsapp/WhatsAppUnavailableException.php
  - app/Notifications/Channels/WhatsAppChannel.php
  - app/Providers/AppServiceProvider.php
  - app/Models/User.php
  - tests/Feature/Comunicacao/WhatsAppGatewayContractTest.php
  - tests/Feature/Comunicacao/WhatsAppChannelTest.php
autonomous: true
requirements: [HU-095]

must_haves:
  truths:
    - "O WhatsApp NUNCA finge envio: o binding default é UnavailableWhatsAppGateway, que lança WhatsAppUnavailableException — a transmissão não ocorre (provedor/credencial pendentes → Fase 13)"
    - "O WhatsAppChannel customizado, ao enviar com gateway indisponível, marca o Communication como BLOQUEADO + audita ('bloqueado'), JAMAIS 'enviado' (espelha ComunicarResultadoRegin); com gateway disponível (fake/spy) ele envia e marca enviado — prova de que liga sozinho na Fase 13 trocando só o binding"
    - "User::routeNotificationForWhatsapp() devolve o phone em E.164 (canal sabe para onde mandar)"
    - "O binding fica no AppServiceProvider ao lado de Regin/SEFAZ/Bap; as credenciais (integrations.whatsapp.base_url/token) e constantes técnicas vêm parametrizadas/config (11-02), nunca hardcoded"
    - "É o ÚNICO editor do AppServiceProvider e do User na Fase 11 (sem conflito com 11-04, que é paralelo)"
  artifacts:
    - path: "app/Services/Whatsapp/UnavailableWhatsAppGateway.php"
      provides: "Contrato degradado honesto — lança WhatsAppUnavailableException (nunca simula)"
      contains: "WhatsAppUnavailableException"
    - path: "app/Notifications/Channels/WhatsAppChannel.php"
      provides: "Canal customizado: captura Unavailable → Communication bloqueado + auditoria; disponível → envia"
      contains: "markAsBlocked"
    - path: "app/Providers/AppServiceProvider.php"
      provides: "Binding default WhatsAppGateway → UnavailableWhatsAppGateway + registro do canal whatsapp"
      contains: "WhatsAppGateway"
  key_links:
    - from: "app/Notifications/Channels/WhatsAppChannel.php"
      to: "app/Services/Whatsapp/WhatsAppGateway.php"
      via: "send() resolve o gateway e captura WhatsAppUnavailableException (degrada para bloqueado)"
      pattern: "WhatsAppUnavailableException"
    - from: "app/Models/User.php"
      to: "phone (E.164)"
      via: "routeNotificationForWhatsapp() entrega o destino ao canal"
      pattern: "routeNotificationForWhatsapp"
---

<objective>
Canal WhatsApp do EP11 atrás de contrato e toggle — bloqueado HONESTO (Wave 2, par paralelo com 11-04; ÚNICO editor de `AppServiceProvider` e `User` na fase). Cria o contrato `WhatsAppGateway` + DTO `WhatsAppMessage` + `UnavailableWhatsAppGateway` (lança `WhatsAppUnavailableException`) + binding (ao lado de Regin/SEFAZ/Bap), o `WhatsAppChannel` customizado (degrada para `bloqueado` no `Communication`, nunca "enviado") e `User::routeNotificationForWhatsapp()`.

Purpose: a regra anti-fachada manda que integração indisponível fique explicitamente bloqueada, nunca simulada. O WhatsApp não tem provedor/credencial (Fase 13), então o binding default LANÇA exceção e o canal audita `bloqueado` — exatamente o padrão já provado em `ComunicarResultadoReginListenerTest`. Quando a Fase 13 ligar o adaptador real, troca-se SÓ o binding e o canal passa a enviar (provado aqui com um spy). O toggle `features.notificacao_whatsapp` (OFF por default, 11-02) é resolvido no dispatcher (11-04) — aqui cuidamos do canal/contrato.
Output: stack WhatsApp (contrato+DTO+Unavailable+Exception+binding) + WhatsAppChannel + routeNotificationForWhatsapp, cobertos por feature tests (bloqueado + caminho de sucesso com spy).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-pendencias-e-comunicacao/11-CONTEXT.md
@docs/superpowers/specs/2026-06-15-pendencias-comunicacao-design.md

# PADRÕES A ESPELHAR (contrato Unavailable + binding + listener que audita bloqueado) — ler antes de codar:
@app/Services/Realty/PropertyRegistryLookup.php
@app/Providers/AppServiceProvider.php
@app/Listeners/NotificarResultadoExpresso.php
@tests/Feature/Expresso/ComunicarResultadoReginListenerTest.php
@app/Models/User.php
@app/Models/Communication.php
@docs/SILE_HUs_Completas_MD/EP11-Pendências-e-Comunicacao/HU-095-Enviar-WhatsApp.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contrato WhatsAppGateway + DTO + UnavailableWhatsAppGateway + Exception + binding</name>
  <read_first>app/Services/Realty/PropertyRegistryLookup.php + UnavailablePropertyRegistryLookup (se existir), app/Providers/AppServiceProvider.php (bloco de bindings Regin/SEFAZ/Bap)</read_first>
  <files>app/Services/Whatsapp/WhatsAppGateway.php, app/Services/Whatsapp/WhatsAppMessage.php, app/Services/Whatsapp/UnavailableWhatsAppGateway.php, app/Services/Whatsapp/WhatsAppUnavailableException.php, app/Providers/AppServiceProvider.php, tests/Feature/Comunicacao/WhatsAppGatewayContractTest.php</files>
  <action>
    TDD estrito (RED primeiro). Espelhar o padrão Realty/Regin:
    - `WhatsAppMessage` (readonly DTO): `to` (E.164), `body`, opcional `meta`.
    - `WhatsAppGateway` (interface): `send(WhatsAppMessage $message): void` — PHPDoc `@throws WhatsAppUnavailableException` (indisponível) e a semântica de que o provider real entra na Fase 13 trocando só o binding.
    - `WhatsAppUnavailableException` (extends RuntimeException) — mensagem honesta (provedor/credencial pendentes).
    - `UnavailableWhatsAppGateway implements WhatsAppGateway`: `send()` SEMPRE lança `WhatsAppUnavailableException` (nunca simula).
    - Binding no `AppServiceProvider::register`, no bloco das integrações bloqueadas (ao lado de Regin/SEFAZ/Bap): `$this->app->bind(WhatsAppGateway::class, UnavailableWhatsAppGateway::class)` com comentário do porquê (Fase 13 troca SÓ este binding).

    `WhatsAppGatewayContractTest` (SQLite): resolve o gateway do container e assere que `send()` lança `WhatsAppUnavailableException` (binding default). Sem qualquer registro de "enviado".
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=WhatsAppGatewayContractTest</verify>
  <acceptance_criteria>
    - grep "WhatsAppGateway::class" e "UnavailableWhatsAppGateway::class" em app/Providers/AppServiceProvider.php.
    - WhatsAppGatewayContractTest verde (Unavailable lança a exceção; nunca simula envio).
  </acceptance_criteria>
  <done>Contrato WhatsApp degradado honesto: o binding default lança exceção — a transmissão não ocorre (Fase 13 troca o binding).</done>
</task>

<task type="auto">
  <name>Task 2: WhatsAppChannel (bloqueado honesto / envia com spy) + User::routeNotificationForWhatsapp</name>
  <read_first>app/Listeners/NotificarResultadoExpresso.php + tests/Feature/Expresso/ComunicarResultadoReginListenerTest.php (padrão capturar Unavailable → auditar 'bloqueado'), app/Models/Communication.php (markAsBlocked/markAsSent), app/Models/User.php</read_first>
  <files>app/Notifications/Channels/WhatsAppChannel.php, app/Providers/AppServiceProvider.php, app/Models/User.php, tests/Feature/Comunicacao/WhatsAppChannelTest.php</files>
  <action>
    TDD estrito. `WhatsAppChannel` (canal customizado de Notification): `send($notifiable, Notification $notification)`:
    - resolve o destino via `$notifiable->routeNotificationFor('whatsapp', $notification)`; sem destino → não envia (degrada).
    - monta `WhatsAppMessage` a partir da Notification (método `toWhatsApp($notifiable)` — implementado pelas Notifications de processo em 11-05/06; no teste use uma Notification inline).
    - chama `WhatsAppGateway::send()`. CAPTURA `WhatsAppUnavailableException`: marca o `Communication` correspondente como `markAsBlocked()` + AUDITA (logName 'notificacoes', event tipo, result 'bloqueado') e NÃO relança (não quebra o fluxo — espelha ComunicarResultadoRegin). Sucesso → `markAsSent()` no Communication.
    - localizar o Communication da linha do canal whatsapp: à discrição (id congelado pela Notification/dispatcher em 11-04, ou lookup por (viability_request_id,type,channel=whatsapp,na_fila)). DOCUMENTAR a escolha.

    Registrar o canal no `AppServiceProvider::boot` com `Notification::extend('whatsapp', fn ($app) => $app->make(WhatsAppChannel::class))` (ou resolvedor equivalente). `User::routeNotificationForWhatsapp()` → `$this->phone` (E.164; já fillable).

    `WhatsAppChannelTest` (SQLite):
    - gateway DEFAULT (Unavailable): enviar pelo canal → Communication vira `bloqueado` + auditoria 'bloqueado'; NENHUM 'enviado' (assert count 0 de enviado).
    - gateway SPY (fake que registra a chamada, via `$this->app->instance(WhatsAppGateway::class, $spy)`): enviar → spy recebe a WhatsAppMessage com o phone E.164; Communication vira `enviado`. Prova de que liga na Fase 13.
    - sem phone no destinatário → não envia (degrada honesto).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=WhatsAppChannelTest</verify>
  <acceptance_criteria>
    - grep "routeNotificationForWhatsapp" em app/Models/User.php; grep "markAsBlocked" em app/Notifications/Channels/WhatsAppChannel.php.
    - WhatsAppChannelTest verde: bloqueado (Unavailable) sem "enviado"; sucesso com spy (E.164); sem destino degrada.
  </acceptance_criteria>
  <done>Canal WhatsApp real e honesto: indisponível → bloqueado+auditoria (nunca finge), disponível (spy) → envia; destino E.164 pelo User.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-095 — provedor indisponível NUNCA finge (contrato Unavailable lança) | WhatsAppGatewayContractTest |
| HU-095 — canal on + indisponível → bloqueado + auditoria (nunca "enviado") | WhatsAppChannelTest (gateway default) |
| HU-095 — liga sozinho na Fase 13 (troca do binding) | WhatsAppChannelTest (gateway spy → enviado, E.164) |
| HU-095 — destino do canal (phone E.164) | WhatsAppChannelTest (routeNotificationForWhatsapp) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="WhatsAppGatewayContractTest|WhatsAppChannelTest"` verde.
- Suíte completa CONTINUA verde; ÚNICO editor de AppServiceProvider e User na Fase 11.
</verification>

<success_criteria>
- WhatsApp atrás de contrato + DTO + Unavailable + Exception + binding (espelha Regin/SEFAZ/Bap); canal customizado degrada para `bloqueado` (nunca "enviado") e envia com spy.
- routeNotificationForWhatsapp = phone E.164. Credenciais/constantes vêm de 11-02 (parametrizado/config), zero hardcoded.
- ZERO dependência nova; provedor real permanece BLOQUEADO (Fase 13 troca só o binding).
</success_criteria>

<output>
Criar `.planning/phases/11-pendencias-e-comunicacao/11-03-SUMMARY.md`: assinatura de WhatsAppGateway/WhatsAppMessage, o nome do canal ('whatsapp'), como o WhatsAppChannel localiza/atualiza o Communication (bloqueado/enviado) e o contrato `toWhatsApp()` esperado das Notifications. Insumo direto de 11-04 (dispatcher congela o canal 'whatsapp' quando toggle on), 11-05/06 (Notifications implementam toWhatsApp) e 11-07 (escalonamento multicanal).
</output>

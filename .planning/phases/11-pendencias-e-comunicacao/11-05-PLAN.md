---
phase: 11-pendencias-e-comunicacao
plan: 05
type: execute
wave: 3
depends_on: ["11-03", "11-04"]
files_modified:
  - app/Services/Analise/PendenciaService.php
  - app/Notifications/PendenciaSolicitadaNotification.php
  - app/Notifications/RespostaPendenciaNotification.php
  - app/Events/PendenciaRespondida.php
  - app/Listeners/NotificarPendencia.php
  - app/Listeners/NotificarRespostaPendencia.php
  - tests/Feature/Analise/PendenciaServiceTest.php
  - tests/Feature/Comunicacao/NotificarPendenciaListenerTest.php
  - tests/Feature/Comunicacao/NotificarRespostaPendenciaListenerTest.php
autonomous: true
requirements: [HU-090, HU-091, HU-092]

must_haves:
  truths:
    - "HU-090: o aviso de pendência ao requerente passa a ser MULTICANAL via o listener AUTO-DESCOBERTO NotificarPendencia (type-hint PendenciaSolicitada; NUNCA Event::listen) → NotificationDispatcher; PendenciaSolicitadaNotification implementa ProcessNotification com via() dinâmico + toDatabase()/toWhatsApp()"
    - "ANTI-DUPLICAÇÃO (crítico): PendenciaService::abrir NÃO chama mais notificarRequerente (método removido) — o listener é o único que notifica; os testes da Fase 10 que asseriam o e-mail na abertura MIGRAM para Notification::fake/communications (cobertura mantida, não removida)"
    - "HU-091/092: ao responder, PendenciaService::responder dispara o evento after-commit PendenciaRespondida; o listener AUTO-DESCOBERTO NotificarRespostaPendencia notifica o ANALISTA responsável (assigned_user_id) — retorno que a Fase 10 não fechava"
    - "Cada listener é registrado UMA única vez (contagem); after-commit garante que rollback NÃO dispara; degradação honesta (sem destinatário → audita, nunca finge)"
    - "O ciclo de estado (em_analise↔em_pendencia), a timeline e as auditorias pendencia-aberta/pendencia-respondida da Fase 10 permanecem INTACTOS (este plano só pluga comunicação, não refaz estado)"
  artifacts:
    - path: "app/Listeners/NotificarPendencia.php"
      provides: "Listener auto-descoberto de PendenciaSolicitada → dispatcher multicanal ao requerente"
      contains: "NotificationDispatcher"
    - path: "app/Services/Analise/PendenciaService.php"
      provides: "abrir() SEM notificarRequerente (removido); responder() dispara PendenciaRespondida after-commit"
      contains: "PendenciaRespondida"
    - path: "app/Listeners/NotificarRespostaPendencia.php"
      provides: "Listener auto-descoberto de PendenciaRespondida → notifica o analista (assigned_user_id)"
      contains: "assigned"
  key_links:
    - from: "app/Listeners/NotificarPendencia.php"
      to: "app/Services/Comunicacao/NotificationDispatcher.php"
      via: "handle(PendenciaSolicitada) chama deliver(requester, PendenciaSolicitadaNotification)"
      pattern: "deliver"
    - from: "app/Services/Analise/PendenciaService.php"
      to: "app/Events/PendenciaRespondida.php"
      via: "responder() despacha PendenciaRespondida após o commit (ShouldDispatchAfterCommit)"
      pattern: "PendenciaRespondida::dispatch"
---

<objective>
Generaliza e fecha o ciclo de pendência da Fase 10 SEM refazer estado (Wave 3, par paralelo com 11-06 e 11-07): HU-090 vira multicanal pelo listener auto-descoberto `NotificarPendencia` (→ dispatcher), com `PendenciaSolicitadaNotification` ganhando `via()` dinâmico + in-app + WhatsApp; o refactor CRÍTICO anti-duplicação remove `notificarRequerente()` do `PendenciaService::abrir` (o listener passa a notificar); e HU-091/092 ganham o evento after-commit `PendenciaRespondida` + listener `NotificarRespostaPendencia` que notifica o analista responsável.

Purpose: o estado (em_analise↔em_pendencia), a timeline e as auditorias já existem e estão corretos — o EP11 só ADICIONA comunicação plena. O maior risco é a DUPLICAÇÃO: hoje `abrir()` dispara o evento E manda um e-mail direto (linha 97). Removemos o e-mail direto e deixamos o listener (gancho do evento) notificar — migrando os testes da Fase 10 para asserir via `Notification::fake`/`communications` (cobertura mantida). O retorno ao analista quando o requerente responde (que a Fase 10 não fechava) entra como evento+listener, espelhando o padrão auto-descoberto travado por contagem.
Output: NotificarPendencia + PendenciaRespondida + NotificarRespostaPendencia + RespostaPendenciaNotification + PendenciaSolicitadaNotification multicanal + PendenciaService refatorado, com testes (incl. MIGRAÇÃO do PendenciaServiceTest).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-pendencias-e-comunicacao/11-CONTEXT.md
@docs/superpowers/specs/2026-06-15-pendencias-comunicacao-design.md
@.planning/phases/11-pendencias-e-comunicacao/11-04-SUMMARY.md

# PADRÕES + ALVOS DO REFACTOR — ler antes de codar:
@app/Services/Analise/PendenciaService.php
@app/Events/PendenciaSolicitada.php
@app/Notifications/PendenciaSolicitadaNotification.php
@app/Listeners/NotificarResultadoExpresso.php
@tests/Feature/Analise/PendenciaServiceTest.php
@tests/Feature/Expresso/ComunicarResultadoReginListenerTest.php
@docs/SILE_HUs_Completas_MD/EP11-Pendências-e-Comunicacao/HU-090-Notificar-pendência.md
@docs/SILE_HUs_Completas_MD/EP11-Pendências-e-Comunicacao/HU-091-Responder-pendência.md
@docs/SILE_HUs_Completas_MD/EP11-Pendências-e-Comunicacao/HU-092-Reabrir-analise.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HU-090 multicanal — NotificarPendencia + PendenciaSolicitadaNotification multicanal + REMOVER notificarRequerente (migrar PendenciaServiceTest)</name>
  <read_first>app/Services/Analise/PendenciaService.php (linhas 94-187: dispatch do evento + notificarRequerente), app/Notifications/PendenciaSolicitadaNotification.php, app/Notifications/Contracts/ProcessNotification.php, app/Services/Comunicacao/NotificationDispatcher.php (11-04), tests/Feature/Analise/PendenciaServiceTest.php</read_first>
  <files>app/Notifications/PendenciaSolicitadaNotification.php, app/Listeners/NotificarPendencia.php, app/Services/Analise/PendenciaService.php, tests/Feature/Comunicacao/NotificarPendenciaListenerTest.php, tests/Feature/Analise/PendenciaServiceTest.php</files>
  <action>
    TDD estrito (RED primeiro). Ordem segura:
    1. `PendenciaSolicitadaNotification` implementa `ProcessNotification`: `communicationType()` = PendenciaAberta; `viabilityRequestId()` = o id já carregado; `via()` DINÂMICO (lê os canais congelados pelo dispatcher, com fallback ['mail']); adiciona `toDatabase()` (título/resumo/link do portal) e `toWhatsApp()` (texto curto com link). Mantém `toMail()` (assunto/corpo agora podem ler `notificacoes.pendencia.assunto`/`corpo` — 11-02). Mantém ShouldQueue + failed().
    2. `NotificarPendencia` (listener auto-descoberto; type-hint `PendenciaSolicitada` no handle; NUNCA Event::listen): resolve o requerente; sem destinatário → audita 'notificacoes'/'pendencia-aberta'/'sem-destinatario' e retorna (honesto); senão `NotificationDispatcher::deliver($requester, new PendenciaSolicitadaNotification(...))`.
    3. REFACTOR `PendenciaService::abrir`: REMOVER a chamada `$this->notificarRequerente(...)` e o método privado `notificarRequerente()` inteiro (e imports não usados: EmailLog/PendenciaSolicitadaNotification se ficarem órfãos). Manter `PendenciaSolicitada::dispatch(...)` after-commit. NÃO mexer no estado/timeline/auditoria pendencia-aberta.
    4. MIGRAR `PendenciaServiceTest`:
       - `test_abrir_envia_email_simples_real_ao_requerente` → renomear/reescrever para asserir via comunicação: com `Notification::fake()` o listener (auto-descoberto) dispara → `Notification::assertSentTo($requester, PendenciaSolicitadaNotification::class)` e `assertDatabaseHas('communications', [type pendencia_aberta, channel email/in_app, status na_fila])`. NÃO asserir mais `email_logs` para a pendência.
       - `test_abrir_sem_destinatario_com_email_audita_e_nao_envia` → a auditoria 'sem-destinatario' agora é do listener; manter a asserção (o evento dispara o listener) OU mover para o NotificarPendenciaListenerTest. Garantir `Notification::assertNothingSent()` + estado em_pendencia real.
       - Os testes de ESTADO (transição, timeline, auditoria pendencia-aberta, evento despachado, bloqueios) permanecem como estão (continuam verdes).

    `NotificarPendenciaListenerTest` (SQLite): evento → 1× notificação (contagem; auto-descoberta); multicanal conforme mapa∩toggles (communications na_fila por canal); sem destinatário → auditoria + nada enviado; whatsapp OFF default → linha desativado (não "enviado").
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="NotificarPendenciaListenerTest|PendenciaServiceTest"</verify>
  <acceptance_criteria>
    - grep -n "notificarRequerente" app/Services/Analise/PendenciaService.php → SEM resultados (método e chamada removidos).
    - grep "NotificationDispatcher" em app/Listeners/NotificarPendencia.php; "implements ProcessNotification" em PendenciaSolicitadaNotification.php.
    - NotificarPendenciaListenerTest verde (1×, multicanal, sem-destinatario, whatsapp desativado) e PendenciaServiceTest MIGRADO verde (assere communications, não email_logs).
  </acceptance_criteria>
  <done>Aviso de pendência multicanal pelo listener único; e-mail direto removido do serviço; testes da Fase 10 migrados (cobertura mantida).</done>
</task>

<task type="auto">
  <name>Task 2: HU-091/092 — PendenciaRespondida (after-commit) + NotificarRespostaPendencia → analista</name>
  <read_first>app/Services/Analise/PendenciaService.php (responder()), app/Events/PendenciaSolicitada.php (padrão ShouldDispatchAfterCommit), app/Models/ViabilityRequest.php (assignedTo()/assigned_user_id)</read_first>
  <files>app/Events/PendenciaRespondida.php, app/Listeners/NotificarRespostaPendencia.php, app/Notifications/RespostaPendenciaNotification.php, app/Services/Analise/PendenciaService.php, tests/Feature/Comunicacao/NotificarRespostaPendenciaListenerTest.php</files>
  <action>
    TDD estrito. 
    1. `PendenciaRespondida` (evento; espelha PendenciaSolicitada: Dispatchable + ShouldDispatchAfterCommit; carrega ViabilityRequest + AnalysisPendency).
    2. `PendenciaService::responder`: ao final, APÓS o commit, `PendenciaRespondida::dispatch($request, $pendency)`. NÃO mexer no estado/timeline/auditoria pendencia-respondida (já corretos).
    3. `RespostaPendenciaNotification implements ProcessNotification` (communicationType = PendenciaRespondida; via() dinâmico; toMail/toDatabase/toWhatsApp) — avisa o analista que a pendência foi respondida e a análise reabriu, com link do processo na gestão.
    4. `NotificarRespostaPendencia` (listener auto-descoberto; type-hint `PendenciaRespondida`; NUNCA Event::listen): destinatário = analista responsável (`$request->assignedTo` / assigned_user_id); sem analista atribuído → audita 'notificacoes'/'pendencia-respondida'/'sem-destinatario' e retorna (honesto); senão `deliver($analista, ...)`.

    `NotificarRespostaPendenciaListenerTest` (SQLite, Notification::fake): responder() (ou event direto) → analista notificado 1× (contagem); communications type pendencia_respondida; after-commit: numa transação que sofre rollback, o evento NÃO dispara (assertNothingSent); sem analista → auditoria + nada enviado.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter="NotificarRespostaPendenciaListenerTest|PendenciaServiceTest"</verify>
  <acceptance_criteria>
    - grep "PendenciaRespondida::dispatch" em app/Services/Analise/PendenciaService.php; "ShouldDispatchAfterCommit" em app/Events/PendenciaRespondida.php.
    - NotificarRespostaPendenciaListenerTest verde (analista 1×, after-commit no rollback, sem-destinatario).
  </acceptance_criteria>
  <done>Resposta de pendência fecha o ciclo: o analista responsável é notificado (multicanal), com evento after-commit honesto e listener único.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-090 — aviso de pendência multicanal (1×, auto-descoberto) | NotificarPendenciaListenerTest |
| HU-090 anti-duplicação — abrir não manda mais e-mail direto | PendenciaServiceTest (migrado: assere communications, não email_logs) |
| HU-091/092 — resposta reabre e NOTIFICA o analista responsável | NotificarRespostaPendenciaListenerTest |
| after-commit — rollback não dispara comunicação | NotificarRespostaPendenciaListenerTest |
| HU-095 — WhatsApp off degrada (desativado, nunca "enviado") | NotificarPendenciaListenerTest |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="NotificarPendenciaListenerTest|NotificarRespostaPendenciaListenerTest|PendenciaServiceTest"` verde.
- ANTI-REGRESSÃO: rodar também `php artisan test --compact tests/Feature/Analise/ProcessoPendenciaEndpointTest.php tests/Feature/Analise/PendenciaRespostaPortalTest.php` — devem seguir VERDES (o ciclo de estado não mudou; ajustar apenas se asseriam o e-mail direto removido).
- Suíte completa CONTINUA verde.
</verification>

<success_criteria>
- HU-090 multicanal por listener único; notificarRequerente REMOVIDO; PendenciaServiceTest migrado (communications) sem perder cobertura.
- HU-091/092: evento after-commit + analista notificado; listeners auto-descobertos travados por contagem.
- Estado/timeline/auditoria da Fase 10 INTACTOS; WhatsApp degrada honesto. ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/11-pendencias-e-comunicacao/11-05-SUMMARY.md`: os listeners criados (e a contagem esperada por evento), o que mudou no PendenciaService (remoção + dispatch), os tipos de Communication gerados (pendencia_aberta/pendencia_respondida) e quais testes da Fase 10 foram migrados/ajustados. Insumo direto de 11-08 (central in-app/histórico exibem essas comunicações) e 11-10 (smoke: abrir→in-app+e-mail→responder→analista notificado).
</output>

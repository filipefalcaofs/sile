---
phase: 11-pendencias-e-comunicacao
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database/seeders/ParameterSeeder.php
  - config/sile.php
  - tests/Feature/Seeders/ParameterSeederTest.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
autonomous: true
requirements: [HU-014, HU-090, HU-093, HU-094, HU-095, HU-147]

must_haves:
  truths:
    - "Os toggles de canal nascem administráveis (HU-014): features.notificacao_email (1), features.notificacao_in_app (1), features.notificacao_whatsapp (0 — bloqueado honesto por default)"
    - "O grupo NOVO notificacoes existe no catálogo: mapa_canais (json type→[canais]), vencimento.antecedencia_dias (3), escalonamento.tratamento (json), escalonamento.gestor_role ('gestor' — destinatário parametrizável, pois não há 'gestor do setor' no schema), pendencia.assunto e pendencia.corpo (templates pt-BR default)"
    - "As credenciais do WhatsApp são parâmetros de integração com teste de conexão: integrations.whatsapp.base_url (requires_connection_test) e integrations.whatsapp.token (sensitive — criptografado, nunca reexibido)"
    - "Todo parâmetro tem espelho de fallback em config/sile.php (Settings resolve banco→config→default inline); as constantes TÉCNICAS do WhatsApp (timeout/tries) ficam SÓ no config (fora do catálogo — precedente [02-02])"
    - "ParameterSeeder é o ÚNICO dono do catálogo na Fase 11; as contagens sobem de 67→78 e o grupo 'notificacoes' entra na lista ordenada, com idempotência e preservação de valor administrado mantidas"
  artifacts:
    - path: "database/seeders/ParameterSeeder.php"
      provides: "11 parâmetros novos (3 features + 6 notificacoes + 2 integrations.whatsapp), upsert só de metadados (preserva value)"
      contains: "notificacoes.mapa_canais"
    - path: "config/sile.php"
      provides: "Espelho de fallback de features.notificacao_*, do bloco notificacoes.* e de integrations.whatsapp.* (+ constantes técnicas)"
      contains: "notificacoes"
    - path: "tests/Feature/Seeders/ParameterSeederTest.php"
      provides: "Contagem 78 + grupo 'notificacoes' na lista ordenada + asserção dos parâmetros novos"
      contains: "notificacoes"
  key_links:
    - from: "database/seeders/ParameterSeeder.php"
      to: "tests/Feature/Seeders/ParameterSeederTest.php"
      via: "contagem total (78) e lista de grupos ordenada (inclui 'notificacoes')"
      pattern: "assertSame\\(78"
    - from: "database/seeders/ParameterSeeder.php"
      to: "config/sile.php"
      via: "cada key tem espelho de fallback (Settings::get resolve banco→config)"
      pattern: "notificacao_whatsapp"
---

<objective>
Catálogo de parâmetros HU-014 da comunicação (Wave 1, par paralelo com 11-01; ÚNICO dono do `ParameterSeeder` e do `config/sile.php` na Fase 11). Adiciona os toggles de canal (`features.notificacao_email/_in_app/_whatsapp`), o grupo NOVO `notificacoes` (mapa de canais, antecedência de vencimento, tratamento e destinatário do escalonamento, templates de pendência) e as credenciais do WhatsApp (`integrations.whatsapp.*`, com teste de conexão e segredo criptografado) — cada um com espelho de fallback no `config/sile.php`.

Purpose: a parametrização máxima (HU-014) é regra do projeto — nenhum valor de negócio hardcoded. Centralizar TODO o catálogo num só plano por wave evita que dois planos disputem `ParameterSeeder`/`config/sile.php` e quebra de contagem nos seeder-tests. O WhatsApp nasce OFF (degradação honesta) e suas credenciais já entram como integração com teste de conexão, prontas para a Fase 13 ligar só o binding.
Output: 11 parâmetros novos no `ParameterSeeder` + espelho no `config/sile.php`, com `ParameterSeederTest`/`DatabaseSeederTest` atualizados (67→78, grupo `notificacoes`).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-pendencias-e-comunicacao/11-CONTEXT.md
@docs/superpowers/specs/2026-06-15-pendencias-comunicacao-design.md

# PADRÕES A ESPELHAR (catálogo + fallback + seeder-tests) — ler antes de codar:
@database/seeders/ParameterSeeder.php
@config/sile.php
@tests/Feature/Seeders/ParameterSeederTest.php
@tests/Feature/Seeders/DatabaseSeederTest.php
@docs/SILE_HUs_Completas_MD/EP11-Pendências-e-Comunicacao/HU-095-Enviar-WhatsApp.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 11 parâmetros novos no ParameterSeeder + espelho no config/sile.php</name>
  <read_first>database/seeders/ParameterSeeder.php (forma das entradas: group/type/default_value/validation_rules/sensitive/requires_connection_test), config/sile.php (blocos features/integrations e o padrão de comentário de fallback)</read_first>
  <files>database/seeders/ParameterSeeder.php, config/sile.php</files>
  <action>
    TDD: a contagem/asserção entra na Task 2 (RED) — aqui é o GREEN do catálogo. Adicionar ao `catalog()` do `ParameterSeeder` (upsert só de metadados — value nunca no update, preserva o administrado):

    Grupo `features` (boolean, ['required','boolean']):
    - `features.notificacao_email` = '1' — "Habilita o canal de e-mail nas notificações de processo".
    - `features.notificacao_in_app` = '1' — "Habilita o canal in-app (central de notificações) nas notificações de processo".
    - `features.notificacao_whatsapp` = '0' — "Habilita o canal WhatsApp (provedor real bloqueado até a Fase 13; desligado degrada de forma comunicada)".

    Grupo NOVO `notificacoes`:
    - `notificacoes.mapa_canais` (json) default `{"pendencia_aberta":["email","in_app"],"pendencia_respondida":["in_app"],"prazo_vencendo":["email","in_app"],"escalonamento_sla":["email","in_app"],"resultado":["email","in_app"]}`, ['required','json'] — "Canais por tipo de notificação (intersecção com os toggles; WhatsApp fica fora por default)".
    - `notificacoes.vencimento.antecedencia_dias` (integer) default '3', ['required','integer','min:1','max:60'] — "Dias de antecedência do alerta de vencimento de prazo (HU-093)".
    - `notificacoes.escalonamento.tratamento` (json) default `{"amarelo":"notificar_analista","vencido":"notificar_gestor"}`, ['required','json'] — "Tratamento do escalonamento por SLA por faixa (HU-147; default só notifica, sem decisão automática)".
    - `notificacoes.escalonamento.gestor_role` (string) default 'gestor', ['required','string','max:50'] — "Papel destinatário do escalonamento de SLA (não há 'gestor do setor' no schema — pendência SEDUR; default role gestor)".
    - `notificacoes.pendencia.assunto` (string) default 'Pendência na sua solicitação de viabilidade {protocolo}', ['required','string','max:150'] — "Assunto do aviso de pendência (HU-090; placeholders substituíveis)".
    - `notificacoes.pendencia.corpo` (string) default texto pt-BR honesto com {protocolo}/{pendencia}, ['required','string','max:2000'] — "Corpo-template do aviso de pendência (HU-090)".

    Grupo `integracoes`:
    - `integrations.whatsapp.base_url` (string) default '', ['nullable','url'], `requires_connection_test => true` — "URL base da API comercial de WhatsApp (provedor real na Fase 13)".
    - `integrations.whatsapp.token` (string) `sensitive => true`, default null, ['nullable','string','max:255'] — "Token/credencial da API de WhatsApp (armazenado criptografado, nunca reexibido)".

    Espelho em `config/sile.php` (Settings resolve banco→config→default inline):
    - no bloco `features`: `notificacao_email => true`, `notificacao_in_app => true`, `notificacao_whatsapp => false`.
    - bloco NOVO `notificacoes` => mapa_canais (array já decodificado), vencimento.antecedencia_dias, escalonamento.tratamento (array) + escalonamento.gestor_role, pendencia.assunto/corpo.
    - bloco `integrations.whatsapp` => base_url '', token '' + CONSTANTES TÉCNICAS fora do catálogo (precedente [02-02]): `timeout => 8`, `tries => 3`, `backoff_ms => 1000` (cadência/resiliência do adaptador real — não são valor de negócio, não vão ao painel).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan config:clear && php artisan tinker --execute 'echo \Database\Seeders\ParameterSeeder::class;'</verify>
  <acceptance_criteria>
    - grep "notificacoes.mapa_canais", "features.notificacao_whatsapp" e "integrations.whatsapp.token" em database/seeders/ParameterSeeder.php.
    - grep "'notificacoes'" e "'whatsapp'" em config/sile.php; token sensitive e base_url requires_connection_test no seeder.
  </acceptance_criteria>
  <done>Catálogo da comunicação completo (11 parâmetros) com espelho de fallback e segredo criptografado/teste de conexão.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar ParameterSeederTest + DatabaseSeederTest (67→78, grupo notificacoes)</name>
  <read_first>tests/Feature/Seeders/ParameterSeederTest.php (contagem 67 + lista de grupos ordenada nas linhas ~18-21 e ~530), tests/Feature/Seeders/DatabaseSeederTest.php (contagem 67 nas linhas ~54 e ~310; permissões 24 NÃO mudam)</read_first>
  <files>tests/Feature/Seeders/ParameterSeederTest.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <action>
    TDD: estas asserções DEVEM ficar vermelhas se a Task 1 não tiver entrado e ficar verdes com ela. Atualizar:

    `ParameterSeederTest`:
    - contagem total `assertSame(67, ...)` → `assertSame(78, ...)` em `test_seeder_cria_catalogo_completo` e em `test_seeder_e_idempotente`.
    - lista ordenada de grupos: inserir `'notificacoes'` entre `'louos'` e `'retencao'` (ordem alfabética de `orderBy('group')`).
    - NOVO método `test_seeder_registra_parametros_de_notificacoes()`: assere os 11 parâmetros novos (group/type/default_value/validation_rules) — notificacao_email/_in_app/_whatsapp (features, boolean), mapa_canais/vencimento.antecedencia_dias/escalonamento.tratamento/escalonamento.gestor_role/pendencia.assunto/pendencia.corpo (notificacoes), whatsapp.base_url (requires_connection_test) e whatsapp.token (sensitive=true, default null). Conferir `typedValue()` array de mapa_canais/tratamento.

    `DatabaseSeederTest`:
    - `assertSame(67, Parameter::query()->count())` → `78` nas duas ocorrências (seed completo + idempotência).
    - NÃO mexer na contagem de permissões (24) nem de roles (4) — esta fase NÃO adiciona permissão nomeada (a central in-app é por dono; o histórico HU-096 reusa `consultar-solicitacoes`/escopo de dono — ver 11-08). Atualizar comentário só se necessário.

    NÃO tocar `RolesAndPermissionsSeederTest` nem `ManageRolesTest` (sem permissão nova).
  </action>
  <verify>php artisan test --compact --filter="ParameterSeederTest|DatabaseSeederTest"</verify>
  <acceptance_criteria>
    - grep "assertSame(78" em ParameterSeederTest.php (2×) e DatabaseSeederTest.php (2×).
    - grep "'notificacoes'" na lista ordenada de grupos do ParameterSeederTest.
    - ParameterSeederTest + DatabaseSeederTest verdes; permissões seguem 24.
  </acceptance_criteria>
  <done>Seeder-tests refletem o catálogo (78 parâmetros, grupo notificacoes), idempotência e preservação de value mantidas; permissões inalteradas.</done>
</task>

</tasks>

## Mapa CA → teste
| HU / RN | Teste |
|---|---|
| HU-014 — parâmetros administráveis (tipo/default/validação/sensível/teste de conexão) | ParameterSeederTest::test_seeder_registra_parametros_de_notificacoes |
| HU-090/094/095 — toggles de canal (email/in_app/whatsapp) | ParameterSeederTest (features.notificacao_*) |
| HU-093 — antecedência do vencimento | ParameterSeederTest (notificacoes.vencimento.antecedencia_dias) |
| HU-147 — tratamento + destinatário (role gestor) do escalonamento | ParameterSeederTest (notificacoes.escalonamento.*) |
| HU-014 — catálogo completo/idempotente | ParameterSeederTest + DatabaseSeederTest (78) |

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="ParameterSeederTest|DatabaseSeederTest"` verde.
- Suíte completa CONTINUA verde; ÚNICO editor de ParameterSeeder/config/sile.php na Fase 11.
</verification>

<success_criteria>
- 11 parâmetros novos administráveis (toggles + grupo notificacoes + credenciais WhatsApp), com fallback em config e segredo criptografado/teste de conexão.
- Contagens 67→78 e grupo `notificacoes` refletidos nos dois seeder-tests; permissões seguem 24 (sem permissão nova nesta fase).
- WhatsApp OFF por default (degradação honesta). ZERO valor de negócio hardcoded. ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/11-pendencias-e-comunicacao/11-02-SUMMARY.md`: a lista final de keys/defaults/validações dos 11 parâmetros (com o JSON default do mapa_canais e do tratamento), o novo total (78) e o ponto de leitura no config. Insumo direto de 11-03 (lê integrations.whatsapp.* + constantes técnicas), 11-04 (lê features.notificacao_* + notificacoes.mapa_canais), 11-05/06 (templates de pendência) e 11-07 (vencimento.antecedencia_dias + escalonamento.tratamento/gestor_role).
</output>

---
phase: 08-solicitacao-de-viabilidade
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - app/Http/Controllers/Gestao/ViabilityServiceTypeController.php
  - app/Http/Requests/Gestao/StoreViabilityServiceTypeRequest.php
  - app/Http/Requests/Gestao/UpdateViabilityServiceTypeRequest.php
  - routes/gestao.php
  - resources/js/pages/gestao/tipos-servico/index.tsx
  - tests/Feature/Solicitacao/ViabilityServiceTypeCrudTest.php
autonomous: true
requirements: [HU-061]

must_haves:
  truths:
    - "O administrador/gestor mantém os tipos de serviço (CRUD) pela retaguarda — são DADOS administráveis (tabela + CRUD), não registry de código (HU-061 RN-005)"
    - "A listagem e o CRUD ficam atrás da permissão manter-tipos-servico; sem ela, 403 auditado (CA-04)"
    - "code é único e imutável na edição (padrão CNAE/CNPJ); desativar (active=false) preserva o histórico — não exclui o que já foi usado em solicitações"
    - "Toda alteração é auditada (RN-002)"
  artifacts:
    - path: "app/Http/Controllers/Gestao/ViabilityServiceTypeController.php"
      provides: "CRUD server-driven dos tipos de serviço (console SEDUR)"
      contains: "ViabilityServiceType"
    - path: "resources/js/pages/gestao/tipos-servico/index.tsx"
      provides: "Tela de manutenção (datatable + modal criar/editar) no padrão console"
      contains: "tipos-servico"
  key_links:
    - from: "routes/gestao.php"
      to: "manter-tipos-servico"
      via: "grupo de rotas protegido pela permissão da fase (08-02)"
      pattern: "permission:manter-tipos-servico"
---

<objective>
Cadastro administrável dos **tipos de serviço** (HU-061 RN-005) — viabilidade de 1º estabelecimento, alteração de endereço/atividade, revisão de TVL, renovação etc. — como DADOS (tabela + CRUD), porque o tipo determina fluxo, documentos e relatórios. CRUD backend no console SEDUR + tela no padrão de listagem da gestão.

Purpose: a solicitação (08-05) precisa selecionar um tipo de serviço parametrizável; sem cadastro administrável, o tipo viraria enum hardcoded (proibido). Lista oficial é pendência SEDUR (seed mínimo no fechamento 08-16) — o cadastro nasce administrável e recebe a carga oficial sem deploy.
Output: ViabilityServiceTypeController (index/store/update/destroy-ou-toggle) + FormRequests + rotas sob `permission:manter-tipos-servico` + tela `gestao/tipos-servico/index.tsx`.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-solicitacao-de-viabilidade/08-CONTEXT.md
@.planning/phases/08-solicitacao-de-viabilidade/08-01-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-02-SUMMARY.md

# PADRÕES A ESPELHAR (CRUD de gestão + datatable + permissão + auditoria) — ler antes de codar:
@app/Http/Controllers/Gestao/CnaeController.php
@app/Http/Requests/Gestao/StoreCnaeRequest.php
@routes/gestao.php
@resources/js/pages/gestao/cnaes/index.tsx
@app/Models/ViabilityServiceType.php
</context>

<skill>
ATIVAR `inertia-react-development` para a tela. Padrão de listagem da gestão (PADRÃO Fase 2.4): PageHeader → Card(CardHeader com ação primária) → CardContent(TableToolbar → DataTable → Pagination); criar/editar em Modal; ações destrutivas/toggle via confirm-dialog. Console SEDUR (variant="console").
</skill>

<tasks>

<task type="auto">
  <name>Task 1: CRUD backend dos tipos de serviço (controller + requests + rotas) com permissão e auditoria</name>
  <read_first>app/Http/Controllers/Gestao/CnaeController.php, app/Http/Requests/Gestao/StoreCnaeRequest.php, routes/gestao.php</read_first>
  <files>app/Http/Controllers/Gestao/ViabilityServiceTypeController.php, app/Http/Requests/Gestao/StoreViabilityServiceTypeRequest.php, app/Http/Requests/Gestao/UpdateViabilityServiceTypeRequest.php, routes/gestao.php, tests/Feature/Solicitacao/ViabilityServiceTypeCrudTest.php</files>
  <action>
    TDD estrito. Controller `Gestao/ViabilityServiceTypeController` (espelhar CnaeController@index server-driven: busca por code/name caseSensitive:false, ordenação whitelistada [code,name], per_page parametrizado — reusar um parâmetro ui existente ou constante; ecoa filters/perPageOptions). store/update via FormRequests; code IMUTÁVEL na edição (UpdateRequest não inclui code nas rules — padrão CNPJ/CNAE). destroy → preferir TOGGLE de active (não excluir registro já referenciado por solicitações; espelhar inativação de usuário/CNAE). Auditoria explícita do CRUD (AuditService log 'solicitacoes' event 'tipo-servico-*' OU HasAuditoria no model — escolher e ser consistente; preferir HasAuditoria no ViabilityServiceType para created/updated, como Company).

    Rotas em `routes/gestao.php` no grupo `auth:gestao`: novo subgrupo `Route::middleware('permission:manter-tipos-servico')->prefix('tipos-servico')->name('tipos-servico.')` com index (GET), store (POST), update (PUT {serviceType}), e o toggle/destroy.

    `ViabilityServiceTypeCrudTest` (SQLite, actingAs($admin,'gestao')):
    - test_lista_exige_permissao: usuário sem manter-tipos-servico → 403 (e Activity 'seguranca' bloqueado — CA-04).
    - test_cria_tipo_de_servico: POST cria com code/name; auditado.
    - test_code_unico: code duplicado → erro de validação.
    - test_code_imutavel_na_edicao: PUT com code diferente NÃO altera o code.
    - test_desativar_preserva_registro: toggle active=false NÃO remove a linha (histórico).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=ViabilityServiceTypeCrudTest</verify>
  <acceptance_criteria>
    - grep "permission:manter-tipos-servico" routes/gestao.php
    - grep "class ViabilityServiceTypeController" e o index server-driven.
    - ViabilityServiceTypeCrudTest verde (permissão, CRUD, code único/imutável, desativação preserva).
  </acceptance_criteria>
  <done>CRUD backend dos tipos de serviço atrás da permissão, com code único/imutável, desativação que preserva histórico e auditoria.</done>
</task>

<task type="auto">
  <name>Task 2: tela de manutenção no console + navegação</name>
  <read_first>resources/js/pages/gestao/cnaes/index.tsx, resources/js/components/ui/data-table, resources/js/components/app/app-sidebar.tsx</read_first>
  <files>resources/js/pages/gestao/tipos-servico/index.tsx</files>
  <action>
    Tela `gestao/tipos-servico/index.tsx` no padrão de listagem da gestão (console): PageHeader, Card com ação primária "Novo tipo de serviço", DataTable (code, name, flow_hint, situação ativo/inativo com Badge), TableToolbar (busca), Pagination; criar/editar em Modal (campos code/name/flow_hint/active); toggle de ativação via confirm-dialog. Acrescentar o item de menu no grupo apropriado da sidebar da gestão (atrás da permissão manter-tipos-servico — seguir como os itens existentes são condicionados por permissão).
  </action>
  <verify>npx tsc --noEmit 2>&1 | tail -20 && npm run build 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - grep "tipos-servico" resources/js/pages/gestao/tipos-servico/index.tsx
    - typecheck e build verdes.
    - Item de navegação adicionado condicionado à permissão.
  </acceptance_criteria>
  <done>Tela de manutenção dos tipos de serviço no console com CRUD por modal e toggle; navegação por permissão.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=ViabilityServiceTypeCrudTest` verde.
- `npx tsc --noEmit` e `npm run build` verdes.
- `php artisan test --compact` — suíte completa CONTINUA verde.
</verification>

<success_criteria>
- Tipos de serviço administráveis (CRUD) atrás da permissão, com auditoria, code único/imutável e desativação que preserva histórico.
- Tela no padrão console; navegação por permissão. Sem valor hardcoded; lista oficial é seed (08-16) substituível sem deploy.
</success_criteria>

<output>
Criar `.planning/phases/08-solicitacao-de-viabilidade/08-03-SUMMARY.md`: rotas/nomes, shape das props da tela, decisão de auditoria (HasAuditoria vs explícita) e como o tipo de serviço é consultado pela criação (08-05). Insumo de 08-05 (select de tipo) e 08-16 (seed mínimo).
</output>

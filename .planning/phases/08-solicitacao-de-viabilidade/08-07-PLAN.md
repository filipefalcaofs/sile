---
phase: 08-solicitacao-de-viabilidade
plan: 07
type: execute
wave: 4
depends_on: ["08-05"]
files_modified:
  - app/Http/Controllers/Portal/SolicitacaoAtividadeController.php
  - app/Http/Requests/Portal/UpdateSolicitacaoAtividadesRequest.php
  - routes/portal.php
  - tests/Feature/Solicitacao/InformarAtividadesTest.php
autonomous: true
requirements: [HU-064, HU-065]

must_haves:
  truths:
    - "O requerente define a atividade principal (CNAE principal) e os CNAEs complementares da solicitação a partir da tabela oficial (HU-064/065)"
    - "O limite de CNAEs complementares é parametrizável (solicitacao.cnaes_complementares.max, default 99) e validado na aplicação"
    - "Exatamente um CNAE principal (is_primary) e unicidade (request, cnae) — espelha company_cnae; só CNAEs ativos são aceitos na seleção"
    - "Mudar atividades/CNAEs zera a simulação anterior (markSimulationStale, HU-063 RN-005); só o dono em rascunho edita; auditado"
  artifacts:
    - path: "app/Http/Controllers/Portal/SolicitacaoAtividadeController.php"
      provides: "Define principal + complementares (sync), valida limite e ativos"
      contains: "is_primary"
  key_links:
    - from: "app/Http/Controllers/Portal/SolicitacaoAtividadeController.php"
      to: "App\\Models\\ViabilityRequest"
      via: "cnaes()->sync com is_primary (conjunto exato — padrão CompanyCnaeService)"
      pattern: "->cnaes\\(\\)"
    - from: "app/Http/Controllers/Portal/SolicitacaoAtividadeController.php"
      to: "App\\Support\\Settings"
      via: "lê solicitacao.cnaes_complementares.max (limite administrável)"
      pattern: "cnaes_complementares.max"
---

<objective>
Instrução das atividades (HU-064) e CNAEs complementares (HU-065) do rascunho: define o CNAE principal e os complementares (até o limite parametrizável, default 99) a partir da tabela oficial, com exatamente um principal e unicidade — espelhando o pivot `company_cnae`/`CompanyCnaeService` da Fase 3. Plano IRMÃO PARALELO do 08-06 (arquivos distintos; ambos dependem só do 08-05).

Purpose: critério 1 do ROADMAP (atividade principal + CNAEs complementares até 99). Reusa o picker/busca de CNAEs e a disciplina de escrita transacional do pivot (Fase 3). Anti-fachada: só CNAEs ativos entram; o limite é parâmetro (não hardcoded); mudar a lista invalida a simulação anterior.
Output: SolicitacaoAtividadeController (update principal+complementares) + UpdateSolicitacaoAtividadesRequest + rota + feature tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-solicitacao-de-viabilidade/08-CONTEXT.md
@.planning/phases/08-solicitacao-de-viabilidade/08-01-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-02-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-05-SUMMARY.md
@docs/SILE_HUs_Completas_MD/EP08-Solicitacao-de-Viabilidade/HU-064-Informar-atividade-econômica.md
@docs/SILE_HUs_Completas_MD/EP08-Solicitacao-de-Viabilidade/HU-065-Informar-CNAEs-complementares.md

# REUSO (pivot transacional + só ativos + escopo) — ler antes de codar:
@app/Services/CompanyCnaeService.php
@app/Http/Controllers/Portal/CompanyCnaeController.php
@app/Http/Controllers/Portal/CnaeSearchController.php
@app/Models/ViabilityRequest.php
@routes/portal.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: update de atividades (principal + complementares com limite/ativos) + invalidar simulação + rota</name>
  <read_first>app/Services/CompanyCnaeService.php, app/Http/Controllers/Portal/CompanyCnaeController.php, app/Models/ViabilityRequest.php, routes/portal.php</read_first>
  <files>app/Http/Controllers/Portal/SolicitacaoAtividadeController.php, app/Http/Requests/Portal/UpdateSolicitacaoAtividadesRequest.php, routes/portal.php, tests/Feature/Solicitacao/InformarAtividadesTest.php</files>
  <action>
    TDD estrito. `Portal/SolicitacaoAtividadeController@update(UpdateSolicitacaoAtividadesRequest, ViabilityRequest $solicitacao)`:
    - `Gate::authorize('update', $solicitacao)` (dono + rascunho).
    - escrita do pivot viability_request_cnaes em DB::transaction, espelhando CompanyCnaeService: sync com conjunto exato incluindo o principal (is_primary=true) e os complementares (is_primary=false). Garantir EXATAMENTE um principal e unicidade (request, cnae).
    - `$solicitacao->markSimulationStale()` (mudou CNAEs → invalida simulação, RN-005).
    - auditoria explícita do conjunto (event 'solicitacao-cnaes', antes/depois) coexistindo com o updated do HasAuditoria.
    `UpdateSolicitacaoAtividadesRequest`:
    - `principal_cnae_id`: required, Rule::exists cnaes where active.
    - `complementares` (array): cada item Rule::exists cnaes where active; principal NÃO pode estar entre os complementares (after()); `max` itens = `Settings::get('solicitacao.cnaes_complementares.max', config(...,99))` (validação dinâmica — espelhar o padrão de validation_rules dinâmico [02-07]); sem duplicados.
    - Mensagens pt-BR.

    Rota: `PUT solicitacoes/{solicitacao}/atividades` (name solicitacoes.atividades) — DEPOIS das literais. (A busca de CNAEs já existe: reusar GET portal.cnaes.search.)

    `InformarAtividadesTest` (SQLite):
    - test_define_principal_e_complementares (CA-01): grava 1 principal + N complementares; unicidade e is_primary corretos.
    - test_respeita_limite_parametrizavel (HU-065): com solicitacao.cnaes_complementares.max=2, 3 complementares → erro; com default 99 aceita mais. (gravar Parameter no teste).
    - test_so_cnaes_ativos: CNAE inativo → erro de validação.
    - test_principal_unico_e_nao_duplicado: principal entre complementares → erro; CNAE repetido → erro.
    - test_mudanca_zera_simulacao (RN-005): simulated_at preenchido → update zera.
    - test_autorizacao (CA-04): terceiro 403; protocolada bloqueada pela policy.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=InformarAtividadesTest</verify>
  <acceptance_criteria>
    - grep "cnaes_complementares.max" app/Http/Requests/Portal/UpdateSolicitacaoAtividadesRequest.php
    - grep "markSimulationStale" app/Http/Controllers/Portal/SolicitacaoAtividadeController.php
    - grep "solicitacoes/{solicitacao}/atividades" routes/portal.php
    - InformarAtividadesTest verde (principal+complementares, limite, ativos, unicidade, invalidação, autorização).
  </acceptance_criteria>
  <done>Atividade principal + complementares com limite parametrizável e só ativos, um principal único, simulação invalidada ao mudar, autorização por policy e auditoria.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=InformarAtividadesTest` verde.
- `php artisan test --compact` — suíte completa CONTINUA verde.
</verification>

<success_criteria>
- Atividade principal + CNAEs complementares (até o limite parametrizável) a partir da tabela oficial, só ativos, um principal único, unicidade garantida.
- Mudança invalida simulação (RN-005); autorização por policy; auditoria. Paralelo ao 08-06 sem conflito de arquivos.
</success_criteria>

<output>
Criar `.planning/phases/08-solicitacao-de-viabilidade/08-07-SUMMARY.md`: rota/contrato do update de atividades, como o pivot é sincronizado (is_primary), o parâmetro de limite e a invalidação da simulação. Insumo direto de 08-09 (simulação itera os CNAEs), 08-08 (resolver une requisitos dos CNAEs) e 08-13 (UI picker).
</output>

---
phase: 08-solicitacao-de-viabilidade
plan: 08
type: execute
wave: 5
depends_on: ["08-04", "08-06", "08-07"]
files_modified:
  - app/Services/Solicitacao/DocumentRequirementResolver.php
  - app/Http/Controllers/Portal/SolicitacaoDocumentoController.php
  - app/Http/Requests/Portal/StoreSolicitacaoDocumentoRequest.php
  - routes/portal.php
  - tests/Feature/Solicitacao/AnexarDocumentoTest.php
  - tests/Feature/Solicitacao/DocumentRequirementResolverTest.php
autonomous: true
requirements: [HU-066, HU-067]

must_haves:
  truths:
    - "O requerente anexa documentos via Storage com disk parametrizado (storage.documentos.disk, nunca público); MIME/tamanho validados por parâmetro; grava disk/path/sha256 (HU-066)"
    - "O DocumentRequirementResolver calcula os obrigatórios = união dos document_requirements.required dos CNAEs da solicitação ∪ condicionais (fachada SEMPRE; concessão SE área pública) — HU-067"
    - "Faltar obrigatório é comunicado (aviso) e BLOQUEIA o protocolo (consumido em 08-10) — nunca silencioso; a tabela por-CNAE vazia ainda valida os obrigatórios-base conhecidos"
    - "Download é por streaming SÓ autenticado (dono via policy); anexo substituível antes do protocolo; tudo auditado (RN-002)"
  artifacts:
    - path: "app/Services/Solicitacao/DocumentRequirementResolver.php"
      provides: "União dos requisitos dos CNAEs + condicionais (fachada/concessão); faltantes"
      contains: "class DocumentRequirementResolver"
    - path: "app/Http/Controllers/Portal/SolicitacaoDocumentoController.php"
      provides: "Upload (Storage, disk param), download streaming autenticado, remoção"
      contains: "Storage::disk"
  key_links:
    - from: "app/Http/Controllers/Portal/SolicitacaoDocumentoController.php"
      to: "App\\Support\\Settings"
      via: "disk = Settings::get('storage.documentos.disk', config(...,'local')) — nunca público"
      pattern: "storage.documentos.disk"
    - from: "app/Services/Solicitacao/DocumentRequirementResolver.php"
      to: "App\\Models\\DocumentRequirement"
      via: "união dos required dos CNAEs (08-04) + condicionais por indicadores (08-06)"
      pattern: "required"
---

<objective>
Anexos (HU-066) e validação documental (HU-067): upload via Storage com disk parametrizado (nunca público), MIME/tamanho por parâmetro, gravando disk/path/sha256; e o `DocumentRequirementResolver` que une os requisitos obrigatórios dos CNAEs da solicitação às condicionais base (foto da fachada sempre; termo de concessão se área pública). Faltar obrigatório vira aviso que BLOQUEIA o protocolo (consumido no 08-10), nunca silencioso.

Purpose: critério 2 do ROADMAP (documentos anexados e obrigatoriedade por CNAE validada antes do protocolo). Anti-fachada: a tabela por-CNAE nasce vazia (carga oficial pendente SEDUR), mas os obrigatórios-base conhecidos JÁ são validados agora; download streaming só autenticado (LGPD). Reusa Storage/URLs do Laravel (sem dep nova).
Output: DocumentRequirementResolver + SolicitacaoDocumentoController (upload/download/remove) + StoreSolicitacaoDocumentoRequest + rotas + feature tests (Storage::fake).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-solicitacao-de-viabilidade/08-CONTEXT.md
@.planning/phases/08-solicitacao-de-viabilidade/08-01-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-02-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-04-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-06-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-07-SUMMARY.md
@docs/SILE_HUs_Completas_MD/EP08-Solicitacao-de-Viabilidade/HU-066-Anexar-documentos.md
@docs/SILE_HUs_Completas_MD/EP08-Solicitacao-de-Viabilidade/HU-067-Validar-documentos-obrigatorios.md

# PADRÕES A ESPELHAR (policy/representação + Settings + auditoria) — ler antes de codar:
@app/Http/Controllers/Portal/CompanyController.php
@app/Policies/ViabilityRequestPolicy.php
@app/Support/Settings.php
@app/Models/DocumentRequirement.php
@app/Models/ViabilityRequest.php
</context>

<skill>
ATIVAR `laravel-boost` e usar `search-docs` (queries: ['filesystem storage', 'file upload validation', 'streamed download response']) antes de codar — Storage/UploadedFile/streamDownload versionados.
</skill>

<tasks>

<task type="auto">
  <name>Task 1: DocumentRequirementResolver (união dos CNAEs + condicionais) + faltantes</name>
  <read_first>app/Models/DocumentRequirement.php, app/Models/ViabilityRequest.php, .planning/phases/08-solicitacao-de-viabilidade/08-04-SUMMARY.md</read_first>
  <files>app/Services/Solicitacao/DocumentRequirementResolver.php, tests/Feature/Solicitacao/DocumentRequirementResolverTest.php</files>
  <action>
    TDD estrito. `DocumentRequirementResolver`:
    - `required(ViabilityRequest $request): Collection<DocumentRequirement>` — união dos document_requirements `required=true,active=true` vinculados aos CNAEs da solicitação (via cnae_document_requirement) ∪ condicionais BASE: foto da fachada SEMPRE; termo de concessão de uso SE `is_public_area`. As condicionais base são DocumentRequirements seedados (códigos estáveis, ex.: 'foto-fachada', 'termo-concessao' — criados no seed 08-16; o resolver os referencia por code e degrada honesto se ausentes no ambiente de teste, ou o teste os cria). Sem duplicar.
    - `missing(ViabilityRequest $request): Collection<DocumentRequirement>` — required() menos os requisitos já cobertos por viability_request_documents (por requirement_id). Base do bloqueio do protocolo (08-10) e do aviso.

    `DocumentRequirementResolverTest` (SQLite):
    - test_uniao_dos_requisitos_dos_cnaes: 2 CNAEs com requisitos required distintos → required() contém ambos, sem duplicar requisito compartilhado.
    - test_fachada_sempre_obrigatoria: required() sempre inclui a foto da fachada.
    - test_concessao_so_quando_area_publica: is_public_area=true → inclui termo de concessão; false → não.
    - test_missing_reflete_anexos_existentes: anexar um requisito → some de missing().
    - test_tabela_por_cnae_vazia_ainda_valida_base (anti-fachada): sem requisitos por-CNAE, required() ainda traz os obrigatórios-base (fachada).
  </action>
  <verify>php artisan test --compact --filter=DocumentRequirementResolverTest</verify>
  <acceptance_criteria>
    - grep "class DocumentRequirementResolver" e métodos required()/missing().
    - grep -i "fachada\|is_public_area\|concess" no resolver (condicionais base).
    - DocumentRequirementResolverTest verde (união, fachada sempre, concessão condicional, missing, base com tabela vazia).
  </acceptance_criteria>
  <done>Resolver une requisitos dos CNAEs + condicionais base (fachada/concessão); missing() é a base do bloqueio do protocolo; valida o conhecido mesmo com tabela por-CNAE vazia.</done>
</task>

<task type="auto">
  <name>Task 2: upload/download/remove de documentos (Storage disk param, sha256, streaming autenticado) + rotas</name>
  <read_first>app/Support/Settings.php, app/Policies/ViabilityRequestPolicy.php, app/Http/Controllers/Portal/CompanyController.php, routes/portal.php</read_first>
  <files>app/Http/Controllers/Portal/SolicitacaoDocumentoController.php, app/Http/Requests/Portal/StoreSolicitacaoDocumentoRequest.php, routes/portal.php, tests/Feature/Solicitacao/AnexarDocumentoTest.php</files>
  <action>
    TDD estrito (usar `Storage::fake()` nos testes). `Portal/SolicitacaoDocumentoController`:
    - `store(StoreSolicitacaoDocumentoRequest, ViabilityRequest $solicitacao)`: Gate::authorize('update', $solicitacao) (dono + rascunho — anexo só antes do protocolo). disk = `Settings::get('storage.documentos.disk', config('sile.storage.documentos.disk','local'))` (NUNCA disk público). Salva o arquivo (`$file->store(...)` no disk), grava viability_request_documents (requirement_id opcional, disk da época, path, original_name, mime_type, size, sha256 = hash_file('sha256', ...), uploaded_by_user_id = ator real). Substituição antes do protocolo (remover anterior do mesmo requirement). Auditoria (event 'documento-anexado').
    - `download(ViabilityRequest $solicitacao, ViabilityRequestDocument $documento)`: Gate::authorize('view', $solicitacao) → streamDownload do disk gravado (só autenticado; nunca URL pública). Auditoria de acesso a dado (event 'documento-download').
    - `destroy(...)`: remover anexo (só rascunho, dono); apaga do disk + linha; auditado.
    `StoreSolicitacaoDocumentoRequest`: file required; mimetypes/mimes derivados de `solicitacao.anexos.mime_permitidos` (Settings, validação dinâmica); max KB derivado de `solicitacao.anexos.max_mb`*1024; requirement_id opcional (Rule::exists). Mensagens pt-BR.

    Rotas (grupo autenticado): `POST solicitacoes/{solicitacao}/documentos`, `GET solicitacoes/{solicitacao}/documentos/{documento}/download`, `DELETE solicitacoes/{solicitacao}/documentos/{documento}` — DEPOIS das literais.

    `AnexarDocumentoTest` (SQLite, Storage::fake):
    - test_anexa_documento (CA-01): upload válido → arquivo no disk fake + linha com sha256/disk/path; auditado.
    - test_rejeita_mime_nao_permitido / test_rejeita_tamanho_acima_do_maximo: validação parametrizada (gravar Parameter no teste).
    - test_download_so_autenticado_e_dono (LGPD): dono baixa (streaming); terceiro → 403 auditado.
    - test_disk_nunca_publico: o disk usado vem do parâmetro (default local), não 'public'.
    - test_substituir_e_remover_antes_do_protocolo: substituição/remoção em rascunho funciona; (a imutabilidade após protocolo é coberta no 08-10).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=AnexarDocumentoTest</verify>
  <acceptance_criteria>
    - grep "storage.documentos.disk" app/Http/Controllers/Portal/SolicitacaoDocumentoController.php
    - grep "sha256" no controller (hash gravado)
    - grep "streamDownload\|response()->download" no controller (download autenticado, sem URL pública)
    - AnexarDocumentoTest verde (upload, MIME/tamanho, download autenticado/dono, disk não-público).
  </acceptance_criteria>
  <done>Upload via Storage (disk parametrizado, nunca público) com sha256; download streaming só do dono autenticado; MIME/tamanho parametrizados; auditado.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="DocumentRequirementResolverTest|AnexarDocumentoTest"` verde.
- `php artisan test --compact` — suíte completa CONTINUA verde.
</verification>

<success_criteria>
- Anexos via Storage (disk parametrizado, nunca público) com MIME/tamanho parametrizados e sha256; download streaming só autenticado/dono (LGPD).
- Resolver une requisitos dos CNAEs + condicionais base (fachada/concessão); missing() bloqueia o protocolo (08-10) com aviso; valida o conhecido mesmo com tabela por-CNAE vazia. Sem dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/08-solicitacao-de-viabilidade/08-08-SUMMARY.md`: contrato do resolver (required/missing), códigos estáveis dos requisitos-base (fachada/concessão), rotas de documentos e a estratégia de disk/streaming. Insumo direto de 08-10 (bloqueio no protocolo) e 08-13 (UI de upload).
</output>

---
phase: 08-solicitacao-de-viabilidade
plan: 12
type: execute
wave: 7
depends_on: ["08-10"]
files_modified:
  - app/Services/Solicitacao/CancelarSolicitacaoService.php
  - app/Http/Controllers/Portal/CancelamentoSolicitacaoController.php
  - app/Http/Requests/Portal/CancelarSolicitacaoRequest.php
  - config/sile.php
  - database/seeders/ParameterSeeder.php
  - routes/portal.php
  - tests/Feature/Solicitacao/CancelarSolicitacaoTest.php
  - tests/Feature/Seeders/ParameterSeederTest.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
autonomous: true
requirements: [HU-070]

must_haves:
  truths:
    - "O dono cancela a solicitação enquanto não decidida — de rascunho e de protocolada (antes da decisão) — via StateMachine (transição auditada + timeline)"
    - "Os estados canceláveis são parametrizáveis (solicitacao.cancelamento.estados_cancelaveis, default rascunho+protocolada) — definição fina é pendência SEDUR"
    - "Cancelar grava cancelled_at/cancelled_reason/cancelled_by_user_id e registra o marco na timeline; tentativa fora dos estados canceláveis é bloqueada e auditada"
    - "Só o dono/representado cancela (policy); auditoria RN-002"
  artifacts:
    - path: "app/Services/Solicitacao/CancelarSolicitacaoService.php"
      provides: "Cancelamento via StateMachine com estados canceláveis parametrizáveis"
      contains: "estados_cancelaveis"
  key_links:
    - from: "app/Services/Solicitacao/CancelarSolicitacaoService.php"
      to: "App\\Services\\Solicitacao\\ViabilityRequestStateMachine"
      via: "transition(...→cancelada) auditada + timeline"
      pattern: "->transition("
---

<objective>
Cancelar solicitação (HU-070): o dono interrompe um processo indevido/duplicado enquanto NÃO decidido — de rascunho e de protocolada (antes da decisão) — via StateMachine (transição auditada + marco na timeline). Os estados canceláveis são parametrizáveis (default rascunho+protocolada); a definição fina é pendência SEDUR.

Purpose: critério 3 do ROADMAP (cancelar conforme regras). Reusa a StateMachine (08-01) e a policy (08-05). Anti-fachada: cancelar grava de verdade (cancelled_*) e registra a transição; fora dos estados canceláveis bloqueia com aviso e audita; nada hardcoded (estados em parâmetro).
Output: CancelarSolicitacaoService + CancelamentoSolicitacaoController + request + 1 parâmetro novo (estados canceláveis) + fallback + rota + feature tests + atualização das contagens de seeder (48→49).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-solicitacao-de-viabilidade/08-CONTEXT.md
@.planning/phases/08-solicitacao-de-viabilidade/08-01-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-02-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-05-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-10-SUMMARY.md
@docs/SILE_HUs_Completas_MD/EP08-Solicitacao-de-Viabilidade/HU-070-Cancelar-solicitacao.md

# PADRÕES A ESPELHAR (StateMachine + policy + parâmetro + seeder counts) — ler antes de codar:
@app/Services/Solicitacao/ViabilityRequestStateMachine.php
@app/Policies/ViabilityRequestPolicy.php
@database/seeders/ParameterSeeder.php
@config/sile.php
@routes/portal.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: parâmetro estados canceláveis (catálogo 48→49) + fallback + contagens</name>
  <read_first>database/seeders/ParameterSeeder.php, config/sile.php, tests/Feature/Seeders/ParameterSeederTest.php</read_first>
  <files>database/seeders/ParameterSeeder.php, config/sile.php, tests/Feature/Seeders/ParameterSeederTest.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <action>
    TDD. Acrescentar ao catálogo (grupo 'solicitacao'):
    - `solicitacao.cancelamento.estados_cancelaveis`: json, '["rascunho","protocolada"]', ['required','json'], 'Estados em que a solicitação pode ser cancelada pelo requerente (antes da decisão) — definição fina pendente SEDUR'.
    Fallback em config/sile.php: `solicitacao.cancelamento.estados_cancelaveis => ['rascunho','protocolada']`.
    Atualizar contagens: ParameterSeederTest (48→49 nos dois pontos) e DatabaseSeederTest (48→49 nos dois pontos); a lista de grupos NÃO muda ('solicitacao' já existe).
  </action>
  <verify>php artisan test --compact --filter="ParameterSeederTest|DatabaseSeederTest"</verify>
  <acceptance_criteria>
    - grep "solicitacao.cancelamento.estados_cancelaveis" database/seeders/ParameterSeeder.php e config/sile.php
    - grep "assertSame(49" nos dois testes de seeder.
    - ParameterSeederTest e DatabaseSeederTest verdes.
  </acceptance_criteria>
  <done>Parâmetro de estados canceláveis no catálogo (49) com fallback; contagens travadas.</done>
</task>

<task type="auto">
  <name>Task 2: CancelarSolicitacaoService + controller/request + rota + UI de cancelamento</name>
  <read_first>app/Services/Solicitacao/ViabilityRequestStateMachine.php, app/Policies/ViabilityRequestPolicy.php, routes/portal.php</read_first>
  <files>app/Services/Solicitacao/CancelarSolicitacaoService.php, app/Http/Controllers/Portal/CancelamentoSolicitacaoController.php, app/Http/Requests/Portal/CancelarSolicitacaoRequest.php, routes/portal.php, tests/Feature/Solicitacao/CancelarSolicitacaoTest.php</files>
  <action>
    TDD estrito. `CancelarSolicitacaoService::cancel(ViabilityRequest $request, User $actor, string $reason): void`:
    - lê os estados canceláveis (`Settings::get('solicitacao.cancelamento.estados_cancelaveis', config(...))`); se o status atual não está na lista → lança `CancelamentoNaoPermitidoException` (bloqueio com aviso) e audita logBlocked.
    - em DB::transaction: grava cancelled_at/cancelled_reason/cancelled_by_user_id e `ViabilityRequestStateMachine::transition($request, Cancelada, $actor, reason: $reason, publicLabel: 'Cancelada a pedido do requerente')` (timeline + auditoria).
    `Portal/CancelamentoSolicitacaoController@destroy(CancelarSolicitacaoRequest, ViabilityRequest $solicitacao)`: Gate::authorize('cancel', $solicitacao); chama o service; traduz a exceção em flash.error; sucesso → redirect com status.
    `CancelarSolicitacaoRequest`: reason required (string, motivo do cancelamento). Mensagens pt-BR.
    Rota: `DELETE solicitacoes/{solicitacao}` (name solicitacoes.cancelar) — DEPOIS das literais. UI: a ação de cancelar é exposta na lista "Minhas solicitações"/detalhe via confirm-dialog (a tela vive no 08-13/08-11; aqui garanta o endpoint + um confirm-dialog mínimo se necessário, mas a integração visual completa é do 08-13).

    `CancelarSolicitacaoTest` (SQLite):
    - test_cancela_rascunho (CA-01): status cancelada, cancelled_* preenchidos, transição + auditoria.
    - test_cancela_protocolada_antes_da_decisao: protocolada → cancelada (dentro dos estados canceláveis).
    - test_bloqueia_fora_dos_estados_cancelaveis: com parâmetro só ['rascunho'], cancelar protocolada → bloqueado + auditado (CancelamentoNaoPermitidoException).
    - test_so_dono_cancela (CA-04): terceiro → 403 auditado.
    - test_motivo_obrigatorio: sem reason → validação.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=CancelarSolicitacaoTest</verify>
  <acceptance_criteria>
    - grep "estados_cancelaveis" app/Services/Solicitacao/CancelarSolicitacaoService.php
    - grep "->transition(" e a transição para Cancelada.
    - grep "solicitacoes/{solicitacao}" routes/portal.php com DELETE (name solicitacoes.cancelar).
    - CancelarSolicitacaoTest verde (rascunho, protocolada, bloqueio fora dos estados, autorização, motivo).
  </acceptance_criteria>
  <done>Cancelamento via StateMachine com estados parametrizáveis, motivo obrigatório, bloqueio auditado fora dos estados, autorização por policy.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="CancelarSolicitacaoTest|ParameterSeederTest|DatabaseSeederTest"` verde.
- `php artisan test --compact` — suíte completa CONTINUA verde.
</verification>

<success_criteria>
- Cancelar de rascunho e protocolada (antes da decisão) com estados parametrizáveis (default), transição auditada + timeline, motivo obrigatório.
- Fora dos estados canceláveis bloqueia com aviso e audita; só o dono cancela; catálogo 49 travado.
</success_criteria>

<output>
Criar `.planning/phases/08-solicitacao-de-viabilidade/08-12-SUMMARY.md`: assinatura do service, o parâmetro de estados canceláveis (49), a exceção e a rota (solicitacoes.cancelar). Insumo direto de 08-13 (ação de cancelar na UI) e 08-16 (contagens/smoke).
</output>

---
phase: 08-solicitacao-de-viabilidade
plan: 16
type: execute
wave: 9
depends_on: ["08-11", "08-12", "08-13", "08-14", "08-15"]
files_modified:
  - database/seeders/ViabilityServiceTypeSeeder.php
  - database/seeders/DocumentRequirementSeeder.php
  - database/seeders/SolicitacaoDevSeeder.php
  - database/seeders/DatabaseSeeder.php
  - app/Console/Commands/SolicitacaoProtocolarCommand.php
  - tests/Feature/Solicitacao/SolicitacaoGoldenCaseTest.php
  - tests/Feature/Solicitacao/SolicitacaoSmokeTest.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
autonomous: false
requirements: [HU-061, HU-062, HU-063, HU-064, HU-065, HU-066, HU-067, HU-068, HU-069, HU-070, HU-141, HU-148, HU-150]

must_haves:
  truths:
    - "Seeds dev preparam o ambiente: tipos de serviço (seed mínimo oficial-pendente), requisitos documentais base (fachada sempre; concessão), e solicitações de exemplo (rascunho/protocolada/cancelada/contingência) — dados fictícios, lógica REAL"
    - "Existe um comando solicitacao:protocolar (ou equivalente) que dá evidência real do fluxo de ponta a ponta a partir de uma solicitação instruída"
    - "Golden/smoke travam a regressão do fluxo: criar→instruir→(simular)→protocolar→consultar→cancelar executando os serviços reais; degradação honesta (sem zona → pendente; doc faltante bloqueia)"
    - "Verificação integral FRESCA: suíte SQLite verde (baseline + novos), @group postgis verde onde toca geometria/concorrência, pint + typecheck + build verdes"
    - "Smoke navegável aprovado (checkpoint humano): wizard cidadão de ponta a ponta + consulta pública assinada + contingência + atendimento presencial"
  artifacts:
    - path: "database/seeders/SolicitacaoDevSeeder.php"
      provides: "Solicitações de exemplo (dados fictícios; lógica real) para dev"
      contains: "ViabilityRequest"
    - path: "tests/Feature/Solicitacao/SolicitacaoSmokeTest.php"
      provides: "Smoke do fluxo de ponta a ponta executando os serviços reais"
      contains: "protocol"
  key_links:
    - from: "database/seeders/DatabaseSeeder.php"
      to: "database/seeders/SolicitacaoDevSeeder.php"
      via: "seeds da fase registrados no seed completo de dev"
      pattern: "SolicitacaoDevSeeder"
---

<objective>
Fechamento da Fase 8: seeds de desenvolvimento (tipos de serviço, requisitos documentais base, solicitações de exemplo), o comando de evidência real do fluxo, golden/smoke de regressão e a **verificação integral fresca** (suíte SQLite + @group postgis + pint + typecheck/build), encerrando com o **smoke navegável** (checkpoint humano).

Purpose: critério de pronto transversal do ROADMAP validado com EVIDÊNCIA FRESCA (regra de verificação antes de completar). Seeds usam dados fictícios mas a lógica é REAL (entrega-funcional); a lista oficial de tipos/requisitos é pendência SEDUR (seed mínimo, substituível). Golden/smoke provam a degradação honesta (sem zona → pendente; documento faltante bloqueia o protocolo).
Output: 3 seeders + registro no DatabaseSeeder + comando + golden/smoke tests + checkpoint de verificação integral e smoke navegável.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-solicitacao-de-viabilidade/08-CONTEXT.md
@.planning/phases/08-solicitacao-de-viabilidade/08-01-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-03-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-04-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-08-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-10-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-11-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-13-SUMMARY.md

# PADRÕES A ESPELHAR (seeds dev; comando de evidência; golden; smoke; verificação) — ler antes de codar:
@database/seeders/DatabaseSeeder.php
@database/seeders/CompanySeeder.php
@app/Console/Commands/ConsultaViabilidadeCommand.php
@tests/Feature/Viabilidade/ConsultaViabilidadeGoldenCaseTest.php
@tests/Feature/Geo/PhaseFourSmokeTest.php
@tests/Feature/Seeders/DatabaseSeederTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: seeds dev (tipos de serviço + requisitos base + solicitações de exemplo) + registro no DatabaseSeeder</name>
  <read_first>database/seeders/DatabaseSeeder.php, database/seeders/CompanySeeder.php</read_first>
  <files>database/seeders/ViabilityServiceTypeSeeder.php, database/seeders/DocumentRequirementSeeder.php, database/seeders/SolicitacaoDevSeeder.php, database/seeders/DatabaseSeeder.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <action>
    TDD. 
    - `ViabilityServiceTypeSeeder`: seed MÍNIMO de tipos de serviço (códigos estáveis: ex. 'viabilidade-1-estabelecimento', 'alteracao-endereco', 'alteracao-atividade', 'renovacao-tvl') — idempotente (upsert por code; preserva ajustes do admin). Lista oficial é pendência SEDUR (ressalva no PHPDoc).
    - `DocumentRequirementSeeder`: seed dos requisitos-base com códigos estáveis usados pelo resolver (08-08): 'foto-fachada' (required, condicional sempre), 'termo-concessao' (condicional área pública), e os documentos comuns (contrato de locação opcional). A obrigatoriedade POR CNAE entra VAZIA (tabela cnae_document_requirement sem carga — pendente SEDUR; sem fachada). Idempotente.
    - `SolicitacaoDevSeeder`: cria solicitações de EXEMPLO para o cidadão dev (cidadao@sile.dev): uma em rascunho (instruída), uma protocolada (com número e timeline), uma cancelada, uma de contingência — usando as FACTORIES e os SERVIÇOS REAIS quando fizer sentido (ex.: protocolar via ProtocolarSolicitacaoService para gerar número/timeline reais). Dados fictícios, lógica real (entrega-funcional). Idempotente (não duplicar em re-seed).
    - Registrar os três no `DatabaseSeeder` (após CompanySeeder; SolicitacaoDevSeeder por último, depende de empresas/CNAEs/quadros).
    - `DatabaseSeederTest`: acrescentar asserções — tipos de serviço seedados (>0), requisitos-base presentes (fachada/concessão por code), e solicitações de exemplo do cidadão (rascunho/protocolada/cancelada/contingência) com idempotência (contagens estáveis no re-seed). Manter a contagem de parâmetros vigente (49, após 08-12).
  </action>
  <verify>php artisan migrate:fresh --seed --env=testing && php artisan test --compact --filter=DatabaseSeederTest</verify>
  <acceptance_criteria>
    - grep "SolicitacaoDevSeeder" database/seeders/DatabaseSeeder.php
    - grep "foto-fachada" database/seeders/DocumentRequirementSeeder.php
    - DatabaseSeederTest verde (tipos/requisitos/solicitações de exemplo; idempotência).
  </acceptance_criteria>
  <done>Seeds dev (tipos mínimos, requisitos base, solicitações de exemplo via serviços reais) idempotentes e registrados; dados fictícios com lógica real.</done>
</task>

<task type="auto">
  <name>Task 2: comando de evidência + golden/smoke do fluxo de ponta a ponta</name>
  <read_first>app/Console/Commands/ConsultaViabilidadeCommand.php, tests/Feature/Viabilidade/ConsultaViabilidadeGoldenCaseTest.php, app/Services/Solicitacao/ProtocolarSolicitacaoService.php</read_first>
  <files>app/Console/Commands/SolicitacaoProtocolarCommand.php, tests/Feature/Solicitacao/SolicitacaoGoldenCaseTest.php, tests/Feature/Solicitacao/SolicitacaoSmokeTest.php</files>
  <action>
    TDD.
    - Comando auto-descoberto `solicitacao:protocolar {solicitacao}` (ou `solicitacao:fluxo`) que dá EVIDÊNCIA REAL: recebe uma solicitação instruída (id) e roda o ProtocolarSolicitacaoService imprimindo o número gerado, a transição e o resultado da simulação (se houver) — espelhar o estilo dos comandos de evidência (louos:enquadrar/viabilidade:consultar). Útil para homologação manual; exit 1 com aviso se faltar documento (bloqueio honesto).
    - `SolicitacaoSmokeTest` (SQLite; seeds reais + factories): executa o FLUXO de ponta a ponta com os SERVIÇOS REAIS — criar rascunho → instruir imóvel/área/atividades → (simular) → protocolar → consultar (autenticado) → cancelar — asserindo o estado a cada passo. Anti-fachada: provar que faltar documento obrigatório BLOQUEIA o protocolo; e que sem zona a simulação fica pendente (nunca permitido/não permitido).
    - `SolicitacaoGoldenCaseTest` (#[DataProvider]): casos-âncora entrada→esperado (ex.: rascunho completo → protocolado com número; rascunho sem fachada → bloqueado; protocolada → cancelável; CNAE de risco baixo → simulação tendência expressa) executando os serviços reais sobre o seed oficial. Espelhar o padrão golden das Fases 5/6/7.
  </action>
  <verify>php artisan test --compact --filter="SolicitacaoSmokeTest|SolicitacaoGoldenCaseTest" && php artisan solicitacao:protocolar --help</verify>
  <acceptance_criteria>
    - grep "solicitacao:" app/Console/Commands/SolicitacaoProtocolarCommand.php
    - grep "DataProvider" tests/Feature/Solicitacao/SolicitacaoGoldenCaseTest.php
    - SolicitacaoSmokeTest e SolicitacaoGoldenCaseTest verdes (fluxo real + degradação honesta).
  </acceptance_criteria>
  <done>Comando de evidência + smoke/golden do fluxo real de ponta a ponta, provando a degradação honesta (bloqueio documental, pendente sem zona).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: verificação integral fresca + smoke navegável (cidadão + pública assinada + contingência + presencial)</name>
  <what-built>
    A Fase 8 completa: fundação (schema/domínio/parâmetros/permissões), cadastros admin (tipos de serviço, requisitos documentais), wizard de solicitação (criar→imóvel→área→atividades→documentos→simulação→protocolar), protocolo com número único + primeiro evento de domínio, consulta (autenticada + pública assinada), cancelamento, e os canais de operador (contingência + atendimento presencial). Reuso real das Fases 1/3/4/5/6/7.
  </what-built>
  <how-to-verify>
    EVIDÊNCIA FRESCA — rodar e LER o output inteiro antes de afirmar (regra de verificação antes de completar):
    1. `vendor/bin/pint --dirty --format agent` — sem pendências.
    2. `php artisan test --compact` — suíte completa SQLite verde (baseline 657 da Fase 7 + os novos da fase; Seeders agora 49 parâmetros / 19 permissões). Registrar o total final no SUMMARY.
    3. `php artisan test --compact --group=postgis` com o container PostGIS de pé — verde (15 da Fase 4 + os novos: derivação da geometria do imóvel, área×polígono, concorrência do número de protocolo). Registrar o total.
    4. `npx tsc --noEmit` e `npm run build` — verdes.
    5. Evidência do comando: `php artisan solicitacao:protocolar <id de uma solicitação instruída do seed>` — imprime número/transição (e bloqueio honesto se faltar doc). Colar o output no SUMMARY.
    6. SMOKE NAVEGÁVEL (`composer dev`):
       a. Cidadão: logar no portal, percorrer o wizard com um endereço de Salvador (mapa demarca o polígono; simulação mostra tendência por CNAE — pendente sem zona COM motivo; faltar a fachada bloqueia o protocolo com aviso). Anexar a fachada e protocolar → número gerado; abrir a consulta do protocolo (timeline + status amigável + prazo com ressalva). Gerar o link público e abrir em aba anônima (sem login) → status + timeline sem dados sensíveis. Cancelar uma solicitação. Conferir mobile (375px) e dark mode.
       b. Operador (console): registrar uma solicitação em contingência (origem auditada) e conferir que segue o mesmo fluxo; iniciar um atendimento presencial por CPF e protocolar em nome do cidadão (banner "Atendendo: ..."; auditoria com os dois CPFs).
    7. Auditoria: `php artisan tinker --execute 'echo App\Models\Activity::where("log_name","solicitacoes")->count();'` > 0 (transições/protocolos/consultas auditados).
    8. Registrar no SUMMARY/STATE/ROADMAP as PENDÊNCIAS que permanecem (sem fachada): HU-071/072 DAM (Fase 13), origem Regin (Fase 13), HU-139 edifício comercial (complemento texto livre), lista oficial de tipos de serviço e requisitos por CNAE (carga SEDUR), prazo estimado real (HU-129/Fase 15), regra fina de duplicidade/cancelamento e procedimento de ciência presencial (SEDUR).
  </how-to-verify>
  <resume-signal>Responder "aprovado" (anexando outputs/prints) ou descrever divergências para correção antes de fechar a fase.</resume-signal>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact` (SQLite) verde; `--group=postgis` verde (container de pé).
- `npx tsc --noEmit` e `npm run build` verdes.
- Comando + golden + smoke dão evidência real; smoke navegável aprovado (cidadão + pública assinada + contingência + presencial).
</verification>

<success_criteria>
- Critérios 1, 2, 3, 5 e 6 do ROADMAP validados com evidência fresca; critério 4 (DAM) permanece BLOQUEADO → Fase 13 (registrado, não simulado).
- Seeds dev com lógica real; golden/smoke travam a regressão e provam a degradação honesta; pendências SEDUR registradas em STATE/ROADMAP.
- ZERO dependência nova; sem fachada.
</success_criteria>

<output>
Criar `.planning/phases/08-solicitacao-de-viabilidade/08-16-SUMMARY.md`: total final da suíte (SQLite + postgis), output do comando de evidência, os seeds/golden/smoke criados, o registro do smoke navegável aprovado e a lista explícita das pendências bloqueadas/adiadas (DAM/Regin/HU-139/tipos/requisitos/prazo/ciência presencial). Atualizar STATE.md e ROADMAP.md no fechamento da fase.
</output>

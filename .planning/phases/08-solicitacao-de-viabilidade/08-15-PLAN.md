---
phase: 08-solicitacao-de-viabilidade
plan: 15
type: execute
wave: 7
depends_on: ["08-10"]
files_modified:
  - database/migrations/*_create_assisted_attendances_table.php
  - app/Models/AssistedAttendance.php
  - database/factories/AssistedAttendanceFactory.php
  - app/Support/Representation/CurrentRepresentation.php
  - app/Http/Middleware/ResolveAssistedAttendance.php
  - app/Http/Controllers/Gestao/AssistedAttendanceController.php
  - routes/gestao.php
  - bootstrap/app.php
  - resources/js/pages/gestao/atendimento/index.tsx
  - tests/Feature/Solicitacao/AtendimentoPresencialTest.php
autonomous: true
requirements: [HU-150]

must_haves:
  truths:
    - "Atendente autorizado (permissão atendimento-presencial) inicia um atendimento informando o CPF do cidadão presente e opera 'em nome de' ele — toda ação registra ator (atendente) e beneficiário (cidadão), nunca como ação do próprio cidadão (HU-150 CA-01/RN-001)"
    - "Reusa o MESMO mecanismo de representação da Fase 1: o middleware do atendimento popula o MESMO CurrentRepresentation/Context que a procuração — a ViabilityRequestPolicy e a auditoria 'em nome de' funcionam sem recriar"
    - "O escopo do atendente é limitado por permissão (sem ações de análise/decisão); o vínculo expira (solicitacao.atendimento.expiracao_minutos) e exige reabertura (CA-02/CA-03)"
    - "Relatórios distinguem origem 'balcão' (dimensão); tudo auditado (RN-002)"
  artifacts:
    - path: "app/Models/AssistedAttendance.php"
      provides: "Vínculo leve de atendimento (attendant/citizen/started_at/expires_at/ended_at)"
      contains: "expires_at"
    - path: "app/Http/Middleware/ResolveAssistedAttendance.php"
      provides: "Popula o MESMO CurrentRepresentation/Context do atendimento ativo (espelha ResolveRepresentation)"
      contains: "CurrentRepresentation"
  key_links:
    - from: "app/Http/Middleware/ResolveAssistedAttendance.php"
      to: "App\\Support\\Representation\\CurrentRepresentation"
      via: "seta o representado (cidadão atendido) no MESMO serviço scoped da Fase 1"
      pattern: "CurrentRepresentation"
    - from: "routes/gestao.php"
      to: "atendimento-presencial"
      via: "rotas do atendimento protegidas pela permissão da fase (08-02)"
      pattern: "permission:atendimento-presencial"
---

<objective>
Canal de operador — **atendimento presencial assistido** (HU-150): o atendente autorizado opera o sistema "em nome de" o cidadão presente no balcão (inclusão digital), reusando INTEGRALMENTE o mecanismo de representação da Fase 1. Um modelo leve `AssistedAttendance` + um middleware análogo ao `ResolveRepresentation` populam o MESMO `CurrentRepresentation`/`Context`, de modo que a `ViabilityRequestPolicy` e a auditoria "em nome de" funcionam sem recriar nada. Vínculo com expiração curta parametrizável e escopo limitado por permissão.

Purpose: critério 6 do ROADMAP (atende cidadão presencialmente "em nome de" com trilha completa). Reuso máximo (CONTEXT: "custo de implementação baixo"). Anti-fachada: toda ação registra os dois CPFs (ator + beneficiário); o atendente não decide processo (escopo por permissão); o vínculo expira de verdade.
Output: migration + model + factory de AssistedAttendance, middleware ResolveAssistedAttendance, ajuste mínimo do CurrentRepresentation/Context, controller + rotas sob `permission:atendimento-presencial`, tela do console, feature tests. Plano IRMÃO PARALELO do 08-14.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-solicitacao-de-viabilidade/08-CONTEXT.md
@.planning/phases/08-solicitacao-de-viabilidade/08-01-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-02-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-05-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-10-SUMMARY.md
@docs/SILE_HUs_Completas_MD/EP08-Solicitacao-de-Viabilidade/HU-150-Atendimento-presencial-assistido.md

# REUSO (representação Fase 1 — espelhar, não recriar) — ler antes de codar:
@app/Support/Representation/CurrentRepresentation.php
@app/Http/Middleware/ResolveRepresentation.php
@app/Models/Procuration.php
@app/Policies/ViabilityRequestPolicy.php
@bootstrap/app.php
@routes/gestao.php
@app/Models/User.php
</context>

<skill>
ATIVAR `inertia-react-development` para a tela do console.
</skill>

<tasks>

<task type="auto">
  <name>Task 1: AssistedAttendance (migration+model+factory) + CurrentRepresentation reusável por atendimento</name>
  <read_first>app/Support/Representation/CurrentRepresentation.php, app/Models/Procuration.php, app/Models/User.php</read_first>
  <files>database/migrations/*_create_assisted_attendances_table.php, app/Models/AssistedAttendance.php, database/factories/AssistedAttendanceFactory.php, app/Support/Representation/CurrentRepresentation.php, tests/Feature/Solicitacao/AtendimentoPresencialTest.php</files>
  <action>
    TDD. Migration `assisted_attendances`: id; attendant_user_id (FK users); citizen_user_id (FK users); started_at (timestamp); expires_at (timestamp); ended_at (timestamp nullable); timestamps. Model `AssistedAttendance` (#[Fillable] dos campos; casts datetimes; belongsTo attendant/citizen; scope active() = ended_at null e expires_at > now(); método isActive()). Factory com states active()/expired()/ended().

    Reuso da representação: `CurrentRepresentation` da Fase 1 expõe grantor() a partir de uma Procuration. Para o atendimento, o "representado" é o cidadão atendido. Estender CurrentRepresentation de forma MÍNIMA e compatível para também aceitar um atendimento (ex.: um setter setAttendance(AssistedAttendance) e grantor() passa a retornar o citizen quando há atendimento ativo, mantendo o comportamento existente da procuração intacto) — OU um método irmão que a policy já consome via grantor(). CRÍTICO: NÃO quebrar o comportamento atual de procuração (os testes da Fase 1/3 devem continuar verdes). Documentar a decisão no SUMMARY.

    Os asserts entram em AtendimentoPresencialTest (Task 3).
  </action>
  <verify>php artisan test --compact --filter="LinkAttorneyTest|RevokeAttorneyTest|MyCompaniesTest"</verify>
  <acceptance_criteria>
    - grep "expires_at" na migration e model AssistedAttendance.
    - grep "scope\|isActive" no model.
    - Os testes EXISTENTES de representação/CompanyPolicy continuam verdes (sem regressão).
  </acceptance_criteria>
  <done>Modelo leve de atendimento + reuso compatível do CurrentRepresentation (grantor = cidadão atendido) sem regressão da procuração.</done>
</task>

<task type="auto">
  <name>Task 2: middleware ResolveAssistedAttendance + registro + permissão</name>
  <read_first>app/Http/Middleware/ResolveRepresentation.php, bootstrap/app.php, routes/gestao.php</read_first>
  <files>app/Http/Middleware/ResolveAssistedAttendance.php, bootstrap/app.php, routes/gestao.php, tests/Feature/Solicitacao/AtendimentoPresencialTest.php</files>
  <action>
    TDD. `ResolveAssistedAttendance` ESPELHANDO ResolveRepresentation: lê o atendimento ativo da sessão (ex.: `attending_attendance_id`), revalida (pertence ao atendente, ativo, não expirado); se válido, `Context::add('acting_for_user_id', citizen_id)` e `CurrentRepresentation::setAttendance(...)` (ou equivalente) — o MESMO estado que a procuração usa, alimentando a auditoria "em nome de" e a policy. Se expirado/ausente, limpa o estado (exige reabertura — CA-03). Registrar o middleware no grupo de rotas do atendimento (ou como alias) em bootstrap/app.php/routes — seguir o padrão de como ResolveRepresentation é aplicado.

    Permissão atendimento-presencial já existe (08-02). As rotas do atendimento ficam atrás dela.
  </action>
  <verify>php artisan route:list >/dev/null && vendor/bin/pint --dirty --format agent</verify>
  <acceptance_criteria>
    - grep "CurrentRepresentation" app/Http/Middleware/ResolveAssistedAttendance.php
    - grep "acting_for_user_id" no middleware (mesmo Context da Fase 1).
    - Middleware registrado/aplicado às rotas do atendimento.
  </acceptance_criteria>
  <done>Middleware do atendimento popula o MESMO Context/CurrentRepresentation da representação; expiração exige reabertura.</done>
</task>

<task type="auto">
  <name>Task 3: controller do atendimento (iniciar/encerrar) + ação em nome de + escopo + tela + auditoria</name>
  <read_first>app/Http/Controllers/Portal/RepresentationController.php, app/Policies/ViabilityRequestPolicy.php, routes/gestao.php, app/Support/Settings.php</read_first>
  <files>app/Http/Controllers/Gestao/AssistedAttendanceController.php, routes/gestao.php, resources/js/pages/gestao/atendimento/index.tsx, tests/Feature/Solicitacao/AtendimentoPresencialTest.php</files>
  <action>
    TDD estrito. `Gestao/AssistedAttendanceController`: start (POST — informa CPF do cidadão; cria AssistedAttendance com expires_at = now()+`solicitacao.atendimento.expiracao_minutos`; grava na sessão; audita início), end (DELETE — encerra). Rotas sob `permission:atendimento-presencial` + o middleware ResolveAssistedAttendance, name atendimento.*. Banner permanente "Atendendo: [nome] — CPF [•••]" via shared prop (espelhar o actingFor do Inertia da Fase 1).

    Tela `gestao/atendimento/index.tsx` (console): iniciar atendimento (CPF), banner do atendimento ativo, e atalhos para as ações permitidas em nome do cidadão (abrir solicitação direta/renovação — reusa o wizard; consultar; encerrar). Item de navegação por permissão.

    `AtendimentoPresencialTest` (SQLite, actingAs atendente gestao com atendimento-presencial):
    - test_acao_em_nome_do_cidadao (CA-01/RN-001): com atendimento ativo, protocolar uma solicitação registra requester = cidadão e created_by/ator = atendente; auditoria com acting_for (os dois CPFs).
    - test_escopo_limitado (CA-02/RN-002): o atendente NÃO consegue ação de análise/decisão (sem permissão) → 403 auditado.
    - test_expiracao_exige_reabertura (CA-03): atendimento expirado → próxima ação não opera em nome de (estado limpo); reabrir exige novo start.
    - test_origem_balcao_para_relatorios (RN-005): a solicitação criada via atendimento é distinguível (dimensão balcão/origem — definir como marcar: origin portal + flag de atendimento OU registro do atendimento vinculado; documentar).
    - test_exige_permissao (CA-04): sem atendimento-presencial → 403 auditado.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=AtendimentoPresencialTest && npx tsc --noEmit 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - grep "permission:atendimento-presencial" routes/gestao.php
    - grep "atendimento.expiracao_minutos\|expires_at" no controller (expiração parametrizada).
    - AtendimentoPresencialTest verde (em nome de, escopo, expiração, origem balcão, permissão); typecheck verde.
  </acceptance_criteria>
  <done>Atendente opera em nome do cidadão (dois CPFs auditados), escopo limitado por permissão, vínculo com expiração que exige reabertura, origem balcão para relatórios.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=AtendimentoPresencialTest` verde.
- `php artisan test --compact` — suíte completa CONTINUA verde (representação/CompanyPolicy sem regressão).
- `npx tsc --noEmit` e `npm run build` verdes.
</verification>

<success_criteria>
- Atendimento presencial reusa o mecanismo de representação da Fase 1 (mesmo Context/CurrentRepresentation/policy/auditoria), com modelo leve + middleware análogo.
- Ações registram ator + beneficiário; escopo limitado por permissão; vínculo expira e exige reabertura; origem balcão para relatórios. Sem regressão da procuração.
</success_criteria>

<output>
Criar `.planning/phases/08-solicitacao-de-viabilidade/08-15-SUMMARY.md`: schema do AssistedAttendance, a decisão de como o CurrentRepresentation foi estendido (compatível com procuração), o middleware e seu registro, a marcação de origem balcão e a pendência SEDUR do procedimento de ciência presencial (RN-004). Insumo do smoke (08-16).
</output>

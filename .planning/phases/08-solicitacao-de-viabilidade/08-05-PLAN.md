---
phase: 08-solicitacao-de-viabilidade
plan: 05
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - app/Policies/ViabilityRequestPolicy.php
  - app/Http/Controllers/Portal/SolicitacaoController.php
  - app/Http/Requests/Portal/StoreSolicitacaoRequest.php
  - app/Services/Solicitacao/DuplicateRequestDetector.php
  - routes/portal.php
  - tests/Feature/Solicitacao/CriarSolicitacaoTest.php
  - tests/Feature/Solicitacao/ViabilityRequestPolicyTest.php
autonomous: true
requirements: [HU-061]

must_haves:
  truths:
    - "O cidadão cria um rascunho de solicitação (origin portal) escolhendo tipo de serviço e empresa; o requerente é o usuário EFETIVO (representado quando em representação) e o created_by é o ator real (HU-061 CA-01)"
    - "A autorização é por ViabilityRequestPolicy espelhando CompanyPolicy + CurrentRepresentation (Fase 1): só o dono/representado opera a própria solicitação; sem vínculo → 403 auditado (CA-04)"
    - "Detecta duplicidade/reincidência (HU-061 RN-007): mesmo CNPJ com processo recente/ativo gera ALERTA ao requerente (não bloqueia), com link ao processo anterior"
    - "Criar exige um tipo de serviço ativo (08-03) e uma empresa do requerente (Fase 3); rascunho nasce em status rascunho via factory/StateMachine inicial"
    - "Tudo auditado (RN-002), inclusive o acting_for quando em representação"
  artifacts:
    - path: "app/Policies/ViabilityRequestPolicy.php"
      provides: "Autorização da solicitação integrada à representação (effectiveUser)"
      contains: "effectiveUser"
    - path: "app/Http/Controllers/Portal/SolicitacaoController.php"
      provides: "store do rascunho (requester=efetivo, created_by=ator) + index Minhas solicitações"
      contains: "class SolicitacaoController"
    - path: "app/Services/Solicitacao/DuplicateRequestDetector.php"
      provides: "Detecção de duplicidade/reincidência por CNPJ (alerta, não bloqueio)"
      contains: "class DuplicateRequestDetector"
  key_links:
    - from: "app/Policies/ViabilityRequestPolicy.php"
      to: "App\\Support\\Representation\\CurrentRepresentation"
      via: "effectiveUser() = grantor() ?? user (espelha CompanyPolicy [01-07]/[03-04])"
      pattern: "CurrentRepresentation"
    - from: "app/Http/Controllers/Portal/SolicitacaoController.php"
      to: "App\\Services\\Solicitacao\\DuplicateRequestDetector"
      via: "alerta de reincidência por CNPJ na criação (RN-007), sem bloquear"
      pattern: "DuplicateRequestDetector"
---

<objective>
Início do processo formal: o cidadão **cria o rascunho** da solicitação (HU-061) escolhendo origem (portal direto), tipo de serviço (08-03) e empresa (Fase 3). A autorização nasce integrada à representação da Fase 1 (`ViabilityRequestPolicy` espelha `CompanyPolicy`): o requerente é o usuário efetivo (representado quando "em nome de"), o `created_by` é o ator real. Detecta duplicidade/reincidência por CNPJ (RN-007) como ALERTA, nunca bloqueio (direito de petição).

Purpose: critério 1 do ROADMAP (criar solicitação com origem e tipo de serviço). Reusa CurrentRepresentation/effectiveUser ([01-07]/[03-04]) e Company (Fase 3) — sem recriar. Anti-fachada: criar grava de verdade um rascunho real (status rascunho) e a duplicidade aponta o processo anterior (link real), sem travar o novo.
Output: ViabilityRequestPolicy + SolicitacaoController (store + index Minhas solicitações) + StoreSolicitacaoRequest + DuplicateRequestDetector + rotas portal.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-solicitacao-de-viabilidade/08-CONTEXT.md
@.planning/phases/08-solicitacao-de-viabilidade/08-01-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-02-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-03-SUMMARY.md
@docs/SILE_HUs_Completas_MD/EP08-Solicitacao-de-Viabilidade/HU-061-Criar-solicitacao-de-viabilidade.md

# PADRÕES A ESPELHAR (policy + representação + store transacional + listagem escopada) — ler antes de codar:
@app/Policies/CompanyPolicy.php
@app/Http/Controllers/Portal/CompanyController.php
@app/Http/Requests/Portal/StoreCompanyRequest.php
@app/Support/Representation/CurrentRepresentation.php
@app/Models/Company.php
@routes/portal.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: ViabilityRequestPolicy (representação) + registro no fluxo de autorização</name>
  <read_first>app/Policies/CompanyPolicy.php, app/Support/Representation/CurrentRepresentation.php</read_first>
  <files>app/Policies/ViabilityRequestPolicy.php, tests/Feature/Solicitacao/ViabilityRequestPolicyTest.php</files>
  <action>
    TDD estrito. `ViabilityRequestPolicy` ESPELHANDO CompanyPolicy: `effectiveUser(User $user): User` = `app(CurrentRepresentation::class)->grantor() ?? $user`. Abilities:
    - view(User, ViabilityRequest): requester_user_id === effectiveUser->id (o dono/representado vê a própria).
    - update(User, ViabilityRequest): igual a view E status === rascunho (só rascunho é editável; protocolada/cancelada não).
    - protocol(User, ViabilityRequest): dono E status rascunho.
    - cancel(User, ViabilityRequest): dono E status em {rascunho, protocolada} (regra fina dos estados canceláveis fica no 08-12 via parâmetro; a policy só garante propriedade + não-decidido).
    Auto-discovery de policies do Laravel 12 resolve ViabilityRequest→ViabilityRequestPolicy; se o projeto registra policies explicitamente, seguir a convenção existente.

    `ViabilityRequestPolicyTest` (SQLite): dono vê/edita o próprio rascunho; terceiro NÃO (false); representado: com CurrentRepresentation setado, o procurador opera as do representado (espelhar o teste da CompanyPolicy); update falso quando status != rascunho.
  </action>
  <verify>php artisan test --compact --filter=ViabilityRequestPolicyTest</verify>
  <acceptance_criteria>
    - grep "effectiveUser" app/Policies/ViabilityRequestPolicy.php
    - grep "CurrentRepresentation" app/Policies/ViabilityRequestPolicy.php
    - ViabilityRequestPolicyTest verde (dono/terceiro/representado; update só em rascunho).
  </acceptance_criteria>
  <done>Policy integrada à representação; só o dono/representado opera; update só em rascunho.</done>
</task>

<task type="auto">
  <name>Task 2: DuplicateRequestDetector (RN-007) — alerta por CNPJ, sem bloquear</name>
  <read_first>app/Models/ViabilityRequest.php, app/Models/Company.php</read_first>
  <files>app/Services/Solicitacao/DuplicateRequestDetector.php, tests/Feature/Solicitacao/CriarSolicitacaoTest.php</files>
  <action>
    TDD. `DuplicateRequestDetector::detect(Company $company, ?string $excludeRequestId = null): ?array` — procura solicitações recentes/ativas da MESMA empresa (por company_id/CNPJ) numa janela de dias definida como CONSTANTE DE CONFIG (`config/sile.php`, ex.: `solicitacao.duplicidade.janela_dias` com default inline) — NÃO adicionar Parameter de catálogo aqui (este plano não toca ParameterSeeder/seeder-tests; isso quebraria as contagens travadas em 08-02/08-12). A "definição de duplicidade/reincidência HU-061 RN-007" é pendência SEDUR: default honesto + ressalva documentada. Retorna null (sem duplicidade) ou um array com {request_id, protocol_number, status, created_at} do processo anterior mais recente, para alerta + link. Por CNPJ funciona; por inscrição imobiliária degrada (lote bloqueado) — só por CNPJ nesta fase, documentado.

    Os testes deste detector entram no CriarSolicitacaoTest (Task 3) que exercita o alerta no fluxo de criação.
  </action>
  <verify>php artisan test --compact --filter=CriarSolicitacaoTest</verify>
  <acceptance_criteria>
    - grep "class DuplicateRequestDetector" app/Services/Solicitacao/DuplicateRequestDetector.php
    - O detector retorna o processo anterior (não lança/baloqueia) — alerta, nunca bloqueio.
  </acceptance_criteria>
  <done>Detector de reincidência por CNPJ retorna o processo anterior para alerta+link; nunca bloqueia (RN-007 honesto; janela parametrizada com ressalva SEDUR).</done>
</task>

<task type="auto">
  <name>Task 3: SolicitacaoController store (rascunho) + index (Minhas solicitações) + rotas</name>
  <read_first>app/Http/Controllers/Portal/CompanyController.php, app/Http/Requests/Portal/StoreCompanyRequest.php, routes/portal.php</read_first>
  <files>app/Http/Controllers/Portal/SolicitacaoController.php, app/Http/Requests/Portal/StoreSolicitacaoRequest.php, routes/portal.php, tests/Feature/Solicitacao/CriarSolicitacaoTest.php</files>
  <action>
    TDD estrito. `Portal/SolicitacaoController`:
    - `store(StoreSolicitacaoRequest)`: cria a ViabilityRequest em rascunho dentro de DB::transaction, com `requester_user_id = effectiveUser`, `created_by_user_id = $request->user()`, `origin = portal`, `service_type_id` (validado: existe e active), `company_id` (validado: empresa COM VÍNCULO do efetivo — reusar a lógica de escopo da Fase 3). Antes de criar, chamar DuplicateRequestDetector e devolver o alerta (flash/prop) sem bloquear. Auditoria automática (HasAuditoria created) enriquecida com acting_for ([01-07]).
    - `index(Request)`: "Minhas solicitações" — lista server-driven SOMENTE das solicitações do requester efetivo (espelhar CompanyController@index: busca por protocol_number/empresa, ordenação whitelist, per_page parametrizado, through() com shape para a tabela: id, protocol_number, status {value,label,publicLabel}, empresa, created_at). (A tela é o 08-13; aqui só o backend + asserções de props.)
    - `StoreSolicitacaoRequest`: valida service_type_id (Rule::exists active), company_id (existe E vínculo do efetivo via after() ou Rule), origin default portal. Mensagens pt-BR.

    Rotas em `routes/portal.php` no grupo autenticado `auth:web` + verified + lgpd.accepted + ResolveRepresentation (onde vivem empresas/procurações): rotas LITERAIS primeiro — `GET solicitacoes` (index), `POST solicitacoes` (store). (As rotas com {solicitacao} entram em 08-06/08-11.) Toggle features.solicitacao_viabilidade: o store/index degrada comunicado quando desligado (Settings::enabled('solicitacao_viabilidade')) — espelhar o padrão de features.procuracoes/cnpj_lookup.

    `CriarSolicitacaoTest` (SQLite, RolesAndPermissionsSeeder + cidadão com empresa):
    - test_cidadao_cria_rascunho (CA-01): POST cria status rascunho, requester=efetivo, created_by=ator, origin portal, service_type/company válidos; redirect com status.
    - test_requer_tipo_de_servico_ativo: service_type inexistente/inativo → erro de validação.
    - test_requer_empresa_vinculada_ao_efetivo: empresa sem vínculo → erro (não cria para empresa de terceiro).
    - test_em_nome_de_cria_para_o_representado (representação): created_by=procurador, requester=representado; auditoria com acting_for.
    - test_alerta_de_duplicidade_nao_bloqueia (RN-007): empresa com solicitação recente → cria mesmo assim + alerta com link ao anterior.
    - test_auditoria_da_criacao (CA-02/RN-002): Activity da criação registrada.
    - test_toggle_desligado_degrada_comunicado: features.solicitacao_viabilidade=0 → bloqueio comunicado (não cria), nunca silencioso.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=CriarSolicitacaoTest</verify>
  <acceptance_criteria>
    - grep "class SolicitacaoController" e o store com requester=efetivo/created_by=ator.
    - grep "GET\|solicitacoes" routes/portal.php (rotas literais antes de binding).
    - grep "DuplicateRequestDetector" no controller.
    - CriarSolicitacaoTest verde (criação, validações, representação, duplicidade-alerta, auditoria, toggle).
  </acceptance_criteria>
  <done>Rascunho criado com requester/created_by corretos, escopo por empresa do efetivo, alerta de duplicidade não-bloqueante, auditoria e toggle comunicado; Minhas solicitações (backend) escopado ao dono.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="ViabilityRequestPolicyTest|CriarSolicitacaoTest"` verde.
- `php artisan test --compact` — suíte completa CONTINUA verde.
</verification>

<success_criteria>
- Criar rascunho (HU-061) com origem/tipo de serviço, requester efetivo e created_by ator; policy integrada à representação; escopo por empresa do dono.
- Duplicidade/reincidência por CNPJ alerta sem bloquear (RN-007); auditoria e toggle comunicados; sem fachada.
</success_criteria>

<output>
Criar `.planning/phases/08-solicitacao-de-viabilidade/08-05-SUMMARY.md`: assinaturas da policy e do store; shape do índice "Minhas solicitações"; contrato do DuplicateRequestDetector; nomes de rota (portal.solicitacoes.*). Insumo direto de 08-06/08-07 (instrução), 08-10 (protocolo), 08-13 (UI), 08-15 (presencial).
</output>

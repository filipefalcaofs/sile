---
phase: 08-solicitacao-de-viabilidade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/*_create_viability_service_types_table.php
  - database/migrations/*_create_document_requirements_table.php
  - database/migrations/*_create_cnae_document_requirement_table.php
  - database/migrations/*_create_protocol_sequences_table.php
  - database/migrations/*_create_viability_requests_table.php
  - database/migrations/*_create_viability_request_cnaes_table.php
  - database/migrations/*_create_viability_request_documents_table.php
  - database/migrations/*_create_viability_request_transitions_table.php
  - app/Enums/ViabilityRequestStatus.php
  - app/Enums/ViabilityRequestOrigin.php
  - app/Models/ViabilityRequest.php
  - app/Models/ViabilityServiceType.php
  - app/Models/DocumentRequirement.php
  - app/Models/ViabilityRequestDocument.php
  - app/Models/ViabilityRequestTransition.php
  - app/Models/ProtocolSequence.php
  - database/factories/ViabilityRequestFactory.php
  - database/factories/ViabilityServiceTypeFactory.php
  - database/factories/DocumentRequirementFactory.php
  - database/factories/ViabilityRequestDocumentFactory.php
  - database/factories/ViabilityRequestTransitionFactory.php
  - database/factories/ProtocolSequenceFactory.php
  - app/Services/Solicitacao/ProtocolNumberGenerator.php
  - app/Services/Solicitacao/ViabilityRequestStateMachine.php
  - app/Services/Solicitacao/InvalidStatusTransitionException.php
  - tests/Feature/Solicitacao/SolicitacaoSchemaTest.php
  - tests/Feature/Solicitacao/ProtocolNumberGeneratorTest.php
  - tests/Feature/Solicitacao/ViabilityRequestStateMachineTest.php
  - tests/Feature/Solicitacao/ProtocolNumberGeneratorPostgisTest.php
autonomous: true
requirements: [HU-061, HU-067, HU-068, HU-070]

must_haves:
  truths:
    - "Existe o aggregate viability_requests (cabeçalho + imóvel embutido 1:1) com os satélites cnaes/documents/transitions e os cadastros viability_service_types, document_requirements (+ pivot cnae) e protocol_sequences"
    - "O polígono é portável: property_polygon_geojson (jsonb) é a FONTE de verdade (a suíte roda em SQLite); property_polygon (geometry 4326) é DERIVADA nullable só no pgsql, com GiST condicional — espelha geo_features/GeoJsonLayerImporter (Fase 4)"
    - "ViabilityRequestStatus tem os estados ativos (rascunho/protocolada/cancelada) e os GANCHOS futuros (aguardando_bap/em_analise/deferida/indeferida/em_pendencia) como casos do enum, com label()/publicLabel() — mas a máquina NÃO os transiciona ainda"
    - "ViabilityRequestStateMachine só permite as transições válidas (rascunho→protocolada, rascunho→cancelada, protocolada→cancelada); transição inválida lança exceção (CA-03) e toda transição grava viability_request_transitions + AuditService::log('solicitacoes','transicao')"
    - "ProtocolNumberGenerator gera {prefixo}-{AAAA}-{NNNNNN} de forma concorrência-segura (protocol_sequences->lockForUpdate() na transação); unicidade provada em SQLite e concorrência real em @group postgis"
    - "As factories cobrem os estados draft/protocoled/cancelled/contingency — base de todos os testes da fase"
  artifacts:
    - path: "database/migrations"
      provides: "7 tabelas driver-aware (geometry derivada + GiST só pgsql)"
      contains: "viability_requests"
    - path: "app/Enums/ViabilityRequestStatus.php"
      provides: "Estados ativos + ganchos futuros com label()/publicLabel()"
      contains: "enum ViabilityRequestStatus"
    - path: "app/Services/Solicitacao/ProtocolNumberGenerator.php"
      provides: "Gerador de protocolo concorrência-seguro parametrizável"
      contains: "lockForUpdate"
    - path: "app/Services/Solicitacao/ViabilityRequestStateMachine.php"
      provides: "Mapa explícito de transições + gravação de transição + auditoria"
      contains: "class ViabilityRequestStateMachine"
  key_links:
    - from: "app/Services/Solicitacao/ViabilityRequestStateMachine.php"
      to: "App\\Support\\Audit\\AuditService"
      via: "log('solicitacoes','transicao', from/to/actor) em toda transição (RN-002)"
      pattern: "logName: 'solicitacoes'"
    - from: "app/Services/Solicitacao/ProtocolNumberGenerator.php"
      to: "App\\Support\\Settings"
      via: "lê solicitacao.protocolo.prefixo/.padding com default inline VIA/6 (não depende do banco)"
      pattern: "solicitacao.protocolo"
    - from: "database/migrations/*_create_viability_requests_table.php"
      to: "App\\Services\\Geo\\GeoJsonLayerImporter"
      via: "geometry derivada driver-aware idêntica ao padrão da Fase 4 (ST_* só pgsql, GiST condicional)"
      pattern: "DB::getDriverName() === 'pgsql'"
---

<objective>
Fundação da Fase 8 — o **modelo de dados e o domínio** que bloqueiam todo o resto: todas as migrations (driver-aware, espelhando a Fase 4 para a geometria derivada), os enums de estado/origem (com os ganchos das fases seguintes), os models + factories, o `ProtocolNumberGenerator` concorrência-seguro e a `ViabilityRequestStateMachine` (mapa explícito + transição auditada).

Purpose: a solicitação é o primeiro processo formal do sistema; tudo (criação, instrução, protocolo, consulta, canais de operador) depende deste esqueleto. Anti-fachada desde a base: a geometria é DERIVADA do GeoJSON (fonte portável) só no Postgres real (a suíte roda em SQLite, como na Fase 4); estados das Fases 9/10/11 entram como ganchos no enum mas a máquina NÃO os transiciona; o número de protocolo é concorrência-seguro de verdade (lockForUpdate + unique), provado em @group postgis.
Output: 7 tabelas, 2 enums, 6 models, 6 factories, `ProtocolNumberGenerator`, `ViabilityRequestStateMachine` (+ exceção), cobertos por feature tests (SQLite + 1 @group postgis de concorrência).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-solicitacao-de-viabilidade/08-CONTEXT.md
@docs/superpowers/specs/2026-06-14-solicitacao-viabilidade-design.md

# PADRÕES A ESPELHAR (driver-aware geometry; models/factories; auditoria; concorrência) — ler antes de codar:
@database/migrations/2026_06_13_214420_create_geo_features_table.php
@app/Services/Geo/GeoJsonLayerImporter.php
@database/migrations/2026_06_12_035745_create_companies_table.php
@app/Models/Company.php
@database/factories/CompanyFactory.php
@app/Models/Cnae.php
@app/Support/Audit/AuditService.php
@app/Enums/ResultadoViabilidade.php
@app/Enums/CompanySource.php
@tests/Feature/Geo/GeoSchemaTest.php
@tests/PostgisTestCase.php
@tests/Feature/Geo/PostgisSpatialRepositoryTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: migrations driver-aware de todas as tabelas (aggregate + satélites + cadastros)</name>
  <read_first>database/migrations/2026_06_13_214420_create_geo_features_table.php, database/migrations/2026_06_12_035745_create_companies_table.php, app/Services/Geo/GeoJsonLayerImporter.php</read_first>
  <files>database/migrations/*_create_viability_service_types_table.php, database/migrations/*_create_document_requirements_table.php, database/migrations/*_create_cnae_document_requirement_table.php, database/migrations/*_create_protocol_sequences_table.php, database/migrations/*_create_viability_requests_table.php, database/migrations/*_create_viability_request_cnaes_table.php, database/migrations/*_create_viability_request_documents_table.php, database/migrations/*_create_viability_request_transitions_table.php, tests/Feature/Solicitacao/SolicitacaoSchemaTest.php</files>
  <action>
    TDD: escrever `SolicitacaoSchemaTest` (Schema::hasTable/hasColumns dos campos-chave, espelhando GeoSchemaTest) ANTES, confirmar RED, depois criar as migrations com `php artisan make:migration --no-interaction`. Criar os cadastros ANTES das tabelas que os referenciam por FK (ordem de timestamp).

    Tabelas (nomes de coluna exatos à discrição, mas cobrir os campos do CONTEXT/spec):

    1. `viability_service_types` (HU-061 RN-005): id; code (string unique); name; flow_hint (string nullable); active (bool default true); timestamps.
    2. `document_requirements` (modelo "Requisito" SIGVISA — HU-067): id; code (string unique); name; description (text nullable); required (bool default true); active (bool default true); validation_instructions (text nullable — gancho IA EP14); timestamps.
    3. `cnae_document_requirement` (pivot N:N): id; cnae_id (FK cnaes, cascadeOnDelete); document_requirement_id (FK, cascadeOnDelete); timestamps; unique(cnae_id, document_requirement_id).
    4. `protocol_sequences`: id; year (integer, unique); last_number (unsignedBigInteger default 0); timestamps.
    5. `viability_requests` (aggregate root):
       - protocol_number (string nullable, unique), status (string, default 'rascunho', index), origin (string, default 'portal');
       - service_type_id (FK viability_service_types, nullable, nullOnDelete), company_id (FK companies, nullable, nullOnDelete);
       - requester_user_id (FK users — beneficiário), created_by_user_id (FK users — ator real "em nome de");
       - used_area_m2 (decimal 10,2 nullable), property_registration (string nullable — inscrição imobiliária);
       - endereço: address_street, address_number, address_complement (string nullable — TEXTO LIVRE, HU-139 adiado), address_neighborhood, address_zip (nullable), address_reference (ponto de referência, nullable);
       - property_polygon_geojson (jsonb nullable — **FONTE de verdade**);
       - indicadores: is_virtual_office, is_public_area, has_independent_access (bool default false);
       - simulação: simulation_snapshot (jsonb nullable), simulation_rules_versions (jsonb nullable), simulation_resultado (string nullable), simulated_at (timestamp nullable);
       - applicant_proceeded_despite (bool default false), contingency_reason (text nullable), external_reference (string nullable — BAP/Regin futuro);
       - protocoled_at (timestamp nullable), cancelled_at (timestamp nullable), cancelled_reason (text nullable), cancelled_by_user_id (FK users nullable);
       - timestamps.
       - **Geometria derivada driver-aware (espelhar geo_features + GeoJsonLayerImporter):** APÓS o Schema::create, somente quando `DB::getDriverName() === 'pgsql'`, adicionar a coluna `property_polygon geometry(Polygon,4326)` NULLABLE e o índice `CREATE INDEX ... USING GIST (property_polygon)` (Pitfall 1: GiST/geometry quebram SQLite). Em SQLite a coluna NÃO existe — o GeoJSON jsonb é a fonte; a geometria é gravada (ST_*) só no pgsql no plano 08-06.
    6. `viability_request_cnaes` (pivot, espelha company_cnae): id; viability_request_id (FK, cascadeOnDelete); cnae_id (FK, restrictOnDelete); is_primary (bool default false); timestamps; unique(viability_request_id, cnae_id).
    7. `viability_request_documents`: id; viability_request_id (FK, cascadeOnDelete); requirement_id (FK document_requirements, nullable, nullOnDelete); disk (string — da época); path; original_name; mime_type; size (unsignedInteger); sha256 (string 64); uploaded_by_user_id (FK users); timestamps.
    8. `viability_request_transitions` (FONTE da timeline HU-069): id; viability_request_id (FK, cascadeOnDelete); from_status (string nullable); to_status (string); reason (text nullable); public_label (string nullable); actor_user_id (FK users nullable); timestamps (a timeline ordena por created_at).

    SolicitacaoSchemaTest: assertSame(true, Schema::hasTable(...)) para as 8 tabelas e hasColumns dos campos críticos (protocol_number/status/origin/property_polygon_geojson/simulation_snapshot em viability_requests; is_primary em viability_request_cnaes; sha256 em documents; to_status em transitions; year/last_number em protocol_sequences). NÃO asserir a coluna geometry property_polygon (só existe em pgsql) — isso fica no @group postgis (Task 3 / 08-06).
  </action>
  <verify>php artisan test --compact --filter=SolicitacaoSchemaTest</verify>
  <acceptance_criteria>
    - `php artisan migrate:fresh` roda limpo em SQLite (sem geometry/GiST quebrando).
    - grep "DB::getDriverName() === 'pgsql'" na migration de viability_requests (geometria + GiST condicionais).
    - grep "property_polygon_geojson" na migration de viability_requests (jsonb fonte).
    - grep "unique" nas pivots viability_request_cnaes e cnae_document_requirement.
    - SolicitacaoSchemaTest verde (8 tabelas + colunas-chave).
  </acceptance_criteria>
  <done>8 tabelas criadas; geometria derivada + GiST só pgsql (fonte = GeoJSON jsonb); schema test verde em SQLite.</done>
</task>

<task type="auto">
  <name>Task 2: enums (Status com ganchos + Origin) + models + casts + relationships + factories</name>
  <read_first>app/Enums/ResultadoViabilidade.php, app/Models/Company.php, database/factories/CompanyFactory.php, app/Concerns/HasAuditoria.php</read_first>
  <files>app/Enums/ViabilityRequestStatus.php, app/Enums/ViabilityRequestOrigin.php, app/Models/ViabilityRequest.php, app/Models/ViabilityServiceType.php, app/Models/DocumentRequirement.php, app/Models/ViabilityRequestDocument.php, app/Models/ViabilityRequestTransition.php, app/Models/ProtocolSequence.php, database/factories/ViabilityRequestFactory.php, database/factories/ViabilityServiceTypeFactory.php, database/factories/DocumentRequirementFactory.php, database/factories/ViabilityRequestDocumentFactory.php, database/factories/ViabilityRequestTransitionFactory.php, database/factories/ProtocolSequenceFactory.php</files>
  <action>
    TDD: cobrir enums e factories com asserts dentro de SolicitacaoSchemaTest (ou um EnumTest dedicado) — labels e estados — RED→GREEN.

    1. `ViabilityRequestStatus` (string enum, label() + publicLabel()):
       - ATIVOS: Rascunho='rascunho', Protocolada='protocolada', Cancelada='cancelada'.
       - GANCHOS (casos no enum, NÃO transicionados nesta fase): AguardandoBap='aguardando_bap', EmAnalise='em_analise', Deferida='deferida', Indeferida='indeferida', EmPendencia='em_pendencia'.
       - label() técnico (ex.: 'Rascunho', 'Protocolada', 'Cancelada', 'Aguardando Junta (BAP)', 'Em análise', 'Deferida', 'Indeferida', 'Em pendência'); publicLabel() em linguagem simples ao cidadão (HU-069 RN-004 — ex.: Protocolada → 'Recebida — em processamento'; AguardandoBap → 'Aguardando confirmação da Junta Comercial — nada a fazer por enquanto').
    2. `ViabilityRequestOrigin` (string enum, label()): Portal='portal', Contingencia='contingencia', Regin='regin' (Regin é GANCHO/bloqueado Fase 13 — existe no enum, mas só portal/contingencia são usados). label() pt-BR.
    3. Models:
       - `ViabilityRequest` (use HasAuditoria como Company — RN-002 created/updated; #[Fillable] com os campos administráveis pelo fluxo, NUNCA protocol_number/status/property_polygon — esses mudam por serviço/máquina). casts(): status→ViabilityRequestStatus, origin→ViabilityRequestOrigin, property_polygon_geojson→array, simulation_snapshot→array, simulation_rules_versions→array, used_area_m2→decimal:2, is_*→bool, simulated_at/protocoled_at/cancelled_at→datetime. Relations: cnaes() belongsToMany(Cnae,'viability_request_cnaes')->withPivot('is_primary')->withTimestamps(); primaryCnae() (wherePivot is_primary true); documents() hasMany; transitions() hasMany (latest p/ timeline); serviceType() belongsTo; company() belongsTo; requester()/createdBy()/cancelledBy() belongsTo(User). Método de domínio `markSimulationStale()` (zera simulation_resultado/simulated_at/snapshot — consumido por 08-06/08-07 quando muda área/CNAE/imóvel, HU-063 RN-005) — escrita única aqui evita conflito entre planos paralelos.
       - `ViabilityServiceType` (#[Fillable] code/name/flow_hint/active; cast active→bool; scope active()).
       - `DocumentRequirement` (#[Fillable] code/name/description/required/active/validation_instructions; casts bool; belongsToMany Cnae 'cnae_document_requirement').
       - `ViabilityRequestDocument` (#[Fillable] requirement_id/disk/path/original_name/mime_type/size/sha256/uploaded_by_user_id; belongsTo request/requirement/uploader).
       - `ViabilityRequestTransition` (#[Fillable] from_status/to_status/reason/public_label/actor_user_id; casts from_status/to_status→ViabilityRequestStatus; belongsTo request/actor).
       - `ProtocolSequence` (#[Fillable] year/last_number).
    4. Factories (espelhar CompanyFactory):
       - `ViabilityRequestFactory` definition() = rascunho (status Rascunho, origin Portal, requester/created_by via User::factory, company via Company::factory, used_area_m2, address_*, property_polygon_geojson com um Polygon de 4 pontos em Salvador). States: draft() (default), protocoled() (status Protocolada, protocol_number 'VIA-'.year.'-000001', protocoled_at now), cancelled() (status Cancelada, cancelled_at/cancelled_reason), contingency() (origin Contingencia, contingency_reason). Helper withPrimaryCnae(?Cnae) (afterCreating attach is_primary true) e withCnaes(int $n).
       - demais factories simples (ViabilityServiceTypeFactory, DocumentRequirementFactory + state required()/optional(), ViabilityRequestDocumentFactory, ViabilityRequestTransitionFactory, ProtocolSequenceFactory).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=SolicitacaoSchemaTest</verify>
  <acceptance_criteria>
    - grep "enum ViabilityRequestStatus" app/Enums/ViabilityRequestStatus.php e os 8 casos (3 ativos + 5 ganchos) + publicLabel().
    - grep "case Regin" app/Enums/ViabilityRequestOrigin.php (gancho presente).
    - grep "use HasAuditoria" app/Models/ViabilityRequest.php
    - grep "markSimulationStale" app/Models/ViabilityRequest.php
    - ViabilityRequest::factory()->protocoled()->create() e ->draft()->withPrimaryCnae()->create() funcionam em teste.
  </acceptance_criteria>
  <done>Enums (ativos + ganchos) com labels; 6 models com casts/relations + markSimulationStale; 6 factories com states draft/protocoled/cancelled/contingency.</done>
</task>

<task type="auto">
  <name>Task 3: ProtocolNumberGenerator concorrência-seguro (unicidade SQLite + concorrência @group postgis)</name>
  <read_first>app/Support/Settings.php, app/Services/Solicitacao/ProtocolNumberGenerator.php, tests/PostgisTestCase.php</read_first>
  <files>app/Services/Solicitacao/ProtocolNumberGenerator.php, tests/Feature/Solicitacao/ProtocolNumberGeneratorTest.php, tests/Feature/Solicitacao/ProtocolNumberGeneratorPostgisTest.php</files>
  <action>
    TDD estrito. `ProtocolNumberGenerator::generate(?int $year = null): string`:
    - `$year ??= (int) now()->year;`
    - dentro de `DB::transaction(...)`: `ProtocolSequence::query()->where('year',$year)->lockForUpdate()->first()` (ou firstOrCreate + lock), incrementa last_number, persiste, e formata `{prefixo}-{AAAA}-{NNNNNN}`.
    - Parametrização HU-014 com **default inline** (não depende do banco/seeder do 08-02): `$prefix = (string) Settings::get('solicitacao.protocolo.prefixo', config('sile.solicitacao.protocolo.prefixo', 'VIA'));` e `$padding = (int) Settings::get('solicitacao.protocolo.padding', config('sile.solicitacao.protocolo.padding', 6));`. `str_pad((string)$number, $padding, '0', STR_PAD_LEFT)`.
    - O gerador NÃO grava em viability_requests — só sequencia. O caller (08-10) usa o número dentro da transação de protocolo; `unique(protocol_number)` é a defesa final.

    `ProtocolNumberGeneratorTest` (SQLite, RefreshDatabase):
    - test_gera_no_formato_parametrizado: generate(2026) === 'VIA-2026-000001'; segundo === '...000002'.
    - test_reinicia_sequencia_por_ano: generate(2026), generate(2027) → '...2027-000001'.
    - test_respeita_prefixo_e_padding_parametrizados: gravar Parameter solicitacao.protocolo.prefixo='TVL' e .padding='4' (ou via config), limpar cache; generate → 'TVL-2026-0001'. (Usa o seeder do 08-02 OU grava Parameter direto — não criar dependência de plano; preferir gravar Parameter diretamente no teste.)
    - test_numeros_sao_unicos_em_sequencia: gerar 50 e asserir count(unique)===50 (unicidade lógica em SQLite — lockForUpdate é no-op aqui).

    `ProtocolNumberGeneratorPostgisTest extends PostgisTestCase` (#[Group('postgis')] herdado): test_concorrencia_nao_duplica_sob_lock — simular duas sequências concorrentes (duas transações/`DB::transaction` sequenciais provando o lock + uma asserção de que last_number avançou sem buraco; espelhar o estilo dos testes postgis da Fase 4). Documentar que o lock real só é exercitado no Postgres (no-op em SQLite).
  </action>
  <verify>php artisan test --compact --filter=ProtocolNumberGeneratorTest && php artisan test --compact --group=postgis --filter=ProtocolNumberGeneratorPostgisTest</verify>
  <acceptance_criteria>
    - grep "lockForUpdate" app/Services/Solicitacao/ProtocolNumberGenerator.php
    - grep "solicitacao.protocolo.prefixo" app/Services/Solicitacao/ProtocolNumberGenerator.php
    - ProtocolNumberGeneratorTest verde (formato, ano, prefixo/padding, unicidade).
    - ProtocolNumberGeneratorPostgisTest verde com o container PostGIS de pé (ou skip honesto sem servidor; FALHA com POSTGIS_TESTS_REQUIRED=true).
  </acceptance_criteria>
  <done>Gerador concorrência-seguro e parametrizado (default inline VIA/6); unicidade em SQLite; concorrência real em @group postgis.</done>
</task>

<task type="auto">
  <name>Task 4: ViabilityRequestStateMachine (mapa explícito + transição auditada + exceção)</name>
  <read_first>app/Support/Audit/AuditService.php, app/Models/ViabilityRequest.php, app/Enums/ViabilityRequestStatus.php</read_first>
  <files>app/Services/Solicitacao/ViabilityRequestStateMachine.php, app/Services/Solicitacao/InvalidStatusTransitionException.php, tests/Feature/Solicitacao/ViabilityRequestStateMachineTest.php</files>
  <action>
    TDD estrito. `ViabilityRequestStateMachine`:
    - Mapa explícito SÓ das transições ativas: `rascunho => [protocolada, cancelada]`, `protocolada => [cancelada]`. NENHUM gancho (aguardando_bap/em_analise/...) entra no mapa — as Fases 9/10/11 só ADICIONAM entradas depois.
    - `canTransition(ViabilityRequestStatus $from, ViabilityRequestStatus $to): bool`.
    - `transition(ViabilityRequest $request, ViabilityRequestStatus $to, ?User $actor = null, ?string $reason = null, ?string $publicLabel = null): ViabilityRequestTransition` — valida (canTransition; senão lança `InvalidStatusTransitionException` — CA-03), atualiza `$request->status`, grava `viability_request_transitions` (from/to/reason/public_label/actor_user_id) e chama `AuditService::log('solicitacoes','transicao', "...{$from->value}→{$to->value}", properties: [...], subject: $request)`. NÃO abre transação própria (o caller — 08-10/08-12 — controla a transação); apenas faz as escritas.
    - Injeta AuditService no construtor (promoção de propriedade).

    `ViabilityRequestStateMachineTest` (SQLite):
    - test_transicao_valida_rascunho_para_protocolada: cria draft; transition(...,Protocolada); status atualizado; 1 viability_request_transitions; 1 Activity log_name 'solicitacoes' event 'transicao'.
    - test_transicao_valida_para_cancelada (de rascunho e de protocolada).
    - test_transicao_invalida_lanca_excecao: protocolada→rascunho (ou cancelada→protocolada) → expectException(InvalidStatusTransitionException); status inalterado; NENHUMA transition gravada (CA-03).
    - test_ganchos_nao_sao_transicionaveis: rascunho→em_analise → exceção (gancho não está no mapa).
    - test_transicao_registra_ator_e_label_publico: actor + publicLabel persistidos na transition.
  </action>
  <verify>php artisan test --compact --filter=ViabilityRequestStateMachineTest</verify>
  <acceptance_criteria>
    - grep "class ViabilityRequestStateMachine" e o mapa com 'rascunho'/'protocolada' apenas.
    - grep "logName: 'solicitacoes'" app/Services/Solicitacao/ViabilityRequestStateMachine.php
    - grep "InvalidStatusTransitionException" (lançada na transição inválida).
    - ViabilityRequestStateMachineTest verde (válidas, inválida, gancho bloqueado, auditoria).
  </acceptance_criteria>
  <done>Máquina de estados com mapa explícito (só ativos), transição inválida → exceção (CA-03), toda transição grava timeline + auditoria (RN-002).</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- Suíte SQLite (RefreshDatabase) migra limpo; geometria/GiST só pgsql (provado em @group postgis). NUNCA usar `migrate:fresh --env=testing` (sem .env.testing roda no Postgres de desenvolvimento).
- `php artisan test --compact --filter="SolicitacaoSchemaTest|ProtocolNumberGeneratorTest|ViabilityRequestStateMachineTest"` verde.
- `php artisan test --compact --group=postgis --filter=ProtocolNumberGeneratorPostgisTest` verde (container de pé) ou skip honesto.
- `php artisan test --compact` — suíte completa CONTINUA verde (só foram adicionadas tabelas/models/serviços novos, sem tocar os existentes).
</verification>

<success_criteria>
- 8 tabelas (aggregate + satélites + cadastros) com geometria derivada driver-aware (fonte = GeoJSON jsonb), espelhando a Fase 4.
- Enums com estados ativos + ganchos (sem transicioná-los) e labels técnico/público.
- ProtocolNumberGenerator concorrência-seguro e parametrizado; StateMachine com transição auditada e exceção em transição inválida.
- Factories com states draft/protocoled/cancelled/contingency — base de toda a fase. ZERO dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/08-solicitacao-de-viabilidade/08-01-SUMMARY.md`: nomes EXATOS das tabelas/colunas, dos enums (casos ativos + ganchos), dos models/relacionamentos (incl. markSimulationStale), das factories e seus states, a assinatura de ProtocolNumberGenerator::generate e de ViabilityRequestStateMachine::transition, e a decisão da geometria driver-aware (coluna/índice só pgsql). Insumo direto de TODOS os planos seguintes.
</output>

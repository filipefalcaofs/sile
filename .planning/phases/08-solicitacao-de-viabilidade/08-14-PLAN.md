---
phase: 08-solicitacao-de-viabilidade
plan: 14
type: execute
wave: 7
depends_on: ["08-10"]
files_modified:
  - app/Http/Controllers/Gestao/ContingenciaController.php
  - app/Http/Requests/Gestao/RegistrarContingenciaRequest.php
  - routes/gestao.php
  - resources/js/pages/gestao/contingencia/create.tsx
  - tests/Feature/Solicitacao/ContingenciaTest.php
autonomous: true
requirements: [HU-148]

must_haves:
  truths:
    - "Operador autorizado (permissão registrar-contingencia) registra uma solicitação manualmente na retaguarda com origem 'contingencia' auditada (HU-148 CA-01)"
    - "A contingência executa EXATAMENTE o mesmo fluxo/motor do canal normal — nunca um atalho decisório (RN-002): mesma criação/instrução/protocolo, mudando só a origem (auditada) e exigindo motivo obrigatório"
    - "Origem 'contingencia' fica visível no processo e nos relatórios; referência externa (BAP/Regin informado pelo requerente) pode ser vinculada sem duplicar processo (RN-004 — reusa a detecção de duplicidade)"
    - "Sem a permissão, 403 auditado (CA-04); contingência é o caminho real de operação enquanto o contrato Regin não chega (Fase 13)"
  artifacts:
    - path: "app/Http/Controllers/Gestao/ContingenciaController.php"
      provides: "Registro de solicitação em contingência na retaguarda (mesmo fluxo, origem auditada)"
      contains: "Contingencia"
  key_links:
    - from: "app/Http/Controllers/Gestao/ContingenciaController.php"
      to: "App\\Services\\Solicitacao\\ProtocolarSolicitacaoService"
      via: "reusa o MESMO protocolo/fluxo do canal normal (sem atalho decisório)"
      pattern: "ProtocolarSolicitacaoService"
    - from: "routes/gestao.php"
      to: "registrar-contingencia"
      via: "rota protegida pela permissão da fase (08-02)"
      pattern: "permission:registrar-contingencia"
---

<objective>
Canal de operador — **contingência** (HU-148): operador autorizado da SEDUR registra manualmente uma solicitação na retaguarda com origem `contingencia` (auditada), preenchendo o MESMO conjunto de dados do formulário oficial e seguindo EXATAMENTE o mesmo fluxo decisório (criação → instrução → protocolo) — nunca um atalho. Motivo obrigatório; referência externa (BAP/Regin) vinculável sem duplicar (RN-004).

Purpose: critério 6 do ROADMAP (registrar em contingência com origem auditada) — e o caminho real de operação enquanto o contrato Regin não chega (Fase 13). Anti-fachada: reusa o ProtocolarSolicitacaoService e os mesmos motores; a contingência muda só a origem e o ator, nunca a lógica; sem adaptador simulado.
Output: ContingenciaController (retaguarda) + request + rota sob `permission:registrar-contingencia` + tela no console + feature tests. Plano IRMÃO PARALELO do 08-15.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-solicitacao-de-viabilidade/08-CONTEXT.md
@.planning/phases/08-solicitacao-de-viabilidade/08-01-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-02-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-05-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-10-SUMMARY.md
@docs/SILE_HUs_Completas_MD/EP08-Solicitacao-de-Viabilidade/HU-148-Registrar-solicitacao-em-contingencia.md

# REUSO (mesmo fluxo/motor; permissão; duplicidade) — ler antes de codar:
@app/Services/Solicitacao/ProtocolarSolicitacaoService.php
@app/Services/Solicitacao/DuplicateRequestDetector.php
@app/Models/ViabilityRequest.php
@app/Enums/ViabilityRequestOrigin.php
@routes/gestao.php
@app/Http/Controllers/Gestao/CnaeController.php
</context>

<skill>
ATIVAR `inertia-react-development` para a tela do console (variant="console").
</skill>

<tasks>

<task type="auto">
  <name>Task 1: backend de contingência (mesmo fluxo, origem auditada, motivo obrigatório) + permissão + rotas</name>
  <read_first>app/Services/Solicitacao/ProtocolarSolicitacaoService.php, app/Services/Solicitacao/DuplicateRequestDetector.php, app/Enums/ViabilityRequestOrigin.php, routes/gestao.php</read_first>
  <files>app/Http/Controllers/Gestao/ContingenciaController.php, app/Http/Requests/Gestao/RegistrarContingenciaRequest.php, routes/gestao.php, tests/Feature/Solicitacao/ContingenciaTest.php</files>
  <action>
    TDD estrito. `Gestao/ContingenciaController` (guard gestao): cria a solicitação com `origin = ViabilityRequestOrigin::Contingencia`, `contingency_reason` (obrigatório), `created_by_user_id = operador` e `requester_user_id` = o cidadão/empresa beneficiária (informado), e segue o MESMO fluxo do canal normal — reusar a criação (08-05) e o ProtocolarSolicitacaoService (08-10) com os mesmos motores/validações (documentos, número, transição, evento). NÃO recriar protocolo. Pode receber `external_reference` (BAP/Regin informado pelo requerente) e usar o DuplicateRequestDetector para não duplicar (RN-004) — alerta/bloqueio de vínculo duplicado conforme o caso. Auditoria com origem contingência e operador (RN-002/003).
    - Rotas em `routes/gestao.php` sob `Route::middleware('permission:registrar-contingencia')->prefix('contingencia')->name('contingencia.')`: create (GET tela), store (POST registrar).

    `ContingenciaTest` (SQLite, actingAs operador gestao com registrar-contingencia):
    - test_registra_em_contingencia_segue_fluxo_padrao (CA-01): store cria com origin contingencia + protocola pelo MESMO serviço; status protocolada, número gerado, transição/auditoria; contingency_reason salvo.
    - test_motivo_obrigatorio (RN-001): sem motivo → validação.
    - test_exige_permissao (CA-04): sem registrar-contingencia → 403 auditado.
    - test_origem_visivel (RN-003): origin contingencia persistida (dimensão para relatórios).
    - test_vinculo_externo_nao_duplica (RN-004): referência externa repetida não cria processo duplicado (reusa o detector).
    - test_mesmo_motor (RN-002): a contingência usa o mesmo ProtocolarSolicitacaoService (sem atalho) — asserir que o bloqueio documental também se aplica.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=ContingenciaTest</verify>
  <acceptance_criteria>
    - grep "permission:registrar-contingencia" routes/gestao.php
    - grep "ProtocolarSolicitacaoService" app/Http/Controllers/Gestao/ContingenciaController.php (mesmo fluxo)
    - grep "Contingencia" no controller (origem auditada)
    - ContingenciaTest verde (fluxo padrão, motivo, permissão, origem visível, não-duplicação, mesmo motor).
  </acceptance_criteria>
  <done>Contingência registra pelo MESMO fluxo/motor com origem auditada e motivo obrigatório, atrás da permissão, sem duplicar e sem atalho decisório.</done>
</task>

<task type="auto">
  <name>Task 2: tela de registro em contingência no console + navegação</name>
  <read_first>resources/js/pages/gestao/cnaes/index.tsx, resources/js/pages/portal/solicitacoes/wizard.tsx, app/Http/Controllers/Gestao/ContingenciaController.php</read_first>
  <files>resources/js/pages/gestao/contingencia/create.tsx</files>
  <action>
    Tela `gestao/contingencia/create.tsx` (console SEDUR): formulário com o mesmo conjunto de dados do formulário oficial (beneficiário por CPF/empresa, imóvel/polígono, área, CNAEs, indicadores, anexos), o MOTIVO da contingência (obrigatório) e a referência externa (BAP/Regin) opcional; submete ao store. Reusar componentes/etapas do wizard quando fizer sentido (mapa, picker) — sem recriar. Aviso visível de que o processo nasce com origem "contingência". Item de navegação no console condicionado à permissão registrar-contingencia.
  </action>
  <verify>npx tsc --noEmit 2>&1 | tail -20 && npm run build 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - grep "contingencia" resources/js/pages/gestao/contingencia/create.tsx
    - typecheck e build verdes; navegação por permissão.
  </acceptance_criteria>
  <done>Tela de contingência no console com o mesmo conjunto de dados + motivo + referência externa; navegação por permissão.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=ContingenciaTest` verde.
- `npx tsc --noEmit` e `npm run build` verdes.
- `php artisan test --compact` — suíte completa CONTINUA verde.
</verification>

<success_criteria>
- Contingência registra pelo MESMO fluxo/motor com origem auditada e motivo obrigatório, atrás da permissão; referência externa não duplica (RN-004).
- Caminho real de operação enquanto o Regin não chega; sem atalho decisório, sem adaptador simulado.
</success_criteria>

<output>
Criar `.planning/phases/08-solicitacao-de-viabilidade/08-14-SUMMARY.md`: rotas/permissão, como o mesmo fluxo é reusado (criação + ProtocolarSolicitacaoService), o tratamento de referência externa/duplicidade e a tela. Insumo do smoke (08-16) e gancho para EP13 (vinculação BAP posterior).
</output>

---
phase: 08-solicitacao-de-viabilidade
plan: 11
type: execute
wave: 7
depends_on: ["08-10"]
files_modified:
  - app/Http/Controllers/Portal/ConsultaProtocoloController.php
  - app/Http/Controllers/Portal/ConsultaProtocoloPublicaController.php
  - app/Services/Solicitacao/TimelineSolicitacao.php
  - routes/portal.php
  - app/Providers/FortifyServiceProvider.php
  - resources/js/pages/portal/solicitacoes/protocolo.tsx
  - resources/js/pages/portal/solicitacoes/protocolo-publico.tsx
  - tests/Feature/Solicitacao/ConsultarProtocoloTest.php
  - tests/Feature/Solicitacao/ConsultarProtocoloPublicoTest.php
autonomous: true
requirements: [HU-069]

must_haves:
  truths:
    - "O dono consulta o protocolo autenticado (escopo do requerente efetivo) com timeline e status em linguagem simples (publicLabel) — HU-069 RN-004/006"
    - "Existe consulta pública SEM login por link assinado (URL::temporarySignedRoute, TTL parametrizável) + middleware signed + throttle parametrizado — mostra status simples + timeline + prazo estimado, SEM dados sensíveis nem anexos (LGPD)"
    - "A timeline vem das viability_request_transitions (fonte real); o prazo estimado é parametrizado com ressalva honesta (medição real depende da HU-129/Fase 15)"
    - "Toda consulta é auditada (RN-002); a pública audita com causer null + IP"
  artifacts:
    - path: "app/Http/Controllers/Portal/ConsultaProtocoloPublicaController.php"
      provides: "Consulta pública por link assinado (sem dados sensíveis)"
      contains: "signed"
    - path: "app/Services/Solicitacao/TimelineSolicitacao.php"
      provides: "Timeline simplificada (etapas concluídas/atual/pendências) a partir das transitions"
      contains: "class TimelineSolicitacao"
  key_links:
    - from: "app/Http/Controllers/Portal/ConsultaProtocoloPublicaController.php"
      to: "URL::temporarySignedRoute"
      via: "link assinado com TTL solicitacao.consulta_publica.assinatura_ttl_dias + throttle:consulta-protocolo"
      pattern: "temporarySignedRoute|signed"
    - from: "app/Services/Solicitacao/TimelineSolicitacao.php"
      to: "viability_request_transitions"
      via: "timeline real a partir das transições (publicLabel)"
      pattern: "transitions"
---

<objective>
Consultar protocolo (HU-069): consulta AUTENTICADA do dono (escopo do requerente efetivo) com timeline e status em linguagem simples; e consulta PÚBLICA sem login por link assinado (TTL parametrizável) + throttle, mostrando status simples + timeline + prazo estimado, sem dados sensíveis nem anexos (LGPD). A timeline vem das transições reais.

Purpose: critério 3 do ROADMAP (protocolo consultável em linguagem simples, timeline e prazo estimado). Reusa o RateLimiter parametrizado (padrão Fase 3.1/7) e URLs assinadas nativas (sem dep nova). Anti-fachada: timeline real (transitions), prazo com ressalva honesta (medição é HU-129/Fase 15), pública sem vazar dado sensível.
Output: ConsultaProtocoloController (autenticado) + ConsultaProtocoloPublicaController (assinado) + TimelineSolicitacao + RateLimiter consulta-protocolo + rotas + 2 páginas + feature tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-solicitacao-de-viabilidade/08-CONTEXT.md
@.planning/phases/08-solicitacao-de-viabilidade/08-01-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-02-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-05-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-10-SUMMARY.md
@docs/SILE_HUs_Completas_MD/EP08-Solicitacao-de-Viabilidade/HU-069-Consultar-protocolo.md

# PADRÕES A ESPELHAR (rota pública + throttle + toggle; policy/escopo; render JSON; página standalone) — ler antes de codar:
@app/Providers/FortifyServiceProvider.php
@routes/portal.php
@app/Http/Controllers/Portal/CompanyController.php
@app/Policies/ViabilityRequestPolicy.php
@app/Support/Audit/AuditService.php
@resources/js/pages/home.tsx
@bootstrap/app.php
</context>

<skill>
ATIVAR `inertia-react-development` (páginas) e `laravel-boost` (`search-docs`: ['signed urls temporarySignedRoute', 'route signed middleware', 'rate limiting']) antes de codar — link assinado + signed middleware + throttle.
</skill>

<tasks>

<task type="auto">
  <name>Task 1: TimelineSolicitacao + RateLimiter consulta-protocolo</name>
  <read_first>app/Models/ViabilityRequest.php, app/Enums/ViabilityRequestStatus.php, app/Providers/FortifyServiceProvider.php</read_first>
  <files>app/Services/Solicitacao/TimelineSolicitacao.php, app/Providers/FortifyServiceProvider.php, tests/Feature/Solicitacao/ConsultarProtocoloTest.php</files>
  <action>
    TDD. `TimelineSolicitacao::build(ViabilityRequest $request, bool $publico = false): array` — monta a timeline simplificada a partir das viability_request_transitions ordenadas por created_at: etapas concluídas, etapa atual, e (no modo autenticado) pendências do requerente. Usa publicLabel() do status para o cidadão (linguagem simples, HU-069 RN-004); no modo público, expõe SÓ data + rótulo amigável (sem reason interno, sem dados sensíveis, sem anexos). Prazo estimado: `Settings::get('solicitacao.prazo_estimado_dias', ...)` com ressalva de estimativa (RN-005 — medição real é HU-129/Fase 15).

    RateLimiter em FortifyServiceProvider (espelhar 'consulta-viabilidade' [07-01]): `RateLimiter::for('consulta-protocolo', fn (Request $r) => Limit::perMinute((int) Settings::get('seguranca.throttle.consulta_protocolo.por_minuto', 30))->by($r->user()?->id ?: $r->ip()));`.

    Os asserts do timeline entram em ConsultarProtocoloTest (Task 2).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan route:list >/dev/null</verify>
  <acceptance_criteria>
    - grep "class TimelineSolicitacao" e uso de transitions + publicLabel.
    - grep "consulta-protocolo" app/Providers/FortifyServiceProvider.php
  </acceptance_criteria>
  <done>Timeline real a partir das transições (linguagem simples) + RateLimiter parametrizado da consulta de protocolo.</done>
</task>

<task type="auto">
  <name>Task 2: consulta AUTENTICADA do dono (escopo + timeline + auditoria) + página</name>
  <read_first>app/Http/Controllers/Portal/CompanyController.php, app/Policies/ViabilityRequestPolicy.php, routes/portal.php, resources/js/pages/portal/solicitacoes</read_first>
  <files>app/Http/Controllers/Portal/ConsultaProtocoloController.php, routes/portal.php, resources/js/pages/portal/solicitacoes/protocolo.tsx, tests/Feature/Solicitacao/ConsultarProtocoloTest.php</files>
  <action>
    TDD estrito. `Portal/ConsultaProtocoloController@show(ViabilityRequest $solicitacao)`: Gate::authorize('view', $solicitacao) (dono/representado); monta a página com os dados do processo (empresa, imóvel resumo, CNAEs, status {value,label,publicLabel}, protocolo) + TimelineSolicitacao::build(..., publico:false) + prazo estimado; auditoria (event 'consulta-protocolo'). Inertia::render('portal/solicitacoes/protocolo').

    Rota (grupo autenticado): `GET solicitacoes/{solicitacao}` (name solicitacoes.show) — DEPOIS das literais (solicitacoes index/create já são literais no 08-05).

    Página `portal/solicitacoes/protocolo.tsx` (PortalLayout autenticado): cabeçalho com número de protocolo + status amigável (publicLabel), timeline (etapas/atual/pendências), prazo estimado com ressalva, dados do processo, link para baixar documentos (autenticado), e o botão "Gerar link público de acompanhamento" (gera o signed link — Task 3). Mobile-first, acessível.

    `ConsultarProtocoloTest` (SQLite):
    - test_dono_consulta_protocolo (CA-01): GET → 200, prop com protocolo/status publicLabel/timeline.
    - test_timeline_em_linguagem_simples (RN-004/006): timeline usa publicLabel e marca etapa atual.
    - test_terceiro_nao_consulta (CA-04): 403 auditado.
    - test_prazo_estimado_parametrizado_com_ressalva (RN-005).
    - test_consulta_auditada (RN-002).
  </action>
  <verify>php artisan test --compact --filter=ConsultarProtocoloTest && npx tsc --noEmit 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - grep "Gate::authorize('view'" app/Http/Controllers/Portal/ConsultaProtocoloController.php
    - grep "component('portal/solicitacoes/protocolo')" tests/Feature/Solicitacao/ConsultarProtocoloTest.php
    - ConsultarProtocoloTest verde; typecheck verde.
  </acceptance_criteria>
  <done>Consulta autenticada escopada ao dono com timeline em linguagem simples, prazo com ressalva e auditoria; página renderizada.</done>
</task>

<task type="auto">
  <name>Task 3: consulta PÚBLICA por link assinado (TTL + throttle + LGPD) + página + render JSON</name>
  <read_first>bootstrap/app.php, routes/portal.php, app/Support/Audit/AuditService.php</read_first>
  <files>app/Http/Controllers/Portal/ConsultaProtocoloPublicaController.php, routes/portal.php, resources/js/pages/portal/solicitacoes/protocolo-publico.tsx, tests/Feature/Solicitacao/ConsultarProtocoloPublicoTest.php</files>
  <action>
    TDD estrito. Geração do link: no controller autenticado (08-11 Task 2) ou um endpoint próprio, `URL::temporarySignedRoute('portal.protocolo.publico', now()->addDays((int) Settings::get('solicitacao.consulta_publica.assinatura_ttl_dias', 30)), ['solicitacao' => $request->id])`. 
    `Portal/ConsultaProtocoloPublicaController@show(ViabilityRequest $solicitacao)`: rota com middleware `signed` + `throttle:consulta-protocolo`, SEM auth. Mostra SÓ: número de protocolo, status amigável (publicLabel), timeline pública (TimelineSolicitacao::build(..., publico:true)), prazo estimado com ressalva. NUNCA dados sensíveis (CPF/CNPJ completo, endereço detalhado), NUNCA anexos. Auditoria com causer null + IP (RN-002, padrão da consulta pública [07-06]).

    Rota PÚBLICA (fora de auth, com signed + throttle), name `portal.protocolo.publico`. Se a página usar JSON/Inertia, garantir o comportamento correto; é uma página Inertia standalone pública (padrão home.tsx), não endpoint JSON — render Inertia normal.

    Página `portal/solicitacoes/protocolo-publico.tsx` STANDALONE pública (padrão home.tsx — header próprio, sem PortalLayout): status amigável + timeline pública + prazo + ressalva. Sem nada sensível. Mobile-first/acessível.

    `ConsultarProtocoloPublicoTest` (SQLite):
    - test_link_assinado_abre_sem_login: gerar signed URL → GET (sem auth) 200 com status/timeline pública.
    - test_link_invalido_ou_expirado_bloqueia: URL sem assinatura/expirada → 403.
    - test_nao_expoe_dados_sensiveis (LGPD): a resposta NÃO contém CPF, CNPJ completo, endereço detalhado nem anexos.
    - test_throttle_aplicado: estourar o limite → 429 (parametrizado).
    - test_auditoria_publica_com_causer_null: Activity 'solicitacoes'/'consulta-protocolo-publica' com causer null + IP.
  </action>
  <verify>php artisan test --compact --filter=ConsultarProtocoloPublicoTest && npx tsc --noEmit 2>&1 | tail -20 && npm run build 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - grep "temporarySignedRoute" no código que gera o link e middleware "signed" + "throttle:consulta-protocolo" na rota pública.
    - grep "component('portal/solicitacoes/protocolo-publico')" no teste.
    - ConsultarProtocoloPublicoTest verde (assinado abre, inválido bloqueia, sem dado sensível, throttle, auditoria); typecheck/build verdes.
  </acceptance_criteria>
  <done>Consulta pública por link assinado (TTL parametrizável) + throttle, sem dados sensíveis/anexos (LGPD), auditada com causer null; página standalone pública.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="ConsultarProtocoloTest|ConsultarProtocoloPublicoTest"` verde.
- `npx tsc --noEmit` e `npm run build` verdes.
- `php artisan test --compact` — suíte completa CONTINUA verde.
</verification>

<success_criteria>
- Consulta autenticada (dono) com timeline em linguagem simples e prazo com ressalva; consulta pública por link assinado (TTL) + throttle, sem dados sensíveis (LGPD), ambas auditadas.
- Timeline real (transitions); RateLimiter parametrizado; sem dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/08-solicitacao-de-viabilidade/08-11-SUMMARY.md`: rotas (autenticada e pública assinada), contrato da timeline/prazo, o que a pública NÃO expõe (LGPD), o nome do RateLimiter e como o link é gerado. Insumo direto de 08-13/08-14 (botão de link público), 08-16 (smoke da consulta pública).
</output>

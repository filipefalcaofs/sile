---
phase: 08-solicitacao-de-viabilidade
plan: 04
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - app/Http/Controllers/Gestao/DocumentRequirementController.php
  - app/Http/Requests/Gestao/StoreDocumentRequirementRequest.php
  - app/Http/Requests/Gestao/UpdateDocumentRequirementRequest.php
  - routes/gestao.php
  - resources/js/pages/gestao/requisitos-documentais/index.tsx
  - tests/Feature/Solicitacao/DocumentRequirementCrudTest.php
autonomous: true
requirements: [HU-067]

must_haves:
  truths:
    - "O administrador mantém os requisitos documentais (modelo 'Requisito' SIGVISA) e os vincula N:N a CNAEs — a obrigatoriedade documental por CNAE é DADO administrável (HU-067), não código"
    - "A tabela por-CNAE nasce vazia (carga oficial pendente SEDUR) — sem fachada; o resolver (08-08) une os requisitos dos CNAEs da solicitação às condicionais base (fachada sempre; concessão se área pública)"
    - "CRUD + vínculo N:N atrás da permissão manter-requisitos-documentais; sem ela, 403 auditado (CA-04)"
    - "code único/imutável; desativar preserva histórico; toda alteração auditada (RN-002)"
  artifacts:
    - path: "app/Http/Controllers/Gestao/DocumentRequirementController.php"
      provides: "CRUD dos requisitos + sync do vínculo N:N com CNAEs"
      contains: "DocumentRequirement"
    - path: "resources/js/pages/gestao/requisitos-documentais/index.tsx"
      provides: "Tela de manutenção (requisitos + CNAEs vinculados) no console"
      contains: "requisitos-documentais"
  key_links:
    - from: "app/Http/Controllers/Gestao/DocumentRequirementController.php"
      to: "cnae_document_requirement"
      via: "sync() do vínculo N:N (conjunto exato — padrão syncSecondaries/syncPermissions)"
      pattern: "documentRequirements\\(\\)|cnaes\\(\\)->sync"
---

<objective>
Cadastro administrável dos **requisitos documentais** (modelo "Requisito" do SIGVISA — HU-067) e do vínculo N:N com CNAEs: a obrigatoriedade documental por CNAE vira DADO versionável e administrável, atendendo a coluna "Documentação específica por CNAE" da planilha unificada (hoje vazia) e preparando o gancho de validação por IA (EP14, `validation_instructions`).

Purpose: a validação documental (08-08) deriva a lista de obrigatórios dos CNAEs da solicitação. Sem cadastro administrável, a regra documental por CNAE seria hardcoded. Nasce honesto: a tabela por-CNAE entra vazia e recebe a carga oficial depois; os obrigatórios-base condicionais (fachada sempre; concessão se área pública) são tratados no resolver (08-08).
Output: DocumentRequirementController (CRUD + sync de CNAEs) + FormRequests + rotas sob `permission:manter-requisitos-documentais` + tela `gestao/requisitos-documentais/index.tsx`.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-solicitacao-de-viabilidade/08-CONTEXT.md
@.planning/phases/08-solicitacao-de-viabilidade/08-01-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-02-SUMMARY.md
@docs/SILE_HUs_Completas_MD/EP08-Solicitacao-de-Viabilidade/HU-067-Validar-documentos-obrigatorios.md

# PADRÕES A ESPELHAR (CRUD de gestão + N:N sync + permissão; picker de CNAE) — ler antes de codar:
@app/Http/Controllers/Gestao/CnaeController.php
@app/Services/CompanyCnaeService.php
@app/Http/Controllers/Portal/CnaeSearchController.php
@routes/gestao.php
@resources/js/pages/gestao/cnaes/index.tsx
@app/Models/DocumentRequirement.php
</context>

<skill>
ATIVAR `inertia-react-development`. Padrão de listagem/CRUD da gestão (Fase 2.4), console SEDUR. O picker de CNAEs reusa o endpoint de busca (CnaeSearchController) — só ativos.
</skill>

<tasks>

<task type="auto">
  <name>Task 1: CRUD backend dos requisitos + vínculo N:N com CNAEs (permissão + auditoria)</name>
  <read_first>app/Http/Controllers/Gestao/CnaeController.php, app/Services/CompanyCnaeService.php, routes/gestao.php, app/Models/DocumentRequirement.php</read_first>
  <files>app/Http/Controllers/Gestao/DocumentRequirementController.php, app/Http/Requests/Gestao/StoreDocumentRequirementRequest.php, app/Http/Requests/Gestao/UpdateDocumentRequirementRequest.php, routes/gestao.php, tests/Feature/Solicitacao/DocumentRequirementCrudTest.php</files>
  <action>
    TDD estrito. Controller `Gestao/DocumentRequirementController`: index server-driven (busca por code/name; ordenação whitelist; per_page parametrizado), store/update via FormRequests (code único; code imutável na edição), toggle active (preserva histórico). Campos: code, name, description, required (bool), active (bool), validation_instructions (text). Vínculo N:N com CNAEs: endpoint para sincronizar os CNAEs de um requisito via `sync()` (conjunto exato — padrão syncSecondaries [03-06]); auditoria explícita do vínculo (event 'requisito-cnaes', antes/depois) coexistindo com o created/updated. Auditoria do requisito via HasAuditoria (como Company) OU explícita — ser consistente com 08-03.

    Rotas em `routes/gestao.php` sob `Route::middleware('permission:manter-requisitos-documentais')->prefix('requisitos-documentais')->name('requisitos-documentais.')`: index, store, update {requirement}, toggle, e o sync de CNAEs (PUT {requirement}/cnaes).

    `DocumentRequirementCrudTest` (SQLite, actingAs admin gestao):
    - test_exige_permissao: sem manter-requisitos-documentais → 403 auditado (CA-04).
    - test_cria_requisito: cria com code/name/required; auditado.
    - test_vincula_cnaes_por_sync: PUT cnaes com [cnaeA, cnaeB] → vínculo exato; novo sync com [cnaeA] remove o B (conjunto exato); auditado antes/depois.
    - test_code_unico_e_imutavel.
    - test_desativar_preserva_registro.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=DocumentRequirementCrudTest</verify>
  <acceptance_criteria>
    - grep "permission:manter-requisitos-documentais" routes/gestao.php
    - grep "class DocumentRequirementController" e o sync N:N dos CNAEs.
    - DocumentRequirementCrudTest verde (permissão, CRUD, sync N:N exato, code único/imutável, desativação preserva).
  </acceptance_criteria>
  <done>CRUD dos requisitos + vínculo N:N por sync, atrás da permissão, com auditoria e desativação que preserva.</done>
</task>

<task type="auto">
  <name>Task 2: tela de manutenção (requisitos + CNAEs vinculados) no console + navegação</name>
  <read_first>resources/js/pages/gestao/cnaes/index.tsx, resources/js/components/ui/data-table, app/Http/Controllers/Portal/CnaeSearchController.php</read_first>
  <files>resources/js/pages/gestao/requisitos-documentais/index.tsx</files>
  <action>
    Tela `gestao/requisitos-documentais/index.tsx` (console): PageHeader, Card com "Novo requisito", DataTable (code, name, obrigatório, situação), Modal criar/editar (code/name/description/required/active/validation_instructions). Gerenciar os CNAEs vinculados de um requisito (picker reusando o endpoint de busca de CNAEs — só ativos; chips dos vinculados; salvar via PUT cnaes). Toggle de ativação via confirm-dialog. Item de navegação condicionado à permissão.
  </action>
  <verify>npx tsc --noEmit 2>&1 | tail -20 && npm run build 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - grep "requisitos-documentais" resources/js/pages/gestao/requisitos-documentais/index.tsx
    - typecheck e build verdes; navegação por permissão.
  </acceptance_criteria>
  <done>Tela de manutenção dos requisitos com CRUD + vínculo de CNAEs (picker reusado); navegação por permissão.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=DocumentRequirementCrudTest` verde.
- `npx tsc --noEmit` e `npm run build` verdes.
- `php artisan test --compact` — suíte completa CONTINUA verde.
</verification>

<success_criteria>
- Requisitos documentais administráveis (CRUD) + vínculo N:N com CNAEs por sync exato, atrás da permissão, auditados.
- Tabela por-CNAE nasce vazia (carga oficial pendente SEDUR) — sem fachada; resolver (08-08) consome.
</success_criteria>

<output>
Criar `.planning/phases/08-solicitacao-de-viabilidade/08-04-SUMMARY.md`: rotas/nomes, como os requisitos são vinculados a CNAEs (relação no model), e o contrato para o DocumentRequirementResolver (08-08). Insumo direto de 08-08 (validação documental).
</output>

---
phase: 08-solicitacao-de-viabilidade
plan: 06
type: execute
wave: 4
depends_on: ["08-05"]
files_modified:
  - app/Http/Controllers/Portal/SolicitacaoImovelController.php
  - app/Http/Requests/Portal/UpdateSolicitacaoImovelRequest.php
  - app/Services/Solicitacao/PropertyGeometryWriter.php
  - routes/portal.php
  - tests/Feature/Solicitacao/InformarImovelTest.php
  - tests/Feature/Solicitacao/InformarImovelPostgisTest.php
autonomous: true
requirements: [HU-062, HU-063]

must_haves:
  truths:
    - "O requerente informa o imóvel (HU-062): polígono de 4 pontos (GeoJSON, fonte), complemento texto livre, ponto de referência, indicadores (escritório virtual/área pública/acessos independentes) — gravados na solicitação"
    - "O território é identificado pelo TerritoryService::identify (centroide do polígono) reusando a Fase 4 — degrada honesto (zona pendente SEDUR), nunca inventa"
    - "A geometria derivada (property_polygon) é gravada driver-aware (ST_MakeValid(ST_SetSRID(ST_GeomFromGeoJSON(?),4326))) SÓ no pgsql — espelha o GeoJsonLayerImporter; em SQLite a fonte é o GeoJSON jsonb"
    - "Área utilizada (HU-063) é validada contra a área do polígono: divergência acima da tolerância parametrizável gera ALERTA (não bloqueia, RN-004); a inconsistência mantida fica registrada"
    - "Mudar imóvel/área zera a simulação anterior (markSimulationStale — HU-063 RN-005); só o dono (policy update, status rascunho) edita; auditado"
  artifacts:
    - path: "app/Http/Controllers/Portal/SolicitacaoImovelController.php"
      provides: "Atualiza imóvel + área do rascunho; identifica território; valida área×polígono"
      contains: "TerritoryService"
    - path: "app/Services/Solicitacao/PropertyGeometryWriter.php"
      provides: "Gravação driver-aware da geometria derivada do GeoJSON (só pgsql)"
      contains: "ST_GeomFromGeoJSON"
  key_links:
    - from: "app/Http/Controllers/Portal/SolicitacaoImovelController.php"
      to: "App\\Services\\Geo\\TerritoryService"
      via: "identify(centroideLat, centroideLng) — reusa a Fase 4 (degrada sem zona)"
      pattern: "->identify("
    - from: "app/Services/Solicitacao/PropertyGeometryWriter.php"
      to: "App\\Services\\Geo\\GeoJsonLayerImporter"
      via: "mesmo padrão ST_MakeValid(ST_SetSRID(ST_GeomFromGeoJSON(?),4326)) só pgsql"
      pattern: "DB::getDriverName() === 'pgsql'"
---

<objective>
Instrução do imóvel (HU-062) e da área (HU-063) do rascunho: grava o polígono de 4 pontos (GeoJSON é a fonte), complemento (texto livre — HU-139 adiado), ponto de referência e os indicadores; identifica o território reusando o `TerritoryService` (Fase 4, degrada honesto sem zona); deriva a geometria driver-aware (só pgsql, idêntico ao `GeoJsonLayerImporter`); e valida a área declarada contra a do polígono com tolerância parametrizável (alerta, não bloqueia — RN-004).

Purpose: critério 1 e 5 do ROADMAP (imóvel/área + inconsistência área×polígono). Reuso total da Fase 4 (TerritoryService/LocationValidationService/mapa) — sem recriar geo. Anti-fachada: zona indisponível → território "pendente" (nunca inventado); geometria derivada real no Postgres (provada em @group postgis); divergência de área alerta sem travar e fica registrada para o analista.
Output: SolicitacaoImovelController (update imóvel+área) + UpdateSolicitacaoImovelRequest + PropertyGeometryWriter (driver-aware) + rotas + feature tests (SQLite) + @group postgis (derivação da geometria + território real do ponto).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-solicitacao-de-viabilidade/08-CONTEXT.md
@.planning/phases/08-solicitacao-de-viabilidade/08-01-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-05-SUMMARY.md
@docs/SILE_HUs_Completas_MD/EP08-Solicitacao-de-Viabilidade/HU-062-Informar-imovel.md
@docs/SILE_HUs_Completas_MD/EP08-Solicitacao-de-Viabilidade/HU-063-Informar-area-utilizada.md

# REUSO (consumir, NÃO recriar) + padrão driver-aware — ler antes de codar:
@app/Services/Geo/TerritoryService.php
@app/Services/Geo/TerritoryResult.php
@app/Services/Geo/LocationValidationService.php
@app/Services/Geo/GeoJsonLayerImporter.php
@app/Services/Geo/PostgisSpatialRepository.php
@app/Models/ViabilityRequest.php
@tests/PostgisTestCase.php
@tests/Feature/Geo/PostgisSpatialRepositoryTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: PropertyGeometryWriter (derivação driver-aware) + InformarImovelPostgisTest</name>
  <read_first>app/Services/Geo/GeoJsonLayerImporter.php, app/Services/Geo/PostgisSpatialRepository.php, tests/PostgisTestCase.php</read_first>
  <files>app/Services/Solicitacao/PropertyGeometryWriter.php, tests/Feature/Solicitacao/InformarImovelPostgisTest.php</files>
  <action>
    TDD. `PropertyGeometryWriter::write(ViabilityRequest $request): void` — quando `DB::getDriverName() === 'pgsql'` e há `property_polygon_geojson`, atualiza a coluna `property_polygon` via `DB::update('UPDATE viability_requests SET property_polygon = ST_MakeValid(ST_SetSRID(ST_GeomFromGeoJSON(?),4326)) WHERE id = ?', [json_encode($geojson), $request->id])` (idêntico ao GeoJsonLayerImporter; geometria NUNCA mass-assign). Em SQLite é NO-OP (coluna não existe; a fonte é o jsonb) — early return.

    `InformarImovelPostgisTest extends PostgisTestCase` (#[Group('postgis')]):
    - test_geometria_derivada_do_geojson_no_postgres: salvar imóvel com polígono; após write(), `SELECT ST_AsText(property_polygon)` não nulo e é POLYGON 4326.
    - test_area_x_poligono_via_postgis (RN-004): reusar LocationValidationService OU consulta espacial real para confirmar que a área declarada vs polígono é avaliada (espelhar PostgisLocationValidationTest). (Se a validação contra LOTE degrada — lote pendente SEDUR —, validar contra a área do PRÓPRIO polígono via ST_Area, como diz o CONTEXT "contra o polígono desenhado funciona".)
  </action>
  <verify>php artisan test --compact --group=postgis --filter=InformarImovelPostgisTest</verify>
  <acceptance_criteria>
    - grep "ST_GeomFromGeoJSON" app/Services/Solicitacao/PropertyGeometryWriter.php
    - grep "DB::getDriverName() === 'pgsql'" app/Services/Solicitacao/PropertyGeometryWriter.php
    - InformarImovelPostgisTest verde com PostGIS de pé (skip honesto sem servidor; FALHA com POSTGIS_TESTS_REQUIRED=true).
  </acceptance_criteria>
  <done>Geometria derivada driver-aware (só pgsql) provada em @group postgis; SQLite usa o GeoJSON jsonb como fonte.</done>
</task>

<task type="auto">
  <name>Task 2: SolicitacaoImovelController update (imóvel + indicadores + território) + área/tolerância + rotas</name>
  <read_first>app/Services/Geo/TerritoryService.php, app/Services/Geo/LocationValidationService.php, app/Http/Controllers/Portal/CompanyController.php, app/Models/ViabilityRequest.php, routes/portal.php</read_first>
  <files>app/Http/Controllers/Portal/SolicitacaoImovelController.php, app/Http/Requests/Portal/UpdateSolicitacaoImovelRequest.php, routes/portal.php, tests/Feature/Solicitacao/InformarImovelTest.php</files>
  <action>
    TDD estrito. `Portal/SolicitacaoImovelController@update(UpdateSolicitacaoImovelRequest, ViabilityRequest $solicitacao)`:
    - `Gate::authorize('update', $solicitacao)` (policy: dono + status rascunho).
    - grava polígono GeoJSON (fonte), address_* (incl. address_complement TEXTO LIVRE), address_reference (obrigatório — RN do HU-062), used_area_m2, indicadores (is_virtual_office/is_public_area/has_independent_access). Em DB::transaction.
    - calcula o centroide do polígono (média dos vértices em PHP — portável SQLite) e chama `TerritoryService::identify($lat,$lng)` (reusa Fase 4); guarda o resumo do território (zona pendente → registrado, nunca inventado) — para exibição/alerta. NÃO recomputa veredito.
    - chama `PropertyGeometryWriter::write($solicitacao)` (deriva geometry só pgsql).
    - VALIDAÇÃO ÁREA×POLÍGONO (HU-063 RN-004): calcula a área do polígono (em pgsql via ST_Area; em SQLite uma aproximação planar simples a partir dos vértices OU pula com aviso honesto) e compara com used_area_m2; se a divergência exceder `solicitacao.area_poligono.tolerancia_percentual` (Settings::get, default 10), devolve ALERTA (flash/prop), NÃO bloqueia; a inconsistência mantida fica registrada (ex.: um campo/flag ou nota auditada) para o analista. Validação contra LOTE oficial degrada (lote pendente SEDUR) — reusar LocationValidationService que já comunica "indisponível".
    - chama `$solicitacao->markSimulationStale()` (mudou imóvel/área → invalida simulação anterior, HU-063 RN-005).
    - auditoria da atualização (HasAuditoria updated + nota de negócio se houver alerta).
    `UpdateSolicitacaoImovelRequest`: valida polígono GeoJSON (type Polygon, anel com >=4 pontos), address_reference obrigatório, used_area_m2 numérico > 0, indicadores boolean; área pública (is_public_area) exige documento de concessão depois (regra documental fica no 08-08, aqui só o indicador). Mensagens pt-BR.

    Rota em `routes/portal.php` (grupo autenticado): `PUT solicitacoes/{solicitacao}/imovel` (name solicitacoes.imovel) — DEPOIS das literais; bind {solicitacao} via Route Model Binding.

    `InformarImovelTest` (SQLite):
    - test_dono_informa_imovel (CA-01): PUT grava polígono/complemento/referência/indicadores/área; território identificado (degradado sem zona) registrado.
    - test_zona_pendente_nao_inventa: território com zona indisponível → registrado como pendente, nunca um valor falso.
    - test_area_divergente_alerta_sem_bloquear (RN-004): área declarada >> área do polígono → alerta presente, registro salvo, sem erro 422.
    - test_mudanca_de_imovel_zera_simulacao (RN-005): com simulated_at preenchido, o update zera (markSimulationStale).
    - test_so_dono_em_rascunho_edita (CA-04): terceiro → 403 auditado; solicitação protocolada → update bloqueado pela policy.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=InformarImovelTest</verify>
  <acceptance_criteria>
    - grep "->identify(" app/Http/Controllers/Portal/SolicitacaoImovelController.php (reuso do TerritoryService)
    - grep "tolerancia_percentual" no controller (alerta parametrizado)
    - grep "markSimulationStale" no controller (RN-005)
    - InformarImovelTest verde (imóvel, zona pendente honesta, alerta de área, invalidação de simulação, autorização).
  </acceptance_criteria>
  <done>Imóvel + área gravados; território reusado (degrada honesto); alerta área×polígono parametrizado sem bloquear; simulação invalidada; autorização por policy.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter=InformarImovelTest` verde.
- `php artisan test --compact --group=postgis --filter=InformarImovelPostgisTest` verde (container de pé) ou skip honesto.
- `php artisan test --compact` — suíte completa CONTINUA verde.
</verification>

<success_criteria>
- Imóvel (polígono fonte + complemento texto livre + referência + indicadores) e área gravados; território reusado da Fase 4 (zona pendente honesta).
- Geometria derivada driver-aware (só pgsql) provada em @group postgis; área×polígono alerta parametrizado sem bloquear; mudança invalida simulação; autorização por policy/auditoria.
</success_criteria>

<output>
Criar `.planning/phases/08-solicitacao-de-viabilidade/08-06-SUMMARY.md`: rota/contrato do update de imóvel, formato do GeoJSON aceito, como o território é resumido/persistido, a estratégia de área×polígono (pgsql vs SQLite) e a chamada de markSimulationStale. Insumo direto de 08-08 (concessão se área pública), 08-09 (ponto da simulação), 08-13 (UI mapa) e 08-10 (protocolo).
</output>

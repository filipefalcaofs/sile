---
phase: 08-solicitacao-de-viabilidade
plan: 13
type: execute
wave: 8
depends_on: ["08-10", "08-11", "08-12"]
files_modified:
  - resources/js/pages/portal/solicitacoes/index.tsx
  - resources/js/pages/portal/solicitacoes/wizard.tsx
  - resources/js/components/solicitacao/etapa-imovel.tsx
  - resources/js/components/solicitacao/etapa-atividades.tsx
  - resources/js/components/solicitacao/etapa-documentos.tsx
  - resources/js/components/solicitacao/etapa-simulacao.tsx
  - resources/js/components/solicitacao/etapa-revisao.tsx
  - app/Http/Controllers/Portal/SolicitacaoController.php
  - routes/portal.php
  - tests/Feature/Solicitacao/SolicitacaoWizardPaginaTest.php
autonomous: true
requirements: [HU-061, HU-062, HU-063, HU-064, HU-065, HU-066, HU-067, HU-141, HU-068, HU-070]

must_haves:
  truths:
    - "O cidadão percorre o wizard multi-etapas no portal: iniciar (tipo/empresa) → imóvel (mapa Leaflet reusado) → área → atividades/CNAEs (picker) → documentos (upload) → simulação (ResultadoViabilidade reusado) → revisão/protocolar — consumindo os endpoints reais"
    - "A etapa de imóvel reusa o mapa Leaflet da Fase 4 (MapaSection/MapImovel) para o polígono de 4 pontos; nada de geo recriado no front"
    - "A etapa de simulação reusa o componente ResultadoViabilidade (Fase 7) e mostra a tendência por CNAE; pendente sem zona aparece COM motivo (nunca permitido/não permitido sem zona)"
    - "Faltar documento obrigatório aparece como aviso antes de protocolar (não silencioso); a simulação não bloqueia (prosseguir com ciência)"
    - "Minhas solicitações lista as do dono com status amigável; cancelar via confirm-dialog; mobile-first e acessível (eMAG/WCAG)"
    - "Há rota GET própria que RENDERIZA o wizard (ex.: GET solicitacoes/nova para novo rascunho e GET solicitacoes/{solicitacao}/editar para continuar) — distinta da rota GET solicitacoes/{solicitacao} do 08-11 (página de protocolo/consulta). Sem isso o wizard não é navegável."
  artifacts:
    - path: "resources/js/pages/portal/solicitacoes/wizard.tsx"
      provides: "Wizard multi-etapas da solicitação (portal light)"
      contains: "wizard"
    - path: "routes/portal.php"
      provides: "Rota GET do wizard (solicitacoes/nova + solicitacoes/{solicitacao}/editar) renderizando a página wizard; aditivo às rotas dos 08-05..08-12"
      contains: "solicitacoes/nova"
    - path: "resources/js/pages/portal/solicitacoes/index.tsx"
      provides: "Lista Minhas solicitações + ações (consultar, cancelar)"
      contains: "solicitacoes"
  key_links:
    - from: "resources/js/components/solicitacao/etapa-imovel.tsx"
      to: "@/components/geo/mapa-section"
      via: "reuso do mapa Leaflet da Fase 4 (polígono 4 pontos)"
      pattern: "MapaSection"
    - from: "resources/js/components/solicitacao/etapa-simulacao.tsx"
      to: "@/components/viabilidade/resultado-viabilidade"
      via: "reuso do componente de resultado da Fase 7 (tendência por CNAE)"
      pattern: "ResultadoViabilidade"
---

<objective>
A jornada do cidadão: o **wizard multi-etapas** de solicitação no portal (iniciar → imóvel → área → atividades/CNAEs → documentos → simulação → revisão/protocolar) e a lista **Minhas solicitações**. Reusa o mapa Leaflet (Fase 4) para o polígono e o componente `ResultadoViabilidade` (Fase 7) para a simulação. Nada de lógica simulada no front: tudo consome os endpoints reais dos planos 08-05..08-12.

Purpose: torna navegável de ponta a ponta o que os backends entregaram (critérios 1, 2, 3, 5 do ROADMAP). Anti-fachada na UI: pendente sem zona aparece com motivo (nunca Permitido/Não permitido sem zona); faltar documento é aviso antes de protocolar; a simulação orienta mas não bloqueia (prosseguir com ciência). Acessível e mobile-first (portal do cidadão — obrigação eMAG/WCAG).
Output: páginas index (Minhas solicitações) + wizard, componentes das etapas, ajuste do SolicitacaoController para render das páginas, e feature test de render Inertia.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-solicitacao-de-viabilidade/08-CONTEXT.md
@.planning/phases/08-solicitacao-de-viabilidade/08-05-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-06-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-07-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-08-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-09-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-10-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-11-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-12-SUMMARY.md

# PADRÕES DE UI A REUSAR (mapa Fase 4; ResultadoViabilidade Fase 7; listagem portal; CNAE picker; CRUD modal) — ler antes de codar:
@resources/js/components/geo/mapa-section.tsx
@resources/js/components/geo/map-imovel.tsx
@resources/js/components/viabilidade/resultado-viabilidade.tsx
@resources/js/pages/portal/empresas/index.tsx
@resources/js/pages/portal/empresas/cadastrar.tsx
@resources/js/pages/portal/empresas/detalhe.tsx
@resources/js/components/ui/data-table
@resources/js/components/ui/confirm-dialog.tsx
</context>

<skill>
ATIVAR `inertia-react-development` (Inertia v3 + React 19): useForm/Form para as etapas, useHttp para a simulação (POST que retorna o resultado), partial reloads entre etapas, validação server-side exibida. Mapa SSR-safe via MapaSection (Pitfall do Leaflet). Padrão de listagem do portal (light) e componentes do design system.
</skill>

<tasks>

<task type="auto">
  <name>Task 1: lista Minhas solicitações (index) + ações consultar/cancelar</name>
  <read_first>resources/js/pages/portal/empresas/index.tsx, resources/js/components/ui/data-table, resources/js/components/ui/confirm-dialog.tsx, app/Http/Controllers/Portal/SolicitacaoController.php</read_first>
  <files>resources/js/pages/portal/solicitacoes/index.tsx, app/Http/Controllers/Portal/SolicitacaoController.php, tests/Feature/Solicitacao/SolicitacaoWizardPaginaTest.php</files>
  <action>
    Página `portal/solicitacoes/index.tsx` (PortalLayout autenticado, padrão da listagem do portal): DataTable das solicitações do dono (protocolo, empresa, status amigável com Badge por status, criada em), TableToolbar (busca por protocolo/empresa), Pagination; ação primária "Nova solicitação"; por linha: consultar (→ solicitacoes.show, 08-11) e cancelar (confirm-dialog → DELETE solicitacoes.cancelar, 08-12, só quando cancelável). Garantir que o SolicitacaoController@index passe o componente correto e o shape (status {value,label,publicLabel}, cancelable bool). Estado vazio amigável.
  </action>
  <verify>npx tsc --noEmit 2>&1 | tail -20 && php artisan test --compact --filter=SolicitacaoWizardPaginaTest</verify>
  <acceptance_criteria>
    - grep "component('portal/solicitacoes/index')" no teste e Inertia::render correspondente no controller.
    - grep "confirm-dialog\|ConfirmDialog" na index (cancelar).
    - typecheck verde; teste de render da index verde.
  </acceptance_criteria>
  <done>Lista Minhas solicitações com status amigável, consultar e cancelar (confirm-dialog); shape do controller alinhado.</done>
</task>

<task type="auto">
  <name>Task 2: wizard multi-etapas (iniciar + imóvel[mapa] + área + atividades/CNAEs)</name>
  <read_first>resources/js/components/geo/mapa-section.tsx, resources/js/pages/portal/empresas/cadastrar.tsx, resources/js/pages/portal/empresas/detalhe.tsx</read_first>
  <files>routes/portal.php, app/Http/Controllers/Portal/SolicitacaoController.php, resources/js/pages/portal/solicitacoes/wizard.tsx, resources/js/components/solicitacao/etapa-imovel.tsx, resources/js/components/solicitacao/etapa-atividades.tsx</files>
  <action>
    Definir a ROTA GET do wizard em `routes/portal.php` (aditivo, sem colidir com as rotas dos 08-05..08-12): `GET solicitacoes/nova` (name `portal.solicitacoes.create`) renderizando a página `portal/solicitacoes/wizard` para um novo rascunho, e `GET solicitacoes/{solicitacao}/editar` (name `portal.solicitacoes.edit`, autorizada pela ViabilityRequestPolicy) para continuar um rascunho existente. NÃO reusar `GET solicitacoes/{solicitacao}` (essa é a página de protocolo/consulta do 08-11). `SolicitacaoController@create`/`@edit` faz `Inertia::render('portal/solicitacoes/wizard', [...])` com o rascunho (quando edit) e os dados de apoio (tipos de serviço, empresas do dono).
    Página `portal/solicitacoes/wizard.tsx` (PortalLayout) orquestrando as etapas com indicador de progresso. Etapas:
    - INICIAR: selecionar tipo de serviço (lista de 08-03) + empresa do dono (lista da Fase 3); cria/continua o rascunho (POST solicitacoes.store, 08-05); exibir o alerta de duplicidade (RN-007) com link ao processo anterior quando vier.
    - IMÓVEL (`etapa-imovel.tsx`): mapa Leaflet reusado (MapaSection/MapImovel) para demarcar o polígono de 4 pontos; campos complemento (texto livre), ponto de referência (obrigatório), indicadores (escritório virtual/área pública/acessos independentes); PUT solicitacoes.imovel (08-06). Exibir o território identificado (zona pendente → comunicado honesto) e o alerta de área×polígono quando vier.
    - ÁREA: campo de área (m²) — pode estar junto da etapa imóvel (PUT imóvel) ou separado; mostrar o alerta de divergência (não bloqueia).
    - ATIVIDADES (`etapa-atividades.tsx`): picker do CNAE principal + complementares (reusa GET portal.cnaes.search, só ativos), respeitando o limite (mensagem do backend); PUT solicitacoes.atividades (08-07).
    Validações server-side exibidas; navegação entre etapas preserva o rascunho.
  </action>
  <verify>php artisan route:list --path=portal/solicitacoes && npx tsc --noEmit 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - route:list mostra GET solicitacoes/nova (portal.solicitacoes.create) e GET solicitacoes/{solicitacao}/editar (portal.solicitacoes.edit), distintas do GET solicitacoes/{solicitacao} (08-11).
    - grep "MapaSection" resources/js/components/solicitacao/etapa-imovel.tsx
    - grep "cnaes.search\|cnaes/search" resources/js/components/solicitacao/etapa-atividades.tsx (picker reusado)
    - typecheck verde.
  </acceptance_criteria>
  <done>Wizard com iniciar/imóvel(mapa)/área/atividades reusando mapa e picker; rascunho preservado entre etapas; alertas honestos exibidos.</done>
</task>

<task type="auto">
  <name>Task 3: etapas documentos + simulação + revisão/protocolar + verificação visual</name>
  <read_first>resources/js/components/viabilidade/resultado-viabilidade.tsx, resources/js/pages/portal/empresas/detalhe.tsx</read_first>
  <files>resources/js/components/solicitacao/etapa-documentos.tsx, resources/js/components/solicitacao/etapa-simulacao.tsx, resources/js/components/solicitacao/etapa-revisao.tsx, resources/js/pages/portal/solicitacoes/wizard.tsx</files>
  <action>
    - DOCUMENTOS (`etapa-documentos.tsx`): upload (POST solicitacoes.documentos, 08-08) com lista dos anexos + dos requisitos obrigatórios faltantes (do resolver) como AVISO; tipos/tamanho comunicados; download dos anexados (autenticado).
    - SIMULAÇÃO (`etapa-simulacao.tsx`): botão "Simular" (POST solicitacoes.simular via useHttp, 08-09) que renderiza `<ResultadoViabilidade>` (Fase 7) por CNAE — tendência viável/condições/indeferimento/pendente; PENDENTE sem zona aparece COM motivo (nunca Permitido/Não permitido sem zona); quando toggle off, comunicar degradação. Não bloqueia.
    - REVISÃO/PROTOCOLAR (`etapa-revisao.tsx`): resumo de tudo + aviso de documentos faltantes (se houver, bloqueia o botão com a lista) + checkbox de ciência "prosseguir mesmo com tendência de indeferimento" (proceed_despite) quando a simulação indicar; botão Protocolar (POST solicitacoes.protocolar, 08-10) → redireciona ao protocolo (08-11) com o número.
    Tratar erros/avisos (documentação incompleta vinda do backend como flash.error) de forma honesta.

    Verificação visual (registrar no SUMMARY): `composer dev`, percorrer o wizard com um endereço de Salvador — mapa demarca o polígono; simulação mostra tendência por CNAE (pendente sem zona com motivo); faltar fachada bloqueia o protocolo com aviso; protocolar gera número e leva à consulta. Conferir mobile (375px) e dark mode.
  </action>
  <verify>npx tsc --noEmit 2>&1 | tail -20 && npm run build 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - grep "ResultadoViabilidade" resources/js/components/solicitacao/etapa-simulacao.tsx
    - grep "proceed_despite" resources/js/components/solicitacao/etapa-revisao.tsx (ciência HU-141)
    - typecheck e build verdes.
  </acceptance_criteria>
  <done>Etapas de documentos (avisos de faltantes), simulação (ResultadoViabilidade reusado, pendente com motivo) e revisão/protocolar (ciência + bloqueio documental); verificação visual registrada.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` e `npm run build` verdes.
- `php artisan test --compact --filter=SolicitacaoWizardPaginaTest` verde.
- `php artisan test --compact` — suíte completa CONTINUA verde.
- Verificação visual (composer dev): wizard de ponta a ponta com mapa, simulação honesta, bloqueio documental e protocolo; mobile + dark mode.
</verification>

<success_criteria>
- Wizard multi-etapas navegável reusando mapa (Fase 4) e ResultadoViabilidade (Fase 7); Minhas solicitações com consultar/cancelar.
- UI honesta: pendente sem zona com motivo; documentos faltantes como aviso que bloqueia protocolo; simulação orienta sem bloquear. Acessível e mobile-first.
</success_criteria>

<output>
Criar `.planning/phases/08-solicitacao-de-viabilidade/08-13-SUMMARY.md`: páginas/componentes criados, contratos TS das etapas, reusos confirmados (MapaSection, ResultadoViabilidade, CNAE picker) e o registro da verificação visual. Insumo do smoke navegável (08-16).
</output>

---
phase: 08-solicitacao-de-viabilidade
plan: 09
type: execute
wave: 5
depends_on: ["08-06", "08-07"]
files_modified:
  - app/Services/Viabilidade/ConsultaViabilidadeService.php
  - app/Services/Solicitacao/SimulacaoSolicitacaoService.php
  - app/Http/Controllers/Portal/SolicitacaoSimulacaoController.php
  - routes/portal.php
  - tests/Feature/Solicitacao/SimulacaoSolicitacaoTest.php
autonomous: true
requirements: [HU-141]

must_haves:
  truths:
    - "A simulação usa os MOTORES REAIS da Fase 7 (ConsultaViabilidadeService) — nunca lógica paralela (HU-141 RN-001); itera os CNAEs da solicitação e propaga o veredito (não decide)"
    - "Reusa o ponto da própria solicitação (centroide do polígono) sem geocodificar de novo — expõe um ponto de entrada por ponto+CNAE no ConsultaViabilidadeService"
    - "A simulação NÃO bloqueia (orientativa, RN-002); persiste snapshot + versões das regras + resultado consolidado e marca simulated_at (RN-003)"
    - "Feature toggle features.simulacao_solicitacao; desligado degrada para fluxo sem simulação, sem falha (RN-005)"
    - "Sem zona oficial, o veredito por CNAE fica 'pendente' (propagado do motor) — nunca permitido/não permitido inventado"
  artifacts:
    - path: "app/Services/Solicitacao/SimulacaoSolicitacaoService.php"
      provides: "Itera CNAEs → ConsultaViabilidadeService por ponto; consolida e persiste snapshot"
      contains: "class SimulacaoSolicitacaoService"
    - path: "app/Services/Viabilidade/ConsultaViabilidadeService.php"
      provides: "Ponto de entrada público por ponto+CNAE (refactor mínimo, sem geocodificar de novo)"
      contains: "consultarPorPonto"
  key_links:
    - from: "app/Services/Solicitacao/SimulacaoSolicitacaoService.php"
      to: "App\\Services\\Viabilidade\\ConsultaViabilidadeService"
      via: "consulta por ponto+CNAE reusando o motor da Fase 7 (propaga veredito)"
      pattern: "ConsultaViabilidadeService"
    - from: "app/Http/Controllers/Portal/SolicitacaoSimulacaoController.php"
      to: "App\\Support\\Settings"
      via: "toggle features.simulacao_solicitacao (degrada sem falha)"
      pattern: "simulacao_solicitacao"
---

<objective>
Simulação pré-protocolo (HU-141): o `SimulacaoSolicitacaoService` itera os CNAEs da solicitação e chama o `ConsultaViabilidadeService` (Fase 7) PELO PONTO da própria solicitação (centroide do polígono — sem geocodificar de novo), PROPAGANDO o veredito do motor LOUOS (sem decisão paralela). Persiste o snapshot + versões + resultado e marca `simulated_at`; é orientativa e NÃO bloqueia o protocolo.

Purpose: critério 5 do ROADMAP (simulação com os motores reais do EP07; orientativa). Reuso total da Fase 7 — o refactor mínimo é EXPOR um ponto de entrada por ponto+CNAE (hoje `consultarPorPonto` é privado). Anti-fachada: mesma versão de regras do fluxo oficial; sem zona → pendente propagado; toggle desligado degrada sem falha.
Output: refactor mínimo no ConsultaViabilidadeService (entrada pública por ponto+CNAE) + SimulacaoSolicitacaoService + SolicitacaoSimulacaoController + rota + feature tests (fake e real do motor).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-solicitacao-de-viabilidade/08-CONTEXT.md
@.planning/phases/08-solicitacao-de-viabilidade/08-01-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-06-SUMMARY.md
@.planning/phases/08-solicitacao-de-viabilidade/08-07-SUMMARY.md
@docs/SILE_HUs_Completas_MD/EP08-Solicitacao-de-Viabilidade/HU-141-Simular-viabilidade-antes-de-protocolar.md

# REUSO (motor da Fase 7 — consumir, NÃO recriar) — ler antes de codar:
@app/Services/Viabilidade/ConsultaViabilidadeService.php
@app/Services/Viabilidade/ConsultaViabilidadeInput.php
@app/Services/Viabilidade/ConsultaViabilidadeResult.php
@app/Services/Geo/TerritoryService.php
@app/Models/ViabilityRequest.php
@app/Support/Settings.php
@tests/Feature/Viabilidade/ConsultaViabilidadeServiceTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: expor ponto de entrada por ponto+CNAE no ConsultaViabilidadeService (refactor mínimo)</name>
  <read_first>app/Services/Viabilidade/ConsultaViabilidadeService.php, app/Services/Viabilidade/ConsultaViabilidadeInput.php</read_first>
  <files>app/Services/Viabilidade/ConsultaViabilidadeService.php, tests/Feature/Solicitacao/SimulacaoSolicitacaoTest.php</files>
  <action>
    TDD. Refactor MÍNIMO e seguro: adicionar um método PÚBLICO no ConsultaViabilidadeService que consulta a partir de um ponto já conhecido + CNAE (+ área), SEM geocodificar de novo — ex.: `consultarPorPontoConhecido(float $lat, float $lng, string $cnae, ?float $area = null): ConsultaViabilidadeResult`, que monta um ConsultaViabilidadeInput (tipo apropriado — pode reusar paraEndereco/paraCnae com a flag de origem, à discrição; preferir um named constructor próprio ou reaproveitar o privado consultarPorPonto). NÃO alterar o comportamento dos 3 métodos públicos existentes (consultarPorEndereco/PorCnae/PorInscricao) — só EXPOR a entrada por ponto. Manter a auditoria existente do orquestrador.

    Confirmar que os testes EXISTENTES de ConsultaViabilidadeService continuam verdes (não quebrar a Fase 7). Os casos de simulação por ponto entram no SimulacaoSolicitacaoTest (Task 2).
  </action>
  <verify>php artisan test --compact --filter="ConsultaViabilidadeServiceTest|SimulacaoSolicitacaoTest"</verify>
  <acceptance_criteria>
    - grep "public function consultarPorPonto" OU novo método público por ponto no ConsultaViabilidadeService (entrada por ponto+CNAE).
    - ConsultaViabilidadeServiceTest (Fase 7) CONTINUA verde (sem regressão).
  </acceptance_criteria>
  <done>Entrada pública por ponto+CNAE exposta no motor da Fase 7, sem geocodificar de novo e sem regressão dos métodos existentes.</done>
</task>

<task type="auto">
  <name>Task 2: SimulacaoSolicitacaoService (itera CNAEs, propaga veredito, persiste snapshot) + controller/rota + toggle</name>
  <read_first>app/Services/Viabilidade/ConsultaViabilidadeResult.php, app/Models/ViabilityRequest.php, app/Support/Settings.php, routes/portal.php</read_first>
  <files>app/Services/Solicitacao/SimulacaoSolicitacaoService.php, app/Http/Controllers/Portal/SolicitacaoSimulacaoController.php, routes/portal.php, tests/Feature/Solicitacao/SimulacaoSolicitacaoTest.php</files>
  <action>
    TDD estrito. `SimulacaoSolicitacaoService::simulate(ViabilityRequest $request): array`:
    - calcula o ponto (centroide do polígono — reusar o helper do 08-06 ou recomputar dos vértices, PHP portável); se não houver polígono, simula por CNAE sem território (degrada honesto).
    - itera os CNAEs da solicitação (principal + complementares) e chama o motor (entrada por ponto+CNAE, área da solicitação) — PROPAGA o veredito de cada CNAE; NÃO decide.
    - consolida por CNAE (tendência: viável / viável com condições / tende a indeferimento / pendente — propagado), captura as versões das regras, e PERSISTE no request: simulation_snapshot (por CNAE), simulation_rules_versions, simulation_resultado (consolidado), simulated_at = now() (RN-003). NÃO reprocessa no protocolo (08-10 lê o snapshot).
    - auditoria (event 'simulacao', com versões) — RN-002.
    `Portal/SolicitacaoSimulacaoController@store(ViabilityRequest $solicitacao)`: Gate::authorize('update', $solicitacao); se `Settings::enabled('simulacao_solicitacao')` for false → degrada comunicado (sem simular, sem falha — RN-005); senão chama o service e devolve o resultado (prop/flash) para a etapa do wizard (08-13). NÃO bloqueia o fluxo.

    Rota: `POST solicitacoes/{solicitacao}/simular` (name solicitacoes.simular) — DEPOIS das literais.

    `SimulacaoSolicitacaoTest` (SQLite; seeds reais dos Quadros/risco + Geocoder/SpatialRepository fake como em ConsultaViabilidadeServiceTest):
    - test_simula_por_cnae_propagando_veredito (CA-01/RN-001): solicitação com 2 CNAEs + área + polígono → snapshot por CNAE com a tendência propagada do motor real; rules_versions presentes.
    - test_sem_zona_veredito_pendente (anti-fachada): sem zona → tendência pendente por CNAE, nunca permitido/não permitido inventado.
    - test_persiste_snapshot_e_simulated_at (RN-003): após simular, simulation_snapshot/rules_versions/resultado e simulated_at preenchidos.
    - test_nao_bloqueia (RN-002): a simulação retorna resultado mas não muda status nem impede edição/protocolo.
    - test_toggle_desligado_degrada (RN-005): features.simulacao_solicitacao=0 → degrada comunicado, sem simular, sem erro.
    - test_auditoria_da_simulacao (RN-002).
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=SimulacaoSolicitacaoTest</verify>
  <acceptance_criteria>
    - grep "class SimulacaoSolicitacaoService" e a iteração dos CNAEs chamando o motor por ponto.
    - grep "simulated_at" no service (persistência do snapshot).
    - grep "simulacao_solicitacao" no controller (toggle).
    - SimulacaoSolicitacaoTest verde (propaga veredito, pendente sem zona, persiste snapshot, não bloqueia, toggle, auditoria).
  </acceptance_criteria>
  <done>Simulação reusa o motor real (propaga veredito), persiste snapshot/versões/simulated_at, não bloqueia, toggle degrada sem falha; sem zona → pendente honesto.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="ConsultaViabilidadeServiceTest|SimulacaoSolicitacaoTest"` verde (sem regressão da Fase 7).
- `php artisan test --compact` — suíte completa CONTINUA verde.
</verification>

<success_criteria>
- Simulação com os motores reais da Fase 7 (RN-001), por ponto da própria solicitação (sem re-geocodificar), propagando o veredito; persiste snapshot/versões/simulated_at (RN-003); não bloqueia (RN-002); toggle degrada (RN-005).
- Sem zona → pendente propagado; nenhuma lógica paralela; sem dependência nova.
</success_criteria>

<output>
Criar `.planning/phases/08-solicitacao-de-viabilidade/08-09-SUMMARY.md`: a assinatura da entrada pública por ponto+CNAE adicionada ao ConsultaViabilidadeService, o contrato do snapshot persistido (chaves), a rota de simular e o comportamento do toggle. Insumo direto de 08-10 (lê snapshot, applicant_proceeded_despite) e 08-13 (UI da etapa de simulação reusa ResultadoViabilidade).
</output>

---
phase: 08-solicitacao-de-viabilidade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database/seeders/ParameterSeeder.php
  - config/sile.php
  - database/seeders/RolesAndPermissionsSeeder.php
  - tests/Feature/Seeders/ParameterSeederTest.php
  - tests/Feature/Seeders/DatabaseSeederTest.php
  - tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
autonomous: true
requirements: [HU-014, HU-061, HU-148, HU-150]

must_haves:
  truths:
    - "Os 13 parâmetros administráveis da Fase 8 entram no catálogo (toggles, protocolo, anexos, disk, tolerância área×polígono, prazo estimado, expiração de atendimento, throttle da consulta de protocolo) com fallback espelhado em config/sile.php"
    - "Nasce o grupo de parâmetros 'solicitacao'; a asserção da lista de grupos dos testes de seeder é atualizada (inclui 'solicitacao')"
    - "O catálogo cresce de 35 para 48 e os DOIS testes de seeder (ParameterSeeder/DatabaseSeeder) travam a nova contagem"
    - "As 5 permissões nomeadas da fase entram aditivas (registrar-contingencia, atendimento-presencial, consultar-solicitacoes, manter-tipos-servico, manter-requisitos-documentais) com atribuição por papel, e o teste de permissões trava a nova contagem (14→19)"
    - "Nenhuma dependência nova; o admin pode mudar todos esses valores sem deploy (Settings::get banco→cache→config)"
  artifacts:
    - path: "database/seeders/ParameterSeeder.php"
      provides: "13 parâmetros novos da Fase 8 (grupo solicitacao + features + seguranca)"
      contains: "solicitacao.protocolo.prefixo"
    - path: "database/seeders/RolesAndPermissionsSeeder.php"
      provides: "5 permissões aditivas da Fase 8 com atribuição por papel"
      contains: "registrar-contingencia"
  key_links:
    - from: "database/seeders/ParameterSeeder.php"
      to: "config/sile.php"
      via: "cada parâmetro tem fallback espelhado (Settings::get lê config sile.{key})"
      pattern: "solicitacao"
    - from: "database/seeders/ParameterSeeder.php"
      to: "tests/Feature/Seeders/ParameterSeederTest.php"
      via: "catálogo 35→48 + grupo 'solicitacao' na lista de grupos"
      pattern: "assertSame(48"
---

<objective>
Fundação de **parametrização e permissões** da Fase 8 (HU-014, aditivo): registrar os 13 parâmetros administráveis que governam a solicitação (toggles, formato de protocolo, anexos, disk de documentos, tolerância área×polígono, prazo estimado, expiração de atendimento, throttle da consulta pública de protocolo), espelhar os fallbacks em `config/sile.php`, e adicionar as 5 permissões nomeadas dos canais de backoffice/cadastros — tudo sem dependência nova.

Purpose: pela disciplina do projeto, nenhum valor de negócio fica hardcoded e nenhuma funcionalidade acoplável nasce sem toggle administrável; toda rota pública nasce com throttle parametrizado. Este plano é o irmão paralelo do 08-01 (não há sobreposição de arquivos) e destrava a parametrização consumida pelos planos seguintes.
Output: catálogo 35→48 (novo grupo 'solicitacao'), fallbacks em config, 5 permissões (14→19) com atribuição por papel, e os três testes de seeder/permissões atualizados.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-solicitacao-de-viabilidade/08-CONTEXT.md
@docs/superpowers/specs/2026-06-14-solicitacao-viabilidade-design.md

# PADRÃO A ESPELHAR (parâmetro + fallback + permissão aditiva + asserções de contagem) — ler antes de codar:
@database/seeders/ParameterSeeder.php
@config/sile.php
@app/Support/Settings.php
@database/seeders/RolesAndPermissionsSeeder.php
@tests/Feature/Seeders/ParameterSeederTest.php
@tests/Feature/Seeders/DatabaseSeederTest.php
@tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: 13 parâmetros novos + fallbacks em config/sile.php (novo grupo 'solicitacao')</name>
  <read_first>database/seeders/ParameterSeeder.php, config/sile.php</read_first>
  <files>database/seeders/ParameterSeeder.php, config/sile.php</files>
  <action>
    Acrescentar ao array `catalog()` do ParameterSeeder (manter ordem/estilo; NÃO remover nada). Defaults honestos (pendências SEDUR ficam com ressalva no description):

    Grupo 'features':
    - `features.solicitacao_viabilidade`: boolean, '1', ['required','boolean'], 'Habilita a solicitação de viabilidade no portal do cidadão'.
    - `features.simulacao_solicitacao`: boolean, '1', ['required','boolean'], 'Habilita a simulação de viabilidade dentro do formulário de solicitação (HU-141; orientativa, não bloqueia)'.

    Grupo 'solicitacao' (NOVO grupo):
    - `solicitacao.cnaes_complementares.max`: integer, '99', ['required','integer','min:1','max:99'], 'Máximo de CNAEs complementares por solicitação'.
    - `solicitacao.protocolo.prefixo`: string, 'VIA', ['required','string','max:10'], 'Prefixo do número de protocolo da viabilidade ({prefixo}-AAAA-NNNNNN) — formato oficial a confirmar SEDUR'.
    - `solicitacao.protocolo.padding`: integer, '6', ['required','integer','min:4','max:10'], 'Quantidade de dígitos da sequência do número de protocolo'.
    - `solicitacao.consulta_publica.assinatura_ttl_dias`: integer, '30', ['required','integer','min:1','max:365'], 'Validade (dias) do link assinado de consulta pública de protocolo (HU-069)'.
    - `solicitacao.anexos.max_mb`: integer, '10', ['required','integer','min:1','max:100'], 'Tamanho máximo (MB) por documento anexado à solicitação'.
    - `solicitacao.anexos.mime_permitidos`: json, '["application/pdf","image/jpeg","image/png"]', ['required','json'], 'Tipos de arquivo aceitos no anexo de documentos da solicitação'.
    - `storage.documentos.disk`: string, 'local', ['required','string','max:50'], 'Disk de armazenamento dos documentos da solicitação (local, s3...) — nunca disk público'.
    - `solicitacao.area_poligono.tolerancia_percentual`: integer, '10', ['required','integer','min:0','max:100'], 'Tolerância (%) entre a área declarada e a área do polígono antes de alertar (HU-063 RN-004; alerta, não bloqueia)'.
    - `solicitacao.prazo_estimado_dias`: integer, '30', ['required','integer','min:1','max:365'], 'Prazo estimado (dias) exibido na consulta de protocolo — ressalva de estimativa; medição real depende da HU-129/Fase 15'.
    - `solicitacao.atendimento.expiracao_minutos`: integer, '30', ['required','integer','min:5','max:240'], 'Expiração (minutos) do vínculo de atendimento presencial assistido (HU-150)'.

    Grupo 'seguranca':
    - `seguranca.throttle.consulta_protocolo.por_minuto`: integer, '30', ['required','integer','min:1','max:300'], 'Limite de consultas públicas de protocolo por minuto por IP/assinatura (HU-069)'.

    `config/sile.php` — espelhar os fallbacks (Settings::get lê config("sile.{key}")):
    - bloco `features`: acrescentar `'solicitacao_viabilidade' => true`, `'simulacao_solicitacao' => true`.
    - NOVO bloco `'solicitacao' => ['cnaes_complementares' => ['max' => 99], 'protocolo' => ['prefixo' => 'VIA', 'padding' => 6], 'consulta_publica' => ['assinatura_ttl_dias' => 30], 'anexos' => ['max_mb' => 10, 'mime_permitidos' => ['application/pdf','image/jpeg','image/png']], 'area_poligono' => ['tolerancia_percentual' => 10], 'prazo_estimado_dias' => 30, 'atendimento' => ['expiracao_minutos' => 30]]`.
    - NOVO bloco `'storage' => ['documentos' => ['disk' => 'local']]`.
    - bloco `seguranca.throttle`: acrescentar `'consulta_protocolo' => ['por_minuto' => 30]`.
  </action>
  <verify>vendor/bin/pint --dirty --format agent && php artisan config:show sile.solicitacao.protocolo.prefixo</verify>
  <acceptance_criteria>
    - grep "solicitacao.protocolo.prefixo" database/seeders/ParameterSeeder.php
    - grep "storage.documentos.disk" database/seeders/ParameterSeeder.php
    - grep "'solicitacao' =>" config/sile.php e grep "'storage' =>" config/sile.php
    - `php artisan config:show sile.storage.documentos.disk` retorna 'local'.
  </acceptance_criteria>
  <done>13 parâmetros no catálogo (grupo novo 'solicitacao') + fallbacks espelhados em config; efeito sem deploy via Settings.</done>
</task>

<task type="auto">
  <name>Task 2: travar contagem 35→48 e o grupo 'solicitacao' nos testes de seeder</name>
  <read_first>tests/Feature/Seeders/ParameterSeederTest.php, tests/Feature/Seeders/DatabaseSeederTest.php</read_first>
  <files>tests/Feature/Seeders/ParameterSeederTest.php, tests/Feature/Seeders/DatabaseSeederTest.php</files>
  <action>
    TDD (RED por contagem antes da Task 1, GREEN depois — confirmar ambos):
    1. `ParameterSeederTest`:
       - `test_seeder_cria_catalogo_completo`: trocar `assertSame(35, ...)` por `48`; e atualizar a lista de grupos para incluir 'solicitacao' e 'storage' se você usar grupo 'storage'. NOTA: o plano usa grupo 'solicitacao' para todos os solicitacao.* e o key 'storage.documentos.disk' fica no grupo 'solicitacao' (key ≠ group é a norma — security.* é grupo 'seguranca'). Portanto a lista nova é `['features', 'geo', 'integracoes', 'louos', 'retencao', 'risco', 'seguranca', 'solicitacao', 'ui']` (acrescenta SÓ 'solicitacao'). Se preferir grupo 'storage' dedicado, ajuste a lista para incluí-lo — mas mantenha a decisão consistente com a Task 1.
       - `test_seeder_e_idempotente`: trocar `assertSame(35, ...)` por `48`.
       - adicionar `test_seeder_registra_parametros_da_solicitacao_de_viabilidade`: asserir o toggle features.solicitacao_viabilidade (group features, boolean, '1'), solicitacao.protocolo.prefixo (group solicitacao, string, 'VIA'), solicitacao.anexos.mime_permitidos (group solicitacao, json, typedValue() === ['application/pdf','image/jpeg','image/png']), storage.documentos.disk (group solicitacao, 'local'), seguranca.throttle.consulta_protocolo.por_minuto (group seguranca, integer, '30').
    2. `DatabaseSeederTest`: trocar `assertSame(35, Parameter::query()->count())` por `48` nos DOIS pontos (test_seed_completo_prepara_ambiente_de_desenvolvimento e test_seed_e_idempotente).
  </action>
  <verify>php artisan test --compact --filter="ParameterSeederTest|DatabaseSeederTest"</verify>
  <acceptance_criteria>
    - grep "assertSame(48" tests/Feature/Seeders/ParameterSeederTest.php (2 ocorrências) e em DatabaseSeederTest.php (2 ocorrências).
    - grep "'solicitacao'" na lista de grupos do ParameterSeederTest.
    - grep "test_seeder_registra_parametros_da_solicitacao_de_viabilidade".
    - ParameterSeederTest e DatabaseSeederTest verdes.
  </acceptance_criteria>
  <done>Contagem 48 travada nos dois seeders; grupo 'solicitacao' na lista; caso dedicado verde.</done>
</task>

<task type="auto">
  <name>Task 3: 5 permissões aditivas da fase + atribuição por papel + contagem 14→19</name>
  <read_first>database/seeders/RolesAndPermissionsSeeder.php, tests/Feature/Authorization/RolesAndPermissionsSeederTest.php</read_first>
  <files>database/seeders/RolesAndPermissionsSeeder.php, tests/Feature/Authorization/RolesAndPermissionsSeederTest.php</files>
  <action>
    TDD. No RolesAndPermissionsSeeder (ADITIVO — givePermissionTo, nunca sync), acrescentar ao array `$permissions`: 'registrar-contingencia', 'atendimento-presencial', 'consultar-solicitacoes', 'manter-tipos-servico', 'manter-requisitos-documentais'.

    Atribuições (espelhar o padrão risco/louos):
    - `administrador`: as 5 (parametriza tudo).
    - `gestor`: 'registrar-contingencia', 'atendimento-presencial', 'consultar-solicitacoes', 'manter-tipos-servico', 'manter-requisitos-documentais' (opera e parametriza os cadastros da fase).
    - `analista`: 'consultar-solicitacoes' (consulta processos no backoffice).
    - `cidadao`: NENHUMA (o cidadão opera as próprias solicitações via policy, não por permissão nomeada).

    `RolesAndPermissionsSeederTest`:
    - acrescentar as 5 permissões à lista de `test_seeder_cria_papeis_e_permissoes_da_fase`.
    - adicionar `test_papeis_recebem_permissoes_de_solicitacao`: admin tem as 5; gestor tem registrar-contingencia/atendimento-presencial/consultar-solicitacoes/manter-tipos-servico/manter-requisitos-documentais; analista tem só consultar-solicitacoes (e NÃO registrar-contingencia); cidadao NÃO tem consultar-solicitacoes.
    - `test_seeder_e_idempotente`: trocar `assertSame(14, Permission::query()->count())` por `19`.
  </action>
  <verify>php artisan test --compact --filter=RolesAndPermissionsSeederTest</verify>
  <acceptance_criteria>
    - grep "registrar-contingencia" e "manter-requisitos-documentais" em RolesAndPermissionsSeeder.php
    - grep "assertSame(19" tests/Feature/Authorization/RolesAndPermissionsSeederTest.php
    - grep "test_papeis_recebem_permissoes_de_solicitacao"
    - RolesAndPermissionsSeederTest verde (lista, atribuições por papel, idempotência 19).
  </acceptance_criteria>
  <done>5 permissões aditivas com atribuição por papel; contagem 19 travada; cidadão opera por policy (sem permissão nomeada).</done>
</task>

</tasks>

<verification>
- `vendor/bin/pint --dirty --format agent` sem pendências.
- `php artisan test --compact --filter="ParameterSeederTest|DatabaseSeederTest|RolesAndPermissionsSeederTest"` verde.
- `php artisan test --compact` — suíte completa CONTINUA verde (só foram acrescidos parâmetros e permissões; nada removido).
</verification>

<success_criteria>
- 13 parâmetros novos (grupo 'solicitacao' + features + seguranca) com fallback em config; catálogo 48 travado nos dois seeders.
- 5 permissões aditivas com atribuição por papel; contagem 19 travada; cidadão por policy.
- ZERO dependência nova; tudo administrável sem deploy.
</success_criteria>

<output>
Criar `.planning/phases/08-solicitacao-de-viabilidade/08-02-SUMMARY.md`: lista EXATA dos 13 parâmetros (chave/grupo/tipo/default) e das 5 permissões com atribuições, a nova contagem (48 parâmetros, 19 permissões) e a decisão do grupo 'solicitacao'. Insumo direto dos planos que leem Settings (08-01 ProtocolNumberGenerator, 08-06 tolerância, 08-08 anexos/disk, 08-09 toggle simulação, 08-11 ttl/throttle/prazo, 08-15 expiração) e que usam as permissões (08-03/04/14/15).
</output>
